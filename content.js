/*!
 * PureKick — ad blocker and chat toolkit for Kick.com
 * Copyright (c) 2026 CodeBB. All rights reserved.
 *
 * SPDX-License-Identifier: LicenseRef-PureKick-Proprietary
 *
 * Proprietary software. Copying, modification, distribution, reverse
 * engineering and republication are prohibited without written permission.
 * See LICENSE. Removing this notice is a separate violation under
 * 17 U.S.C. § 1202.
 *
 * Contact: info@codebb.co
 */
/*
 * content.js  —  izole (isolated) dünya, document_start.
 *
 * 1) Kick üst header'ına doğal uyan PureKick butonu; tıklayınca Kick'in profil
 *    menüsüyle AYNI tarzda sade bir kart açar (rozet-logo + başlık + açıklama +
 *    durum + tek "duraklat/başlat" butonu). Aktifken hepsi aktif, pasifken hepsi
 *    pasif. Shadow DOM ile izole; Kick'in yapısını bozmaz.
 *    (Sürüm notları / Yardım yönetim panelindedir; popup yalnız aç/kapa.)
 * 2) DOM banner/overlay reklamlarını gizler.
 * 3) MAIN dünyadan gelen sinyalleri arka plana iletir (istatistik).
 * 4) Ayarları MAIN dünyaya/worker'a aktarır.
 */
(function () {
  'use strict';

  /* ══════════════════════════════════════════════════════════════════════
   * ERKEN PERDE — kenar çubuğu ilk boyamada örtülür
   *
   * Kick takip listesini 10 satırda başlatıyor, biz açarak büyütüyoruz.
   * Perde ilk tura kadar inmezse arada Kick'in kendi kısa listesi boyanıyor
   * ve göz çarpıyor. Bu blok `document_start`'ta, kap DOM'a girmeden önce
   * çalışıyor.
   *
   * Kap Kick'in KENDİ yapısından bulunuyor: o anda hiçbir işaretimiz yok.
   * Ölçüldü — seçici yalnız takip kabını buluyor, Önerilen'e dokunmuyor.
   * ══════════════════════════════════════════════════════════════════════ */
  var PK_HAZIR = 'data-pk-kenar-hazir';
  /* 6000 idi. Emniyet tavanı bu kadar uzun olunca, perdeyi indirmesi gereken
     tur herhangi bir sebeple çalışmazsa kenar çubuğu 6 SANİYE boş duruyordu
     (kullanıcı bildirdi: "geç yükleniyor, bazen hiç yüklenmiyor"). Perdenin
     işi zaten ilk turdaki titremeyi gizlemek; 2.5 sn'de inmediyse gizleyecek
     bir şey kalmamıştır ve boş kenar çubuğu maliyeti faydasını geçer. */
  var PK_ERKEN_TAVAN = 2500;
  /* `sabitKanal` ayarının localStorage aynası. document_start'ta
     chrome.storage okunamıyor (asenkron), localStorage senkron okunuyor —
     perdenin çizilip çizilmeyeceğine ilk boyamadan önce karar verebilmek
     için tek yol bu. Yalnız tek bir 0/1 tutuyor, kullanıcı verisi değil. */
  function pkKenarAynala(s) {
    try { localStorage.setItem('pk-kenar', (s && s.sabitKanal === false) ? '0' : '1'); } catch (e) {}
  }
  function pkKenarHazir() {
    try { document.documentElement.setAttribute(PK_HAZIR, '1'); } catch (e) {}
  }
  /* NON_CHANNEL YUKARI TAŞINDI: pkErkenPerde'nin document_start'ta URL'den
     kanal olup olmadığını (scMevcutKanal) bilmesi gerekiyor. */
  var NON_CHANNEL = { '': 1, browse: 1, following: 1, 'followed-channels': 1, search: 1, drops: 1,
    category: 1, categories: 1, clips: 1, subscriptions: 1, messages: 1, dashboard: 1, settings: 1,
    'privacy-policy': 1, 'terms-of-service': 1, about: 1, help: 1, recommended: 1, trending: 1 };
  (function pkErkenPerde() {
    try {
      /* 10.12 — KANAL-DIŞINDA PERDE ÇEKME. Perde, sabit-kanal yeniden
         sıralaması titremesini gizlemek için; o iş artık yalnız kanal
         sayfasında koşuyor. Ana sayfada perde çekilirse tur onu kaldırmaz ve
         Kick'in takip/öneri listesi gizli kalır (bulunan hata). */
      if (!scMevcutKanal()) return;
      /* ÖZELLİK KAPALIYSA PERDE HİÇ ÇEKİLMEZ. Ayarlar chrome.storage'dan
         ASENKRON geliyor, document_start'ta henüz elimizde yok; bu yüzden
         `sabitKanal` değeri localStorage'a aynalanıyor (senkron okunur,
         sayfayla aynı köken). Ayna yoksa AÇIK varsayılır — ilk kurulumda
         perde görevini yapsın. Bkz. pkKenarAynala(). */
      try { if (localStorage.getItem('pk-kenar') === '0') return; } catch (e) {}
      /* Kap iki yoldan bulunuyor:
           a) gerçek satırlardan — liste geldiyse
           b) "daha fazla göster" düğmesinden — Kick'in İSKELET aşamasında
              satır yok ama düğme var; (a) tek başına o aşamada tutmuyordu ve
              Kick'in başlığı ile "Daha Fazla Göster" görünüyordu.
         Ölçüldü: (b) tam olarak takip bölümünü buluyor, Önerilen'e dokunmuyor. */
      var kapA = ':has(> button > a[data-testid^="sidebar-following-channel-"])';
      var kapB = 'section:has(+ div > [data-testid="sidebar-show-more-following"])';
      var kutuB = 'div:has(> [data-testid="sidebar-show-more-following"])';
      /* İÇERİKTEN BAĞIMSIZ: bölüm daha boşken bile tutar. Kick'in kendi
         sınıf birleşimi; ölçüldü, yalnız iki kenar çubuğu bölümünü buluyor. */
      var kapC = 'section.flex.w-full.flex-col.p-3';
      /* Kutu, düğme kimliği olmadan da basılabiliyor; sınıfından da yakala.
         Ölçüldü: yalnız iki "daha fazla" kutusunu buluyor. */
      var kutuC = 'div.mx-3.flex.items-center.justify-between';
      var ort = 'html:not([' + PK_HAZIR + ']) ';
      var st = document.createElement('style');
      st.setAttribute('data-pk-erken-perde', '1');
      st.textContent =
        ort + kapA + '>*:not(.pk-iskelet){display:none}' +
        ort + kapB + '>*:not(.pk-iskelet){display:none}' +
        /* "Daha Fazla Göster" kutusu da yüklenirken görünmesin. */
        ort + kutuB + '{display:none}' +
        ort + kapC + '>*:not(.pk-iskelet){display:none}' +
        ort + kutuC + '{display:none}' +
        /* Yer tutuluyor: kap sıfıra çöküp sonra açılsaydı düzeltmeye
           çalıştığımız sıçramanın aynısı olurdu. 8 satır x 44px. */
        ort + kapA + '{min-height:352px}' +
        ort + kapB + '{min-height:352px}';
      (document.head || document.documentElement).appendChild(st);
      /* EMNİYET: ne olursa olsun kalkar. */
      setTimeout(pkKenarHazir, PK_ERKEN_TAVAN);
    } catch (e) { pkKenarHazir(); }
  })();

  // blockVodAds: GEÇMİŞ YAYIN (VOD) reklamı — canlıdan farklı mekanizma (SSAI), ayrı anahtar
  var DEFAULTS = {
    aiMod: true, baglantiDurum: true, bahsedilme: true, bahsetRozet: true,
    bahsetSes: false, bahsetSesTon: 'cinlama', bahsetSesSeviye: 60,
    bahsetTikla: true, banDetay: true,
    banSablon: false, bindir: false, bindirBoy: 55, bindirKonum: 'sol-alt', bindirSaydam: 90,
    blockDom: true, blockVideoAds: true, blockVodAds: true, botYoksay: true, consented: true,
  /* KAPALI BAŞLIYOR (opt-in): hesabın adına Kick'e istek atan tek özellik bu.
     Kullanıcı bilmeden ödül talep etmek sürpriz olurdu; açan bilerek açsın. */
  otoClaim: false,
  /* Kart genişletme AÇIK gelir: eklediğimiz bloklar Kick'in dar kartında
     sıkışıyor. Dar hâlini isteyen kapatabilir. */
  kartGenis: true,
  /* KAPALI BAŞLIYOR: geri alması zor, çok kanallı bir işlem. Açan bilerek açsın. */
  crossBan: false,
  /* KAPALI BAŞLIYOR: açık kaldığında sen bakmadan da susturuyor. */
  topluTo: false,
  topluSil: false,
  /* KAPALI BAŞLIYOR: senin adına sohbete mesaj YAZIYOR. Sessizce açık
     kalması kimsenin beklemediği bir şey. */
  otoMesaj: false,
    ekranGoruntusu: true, emoteOniz: true, enabled: true, gizleIkonu: false, gorselBuyut: true,
    hizliMod: false, istatistik: true, kartBilgi: true, kartSagTik: false,
    kartTakipci: true, kartMesajlar: true, kategoriGizle: true, kisiGizle: true,
    mentionAd: '', mesajEylem: true, modGunluk: true, modSerit: true, modSohbette: true, notlar: true,
    kelimeVurgu: true, oncelik: true, onizleme: true, otoKalite: '1080', otoSes: 'oto', otoTiyatro: false, rolVurgu: true,
    hakkindaEk: true, kenarGrup: true, kuralKabul: false, sabitKanal: true, sagTik: false, scEtiket: true, scFiltreCubugu: true, scSayac: true, sekmeIci: true, showBadge: true, silinenGoster: true, ssBirincil: 'indir',
    ssIkincil: 'pano', tamEkranSon: true, temizlemeKoru: false, tiyatroNav: false, uyarHiz: true,
    videoIstat: true, yanSohbet: true,
    /* 10.16 Yenilikleri */
    yayinBildirim: true, sohbetFiltre: true, klipSure: 30, klipFormat: 'webm',
    sohbetTts: false, ttsSes: 'oto', ttsHiz: 1, ttsSesSeviye: 80, ttsYalnizcaBahsetme: true,
    emoteSpamFiltre: true, izlemeSureTakip: true, kanalNotlari: true, hizliModGoster: true,
    cokluYayin: true,
    /* 10.17 Gelişmiş Özellikler */
    pipMod: true, sesYukseltici: 100, videoParlaklik: 100, videoKontrast: 100, videoDoygunluk: 100,
    oledMod: false, sohbetSekmeler: true, sohbetDondur: true, guvenliLinkKalkan: true,
    sohbetFontBoyut: 'orta', sohbetFontFamily: 'varsayilan',
    sohbetCeviri: true, ceviriDili: (typeof navigator !== 'undefined' && navigator.language ? navigator.language.slice(0, 2).toLowerCase() : 'tr'), otomatikCevir: false,
    kaynakDil: 'auto', ceviriModu: 'oto',
    /* 10.18 Mega Yenilikler */
    anaSayfaOtoDurdur: false, kenarCevrimdisiGizle: false, videoTiklaDurdur: true,
    ssKisakol: 'Alt+S', klipKisakol: 'Alt+K', sadeceSesModu: false, canliAltyazi: false, sesliDublaj: false,
    altyaziKaynakDil: 'auto', altyaziHedefDil: (typeof navigator !== 'undefined' && navigator.language ? navigator.language.slice(0, 2).toLowerCase() : 'tr'),
    altyaziFontBoyut: 16, altyaziRenk: '#facc15', altyaziKalinlik: '700', altyaziBgOpaklik: 75, altyaziKonum: 'alt',
    holoKartEfekt: 'pikachu', kartTipi: 'koleksiyon', koleksiyonKartDosya: 'card_1.jpg', ozelKartUrl: '', profilKartBase64: '',
    aydinlikTema: false, emoteBoyut: 36,
    videoZoom: true, hypeOlcer: true, aiYayinOzet: true
  };   // 10.18 // background.js ile tutarli — onay ekrani kaldirildi, kurulunca otomatik aktif
  var settings = Object.assign({}, DEFAULTS);

  // MAIN-world hook ile aramızdaki köprüyü doğrulamak için oturuma özel rastgele jeton.
  // Sayfadaki başka bir script (source:'kab') mesajını taklit edip engellemeyi kapatamaz / istatistik şişiremez.
  var pkNonce = (function () {
    try { return (self.crypto && crypto.randomUUID) ? crypto.randomUUID() : (Date.now().toString(36) + Math.random().toString(36).slice(2)); }
    catch (e) { return String(Date.now()) + Math.random(); }
  })();

  /* i18n — tarayıcı diline göre çeviri (desteklenmeyen dil İngilizce'ye düşer).
     $1/$2 yer tutucularını KENDİMİZ dolduruyoruz, chrome.i18n'in substitutions
     parametresine bırakmıyoruz: o parametre tarayıcıya göre `placeholders`
     bloğu ister ve sayı geçilirse davranış değişir. Burada kural tek: metni
     sözlükten al, $n'leri sırayla doldur. Anahtar yoksa anahtarın kendisi döner
     (ekranda boşluk değil, ne olduğu belli bir metin görünsün). */
  /* YER TUTUCU: sözlükte `$1` DEĞİL `{1}` kullanılır.
     Sebep: `chrome.i18n.getMessage` `$n` işaretlerini kendisi işliyor ve
     karşılık verilmediğinde siliyor — bizim değiştirmemize sıra gelmeden
     metin boşalıyordu (canlıda ölçüldü: "  , kullanıcısını  süreyle
     susturdu"). `{n}` işaretine tarayıcı dokunmuyor, tek yetkili biziz. */
  function t(k, sub) {
    var m;
    try { m = chrome.i18n && chrome.i18n.getMessage(k); } catch (e) {}
    if (!m) {
      /* BAĞLAM ÖLDÜYSE HAM ANAHTAR BASMA.
         Eklenti güncellenince/yeniden yüklenince açık sekmedeki eski betiğin
         `chrome.*` erişimi kopar; `getMessage` boş döner. Eskiden burada
         anahtarın kendisi dönüyordu ve Kick'in sohbetine
         "pkModSohbetTemizlendi" gibi hata ayıklama metni basılıyordu
         (kullanıcı ekran görüntüsüyle bildirdi).
         Artık boş dönüyoruz; tur döngüsü zaten pkKendiniTopla ile
         düğümlerimizi toplayacak. Anahtar GERÇEKTEN eksikse (geliştirme)
         eskisi gibi anahtar dönsün — orada görünür olması işe yarıyor. */
      if (!pkBaglamCanli()) return '';
      return k;
    }
    if (sub == null) return m;
    var a = Array.isArray(sub) ? sub : [sub];
    return m.replace(/\{(\d)\}/g, function (_, i) { return a[i - 1] != null ? a[i - 1] : ''; });
  }

  // Kick API'sinden gelen metinler üçüncü taraf verisi → HTML'e basmadan önce kaçış.
  var ESC = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' };
  function esc(s) { return String(s == null ? '' : s).replace(/[&<>"']/g, function (c) { return ESC[c]; }); }

  // Anlık yerel önbellek okuma (gecikmesiz ilk yükleme)
  try {
    var cached = localStorage.getItem('pk_settings_cache');
    if (cached) {
      var cObj = JSON.parse(cached);
      if (cObj && typeof cObj === 'object') Object.assign(settings, cObj);
    }
  } catch (e) {}

  function sendBg(msg) {
    if (msg && msg.type === 'setSettings' && msg.payload) {
      Object.assign(settings, msg.payload);
      try { localStorage.setItem('pk_settings_cache', JSON.stringify(settings)); } catch (e) {}
      try {
        var sSync = Object.assign({}, settings);
        if (sSync.profilKartBase64 && sSync.profilKartBase64.length > 8000) delete sSync.profilKartBase64;
        chrome.storage.sync.set({ settings: sSync }).catch(function () {});
      } catch (e) {}
      try { chrome.storage.local.set({ settings: settings }); } catch (e) {}
    }
    return new Promise(function (resolve) {
      try { chrome.runtime.sendMessage(msg, function (r) { void chrome.runtime.lastError; resolve(r || {}); }); }
      catch (e) { resolve({}); }
    });
  }
  function reportStat(kind, count) { sendBg({ type: 'stat', kind: kind, count: count }); }

  function isActive() { return settings.consented && settings.enabled; } // onaysız → pasif
  function pushConfigToPage() {
    var active = isActive();
    try { window.postMessage({ source: 'kab-cfg', n: pkNonce, settings: { enabled: active, blockVideoAds: settings.blockVideoAds, blockVodAds: settings.blockVodAds } }, '*'); } catch (e) {}
    try { localStorage.setItem('__kab_video', (active && settings.blockVideoAds) ? '1' : '0'); } catch (e) {}
    // Ayrı bayrak: worker-hook document_start'ta config mesajını beklemeden okuyabilsin
    try { localStorage.setItem('__kab_vod', (active && settings.blockVodAds !== false) ? '1' : '0'); } catch (e) {}
  }

  /* ==================== HEADER BUTONU + KICK-TARZI KART ==================== */
  // PureKick "PK" monogram logosu (blok/keskin, Kick diline uygun) — fill=currentColor: butonda beyaz, yeşil vs. otomatik
  var SHIELD = '<svg viewBox="88 43 356 323" fill="currentColor"><path fill-rule="evenodd" d="M88 43 H255 V232 H145 V366 H88 Z M151 98 H190 A8 8 0 0 1 198 106 V169 A8 8 0 0 1 190 177 H151 A8 8 0 0 1 143 169 V106 A8 8 0 0 1 151 98 Z"/><rect x="278" y="43" width="57" height="323"/><rect x="88" y="300" width="247" height="66"/><rect x="389" y="43" width="55" height="105"/><rect x="389" y="261" width="55" height="105"/><path d="M334 156 L389 114 L444 147 L334 204 Z"/><path d="M334 253 L389 295 L444 262 L334 204 Z"/></svg>';
  var I_CHEV = '<svg class="ch" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18l6-6-6-6"/></svg>';
  // supporter badge (kucuk statik) — popup ile ayni gorunum, CSP'ye takilmaz
  var DROPS_URL = 'https://kick.com/drops/all-campaigns';
  var SITE_URL = 'https://purekick.pumpzera.cc/';   // "Sohbet rozeti kazan" CTA hedefi

  var CSS =
    ':host{all:initial}' +
    '*{box-sizing:border-box;margin:0;padding:0;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif}' +
    // Buton Kick'in yan ikonlarıyla birebir: 36x36, radius 4px, gri kutu #42474d, beyaz ikon (kullanıcı isteği — sağdaki ödül butonuyla aynı)
    '.btn{position:relative;width:36px;height:36px;border:none;border-radius:4px;cursor:pointer;display:flex;align-items:center;justify-content:center;background:#42474d;color:#fff;transition:.15s cubic-bezier(.4,0,.2,1)}' +
    '.btn:hover{background:#4e545b}.btn:active{transform:scale(.95)}.btn.open{background:#4e545b}.btn.off{color:#8b969d}.btn svg{width:16px;height:16px;display:block}' +
    '.count{position:absolute;top:-1px;right:-1px;min-width:16px;height:16px;padding:0 3px;border-radius:8px;background:#fa2d2d;color:#fff;font-size:10px;font-weight:800;display:none;align-items:center;justify-content:center;line-height:1;box-shadow:0 0 0 2px #17181c;pointer-events:none}' +
    '.count.show{display:flex}' +
    // menu — KICKs satın alma dropdown'u birebir: keskin köşeler (rounded-lg 8px), gri buton + yeşil yazı + açık gri çerçeve
    // Kick token'ları: surface-base #171a1c · surface-highest #232629 · overlay #2e3237 · primary #53fc18 · outline-region rgba(240,241,242,.4) · onSurfaceSecondary #9fa6ad
    '.menu{position:fixed;width:344px;max-width:calc(100vw - 20px);background:#171a1c;border:1px solid rgba(240,241,242,.08);border-radius:8px;box-shadow:0 12px 44px rgba(0,0,0,.66),0 2px 8px rgba(0,0,0,.5);color:#fff;display:none;z-index:2147483000;overflow:hidden}' +
    '.menu.show{display:block;animation:pk .13s ease-out}@keyframes pk{from{opacity:0;transform:translateY(-6px) scale(.985)}to{opacity:1;transform:none}}' +
    '.mhead{display:flex;align-items:center;gap:11px;padding:14px 14px 12px}' +
    '.micon{width:38px;height:38px;border-radius:8px;background:rgba(255,255,255,.12);display:flex;align-items:center;justify-content:center;color:#fff;flex:none}.micon svg{width:22px;height:22px}' +
    '.mttl{flex:1;min-width:0}' +
    '.mttl .name{font-size:16px;font-weight:800;line-height:1.15;color:#fff}' +
    '.mttl .desc{font-size:11.5px;color:#9fa6ad;margin-top:2px;line-height:1.4}' +
    '.mx{width:32px;height:32px;flex:none;border:none;background:transparent;color:#9fa6ad;border-radius:8px;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:.15s}' +
    '.mx:hover{background:rgba(240,241,242,.08);color:#fff}.mx svg{width:20px;height:20px}' +
    '.mbody{padding:0 14px 14px}' +
    // Bölüm ayracı (Kick "bg-outline-decorative h-px" ince çizgisi gibi)
    '.sep{height:1px;background:rgba(240,241,242,.08);margin:14px 0}' +
    // buton = KICKs fiyat butonu birebir: gri zemin #2e3237 + açık gri çerçeve (outline-region) + yeşil yazı (aktif), rounded-md 6px
    '.primary{width:100%;height:44px;cursor:pointer;border-radius:6px;font-size:13.5px;font-weight:800;background:#2e3237;border:1px solid rgba(240,241,242,.4);color:#53fc18;display:flex;align-items:center;justify-content:center;gap:9px;transition:.12s}' +
    '.primary:hover{background:#363b40;border-color:rgba(240,241,242,.55)}.primary:active{transform:scale(.99)}' +
    '.primary .pd{width:9px;height:9px;border-radius:50%;background:#53fc18;box-shadow:0 0 7px rgba(83,252,24,.6);flex:none}' +
    '.primary.off{color:#9fa6ad}' +
    '.primary.off:hover{background:#363b40}.primary.off .pd{background:#9fa6ad;box-shadow:none}' +
    // AKTİFken buton kapatma yapmaz → durum göstergesi gibi davransın (tıklanabilir görünmesin): imleç düz, hover yükselmesi yok.
    '.primary.locked{cursor:default}' +
    '.primary.locked:hover{background:#2e3237;border-color:rgba(240,241,242,.4)}' +
    '.primary.locked:active{transform:none}' +
    '.drops{display:block}' +
    // Aktif drop yoksa ortalanmış bildirim
    '.dempty{text-align:center;color:#9fa6ad;font-size:12px;font-weight:600;padding:6px 8px 2px}' +
    '.dhead{display:flex;align-items:center;gap:6px;font-size:10.5px;font-weight:800;text-transform:uppercase;letter-spacing:.4px;color:#9fa6ad;margin-bottom:8px}' +
    '.dhead .dn{background:rgba(83,252,24,.16);color:#53fc18;border-radius:6px;padding:0 5px;font-size:10px;letter-spacing:0}' +
    '.dlist{max-height:292px;overflow-y:auto;display:flex;flex-direction:column;gap:8px}' +
    '.dlist::-webkit-scrollbar{width:6px}.dlist::-webkit-scrollbar-thumb{background:rgba(255,255,255,.16);border-radius:3px}.dlist::-webkit-scrollbar-track{background:transparent}' +
    '.di{display:flex;align-items:center;gap:10px;padding:8px 10px;border-radius:8px;background:#232629;border:1px solid rgba(240,241,242,.06);text-decoration:none;color:#fff;transition:.15s}' +
    '.di:hover{background:#2e3237;border-color:rgba(240,241,242,.14)}' +
    '.di .img{width:36px;height:36px;border-radius:6px;object-fit:cover;flex:none;background:rgba(255,255,255,.06);display:flex;align-items:center;justify-content:center;font-size:18px;line-height:1}' +
    '.di .tx{flex:1;min-width:0;display:flex;flex-direction:column;gap:2px}' +
    '.di .l1{font-size:12px;font-weight:700;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}' +
    '.di .l2{font-size:10.5px;font-weight:600;color:#9fa6ad}' +
    '.di .ch{width:13px;height:13px;flex:none;color:#9fa6ad}' +
    // "Sohbet rozeti kazan" CTA — dikkat çekici gradyan (yeşil→camgöbeği) + ışık geçişi. Giriş yapınca gizlenir.
    '.cta{display:flex;align-items:center;gap:10px;padding:10px 12px;border-radius:8px;text-decoration:none;position:relative;overflow:hidden;background:linear-gradient(120deg,rgba(83,252,24,.16),rgba(0,229,255,.16));border:1px solid rgba(0,229,255,.35);transition:.15s}' +
    '.cta:hover{background:linear-gradient(120deg,rgba(83,252,24,.26),rgba(0,229,255,.26));border-color:rgba(0,229,255,.6)}' +
    '.cta::after{content:"";position:absolute;top:0;left:-60%;width:40%;height:100%;background:linear-gradient(100deg,transparent,rgba(255,255,255,.35),transparent);transform:skewX(-20deg);animation:ctaShine 3s ease-in-out infinite}' +
    '@keyframes ctaShine{0%,12%{left:-60%}62%,100%{left:150%}}' +
    '.cta-badge{width:30px;height:30px;border-radius:6px;display:block;flex:none;position:relative;z-index:1;filter:drop-shadow(0 0 6px rgba(83,252,24,.4))}' +
    '.cta-tx{flex:1;min-width:0;display:flex;flex-direction:column;justify-content:center;position:relative;z-index:1}' +
    '.cta-t1{font-size:13px;font-weight:800;color:#eef1f2;line-height:1.25}' +
    '.cta.hide{display:none}' +
    // Tema Rengi seçici (recolor.js motorunu besler)
    '.theme-lbl{font-size:11px;font-weight:700;color:#9fa6ad;text-transform:uppercase;letter-spacing:.4px;margin-bottom:9px}' +
    '.sw-row{display:flex;gap:7px;flex-wrap:wrap}' +
    '.sw{width:28px;height:28px;border-radius:7px;cursor:pointer;border:2px solid transparent;box-shadow:inset 0 0 0 1px rgba(255,255,255,.14);padding:0;transition:transform .1s;flex:none}' +
    '.sw:hover{transform:scale(1.12)}' +
    '.sw.on{border-color:#fff}' +
    '.sw-reset{background:#2e3237;display:flex;align-items:center;justify-content:center;color:#9fa6ad}' +
    '.sw-reset:hover{color:#eef1f2}' +
    '.sw-reset svg{width:15px;height:15px}';

  /* 10.2.1 TEMİZLİĞİ — eski açılır menü kaldırıldı.
     Buton artık doğrudan TAM PANELİ açıyor (`panelToggle`), menü hiç açılmıyordu:
     `toggleMenu` hiçbir yerden çağrılmıyordu (ölçüldü: 1 geçiş = yalnız tanımı).
     Yine de her sayfada kuruluyor, CTA/renk kutucukları/drops listesi görünmeyen
     bir yere çiziliyordu. Geriye yalnızca BUTON ve üstündeki bildirim rozeti kaldı. */
  var WIDGET_HTML =
    '<style>' + CSS + '</style>' +
    '<button class="btn" id="btn" title="' + t('btnTitle') + '">' + SHIELD + '<span class="count" id="badge"></span></button>';

  var host = null, sroot = null;
  var linked = false;   // kullanıcı purekick.pumpzera.cc'de giriş yaptı mı (pkLink) → CTA'yı gizle
  /* 10.15 — kullanıcının KENDİ abonelik durumu. heartbeat yanıtından geliyor
     (o uç nokta kick_id+token ile kimlik doğruluyor, kişiye özel veri dönebilir;
     api/badges.php herkese açık olduğu için oraya konamazdı). 6 saatte bir
     tazeleniyor — rozet önbelleğiyle aynı tempo, ekstra istek yok. */
  var abone = { abone: false, ay: 0, rozet: '', gecmis: false, bitis: '', ts: 0 };
  /* VARSAYILAN adres — gerçek adres SUNUCUDAN (heartbeat yanıtı) geliyor.
     Burası yalnızca yedek: heartbeat yalnız giriş yapmış kullanıcıya
     çalışıyor, giriş yapmamış kişi de düğmeyi görebilmeli.
     Böylece kampanya adresi değişirse config.php yetiyor, mağaza
     güncellemesi gerekmiyor. */
  /* Abonelik SİTEDEN yapılır; eklenti doğrudan Patreon'a göndermez. Ödemeyi
     kişiye ancak Kick hesabı bağlıyken eşleyebiliyoruz (users.patreon_user_id),
     ve bağlama adımı panelde. Doğrudan Patreon'a yollasaydık bağlamadan abone
     olan kullanıcı parasını öder ama rozetini hiç alamazdı. */
  function abonePanelAdresi() { return SITE_URL + 'dashboard.php'; }

  // ---- Tema Rengi: menüden renk seç → recolor.js Kick arayüzünü boyar (storage.local.theme) ----
  var THEME_PRESETS = ['#a855f7', '#3b82f6', '#06b6d4', '#ef4444', '#f97316', '#ec4899', '#eab308'];
  /* Tema adları artık anahtar; gösterirken t() ile çevriliyor. */
  var TEMA_ADLARI = { '#a855f7': 'pkTemaMor', '#3b82f6': 'pkTemaMavi', '#06b6d4': 'pkTemaTurkuaz', '#ef4444': 'pkTemaKirmizi',
                      '#f97316': 'pkTemaTuruncu', '#ec4899': 'pkTemaPembe', '#eab308': 'pkTemaSari', '#53fc18': 'pkTemaKickYesili' };
  /* Kick wordmark'ının yalnızca "KICK" harfleri (BETA yok). Yol verisi UYDURMA DEĞİL:
     Kick'in kendi logo SVG'si canlı sayfadan çekilip ayrıştırıldı (5 yoldan ilki
     harfler, kalan 4'ü "BETA" — onları almadık). Harfler 0→77.88 arasında bittiği
     için viewBox 78 genişliğe kırpıldı, yoksa sağda boşluk kalıyordu. */
  var KICK_YAZI_YOL = 'M0 0.0307312H8.34073V5.79423H11.1163V2.91248H13.8919V0.0307312H22.2326V8.69274H19.457V11.5745H16.6815V14.4562H19.457V17.338H22.2326V26H13.8919V23.1182H11.1163V20.2365H8.34073V26H0V0.0307312ZM55.6444 0.0307312H63.9852V5.79423H66.7608V2.91248H69.5363V0.0307312H77.8771V8.69274H75.1015V11.5745H72.3259V14.4562H75.1015V17.338H77.8771V26H69.5363V23.1182H66.7608V20.2365H63.9852V26H55.6444V0.0307312ZM25.039 0.0307312H33.3797V26H25.039V0.0307312ZM38.9462 0.0307312V2.91248H36.1706V23.1029H38.9462V25.9846H52.8535V17.3226H44.5128V8.66061H52.8535V0H38.9462V0.0307312Z';
  function kickLogoSvg(renk, en) {
    return '<svg viewBox="0 0 78 26" width="' + en + '" height="' + Math.round(en * 26 / 78) + '" ' +
      'fill="none" style="display:block"><path fill-rule="evenodd" clip-rule="evenodd" d="' +
      KICK_YAZI_YOL + '" fill="' + renk + '"/></svg>';
  }
  var themeColor = '#53fc18';   // '#53fc18' = varsayılan (recolor kapalı, Kick yeşili)
  function setTheme(color) {
    color = String(color || '').toLowerCase();
    if (!/^#[0-9a-f]{6}$/.test(color)) return;
    themeColor = color;
    try { sabitRenkUygula(); } catch (e) {}
    try { chrome.storage.local.set({ theme: { color: color, logo: true } }); } catch (e) {}
  }
  /* markSwatch/buildSwatches SİLİNDİ: menüdeki renk kutucuklarını çiziyorlardı.
     Tema seçimi artık panelde (bolumTema) — orası her açılışta yeniden çiziliyor,
     ayrı bir "seçiliyi işaretle" adımına gerek yok. */

  function navRightContainer() {
    var W = window.innerWidth;
    var els = [].slice.call(document.querySelectorAll('nav button, nav a, header button, header a'))
      .map(function (el) { return { el: el, r: el.getBoundingClientRect() }; })
      .filter(function (x) { return x.r.width > 0 && x.r.height > 0 && x.r.top >= 2 && x.r.bottom <= 64 && x.r.left > W - 360; })
      .sort(function (a, b) { return a.r.left - b.r.left; });
    if (!els.length) return null;
    var p = els[els.length - 1].el.parentElement;
    return (p && p.children && p.children.length >= 2) ? p : (p && p.parentElement) || p;
  }

  // Tek ana anahtar: aktifken hepsi aktif, pasifken hepsi pasif.
  function setMaster(on) {
    settings.enabled = on; settings.blockVideoAds = on; settings.blockVodAds = on; settings.blockDom = on; settings.showBadge = on;
    renderWidget();
    pushConfigToPage(); // __kab_video bayrağı reload öncesi güncellensin (worker-hook onu okur)
    // Ayar kaydedilince sayfayı yenile: video reklam engelleme (worker-hook) ancak
    // sayfa yeniden yüklenince devreye girer/çıkar; bu yüzden reload gerekli.
    sendBg({ type: 'setSettings', payload: { enabled: on, blockVideoAds: on, blockVodAds: on, blockDom: on, showBadge: on } })
      .then(function () { try { location.reload(); } catch (e) {} });
  }

  /* ══════════════════════════════════════════════════════════════════════
   * SADECE REKLAM ENGELLEYİCİ ÖN AYARI
   * ══════════════════════════════════════════════════════════════════════ */

  /* Bu modda AÇIK kalanlar. Geri kalan bütün boole özellikler kapanır. */
  var PK_REKLAM_AYARI = ['enabled', 'blockDom', 'blockVideoAds', 'blockVodAds', 'showBadge'];

  /* Ön ayarın tam hâli. Sabit liste yerine `settings` üzerinden hesaplanıyor:
     ileride eklenen bir özelliği listeye yazmayı unutsak bu modda sessizce
     açık kalırdı. Sayı/metin ayarları korunuyor — onların "kapalısı" yok. */
  function reklamModuYamasi() {
    var y = {};
    PK_REKLAM_AYARI.forEach(function (k) { y[k] = true; });
    Object.keys(settings).forEach(function (k) {
      if (k === 'consented') return;                     // özellik değil, onay durumu
      if (PK_REKLAM_AYARI.indexOf(k) >= 0) return;
      if (typeof settings[k] !== 'boolean') return;      // kalite, yazı boyu, konum...
      y[k] = false;
    });
    return y;
  }

  /* Ayrı bayrak TUTMUYORUZ: durum ayarlardan hesaplanıyor. Bayrak tutsaydık
     kullanıcı başka bir özelliği açınca bayrak ayarlarla çelişebilirdi. */
  function reklamModuMu() {
    var y = reklamModuYamasi();
    for (var k in y) if (settings[k] !== y[k]) return false;
    return true;
  }

  function reklamModuUygula() {
    var y = reklamModuYamasi();
    Object.keys(y).forEach(function (k) { settings[k] = y[k]; });
    renderWidget();
    pushConfigToPage();
    /* `setMaster` ile aynı sebeple yeniden yükleniyor: video reklam engelleme
       sayfa dünyasındaki kancaya bağlı ve o ancak yeniden yüklemede devreye
       giriyor. Ayrıca kapatılan özelliklerin sayfada bıraktığı arayüz de
       böyle temizleniyor. */
    sendBg({ type: 'setSettings', payload: y })
      .then(function () { try { location.reload(); } catch (e) {} });
  }

  // Kick üst menüsündeki "Günlük Ödül Al" (Claim Daily Reward) butonu — dil bağımsız.
  function claimBtn() {
    var btns = document.querySelectorAll('header button, nav button, button[aria-haspopup="dialog"]');
    for (var i = 0; i < btns.length; i++) {
      var b = btns[i], al = (b.getAttribute('aria-label') || '').toLowerCase();
      if (/reward|ödül|odul|belohnung|récompense|recompensa|награ/.test(al)) return b;
      try { if (b.querySelector('svg path[d^="M6 7.33301"]')) return b; } catch (e) {}
    }
    return null;
  }

  function buildWidget() {
    if (host && document.contains(host)) return true;
    var claim = claimBtn();
    var container = (claim && claim.parentElement) ? claim.parentElement : navRightContainer();
    if (!container) return false;
    host = document.createElement('div');
    host.id = 'purekick-widget';
    host.style.cssText = 'display:inline-flex;align-items:center';
    sroot = host.attachShadow({ mode: 'open' });
    sroot.innerHTML = WIDGET_HTML;
    // Claim butonunun SOLUNA yerleştir; bulunamazsa yedek olarak en sağa.
    if (claim && claim.parentElement === container) container.insertBefore(host, claim);
    else container.appendChild(host);

    var ustB = sroot.getElementById('btn');
    ustB.addEventListener('click', function (e) { e.stopPropagation(); pkIsaretKapat(); panelToggle(); });
    /* Üst çubuk düğmesi GÖLGE DOM içinde: `pkIsaretKapat` belgedeki
       düğümleri tarıyor, buraya ulaşamıyor — bu yüzden kendi temizliğini
       kendi yapıyor. */
    pkUstIsaret = function () {
      if (pkIsaretAcik) {
        ustB.style.setProperty('box-shadow', '0 0 0 2px rgb(83,252,24)');
        ustB.style.setProperty('border-radius', '4px');
      } else ustB.style.removeProperty('box-shadow');
    };
    pkUstIsaret();
    renderWidget();
    if (isActive()) loadDrops(false); // rozet için drop sayısını önceden yükle (önbellekli)
    return true;
  }

  /* positionMenu / toggleMenu / openMenu / closeMenu / renderCta SİLİNDİ —
     hepsi yalnızca açılmayan menüye hizmet ediyordu. */

  function renderWidget() {
    if (!sroot) return;
    // Buton pasifken soluklaşır; durum metni/CTA menüde olduğu için kalktı.
    sroot.getElementById('btn').classList.toggle('off', !settings.enabled);
    renderDrops();
  }

  /* ---------- Aktif Drop kampanyası satırı ---------- */
  var dropsData = null;
  var seenIds = [];   // görülmüş (menüde açılıp izlenmiş) kampanya id'leri — storage'da kalıcı

  // "3 gün kaldı" / "5 saat kaldı" (tekil-çoğul ayrı anahtarlarda)
  /* Bitiş tarihi, kullanıcının diline göre kısa biçimde (ör. "23 Ağu").
     Kalan süre tek başına "ne zaman bitiyor" sorusunu cevaplamıyordu. */
  function fmtEnd(endsAt) {
    var d = new Date(endsAt);
    if (isNaN(d.getTime())) return '';
    var dil;
    try { dil = chrome.i18n && chrome.i18n.getUILanguage && chrome.i18n.getUILanguage(); } catch (e) {}
    try { return d.toLocaleDateString(dil || undefined, { day: 'numeric', month: 'short' }); }
    catch (e) { return ''; }
  }

  function fmtLeft(endsAt) {
    var ms = Date.parse(endsAt) - Date.now();
    if (!isFinite(ms) || ms <= 0) return '';
    var days = Math.floor(ms / 86400000);
    if (days >= 1) return t(days === 1 ? 'dropDay' : 'dropDays', [String(days)]);
    var hours = Math.max(1, Math.floor(ms / 3600000));
    return t(hours === 1 ? 'dropHour' : 'dropHours', [String(hours)]);
  }

  // Butonun köşesindeki bildirim rozeti — yalnızca HENÜZ GÖRÜLMEMİŞ aktif drop sayısı.
  function unseenDrops() {
    var list = (isActive() && dropsData && dropsData.active) ? dropsData.active : [];
    return list.filter(function (d) { return d && d.id && seenIds.indexOf(d.id) < 0; });
  }
  function renderBadge() {
    if (!sroot) return;
    var b = sroot.getElementById('badge');
    if (!b) return;
    /* YALNIZ DROPS. Bahsedilme sayısı buradan ÇIKARILDI: aynı bildirim hem
       üst çubuktaki düğmede hem sohbet şeridindeki PK düğmesinde çıkıyordu.
       Bahsedilme sohbete ait bir olay, göstergesi de sohbetin yanında dursun
       (bkz. pkAyarNoktaTazele). */
    var n = unseenDrops().length;
    if (n > 0) { b.textContent = String(n); b.classList.add('show'); } // gerçek sayı (9+ kapağı yok)
    else { b.classList.remove('show'); }
  }

  // Menü açılıp droplar görüntülenince: o an aktif olan tüm kampanyaları "görüldü"
  // say → rozet kaybolur. Kalıcı olsun diye storage'a yaz (sayfa yenilense de gelmez).
  // Sadece aktifleri saklarız → biten kampanyalar listeden düşer, liste şişmez.
  // 10.2.1: eski menunun `open` bayragina bagli DEGIL — paneldeki Drops bolumu de cagirir,
  // yoksa buton paneli actigi icin bildirim hic temizlenmezdi.
  function markDropsSeen() {
    var active = (isActive() && dropsData && dropsData.active) ? dropsData.active : [];
    if (!active.length) return;
    var ids = active.map(function (d) { return d.id; }).filter(Boolean);
    // Zaten hepsi görülmüşse boşuna yazma.
    var changed = ids.length !== seenIds.length || ids.some(function (id) { return seenIds.indexOf(id) < 0; });
    if (!changed) return;
    seenIds = ids;
    try { chrome.storage.local.set({ seenDrops: seenIds }); } catch (e) {}
    renderBadge();
  }

  // Fare tekerleğinde pürüzsüz kaydırma (tak-tak yerine akıcı, easing'li).
  // Taşma yoksa veya sınırdaysa native davranışa bırakır (sayfa normal kayar).
  function attachSmoothScroll(el) {
    var target = el.scrollTop, raf = 0;
    function step() {
      var cur = el.scrollTop, diff = target - cur;
      if (Math.abs(diff) < 0.5) { el.scrollTop = target; raf = 0; return; }
      el.scrollTop = cur + diff * 0.2;
      raf = requestAnimationFrame(step);
    }
    el.addEventListener('wheel', function (e) {
      var max = el.scrollHeight - el.clientHeight;
      if (max <= 0) return;
      if ((el.scrollTop <= 0 && e.deltaY < 0) || (el.scrollTop >= max - 0.5 && e.deltaY > 0)) return; // sınır → sayfaya bırak
      e.preventDefault();
      var dy = e.deltaY * (e.deltaMode === 1 ? 16 : (e.deltaMode === 2 ? el.clientHeight : 1)); // satır/sayfa modunu px'e çevir
      if (!raf) target = el.scrollTop;                 // yeni jest → mevcut konumdan başla
      target = Math.max(0, Math.min(target + dy, max));
      if (!raf) raf = requestAnimationFrame(step);
    }, { passive: false });
  }

  function renderDrops() {
    if (!sroot) return;
    renderBadge();
    var wrap = sroot.getElementById('drops');
    if (!wrap) return;
    var list = (isActive() && dropsData && dropsData.active) ? dropsData.active : [];
    if (!list.length) {
      // Aktif drop yok → başlık + ortalanmış bildirim (bölüm daima görünür)
      wrap.innerHTML = '<div class="dhead">' + esc(t('dropsTitle')) + '</div>' +
        '<div class="dempty">' + esc(t('dropsEmpty')) + '</div>';
      return;
    }

    // Tüm aktif kampanyalar tek tek listelenir; liste taşarsa dikeyde kaydırılır.
    var rows = '';
    for (var i = 0; i < list.length; i++) {
      var c = list[i];
      var title = c.category || c.name || '';
      var left = fmtLeft(c.endsAt);
      var media = c.image
        ? '<img class="img" src="' + esc(c.image) + '" alt="" referrerpolicy="no-referrer">'
        : '<span class="img">🎁</span>';
      rows += '<a class="di" href="' + DROPS_URL + '" target="_blank" rel="noopener noreferrer" title="' + esc(t('dropTitle')) + '">' +
        media +
        '<span class="tx">' +
          '<span class="l1">' + esc(title) + '</span>' +
          (left ? '<span class="l2">' + esc(left) + '</span>' : '') +
        '</span>' + I_CHEV +
      '</a>';
    }
    wrap.innerHTML = '<div class="dhead">' + esc(t('dropsTitle')) + '<span class="dn">' + list.length + '</span></div>' +
      '<div class="dlist">' + rows + '</div>';
    wrap.classList.add('show');

    // Görsel yüklenemezse (CSP/ağ) o satırda 🎁 emoji rozetine düş.
    var imgs = wrap.querySelectorAll('img.img');
    for (var k = 0; k < imgs.length; k++) {
      (function (im) {
        im.addEventListener('error', function () {
          var s = document.createElement('span'); s.className = 'img'; s.textContent = '🎁';
          if (im.parentNode) im.parentNode.replaceChild(s, im);
        }, { once: true });
      })(imgs[k]);
    }

    var dl = wrap.querySelector('.dlist');
    if (dl) attachSmoothScroll(dl); // liste taşarsa tekerlek yumuşak kaysın
  }

  function loadDrops(force) {
    if (!isActive()) { dropsData = null; renderDrops(); return; }
    sendBg({ type: 'getDrops', force: !!force }).then(function (d) {
      dropsData = (d && d.active) ? d : { active: [] };
      renderDrops();
      /* "Görüldü" işareti YALNIZCA kullanıcı listeye baktığında konur — burada
         markDropsSeen() ÇAĞRILMAZ, yoksa veri her çekildiğinde bildirim kullanıcı
         görmeden silinirdi. İşareti paneldeki Drops bölümü koyuyor. */
    });
  }

  function watchdog() {
    /* KENDİMİZİ TOPLADIYSAK YENİDEN KURMA. Eklenti yeniden yüklenince/
       güncellenince bağlam ölüyor, `pkTur` `pkKendiniTopla`yı çağırıp arayüzü
       kaldırıyor — ama gözcü 800ms sonra "arayüz kayıp" deyip geri kuruyordu.
       `pkKendiniTopla` ikinci kez çalışmadığı için (kendi bayrağı var) ölü
       arayüz sayfada kalıcı oluyordu: düğmeler duruyor, hiçbiri çalışmıyor,
       etiketler boş (`t()` bağlam ölünce '' dönüyor). Sayfa yenilenene kadar
       böyle kalıyordu. */
    if (pkToplandi || !pkBaglamCanli()) return;
    pkGozlemcileriSenkronla();   // 10.12 — ana sayfa/kanal geçişinde gözlemcileri aç/kapat
    if (!settings.consented) return;
    if (!host || !document.contains(host)) { host = null; sroot = null; buildWidget(); }
  }

  /* ==================== DOM reklam temizleyici ==================== */
  var AD_SELECTORS = [
    '[id^="google_ads"]', '[id^="div-gpt-ad"]', '[class*="gpt-ad"]', '[data-google-query-id]',
    '[class*="ima-ad"]', '[id*="ima-ad"]', '.ima-ad-container',
    '[id*="ad-slot"]', '[class*="ad-overlay"]', '[class*="ad-container"]', '[class*="ad_container"]',
    '[class*="advertisement"]', '[class~="video-ad"]', '[class*="video-ad-"]', '[class*="preroll"]', '[class*="midroll"]',
    'iframe[src*="doubleclick"]', 'iframe[src*="googlesyndication"]', 'iframe[src*="imasdk"]', 'iframe[src*="googleadservices"]',
    '[class*="StreamAd"]', '[class*="stream-ad"]', '[class*="AdOverlay"]', '[class*="ad-break-overlay"]'
  ].join(', ');
  var MARKER = 'data-kab-hidden';
  var observer = null;
  function hideEl(el) {
    if (!(el instanceof HTMLElement) || el.hasAttribute(MARKER)) return false;
    try { if (!el.matches(AD_SELECTORS)) return false; } catch (e) { return false; }
    el.setAttribute(MARKER, '1');
    el.style.setProperty('display', 'none', 'important');
    el.style.setProperty('visibility', 'hidden', 'important');
    el.style.setProperty('height', '0', 'important');
    return true;
  }
  function sweep(root) {
    var n = 0;
    try { var els = root.querySelectorAll(AD_SELECTORS); for (var i = 0; i < els.length; i++) if (hideEl(els[i])) n++; } catch (e) {}
    if (n) reportStat('dom', n);
  }
  /* DEBOUNCE (10.9) — SKELETON KASMASI DÜZELTMESİ.
     ÖNCEKİ SÜRÜM her mutasyonda, EKLENEN HER DÜĞÜMDE ayrı bir
     querySelectorAll(AD_SELECTORS) koşuyordu. Ana sayfanın ilk render'ında
     React binlerce düğüm bastığı için bu, kare başına yüzlerce/binlerce tarama
     demekti ve main thread'i saniyelerce kilitliyordu ("skeleton 40-45 sn").
     Browse'da içerik az olduğu için hiç görünmüyordu.
     ÇÖZÜM: mutasyonları tek bir rAF'te topla, kare başına EN FAZLA BİR kez
     işle. Yığın büyükse (render fırtınası) düğüm-başına tarama yerine TEK bir
     belge taraması yap (kesişen alt ağaçları defalarca taramaktan ucuz).
     Reklam yine gizlenir; yalnız en fazla bir kare (~16 ms) gecikmeyle. */
  var temizKuyruk = [];
  var temizBekleyen = false;
  function temizIsle() {
    temizBekleyen = false;
    var nodes = temizKuyruk; temizKuyruk = [];
    var n = 0;
    if (nodes.length > 30) {
      /* Çok düğüm → tek belge taraması. querySelectorAll native ve tek geçiş;
         yüzlerce alt-ağaç taramasından çok daha ucuz. */
      try { var all = document.querySelectorAll(AD_SELECTORS); for (var a = 0; a < all.length; a++) if (hideEl(all[a])) n++; } catch (e) {}
    } else {
      for (var i = 0; i < nodes.length; i++) {
        var node = nodes[i];
        if (!(node instanceof HTMLElement)) continue;
        if (hideEl(node)) n++;
        try { var kids = node.querySelectorAll(AD_SELECTORS); for (var k = 0; k < kids.length; k++) if (hideEl(kids[k])) n++; } catch (e) {}
      }
    }
    if (n) reportStat('dom', n);
  }
  function startCleaner() {
    if (observer) return;
    sweep(document);
    observer = new MutationObserver(function (muts) {
      for (var i = 0; i < muts.length; i++) {
        var added = muts[i].addedNodes;
        for (var j = 0; j < added.length; j++) {
          if (added[j] instanceof HTMLElement) temizKuyruk.push(added[j]);
        }
      }
      if (temizBekleyen) return;
      temizBekleyen = true;
      requestAnimationFrame(temizIsle);
    });
    observer.observe(document.documentElement, { childList: true, subtree: true });
  }
  function stopCleaner() { if (observer) { observer.disconnect(); observer = null; } }

  /* ---------- MAIN dünyadan gelen sinyaller ---------- */
  window.addEventListener('message', function (e) {
    if (e.source !== window) return;
    var d = e.data;
    if (!d || d.source !== 'kab' || d.n !== pkNonce) return;   // jeton doğrulaması → sahte 'kab' mesajları reddedilir
    if (d.type === 'chatEvent') { try { sohbetOlayi(d); } catch (er) {} return; }   // 10.5 — moderasyon olayları
    /* Uzaktan kart açma sonucu. worker-hook sohbetteki bir gönderen düğümünü
       ödünç alıyor; sohbet boşsa ödünç alacak düğüm yok ve kart açılmıyor.
       Sessiz kalmak "tıkladım bir şey olmadı" hissi veriyordu. */
    if (d.type === 'kartSonuc') { if (!d.ok) { try { toast(t('pkTKartAcilamadi')); } catch (er) {} } return; }
    if (d.type === 'adDetected') reportStat('video', 1);
    /* Oynatıcının kullandığı VOD adresi (worker-hook duyurur) → "VOD İndir"
       hangi yayın olursa olsun kaynağı bilir; süre eşlemesine muhtaç kalmaz. */
    if (d.type === 'vodUrl' && typeof d.url === 'string') vodSonUrl = { url: d.url, yol: location.pathname };
  });

  /* ==================== Chat rozetleri (uzaktan yönetilen) ====================
   * badges.json geliştiricinin GitHub reposundan (background üzerinden) çekilir;
   * liste/görsel değişince ~5-10 dk içinde açık sekmelere otomatik yansır.
   * Kullanıcı adına göre eşleştirir (küçük harf). innerHTML KULLANMAZ. */
  var badgeMaps = null, badgeObserver = null, badgeTimer = null, badgeStyleAdded = false, pkTipEl = null;
  var badgeGorunurBagli = false;   // visibilitychange dinleyicisi bir kez bağlansın

  /* MOTORUN KAPISI. Eskiden yalnız `consented`e bakıyordu ve "Bu sitede
     duraklat" (`enabled: false`) motoru DURDURMUYORDU: bütün belgeyi izleyen
     gözcü ve her değişiklikte koşan 38 işlevlik tur çalışmaya devam ediyordu.
     Duraklatan kullanıcı yavaşlıktan kurtulmuyordu. */
  function badgeActive() { return isActive(); }

  /* Işıltılı rozetler ANAHTARA göre. Tablo olmasının sebebi ileride ikinci
     bir ışıltılı rozet çıkarsa tek satır eklenebilsin — `PK_OZEL_CERCEVE`
     ile aynı desen. Anahtar sunucudaki `badges.key` ile birebir aynı olmalı. */
  var PK_ISILTILI_ROZET = { pk: 1 };

  /* Üç yüzey de aynı kuralı ALMAK ZORUNDA: rozet nerede çıkıyorsa ışıltı,
     isim rengi ve ipucu orada olacak (sohbet, profil kartı, yayıncı başlığı). */
  var PK_ROZET_YUZEY = ['.pk-badge', '.pk-pop-badge', '.pk-stream-badge'];

  function isiltiCss() {
    var anahtar = Object.keys(PK_ISILTILI_ROZET);
    if (!anahtar.length) return '';
    var sec = [];
    anahtar.forEach(function (k) {
      PK_ROZET_YUZEY.forEach(function (y) {
        sec.push(y + '[data-pk-key="' + k + '"]');
      });
    });
    var kap = sec.join(',');
    var ust = sec.map(function (s) { return s + '::after'; }).join(',');
    /* Bant AYRI DÜĞÜM DEĞİL, `::after`: Kick rozet düğümlerini geri
       dönüştürüyor, fazladan çocuk eklemek oralarda artık bırakıyor. */
    return kap + '{position:relative;overflow:hidden;border-radius:4px}' +
      ust + '{content:"";position:absolute;top:-50%;left:-90%;width:42%;height:200%;' +
        'background:linear-gradient(90deg,transparent,rgba(255,255,255,.65),transparent);' +
        'pointer-events:none;animation:pk-rozet-isik 3.2s cubic-bezier(.4,0,.2,1) infinite}' +
      /* Süpürme %55'te bitiyor, kalan süre bekleme: sürekli parlayan rozet
         sohbette göz yorar, ara ara göz kırpsın istiyoruz. */
      '@keyframes pk-rozet-isik{0%{transform:translateX(0) rotate(20deg)}' +
        '55%,100%{transform:translateX(560%) rotate(20deg)}}' +
      '@media (prefers-reduced-motion:reduce){' + ust + '{animation:none;opacity:0}}';
  }

  /* Abonelik bölümünün dönen ışıklı çerçevesi + tanıtım demosu.

     IŞIK NEDEN DÖNEN SÖZDE-ÖĞE: `@property` ile açı canlandırmak daha
     az kod olurdu ama Firefox'a geç geldi; desteklenmediğinde animasyon
     hiç başlamıyor ve çerçeve donuk kalıyor. Conic gradyanlı bir katmanı
     `rotate` ile döndürmek iki tarayıcıda da çalışıyor.

     Kare katman kabın 2 katı: köşelerde de ışık geçsin diye. `overflow`
     kırpıyor, `.pk-ab-ic` ortayı kapatıyor — geriye 2px'lik çerçeve kalıyor. */
  function aboneCss() {
    /* MENÜ VURGUSU: soldaki "Abonelik" satırı hem ÇERÇEVELİ hem ışık geçiyor.

       ÇERÇEVE `box-shadow: inset`, `border` DEĞİL: border satırın kutu
       ölçüsünü büyütüp diğer menü satırlarıyla hizasını bozardı. İçe gölge
       yer kaplamıyor.

       ARKA PLAN DA `box-shadow` ile: `menuSatiri` satır içi `background`
       yazıyor (seçiliyken rgb(35,38,41), değilken none) ve satır içi stil
       sınıf kuralını yener — buradan `background-color` vermek işe yaramaz.
       İkinci bir içe gölge aynı işi görüyor ve seçili/seçili değil ayrımını
       bozmuyor.

       IŞIK BANDI DAR VE ARALIKLI: önceki sürüm %38 genişlikte ve sürekliydi,
       satırı dolduran yeşil bir blok gibi görünüyordu. %20 bant + süpürme
       %40'ta bitip kalan süre bekliyor → göz kırpması gibi geçiyor. */
    return '.pk-menu-isik{position:relative;overflow:hidden;border-radius:4px;' +
        'box-shadow:inset 0 0 0 1px rgba(83,252,24,.45),inset 0 0 14px rgba(83,252,24,.10)}' +
      /* İçerik ışığın ÜSTÜNDE kalsın: ikon ve yazı sönük görünmesin. */
      '.pk-menu-isik>*{position:relative;z-index:1}' +
      '.pk-menu-isik::after{content:"";position:absolute;top:-50%;left:-40%;width:20%;height:200%;' +
        'pointer-events:none;z-index:0;' +
        'background:linear-gradient(90deg,transparent,rgba(83,252,24,.35),transparent);' +
        'animation:pk-menu-isik 4.2s cubic-bezier(.4,0,.2,1) infinite}' +
      '@keyframes pk-menu-isik{0%{transform:translateX(0) rotate(18deg)}' +
        '40%,100%{transform:translateX(700%) rotate(18deg)}}' +
      '@media (prefers-reduced-motion:reduce){.pk-menu-isik::after{animation:none;opacity:0}}' +
      /* Rozet şeridi: sohbetteki 18px burada okunmuyor, 26px'e büyütüldü. */
      '.pk-ab-rozetler{display:flex;gap:4px;flex-wrap:wrap}' +
      '.pk-ab-rozetler img{width:26px;height:26px;object-fit:contain;display:block}' +
      /* Satır ve isim RENKLERİ burada YOK: onları akanSatirBoya/akanAdBoya
         satır içi `!important` ile yazıyor (sohbetteki koddan). Buradaki
         kurallar yalnız yerleşim. */
      '.pk-ab-satir{display:flex;align-items:center;gap:6px;border-radius:4px;padding:5px 8px;' +
        'font-size:13px;line-height:1.6;min-width:0}' +
      '.pk-ab-satir>img{width:18px;height:18px;object-fit:contain;flex:none}' +
      '.pk-ab-ad{font-weight:700;white-space:nowrap}' +
      '.pk-ab-mesaj{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:#8b969d}';
  }

  function addBadgeStyle() {
    if (badgeStyleAdded || document.getElementById('pk-badge-style')) { badgeStyleAdded = true; return; }
    var css =
      '.pk-badge{position:relative;display:inline-flex;align-items:center;justify-content:center;align-self:center;vertical-align:middle;flex:none;width:calc(1em*18/13);height:calc(1em*18/13);margin-right:4px;cursor:inherit}' +
      '.pk-badge>img{width:100%;height:100%;object-fit:contain;display:block}' +
      '.pk-badge>.pk-pill{display:inline-flex;align-items:center;height:100%;padding:0 5px;border-radius:4px;font-size:10px;font-weight:800;line-height:1;color:#0a0a0a;white-space:nowrap}' +
      // Kick tooltip'inin oku: beyaz üçgen 10x5, altta ortalı.
      '.pk-tip::after{content:"";position:absolute;top:100%;left:50%;margin-left:-5px;width:0;height:0;border:5px solid transparent;border-top-color:#fff}' +
      /* Oynatıcı ipucunun oku da beyaz ve aynı ölçüde (10×5, canlıda
         ölçüldü) — `.pk-tip::after` kuralı zaten onu çiziyor, ayrıca bir
         şey gerekmiyor. Kapatma kuralı KALDIRILDI: ok kapalıyken kutu
         havada duruyordu. */
      '.pk-tip-oyn{}' + isiltiCss() + aboneCss() + susStilKur();
    var s = document.createElement('style'); s.id = 'pk-badge-style'; s.textContent = css;
    (document.head || document.documentElement).appendChild(s); badgeStyleAdded = true;
  }

  // Tooltip: KICK'İN KENDİ tooltip class'larını kullanır (bg-white/text-black/rounded-md...).
  // Sayfada Kick'in CSS'i yüklü olduğu için görünüm birebir Kick tooltip'iyle aynı olur.
  // Konumlandırma bize ait (fixed) — chat overflow'una takılıp kırpılmaz.
  var KICK_TIP_CLASS = 'z-tooltip select-none rounded-md bg-white p-[5px] text-sm font-medium leading-5 text-black pointer-events-none pk-tip';
  /* Oynatıcı şeridindeki ipucu. Kick'in kendi ölçüleri (canlıda ölçüldü):
     beyaz zemin, siyah yazı, 2px köşe, 5px iç boşluk, gölge yok, ok yok. */
  var KICK_TIP_OYNATICI = 'z-tooltip select-none text-sm font-medium leading-5 pointer-events-none pk-tip pk-tip-oyn';
  /* İpucunun dayandığı öğe ve boşluk — kaydırmada yeniden konumlandırmak
     için saklanıyor. */
  var pkTipHedef = null, pkTipBosluk = 11;

  function pkTipKonumla() {
    if (!pkTipEl || !pkTipHedef) return;
    var r = pkTipHedef.getBoundingClientRect();
    var w = pkTipEl.offsetWidth || 0, h = pkTipEl.offsetHeight || 24;
    // yatayda ekran dışına taşmasın
    var x = Math.max(w / 2 + 4, Math.min(r.left + r.width / 2, window.innerWidth - w / 2 - 4));
    pkTipEl.style.left = Math.round(x) + 'px';
    // kutunun ALTI, öğenin `bosluk` kadar üstünde; üstte yer yoksa ALTA al
    var top = r.top - pkTipBosluk;
    if (top - h < 4) top = r.bottom + pkTipBosluk + h;
    pkTipEl.style.top = Math.round(top) + 'px';
  }

  /* Kaydırmada GİZLEME, TAKİP ET. Dayanak DOM'dan çıktıysa ya da görünmez
     olduysa gizle — asılı kalmasın. */
  function pkTipTakip() {
    if (!pkTipEl || pkTipEl.style.display === 'none') return;
    var e = pkTipHedef;
    if (!e || !e.isConnected) { hideTip(); return; }
    var r = e.getBoundingClientRect();
    if (r.width <= 0 || r.height <= 0) { hideTip(); return; }
    pkTipKonumla();
  }

  /* ── TAM EKRAN KATMANI ────────────────────────────────────────────────
     Tam ekranda tarayıcı yalnız tam ekran ögesinin alt ağacını çizer;
     `document.body`ye eklenen katmanlarımız görünmez kalıyordu. Bütün
     üst düzey katmanlar artık buraya ekleniyor ve tam ekrana girip
     çıkınca `pkKatmanTasi` ile taşınıyor. */
  function pkKatman() {
    return document.fullscreenElement || document.webkitFullscreenElement || document.body;
  }
  function pkKatmanaEkle(n) {
    if (!n) return n;
    try { n.setAttribute('data-pk-katman', '1'); } catch (e) {}
    pkKatman().appendChild(n);
    return n;
  }
  function pkKatmanTasi() {
    var kok = pkKatman();
    var l = document.querySelectorAll('[data-pk-katman]');
    for (var i = 0; i < l.length; i++) {
      if (l[i].parentNode !== kok) { try { kok.appendChild(l[i]); } catch (e) {} }
    }
  }

  function showTip(badgeEl, label, oynatici, bosluk) {
    if (!label) return;
    if (!pkTipEl || !document.contains(pkTipEl)) {
      pkTipEl = document.createElement('div');
      pkTipEl.className = KICK_TIP_CLASS;
      // Görünüm INLINE da verilir: Kick sınıflarını değiştirse bile tooltip
      // görünmez kalmasın (sadece class'a güvenmek kırılgandı).
      pkTipEl.style.cssText = 'position:fixed;transform:translate(-50%,-100%);white-space:nowrap;z-index:2147483000;display:none;' +
        'background:#fff;color:#000;border-radius:6px;padding:5px;font-size:14px;line-height:20px;font-weight:500;' +
        'pointer-events:none;box-shadow:0 2px 8px rgba(0,0,0,.35)';
      pkKatmanaEkle(pkTipEl);
      // Sanal liste kayınca imleç rozetten "kopar" ve mouseleave gelmeyebilir → takılı kalmasın.
      document.addEventListener('scroll', pkTipTakip, true);
      window.addEventListener('blur', hideTip);
    }
    /* Varyanta göre görünümü her seferinde ayarla: aynı düğüm hem rozet hem
       oynatıcı ipucu için kullanılıyor. */
    if (oynatici) {
      pkTipEl.className = KICK_TIP_OYNATICI;
      pkTipEl.style.setProperty('background', '#fff');
      pkTipEl.style.setProperty('color', '#000');
      pkTipEl.style.setProperty('padding', '5px');
      pkTipEl.style.setProperty('border-radius', '2px');
      pkTipEl.style.setProperty('box-shadow', 'none');
    } else {
      pkTipEl.className = KICK_TIP_CLASS;
      pkTipEl.style.setProperty('background', '#fff');
      pkTipEl.style.setProperty('color', '#000');
      pkTipEl.style.setProperty('padding', '5px');
      pkTipEl.style.setProperty('border-radius', '6px');
      pkTipEl.style.setProperty('box-shadow', '0 2px 8px rgba(0,0,0,.35)');
    }
    pkTipEl.textContent = label;
    pkTipEl.style.display = 'block';
    pkTipHedef = badgeEl;
    pkTipBosluk = (typeof bosluk === 'number') ? bosluk : (oynatici ? 2 : 11);
    pkTipKonumla();
  }
  function hideTip() { if (pkTipEl) pkTipEl.style.display = 'none'; pkTipHedef = null; }

  /* Rozetin üstüne gelince isim baloncuğu — TEK yerden bağlanır ki rozet nerede
     basılırsa basılsın (sohbet, sabitlenmiş mesaj, profil kartı, yayıncı başlığı)
     tooltip HER ZAMAN olsun. pointer* olayları da eklenir (dokunmatik/kalem). */
  function bindTip(el, label, oynatici, bosluk) {
    if (!label) return;
    var show = function () { showTip(el, label, oynatici, bosluk); };
    el.addEventListener('mouseenter', show);
    el.addEventListener('pointerenter', show);
    el.addEventListener('mouseleave', hideTip);
    el.addEventListener('pointerleave', hideTip);
  }

  // Bir kullanıcı BİRDEN FAZLA badge alabilir (Kick gibi). Hem "badge":"dev"
  // hem "badge":["dev","supporter"] hem de aynı user için birden çok satır desteklenir.
  /* 10.15 — AKAN İSİM PALETİ.
     Akan gradyan `background-repeat:repeat` + 300% genişlikle kayıyor; iki
     renkle biten bir dizi döşenince başla son arasında SERT BİR EK yeri
     oluşuyor ve döngü her turda "zıplıyor". Diziyi palindroma çevirmek
     (a,b → a,b,a) dikişi yok ediyor: son durak ilk durakla aynı renk.
     Koda gömülü `PK_OZEL_AD.pumpzera` de tam bu yüzden 5 duraklı palindrom. */
  function akanPalet(renkler) {
    var r = (renkler || []).filter(function (c) { return /^#[0-9a-f]{6}$/i.test(c); })
                           .map(function (c) { return c.toLowerCase(); });
    if (r.length < 2) return null;                 // tek renk akmaz, durgun kalsın
    return r.concat(r.slice(0, -1).reverse());     // a,b -> a,b,a
  }

  /* ══════════════════════════════════════════════════════════════════════
   * ROZET SIRASI — YÖNETİM PANELİNDEN
   *
   * Her rozet tanımının "Sıra (sort)" alanı var. KÜÇÜK SAYI SOLDA, büyük sayı
   * sağda; en büyüğü isme en yakın durur — Kick de en sağdaki rozeti "asıl"
   * rozet gibi gösteriyor.
   *
   * TEK İSTİSNA kıdem rozetleri (sub1…sub48): onlar sistem rozeti, paneldeki
   * sayıya bakılmadan HER ZAMAN en sağda — isme en yakın. Öteki rozetler
   * paneldeki sıraya göre onların soluna diziliyor.
   *
   * Sıralama İSTEMCİDE SABİT DEĞİL: önce burada "supporter → kıdem → özel"
   * diye gömülü bir kural yazmıştım, ama o kural yeni bir rozet eklendiğinde
   * eklentiyi güncellemeyi gerektiriyordu. Panelden çekince rozet düzeni
   * sunucudan yönetiliyor, eklenti hiç bilmeden doğru sırada basıyor.
   *
   * Sunucu sırayı GARANTİ ETMİYOR (aynı kullanıcı için birden çok satır
   * dönebiliyor, bkz. byName birleştirmesi), düzeni burada kuruyoruz.
   * ══════════════════════════════════════════════════════════════════════ */
  /* Kıdem (abonelik) rozeti mi? Anahtarlar YERELDE üretiliyor: 'sub' + kademe.
     Listeden türetiyoruz, kalıp eşleştirmiyoruz — yeni bir kademe eklenirse
     burası kendiliğinden doğru kalıyor. */
  function pkKidemRozetMi(k) {
    var a = String(k || '').toLowerCase();
    for (var i = 0; i < PK_AB_KADEME.length; i++) {
      if (a === 'sub' + PK_AB_KADEME[i]) return true;
    }
    return false;
  }

  function badgeSirasi(k) {
    /* KIDEM ROZETİ HER ZAMAN EN SAĞDA — isme en yakın, paneldeki sıra sayısına
       BAKILMAZ. Bunlar sistem rozeti: kullanıcının kaç aydır abone olduğunu
       gösteriyor ve yeri sabit olmalı. Öteki rozetler panelden gelen sıraya
       göre onun soluna diziliyor. */
    if (pkKidemRozetMi(k)) return 1e9;
    var d = (badgeMaps && badgeMaps.defs) ? badgeMaps.defs[k] : null;
    var n = d ? parseInt(d.sort, 10) : NaN;
    /* SIRASIZ ROZET kıdemin SOLUNA, ötekilerin sağına. Panelde sıra girilmemiş
       yeni bir rozet ortada kaybolmasın; kararlı sıralama sayesinde kendi
       aralarında sunucudan geldikleri düzeni koruyorlar. */
    return isFinite(n) ? n : 9999;
  }

  function badgeKeysFor(username) {
    if (!badgeMaps || !username) return [];
    var l = badgeMaps.byName[username.toLowerCase()];
    /* Tek rozette sıralamaya gerek yok — bu işlev mesaj başına, tur başına
       çağrılıyor; kullanıcıların büyük çoğunluğunda 0 ya da 1 rozet var. */
    if (!l || l.length < 2) return l || [];
    /* KOPYA üzerinde sıralıyoruz: `byName` önbellek, yerinde sıralamak
       sunucudan gelen veriyi kalıcı değiştirirdi. `sort` kararlı olduğu için
       aynı gruptaki rozetler kendi aralarında sırasını koruyor. */
    return l.slice().sort(function (x, y) { return badgeSirasi(x) - badgeSirasi(y); });
  }

  /* ---- Sorgu-modu: yalnızca GÖRÜNEN kullanıcı adları sunucuya sorulur (mahremiyet) ----
   * Tam liste indirilmez; görünen her ad "note" edilir, yeni adlar kısa bir batch'te (120ms)
   * toplanıp tek POST'ta sorulur. Cevap byName'e BİRİKTİRİLEREK eklenir → tekrar yazan herkes
   * cache'ten anında. Backlog (açılışta yüklü ~30-50 mesaj) ilk geçişte toplanıp HEMEN sorulur. */
  var badgeWanted = {};    // sorulmuş (lowercased) adlar → tekrar sorulmaz
  var badgePending = {};   // sırada bekleyen yeni adlar
  var badgeFlushTimer = 0;
  // TAM LİSTE modundayken (server rozetli tüm kullanıcıları tek seferde döndürüyor)
  // ad ad sorguya gerek yok — rozetler ağ beklemeden anında basılır.
  var badgeFullList = false;

  function noteUser(uname) {
    if (badgeFullList) return;   // liste zaten elimizde
    if (!uname) return;
    var n = String(uname).toLowerCase();
    if (badgeWanted[n]) return;
    badgeWanted[n] = 1; badgePending[n] = 1;
    if (!badgeFlushTimer) badgeFlushTimer = setTimeout(flushBadgeLookup, 120);
  }
  function flushBadgeLookup() {
    if (badgeFlushTimer) { clearTimeout(badgeFlushTimer); badgeFlushTimer = 0; }
    var names = Object.keys(badgePending);
    badgePending = {};
    if (!names.length || !badgeMaps) return;
    // DİKKAT: sendBg yanıt gelmezse {} döner (nesne!) — `users || []` bunu YAKALAMAZ.
    // Service worker uykudaysa ya da eklenti güncellenip bağlam düştüyse buraya {} gelir.
    sendBg({ type: 'lookupBadges', names: names }).then(function (users) {
      if (mergeBadgeUsers(names, users)) { processAllMessages(); yanitAlintiRozetleri(); processProfilePopups(); processStreamerHeader(); processSearchResults(); }
    });
  }
  // Sonucu byName/colorsByName'e biriktir. Sorulup DÖNMEYEN adların rozetini temizle
  // (admin badge'i geri alırsa ~5dk'daki yeniden sorguda düşsün).
  function mergeBadgeUsers(reqNames, users) {
    if (!badgeMaps) return false;
    // Yanıt DİZİ DEĞİLSE ({} veya undefined) istek başarısız demektir: service worker
    // uykuda ya da eklenti güncellenirken bağlam düşmüş. HİÇBİR ŞEY YAPMA —
    //   • eskiden burada `.forEach is not a function` ile patlıyordu,
    //   • sadece boş diziye çevirmek de yanlış olur: aşağıdaki temizlik adımı
    //     "sunucu bu adları döndürmedi" sanıp sohbetteki rozetleri SİLERDİ.
    // Gerçek boş dizi [] ise rozet gerçekten yok → temizlik doğru çalışsın.
    if (!Array.isArray(users)) return false;
    var defs = badgeMaps.defs || {};
    var changed = false, returned = {};
    users.forEach(function (u) {
      if (!u || !u.user) return;
      var name = String(u.user).toLowerCase(); returned[name] = 1;
      var keys = Array.isArray(u.badge) ? u.badge : [u.badge];
      var badgeGrad = null;
      keys.forEach(function (k) {
        if (!k || !defs[k]) return;
        if (!badgeMaps.byName[name]) badgeMaps.byName[name] = [];
        if (badgeMaps.byName[name].indexOf(k) < 0) { badgeMaps.byName[name].push(k); changed = true; }
        if (Array.isArray(defs[k].gradient) && defs[k].gradient.length) badgeGrad = defs[k].gradient;
      });
      /* 10.15 — İSİM RENGİ artık AKAN (aboneye özel).
         AYRIM ÖNEMLİ: `u.gradient` KULLANICININ KENDİ seçimi (sunucudan, yalnız
         aktif aboneye gönderiliyor); `badgeGrad` ise rozetin kendi rengi ve
         istemcide `defs`ten hesaplanıyor. İkisi burada zaten ayrılabildiği için
         sunucuya yeni bir alan eklemeye gerek yok:
             kullanıcının seçimi var  -> AKAN gradyan (ozelAdUygula)
             yalnız rozet rengi var   -> DURGUN gradyan (applyNameGradient)
         Akan varken durgunu da yazmıyoruz: applyNameGradient önce koşup ismi
         durgun boyar, ardından ozelAdUygula üstüne yazardı — ilk karede
         gözle görülür bir renk sıçraması olurdu. */
      if (!badgeMaps.akanByName) badgeMaps.akanByName = {};
      var akan = Array.isArray(u.gradient) ? akanPalet(u.gradient) : null;
      if (akan) {
        badgeMaps.akanByName[name] = akan;
        if (badgeGrad) badgeMaps.colorsByName[name] = badgeGrad.slice();
        else delete badgeMaps.colorsByName[name];
      } else {
        delete badgeMaps.akanByName[name];          // abonelik bitti → akan düşer
        if (Array.isArray(u.gradient) && u.gradient.length) badgeMaps.colorsByName[name] = u.gradient.slice();
        else if (badgeGrad) badgeMaps.colorsByName[name] = badgeGrad.slice();
      }
      /* 10.15 — SATIR RENGİ (sohbet satırının hareketli çerçevesi). Eskiden
         `PK_OZEL_CERCEVE` diye koda GÖMÜLÜ bir haritaydı ve tek hesap vardı.
         Artık sunucudan geliyor: kimin hak ettiğine sunucu karar veriyor
         (abonelik), yeni kişi için eklenti güncellemesi gerekmiyor.
         Sunucu ayrıca denetliyor ama BURADA DA süzüyoruz — değer doğrudan
         CSS'e giriyor, #rrggbb dışında hiçbir şey geçmemeli. */
      /* Gerçek kıdem (ay). Rozet kademesinden BAĞIMSIZ: 2 aylık kullanıcı
         sub1 rozeti taşır ama ipucunda "2" yazmalı. */
      if (!badgeMaps.ayByName) badgeMaps.ayByName = {};
      var ayS = parseInt(u.ay, 10);
      if (ayS > 0) badgeMaps.ayByName[name] = ayS; else delete badgeMaps.ayByName[name];

      if (!badgeMaps.satirByName) badgeMaps.satirByName = {};
      if (typeof u.satir === 'string' && /^#[0-9a-f]{6}$/i.test(u.satir)) {
        badgeMaps.satirByName[name] = u.satir.toLowerCase();
      } else if (badgeMaps.satirByName[name]) {
        delete badgeMaps.satirByName[name];            // hak düştü → temizle
      }
      // Profil çerçevesi: kullanıcı başına en fazla 1, tanımı `frames` içinde olmalı
      if (!badgeMaps.frameByName) badgeMaps.frameByName = {};
      if (u.frame && badgeMaps.frames && badgeMaps.frames[u.frame]) {
        if (badgeMaps.frameByName[name] !== u.frame) { badgeMaps.frameByName[name] = u.frame; changed = true; }
      } else if (badgeMaps.frameByName[name]) { delete badgeMaps.frameByName[name]; changed = true; }
    });
    (reqNames || []).forEach(function (n) {
      n = String(n).toLowerCase();
      if (!returned[n] && badgeMaps.byName[n]) { delete badgeMaps.byName[n]; delete badgeMaps.colorsByName[n]; changed = true; }
      // Sunucu artık döndürmüyorsa çerçeveyi de düşür (admin geri alınca kaybolsun)
      if (!returned[n] && badgeMaps.frameByName && badgeMaps.frameByName[n]) { delete badgeMaps.frameByName[n]; changed = true; }
      /* 10.15 — AKAN İSİM ve SATIR RENGİ de aynı temizliğe girer.
         Kullanıcı yanıtta GELDİĞİ sürece yukarıdaki blok zaten düşürüyor
         (alan boş gelince siliniyor). Ama aboneliği bitip TEK rozeti de
         kalmayan kişi yanıttan tamamen çıkıyor; o durumda burası olmasa
         akan ismi ve çerçevesi sayfa yenilenene kadar ekranda kalırdı. */
      if (!returned[n] && badgeMaps.akanByName && badgeMaps.akanByName[n]) { delete badgeMaps.akanByName[n]; changed = true; }
      if (!returned[n] && badgeMaps.satirByName && badgeMaps.satirByName[n]) { delete badgeMaps.satirByName[n]; changed = true; }
      if (!returned[n] && badgeMaps.ayByName && badgeMaps.ayByName[n]) { delete badgeMaps.ayByName[n]; changed = true; }
    });
    return changed;
  }
  /* ══════════════════════════════════════════════════════════════════════
   * SOHBET SATIRI NEDİR — `[data-index]` TEK BAŞINA YETMİYOR
   *
   * Moderatör panosu (dashboard.kick.com/moderator/...) da kick.com alan
   * adında, yani içerik betiğimiz orada da çalışıyor. Panonun yeniden
   * boyutlanabilir PANEL düzeni `data-index` kullanıyor ama değerleri
   * "root", "a", "b"… Belge geneline `querySelectorAll('[data-index]')`
   * deyince "Denetim Eylemleri", "Yayın İstatistikleri" gibi koca paneller
   * birer sohbet balonu sanılıyordu: panelin içindeki herhangi bir kullanıcı
   * adı düğmesi o panelin "yazarı" oluyor, mesaj kısayolu şeridi panelin
   * tepesine açılıyordu. (Canlıda ölçüldü: panoda 11 panel düğümü, 3 gerçek
   * sohbet satırı.)
   *
   * İKİ KAPI — panel düğümleri ikisini de geçemiyor:
   *   1) satır SOHBET KAYDIRICISININ (#chatroom-messages) içinde olacak
   *   2) `data-index` SAYI olacak (sohbet satırları 0, 1, 2…)
   * Kanal sayfasında ölçüldü: bütün satırlar ikisini de geçiyor, tek satır
   * bile kaybolmuyor.
   *
   * Kaydırıcı hiç bulunamazsa yalnız (2) uygulanıyor: Kick kabın kimliğini
   * değiştirirse özellikler büsbütün susmasın.
   * ══════════════════════════════════════════════════════════════════════ */
  function pkSatirNo(n) {
    var v = n && n.getAttribute && n.getAttribute('data-index');
    return (v && /^\d+$/.test(v)) ? v : null;
  }

  /* Ekrandaki sohbet satırları. Kick aynı kaptan birden çok tutabiliyor
     (görünür + 0×0 gizli ikiz), hepsini tarıyoruz. */
  function sohbetSatirlari() {
    var out = [], kaplar = document.querySelectorAll('#chatroom-messages'), i, j, d;
    for (i = 0; i < kaplar.length; i++) {
      d = kaplar[i].querySelectorAll('[data-index]');
      for (j = 0; j < d.length; j++) if (pkSatirNo(d[j])) out.push(d[j]);
    }
    if (kaplar.length) return out;                 // kap var, sonuç boşsa gerçekten boş
    d = document.querySelectorAll('[data-index]'); // kap yok → yedek: yalnız sayı kapısı
    for (j = 0; j < d.length; j++) if (pkSatirNo(d[j])) out.push(d[j]);
    return out;
  }

  /* `n`'i içeren sohbet satırı, yoksa null. Düz `closest('[data-index]')`
     panoda panel düğümüne çarpıyordu. */
  function pkSatirBul(n) {
    var m = (n && n.closest) ? n.closest('[data-index]') : null;
    return pkSatirNo(m) ? m : null;
  }

  function noteVisibleUsers() {
    var msgs = sohbetSatirlari();
    for (var i = 0; i < msgs.length; i++) {
      try { var ub = usernameButton(msgs[i]); if (ub) { var n = (ub.textContent || '').trim(); if (n) noteUser(n); } } catch (e) {}
    }
    var slug = (location.pathname.split('/').filter(Boolean)[0] || ''); if (slug) noteUser(slug);
    var h1 = document.querySelector('h1'); if (h1) { var hn = (h1.textContent || '').trim(); if (hn) noteUser(hn); }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * PROFİL ÇERÇEVESİ — avatarın DIŞINA çizilen halka/dekor
   *
   * CANLI ÖLÇÜMLE BULUNANLAR (pumpzera profilinde, 4 farklı çerçeveyle):
   *  · Ölçek 1.20. 1.25'te üstü olan tasarımlar (ör. ekmek dilimi) kesiliyordu;
   *    kanal başlığında avatarın ~7px üstünden fazlası kırpılıyor.
   *  · Avatarın kabı zaten `position:relative` → çerçeve oraya mutlak konumlu
   *    KARDEŞ olarak girer; Kick'in DOM'u değişmez, dairesel kırpma bozulmaz.
   *  · Çerçeve avatar kutusunu aşınca üstteki `overflow-hidden` kaba çarpıp
   *    kırpılıyor → o zincirde kırpmayı açmak ŞART (avatarın kendi dairesel
   *    kırpmasına DOKUNULMAZ).
   *  · Kick avatara halka çizmiyor (gölge/kenarlık/outline/::before hepsi boş),
   *    bu yüzden "canlı" ayrımı DOM'dan değil `livestream.is_live`'dan gelir.
   * ══════════════════════════════════════════════════════════════════════ */
  var CERCEVE_OLCEK = 1.20;
  var canliDurum = Object.create(null);   // slug -> true/false/'bekliyor' (API sonucu, oturum boyu)

  /* Kanalın canlı olup olmadığı — tek yetkili kaynak. Sonuç önbelleklenir,
     her tur yeni istek atılmaz. */
  function kanalCanliMi(slug) {
    if (!slug) return false;
    var v = canliDurum[slug];
    if (v === true || v === false) return v;
    if (v === 'bekliyor') return false;
    canliDurum[slug] = 'bekliyor';
    fetch('https://kick.com/api/v2/channels/' + encodeURIComponent(slug), { credentials: 'include' })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) { canliDurum[slug] = !!(j && j.livestream && j.livestream.is_live); })
      .catch(function () { canliDurum[slug] = false; });
    return false;
  }

  /* Avatarın sahibi olan kullanıcı adı — yüzeye göre farklı yerden okunur. */
  function cerceveKullanici(img) {
    var a = img.closest && img.closest('a[href^="/"]');
    if (a) {
      var s = chanSlug(a);
      if (s) return s;
    }
    // Aktif sohbetçiler satırı: <button><img><span isim></span></button>
    var b = img.closest && img.closest('button');
    if (b) {
      var sp = b.querySelector('span.min-w-0.flex-1.truncate');
      if (sp) { var n = (sp.textContent || '').trim(); if (n) return n.toLowerCase(); }
    }
    // Sohbet profil kartı
    var kart = img.closest && img.closest('div.bg-surface-highest');
    if (kart) {
      var ad = kart.querySelector('a.select-text.font-semibold');
      if (ad) { var n2 = (ad.textContent || '').trim(); if (n2) return n2.toLowerCase(); }
    }
    /* Adres çubuğundaki slug'a düşmek YALNIZCA kanal başlığındaki büyük avatar
       için geçerli. Genel yedek olarak kullanmak felaketti: sohbetteki her kare
       görsel sayfa sahibinin adını alıyor ve tek testte 233 öğeye çerçeve
       takılıyordu (ölçüldü). Koşul: ana içerikte ve ≥56px. */
    var r = img.getBoundingClientRect();
    if (r.left > 280 && r.width >= 56) {
      var p = (location.pathname || '').split('/').filter(Boolean);
      if (p.length && !NON_CHANNEL[p[0].toLowerCase()]) return p[0].toLowerCase();
    }
    return '';
  }

  /* Çerçevenin taşabilmesi için avatardan yukarı giden zincirdeki kırpmaları aç.
     Avatarın KENDİ dairesel kırpmasına dokunmayız, yoksa kare görünür. */
  /* ---- Profil resmine tıklayınca HD hâlini tam ekran aç ----
   * Kick avatarı boyut varyantlarıyla saklıyor (canlı ölçüm):
   *   …/profile_image/conversion/<uuid>-thumb.webp     →  75x75
   *   …/profile_image/conversion/<uuid>-medium.webp    → 150x150
   *   …/profile_image/conversion/<uuid>-fullsize.webp  → 350x350  ← en büyüğü
   * `-large` ve eksiz sürüm YOK (denendi, 404). Varsayılan avatarlarda
   * (kick.com/img/default-profile-pictures/…) varyant yok, 250x250 tek dosya. */
  function hdAvatarAdresi(src) {
    var u = String(src || '').split('?')[0];
    if (!u) return null;
    if (/\/profile_image\/conversion\//.test(u)) return u.replace(/-(thumb|medium|fullsize)\.webp$/i, '-fullsize.webp');
    if (/default-profile-pictures/.test(u)) return u;   // varsayılanın tek boyutu var
    return null;
  }

  function hdAvatarAc(src) {
    var adres = hdAvatarAdresi(src);
    if (!adres) return;
    /* ÖLÇÜLDÜ (canlı, files.kick.com):
         -thumb     1.6 KB
         -medium      4 KB
         -fullsize  350x350, 12 KB
         ham        360x360, 25 KB   ← `conversion/` klasörü ve son ek olmadan
       Ham dosya kullanıcının yüklediği aslı: biraz daha büyük ve daha az
       sıkıştırılmış. Kick daha yüksek çözünürlüklü bir sürüm TUTMUYOR.
       Ham yoksa (eski kayıtlar) `-fullsize`a düşüyoruz. */
    var ham = adres.indexOf("/conversion/") >= 0
      ? adres.replace("/conversion/", "/").replace(/-(thumb|medium|fullsize)\.webp$/i, "")
      : "";
    var eski = document.getElementById('pk-hd'); if (eski) eski.remove();
    var perde = el('div', '');
    perde.id = 'pk-hd';
    /* Ölçüler açıkça: `inset` kısayolu dönüşümlü bir ata altında şaşabiliyor,
       perde o zaman görüntü alanını kaplamıyor ve görsel kenara kayıyordu. */
    perde.style.cssText = 'position:fixed;left:0;top:0;width:100vw;height:100vh;' +
      'z-index:2147483300;background:rgba(0,0,0,.85);' +
      'display:flex;align-items:center;justify-content:center;padding:24px;cursor:zoom-out';

    /* KARE KAP. Eskiden yarıçap doğrudan görsele veriliyordu; görsel kare
       olmadığı için %50 yarıçap onu ELİPSE çeviriyordu. */
    var kap = el('div', '');
    kap.style.cssText = 'position:relative;display:flex;align-items:center;justify-content:center;' +
      'background:rgba(255,255,255,.04);border-radius:10px;overflow:hidden;' +
      'box-shadow:0 24px 64px rgba(0,0,0,.6);cursor:default';

    var im = document.createElement('img');
    im.alt = '';
    im.referrerPolicy = 'no-referrer';
    im.style.cssText = 'display:block;width:100%;height:100%;object-fit:contain';
    /* Ham gelmezse dönüştürülmüş sürüme düş — tek seferlik, döngü olmasın. */
    if (ham) {
      im.addEventListener('error', function bir() {
        im.removeEventListener('error', bir);
        im.src = adres;
      });
    }
    im.src = ham || adres;

    /* GERÇEK ÇÖZÜNÜRLÜĞÜN ÜSTÜNE ÇIKMA. Kick'in `-fullsize` dosyası çoğu
       zaman ~350px; 860px'e şişirmek yalnız bulanıklık üretir. Yüklenince
       görselin kendi ölçüsü okunup kare kenar ona göre veriliyor. */
    var kenarUygula = function () {
      var enBuyuk = Math.min(window.innerWidth * 0.86, window.innerHeight * 0.86, 860);
      var dogal = Math.max(im.naturalWidth || 0, im.naturalHeight || 0);
      /* OLCULDU: Kick profil resimlerini ~350-360px te siniriyor, daha
         yuksek cozunurluklu surum YOK. Iki kata kadar buyutmek fotografta
         kabul edilebilir duruyor; otesi yalniz bulaniklik uretir. */
      var kenar = dogal ? Math.min(dogal * 2, enBuyuk) : enBuyuk;
      kenar = Math.max(220, Math.round(kenar));      // çok küçük avatarda da bakılabilir kalsın
      kap.style.width = kenar + 'px';
      kap.style.height = kenar + 'px';
    };
    kenarUygula();                                    // önbellekten geldiyse hemen
    im.addEventListener('load', kenarUygula);
    window.addEventListener('resize', kenarUygula);

    kap.addEventListener('click', function (e) { e.stopPropagation(); });
    kap.appendChild(im);
    perde.appendChild(kap);
    var kapat = function () {
      perde.remove();
      window.removeEventListener('resize', kenarUygula);
    };
    perde.addEventListener('click', kapat);
    pkKatmanaEkle(perde);
    var esc = function (e) {
      if (e.key === 'Escape') { kapat(); document.removeEventListener('keydown', esc); }
    };
    document.addEventListener('keydown', esc);
  }

  /* Profil kartındaki avatarı tıklanabilir yap (yalnızca kartta — sohbetteki
     küçük avatarlara dokunmuyoruz, oraya Kick'in kendi tıklaması bağlı). */
  function hdAvatarKur(kart) {
    var av = kart.querySelector('img:not(.pk-cerceve)');
    if (!av || av.getAttribute('data-pk-hd')) return;
    if (!hdAvatarAdresi(av.getAttribute('src'))) return;
    av.setAttribute('data-pk-hd', '1');
    av.style.cursor = 'zoom-in';
    av.title = t('pkProfilBuyuk');
    av.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      hdAvatarAc(av.getAttribute('src'));
    });
  }

  /* Sayfa değişince (SPA) kalan çerçeveleri ve açılan kırpmaları geri al —
     yoksa Takip Edilen Kanallar'a geçince önceki sayfanın çerçevesi asılı kalır. */
  function cerceveTemizle() {
    var c = document.querySelectorAll('.pk-cerceve');
    for (var i = 0; i < c.length; i++) c[i].remove();
    var k = document.querySelectorAll('[data-pk-kirpma]');
    for (var j = 0; j < k.length; j++) { k[j].style.removeProperty('overflow'); k[j].removeAttribute('data-pk-kirpma'); }
  }

  function cerceveKirpmaAc(img) {
    var kendi = img.parentElement;
    var ar = img.getBoundingClientRect();
    var enYuksek = ar.height * 4;   // avatarın 4 katından uzun kap = sayfa düzeni, çerçeve kabı değil
    for (var e = img, i = 0; i < 6 && e.parentElement; i++) {
      e = e.parentElement;
      if (e === kendi) continue;
      if (e.getAttribute('data-pk-kirpma')) continue;
      var er = e.getBoundingClientRect();
      /* 1) BÜYÜK KAPLARA DOKUNMA. Çerçeveyi kesen kap avatarın hemen etrafındaki
         küçük sarmal olur (ölçüm: 64px avatar → 206x72 kap). Yukarı çıkmaya devam
         edersek sayfanın ana kapsayıcılarına ulaşırız. */
      if (er.height > enYuksek) break;
      var c = getComputedStyle(e);
      /* 2) KAYDIRILABİLİR KABA DOKUNMA — bu gerçek bir hataydı: takip edilen
         kanallar sayfasında `overflow:auto` olan liste kabını `visible` yapınca
         kaydırma ölüyor ve kap tüm içerik boyuna açılıyordu (ölçüldü: kart
         256x658 → 256x795, sayfa kayıyordu). */
      if (c.overflowY === 'auto' || c.overflowY === 'scroll' ||
          c.overflowX === 'auto' || c.overflowX === 'scroll') continue;
      if (c.overflow === 'visible') continue;
      e.setAttribute('data-pk-kirpma', '1');
      e.style.overflow = 'visible';
    }
  }

  function cerceveUygula() {
    if (!badgeActive() || !badgeMaps || !badgeMaps.frames) return;
    /* "Takip Edilen Kanallar" sayfasında çerçeve GÖSTERİLMEZ (kullanıcı isteği).
       Oradaki kartlar dar ve sık dizili; çerçeve kart düzenini bozuyordu. */
    if (/^\/following(\/|$)/.test(location.pathname || '')) { cerceveTemizle(); return; }
    var fdefs = badgeMaps.frames, fmap = badgeMaps.frameByName || {};
    var bosMu = true; for (var k in fdefs) { bosMu = false; break; }
    if (bosMu) return;
    var imgs = document.querySelectorAll('img');
    for (var i = 0; i < imgs.length; i++) {
      var img = imgs[i];
      if (img.classList.contains('pk-cerceve')) continue;
      /* EMOTELERİ ÖLÇMEDEN ELE. `getBoundingClientRect` yerleşim hesabı
         zorluyor ve bu döngü sohbet her değiştiğinde dönüyor; kalabalık
         sohbette sayfadaki `img`lerin neredeyse tamamı emote oluyor, yani
         kare başına yüzlerce gereksiz ölçüm. Emote adresi sabit
         (`files.kick.com/emotes/...`) ve asla avatar olamaz — dizgi
         karşılaştırması yerleşime dokunmuyor. */
      if ((img.src || '').indexOf('/emotes/') >= 0) continue;
      var r = img.getBoundingClientRect();
      if (r.width < 20 || r.width > 160 || Math.abs(r.width - r.height) > 3) continue;  // kare değil
      /* Avatar olduğunu doğrula: Kick avatarları DAİRESEL kaba oturur. Bu kontrol
         olmadan emote/küçük resim gibi kare görseller de avatar sanılıyordu. */
      var sarmal = img.parentElement;
      if (!sarmal) continue;
      var yc = getComputedStyle(sarmal).borderRadius;
      if (!/9999px|50%/.test(yc) && !/rounded-full/.test(String(sarmal.className || ''))) continue;
      var kap = sarmal.parentElement;
      if (!kap) continue;
      /* SADECE İKİ YÜZEY (kullanıcı isteği):
           1) Kanal sayfasının kendi profil avatarı
           2) Sohbette isme tıklayınca açılan profil kartı
         Kenar çubuğu (takip edilenler / önerilenler), ana sayfa, kategori,
         arama gibi liste kartlarında çerçeve GÖSTERİLMEZ — oralarda avatarlar
         küçük ve sık dizili, çerçeve düzeni bozuyordu. */
      var kartIci = img.closest('div.bg-surface-highest');
      var profilKarti = !!(kartIci && kartIci.querySelector('a.select-text.font-semibold'));
      var kanalAvatari = !!sayfaSlug() && r.left > 280 && r.width >= 56;
      if (!profilKarti && !kanalAvatari) {
        var eski = kap.querySelector(':scope > .pk-cerceve');
        if (eski) eski.remove();
        continue;
      }
      var ad = cerceveKullanici(img);
      var anahtar = ad ? fmap[ad] : null;
      var mevcut = kap.querySelector(':scope > .pk-cerceve');
      // Kanal CANLIYSA çerçeve gösterilmez (kullanıcı isteği).
      if (anahtar && ad && kanalCanliMi(ad)) anahtar = null;
      if (!anahtar || !fdefs[anahtar]) { if (mevcut) mevcut.remove(); continue; }
      var boy = Math.round(r.width * CERCEVE_OLCEK);
      if (getComputedStyle(kap).position === 'static') kap.style.position = 'relative';
      /* Konum AVATARIN kap içindeki gerçek yerinden hesaplanır — `left:50%` KABA
         göre ortalıyordu. Kanal sayfasında kap avatarla aynı boyutta olduğu için
         (64x64) doğru görünüyordu; sohbet profil kartında kap `flex items-center
         gap-2` yani avatar+isim satırının tamamı (292x44) olduğu için çerçeve
         avatarın 120px SAĞINA düşüyordu (ölçüldü). */
      var kr = kap.getBoundingClientRect();
      var sol = Math.round(r.left - kr.left + r.width / 2);
      var ust = Math.round(r.top - kr.top + r.height / 2);
      if (mevcut && mevcut.getAttribute('data-pk-f') === anahtar &&
          Math.round(mevcut.getBoundingClientRect().width) === boy) {
        // Aynı çerçeve duruyor → yalnızca konumu tazele (kart/sayfa kaymış olabilir)
        if (mevcut.style.left !== sol + 'px') mevcut.style.left = sol + 'px';
        if (mevcut.style.top !== ust + 'px') mevcut.style.top = ust + 'px';
        continue;
      }
      if (mevcut) mevcut.remove();
      cerceveKirpmaAc(img);
      var c = document.createElement('img');
      c.className = 'pk-cerceve';
      c.setAttribute('data-pk-f', anahtar);
      c.src = fdefs[anahtar].image;
      c.alt = '';
      c.referrerPolicy = 'no-referrer';
      c.style.cssText = 'position:absolute;left:' + sol + 'px;top:' + ust + 'px;' +
        'transform:translate(-50%,-50%);' +
        'width:' + boy + 'px;height:' + boy + 'px;pointer-events:none;z-index:2;max-width:none';
      c.addEventListener('error', function () { if (c.parentNode) c.parentNode.removeChild(c); }, { once: true });
      kap.appendChild(c);
    }
  }

  /* 10.15 — KADEME ROZETİNİN İPUCU.
     Rozet görseli 3 aya kadar `sub1` kalıyor (kademeler 1·3·6·12…), ama
     kullanıcı 2. ayındaysa ipucunda "1 Aylık Destekçi" yazmak YANLIŞ BİLGİ.
     Sunucu gerçek kıdemi ayrıca gönderiyor (`u.ay`); tanımda `ay` alanı olan
     rozetlerde (yani kademe rozetlerinde) ipucunu o sayıdan kuruyoruz.
     Metin eklentinin kendi sözlüğünden geliyor → 7 dilde doğru; DB'deki
     etiket Türkçe sabit olduğu için onu kullanmak yabancı kullanıcıda
     Türkçe kalırdı. */
  function rozetIpucu(def, key, ad) {
    if (!def || !def.ay) return (def && def.label) || key || '';   // kademe rozeti değil
    var k = String(ad || '').trim().toLowerCase();
    var ay = (badgeMaps && badgeMaps.ayByName) ? badgeMaps.ayByName[k] : 0;
    /* Kullanıcının gerçek ayı yoksa KADEMENİN KENDİ eşiğine düşüyoruz —
       veritabanındaki etikete DEĞİL. Etiket Türkçe sabit olduğu için yabancı
       kullanıcıda Türkçe kalırdı; buradan üretilen metin 7 dilde doğru. */
    if (!ay || ay < 1) ay = def.ay;
    /* Tekil/çoğul ayrı anahtar: İspanyolca/Portekizce'de "1 meses" yanlış. */
    return t(ay === 1 ? 'pkAboneAy1' : 'pkAboneAy', [String(ay)]);
  }

  function makeBadge(def, key, ad) {
    var b = document.createElement('span'); b.className = 'pk-badge';
    var src = def.img || def.image;               // önce gömülü data-URI (anlık), yoksa URL
    if (src) {
      var img = document.createElement('img'); img.src = src; img.alt = def.label || ''; img.referrerPolicy = 'no-referrer';
      img.addEventListener('error', function () { // görsel yüklenemezse renkli yazı etiketine düş
        var pill = document.createElement('span'); pill.className = 'pk-pill';
        pill.style.background = (Array.isArray(def.color) ? def.color[0] : def.color) || '#53fc18'; pill.textContent = (def.label || '?').slice(0, 3);
        if (img.parentNode) img.parentNode.replaceChild(pill, img);
      }, { once: true });
      b.appendChild(img);
    } else {
      var pill = document.createElement('span'); pill.className = 'pk-pill';
      pill.style.background = (Array.isArray(def.color) ? def.color[0] : def.color) || '#53fc18'; pill.textContent = def.label || '?';
      b.appendChild(pill);
    }
    bindTip(b, rozetIpucu(def, key, ad));
    return b;
  }

  function usernameButton(msg) {
    // msg ya mesaj kabı ([data-index]) ya da doğrudan isim satırının kendisi
    // (sabitlenmiş mesaj / yanıt önizlemesi gibi [data-index] dışındaki bloklar).
    var cont = (msg.matches && msg.matches('[class*="items-baseline"]')) ? msg : msg.querySelector('[class*="items-baseline"]');
    if (cont) { var b = cont.querySelector('button'); if (b) return b; }
    var btns = msg.querySelectorAll('button');
    for (var i = 0; i < btns.length; i++) { var t = btns[i].textContent.trim(); if (t && t.length < 26) return btns[i]; }
    return null;
  }

  // Badge sahiplerinin ismini gradyan renklerle boyar (background-clip:text).
  // Renkler kullanıcının badge'lerinin color'larından gelir. Recycle'a dayanıklı (sig kontrolü).
  function applyNameGradient(ub, colors) {
    /* Hesaba özel akan isim varsa rozet gradyanı karışmaz: ikisi de aynı
       özelliklere `!important` basıyor, sırayla birbirini ezip ismi titretir. */
    if (ub.getAttribute('data-pk-ozelad')) return;
    var sig = colors.join(',');
    if (colors.length >= 1) {
      if (ub.getAttribute('data-pk-grad') === sig) return;
      var stops = colors.length >= 2 ? colors : [colors[0], colors[0]];
      ub.style.setProperty('background', 'linear-gradient(90deg,' + stops.join(',') + ')', 'important');
      ub.style.setProperty('-webkit-background-clip', 'text', 'important');
      ub.style.setProperty('background-clip', 'text', 'important');
      ub.style.setProperty('-webkit-text-fill-color', 'transparent', 'important');
      ub.style.setProperty('color', 'transparent', 'important');
      ub.setAttribute('data-pk-grad', sig);
    } else if (ub.getAttribute('data-pk-grad') != null) {   // artık badge yok → bizim gradyanı temizle
      ['background', '-webkit-background-clip', 'background-clip', '-webkit-text-fill-color', 'color'].forEach(function (p) { ub.style.removeProperty(p); });
      ub.removeAttribute('data-pk-grad');
    }
  }

  /* 10.15 — İSMİN BOYANDIĞI TEK KAPI.
     [[badge-render-rule]]: rozet nerede çıkıyorsa isim rengi de orada olacak.
     Akan (aboneye özel) ile durgun (rozet rengi) arasındaki seçimi her yüzeyde
     ayrı ayrı yazmak yerine burada bir kez veriyoruz — yeni bir yüzey eklenince
     yalnız bu işlev çağrılıyor, kural kendiliğinden geliyor.

     SIRA ÖNEMLİ: önce `ozelAdUygula`. Akan renk varsa düğüme `data-pk-ozelad`
     koyup boyuyor; `applyNameGradient` o işareti görünce erken dönüyor, yani
     üstüne yazmıyor. Akan yoksa `ozelAdUygula` kendi bıraktığı stili temizleyip
     (abonelik bitmiş olabilir) çekiliyor, durgun boyama devreye giriyor. */
  function isimBoya(el, ad) {
    if (!el) return;
    var k = String(ad || '').trim().toLowerCase();
    if (!k) return;
    try { ozelAdUygula(el, k); } catch (e) {}
    try {
      applyNameGradient(el, (badgeMaps.colorsByName && badgeMaps.colorsByName[k]) || []);
    } catch (e) {}
  }

  function processMessage(msg) {
    var ub = usernameButton(msg); if (!ub) return;
    var uname = ub.textContent.trim(); if (!uname) return;
    noteUser(uname);                       // görünen adı sorguya al (bilinmiyorsa çekilir)
    var cont = ub.parentElement; if (!cont) return;
    var keys = badgeKeysFor(uname);
    isimBoya(ub, uname);                   // isim rengi: akan (abone) ya da rozet gradyanı
    var existing = cont.querySelectorAll(':scope > .pk-badge');
    var have = []; for (var i = 0; i < existing.length; i++) have.push(existing[i].getAttribute('data-pk-key') || '');
    if (have.join('|') === keys.join('|')) return;               // doğru rozet(ler) zaten yerinde
    for (var j = 0; j < existing.length; j++) existing[j].remove(); // recycle/eski → temizle, sırayla yeniden bas
    for (var m = 0; m < keys.length; m++) {
      var def = badgeMaps.defs[keys[m]]; if (!def) continue;
      var el = makeBadge(def, keys[m], uname);
      el.setAttribute('data-pk-key', keys[m]);
      el.setAttribute('data-pk-for', uname.toLowerCase());
      cont.insertBefore(el, ub);
    }
  }

  /* YANIT ÖNİZLEMESİ — "X isimli kullanıcıya yanıt veriyor" satırındaki isim.
     Rozet kuralı gereği rozetin göründüğü her yüzeyde isim rengi + tooltip de olmalı;
     burada hiçbiri yoktu, yanıtlanan kişi rozetsiz görünüyordu.

     Canlı DOM'dan ölçülen yapı (tahmin değil):
       button.truncate
         ├ svg (yanıt oku, size-3)
         └ span.ml-1
             ├ span          ← yanıtlanan kullanıcı adı (sınıfsız, İLK span)
             ├ ": "
             ├ span.inline   ← alıntılanan mesaj metni
             └ " isimli kullanıcıya yanıt veriyor"

     Rozet boyutu em tabanlı (calc(1em*18/13)) olduğu için bu text-xs satırda
     kendiliğinden küçülür; ayrı bir ölçü vermeye gerek yok. */
  function yanitAlintiRozetleri() {
    if (!badgeMaps) return;
    var kaplar = document.querySelectorAll('button.truncate > span.ml-1');
    for (var i = 0; i < kaplar.length; i++) {
      var kap = kaplar[i];
      // İsim = kendi rozetimiz OLMAYAN ve mesaj metni (span.inline) OLMAYAN ilk span.
      // Rozeti ismin soluna eklediğimiz için "ilk çocuk" demek yeterli değil:
      // ikinci turda kendi rozetimizi isim sanardık.
      var ad = null;
      for (var c = kap.firstElementChild; c; c = c.nextElementSibling) {
        if (c.tagName !== 'SPAN') continue;
        if (c.classList.contains('pk-badge') || c.classList.contains('inline')) continue;
        ad = c; break;
      }
      if (!ad) continue;
      var uname = (ad.textContent || '').trim();
      if (!uname || uname.length > 25) continue;
      noteUser(uname);
      var keys = badgeKeysFor(uname);
      isimBoya(ad, uname);
      var eski = kap.querySelectorAll(':scope > .pk-badge');
      var mevcut = []; for (var j = 0; j < eski.length; j++) mevcut.push(eski[j].getAttribute('data-pk-key') || '');
      if (mevcut.join('|') === keys.join('|')) continue;          // doğru rozet zaten yerinde
      for (var k = 0; k < eski.length; k++) eski[k].remove();     // sanal liste geri dönüştürdü → yeniden bas
      for (var m = 0; m < keys.length; m++) {
        var def = badgeMaps.defs[keys[m]]; if (!def) continue;
        var b = makeBadge(def, keys[m], uname);
        b.setAttribute('data-pk-key', keys[m]);
        b.setAttribute('data-pk-for', uname.toLowerCase());
        kap.insertBefore(b, ad);
      }
    }
  }
  /* ══════════════════════════════════════════════════════════════════════
   * SOHBETÇİLER — kim ne zaman yazdı
   *
   * Kick'in kendi "Active chatters" listesi yalnızca BAĞLI kişileri gösterir;
   * kimin ne zaman konuştuğunu göstermez. Burada sohbeti dinleyip her kullanıcının
   * SON MESAJ ZAMANINI tutuyoruz; 10 dakikadır yazmayan "Çevrimdışı" sayılır.
   *
   * İki ölçülmüş dayanak (tahmin değil, canlı sayfada doğrulandı):
   *  1) Her mesajın artan bir `data-index`'i var. Sohbet SANALLAŞTIRILMIŞ liste,
   *     düğümleri geri dönüştürüyor — indeks olmasaydı ekranda duran eski mesajı
   *     her turda yeniden sayar, herkesi sonsuza dek "Sohbette" gösterirdik.
   *  2) Her mesaj kendi saatini taşıyor (`.text-neutral`, "SS:DD") → sayfa
   *     açıldığında ekranda hazır duran eski mesajlar da DOĞRU zamanla işlenir,
   *     yoksa hepsi "az önce" olurdu.
   * ══════════════════════════════════════════════════════════════════════ */
  var SC_ESIK = 10 * 60 * 1000;          // bu süredir yazmayan → Çevrimdışı
  var GIZLI_MESAJ_SINIR = 500;            // kalıcı listede en fazla kaç gizli mesaj
  /* Profil kartındaki listede AYNI ANDA görünen satır sayısı.
     Liste bununla KESİLMEZ — kutunun boyu bu kadar olur, gerisi kaydırılır.
     (Sohbet akışında açılan küçük liste ayrı: orası kesiliyor, çünkü
     sanallaştırılmış listenin içinde büyüyen bir kutu satır ölçüsünü bozar.) */
  var PK_KART_SATIR = 6;
  /* Bir satırın yüksekliği: `.pk-mesaj-satir` 14px/21px + 4px üst + 4px alt. */
  var PK_KART_SATIR_PX = 29;
  var scKanal = '';
  var scListe = Object.create(null);     // nick(küçük harf) -> { ad, ts, adet, mesajlar[] }
  var scGorulen = Object.create(null);   // "kapsam:indeks" -> işlendi
  var scGorulenAdet = 0;

  /* ---------- 10.5: OTURUM İSTATİSTİĞİ ----------
     Sohbetçiler motoru zaten her mesajı ayrıştırıp saklıyor; istatistik için
     ayrı bir tarama YAPMIYORUZ — aynı geçişte birkaç sayaç artırıyoruz.
     Maliyeti ihmal edilebilir, sunucuya tek bir istek bile gitmez. */
  var scBaslangic = 0;                      // oturumun ilk mesajının zamanı
  var scSonMesajTs = 0;                     // en son işlenen mesajın zamanı
  var scDakika = Object.create(null);       // "SS:DD" -> o dakikadaki mesaj sayısı
  var scEmoteSayac = Object.create(null);   // emote adı -> kaç kez kullanıldı
  var scEmoteSrc = Object.create(null);     // emote adı -> görsel adresi (sıralamada resim göstermek için)
  var scEmoteKisi = Object.create(null);    // nick -> { ad, emote, solo }
  var scTop = { mesaj: 0, kelime: 0, emote: 0, solo: 0, yanit: 0 };
  var scZirve = { dk: '', adet: 0 };        // sohbetin en yoğun dakikası
  var scIzleyici = { anlik: 0, top: 0, orn: 0, zirve: 0 };   // izleyici örneklemesi

  function scIstatSifirla() {
    scBaslangic = 0; scSonMesajTs = 0;
    scDakika = Object.create(null); scEmoteSayac = Object.create(null); scEmoteSrc = Object.create(null);
    scEmoteKisi = Object.create(null);
    scTop = { mesaj: 0, kelime: 0, emote: 0, solo: 0, yanit: 0 };
    scZirve = { dk: '', adet: 0 };
    scIzleyici = { anlik: 0, top: 0, orn: 0, zirve: 0 };
  }

  /* Tek bir mesajı sayaçlara işler. `pr` = scParcala çıktısı (parça dizisi). */
  function scIstatIsle(ad, ts, pr, yanitMi) {
    if (!scBaslangic || ts < scBaslangic) scBaslangic = ts;
    if (ts > scSonMesajTs) scSonMesajTs = ts;
    scTop.mesaj++;
    if (yanitMi) scTop.yanit++;

    var dk = scSaat(ts);
    var n = (scDakika[dk] = (scDakika[dk] || 0) + 1);
    if (n > scZirve.adet) { scZirve = { dk: dk, adet: n }; }

    var kk = ad.toLowerCase();
    var ek = scEmoteKisi[kk] || (scEmoteKisi[kk] = { ad: ad, emote: 0, solo: 0 });
    ek.ad = ad;

    var emote = 0, yazi = '';
    for (var i = 0; i < pr.length; i++) {
      if (pr[i].t === 'e') {
        emote++;
        var a = pr[i].a || '?';
        scEmoteSayac[a] = (scEmoteSayac[a] || 0) + 1;
        if (pr[i].s && !scEmoteSrc[a]) scEmoteSrc[a] = pr[i].s;   // sıralamada resim gösterebilmek için
      }
      else yazi += ' ' + pr[i].v;
    }
    scTop.emote += emote;
    ek.emote += emote;
    var kelimeler = yazi.trim() ? yazi.trim().split(/\s+/).length : 0;
    scTop.kelime += kelimeler;
    // "sadece emote" = hiç yazı yok ama en az bir emote var
    if (emote > 0 && kelimeler === 0) { scTop.solo++; ek.solo++; }
  }

  /* İzleyici sayısını Kick'in kendi başlığından okur (ağ isteği YOK). */
  function scIzleyiciOrnekle() {
    var n = 0;
    var ogeler = document.querySelectorAll('span, div');
    for (var i = 0; i < ogeler.length; i++) {
      var e = ogeler[i];
      if (e.children.length) continue;
      var t = (e.textContent || '').trim();
      if (!/^[0-9][0-9.,]*$/.test(t)) continue;
      var ana = e.parentElement && (e.parentElement.textContent || '');
      /* Kick iki farklı yazım kullanıyor: "9.905 İzleyici" ve "4.017 kişi izliyor".
         Eskiden yalnız "izleyici" aranıyordu ve ikinci düzende sayı hiç bulunamıyordu. */
      if (!ana || !/izleyici|izliyor|viewer|watching/i.test(ana)) continue;
      n = parseInt(t.replace(/[.,]/g, ''), 10);
      break;
    }
    if (!n || isNaN(n)) return;
    scIzleyici.anlik = n;
    scIzleyici.top += n; scIzleyici.orn++;
    if (n > scIzleyici.zirve) scIzleyici.zirve = n;
  }

  /* ---------- 10.5: BAHSEDİLMELER ----------
     Sohbette senden bahsedilen mesajları ayrı bir kutuda toplar. Ağ isteği yok:
     zaten ayrıştırdığımız mesaj parçalarında ad aranır. Ad, siteye bağlıysa
     hesabından; değilse ayarlardan girilen addan gelir (birden fazla olabilir). */
  var PK_MENTION_SINIR = 200;
  var pkMentionList = [];        // { ad, ts, p }
  var pkMentionYeni = 0;         // görülmemiş sayısı (rozet için)
  /* Liste açıldı ama henüz "okundu" saymadık — sayfadan çıkışta sayılacak.
     Bkz. bolumBahsedilme / pkMentionOkunduUygula. */
  var pkMentionOkuBekle = false;
  var pkBenimAd = '';            // siteye bağlı hesabın adı

  /* ══════════════════════════════════════════════════════════════════════
   * BİLDİRİM SESİ
   *
   * Ses DOSYA DEĞİL, WebAudio ile üretiliyor: pakete tek bayt eklemiyor,
   * web_accessible_resources gerektirmiyor ve mağaza incelemesinde
   * açıklanacak yeni bir varlık çıkmıyor.
   * ══════════════════════════════════════════════════════════════════════ */
  var PK_SES_SECENEK = ['cinlama', 'bip', 'damla', 'puf'];
  var pkSesCtx = null;
  var pkSesSon = 0;        // son çalma anı — üst üste bahsedilmede tek ses
  var pkSesAcilis = 0;     // bu andan önce yakalananlar SESSİZ (bkz. aşağısı)

  function pkSesTonu() {
    var k = settings.bahsetSesTon;
    return PK_SES_SECENEK.indexOf(k) >= 0 ? k : 'cinlama';
  }
  function pkSesSeviyesi() {
    var v = parseInt(settings.bahsetSesSeviye, 10);
    if (!isFinite(v)) v = 60;
    return Math.max(0, Math.min(100, v));
  }
  function pkSesAdi(k) {
    if (k === 'bip')   return t('pkSesBip');
    if (k === 'damla') return t('pkSesDamla');
    if (k === 'puf')   return t('pkSesPuf');
    return t('pkSesCinlama');
  }

  /* Bağlam TEK ve TEMBEL: her seste yenisini açmak tarayıcının bağlam
     sınırına (~6) takılıyor ve bir süre sonra ses hiç çıkmıyor. */
  function pkSesBaglam() {
    if (pkSesCtx) return pkSesCtx;
    var C = window.AudioContext || window.webkitAudioContext;
    if (!C) return null;
    try { pkSesCtx = new C(); } catch (e) { return null; }
    return pkSesCtx;
  }

  /* Tek nota. Zarf HER NOTADA ayrı: dalga birden kesilince "tık" duyuluyor.
     Sıfıra üstel rampa çalışmadığı için 0.0001'e iniyoruz. */
  function pkSesNota(ctx, cikis, tur, hz, hz2, t0, sure) {
    var o = ctx.createOscillator();
    var g = ctx.createGain();
    o.type = tur;
    o.frequency.setValueAtTime(hz, t0);
    if (hz2) o.frequency.exponentialRampToValueAtTime(hz2, t0 + sure);
    g.gain.setValueAtTime(0.0001, t0);
    g.gain.exponentialRampToValueAtTime(1, t0 + 0.012);
    g.gain.exponentialRampToValueAtTime(0.0001, t0 + sure);
    o.connect(g); g.connect(cikis);
    o.start(t0); o.stop(t0 + sure + 0.02);
  }

  /* Argümansız çağrılırsa ayarlardaki tonu/seviyeyi kullanır. */
  function pkBildirimSesi(ton, seviye) {
    var ctx = pkSesBaglam(); if (!ctx) return;
    /* Sekme arka plandayken bağlam askıya alınıyor; uyandırmadan çalarsak
       ses hiç duyulmaz. */
    if (ctx.state === 'suspended') { try { ctx.resume(); } catch (e) {} }
    var ana = ctx.createGain();
    /* 0.22 tepe: WebAudio ham genliği yüksek, 1.0 kulakta çok sert. */
    ana.gain.value = (seviye == null ? pkSesSeviyesi() : seviye) / 100 * 0.22;
    ana.connect(ctx.destination);
    var k = ton || pkSesTonu();
    var t0 = ctx.currentTime + 0.01;
    if (k === 'bip')        pkSesNota(ctx, ana, 'square',   880, 0,    t0, 0.09);
    else if (k === 'damla') pkSesNota(ctx, ana, 'sine',    1200, 420,  t0, 0.22);
    else if (k === 'puf')   pkSesNota(ctx, ana, 'triangle', 520, 340,  t0, 0.13);
    else {                  pkSesNota(ctx, ana, 'sine',     880, 0,    t0, 0.14);
                            pkSesNota(ctx, ana, 'sine',    1320, 0,    t0 + 0.11, 0.20); }
  }

  /* OTOMATİK OYNATMA KİLİDİ. Tarayıcı, kullanıcı sayfayla etkileşmeden
     AudioContext'in ses çıkarmasına izin vermiyor — bağlam 'suspended'
     doğuyor ve çalma isteği sessizce yutuluyor. Yenilemeden sonraki İLK
     bahsedilme bu yüzden duyulmazdı. İlk gerçek tıklamada/tuşta bağlamı
     uyandırıyoruz; bir kez yetiyor, sonrası serbest.
     `capture` ve `passive`: Kick'in kendi dinleyicilerinden önce çalışsın,
     kaydırmayı geciktirmesin. */
  var pkSesKilitAcildi = false;
  function pkSesKilitAc() {
    /* Özellik kapalıyken bağlam bile açmıyoruz — kullanmayan kullanıcıya
       bedeli sıfır. Açıldığı an zaten ayar düğmesine basılıyor, o tıklama
       kilidi kendisi açıyor. */
    if (pkSesKilitAcildi || settings.bahsetSes !== true) return;
    var c = pkSesBaglam(); if (!c) return;
    pkSesKilitAcildi = true;
    if (c.state === 'suspended') { try { c.resume(); } catch (e) {} }
  }
  try {
    document.addEventListener('pointerdown', pkSesKilitAc, { capture: true, passive: true });
    document.addEventListener('keydown', pkSesKilitAc, { capture: true, passive: true });
  } catch (e) {}

  function pkMentionSifirla() {
    pkMentionList = []; pkMentionYeni = 0; pkMentionOkuBekle = false;
    /* Kanala girerken EKRANDA DURAN eski mesajlar da taranıyor. O turda ses
       çalarsak kanala girer girmez arka arkaya bip geliyor. Kısa bir sessiz
       pencere: ilk tarama bunun içinde bitiyor, gerçek bir bahsedilme ise
       zaten pencereden sonra geliyor. */
    pkSesAcilis = Date.now() + 4000;
  }

  /* Bekleyen "okundu" damgasını uygular. Bahsedilmeler sayfası kapanınca ya da
     panelde başka bir sayfaya geçilince çağrılıyor. */
  function pkMentionOkunduUygula() {
    if (!pkMentionOkuBekle) return;
    pkMentionOkuBekle = false;
    pkMentionYeni = 0;
    try { renderBadge(); } catch (e) {}
  }

  /* Aranacak adlar: hesap adı + kullanıcının elle girdikleri. Küçük harfe indirilir. */
  function pkMentionAdlari() {
    var out = [];
    /* Ad sırası: site bağlantısı → sayfadaki açık hesap → elle girilenler.
       Elle giriş arayüzden kaldırıldı ama eski kullanıcılarda kayıtlı kalmış
       olabilir, onu da okumaya devam ediyoruz. */
    var ben = pkKendiAdim();
    if (ben) out.push(pkKucult(ben));
    var ek = takipKelimeleri();
    for (var i = 0; i < ek.length; i++) if (out.indexOf(ek[i]) < 0) out.push(ek[i]);
    return out;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * TAKİP EDİLEN KELİMELER
   *
   * Kullanıcının virgülle yazdığı kelimeler. İKİ İŞ birden yapıyorlar:
   *  · adın gibi takip ediliyorlar → bahsedilmeler listesi, sayaç, ses
   *  · geçtikleri mesajlar öncelikli kişilerdeki gibi yeşil şeritle işaretleniyor
   * Ham metin `settings.mentionAd`; ayrıştırılmış hâli burada ÖNBELLEKLİ,
   * çünkü sohbet turunda satır başına okunuyor.
   * ══════════════════════════════════════════════════════════════════════ */
  var kelimeHam = null, kelimeListe = [], kelimeYazim = [];
  function takipKelimeAyristir() {
    var ham = settings.mentionAd || '';
    if (ham === kelimeHam) return;
    kelimeHam = ham;
    kelimeListe = []; kelimeYazim = [];
    var p = ham.split(',');
    for (var i = 0; i < p.length; i++) {
      var g = p[i].trim();
      var a = pkKucult(g);
      /* Tekrar denetimi KATLANMIŞ hâl üzerinden: "Kod" ile "KOD" aynı
         kelime, listede iki kez durmasın. */
      if (!a || kelimeListe.indexOf(a) >= 0) continue;
      kelimeListe.push(a);      // eşleştirme için
      kelimeYazim.push(g);      // kullanıcıya gösterim için
    }
  }
  /* Eşleştirmede kullanılan katlanmış liste. */
  function takipKelimeleri() { takipKelimeAyristir(); return kelimeListe; }
  /* Kutuda ve etiketlerde gösterilen hâl: kullanıcının YAZDIĞI gibi.
     Katlanmışını göstermek "ISLIK yazdım, islik oldu" gibi görünür ve
     kelimeyi bozduğumuz izlenimi verir — oysa yalnız karşılaştırma katlanıyor. */
  function takipKelimeYazi() { takipKelimeAyristir(); return kelimeYazim; }

  /* Satırda takip edilen kelimelerden biri geçiyor mu?

     İKİ AŞAMALI. Önce satırın ham metninde ucuz tarama: `textContent` tek bir
     DOM okuması, JS gezinmesi yok. Kelime satırın HİÇBİR yerinde yoksa mesajda
     da yoktur — satırların neredeyse tamamı burada eleniyor. Ancak eşleşme
     çıkarsa `mesajMetniAl` ile doğruluyoruz: ham metin kullanıcı adını, saati
     ve bizim eklediğimiz etiketleri de içeriyor, onlardan gelen eşleşme
     sayılmamalı. Pahalı yol yalnız gerçek adaylarda çalışıyor.

     Kelime sınırı `kelimeGecti` ile — bahsedilme listesiyle AYNI kural.
     Ayrışırlarsa mesaj yeşil olup listede çıkmaz (ya da tersi) ve kullanıcı
     sebebini anlayamaz. */
  function kelimeVurgusuVar(m) {
    var kl = takipKelimeleri();
    if (!kl.length) return false;
    /* Ön eleme, doğrulamayla AYNI küçültmeyi kullanmalı: aksi hâlde
       büyük harfli satır burada elenir, `kelimeGecti` hiç çalışmaz. */
    var ham = pkKucult(m.textContent);
    if (!ham) return false;
    var i, aday = false;
    for (i = 0; i < kl.length && !aday; i++) if (ham.indexOf(kl[i]) >= 0) aday = true;
    if (!aday) return false;
    var metin = '';
    try { metin = mesajMetniAl(m); } catch (e) { return false; }
    for (i = 0; i < kl.length; i++) if (kelimeGecti(metin, kl[i])) return true;
    return false;
  }

  function pkMentionBak(ad, ts, pr) {
    if (!settings.bahsedilme) return;
    var adlar = pkMentionAdlari();
    if (!adlar.length) return;
    /* KENDİ MESAJIN denetimi YALNIZ kendi adına bakar — `adlar` listesinin
       tamamına DEĞİL. Liste artık takip edilen kelimeleri de taşıyor:
       kullanıcı "seboi" kelimesini takibe alırsa, Seboi adlı kişinin
       BÜTÜN mesajları "kendi mesajım" sayılıp atılıyordu — ne listeye
       düşüyordu ne ses çıkıyordu, üstelik sohbette yeşil şerit göründüğü
       için özellik çalışıyormuş gibi duruyordu. (Canlıda bulundu.)
       Ad henüz çözülmemişse (`ben` boş) atlamıyoruz: bir sonraki turda
       ad gelince kendiliğinden düzeliyor. */
    var ben = pkKucult(pkKendiAdim());
    if (ad && ben && pkKucult(ad) === ben) return;
    var yazi = '';
    for (var i = 0; i < pr.length; i++) if (pr[i].t === 'y') yazi += ' ' + pr[i].v;
    yazi = yazi.toLowerCase();
    if (!yazi.trim()) return;
    var buldu = false;
    for (var j = 0; j < adlar.length && !buldu; j++) {
      // Kelime sınırı kontrolü ortak `kelimeGecti` yardımcısında (10.5 —
      // profil kartındaki "gelen yanıtlar" da aynı kurala ihtiyaç duydu).
      if (kelimeGecti(yazi, adlar[j])) buldu = true;
    }
    if (!buldu) return;
    pkMentionList.push({ ad: ad, ts: ts, p: pr });
    if (pkMentionList.length > PK_MENTION_SINIR) pkMentionList.shift();
    pkMentionYeni++;
    renderBadge();
    /* SES: sayaçtan bağımsız. Rozeti kapatıp sesi açık bırakan kullanıcı
       kırmızı rakam görmeden haberdar olmak istiyor. */
    if (settings.bahsetSes === true) {
      var simdi = Date.now();
      /* Tek mesajda birden çok ad geçebilir ya da art arda bahsedilebilir;
         her biri için ayrı ses üst üste binip gürültü oluyor. */
      if (simdi >= pkSesAcilis && simdi - pkSesSon >= 1200) {
        pkSesSon = simdi;
        try { pkBildirimSesi(); } catch (e) {}
      }
    }
  }

  /* ---------- 10.5: KULLANICI NOTLARI ----------
     Kişi başına yerel not. Sunucuya gitmez, yalnızca chrome.storage.local'da durur.
     Profil kartında görünür ve orada düzenlenir. */
  var pkNotlar = Object.create(null);      // nick(küçük) -> metin

  function notOku(nick) { return pkNotlar[String(nick || '').toLowerCase()] || ''; }
  function notYaz(nick, metin) {
    var k = String(nick || '').toLowerCase(); if (!k) return;
    metin = String(metin || '').slice(0, 500);
    if (metin) pkNotlar[k] = metin; else delete pkNotlar[k];
    try { chrome.storage.local.set({ pkNotlar: pkNotlar }); } catch (e) {}
  }

  /* Mesaj gövdesini ayıklar. Balonun yapısı ÖLÇÜLDÜ:
   *   [span.text-neutral "04:45"] [div(rozetler + isim butonu)] [span ":"] [span metin]
   * Bu yüzden İSİM KABINDAN SONRAKİ düğümler alınır — Kick'in kimlik rozetleri
   * (Seviye 37, 5-Aylık Abone…) isimden ÖNCE durduğu için doğal olarak dışarıda
   * kalır. İlk denemede tüm satırı okuyunca o rozet adları metne karışıyordu.
   * Emoteler <img> → alt metni (ör. "KEKW"), alıntı önizlemesi çıkarılır. */
  /* Çıktı: parça dizisi — {t:'y', v:'yazı'} veya {t:'e', s:'emote adresi', a:'ad'}.
     Düz metin yerine parçalar saklanır ki profil kartında EMOTELER GÖRSEL olarak
     çıksın (önce alt metni yazıyorduk, "KEKW" diye görünüyordu). innerHTML asla
     kullanılmaz; parçalardan düğüm kurulur. */
  /* Sohbet balonuna BİZİM eklediğimiz düğüm mü? `pk-` ile başlayan sınıf ya
     da `data-pk-*` niteliği taşıyorsa bizimdir ve mesaj içeriği sayılmaz. */
  function pkBizimDugum(n) {
    if (!n || n.nodeType !== 1) return false;
    try {
      var c = n.classList;
      if (c) for (var i = 0; i < c.length; i++) if (c[i].indexOf('pk-') === 0) return true;
      var a = n.attributes;
      if (a) for (var j = 0; j < a.length; j++) if (a[j].name.indexOf('data-pk') === 0) return true;
    } catch (e) {}
    return false;
  }

  /* SAYDAM SARMAL — bizim düğümümüz ama Kick'in KENDİ metnini sarıyor, metin
     EKLEMİYOR. `pkBizimDugum` bunları da eliyordu ve sonuç ağırdı:
     `bahsetTiklanabilir` "@ad"i tıklanabilir yapmak için `pk-bahset`
     span'ına sarıyor; o an ad mesaj METNİNDEN DÜŞÜYORDU.

     Canlıda ölçüldü: "@pumpzera" yazılı mesajın gövdesi BOŞ çıkıyordu.
     Yani bahsedilme yakalanmıyor, sohbet istatistiği/araması o parçayı
     görmüyor, toplu timeout ve kelime vurgusu da kaçırıyordu. Turda
     `bahsetTiklanabilir` (15) `sohbetTara`dan (21) ÖNCE çalıştığı için
     sarma HER ZAMAN taramadan önce oluyor — arada sıra yarışı bile yok.

     METİN EKLEYEN düğümlerimiz (rozet, "(silindi)", "Göster", bağlantı
     önizlemesi) elenmeye devam ediyor: onlar mesajın parçası değil. */
  function pkSaydamSarmal(n) {
    return !!(n && n.classList && n.classList.contains('pk-bahset'));
  }

  function scParcala(m) {
    var ub = m.querySelector('button.inline.font-bold'); if (!ub) return [];
    var ad = ub.parentElement, balon = ad && ad.parentElement; if (!balon) return [];
    var out = [];
    function yaz(s) {
      if (!s) return;
      var son = out[out.length - 1];
      if (son && son.t === 'y') son.v += s; else out.push({ t: 'y', v: s });
    }
    function gez(n) {
      if (n.nodeType === 3) { yaz(n.nodeValue); return; }
      if (n.nodeType !== 1) return;
      /* BİZE AİT hiçbir düğüm mesaj içeriği değildir. Tek tek sınıf saymak
         yerine kalıba bakıyoruz: rozet, bağlantı önizleme, "(silindi)"
         etiketi, "Göster" düğmesi, saat etiketi… hepsi buradan eleniyor.
         (Canlıda görüldü: kayıtta "Deleted by a moderator (silindi)Göster"
         yazıyordu — kendi eklediklerimizi okuyorduk.) */
      if (pkBizimDugum(n) && !pkSaydamSarmal(n)) return;
      if (n.tagName === 'BUTTON' && n.classList && n.classList.contains('truncate')) return;  // alıntılı yanıt önizlemesi
      if (n.tagName === 'IMG') {
        var src = n.getAttribute('src') || '', alt = (n.getAttribute('alt') || '').trim();
        if (src) out.push({ t: 'e', s: src, a: alt }); else yaz(alt ? ' ' + alt + ' ' : '');
        return;
      }
      for (var c = n.firstChild; c; c = c.nextSibling) gez(c);
    }
    var gor = false;
    for (var n = balon.firstChild; n; n = n.nextSibling) {
      if (n === ad) { gor = true; continue; }
      if (!gor) continue;
      if (n.nodeType === 1 && (n.textContent || '').trim() === ':' && !n.querySelector('img')) continue;
      gez(n);
    }
    // baştaki/sondaki boşlukları kırp, uzun metni sınırla
    for (var i = 0; i < out.length; i++) {
      if (out[i].t !== 'y') continue;
      out[i].v = out[i].v.replace(/\s+/g, ' ');
      if (i === 0) out[i].v = out[i].v.replace(/^[\s:：]+/, '');
      if (i === out.length - 1) out[i].v = out[i].v.replace(/\s+$/, '');
      if (out[i].v.length > 200) out[i].v = out[i].v.slice(0, 200) + '…';
    }
    return out.filter(function (x) { return x.t !== 'y' || x.v !== ''; });
  }

  /* KANAL KİMLİĞİ — bütün sohbet özellikleri buna bakıyor.

     Normal sayfa: kick.com/<kanal> (tek parçalı yol).
     MODERATÖR PANOSU: dashboard.kick.com/moderator/<kanal> — iki parçalı
     olduğu için eskiden kanal SAYILMIYORDU ve panoda turumuz hiç çalışmıyordu
     (canlıda ölçüldü: PK düğmesi yok, oysa dişli, sohbet kutusu, altlık hepsi
     yerinde). Oysa orası gerçek bir sohbet ve moderatör vaktinin çoğunu orada
     geçiriyor. Kimliği oradan da çıkarıyoruz: PK düğmesi, bahsedilmeler,
     istatistik, moderasyon paneli panoda da çalışsın.

     Panonun panel düğümleri de `data-index` taşıyor ama sohbet satırı ayrımı
     `sohbetSatirlari()` içinde zaten yapılıyor (kap + sayısal indis). */
  function scMevcutKanal() {
    var p = (location.pathname || '').split('/').filter(Boolean);
    if (/(^|.)dashboard.kick.com$/i.test(location.hostname || '')) {
      /* PANODA YALNIZ /moderator/<kanal>. kick.com'un "tek parçalı yol =
         kanal" kuralını buraya taşımak yanlış olurdu: /stream, /analytics
         gibi yollar kanal sanılırdı (testte yakalandı, "stream" adlı bir
         kanal uyduruyordu). */
      return (p.length === 2 && p[0].toLowerCase() === 'moderator') ? p[1].toLowerCase() : '';
    }
    /* POPOUT SOHBET: /popout/<kanal>/chat — Kick'in kendi küçük sohbet
       penceresi. Yol ÜÇ parçalı olduğu için "tek parça = kanal" kuralına
       takılmıyordu ve PureKick bu sayfada hiç çalışmıyordu. Otomatik mesajın
       arka plan evi burası: kullanıcı pencereyi açık bırakıyor, duyuru oradan
       gidiyor, kanalın kendi sekmesinde durmak gerekmiyor. */
    if (p.length === 3 && p[0].toLowerCase() === 'popout' && p[2].toLowerCase() === 'chat') {
      return p[1].toLowerCase();
    }
    return (p.length === 1 && !NON_CHANNEL[p[0].toLowerCase()]) ? p[0].toLowerCase() : '';
  }
  /* Mesajın kendi saatinden zaman damgası. Saat okunamazsa şimdiki zaman. */
  function scZaman(m) {
    var t = m.querySelector('.text-neutral');
    var s = t && (t.textContent || '').trim();
    var mm = s && s.match(/^(\d{1,2}):(\d{2})$/);
    if (!mm) return Date.now();
    var d = new Date(); d.setHours(+mm[1], +mm[2], 0, 0);
    var ts = d.getTime();
    if (ts > Date.now() + 120000) ts -= 86400000;   // gece yarısını devretmiş mesaj
    return ts;
  }
  /* İki yolun (DOM ve soket) aynı mesaj için ürettiği anahtar. Boşluk ve
     büyük/küçük harf normalize edilir: DOM metni ile soketin ham metni
     arasındaki ufak biçim farkları aynı mesajı iki kez saydırmasın. */
  function scOzet(pr) {
    var o = '';
    for (var q = 0; q < pr.length && o.length < 80; q++) o += (pr[q].t === 'y' ? pr[q].v : pr[q].a);
    return o.replace(/\s+/g, '').toLowerCase().slice(0, 60);
  }
  function scAnahtar(ad, ts, pr) {
    return ad.toLowerCase() + '|' + scSaat(ts) + '|' + scOzet(pr);
  }

  /* ZAMANSIZ anahtar + son görülme zamanı. Dakikaya çivilenmiş anahtar
     kırılgandı: soket sunucu saatini, DOM tarama satırdaki saati kullanıyor
     ve saat okunamazsa `scZaman` TARAMA ANINI uyduruyor. İki yol dakika
     sınırının iki yanına düşünce aynı mesaj ikinci kez giriyordu. */
  var SC_TEKRAR_PAY = 90000;                 // 90 sn
  var scSonGorulme = Object.create(null);    // 'ad|özet' -> ts
  function scZamansizAnahtar(ad, pr) { return ad.toLowerCase() + '|' + scOzet(pr); }

  /* ══════════════════════════════════════════════════════════════════════
   * OTURUM KURTARMA
   * Sekme kazara kapanırsa toplanan sohbet verisi kaybolmasın. Kanal başına
   * diske yazılır; 15 dakika içinde aynı yayına dönülürse geri yüklenir.
   * ══════════════════════════════════════════════════════════════════════ */
  var OT_DB = 'purekick', OT_MAGAZA = 'oturum', OT_OMUR = 15 * 60 * 1000;   // 15 dk
  var otDb = null, otSonYazma = 0, otSonImza = '';

  function otAc(bitince) {
    if (otDb) { bitince(otDb); return; }
    var istek;
    try { istek = indexedDB.open(OT_DB, 1); } catch (e) { bitince(null); return; }
    istek.onupgradeneeded = function () {
      try { istek.result.createObjectStore(OT_MAGAZA, { keyPath: 'slug' }); } catch (e) {}
    };
    istek.onsuccess = function () { otDb = istek.result; bitince(otDb); };
    istek.onerror = function () { bitince(null); };
  }

  /* Diske yazılacak durum. Bellekteki sınırlar (500 mesaj/kişi, 4000 kişi)
     zaten uygulandığı için ayrıca budamaya gerek yok. */
  function otToparla() {
    return {
      liste: scListe, baslangic: scBaslangic, sonTs: scSonMesajTs,
      dakika: scDakika, emoteSayac: scEmoteSayac, emoteSrc: scEmoteSrc,
      emoteKisi: scEmoteKisi, top: scTop, zirve: scZirve, izleyici: scIzleyici,
      mention: pkMentionList, gunluk: modGunluk, silinen: silinenler,
      sustur: aktifSusturma, susturAd: susturmaAdi, rol: kisiRol
    };
  }

  function otDagit(v) {
    if (!v) return;
    if (v.liste) scListe = v.liste;
    scBaslangic = v.baslangic || 0; scSonMesajTs = v.sonTs || 0;
    if (v.dakika) scDakika = v.dakika;
    if (v.emoteSayac) scEmoteSayac = v.emoteSayac;
    if (v.emoteSrc) scEmoteSrc = v.emoteSrc;
    if (v.emoteKisi) scEmoteKisi = v.emoteKisi;
    if (v.top) scTop = v.top;
    if (v.zirve) scZirve = v.zirve;
    if (v.izleyici) scIzleyici = v.izleyici;
    if (v.mention) pkMentionList = v.mention;
    if (v.gunluk) modGunluk = v.gunluk;
    if (v.silinen) silinenler = v.silinen;
    if (v.sustur) aktifSusturma = v.sustur;
    if (v.susturAd) susturmaAdi = v.susturAd;
    if (v.rol) kisiRol = v.rol;
    /* `scGorulen` YENİDEN KURULMAZ: o yalnız aynı mesajı iki kez saymamak
       için. Geri yüklemeden sonra sohbette görünen eski mesajlar yeniden
       taranırsa çift sayım olmasın diye anahtarları da geri koyuyoruz. */
    scGorulen = Object.create(null); scGorulenAdet = 0;
    for (var k in scListe) {
      var m = scListe[k] && scListe[k].mesajlar;
      if (!m) continue;
      for (var i = 0; i < m.length; i++) {
        var a = scAnahtar(scListe[k].ad, m[i].ts, m[i].p);
        if (!scGorulen[a]) { scGorulen[a] = 1; scGorulenAdet++; }
      }
    }
  }

  /* Boş oturumu diske yazma — kanaldan çıkarken bellek boşalıyor ve bir
     sonraki tur iyi kaydın üstüne boş kayıt yazardı. */
  function otBosMu() { return !scBaslangic && !modGunluk.length && !pkMentionList.length; }

  function otKaydet(slug, zorla) {
    if (!slug || otBosMu()) return;
    var imza = scTop.mesaj + '|' + modGunluk.length + '|' + pkMentionList.length;
    if (!zorla && imza === otSonImza) return;              // değişmediyse yazma
    otSonImza = imza;
    otAc(function (db) {
      if (!db) return;
      try {
        var t2 = db.transaction(OT_MAGAZA, 'readwrite');
        t2.objectStore(OT_MAGAZA).put({ slug: slug, ts: Date.now(), veri: otToparla() });
      } catch (e) {}
    });
  }

  function otSil(slug) {
    otAc(function (db) {
      if (!db) return;
      try { db.transaction(OT_MAGAZA, 'readwrite').objectStore(OT_MAGAZA).delete(slug); } catch (e) {}
    });
  }

  /* Süresi dolmuş bütün kayıtları temizle — disk boşuna şişmesin. */
  function otBudama() {
    otAc(function (db) {
      if (!db) return;
      try {
        var st = db.transaction(OT_MAGAZA, 'readwrite').objectStore(OT_MAGAZA);
        var im = st.openCursor();
        im.onsuccess = function () {
          var c = im.result; if (!c) return;
          if (Date.now() - (c.value && c.value.ts || 0) > OT_OMUR) { try { c.delete(); } catch (e) {} }
          c.continue();
        };
      } catch (e) {}
    });
  }

  /* Kanala girerken çağrılır. 15 dakikadan yeni kayıt varsa geri yükler. */
  function otYukle(slug) {
    if (!slug) return;
    otAc(function (db) {
      if (!db) return;
      try {
        var st = db.transaction(OT_MAGAZA, 'readonly').objectStore(OT_MAGAZA);
        var im = st.get(slug);
        im.onsuccess = function () {
          var kayit = im.result;
          if (!kayit || !kayit.veri) return;
          if (Date.now() - (kayit.ts || 0) > OT_OMUR) { otSil(slug); return; }
          /* Yükleme asenkron: bu arada kullanıcı başka kanala geçmiş olabilir. */
          if (scKanal !== slug) return;
          /* Zaten veri toplandıysa üstüne yazma. */
          if (!otBosMu()) return;
          try { otDagit(kayit.veri); } catch (e) { return; }
          try { toast(t('pkTOturumGeriYuklendi')); } catch (e) {}
        };
      } catch (e) {}
    });
  }

  /* KANAL SAYFASINDA MIYIZ? /<kanal> ve /<kanal>/videos/<id> evet;
     ana sayfa, gözat, kategori, takip edilenler, droplar HAYIR.
     `scMevcutKanal` yalnız canlı kanal yolunu kabul ediyor, VOD'u değil —
     oynatıcı özellikleri VOD'da da lazım olduğu için ikisini birleştiriyoruz. */
  function kanalSayfasi() {
    return !!scMevcutKanal() || !!vodSayfasi();
  }

  /* Kanal değişti mi — sohbetTara ve soket işleyicisi ortak kullanır. */
  function scKanalKontrol() {
    var k = scMevcutKanal();
    if (!k) {
      /* KANALDAN CIKTIK (ana sayfa, gozat, ayarlar, VOD...).
         ESKIDEN BURADA HICBIR SEY YAPILMIYORDU: veri yalnizca BASKA bir
         kanala gecince siliniyordu. Ana sayfaya ugrayip AYNI kanala donunce
         `k === scKanal` oluyor, sifirlama calismiyor ve ilk ziyaretin
         sohbet kaydi/istatistigi oldugu gibi geri geliyordu.
         Kullanici kurali: veri yalnizca O YAYINDAYKEN yasar. */
      /* Kanaldan çıkarken SON HÂLİ diske yaz, sonra belleği boşalt.
         Disk kalır: 15 dakika içinde dönersen geri yüklenir. */
      if (scKanal) { otKaydet(scKanal, true); scKanal = ''; yayinBittiSifirla(); }
      return '';
    }
    if (k !== scKanal) {
      if (scKanal) otKaydet(scKanal, true);          // öncekini diske bırak
      scKanal = k; scListe = Object.create(null);
      scGorulen = Object.create(null); scGorulenAdet = 0;
      scRoster = Object.create(null); scKickSayi = 0; scSayacYazdik = '';
      scIstatSifirla(); pkMentionSifirla(); olaySifirla(); kanalDurumSifirla();
      topluToSifirla();
      topluSilSifirla();
      omSifirla();
      otBudama();                                     // süresi dolanları at
      otYukle(k);                                     // 15 dk içindeyse geri yükle
    }
    return k;
  }

  /* Bir mesajı sayaçlara işler. İki yol da buraya girer; anahtar zaten
     görülmüşse hiçbir şey yapmaz. Böylece DOM ve soket çakışmaz. */
  function scMesajIsle(ad, ts, pr, yanitMi, id) {
    if (!ad) return false;
    /* İKİ anahtar: mesaj kimliği (kesin) ve içerik (yedek). Kimlik her zaman
       elde olmuyor — worker-hook damgalayana kadar DOM'da yok. Bu yüzden
       ikisi de kaydediliyor ve ikisi de kontrol ediliyor; mesaj hangi yoldan
       önce gelirse gelsin ikinci kez sayılmıyor. */
    var kimlikAnahtar = id ? '#' + id : '';
    var icerikAnahtar = scAnahtar(ad, ts, pr);
    if (kimlikAnahtar && scGorulen[kimlikAnahtar]) return false;   // aynı kimlik → kesin aynı

    /* ══════════════════════════════════════════════════════════════════════
     * İÇERİK ÇAKIŞMASI İKİ AYRI ŞEY OLABİLİR
     *
     *   a) AYNI mesajın ikinci yolu (soket + DOM taraması aynı mesajı görür)
     *   b) Kullanıcı AYNI METNİ TEKRAR yazdı — spam tam olarak budur
     *
     * Eskiden ikisi de yineleme sayılıyordu. Sonuç: 41 kez "a" yazan biri
     * listeye yalnız 2 kayıt bırakıyordu ve toplu mesaj silme de o 2'sini
     * siliyordu (kullanıcı bildirdi — özelliğin asıl işi bu, bulunması iyi
     * oldu). İstatistikteki mesaj sayısı da aynı ölçüde eksikti.
     *
     * AYIRT EDİCİ: (a) durumunda listede AYNI İÇERİKLİ, KİMLİKSİZ bir kayıt
     * vardır — DOM taraması mesajı worker-hook damgalamadan görmüştür.
     * Kimliği ona işleyip yinelemeyi atıyoruz. Öyle bir kayıt yoksa ve
     * elimizde kimlik varsa bu YENİ bir mesajdır.
     *
     * Kimlik hiç yoksa ayırt edemiyoruz; o hâlde eski davranış sürüyor
     * (yineleme say) — çift saymak, eksik saymaktan daha zararlı.
     * ══════════════════════════════════════════════════════════════════════ */
    function scKimliksizEslesme() {
      var v0 = scListe[ad.toLowerCase()];
      if (!v0 || !v0.mesajlar) return null;
      var oz = scOzet(pr);
      /* Sondan geriye, sınırlı: ikinci yol saniyeler içinde geliyor, spam
         listesinin tamamını taramak her mesajda pahalı olurdu. */
      var alt = Math.max(0, v0.mesajlar.length - 20);
      for (var q = v0.mesajlar.length - 1; q >= alt; q--) {
        var m0 = v0.mesajlar[q];
        if (!m0.id && scOzet(m0.p) === oz) return m0;
      }
      return null;
    }
    function scYinelemeMi() {
      var esk = scKimliksizEslesme();
      if (esk) {
        /* (a) — kimliği eskiye işliyoruz ki toplu silme onu adresleyebilsin. */
        if (kimlikAnahtar) { esk.id = id; scGorulen[kimlikAnahtar] = 1; scGorulenAdet++; }
        return true;
      }
      return !kimlikAnahtar;
    }

    if (scGorulen[icerikAnahtar] && scYinelemeMi()) return false;
    /* PAYLI DENETİM: dakika anahtarının kaçırdığı durumu yakalıyor (iki yolun
       saatleri dakika sınırının iki yanına düştüğünde). Aynı ayırt edici
       buraya da uygulanıyor. */
    var zAnahtar = scZamansizAnahtar(ad, pr);
    var oncekiTs = scSonGorulme[zAnahtar];
    if (oncekiTs != null && Math.abs(ts - oncekiTs) <= SC_TEKRAR_PAY && scYinelemeMi()) return false;

    var kk = ad.toLowerCase(), v = scListe[kk];
    scRoster[kk] = 1;
    if (!v) {
      v = scListe[kk] = { ad: ad, ts: ts, adet: 0, mesajlar: [] };
    }
    v.ad = ad; if (ts > v.ts) v.ts = ts; v.adet++;
    if (kimlikAnahtar) { scGorulen[kimlikAnahtar] = 1; scGorulenAdet++; }
    scGorulen[icerikAnahtar] = 1; scGorulenAdet++;
    scSonGorulme[zAnahtar] = ts;
    if (pr.length) {
      /* SINIR YOK (kullanıcı kararı). Oturum zaten yayından/kanaldan
         çıkınca siliniyor; ömür boyu birikme riski yok. */
      /* KİMLİK DE SAKLANIYOR: toplu mesaj silme geçmişi tararken sileceği
         mesajı ancak kimliğiyle adresleyebiliyor (uçtaki yol
         /chatrooms/{oda}/messages/{kimlik}). Kimliksiz kayıt geçmişte
         silinemez — canlı kipte sorun yok, orada kimlik elde. */
      v.mesajlar.push({ ts: ts, p: pr, id: id || '' });
    }
    try { scIstatIsle(ad, ts, pr, !!yanitMi); } catch (e) {}
    try { pkMentionBak(ad, ts, pr); } catch (e) {}
    /* TOPLU TIMEOUT canlı kipi. Kapalıyken tek karşılaştırma; açıkken
       mesaj daha ekrana çizilmeden susturma isteği gidiyor. */
    try { topluToBak(ad, pr); } catch (e) {}
    /* TOPLU MESAJ SİLME canlı kipi — kimliği de alıyor, silinecek adres o. */
    try { topluSilBak(ad, pr, id); } catch (e) {}
    /* OTOMATİK MESAJ: mesaj sayacı ve komut cevabı. */
    try { otoMesajBak(ad, pr); } catch (e) {}
    /* Bellek emniyeti: sayfa saatlerce açık kalırsa işaret kümesi şişmesin. */
    if (scGorulenAdet > 20000) {
      scGorulen = Object.create(null); scGorulenAdet = 0;
      scSonGorulme = Object.create(null);      // aynı emniyet bunu da kapsasın
    }
    return true;
  }

  /* Kick'in ham mesaj metnini parçalara böler. Emoteler metinde
     `[emote:12345:ad]` biçiminde geliyor — DOM'daki <img alt="ad"> ile aynı
     ada karşılık gelir, bu yüzden iki yolun özeti tutuyor. */
  function scParcalaMetin(metin) {
    var out = [], s = String(metin || '');
    function yaz(x) {
      if (!x) return;
      var son = out[out.length - 1];
      if (son && son.t === 'y') son.v += x; else out.push({ t: 'y', v: x });
    }
    var re = /\[emote:(\d+):([^\]]*)\]/g, son = 0, m;
    while ((m = re.exec(s))) {
      yaz(s.slice(son, m.index));
      /* Adres kalıptan kuruluyor: Kick emote görselleri
         files.kick.com/emotes/<id>/fullsize (canlıda doğrulandı). Boş
         bırakılırsa <img src=""> kırık resim çiziyordu. */
      out.push({ t: 'e', s: 'https://files.kick.com/emotes/' + m[1] + '/fullsize', a: m[2] || '?' });
      son = m.index + m[0].length;
    }
    yaz(s.slice(son));
    return out;
  }

  /* Soketten gelen sohbet mesajı. ARKA PLAN SEKMESİNDE DE ÇALIŞIR — toplamanın
     bir süre sonra durmasının sebebi buranın bağlı olmamasıydı. */
  function scOlaydanMesaj(d) {
    if (!scKanalKontrol()) return;
    if (!d || !d.kim) return;
    scMesajIsle(d.kim, d.ts || Date.now(), scParcalaMetin(d.metin), !!d.yanit, d.id || '');
  }

  function sohbetTara() {
    if (!scKanalKontrol()) return;
    /* HATA DÜZELTMESİ — "işlendi" işareti SU SEVİYESİ değil, KÜME.
       Eskiden en büyük indeks tutuluyor ve satır okunamasa bile seviye
       yükseltiliyordu: React satırı henüz doldurmadan gördüğümüz mesaj
       (kullanıcı adı yok → `continue`) bir daha asla işlenmiyordu. Küme ile
       okunamayan satır bir sonraki turda yeniden denenir, çift de sayılmaz.

       NOT: bu tarama artık TEK kaynak değil — asıl akış soketten geliyor
       (scOlaydanMesaj). Burası sayfaya girmeden önce yazılmış mesajları ve
       kanca kurulamadığı durumu karşılıyor. Anahtar ortak olduğu için aynı
       mesaj iki yoldan gelse de bir kez sayılır. */
    var msgs = sohbetSatirlari();
    for (var i = 0; i < msgs.length; i++) {
      var m = msgs[i];
      var ub = usernameButton(m); if (!ub) continue;   // satır henüz hazır değil
      var ad = (ub.textContent || '').trim(); if (!ad) continue;
      /* Anahtar = kim + hangi dakika + metin. `data-index` KULLANILMAZ:
         Kick sohbet tamponunu kırpınca indeksler kayıyor, aynı mesaj yeni
         indeksle ikinci kez sayılıyordu. Yan Sohbet'in indeksleri de 0'dan
         başlayıp ana sohbetle çakışıyor — bu anahtarla o sorun da yok. */
      /* Silinmiş olarak işaretlediğimiz satırı okuma: Kick içeriği
         "Deleted by a moderator" ile değiştirdi, orijinal zaten elimizde. */
      if (m.getAttribute('data-pk-silindi')) continue;
      scMesajIsle(ad, scZaman(m), scParcala(m), !!m.querySelector('button.truncate'),
                  m.getAttribute('data-pk-mid') || '');
    }
  }
  function scGecen(ms) {
    var dk = Math.floor(ms / 60000);
    if (dk < 1) return t('pkAzOnce');
    if (dk < 60) return t('pkDkOnce', [String(dk)]);
    var sa = Math.floor(dk / 60);
    if (sa < 24) return t('pkSaOnce', [String(sa)]);
    return t('pkGunOnce', [String(Math.floor(sa / 24))]);
  }
  function scKisa(ms) {                       // dar satıra sığan biçim
    var dk = Math.floor(ms / 60000);
    if (dk < 60) return dk + ' dk';
    var sa = Math.floor(dk / 60);
    return sa < 24 ? sa + ' sa' : Math.floor(sa / 24) + ' g';
  }

  /* ---- Kick'in KENDİ "Active chatters" satırlarına durum yazısı ekle ----
   * Yeni bir liste çizmiyoruz; Kick'inkinin içine giriyoruz.
   *
   * Satır YAPIYA GÖRE bulunur, metne göre değil (başlık dile göre değişiyor):
   * doğrudan çocukları yuvarlak bir <img> ve `span.min-w-0.flex-1.truncate` olan
   * <button>. Ölçüm: satır 40px, `gap-4`, `p-2 pl-6`, isim `text-base` 16/400.
   * İsim hücresi `flex-1` olduğu için sona eklediğimiz etiket sağa yaslanır.
   *
   * Liste SANALLAŞTIRILMIŞ (285 kişiden ~100'ü DOM'da) ve React satırları
   * yeniden kullanıyor → her turda yeniden yazılır, kaydırınca da doğru kalır. */
  var scFiltre = 'hepsi';                 // hepsi | online | offline
  var scRoster = Object.create(null);     // oturumda GÖRÜLEN herkes (yazan + listede beliren)
  var scKickSayi = 0, scSayacYazdik = '';

  function sohbetciEtiketle() {
    var simdi = Date.now();
    var isimler = document.querySelectorAll('button > span.min-w-0.flex-1.truncate');
    for (var i = 0; i < isimler.length; i++) {
      var isimEl = isimler[i], btn = isimEl.parentElement;
      if (!btn || !/state-layer-surface/.test(String(btn.className || ''))) continue;
      var av = btn.querySelector(':scope > img');
      if (!av || !/rounded-full/.test(String(av.className || ''))) continue;   // sohbetçi satırı değil
      var ad = (isimEl.textContent || '').trim();
      if (!ad) continue;
      scRoster[ad.toLowerCase()] = 1;                       // sayaç için: bu kişiyi gördük
      var v = scListe[ad.toLowerCase()];
      var et = btn.querySelector(':scope > .pk-sc');
      var canli = !!v && (simdi - v.ts) < SC_ESIK;
      // Filtre: "online" = son 10 dk içinde yazmış · "offline" = yazmamış ya da eskimiş
      var gizle = (scFiltre === 'online' && !canli) || (scFiltre === 'offline' && canli);
      if (btn.style.display !== (gizle ? 'none' : '')) btn.style.display = gizle ? 'none' : '';
      if (!v) { if (et) et.remove(); continue; }             // hiç yazmamış → bilgimiz yok, uydurma
      /* 10.14 — durum etiketi AYRI ayar. Kapalıyken basılmaz, basılmışsa
         kaldırılır. Üstteki `scRoster` sayımı ve filtre bundan ETKİLENMEZ —
         sayaç ve filtre kendi anahtarlarıyla yönetiliyor. */
      if (settings.scEtiket === false) { if (et) et.remove(); continue; }
      if (!et) { et = el('span', 'pk-sc'); btn.appendChild(et); }
      var gecen = simdi - v.ts;
      var yazi = canli ? t('pkSohbette') : t('pkCevrimdisiEki', [scKisa(gecen)]);
      if (et.getAttribute('data-pk-y') === yazi) continue;   // değişmediyse DOM'a dokunma
      et.setAttribute('data-pk-y', yazi);
      et.className = 'pk-sc shrink-0 text-sm ' + (canli ? 'text-primary-base' : 'text-subtle');
      et.textContent = yazi;
      et.title = t('pkSonMesajiIpucu', [scGecen(gecen), String(v.adet)]);
    }
    scBosGruplar();
  }

  /* Filtre yüzünden tek satırı kalmayan grubu (Moderators/VIPs/Chatters) BAŞLIĞIYLA
     birlikte gizle — yoksa "Moderators" yazısı boşlukta asılı kalıyor (ölçüldü).
     Grup yapısı: DIV[ H3(başlık) , …, UL.list-none > LI > button ]. */
  function scBosGruplar() {
    var uller = document.querySelectorAll('ul.list-none');
    for (var u = 0; u < uller.length; u++) {
      var satirlar = uller[u].querySelectorAll('li > button');
      if (!satirlar.length) continue;
      var acik = false;
      for (var s = 0; s < satirlar.length; s++) if (satirlar[s].style.display !== 'none') { acik = true; break; }
      var grup = uller[u];
      for (var g = 0; g < 4 && grup.parentElement; g++) {
        grup = grup.parentElement;
        if (grup.firstElementChild && grup.firstElementChild.tagName === 'H3') break;
      }
      if (!grup.firstElementChild || grup.firstElementChild.tagName !== 'H3') continue;
      var d = acik ? '' : 'none';
      if (grup.style.display !== d) grup.style.display = d;
    }
  }

  /* Başlıktaki sayı: <h2> içinde iki <span> var, ikincisi "(296)" (yapıya göre bulunur).
   *
   * Kick bu sayıyı kendi listesine göre düşürebiliyor — bir saattir yayını izleyip
   * yüzlerce kişi görmüşken 100'e inmesi bilgi kaybı. Bu yüzden ekranda
   * max(Kick'in sayısı, oturumda GÖRDÜĞÜMÜZ benzersiz kişi) gösteriyoruz; bizim
   * küme yalnızca büyüdüğü için sayı geri gitmiyor.
   *
   * Kendi yazdığımızı geri okuyup Kick'in gerçek değerini kaybetmemek için en son
   * yazdığımız metni saklıyoruz: metin ondan farklıysa Kick yeniden çizmiş demektir. */
  function scSayacEl() {
    var h2ler = document.querySelectorAll('h2');
    for (var i = 0; i < h2ler.length; i++) {
      var ch = h2ler[i].children;
      if (ch.length === 2 && /^\(\s*[\d.,]+\s*\)$/.test((ch[1].textContent || '').trim())) return ch[1];
    }
    return null;
  }
  function sohbetciSayac() {
    var e = scSayacEl(); if (!e) return;
    /* 10.14 — kapalıyken KICK'İN KENDİ sayısı kalır: üstüne yazdıysak geri
       koyar ve ipucunu kaldırırız. İşaret (scSayacYazdik) boşaltıldığı için
       sonraki turlarda bu blok bir daha iş yapmaz. */
    if (settings.scSayac === false) {
      if (scSayacYazdik && (e.textContent || '').trim() === scSayacYazdik) {
        if (scKickSayi) e.textContent = '(' + scKickSayi + ')';
        scSayacYazdik = '';
      }
      if (e.title) e.removeAttribute('title');
      return;
    }
    var metin = (e.textContent || '').trim();
    if (metin !== scSayacYazdik) {                 // Kick yeniden çizmiş → gerçek sayıyı al
      var n = parseInt(metin.replace(/\D/g, ''), 10);
      if (!isNaN(n)) scKickSayi = n;
    }
    var bizim = Object.keys(scRoster).length;
    var yeni = '(' + Math.max(scKickSayi, bizim) + ')';
    if (metin !== yeni) { e.textContent = yeni; scSayacYazdik = yeni; }
    e.title = t('pkSohbetciIpucu', [String(scKickSayi), String(bizim)]);
  }

  /* Arama kutusunun altına Tümü / Sohbette / Çevrimdışı filtresi.
     Kick'in kendi ölçüleriyle: h-8 · text-sm · font-semibold · rounded (4px). */
  var SC_FILTRELER = [{ id: 'hepsi', ad: t('pkTumu') }, { id: 'online', ad: t('pkSohbette') }, { id: 'offline', ad: t('pkCevrimdisi') }];
  /* 10.14 — çubuk kapatılınca yalnız çubuğu SİLMEK yetmez: seçim "online"da
     kalmışsa liste filtreli (satırlar display:none) kalırdı. Seçimi "hepsi"ye
     alıp etiketlemeyi bir kez daha koşturuyoruz ki gizlenenler geri gelsin. */
  function scFiltreTemizle() {
    var d = document.querySelectorAll('.pk-scf');
    for (var i = 0; i < d.length; i++) d[i].remove();
    if (scFiltre !== 'hepsi') { scFiltre = 'hepsi'; try { sohbetciEtiketle(); } catch (e) {} }
  }

  function sohbetciFiltreCiz() {
    if (settings.scFiltreCubugu === false) { scFiltreTemizle(); return; }
    var sayac = scSayacEl(); if (!sayac) return;
    var h2 = sayac.parentElement, header = h2 && h2.parentElement;
    var sec = header && header.parentElement;
    if (!sec || sec.children.length < 3) return;
    var mevcut = sec.querySelector(':scope > .pk-scf');
    if (!mevcut) {
      mevcut = el('div', 'pk-scf flex items-center gap-1.5 shrink-0');
      /* Yatay boşluk YOK: çubuk panelin bir kenarından diğerine yapışır (arama
         kutusundaki 12px içerlek hizaya değil, tam köşelere). Üstte boşluk yok,
         arama kabının kendi 12px'i zaten var. */
      mevcut.style.cssText = 'padding:0 0 12px';
      sec.insertBefore(mevcut, sec.children[2]);      // arama kutusundan sonra, listeden önce
    }
    if (mevcut.getAttribute('data-pk-f') === scFiltre) return;   // sadece seçim değişince yeniden çiz
    mevcut.setAttribute('data-pk-f', scFiltre);
    mevcut.textContent = '';
    SC_FILTRELER.forEach(function (f) {
      var secili = scFiltre === f.id;
      /* `flex-1` + `min-w-0`: üç buton kalan genişliği eşit paylaşır → sohbet paneli
         genişleyince uzar, daralınca kısalır; yazı her zaman ortalı. */
      var b = el('button', 'flex-1 min-w-0 h-8 px-1 rounded text-sm font-semibold transition-all ' +
        'flex items-center justify-center whitespace-nowrap ' +
        (secili ? 'bg-secondary-base text-white' : 'text-subtle betterhover:hover:bg-surface-highest'), f.ad);
      b.addEventListener('click', function (ev) {
        ev.preventDefault(); ev.stopPropagation();
        scFiltre = f.id; sohbetciFiltreCiz(); sohbetciEtiketle();
      });
      mevcut.appendChild(b);
    });
  }

  /* Bağlantı önizleme KENDİ adımı. Eskiden rozet çiziminin içindeydi ve
     rozet verisi gelmeyince (sunucu yavaş/429/çevrimdışı) düğme hiç
     çıkmıyordu — iki özelliğin birbiriyle ilgisi yok. */
  function lpTum() {
    if (!settings.consented) return;
    /* 10.14 — "Bağlantıları sayfa içinde aç" KAPALIYKEN düğme hiç basılmasın.
       Eskiden yalnız tıklama yakalama (sekmeIciTikla) ayara bakıyordu; düğme
       ayardan bağımsız basıldığı için kullanıcı ayarı kapatınca "hâlâ önizle
       gösteriyor" oluyordu. */
    if (settings.sekmeIci !== true) return;
    var msgs = sohbetSatirlari();
    for (var i = 0; i < msgs.length; i++) { try { lpMesaj(msgs[i]); } catch (e) {} }
  }

  /* Ayar kapatılınca ekranda duran önizleme düğmelerini toplar ve işaretlerini
     siler — yeniden açılınca aynı mesajlara tekrar basılabilsin diye. */
  function lpTemizle() {
    try {
      var d = document.querySelectorAll('.pk-lp');
      for (var i = 0; i < d.length; i++) d[i].remove();
      var m = document.querySelectorAll('[data-pk-lp]');
      for (var j = 0; j < m.length; j++) m[j].removeAttribute('data-pk-lp');
      lpKapat();
    } catch (e) {}
  }

  function processAllMessages() {
    if (!badgeActive() || !badgeMaps) return;
    var msgs = sohbetSatirlari();
    for (var i = 0; i < msgs.length; i++) { try { processMessage(msgs[i]); } catch (e) {} }
    // SABİTLENMİŞ MESAJ (ve yanıt önizlemesi gibi bloklar): sohbet mesajıyla aynı
    // yapıda ama [data-index] İÇİNDE DEĞİL → yukarıdaki tarama kaçırıyordu.
    // Kullanıcı adının geçtiği her yerde rozet görünsün diye burayı da işleriz.
    // Kanal "Hakkında" başlığı, izleyici sayacı gibi bloklar da items-baseline
    // kullanıyor → yalnızca İÇİNDE METİNLİ BİR BUTON (kullanıcı adı) olanları işleriz.
    var extra = document.querySelectorAll('[class*="items-baseline"]');
    for (var k = 0; k < extra.length; k++) {
      try {
        var blk = extra[k];
        if (pkSatirBul(blk)) continue;
        var nb = blk.querySelector('button');
        if (!nb || !(nb.textContent || '').trim()) continue;
        processMessage(blk);
      } catch (e) {}
    }
  }

  /* ---- Profil kartı (chat'te isme tıklayınca açılan popup) rozetleri ----
   * Kick'in "Rozetler" bölümü dile göre değişir (Badges/Insignias...); o yüzden
   * ETİKET metnine göre DEĞİL, Kick'in kendi badge img'ine (src*="/chat/badges/")
   * göre kutuyu buluruz → dil-bağımsız. Kullanıcının hiç Kick rozeti yoksa satırı
   * kendimiz oluştururuz (etiket i18n'den). Rozetler Kick'inkiyle aynı 18px + hover tooltip. */
  function makePopupBadge(def, key, ad) {
    var w = document.createElement('div');
    w.className = 'pk-pop-badge inline-flex shrink-0 items-center';
    w.style.cssText = 'height:18px;cursor:inherit';
    var fallback = function () {
      var s = document.createElement('span');
      s.style.cssText = 'width:18px;height:18px;border-radius:4px;display:block;background:' + ((Array.isArray(def.color) ? def.color[0] : def.color) || '#53fc18');
      return s;
    };
    var src = def.img || def.image;               // önce gömülü data-URI (anlık), yoksa URL
    if (src) {
      var img = document.createElement('img'); img.src = src; img.alt = def.label || ''; img.width = 18; img.height = 18; img.referrerPolicy = 'no-referrer';
      img.style.cssText = 'width:18px;height:18px;object-fit:contain;display:block';
      img.addEventListener('error', function () { if (img.parentNode) img.parentNode.replaceChild(fallback(), img); }, { once: true });
      w.appendChild(img);
    } else { w.appendChild(fallback()); }
    bindTip(w, rozetIpucu(def, key, ad));   // Kick gibi: üstüne gelince isim baloncuğu
    return w;
  }
  /* KICK'İN ROZET SARMALAYICISI — TEK KAYNAK.
   * Kutuyu bulan kod da mükerrer satırı süpüren kod da bunu kullanır. Eskiden
   * iki ayrı kopyaydı; biri düzeltilip diğeri unutulunca mükerrer satır bir
   * daha hiç temizlenmiyordu.
   *
   * `size-[…]` ŞARTI YOK — canlıda ölçüldü: SEVİYE rozetinin sarmalayıcısı
   * `inline-flex shrink-0 items-center text-[18px]`, size sınıfı taşımıyor.
   * Şartı koyunca yalnız seviye rozeti olan kullanıcıda hiçbir şey eşleşmiyor
   * ve AYRI bir "Rozetler" satırı açılıyordu.
   * Yanlış eşleşme riski yok: kartta bu desene uyan yalnız rozetler var;
   * Kick'in butonları `shrink-0` taşımıyor ve zaten <button>.
   * :not(.pk-pop-badge) ŞART — rozetlerimizi kutunun BAŞINA ekliyoruz, aksi
   * halde ilk eşleşen kendi rozetimiz olur ve kutuyu bulamayız. */
  var PK_KICK_ROZET = 'div.inline-flex.shrink-0.items-center:not(.pk-pop-badge)';

  /* Kick'in rozet satırını YAPIDAN bulur — etiket metnine bakmadan, çünkü
   * metin dile göre değişiyor (Rozetler/Badges/Insignias…). Satır: kabı
   * `w-fit` olan bir <span> etiket + hemen ardından gelen kutu. Kullanıcının
   * hiç rozeti yoksa sarmalayıcı bulunmaz ama satır durabilir; bu yol o
   * durumda da Kick'in kutusunu bulur, biz ikinci satır açmayız. */
  function kickRozetSatiri(card) {
    var etiketler = card.querySelectorAll('div[class*="w-fit"] > span');
    for (var i = 0; i < etiketler.length; i++) {
      var kutu = etiketler[i].nextElementSibling;
      if (kutu && kutu.tagName === 'DIV' && !etiketler[i].closest('[data-pk-rozet]')) return kutu;
    }
    return null;
  }

  /* Kick'in "Rozetler" satırındaki rozet KUTUSUNU bulur (dil-bağımsız). */
  /* Kick'in rozet KUTUSUNU bulur; BULAMAZSA null döner — kendi satırımızı
     AÇMIYORUZ.

     Kick'te her kullanıcının en az bir seviye rozeti var, yani Kick'in satırı
     her zaman geliyor. Bulamadığımız tek an, satır henüz boyanmadan bizim
     koşmamız. Eskiden o anda kendi satırımızı açıyorduk ve Kick'inki gelince
     ekranda İKİ "Rozetler" satırı kalıyordu. Artık o turu boş geçiyoruz;
     çağıran `if (!box) return;` diyor ve bir sonraki turda satır yerinde
     oluyor. Mükerrer satır böylece yapısal olarak imkânsız. */
  function popupBadgeBox(card) {
    var wrap = card.querySelector(PK_KICK_ROZET);
    var kutu = (wrap && wrap.parentElement) ? wrap.parentElement : kickRozetSatiri(card);
    if (!kutu) return null;
    /* Eski sürümden kalmış satır varsa (sekme açıkken güncellendiyse) temizle. */
    var dup = card.querySelector('[data-pk-rozet]');
    if (dup) dup.remove();
    return kutu;
  }
  function injectPopupBadges(card, nameEl) {
    var uname = nameEl.textContent.trim(); if (!uname) return;
    noteUser(uname);                       // profil kartındaki adı da sorguya al
    isimBoya(nameEl, uname);               // profil kartında da aynı isim rengi
    var keys = badgeKeysFor(uname); if (!keys.length) return;
    var box = popupBadgeBox(card); if (!box) return;
    var existing = box.querySelectorAll(':scope > .pk-pop-badge');
    var have = []; for (var i = 0; i < existing.length; i++) have.push(existing[i].getAttribute('data-pk-key') || '');
    if (have.join('|') === keys.join('|')) return;                   // doğru rozet(ler) zaten yerinde
    for (var j = 0; j < existing.length; j++) existing[j].remove();
    // Rozetlerimiz Kick'inkilerin ÖNÜNE (satırın en başına) gelsin — sırayı koruyarak.
    var firstOrig = null, ch = box.children;
    for (var q = 0; q < ch.length; q++) { if (!ch[q].classList.contains('pk-pop-badge')) { firstOrig = ch[q]; break; } }
    for (var m = 0; m < keys.length; m++) {
      var def = badgeMaps.defs[keys[m]]; if (!def) continue;
      var el = makePopupBadge(def, keys[m], uname); el.setAttribute('data-pk-key', keys[m]);
      if (firstOrig) box.insertBefore(el, firstOrig); else box.appendChild(el);
    }
  }
  /* Bu kabın içinde BAŞKA bir profil kartı adayı var mı? Varsa bu dış kap
     demektir ve işlenmemeli — yoksa aynı kart iki kez işlenir. */
  function pkIcAdayVar(kap) {
    var ic = kap.querySelectorAll('div.bg-surface-highest');
    for (var i = 0; i < ic.length; i++) {
      if (ic[i].querySelector('a.select-text.font-semibold')) return true;
    }
    return false;
  }

  /* MÜKERRER "Rozetler" SATIRI SÜPÜRGESİ.

     Kick rozetlerini kartın geri kalanından BİR KARE SONRA basıyor. O aradaki
     turda kendi satırımızı açıyoruz; Kick'inki gelince ikisi birden görünüyor.
     Eski temizlik yalnız o an işlenen kartın İÇİNE bakıyordu ve üç durumda
     hiç çalışmıyordu:
       1) kullanıcının PureKick rozeti yoksa injectPopupBadges erken dönüyor,
          temizliğe hiç uğranmıyor;
       2) satır iç içe kart adaylarından DIŞTAKİNE eklendiyse o kart artık
          atlanıyor (pkIcAdayVar) ve bir daha ziyaret edilmiyor;
       3) kart yeniden çizilince kopan satır DOM'da asılı kalıyor.

     Süpürge her turda BÜTÜN belgeyi tarıyor: aynı kartta Kick'in kendi rozet
     sarmalayıcısı varsa bizim satır fazlalıktır. Kullanıcının hiç Kick rozeti
     yoksa sarmalayıcı da yoktur ve satırımız yerinde kalır. */
  function mukerrerRozetSatiriTemizle() {
    /* `data-pk-rozet` yalnızca BİZİM açtığımız satırın işaretiydi ve artık
       satır açmıyoruz; kalan varsa eski sürümden kalmadır (sekme açıkken
       güncelleme). Koşulsuz siliyoruz — eski koşul "Kick'in satırını
       görebiliyor muyum" diye soruyordu, yani hatayı doğuran yarış
       durumunun aynısına bağlıydı ve çoğu zaman temizleyemiyordu. */
    var satirlar = document.querySelectorAll('[data-pk-rozet]');
    for (var i = 0; i < satirlar.length; i++) satirlar[i].remove();
  }

  /* ══════════════════════════════════════════════════════════════════════
   * PROFİL KARTINI GENİŞLET
   *
   * Kick'in kartı dar; içine eklediğimiz bloklar (bilgi satırları, notlar,
   * mesaj sekmeleri, hızlı moderasyon) sıkışıyordu.
   *
   * ÖLÇÜ HEM KARTA HEM KABINA yazılıyor: kart mutlak konumlu bir kabın
   * içinde ve genişliği oradan alıyor — yalnız karta yazmak kabı dar
   * bırakıp taşmaya yol açıyor.
   *
   * TAŞMA DÜZELTMESİ `margin-left` İLE, `transform` İLE DEĞİL: sürükleme
   * özelliği (`surukleYap`) kabın `transform`unu kendi kullanıyor; oraya
   * yazmak sürüklemeyi bozardı. Kick kartı sağ kenara yakın açtığında
   * genişleme ekran dışına taşıyor, bir kez sola çekiyoruz.
   *
   * YALNIZ BİR KEZ: işaret konduktan sonra bir daha ölçüye dokunmuyoruz,
   * yoksa kullanıcı kartı sürüklerken her turda geri çekerdik.
   * ══════════════════════════════════════════════════════════════════════ */
  var KART_GENIS_PX = 420;
  var KART_GENIS_IM = 'data-pk-genis';

  function kartOlcuSil(e) {
    if (!e) return;
    ['width', 'max-width', 'margin-left',
     'background-color', 'border', 'border-radius', 'box-shadow']
      .forEach(function (p) { e.style.removeProperty(p); });
  }

  function kartGenislet(kart) {
    if (!kart) return;
    var kap = null;
    try { kap = tasinacakKap(kart); } catch (e) {}
    if (settings.kartGenis === false) {
      if (kart.getAttribute(KART_GENIS_IM)) {
        kart.removeAttribute(KART_GENIS_IM);
        kartOlcuSil(kart);
        if (kap && kap !== kart) kartOlcuSil(kap);
      }
      return;
    }
    if (kart.getAttribute(KART_GENIS_IM)) return;      // ölçü zaten verildi
    kart.setAttribute(KART_GENIS_IM, '1');
    /* Dar ekranda kart pencereyi aşmasın: istenen genişlik ile ekranın
       izin verdiği arasından küçüğü seçiliyor. */
    var g = Math.max(280, Math.min(KART_GENIS_PX, window.innerWidth - 24));
    /* ÖLÇÜ YALNIZ KABA. Karta da yazmak KAYMAYA yol açıyordu: kartla kap
       arasında bir ara sarmalayıcı var (`flex cursor-auto flex-col gap-2`) ve
       kabın iç boşluğu kadar dar. Kart o sarmalayıcıdan 32px taşıyor,
       KARDEŞİ olan moderasyon paneli ise dar kalıyordu — canlıda ölçüldü:
       kap 420, ara 388, kart 420, kardeş 388.
       Yalnız kaba yazınca ara sarmalayıcı ve İKİ KARDEŞ de 388'e oturuyor;
       kart yine eskisinden (324) belirgin geniş. */
    if (kap && kap !== kart) {
      kap.style.setProperty('width', g + 'px', 'important');
      kap.style.setProperty('max-width', 'calc(100vw - 24px)', 'important');
      /* ÇERÇEVE + ZEMİN. Kart yayının/sayfanın üstünde süzülüyordu, arkası
         okunmayı zorlaştırıyordu. Zemin rengi Kick'in KENDİ kap rengi
         (ölçüldü: rgb(23,26,28)) — uydurma bir renk değil; moderasyon
         görünümündeki kartın zemini de bu. Kenarlık ve gölge kartı
         arkasındaki içerikten ayırıyor. */
      kap.style.setProperty('background-color', 'rgb(23,26,28)', 'important');
      kap.style.setProperty('border', '1px solid rgba(255,255,255,.10)', 'important');
      kap.style.setProperty('border-radius', '10px', 'important');
      kap.style.setProperty('box-shadow', '0 12px 32px rgba(0,0,0,.55)', 'important');
    } else {
      /* Kap bulunamadıysa (yapı değişmiş) karta yazmaktan başka yol yok. */
      kart.style.setProperty('width', g + 'px', 'important');
      kart.style.setProperty('max-width', 'calc(100vw - 24px)', 'important');
    }
    try {
      var hedef = kap || kart;
      var r = hedef.getBoundingClientRect();
      var tasma = Math.round(r.right - (window.innerWidth - 8));
      if (tasma > 0) hedef.style.setProperty('margin-left', (-tasma) + 'px');
    } catch (e) {}
  }

  /* 13. GELİŞMİŞ PROFİL KARTI & KOLEKSİYON TEMASI MOTORU */
  function injectProfilKartiTemasi(card, username) {
    if (!card || !username) return;
    var kap = tasinacakKap(card) || card;
    if (kap.getAttribute('data-pk-holo-kart')) return;
    kap.setAttribute('data-pk-holo-kart', '1');

    chrome.storage.local.get(['pkProfilKartlari', 'settings'], function (res) {
      var s = (res && res.settings) || settings || {};
      var profiller = (res && res.pkProfilKartlari) || {};
      var uKey = username.toLowerCase();
      var benKey = (s.mentionAd || localStorage.getItem('pk_user_name') || '').toLowerCase();

      var profil = profiller[uKey] || (benKey && uKey === benKey ? s : null) || s;
      
      var holoEfekt = profil.holoKartEfekt || 'pikachu';
      var kartTipi = profil.kartTipi || 'koleksiyon';
      var ozelUrl = profil.ozelKartUrl || '';
      var yerelBase64 = profil.profilKartBase64 || s.profilKartBase64 || '';
      var dosyaAdi = profil.koleksiyonKartDosya || 'card_1.jpg';
      
      var bgUrl = '';
      if (kartTipi === 'yerel' && yerelBase64) {
        bgUrl = yerelBase64;
      } else if (kartTipi === 'ozel' && ozelUrl) {
        bgUrl = ozelUrl;
      } else if (kartTipi === 'koleksiyon' && dosyaAdi) {
        try {
          bgUrl = chrome.runtime.getURL('profil-kartlari/' + dosyaAdi);
        } catch (e) {
          bgUrl = '';
        }
      }

      // İçteki opak gri Kick zeminlerini şeffaflaştır (2. Madde Düzeltmesi)
      card.style.setProperty('background', 'transparent', 'important');
      card.style.setProperty('background-color', 'transparent', 'important');
      var icBloklar = card.querySelectorAll('.bg-surface-base, .bg-surface-lowest, .bg-surface-lower, .bg-surface-highest, [class*="bg-surface"]');
      for (var bi = 0; bi < icBloklar.length; bi++) {
        icBloklar[bi].style.setProperty('background-color', 'rgba(15, 23, 42, 0.45)', 'important');
        icBloklar[bi].style.setProperty('backdrop-filter', 'blur(3px)', 'important');
      }

      if (bgUrl) {
        kap.style.setProperty('background-image', 'linear-gradient(rgba(15, 23, 42, 0.45), rgba(15, 23, 42, 0.70)), url("' + bgUrl + '")', 'important');
        kap.style.setProperty('background-size', 'cover', 'important');
        kap.style.setProperty('background-position', 'center', 'important');
        kap.style.setProperty('border-radius', '12px', 'important');
      }

      // Kartın içindeki Kullanıcı Adına Canlı İsim Efektini Uygula
      var nameEl = card.querySelector('a.select-text.font-semibold, [class*="font-semibold"], h4, [class*="username"]');
      if (nameEl && holoEfekt && holoEfekt !== 'varsayilan' && holoEfekt !== 'yok') {
        nameEl.classList.add('pk-chat-holo-name', 'pk-name-' + holoEfekt);
      }

      if (holoEfekt && holoEfekt !== 'varsayilan' && holoEfekt !== 'yok') {
        kap.classList.add('pk-holo-' + holoEfekt);
      }
    });
  }

  function processProfilePopups() {
    mukerrerRozetSatiriTemizle();
    var cards = document.querySelectorAll('div.bg-surface-highest');
    for (var i = 0; i < cards.length; i++) {
      var nameEl = cards[i].querySelector('a.select-text.font-semibold');
      if (!nameEl) continue;
      /* Sabitlenmiş kopyanın içine girme: o canlı kart değil, donuk görüntü.
         Girersek her turda blok ekleyip kart üstüne kart yığıyoruz. */
      if (cards[i].closest('[data-pk-sabit]')) continue;
      /* İç içe adaylarda yalnız EN İÇTEKİ işlensin — dış kap da aynı ismi
         gördüğü için ikinci kez işlenip blokları çoğaltıyordu. */
      if (pkIcAdayVar(cards[i])) continue;
      // Rozetler rozet verisine bağlı; "Son Mesajlar" ve kaydırma bağlı DEĞİL.
      /* ÖLÇÜ ÖNCE: içerik blokları genişliğe göre yerleşsin. */
      var uName = (nameEl.textContent || '').trim();
      try { kartGenislet(cards[i]); } catch (e) {}
      try { injectProfilKartiTemasi(cards[i], uName); } catch (e) {}
      if (badgeActive() && badgeMaps) { try { injectPopupBadges(cards[i], nameEl); } catch (e) {} }
      try { surukleYap(tasinacakKap(cards[i])); } catch (e) {}
      try { profilKartiIkonlari(cards[i]); } catch (e) {}
      try { hdAvatarKur(cards[i]); } catch (e) {}
      try { injectPopupMesajlar(cards[i], uName); } catch (e) {}
      try { injectPopupYanitlar(cards[i], uName); } catch (e) {}
      try { injectPopupModer(cards[i]); } catch (e) {}
      try { kapatHizala(cards[i]); } catch (e) {}
      try { crossKartIkonu(cards[i], uName); } catch (e) {}
      try { pkYetkiIkonu(cards[i], uName); } catch (e) {}
    }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * SOHBETTE ARA — sohbet sütununu kaplayan tam ekran panel
   *
   * Kaynak veri: sohbet toplayıcısının (`scListe`) kanala girildiğinden beri
   * biriktirdiği mesajlar. Yeni bir istek atılmaz, her şey yerelde.
   *
   * Görünüm Kick'in kendi panellerinden ÖLÇÜLDÜ:
   *  · başlık: 50px, `border-b border-outline-decorative`, `gap-2.5 p-1.5`,
   *    ortalanmış `text-base font-bold` başlık (Aktif Sohbetçiler başlığıyla aynı)
   *  · arama kutusu: kap `border-outline-region` + `h-8` + 4px köşe + 12px yan
   *    boşluk, giriş şeffaf 16px/400 (Kick'in arama kutusundan birebir)
   *  · ikon butonu: `size-9` + `state-layer-surface bg-transparent` (dişli ile aynı)
   * ══════════════════════════════════════════════════════════════════════ */
  var ARA_ID = 'pk-ara-panel';
  var araSorgu = '';
  var araAdFiltre = '';           // 10.5 — ikinci filtre (kullanıcı adı)
                  // ekrana basılan en fazla sonuç

  var I_ARA = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:16px;height:16px;display:block">' +
    '<path d="M10.6 2a8.6 8.6 0 1 0 5.2 15.5l4.3 4.3a1.4 1.4 0 0 0 2-2l-4.3-4.3A8.6 8.6 0 0 0 10.6 2zm0 2.8a5.8 5.8 0 1 1-5.8 5.8 5.8 5.8 0 0 1 5.8-5.8z"/></svg>';

  /* Araç çubuğundaki yuva: "Gönder"den yukarı çıkıp sol gruptaki
     `justify-between` kabını alıyoruz — dişlinin solundaki boşluk orası. */
  /* GÖRÜNÜRLÜK ŞARTI: Kick sayfada "Gönder" butonunun İKİ kopyasını tutuyor —
     masaüstü düzeni için görünür olanı ve mobil düzen için gizli (0x0) olanı.
     Eskiden DOM sırasındaki ilki alınıyordu; gizli kopya öne geçtiğinde onun
     ata zinciri 0x0 oluyor, beklenen çocuk sayısı tutmuyor ve fonksiyon null
     dönüyordu → arama butonu hiç kurulmuyordu. Hangi kopyanın önce geldiği
     düzene/genişliğe göre değiştiği için hata da "bazen" görünüyordu. */
  function araYuva() {
    /* Kick gönder düğmesine #send-message-button kimliğini veriyor — metin
       eşleştirmesine (7 dil!) gerek yok. Kimlik İKİ düğümde birden var
       (masaüstü + mobil), o yüzden görünür olanı seçiliyor. */
    var g = pkIlkGorunur('#send-message-button');
    if (!g) {
      var btns = document.querySelectorAll('button');
      for (var i = 0; i < btns.length; i++) {
        if (!/^(gönder|send)$/i.test((btns[i].textContent || '').trim())) continue;
        if (!pkGorunur(btns[i])) continue;
        g = btns[i]; break;
      }
    }
    if (!g || !g.parentElement || !g.parentElement.parentElement) return null;
    var sol = g.parentElement.parentElement.children[0];
    return (sol && sol.children.length >= 2) ? sol.children[1] : null;
  }

  /* Sohbet yazma çubuğundaki DİŞLİ (Kick'in sohbet ayarları). Yenilik zinciri
     buradan başlıyor: dişli → "PureKick Sohbet Ayarları" satırı → menü.
     Düğmeyi "Gönder"in solundaki gruptan alıyoruz; DİL BAĞIMSIZ, ölçüye de
     bakmıyoruz — grubun SON düğmesi dişli (arama ve bizim düğmelerimiz
     `data-pk-*` taşıdığı için eleniyor). */
  function kickAyarDugmesi() {
    /* 10.14 — Düğme KICK'İN KENDİ İKON KİMLİĞİYLE bulunuyor:
       svg[data-ds-icon="Settings"]. Eskiden "gruptaki SON görünür düğme"
       alınıyordu; Kick araç şeridine yeni bir düğme ekleyince (ör. "KICKs
       Satın Al") yenilik noktası YANLIŞ düğmeye çıkıyordu. İkon kimliği
       dilden, sıradan ve kap yapısından bağımsız. */
    var kokler = [];
    var g = pkIlkGorunur('#send-message-button');
    if (g && g.parentElement && g.parentElement.parentElement) {
      var kap = g.parentElement.parentElement.children[0];
      if (kap) kokler.push(kap);                 // önce sohbet yazma şeridi
    }
    var sut = pkIlkGorunur('#channel-chatroom');
    if (sut) kokler.push(sut);                   // yedek: sohbet sütununun tamamı
    for (var k = 0; k < kokler.length; k++) {
      var ikonlar = kokler[k].querySelectorAll('svg[data-ds-icon="Settings"]');
      for (var i = 0; i < ikonlar.length; i++) {
        var b = ikonlar[i].closest ? ikonlar[i].closest('button') : null;
        if (!b) continue;
        if (b.getAttribute('data-pk-smenu') || b.getAttribute('data-pk-ara')) continue;  // bizimkiler
        if (!pkGorunur(b)) continue;             // Kick'in 0×0 gizli ikizi
        return b;
      }
    }
    return null;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * PUREKICK AYAR DÜĞMESİ — Kick'in dişlisinin yanında
   *
   * Eskiden ayarlarımız Kick'in KENDİ ayar menüsüne bir satır olarak
   * ekleniyordu. O menüyü React her açılışta yeniden çiziyor: satırımız
   * kimi zaman geç geliyor, kimi zaman menü kapanıp açılınca kayboluyordu.
   * Kendi düğmemiz o kırılganlığı tamamen kaldırıyor — Kick'in menüsüne
   * hiç dokunmuyoruz.
   *
   * Sınıf DİŞLİDEN kopyalanıyor: ölçü, hover ve durum katmanı birebir aynı
   * olsun, şeride yamanmış gibi durmasın.
   * ══════════════════════════════════════════════════════════════════════ */
  var PK_AYAR_IM = 'data-pk-ayarbtn';

  /* Okunmamış bahsedilme var mı — düğmedeki sayaç ve tıklama davranışı
     ikisi de buna bakıyor, tek yerden okunsun. */
  function pkBahsetYeni() {
    if (settings.bahsedilme === false) return 0;
    /* Sayaç AYRI kapatılabiliyor: kullanıcı bahsedilmeleri toplamayı
       sürdürüp yalnız kırmızı rakamı görmek istemeyebilir. Liste dolmaya
       devam ediyor, yalnız gösterge susuyor. */
    if (settings.bahsetRozet === false) return 0;
    return pkMentionYeni > 0 ? pkMentionYeni : 0;
  }

  /* Düğmedeki gösterge. İKİ TÜR VAR ve aynı anda yalnız biri gösteriliyor:
     · okunmamış bahsedilme → KIRMIZI SAYI (kaç kişi bahsetti)
     · yeni özellik → yeşil nokta
     Bahsedilme öncelikli: sayı somut bir bilgi, nokta yalnız "bak" diyor.
     Her turda yeniden kuruluyor çünkü sayı değişiyor. */
  function pkAyarNoktaTazele(b) {
    /* Bekleyen "okundu" damgasının SON DURAĞI. pkAyarSayfaKapat yalnız geri
       okuna basınca çalışıyor; kullanıcı Kick'in panelini komple kapatırsa
       o hiç çağrılmıyordu. Sayfa düğümü DOM'da yoksa liste kapanmıştır. */
    if (pkMentionOkuBekle && !document.querySelector('[data-pk-ayar-sayfa]')) {
      pkMentionOkunduUygula();
    }
    var eski = b.querySelector(':scope > [data-pk-gosterge]');
    var sayi = pkBahsetYeni();
    var imza = sayi ? ('s' + sayi) : (pkYeniVarMi() ? 'n' : '');
    if (eski && eski.getAttribute('data-pk-gosterge') === imza) return;
    if (eski) eski.remove();
    if (!imza) return;
    var g;
    if (sayi) {
      g = sekmeRozeti(sayi > 99 ? 99 : sayi);
      g.style.cssText += ';position:absolute;top:-3px;right:-3px;margin:0;pointer-events:none';
    } else {
      g = pkYeniNoktasi();
      g.style.cssText += ';position:absolute;top:2px;right:2px;margin:0;pointer-events:none';
    }
    g.setAttribute('data-pk-gosterge', imza);
    b.appendChild(g);
  }

  function pkAyarDugmesiKur() {
    if (!settings.consented) return;
    var disli = kickAyarDugmesi();
    if (!disli || !disli.parentElement) return;
    var kap = disli.parentElement;
    var eski = kap.querySelector(':scope > [' + PK_AYAR_IM + ']');
    /* React şeridi yeniden çizince düğmemiz düşüyor; her turda varlığını
       doğruluyoruz. Duruyorsa yalnız yenilik noktası tazeleniyor. */
    if (eski) { pkAyarNoktaTazele(eski); pkIsaretUygula(eski); return; }
    var b = el('button', (disli.className || '').toString());
    b.setAttribute(PK_AYAR_IM, '1');
    b.type = 'button';
    b.title = 'PureKick';
    b.setAttribute('aria-label', 'PureKick');
    b.style.setProperty('position', 'relative');   // yenilik noktası için
    pkIsaretUygula(b);
    /* ZEMİN YOK — yalnız PK simgesi. Bir ara gri kutu vardı (rgb(66,71,77),
       üst çubuktaki PK ile aynı olsun diye); kullanıcı kaldırılmasını istedi:
       yanındaki dişli zeminsiz duruyor ve tek kutu şeritte yama gibi
       görünüyordu. Zemin gidince düğme Kick'in kendi sınıflarını (dişliden
       kopyalanıyor) olduğu gibi kullanıyor, hover katmanı da oradan geliyor. */
    var ik = el('span', 'inline-flex shrink-0');
    ik.innerHTML = SHIELD;
    var sv = ik.querySelector('svg');
    if (sv) { sv.style.width = '17px'; sv.style.height = '17px'; sv.style.display = 'block'; }
    b.appendChild(ik);
    b.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      /* AÇ/KAPA. Panel artık BİZİM: Kick'in dişlisine basıp panelin
         gelmesini yoklamaya gerek kalmadı, doğrudan çiziyoruz. Ölçüt DOM,
         bayrak değil — bayrak gerçekle ayrışabiliyor. */
      pkIsaretKapat();                    // tıklandı → yol gösterme bitti
      if (pkPanelVar()) { pkAyarSayfaKapat(); return; }
      /* AYNI YERE DENK GELEN NE VARSA KAPANIYOR: moderasyon/silinen
         mesajlar panellerimiz ve Kick'in kendi ayar paneli. Kick'inkini
         dişlisine basarak kapatıyoruz — kendi düğümlerine dokunmuyoruz. */
      pkUstPanelleriKapat();
      /* OKUNMAMIŞ BAHSEDİLME VARSA doğrudan o sayfaya. Kullanıcı sayıyı
         görüp tıklıyor; araya menü koymak fazladan bir adım olurdu. */
      var ac = function () {
        try {
          if (pkBahsetYeni()) pkAyarSayfaAc(t('pkBahsedilmeler'), bolumBahsedilme, pkAyarMenuAc);
          else pkAyarMenuAc();
        } catch (er) {}
      };
      /* KICK'İN PANELİ KAPANMADAN AÇMIYORUZ — ikisi aynı yere denk geliyor.

         DİŞLİYE HEMEN BASMAK YANLIŞTI: gerçek fare tıklaması Kick'in kendi
         "dışarı tıklandı" davranışını tetikleyip paneli ZATEN kapatıyor.
         Biz o sırada dişliye basınca panel geri AÇILIYOR, sonra da turdaki
         denetim bizimkini kapatıyordu — düğme hiç çalışmıyormuş gibi
         görünüyordu. (Canlıda ölçüldü: programatik tıkla açılıyor, gerçek
         tıkla açılmıyordu; fark tam olarak buydu.)

         Sıra: önce KENDİ kapanmasını bekle. 400 ms'de kapanmadıysa
         (klavye/erişilebilirlik gibi dışarı-tık üretmeyen yollar) dişliye
         bir kez bas. Hiç kapanmazsa yine de açıyoruz. */
      if (!kickPaneliAcikMi()) { ac(); return; }
      var dene = 0, disliBasildi = false;
      (function bekle() {
        if (!kickPaneliAcikMi()) { ac(); return; }
        if (dene === 10 && !disliBasildi) {
          disliBasildi = true;
          var d2 = kickAyarDugmesi();
          if (d2) { try { d2.click(); } catch (er) {} }
        }
        if (++dene >= 30) { ac(); return; }   // en çok ~1,2 sn
        setTimeout(bekle, 40);
      })();
    });
    /* Dişlinin HEMEN SAĞINA. */
    if (disli.nextSibling) kap.insertBefore(b, disli.nextSibling);
    else kap.appendChild(b);
    pkAyarNoktaTazele(b);
  }

  /* Dişliye yenilik noktası koyar / kaldırır. Düğüm KICK'İN — kutu ölçüsüne
     dokunulmuyor, nokta `position:absolute` ile üstüne biniyor. Her turda
     yeniden doğrulanıyor: React düğmeyi yeniden çizince işaretimiz gidiyor. */
  function kickAyarNoktasiKur() {
    if (!settings.consented) return;
    var b = kickAyarDugmesi();
    if (!b) return;
    var varMi = pkYeniVarMi();
    var eski = b.querySelector(':scope > [data-pk-yenilik]');
    if (!varMi) { if (eski) eski.remove(); return; }
    if (eski) return;
    /* Konumlandırma için ata `relative` olmalı; Kick'in kendi sınıfı bunu
       vermiyorsa satır içi veriyoruz (ölçü değiştirmez). */
    var kon = '';
    try { kon = getComputedStyle(b).position; } catch (e) {}
    if (kon === 'static') b.style.setProperty('position', 'relative');
    var n = pkYeniNoktasi();
    n.setAttribute('data-pk-yenilik', '1');
    n.style.cssText += ';position:absolute;top:2px;right:2px;margin:0;pointer-events:none';
    b.appendChild(n);
  }
  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — SOHBET MENÜSÜ
   * Sohbet özellikleri sohbetten açılır (ayarlar panelinden değil). Arama
   * butonunun yanına ikinci bir ikon konur; menü Kick'in kendi yüzey
   * sınıflarıyla çizilir.
   * ══════════════════════════════════════════════════════════════════════ */

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — KENAR ÇUBUĞU YAYIN ÖNİZLEMESİ
   * Sol menüdeki bir kanalın üstüne gelince küçük bir kart açılır: yayın
   * küçük resmi, başlık, kategori, izleyici.
   *
   * NEDEN IFRAME DEĞİL: canlı oynatıcıyı iframe'le gömmek ikinci bir video
   * akışı başlatır — bant genişliği ve CPU iki katına çıkar, üstelik reklam
   * engelleme o iframe'de çalışmaz. Küçük resim anında gelir ve bedavadır.
   * Veri Kick'in kendi ucundan, çerezsiz, kanal başına tek istek (bellekte).
   * ══════════════════════════════════════════════════════════════════════ */

  /* Kick, kenar çubuğu satırlarında yayın başlığını NATIVE `title` niteliğiyle
     gösteriyor — tarayıcının kendi sarı/siyah ipucu kutusu. Bizim kartımız
     açılınca ikisi üst üste biniyor ve okunmaz hâle geliyor.

     Kick'in CSS'ine ya da bileşenine dokunmuyoruz: yalnız o satırdaki `title`
     niteliklerini geçici olarak alıp kendi veri niteliğimizde saklıyoruz, kart
     kapanınca aynen geri koyuyoruz. Sayfa hiçbir şey kaybetmiyor. */





  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — SOHBET OLAYLARI: SİLİNEN MESAJLAR + MODERASYON GÜNLÜĞÜ
   *
   * Veri MAIN dünyadaki Pusher dinleyicisinden gelir (worker-hook.js).
   * Kendi soketimizi açmıyoruz — Kick'in zaten açık bağlantısını dinliyoruz.
   *
   * NEDEN SOKET GEREKLİ: silinen mesajı DOM'dan anlamak mümkün değil. Kick
   * sohbeti sanallaştırıyor, mesajlar normal akışta da DOM'dan çıkıyor;
   * "kayboldu" ile "silindi" ayırt edilemiyor. Soket olayı bunu kesin söylüyor.
   *
   * EŞLEŞTİRME: Kick'in sohbet balonunda mesaj kimliği YOK (ölçüldü — yalnız
   * data-index var). Bu yüzden gelen her ChatMessageEvent'i kimlik→(kim, metin)
   * olarak saklıyoruz; MessageDeletedEvent geldiğinde o kimlikten kime/neye ait
   * olduğunu buluyoruz.
   * ══════════════════════════════════════════════════════════════════════ */
  var MOD_SINIR = 300;
  var mesajKimlik = Object.create(null);   // uuid -> { kim, metin, ts }
  var mesajKimlikSira = [];
  var silinenler = [];                     // { kim, metin, ts }
  var modGunluk = [];                      // { tur, kim, yapan, ts, bitis }
  var aktifSusturma = Object.create(null); // nick -> bitiş zamanı (ms)
  /* Anahtarlar küçük harfli; listede kullanıcının kendi yazımı görünsün diye
     görünen ad ayrı tutuluyor. */
  var susturmaAdi = Object.create(null);   // nick -> görünen ad

  function olaySifirla() {
    mesajKimlik = Object.create(null); mesajKimlikSira = [];
    silinenler = []; modGunluk = [];
    /* Görünen ad haritası aktif susturmalarla BİRLİKTE gitmeli; ayrı
       kalırsa eski kanalın adları birikir. */
    aktifSusturma = Object.create(null); susturmaAdi = Object.create(null);
  }

  /* Yayın bitti: sohbet birikimini bırak ama KANALDA KALIYORUZ.
     `scKanal` sıfırlanmaz — aksi hâlde bir sonraki mesaj "kanal değişti"
     sanılıp ikinci kez sıfırlama tetiklenirdi. */
  function yayinBittiSifirla() {
    scListe = Object.create(null);
    scGorulen = Object.create(null); scGorulenAdet = 0;
    scRoster = Object.create(null); scKickSayi = 0; scSayacYazdik = '';
    scIstatSifirla(); pkMentionSifirla(); olaySifirla(); kanalDurumSifirla();
  }

  /* Kanal değişince gidenler — hepsi KANALA ÖZEL veri.
     Kullanıcı tercihleri (notlar, gizlenen kişiler, vurgu muafiyeti, ceza
     sebepleri) burada YOK: onlar kanaldan bağımsızdır ve kalmalıdır. */
  function kanalDurumSifirla() {
    /* Rol kanala özel: A'nın moderatörü B'de moderatör değil. */
    kisiRol = Object.create(null); kisiRolSira = [];
    msYasakli = Object.create(null);           // "bu oturumda banlandı" filtresi
    pkKisiKanal = Object.create(null);         // anahtarında kanal yok → kanalla birlikte git
    pkKisiBekleyen = Object.create(null);      // eski kanalın bekleyen istekleri

    bindirMesajlar = [];                       // yayın üstü bindirme tamponu
    try { bindirSatirCiz(); } catch (e) {}     // ekranda duran eski satırları da temizle

    kurtarilan = [];                           // sohbet temizlemeden kurtarılanlar
    try { kurtarmaKapat(); } catch (e) {}      // kutusu açıksa kapat
  }

  function modGunlukEkle(kayit) {
    modGunluk.push(kayit);
    if (modGunluk.length > MOD_SINIR) modGunluk.shift();
  }

  function sohbetOlayi(d) {
    /* ANA SAYFA KAPISI — Kick ana sayfada öne çıkan yayının sohbetini basıyor
       ve soket o yayının olaylarını da taşıyor. Kanal/VOD sayfasında DEĞİLSEK
       hiç işlemeyiz: yoksa kullanıcının seçmediği bir yayının mod olayları ana
       sayfaya yazılır ve her mesaj için boşuna sohbet/rol/kaplama işlenir (ana
       sayfa kasması). DOM turu 10.7'de kanalSayfasi() kapısına alınmıştı; soket
       yolu (bu işleyici) atlanmıştı — burada da kapatıyoruz. */
    if (!kanalSayfasi()) return;
    /* Rol kaydı moderasyon günlüğünden BAĞIMSIZ — vurgulama açıkken günlük
       kapalı olabilir. Bu yüzden kapı kontrolünden ÖNCE. */
    sonOlayZamani = Date.now();                     // 10.5 baglanti durumu
    /* YAYIN BİTTİ → toplanan sohbet verisini bırak. Kullanıcı isteği:
       "yayındayken sadece duracak, yayından çıktığında silmesi lazım".
       Kanal değişimindeki sıfırlamanın aynısı; ayrı bir yol tutmuyoruz. */
    if (d.ad === 'StopStreamBroadcast') {
      /* Yayın bitti: geri yüklenecek bir şey kalmadı, disk kaydını da at. */
      try { if (scKanal) otSil(scKanal); } catch (e) {}
      try { yayinBittiSifirla(); } catch (e) {}
      return;
    }
    if (d.ad === 'ChatMessageEvent' && d.kim) rolYaz(d.kim, d.rol);
    if (d.ad === 'ChatMessageEvent') { try { bindirEkle(d.kim, d.rol, d.metin, d.ts, d.renk); } catch (e) {} }
    /* Sayaç/kayıt/bahsedilme buradan besleniyor. `settings.modGunluk`
       kapısının ÜSTÜNDE: moderasyon günlüğü kapalıyken de istatistik akmalı. */
    if (d.ad === 'ChatMessageEvent') { try { scOlaydanMesaj(d); } catch (e) {} }

    // 10.19 — Canlı Soket Bağış, Hediye Sub ve Kicks Olayları
    if (d.ad === 'GiftedSubscriptionsEvent') {
      try {
        var adet = d.adet || 1;
        pkBagisKaydet(d.kim, 'Gift Sub', adet + ' Hediye Sub', adet, 'Topluluğa ' + adet + ' adet hediye abonelik');
      } catch (e) {}
      return;
    }
    if (d.ad === 'SubscriptionEvent') {
      try {
        var sure = (d.ay || 1) + '. Ay';
        pkBagisKaydet(d.kim, 'Abone', sure + ' Abone', 1, d.kim + ' kanala abone oldu (' + sure + ')');
      } catch (e) {}
      return;
    }
    if (d.ad === 'LivestreamReactionEvent' && d.kicks > 0) {
      try {
        pkBagisKaydet(d.kim, 'Kicks', d.kicks + ' Kicks', d.kicks, d.kicks + ' Kicks desteği');
      } catch (e) {}
      return;
    }
    if (d.ad === 'ChatMessageEvent' && d.kicks > 0) {
      try {
        pkBagisKaydet(d.kim, 'Kicks', d.kicks + ' Kicks', d.kicks, d.kicks + ' Kicks gönderdi');
      } catch (e) {}
    }

    /* Ban/susturma/ban kaldırma/temizleme sohbet akışına yazılır. Kick
       bunları yalnız modlara gösteriyor, normal izleyici hiç görmüyordu.
       `settings.modGunluk` kapısının ÜSTÜNDE: günlük kapalıyken de görünsün. */
    if (d.ad !== 'ChatMessageEvent' && d.ad !== 'MessageDeletedEvent') { try { msModOlayi(d); } catch (e) {} }
    if (settings.modGunluk === false) return;
    var ad = d.ad;
    if (ad === 'ChatMessageEvent') {
      if (!d.id) return;
      mesajKimlik[d.id] = { kim: d.kim || '', metin: d.metin || '', ts: d.ts || Date.now() };
      mesajKimlikSira.push(d.id);
      // bellek emniyeti: yalnız son N mesajın kimliği tutulur
      while (mesajKimlikSira.length > 2000) delete mesajKimlik[mesajKimlikSira.shift()];
      return;
    }
    if (ad === 'MessageDeletedEvent') {
      var m = d.id && mesajKimlik[d.id];
      /* Kimliği bilmiyorsak (biz gelmeden önce yazılmış mesaj) yine de kaydet —
         "bir mesaj silindi" bilgisi de değerli. */
      try { msMesajSilindi(d.id, d.otoMod, d.kural); } catch (e) {}
      silinenler.push(m ? { kim: m.kim, metin: m.metin, ts: m.ts, silinme: Date.now() }
                        : { kim: '', metin: '', ts: 0, silinme: Date.now() });
      if (silinenler.length > MOD_SINIR) silinenler.shift();
      modGunlukEkle({ tur: 'sil', kim: (m && m.kim) || '', metin: (m && m.metin) || '',
                      yapan: d.yapan || '', otoMod: d.otoMod, ts: Date.now() });
      modNokta();
      return;
    }
    if (ad === 'UserBannedEvent') {
      var bitisMs = d.bitis ? Date.parse(d.bitis) : 0;
      modGunlukEkle({ tur: d.kalici ? 'yasak' : 'sustur', kim: d.kim || '', yapan: d.yapan || '',
                      ts: d.ts || Date.now(), bitis: bitisMs || 0,
                      sebep: d.sebep || '', itiraz: !!d.itiraz });
      if (!d.kalici && bitisMs) {
        var sk = (d.kim || '').toLowerCase();
        aktifSusturma[sk] = bitisMs; susturmaAdi[sk] = d.kim || sk;
      }
      modNokta();
      return;
    }
    if (ad === 'UserUnbannedEvent') {
      modGunlukEkle({ tur: 'kaldir', kim: d.kim || '', yapan: d.yapan || '', ts: d.ts || Date.now() });
      var uk = (d.kim || '').toLowerCase();
      delete aktifSusturma[uk]; delete susturmaAdi[uk];
      modNokta();
      return;
    }
    if (ad === 'ChatroomClearEvent') {
      try { sohbetTemizlendi(); } catch (e) {}      // 10.5 mesajlari kurtar
      modGunlukEkle({ tur: 'temizle', ts: d.ts || Date.now() });
      modNokta();
    }
  }

  /* Süresi dolan susturmaları düşür — panel açıkken saniyede bir çağrılır. */
  function susturmaTemizle() {
    var simdi = Date.now(), degisti = false;
    for (var k in aktifSusturma) {
      if (aktifSusturma[k] <= simdi) { delete aktifSusturma[k]; delete susturmaAdi[k]; degisti = true; }
    }
    return degisti;
  }

  /* Yeni moderasyon olayı → sohbet başlığındaki araç düğmesinde nokta.
     (Eskiden kanal eylem satırındaki menüyü besliyordu; o menü kalktı.) */
  function modNokta() { try { sohbetMenuNokta(); } catch (e) {} }

  /* ---------- Moderasyon paneli (sohbet alanını kaplar) ---------- */
  var MOD_ID = 'pk-mod-panel';
  var modSekme = 'mod';                  // mod | silinen | sustur (ayar paneliyle aynı)
  /* Sütuna `position:relative` BİZ verdiysek kapanışta geri alıyoruz —
     Kick'in yerleşiminde kalıcı iz bırakmayalım. */
  var MOD_KONUM = 'data-pk-mod-konum';
  function modKonumGeriAl() {
    var q = document.querySelectorAll('[' + MOD_KONUM + ']');
    for (var i = 0; i < q.length; i++) {
      q[i].style.removeProperty('position');
      q[i].style.removeProperty('isolation');
      q[i].removeAttribute(MOD_KONUM);
    }
  }

  function modPanelKapat() {
    modPanelSayacDur();
    var p = document.getElementById(MOD_ID); if (p) p.remove();
    modKonumGeriAl();
  }

  /* Günlük satırının okunur hâli. Çeviri geçişinde ATLANMIŞTI: metinler
     birleştirmeyle kuruluyordu, tarayıcı yalnız `toast('…')` gibi tek parçalı
     çağrıları buluyordu. Artık yer tutuculu anahtarlarla kuruluyor. */
  /* Kick'in yapay zekâ moderasyonunun görünen adı. Kullanıcı adı değildir:
     tıklanabilir yapılmaz, rozet/renk almaz, profil kartı açmaz. */
  var PK_AIMOD_AD = 'AiMod';

  function modSatirYazi(x) {
    var kim = x.kim || '?';
    var yapan = x.yapan || t('pkBirModerator');
    
    if (x.tur === 'sil') {
      if (!x.kim) return t('pkModBirMesajSilindi');
      if (x.otoMod === true) {
        return settings.aiMod !== false ? t('pkModOtoSilindi', [kim, PK_AIMOD_AD])
                                        : t('pkModSilindi', [kim]);
      }
      return t('pkModOtoSilindi', [kim, t('pkBirModeratorIci')]);
    }
    if (x.tur === 'yasak') return t('pkModYasakladi', [yapan, kim]);
    if (x.tur === 'sustur') {
      var sn = x.bitis ? Math.max(0, Math.round((x.bitis - x.ts) / 1000)) : 0;
      var sure = pkSureYazi(sn);
      return t('pkModSusturdu', [yapan, kim, sure]);
    }
    if (x.tur === 'kaldir') return t('pkModCezaKaldirdi', [yapan, kim]);
    if (x.tur === 'temizle') return t('pkModSohbetTemizlendi');
    return '';
  }

  var I_MS_SIL = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:15px;height:15px;display:block">' +
    '<path d="M6 7h12l-1 13H7L6 7zm3-3h6l1 2H8l1-2z"/></svg>';
  var I_MS_BAN = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" style="width:15px;height:15px;display:block">' +
    '<circle cx="12" cy="12" r="9"/><path d="M5.6 5.6l12.8 12.8"/></svg>';
  var I_MS_SUS = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" style="width:15px;height:15px;display:block">' +
    '<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>';
  var I_MS_KALDIR = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" style="width:15px;height:15px;display:block">' +
    '<circle cx="12" cy="12" r="9"/><path d="M8 12.5l2.5 2.5L16 9.5"/></svg>';
  var I_MS_TEMIZ = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" style="width:15px;height:15px;display:block">' +
    '<path d="M4 7h16M9 7V5h6v2M7 7l1 12h8l1-12"/></svg>';

  /* ══════════════════════════════════════════════════════════════════════
   * MODERASYON OLAYLARINI SOHBET AKIŞINA YAZ
   * ══════════════════════════════════════════════════════════════════════ */

  var MS_ETIKET = 'data-pk-mod-kutu';
  var msStil = false;
  var msYasakli = Object.create(null);      // oturumda banlananlar (küçük harf)

  /* Tablodan CSS üretir; hesap eklemek tek satır. Tırnak ve ters bölü
     kaçırılıyor — `content` bir CSS dizesi, kaçırılmazsa kural bozulur ve
     ARDINDAN GELEN kurallar da düşer. */
  function pkOzelAdSimgeCss() {
    var out = '';
    for (var k in PK_OZEL_AD_SIM) {
      var s2 = PK_OZEL_AD_SIM[k];
      if (!s2 || !s2.length) continue;
      var sec = '[data-pk-ozelad="' + k + '"]';
      if (s2[0]) out += sec + '::before{content:"' + pkCssDize(s2[0]) + '"}';
      if (s2[1]) out += sec + '::after{content:"' + pkCssDize(s2[1]) + '"}';
    }
    return out;
  }
  function pkCssDize(v) { return String(v).replace(/\\/g, '\\\\').replace(/"/g, '\\"'); }

  /* msStilKur'dan ONCE tanimli olmali: stil metni bu degeri gomuyor ve
     msStilKur ilk cagrildiginda bir kez kuruluyor. Asagida tanimliyken
     erken bir cagride kural '[undefined]' olarak yaziliyordu. */
  var SIL_GIZLE = 'data-pk-sil-gizle';

  function msStilKur() {
    if (msStil) return; msStil = true;
    var st = document.createElement('style');
    st.setAttribute('data-pk-mod-stil', '1');
    st.textContent =
      '[' + MS_ETIKET + ']{display:block;padding:2px 0}' +
      /* Kutu: kırmızı çerçeve + solda kalın vurgu. Sohbet akışında mesaj
         gibi dursun ama moderasyon olduğu ilk bakışta belli olsun. */
      '.pk-ms-cerceve{position:relative;overflow:hidden;display:flex;align-items:flex-start;' +
        'gap:6px;margin:2px 0;padding:6px 8px;border:1px solid rgba(255,72,72,.42);' +
        /* KARE köşe (kullanıcı isteği). Yalnız köşe değeri değişti, ölçü aynı. */
        'border-left:3px solid #ff4848;border-radius:0;background:rgba(255,72,72,.10);' +
        'color:#ff9a9a}' +
      /* Işık taraması: ayrı düğüm yok, katman ::after ile çiziliyor. */
      '.pk-ms-cerceve::after{content:"";position:absolute;inset:0;pointer-events:none;' +
        'background:linear-gradient(100deg,transparent 18%,rgba(255,105,105,.30) 50%,transparent 82%);' +
        'transform:translateX(-100%);animation:pk-ms-isik 2.6s ease-in-out infinite alternate}' +
      '@keyframes pk-ms-isik{from{transform:translateX(-100%)}to{transform:translateX(100%)}}' +
      /* Hesaba özel çerçevenin ışık taraması. Sözde öğe DEĞİL: sohbet satırına
         `overflow:hidden` koymak Kick'in taşan içeriğini kırpabilirdi. Arka
         plan kutuya kendiliğinden kırpılıyor. Kayma bir görüntü genişliği
         (300%) için 150% — `pk-ozel-ad` ile aynı hesap. */
      '@keyframes pk-cerceve-isik{from{background-position:0% 0%}to{background-position:150% 0%}}' +
      '@media (prefers-reduced-motion:reduce){[data-pk-cerceve]{animation:none !important}}' +
      /* YEŞİL VARYANT — ceza kaldırma. Yalnız renkler değişiyor, ölçü aynı. */
      '.pk-ms-cerceve.pk-ms-yesil{border-color:rgba(83,252,24,.42);border-left-color:#53fc18;' +
        'background:rgba(83,252,24,.10);color:#a6f08a}' +
      '.pk-ms-cerceve.pk-ms-yesil::after{background:linear-gradient(100deg,transparent 18%,' +
        'rgba(120,255,90,.28) 50%,transparent 82%)}' +
      '@media (prefers-reduced-motion:reduce){.pk-ms-cerceve::after{animation:none;opacity:0}}' +
      /* HESABA ÖZEL İSİM — akan gradyan.
         SATIRA HİÇBİR ŞEY YAZILMIYOR. Önceki sürüm satıra çerçeve/dolgu
         koyuyordu; Kick sohbeti SANALLAŞTIRILMIŞ liste (her satır
         `position:absolute` + `translateY`, konumlar ÖLÇÜLMÜŞ yüksekliklerden
         hesaplanıyor), satırın kutu ölçüsünü değiştirmek Kick'in kaydettiği
         yükseklikle ekrandakini ayırıp mesajlar arasında boşluk bırakıyordu.
         Sadece metin boyanıyor: yükseklik değişmiyor.
         Kuralın kendisi de satır içi yazılıyor (`applyNameGradient` de aynı
         özelliklere `!important` basıyor); burada yalnız kare dizisi duruyor. */
      /* Kayma tam BİR görüntü genişliği olmalı, yoksa döngü başa sıçrarken
         zıplıyor. Konum yüzdesi (kutu − görüntü) üzerinden ölçülüyor:
         görüntü 300% ise %1 konum = kutunun %2'si, bir görüntü genişliği
         (300%) için gereken konum farkı 150%. */
      '@keyframes pk-ozel-ad{from{background-position:0% 50%}to{background-position:150% 50%}}' +
      /* Hesaba özel baş/son simgeleri. SÖZDE ÖĞE, metin DEĞİL: metne
         yazsaydık `textContent` ile isim okuyan her yer (rozet araması,
         sohbetçi listesi, bahsetme) `⌞ad⌝` görür ve eşleşme kırılırdı.
         Simge ismin gradyanını paylaşıyor; ayrı renk verilmiyor. */
      pkOzelAdSimgeCss() +
      /* SİLİNEN MESAJ — ÇERÇEVE VE ARKA PLAN YOK (kullanıcı isteği).
         Önceki sürümde kırmızı çerçeve + kırmızı zemin + ışık taraması vardı;
         sohbette fazla bağırıyordu. Silindiği zaten üzeri çizili metinden,
         "(silindi)" etiketinden ve kural rozetlerinden anlaşılıyor.
         SATIRIN ÖLÇÜSÜNE ZATEN DOKUNULMUYORDU: sohbet sanallaştırılmış liste,
         padding/border eklemek mesajlar arasında boşluk açıyor (ölçülmüş). */
      '[' + SIL_GIZLE + ']{display:none !important}' +
      '[data-pk-silindi] .pk-sil-et{font-weight:700;white-space:nowrap;opacity:.6}' +
      '[data-pk-silindi] .pk-sil-ayrac{opacity:.6}' +
      /* ÜZERİ ÇİZİLİ ORİJİNAL METİN. Satır zaten %50 soluk; metni biraz daha
         soluklaştırıp çizgiyi belirginleştiriyoruz ki "bu silindi" okunsun
         ama metin hâlâ seçilebilsin. */
      '[data-pk-silindi] .pk-sil-metin{text-decoration:line-through;opacity:.55;' +
        'text-decoration-thickness:1px;text-decoration-color:rgba(255,120,120,.9);' +
        'transition:opacity .12s ease}' +
      /* ÜSTÜNE GELİNCE OKUNSUN: çizgi kalkar, solukluk gider. Silinen mesajı
         okumak isteyen fareyi götürmesi yetsin — tıklamaya gerek kalmasın. */
      '[data-pk-silindi]:hover .pk-sil-metin{text-decoration:none;opacity:1}' +
      /* Etiketler ÇİZİLMEZ ve SOLUK DEĞİL: sebep okunaklı kalmalı.
         `[data-pk-silindi] > *{opacity:.5}` kuralını burada geri alıyoruz. */
      '[data-pk-silindi] .pk-sil-cip{opacity:1;display:inline-flex;align-items:center;' +
        'margin-left:5px;padding:0 5px;height:15px;border-radius:3px;font-size:10px;' +
        'font-weight:800;line-height:1;white-space:nowrap;vertical-align:middle;' +
        'text-decoration:none}' +
      '.pk-sil-cip-ai{background:#ff8a00;color:#140c00}' +
      '.pk-sil-cip-kural{background:rgba(255,72,72,.18);color:#ffb3b3;' +
        'box-shadow:inset 0 0 0 1px rgba(255,72,72,.38)}' +
      '.pk-sil-goster{opacity:1;margin-left:6px;padding:0 4px;font-size:12px;' +
        'font-weight:700;color:#53fc18;background:none;border:0;cursor:pointer;white-space:nowrap}' +
      '.pk-sil-goster:hover{text-decoration:underline}' +
      /* Kutudaki adlar: tıklanabilir olduğu belli olsun. */
      '.pk-ms-ad{position:relative;z-index:1;font-weight:700;cursor:pointer;text-decoration:underline;' +
        'text-decoration-style:dotted;text-underline-offset:2px}' +
      '.pk-ms-ad:hover{text-decoration-style:solid}' +
      /* Kick'in sohbet satırı görünümü (ölçüler canlıdan): 14px/21px gövde,
         4px 8px iç boşluk, üzerine gelince bir ton açılan zemin. */
      '.pk-mesaj-liste{opacity:1;display:flex;margin-top:2px}' +
      '.pk-mesaj-satir{font-size:14px;line-height:21px;color:#fff;padding-top:4px;padding-bottom:4px}' +
      '.pk-mesaj-satir:hover{background:rgb(38,42,45)}' +
      '.pk-mesaj-saat{font-size:12px;line-height:18px}' +
      '.pk-sil-icerik{opacity:1;display:block;margin-top:2px;padding-left:8px;' +
        'border-left:2px solid rgba(240,241,242,.24)}' +
      '.pk-mod-actor-badge{background:rgba(168,85,247,0.18);color:#d8b4fe;border:1px solid rgba(168,85,247,0.4);border-radius:4px;padding:1px 6px;font-weight:800;display:inline-flex;align-items:center;gap:3px;font-size:11.5px;}' +
      '.pk-mod-hedef-user{background:rgba(239,68,68,0.15);color:#fca5a5;border:1px solid rgba(239,68,68,0.35);border-radius:4px;padding:1px 6px;font-weight:800;font-size:11.5px;}' +
      '.pk-mod-aimod-badge{background:rgba(249,115,22,0.18);color:#fdba74;border:1px solid rgba(249,115,22,0.4);border-radius:4px;padding:1px 5px;font-weight:800;font-size:11px;}';
    (document.head || document.documentElement).appendChild(st);
  }

  function msSohbetKabi() { return pkIlkGorunur('#chatroom-messages'); }

  function msSatirlar() {
    var kap = msSohbetKabi();
    if (!kap) return [];
    /* Sayı kapısı burada da: kap bulunamayıp yedek sütun arayışı panoda bir
       PANEL düğümüne düşerse o panelin alt panelleri satır sanılmasın. */
    var d = kap.querySelectorAll('[data-index]'), out = [];
    for (var i = 0; i < d.length; i++) if (pkSatirNo(d[i])) out.push(d[i]);
    return out;
  }

  /* Dibe yakınsak kaydırmayı tekrar dibe sabitle. Yukarı kaydırmış
     kullanıcıyı yerinden oynatma. */
  function msDibeYapistir() {
    var kap = msSohbetKabi(); if (!kap) return;
    setTimeout(function () {
      try {
        var kalan = kap.scrollHeight - kap.clientHeight - kap.scrollTop;
        if (kalan < 100) kap.scrollTop = kap.scrollHeight;
      } catch (e) {}
    }, 8);
  }

  /* Kutuyu sohbete koy. Kardeş olarak DEĞİL, son balonun içine çocuk olarak. */
  function msSohbeteYaz(kutu) {
    var satir = msSatirlar();
    if (!satir.length) return false;
    for (var i = satir.length - 1; i >= 0; i--) {
      var el2 = satir[i];
      var ub = usernameButton(el2);
      var ad = ub ? (ub.textContent || '').trim().toLowerCase() : '';
      /* Banlanan kişinin balonuna koyma — Kick o balonları siliyor. */
      if (ad && msYasakli[ad]) continue;
      el2.appendChild(kutu);
      msDibeYapistir();
      return true;
    }
    return false;
  }

  /* Bir moderasyon satırı: ikon + metin, Kick'in kendi sohbet ölçüleriyle. */
  /* `yesil`: ceza KALDIRMA olayları. Ban/susturma kırmızı kalıyor; kaldırma
     iyi haber, aynı kırmızıyla göstermek yanlış okunuyordu (kullanıcı
     bildirdi: "ceza kaldırma yeşil olsun"). */
  function msKutu(ikonHtml, metin, yesil) {
    msStilKur();
    var d = el('div', 'px-2 lg:px-3 py-1');
    d.setAttribute(MS_ETIKET, '1');
    var ic = el('div', 'pk-ms-cerceve text-sm' + (yesil ? ' pk-ms-yesil' : ''));
    var ik = el('span', 'inline-flex shrink-0');
    ik.style.cssText = 'width:15px;height:15px;margin-top:2px;position:relative;z-index:1';
    ik.innerHTML = ikonHtml;
    var sv = ik.querySelector('svg');
    if (sv) { sv.style.width = '15px'; sv.style.height = '15px'; }
    ic.appendChild(ik);
    var y = el('span', 'min-w-0 break-words');
    y.innerHTML = metin;
    y.style.cssText = 'position:relative;z-index:1;line-height:1.45;';
    ic.appendChild(y);
    d.appendChild(ic);
    return d;
  }

  /* ---------- Silinen mesajı yerinde işaretle ---------- */

  /* Mesajın metin gövdesi — silme etiketini buranın sonuna asıyoruz. */
  function msGovde(satir) {
    var ub = satir.querySelector('button.inline.font-bold');
    var ad = ub && ub.parentElement;
    return (ad && ad.parentElement) || satir;
  }

  /* Çiğnenen kuralın okunur adı. Sözlükte varsa çevirisi, yoksa ham anahtar
     okunur hâle getirilir ("hate_speech" → "Hate Speech") — Kick yeni bir
     kural ekleyince etiket boş kalmasın diye. `t()` KULLANILMIYOR: o, anahtar
     yoksa anahtarın kendisini döndürüyor ve "pkKuralFoo" diye çirkin bir
     metin çıkardı; getMessage boş döndüğü için eksiği anlayabiliyoruz. */
  function silKuralAdi(k) {
    var ad = String(k || '').trim();
    if (!ad) return '';
    var anahtar = 'pkKural' + ad.replace(/(^|[_-])([a-z])/g, function (x, a, b) { return b.toUpperCase(); });
    var cev = '';
    try { cev = chrome.i18n.getMessage(anahtar) || ''; } catch (e) {}
    if (cev) return cev;
    try { if (window.__pkDebug) console.warn('[PK] sozlukte olmayan kural: ' + ad + ' (' + anahtar + ')'); } catch (e) {}
    return ad.replace(/[_-]+/g, ' ').replace(/\b\w/g, function (c) { return c.toUpperCase(); });
  }

  /* Silinen mesajı YERİNDE gösterir: Kick'in "Deleted by a moderator" yer
     tutucusu gizlenir, yerine ORİJİNAL metin ÜZERİ ÇİZİLİ basılır; yanına
     "(Silindi)", yapay zekâ sildiyse AiMod rozeti ve çiğnenen kural(lar).

     Eskiden metin "Mesajı göster" düğmesinin arkasındaydı. Kullanıcı isteğiyle
     açık hâle getirildi: neyin neden silindiği tek bakışta görünsün.
     `silinenGoster` ayarı hâlâ ana kapı — kapatan hiçbirini görmüyor.

     YER TUTUCU NASIL BULUNUYOR: kullanıcı adı düğmesinin gövdedeki atasını
     buluyoruz, ondan SONRAKİ kardeşler mesaj içeriğidir. Ada göre değil
     yapıya göre; Kick yer tutucu metnini dile göre değiştiriyor. Ata
     bulunamazsa hiç dokunulmuyor ve eski "göster" düğmesine düşülüyor —
     yanlış düğümü gizlemektense mesajı gizli bırakmak yeğdir. */
  /* ÖZ-ONARIM. Sohbet sanallaştırılmış liste: Kick satır kaplarını geri
     dönüştürüyor. İşaretlediğimiz bir kap başka bir mesaja ayrılırsa
     gizlediğimiz düğümler o mesajı da gizler — kullanıcı boş satır görür.
     Her turda satırın güncel `data-pk-mid`'i, işaretlerken sakladığımız
     kimlikle karşılaştırılıyor; tutmuyorsa bıraktığımız her şey geri alınır.
     `data-pk-mid` artık indeks değişince tazeleniyor (worker-hook). */
  function silinmisDenetle() {
    var q = document.querySelectorAll('[data-pk-silindi]');
    for (var i = 0; i < q.length; i++) {
      var satir = q[i];
      var isaret = satir.getAttribute('data-pk-silindi') || '';
      if (isaret === '1') continue;                 // eski biçim, kimliksiz
      if (satir.getAttribute('data-pk-mid') === isaret) continue;   // hâlâ aynı mesaj
      satir.removeAttribute('data-pk-silindi');
      var gz = satir.querySelectorAll('[' + SIL_GIZLE + ']');
      for (var g = 0; g < gz.length; g++) gz[g].removeAttribute(SIL_GIZLE);
      var ek = satir.querySelectorAll('.pk-sil-ek, .pk-sil-goster, .pk-sil-icerik');
      for (var e2 = 0; e2 < ek.length; e2++) ek[e2].remove();
    }
  }

  function msSilindiIsaretle(id, otoMod, kural) {
    if (!id) return false;
    var satir = document.querySelector('[data-pk-mid="' + id + '"]');
    if (!satir) return false;
    if (satir.getAttribute('data-pk-silindi')) return true;
    msStilKur();
    /* Kimliği DEĞERE yazıyoruz: `silinmisDenetle` satır geri dönüşmüş mü
       diye bunu satırın güncel `data-pk-mid`'iyle karşılaştırıyor. */
    satir.setAttribute('data-pk-silindi', id);
    var govde = msGovde(satir);
    var kayit = mesajKimlik[id];

    /* Kick'in yer tutucusunu gizle ve orijinali üzeri çizili yaz. */
    var yazildi = false;
    if (kayit && kayit.metin) {
      var ub = satir.querySelector('button.inline.font-bold');
      var adKap = ub;
      while (adKap && adKap.parentElement && adKap.parentElement !== govde) adKap = adKap.parentElement;
      if (adKap && adKap.parentElement === govde) {
        /* GİZLEME NİTELİKLE, SATIR İÇİ STİLLE DEĞİL.
           Satır içi `display:none` geri alınamıyordu: eklenti çekilince ya da
           satır geri dönüşünce Kick'in düğümü gizli kalıyordu. Nitelik hem
           `PK_KICK_ISARET` temizliğine giriyor hem `silinmisDenetle` ile tek
           satırda geri alınabiliyor.
           Metin düğümlerine DOKUNULMUYOR: niteliği yok, içeriğini silmek geri
           alınamaz olurdu. Ayracı zaten kendimiz basıyoruz. */
        var ayracVar = false;
        var n = adKap.nextSibling;
        while (n) {
          if (n.nodeType === 1) n.setAttribute(SIL_GIZLE, '1');
          else if (n.nodeType === 3 && (n.textContent || '').indexOf(':') >= 0) ayracVar = true;
          n = n.nextSibling;
        }
        /* ":" Kick'te kullanıcı adından SONRAKİ kardeşte duruyor ve onu
           gizliyoruz → ad ile metin birleşik yazılıyordu ("Seboiai mod").
           Metin düğümünde sağlam kaldıysa ikinci kez basmıyoruz. */
        if (!ayracVar) govde.appendChild(el('span', 'pk-sil-ayrac pk-sil-ek', ': '));
        var metin = el('span', 'pk-sil-metin pk-sil-ek');
        var g = mesajGovdesi(scParcalaMetin(kayit.metin), 18);
        while (g.firstChild) metin.appendChild(g.firstChild);
        govde.appendChild(metin);
        yazildi = true;
      }
    }

    govde.appendChild(el('span', 'pk-sil-et pk-sil-ek', ' ' + t('pkSilindiEtiket')));

    /* AiMod rozeti yalnız bayrak AÇIKÇA true ise. `null` "bilmiyoruz" demek;
       orada kimseyi suçlamıyoruz (msSilenAdi'ndaki aynı kural). */
    if (otoMod === true && settings.aiMod !== false) {
      govde.appendChild(el('span', 'pk-sil-cip pk-sil-cip-ai pk-sil-ek', PK_AIMOD_AD));
    }
    if (Array.isArray(kural)) {
      for (var i = 0; i < kural.length; i++) {
        var ka = silKuralAdi(kural[i]);
        if (ka) govde.appendChild(el('span', 'pk-sil-cip pk-sil-cip-kural pk-sil-ek', ka));
      }
    }

    /* Metni yerinde yazamadıysak (yapı tanınmadı) eski yol: isteğe bağlı aç. */
    if (yazildi || !kayit || !kayit.metin) return true;
    var but = el('button', 'pk-sil-goster', t('pkMesajiGoster'));
    var kutu = null;
    but.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (kutu) { kutu.remove(); kutu = null; but.textContent = t('pkMesajiGoster'); return; }
      kutu = el('div', 'pk-sil-icerik');
      var g2 = mesajGovdesi(scParcalaMetin(kayit.metin), 18);
      while (g2.firstChild) kutu.appendChild(g2.firstChild);
      govde.appendChild(kutu);
      but.textContent = t('pkMesajiGizle');
    });
    govde.appendChild(but);
    return true;
  }

  /* ---------- Olaylardan sohbete satır ---------- */

  /* Kutunun metnine "Göster" düğmesi ekler; basınca orijinal mesaj altında
     açılır. Metin soketten zaten elimizde, ek istek yok. */
  function msGosterEkle(kutu, metin) {
    var ic = kutu.querySelector('.pk-ms-cerceve'); if (!ic || !metin) return;
    var yazi = ic.lastElementChild || ic;
    /* Burada da AÇIK başlıyor — moderasyon listesiyle aynı davranış. */
    var but = el('button', 'pk-sil-goster', t('pkMesajiGizle'));
    var kap = null;
    function ac() {
      kap = el('div', 'pk-sil-icerik');
      var g = mesajGovdesi(scParcalaMetin(metin), 18);
      while (g.firstChild) kap.appendChild(g.firstChild);
      yazi.appendChild(kap);
      but.textContent = t('pkMesajiGizle');
    }
    but.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (kap) { kap.remove(); kap = null; but.textContent = t('pkMesajiGoster'); return; }
      ac();
    });
    yazi.appendChild(but);
    ac();
  }

  /* Kutudaki metin düğümlerinde geçen adları tıklanabilir yapar.
     Uzun addan kısaya bakılır: "ali" ile "aliveli" karışmasın. */
  /* `adlar` tıklanabilir olur; `korunan` yalnız EŞLEŞME SINIRI için listeye
     girer, düz metin kalır. Moderatör adını listeden tamamen çıkarırsak,
     hedefin adı onun içinde geçtiğinde (ör. mod "ahmetcan", hedef "ahmet")
     moderatörün adının BİR PARÇASI tıklanabilir oluyor — uzunluğa göre
     sıralama tam da bunu önlemek için var. */
  function msAdlariBagla(kutu, adlar, korunan) {
    var ic = kutu.querySelector('.pk-ms-cerceve'); if (!ic) return;
    var hedef = ic.lastElementChild || ic;
    var liste = [], baglanir = Object.create(null);
    function adEkle(kaynak, tiklanir) {
      for (var i = 0; i < (kaynak || []).length; i++) {
        var a = String(kaynak[i] || '').trim();
        if (!a) continue;
        if (liste.indexOf(a) < 0) liste.push(a);
        if (tiklanir) baglanir[a] = 1;
      }
    }
    adEkle(adlar, true);
    adEkle(korunan, false);
    if (!liste.length) return;
    liste.sort(function (x, y) { return y.length - x.length; });

    var cocuklar = [];
    for (var c = hedef.firstChild; c; c = c.nextSibling) cocuklar.push(c);
    for (var j = 0; j < cocuklar.length; j++) {
      var d = cocuklar[j];
      if (d.nodeType !== 3) continue;                 // yalnız metin düğümleri
      var kalan = d.nodeValue, parcalar = [];
      while (kalan) {
        var enErken = -1, bulunan = '';
        for (var k = 0; k < liste.length; k++) {
          var p = kalan.indexOf(liste[k]);
          if (p < 0) continue;
          if (enErken < 0 || p < enErken) { enErken = p; bulunan = liste[k]; }
        }
        if (enErken < 0) { parcalar.push(document.createTextNode(kalan)); break; }
        if (enErken > 0) parcalar.push(document.createTextNode(kalan.slice(0, enErken)));
        parcalar.push(baglanir[bulunan] ? msTiklanirAd(bulunan)
                                        : document.createTextNode(bulunan));
        kalan = kalan.slice(enErken + bulunan.length);
      }
      if (parcalar.length <= 1) continue;
      for (var q = 0; q < parcalar.length; q++) hedef.insertBefore(parcalar[q], d);
      hedef.removeChild(d);
    }
  }

  /* Tıklanabilir ad. Basınca o kişinin yazdıkları `yer`in altında açılır. */
  function msTiklanirAd(isim) {
    var sp = el('span', 'pk-ms-ad', isim);
    sp.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      /* Kick'in KENDİ profil kartını açar — panel listelerindeki adlarla
         (pkAdOgesi) aynı davranış. Eskiden burada kişinin BÜTÜN mesajları
         alt alta listeleniyordu: "Göster" zaten silinen mesajı açtığı için
         tekrar oluyordu, üstelik aynı görünen ad iki yerde iki farklı iş
         yapıyordu. */
      if (!profilKartiniAc(isim)) toast(t('pkTKisiSohbetteYok'));
    });
    return sp;
  }

  /* Kişinin sohbette yazdıkları — `scListe`'den, en yeni en altta.
     Kayıt yoksa null döner. */
  function msMesajSilindi(id, otoMod, kural) {
    if (settings.silinenGoster === false) return;
    var m = id && mesajKimlik[id];
    /* Önce yerinde işaretlemeyi dene. Kick balonu kaldırıyor olabilir —
       kısa bir gecikmeyle bakıp gitmişse sohbete satır olarak yaz. */
    var isaretlendi = msSilindiIsaretle(id, otoMod, kural);
    setTimeout(function () {
      try {
        if (isaretlendi && document.querySelector('[data-pk-mid="' + id + '"]')) return;
        if (!m || !m.kim) return;
        var kutu = msKutu(I_MS_SIL, modSatirYazi({ tur: 'sil', kim: m.kim, metin: m.metin,
                                                   yapan: '', otoMod: otoMod, ts: Date.now() }));
        msAdlariBagla(kutu, [m.kim]);         // ad tıklanınca yazdıkları açılsın
        msGosterEkle(kutu, m.metin);          // içerik burada da görülebilsin
        msSohbeteYaz(kutu);
      } catch (e) {}
    }, 500);
  }

  function msModOlayi(d) {
    if (settings.modSohbette === false) return;
    var ad = d.ad, ts = d.ts || Date.now();
    if (ad === 'UserBannedEvent') {
      if (d.kim) msYasakli[String(d.kim).toLowerCase()] = 1;
      var bitisMs = d.bitis ? Date.parse(d.bitis) : 0;
      var k1 = msKutu(d.kalici ? I_MS_BAN : I_MS_SUS,
        modSatirYazi({ tur: d.kalici ? 'yasak' : 'sustur', kim: d.kim || '',
                       yapan: d.yapan || '', ts: ts, bitis: bitisMs || 0 }));
      msAdlariBagla(k1, [d.kim], [d.yapan]);
      msSohbeteYaz(k1);
      return;
    }
    if (ad === 'UserUnbannedEvent') {
      if (d.kim) delete msYasakli[String(d.kim).toLowerCase()];
      var k2 = msKutu(I_MS_KALDIR,
        modSatirYazi({ tur: 'kaldir', kim: d.kim || '', yapan: d.yapan || '', ts: ts }), true);
      msAdlariBagla(k2, [d.kim], [d.yapan]);
      msSohbeteYaz(k2);
      return;
    }
    if (ad === 'ChatroomClearEvent') {
      msSohbeteYaz(msKutu(I_MS_TEMIZ, modSatirYazi({ tur: 'temizle', ts: ts })));
    }
  }

  /* Moderasyon — Kick ayar panelinin içindeki sayfa. Veri modGunluk ve
     silinenler'den; ayrı kaynak yok, metin panelinkiyle aynı. */
  /* Panel listelerinde kullanıcı adı: PureKick rozeti + isim rengi/gradyanı +
     ipucu, tıklayınca Kick'in kendi profil kartı. Rozetin göründüğü her yerde
     üçünün birden olması kuralı burada da geçerli.

     `noteUser` çağrılıyor ki o an sohbete yazmamış biri de rozet sorgusuna
     girsin — panelde ad görünüyorsa rozeti de görünmeli. */
  /* `sade` → adı SÜSSÜZ çizer: PureKick rozeti yok, rol rengi yok, abonelik
     gradyanı yok. Kick'in düz ad rengiyle kalıyor; tıklanabilirlik duruyor.

     İki ayrı sebep, aynı çözüm:
      · Rozet 18px'lik bir görsel. Adın CÜMLE İÇİNDE geçtiği moderasyon
        günlüğünde satır yüksekliğini büyütüp sarmayı kaydırıyordu.
      · Rol rengi/gradyan cümleyi alacaya çeviriyordu: aynı satırda yeşil,
        mor, kırmızı ad yan yana gelince "kim ne yaptı" okunmuyordu.
     `zorRenk` verilirse o yine kazanıyor — moderasyon günlüğü işlemi YAPANI
     böyle maviye boyuyor.

     Adın TEK BAŞINA durduğu yüzeylerde (arama, sohbetçi listesi,
     bahsedilmeler) ikisi de sorun değil; orada süs duruyor. */
  function pkAdOgesi(isim, ekSinif, zorRenk, sade) {
    var kap = el('span', 'inline-flex items-center gap-1 min-w-0');
    var k = String(isim || '').toLowerCase();
    if (!k) return kap;
    try { noteUser(isim); } catch (e) {}

    if (badgeMaps && !sade) {
      var anahtarlar = badgeKeysFor(isim), defs = badgeMaps.defs || {};
      for (var i = 0; i < anahtarlar.length; i++) {
        var def = defs[anahtarlar[i]];
        if (def) { try { kap.appendChild(makeBadge(def, anahtarlar[i], k)); } catch (e) {} }
      }
    }

    var ad = el('span', 'font-bold truncate ' + (ekSinif || 'text-white'), isim);
    ad.style.cursor = 'pointer';
    var rol = sade ? '' : kisiRol[k];
    if (rol && rolRengi(rol)) ad.style.color = rolRengi(rol);
    if (!sade && badgeMaps && badgeMaps.colorsByName) {
      try { isimBoya(ad, k); } catch (e) {}
    }
    /* Moderasyon listesi rengi ZORLAR: rol rengi ve rozet gradyanı
       `!important` ile yazıldığı için önce onları söküp sonra kendi rengimizi
       yine `!important` ile koyuyoruz — yoksa mavi/kırmızı ayrımı görünmezdi. */
    if (zorRenk) {
      ['background', '-webkit-background-clip', 'background-clip', '-webkit-text-fill-color']
        .forEach(function (p) { ad.style.removeProperty(p); });
      ad.removeAttribute('data-pk-grad');
      ad.style.setProperty('color', zorRenk, 'important');
      ad.style.fontWeight = '800';
      ad.style.padding = '0 3px';
      ad.style.borderRadius = '3px';
      if (zorRenk === '#c084fc') {
        ad.style.background = 'rgba(192, 132, 252, 0.12)';
        ad.style.border = '1px solid rgba(192, 132, 252, 0.3)';
      } else if (zorRenk === '#ef4444') {
        ad.style.background = 'rgba(239, 68, 68, 0.12)';
        ad.style.border = '1px solid rgba(239, 68, 68, 0.3)';
      } else if (zorRenk === '#53fc18') {
        ad.style.background = 'rgba(83, 252, 24, 0.12)';
        ad.style.border = '1px solid rgba(83, 252, 24, 0.3)';
      } else if (zorRenk === '#38bdf8') {
        ad.style.background = 'rgba(56, 189, 248, 0.12)';
        ad.style.border = '1px solid rgba(56, 189, 248, 0.3)';
      }
    }
    ad.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (!profilKartiniAc(isim)) toast(t('pkTKisiSohbetteYok'));
    });
    kap.appendChild(ad);
    return kap;
  }

  /* Sıra satırının başına emote görselini koyar. Adres yoksa (o emote
     yalnız eski DOM taramasından geldiyse) satır yazıyla kalır. */
  function emoteResmiEkle(satir, ad) {
    var src = scEmoteSrc[ad]; if (!src) return;
    var im = document.createElement('img');
    im.src = src; im.alt = ad; im.title = ad; im.referrerPolicy = 'no-referrer';
    im.style.cssText = 'height:24px;width:auto;flex-shrink:0;display:inline-block';
    im.addEventListener('error', function () { if (im.parentNode) im.parentNode.removeChild(im); }, { once: true });
    /* Sıra numarasından hemen sonra. */
    if (satir.children.length > 1) satir.insertBefore(im, satir.children[1]);
    else satir.appendChild(im);
  }

  /* Silinen mesajın metni. AÇIK BAŞLIYOR (kullanıcı isteği): moderasyon
     listesinde "neyi sildi" sorusunun cevabı asıl bilgi, bir tık arkasında
     durması işi yavaşlatıyordu. Düğme kalıyor — isteyen kapatabilsin.
     Hem sohbetteki kutuda hem moderasyon listesinde kullanılıyor. */
  function pkGosterAcKapa(yer, metin) {
    if (!metin) return;
    msStilKur();
    var but = el('button', 'pk-sil-goster', t('pkMesajiGizle'));
    var kap = null;
    function ac() {
      kap = el('div', 'pk-sil-icerik');
      var g = mesajGovdesi(scParcalaMetin(metin), 18);
      while (g.firstChild) kap.appendChild(g.firstChild);
      yer.appendChild(kap);
      but.textContent = t('pkMesajiGizle');
    }
    but.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (kap) { kap.remove(); kap = null; but.textContent = t('pkMesajiGoster'); return; }
      ac();
    });
    yer.appendChild(but);
    ac();                                   // açık başla
  }

  /* AYAR PANELİNDEKİ MODERASYON BÖLÜMÜ KALDIRILDI (kullanıcı kararı).
     Sekme durumu (`pkModSekme`), geri sayımı (`pkModSayacKur`) ve liste
     çizicisi (`pkModListeCiz`) yalnız o bölüme aitti. Sohbet şeridindeki
     panelin KENDİ eşdeğerleri var (`modSekme`, `modPanelSayacKur`,
     `modGovdeDoldur`) ve onlar duruyor; ortak olan satır çizici
     `modSatirlariCiz` de duruyor. */

  /* Ceza süresi metni — TEK birim, bakışta okunsun diye.
     60 sn'den sonra dakika, 60 dk'dan sonra saat, 24 saatten sonra gün.
     Eskiden üç ayrı yerde yalnız dakika/saniye vardı: bir günlük susturma
     "1440 dakika", bir haftalık "10080 dakika" diye yazılıyordu.

     SAAT/GÜN'DE ONDALIK VAR: 90 dakikayı yuvarlayıp "2 saat" demek yalan
     olurdu, "1,5 saat" doğru. Ayırıcı dile göre değişiyor (tr virgül,
     en nokta) — toLocaleString hallediyor. Dakika tam sayı kalıyor:
     Kick zaman aşımını zaten tam dakika veriyor. */
  /* Ayırıcı EKLENTİ diline göre, tarayıcı diline göre DEĞİL: arayüz
     İngilizceyken Türkçe tarayıcıda "1,5 hours" çıkıyordu. */
  var pkSureDil = null;
  function pkSureMetin(y) {
    if (pkSureDil === null) {
      try { pkSureDil = chrome.i18n.getUILanguage() || undefined; } catch (e) { pkSureDil = undefined; }
    }
    try { return y.toLocaleString(pkSureDil); } catch (e) { return String(y); }
  }
  /* Tekil/çoğul AYRI anahtar. Sözlükte yalnız çoğul vardı ve İngilizcede
     "1 hours", Almancada "1 Tage", İspanyolcada "1 días" yazıyordu. Türkçe
     ve Rusça (kısaltma) etkilenmiyor ama anahtarlar 7 dilde de var.

     TEKİL/ÇOĞUL YUVARLANMIŞ SAYIYA BAKAR, ham değere değil. Önceki sürümde
     ayrıydı ve uyuşmuyordu: 90000 sn için ham değer 1,0417 (≠1 → çoğul) ama
     yazılan sayı 1 idi → "1 days". Tek yerde yuvarlayıp ikisinde de onu
     kullanıyoruz. Ondalıkta doğal olarak çoğul çıkar: "1.5 hours". */
  function pkSureParca(anahtar, n, tamSayi) {
    var y = tamSayi ? n : Math.round(n * 10) / 10;
    return t(y === 1 ? anahtar + '1' : anahtar, [pkSureMetin(y)]);
  }
  function pkSureYazi(sn) {
    sn = Math.max(0, Math.round(sn));
    if (sn >= 86400) return pkSureParca('pkSureGun',  sn / 86400);
    if (sn >= 3600)  return pkSureParca('pkSureSaat', sn / 3600);
    if (sn >= 60)    return pkSureParca('pkSureDakika', Math.round(sn / 60), true);
    return pkSureParca('pkSureSaniye', sn, true);
  }

  /* Kalan süre: bir saatin altında canlı sayaç (5:04), üstünde kaba birim
     ("23 saat") — Kick'in kendi zaman aşımı kartındaki gibi. Yukarıdaki
     pkSureYazi'den AYRI: orası sabit metin, burası saniye saniye akıyor. */
  function pkSusturmaKalan(ms) {
    var sn = Math.max(0, Math.round(ms / 1000));
    if (sn >= 86400) return t('pkSureGun', [String(Math.floor(sn / 86400))]);
    if (sn >= 3600) return t('pkSureSaat', [String(Math.floor(sn / 3600))]);
    var dk = Math.floor(sn / 60), s2 = sn % 60;
    return dk + ':' + (s2 < 10 ? '0' : '') + s2;
  }

  function pkTarihSaat(ms) {
    var d = new Date(ms), ik = function (n) { return (n < 10 ? '0' : '') + n; };
    return d.getFullYear() + '-' + ik(d.getMonth() + 1) + '-' + ik(d.getDate()) +
           ' ' + ik(d.getHours()) + ':' + ik(d.getMinutes());
  }


  /* Moderasyon satır renkleri — ikisi de Kick'in kendi paletinden. */
  /* Moderasyon günlüğünde ÜÇ renk, hepsi Kick'in kendi paletinden:
       yapan  → mavi   (Kick moderatör rozetinin rengi)
       ceza   → kırmızı (yasaklandı / susturuldu)
       af     → yeşil   (yasağı/cezası kaldırıldı)
     Renk satırın SONUCUNU bir bakışta veriyor; cümleyi okumadan "kötü mü
     iyi mi" anlaşılıyor. Kendi rol rengi/abonelik gradyanı burada
     kapatılıyor (bkz. `sade`) — o üç renkle karışıp anlamı bulandırıyordu. */
  var MOD_RENK_MOD  = '#c084fc';    // Mor moderatör rozet rengi
  var MOD_RENK_CEZA = '#ef4444';    // Canlı kırmızı ceza rengi
  var MOD_RENK_AF   = '#53fc18';    // Kick birincil yeşili — af rengi
  var MOD_RENK_SIL  = '#38bdf8';    // Silinen mesaj hedef kullanıcı rengi

  /* Cümleyi adlara göre parçalara böler: [{m:'düz metin'} | {ad:'Ali',renk}].
     UZUNDAN KISAYA eşleşir — kısa ad uzun adın içinde kalıp onu bölmesin
     (ör. "Ali" adı "Alican"ın içinde). */
  function modAdBol(yazi, adaylar) {
    var parca = [{ m: String(yazi || '') }];
    (adaylar || []).filter(function (a) { return a && a.ad; })
      .sort(function (a, b) { return b.ad.length - a.ad.length; })
      .forEach(function (a) {
        var yeni = [];
        parca.forEach(function (p) {
          if (p.ad) { yeni.push(p); return; }            // zaten ad parçası
          var kalan = p.m, i;
          while ((i = kalan.toLowerCase().indexOf(a.ad.toLowerCase())) >= 0) {
            if (i > 0) yeni.push({ m: kalan.slice(0, i) });
            yeni.push({ ad: kalan.substr(i, a.ad.length), renk: a.renk });
            kalan = kalan.slice(i + a.ad.length);
          }
          if (kalan) yeni.push({ m: kalan });
        });
        parca = yeni;
      });
    return parca;
  }

  /* Bir moderasyon olayının hangi adı hangi renkte olacağı. */
  function modAdaylar(x) {
    var hedef = '';
    if (x.tur === 'yasak' || x.tur === 'sustur') hedef = MOD_RENK_CEZA;
    else if (x.tur === 'kaldir') hedef = MOD_RENK_AF;
    else if (x.tur === 'sil') hedef = MOD_RENK_SIL;
    return [
      x.yapan ? { ad: x.yapan, renk: MOD_RENK_MOD } : null,
      x.kim ? { ad: x.kim, renk: hedef } : null
    ].filter(Boolean);
  }

  /* MODERASYON SATIRLARINI VERİLEN ELEMANA ÇİZER — ORTAK ÇİZİCİ.
     Hem ayar panelindeki liste hem sohbet şeridinin paneli bunu çağırıyor;
     böylece kullanıcı bilgisi tasarımı (renkli tıklanabilir adlar, şerit,
     sonuç sayısı, silinen içeriği aç/kapa, susturmada canlı geri sayım) tek
     yerde ve iki yüzeyde birebir aynı. `sekme`: 'mod' | 'silinen' | 'sustur'. */
  function modSatirlariCiz(liste, sekme) {
    if (!liste) return;
    liste.textContent = '';
    /* Günlük sekmelerinde satır arası boşluk YOK — şeritler bitişik olmazsa
       "bir gri bir yok" ayrımı okunmuyor. Susturma kartları boşluklu kalır. */
    liste.className = 'flex flex-col px-2' + (sekme === 'sustur' ? ' gap-1' : '');
    if (sekme === 'sustur') { pkModSusturmaCiz(liste); return; }

    /* İKİSİ DE modGunluk'tan. `silinenler` ile birleştirmek her satırı iki
       kez gösteriyordu; ayrıca modGunluk kaydı "kim sildi" bilgisini taşıyor. */
    var silMi = sekme === 'silinen';
    var kayit = [];
    for (var i = 0; i < modGunluk.length; i++) {
      var e2 = modGunluk[i];
      if (silMi ? e2.tur === 'sil' : e2.tur !== 'sil') kayit.push(e2);
    }
    if (!kayit.length) {
      liste.appendChild(el('div', 'text-sm text-subtle', t(silMi ? 'pkSilinenYok' : 'pkModBos')));
      return;
    }
    liste.appendChild(el('div', 'text-xs text-subtle', t('pkSonucSayisiN', [String(kayit.length)])));
    var n = Math.min(kayit.length, 150);
    /* YENİ OLAN ÜSTTE: sondan başlayıp geriye. Gösterilen küme aynı (en
       yeni `n` kayıt), yalnız sıra ters. `sira` 0'dan arttığı için şeritleme
       bozulmuyor. */
    for (var j = kayit.length - 1, sira = 0; j >= kayit.length - n; j--, sira++) {
      var x = kayit[j];
      /* ŞERİT: bir satır gri, bir satır zeminsiz. */
      var r = el('div', 'flex items-start gap-2 text-sm rounded' + pkSeritSinif(sira));
      r.style.cssText = 'padding:3px 6px';
      r.appendChild(el('span', 'text-subtle shrink-0', scSaat(x.ts)));
      var sag = el('span', 'min-w-0 break-words');
      /* Adlar CÜMLENİN İÇİNDE tıklanabilir ve renkli. Eskiden hedef ad ayrıca
         başa da ekleniyordu: aynı ad iki kez görünüyordu. */
      modAdBol(modSatirYazi(x), modAdaylar(x)).forEach(function (p) {
        if (p.ad) {
          var adEl = pkAdOgesi(p.ad, null, p.renk || null, true);
          // Moderasyon yapan kişi: Mod ise 🛡️, Yayıncı ise 👑, Bot ise 🤖
          if (x.yapan && p.ad.toLowerCase() === x.yapan.toLowerCase()) {
            var k = p.ad.toLowerCase();
            var rol = kisiRol[k];
            var rolIkon = '🛡️';
            if (rol === 'broadcaster' || (scKanal && scKanal.toLowerCase() === k)) rolIkon = '👑';
            else if (rol === 'bot' || k === 'aimod') rolIkon = '🤖';
            var rTag = el('span', 'font-black mr-1 text-xs', rolIkon);
            adEl.insertBefore(rTag, adEl.firstChild);
          }
          sag.appendChild(adEl);
        } else {
          sag.appendChild(document.createTextNode(p.m));
        }
      });
      if (x.tur === 'sil' && x.metin) pkGosterAcKapa(sag, x.metin);
      r.appendChild(sag);
      liste.appendChild(r);
    }
  }

  /* Ayar panelindeki liste — ortak çiziciyi kendi sekmesiyle çağırır. */
  /* Sekme rozetindeki sayı — ayar panelinin sonuç sayısıyla AYNI kaynak.
     Silinen sayısı `silinenler` dizisinden DEĞİL, modGunluk'un 'sil'
     kayıtlarından; yoksa iki yüzey iki farklı sayı gösterirdi. */
  function modSekmeSayi(sekme) {
    if (sekme === 'sustur') { susturmaTemizle(); return Object.keys(aktifSusturma).length; }
    var sil = sekme === 'silinen', n = 0;
    for (var i = 0; i < modGunluk.length; i++) if ((modGunluk[i].tur === 'sil') === sil) n++;
    return n;
  }

  /* Kick'in kendi zaman aşımı kartındaki bilgiler: ad · durum · kalan · bitiş. */
  function pkModSusturmaCiz(liste) {
    susturmaTemizle();
    var adlar = Object.keys(aktifSusturma);
    if (!adlar.length) { liste.appendChild(el('div', 'text-sm text-subtle', t('pkCezaYok'))); return; }
    adlar.sort(function (a, b) { return aktifSusturma[a] - aktifSusturma[b]; });   // önce bitecek üstte
    liste.appendChild(el('div', 'text-xs text-subtle', t('pkSonucSayisiN', [String(adlar.length)])));
    adlar.forEach(function (k) {
      var bitis = aktifSusturma[k];
      var r = el('div', 'bg-surface-highest rounded flex flex-col gap-1 border border-red-500/20');
      r.style.cssText = 'padding:10px 12px; margin-bottom:4px; border-left:3px solid #ef4444;';
      var ust = el('div', 'flex items-center justify-between gap-2');
      
      var adKapsayici = el('div', 'flex items-center gap-1.5');
      var cezaIkon = el('span', 'text-xs font-black text-red-400 bg-red-500/10 px-1.5 py-0.5 rounded border border-red-500/30', '🚫 Susturuldu');
      adKapsayici.appendChild(cezaIkon);
      var adEl = pkAdOgesi(susturmaAdi[k] || k, 'text-base font-black', MOD_RENK_CEZA, true);
      adKapsayici.appendChild(adEl);
      ust.appendChild(adKapsayici);

      var kalan = el('span', 'text-sm font-black text-primary-base shrink-0 bg-primary-base/10 px-2 py-0.5 rounded', pkSusturmaKalan(bitis - Date.now()));
      kalan.setAttribute('data-pk-kalan', String(bitis));      // sayaç bunu günceller
      ust.appendChild(kalan);
      r.appendChild(ust);
      r.appendChild(el('div', 'text-xs text-gray-300 font-medium', t('pkZamanAsimi')));
      r.appendChild(el('div', 'text-[11px] text-subtle', t('pkSusturmaBitis', [pkTarihSaat(bitis)])));
      liste.appendChild(r);
    });
  }

  /* ══════════════════════════════════════════════════════════════════════
   * SOHBETİN ÜSTÜNDEKİ MODERASYON ŞERİDİ
   * Panelin kapısı. Panel koddaydı ama onu açan hiçbir yer yoktu.
   * ══════════════════════════════════════════════════════════════════════ */
  var MOD_SERIT = 'data-pk-mod-serit';

  function modSeritKaldir() {
    var q = document.querySelectorAll('[' + MOD_SERIT + ']');
    for (var i = 0; i < q.length; i++) q[i].remove();
  }

  /* Mesaj alanını tutan esnek sütun. Sınıfla arıyoruz, ölçüyle değil:
     sohbet paneli yeniden boyutlanıyor. */
  function modSeritKolu() {
    var m = araAlan(); if (!m) return null;
    var k = m;
    while (k && !(k.classList && k.classList.contains('flex-col') && k.classList.contains('flex-1'))) {
      k = k.parentElement;
    }
    return k;
  }

  function modPanelAcikMi() { return !!document.getElementById(MOD_ID); }

  /* Susturma sekmesindeki canlı geri sayım. (Ayar panelinde de bir eşi vardı;
     o bölüm kaldırılınca tek kalan bu oldu.) */
  var modPanelSayac = 0;
  function modPanelSayacDur() { if (modPanelSayac) { clearInterval(modPanelSayac); modPanelSayac = 0; } }
  function modPanelSayacKur() {
    modPanelSayacDur();
    if (modSekme !== 'sustur') return;
    modPanelSayac = setInterval(function () {
      var pn = document.getElementById(MOD_ID);
      if (!pn || !document.contains(pn)) { modPanelSayacDur(); return; }   // panel kapandı
      if (susturmaTemizle()) {                                            // biri düştü → yeniden çiz
        var g0 = pn.querySelector('[data-pk-mod-govde]'); if (g0) modGovdeDoldur(g0); return;
      }
      var k = pn.querySelectorAll('[data-pk-kalan]');
      for (var i = 0; i < k.length; i++) {
        k[i].textContent = pkSusturmaKalan(Number(k[i].getAttribute('data-pk-kalan')) - Date.now());
      }
    }, 1000);
  }

  function modSeritKur() {
    if (!settings.consented || settings.modSerit === false || settings.modGunluk === false) {
      modSeritKaldir(); return;
    }
    var kol = modSeritKolu(); if (!kol) return;
    var st = kol.querySelector(':scope > [' + MOD_SERIT + ']');
    if (!st) {
      st = el('div', 'flex w-full shrink-0 items-center gap-1 border-outline-decorative border-b');
      st.setAttribute(MOD_SERIT, '1');
      st.style.cssText = 'padding:6px 8px';
      kol.insertBefore(st, kol.firstChild);
    }
    /* İmza kapısı: şerit ana turda çalışıyor; her turda yeniden yazılırsa
       tıklanmak üzere olan düğme altından kayar. */
    var acik = modPanelAcikMi();
    var imza = (acik ? 'a' : 'k') + '|' + modSekme + '|' + modGunluk.length + '|' +
               Object.keys(aktifSusturma).length;
    if (st.getAttribute('data-pk-imza') === imza) return;
    st.setAttribute('data-pk-imza', imza);
    st.textContent = '';

    [['mod', t('pkModSekmeOlaylar'), modSekmeSayi('mod')],
     ['silinen', t('pkModSekmeSilinen'), modSekmeSayi('silinen')],
     ['sustur', t('pkModSekmeSustur'), modSekmeSayi('sustur')]
    ].forEach(function (sk) {
      /* Seçili görünüm YALNIZ panel açıkken: panel kapalıyken hiçbir sekme
         vurgulu olmamalı, yoksa "açık" sanılır. */
      var secili = acik && modSekme === sk[0];
      var b = el('button', 'flex-1 min-w-0 h-7 px-1 rounded text-xs font-semibold transition-all ' +
        'flex items-center justify-center gap-1 truncate ' +
        (secili ? 'bg-secondary-base text-white' : 'text-subtle betterhover:hover:bg-surface-highest'));
      b.type = 'button';
      b.style.cssText = 'border:0;cursor:pointer';
      b.setAttribute('data-pk-mod-sekme', sk[0]);
      b.appendChild(el('span', 'truncate', sk[1]));
      if (sk[2]) {
        var sy = el('span', 'shrink-0 text-xs font-bold', String(sk[2]));
        if (!secili) sy.style.color = 'rgb(83,252,24)';
        b.appendChild(sy);
      }
      b.addEventListener('click', function (e) {
        e.preventDefault(); e.stopPropagation();
        /* Aynı sekme + açık -> kapat. Başka sekme -> ona geç (panel açık kalır). */
        if (modPanelAcikMi() && modSekme === sk[0]) modPanelKapat();
        else { modSekme = sk[0]; modPanelAc(); }
        modSeritKur();
      });
      st.appendChild(b);
    });
  }

  /* Sohbetin ÜSTÜNE açılan panellerimiz. Hepsi aynı yeri kaplıyor, biri
     açılırken ötekiler kapanmalı. */
  function pkUstPanelleriKapat() {
    try { modPanelKapat(); } catch (e) {}
    try { araKapat(); } catch (e) {}
    try { mentionPanelKapat(); } catch (e) {}
  }

  function modPanelAc() {
    if (settings.modGunluk === false) { toast(t('pkTModGunluguKapali')); return; }
    pkUstPanelleriKapat();
    /* ŞERİDİN ALTINA, SOHBET SÜTUNUNUN AKIŞINA giriyor.
       Eskiden mesaj kabının içine mutlak konumla basılıyordu ve GÖRÜNMÜYORDU:
       o kap kayan ve içerik-kısıtlı, sohbet dibe kayınca panel görüş dışında
       kalıyordu (canlıda ölçüldü). */
    var kol = modSeritKolu(); if (!kol) { toast(t('pkTSohbetAlaniYok')); return; }

    /* AYAR SAYFAMIZ DA KAPANIYOR: ikisi de sohbet sütununu kaplıyor, açık
       kalan öteki panelin altında görünmez bir katman bırakıyordu.
       KAP DENETİMİNDEN SONRA: yukarıda çıkılırsa panel hiç açılmıyor demektir,
       kullanıcının açık sayfasını boşuna kapatmayalım. */
    pkAyarSayfaKapat();

    var p = el('div', 'bg-surface-base border-outline-decorative flex flex-col ' +
      'overflow-hidden border-b text-white');
    p.id = MOD_ID;
    /* Ölçü aşağıda, kap bulunduktan sonra veriliyor (şeridin yüksekliği
       gerekiyor). */

    /* SEKME SATIRI YOK — sekmeler şeridin kendisi. Burada da olsaydı seçili
       durum iki yerde ayrı ayrı yönetilirdi. */
    var govde = el('div', 'flex-1 min-h-0 overflow-y-auto');
    govde.setAttribute('data-pk-mod-govde', '1');
    /* Yatay boşluğu iç liste (`px-2`) veriyor — ayar paneliyle aynı ritim. */
    govde.style.cssText = 'padding:8px 0;overscroll-behavior:contain';
    modGovdeDoldur(govde);
    p.appendChild(govde);

    /* ══════════════════════════════════════════════════════════════════
       SÜTUNUN ÜSTÜNE BİNDİRİYOR — AKIŞA GİRMİYOR.

       Akışa girdiğinde mesaj listesi yeterince daralmıyor ve altındaki
       SABİTLENMİŞ MESAJ ile hediye/sohbetçi şeridi aşağı kayıp görüş dışında
       kalıyordu (kullanıcı bildirdi). Kardeşlere `min-height:0` vermek de
       yetmedi: kayan liste doğrudan çocuk değil, daha derinde.

       Mutlak konum SÜTUNA göre veriliyor. Daha önce mesaj KABINA konulmuş ve
       görünmez olmuştu; sebep o kabın kayması ve içerik-kısıtlı olmasıydı.
       `kol` kaymıyor — kayan, onun içindeki liste. Bu yüzden panel her
       durumda görüş alanında kalıyor.

       Yükseklik SABİT YARIM: içerik azken ("henüz moderasyon işlemi yok")
       panel iki satır boyunda açılıyordu, kullanıcı her seferinde aynı
       büyüklüğü bekliyor. `max-height` şeridin altında kalan yeri aşmasın
       diye ayrıca sınırlıyor. */
    /* SÜTUNU YIĞIN BAĞLAMI YAP (`isolation:isolate`).
       Sabitlenmiş mesaj şeridinin z-index'i bizimkinden yüksekti ve panelin
       üstüne biniyordu (kullanıcı bildirdi). Sadece sayıyı büyütmek YANLIŞ
       olurdu: `position:relative` tek başına yığın bağlamı KURMUYOR, yani
       büyük bir değer panelin PureKick'in kendi katmanlarının (ayar paneli,
       toast) da üstüne çıkmasına yol açardı.
       `isolation:isolate` sütunu kendi bağlamına kapatıyor: içeride her şeyin
       üstündeyiz, dışarıda hiçbir şeyi örtmüyoruz. `z-index` değeri de bu
       yüzden serbestçe büyük seçilebiliyor.
       İkisi de bizden geldiyse kapanışta geri alınıyor. */
    try {
      var ks = getComputedStyle(kol);
      if (ks.position === 'static' || ks.isolation !== 'isolate') {
        if (ks.position === 'static') kol.style.setProperty('position', 'relative');
        kol.style.setProperty('isolation', 'isolate');
        kol.setAttribute(MOD_KONUM, '1');
      }
    } catch (e) {}
    var st = kol.querySelector(':scope > [' + MOD_SERIT + ']');
    var ust = 0;
    try { ust = st ? Math.round(st.getBoundingClientRect().height) : 0; } catch (e) {}
    /* Büyük değer güvenli: sütun `isolation:isolate` ile yalıtıldı, panel o
       bağlamdan dışarı çıkamıyor. */
    p.style.cssText = 'position:absolute;left:0;right:0;top:' + ust + 'px;z-index:2147483000;' +
      'height:50%;max-height:calc(100% - ' + ust + 'px);min-height:0;' +
      'box-shadow:0 10px 24px rgba(0,0,0,.45)';
    kol.appendChild(p);
    modPanelSayacKur();
  }

  function modGovdeDoldur(govde) {
    if (!govde) return;
    govde.textContent = '';
    /* AYAR PANELİYLE BİREBİR AYNI İÇERİK: ortak çizici. İç liste ayrı bir
       eleman çünkü `modSatirlariCiz` sınıfı ezer; govde kaydırma kabı olarak
       kalıyor. */
    var liste = el('div', 'flex flex-col gap-1 px-2');
    govde.appendChild(liste);
    modSatirlariCiz(liste, modSekme);
  }


  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — VİDEO İSTATİSTİKLERİ + YAYIN SÜRESİ
   * Tüm veriler <video> elementinden okunur; hiçbir ağ isteği yapılmaz.
   * FPS ve bit hızı anlık ölçülemez — iki örnek arasındaki FARKTAN hesaplanır,
   * bu yüzden panel açık kaldıkça değer oturur (ilk saniyede "—" görünür).
   * ══════════════════════════════════════════════════════════════════════ */
  var VISTAT_ID = 'pk-vistat';
  var vsOnceki = null;                 // { ts, kare, bayt }
  var vsZaman = 0;

  function vistatKapat() {
    clearInterval(vsZaman); vsZaman = 0; vsOnceki = null;
    var p = document.getElementById(VISTAT_ID); if (p) p.remove();
  }

  function vsOku() {
    var v = oynaticiVideosu(); if (!v) return null;
    var q = null;
    try { q = v.getVideoPlaybackQuality ? v.getVideoPlaybackQuality() : null; } catch (e) {}
    var toplam = q ? q.totalVideoFrames : (v.webkitDecodedFrameCount || 0);
    var dusen = q ? q.droppedVideoFrames : (v.webkitDroppedFrameCount || 0);
    var bayt = v.webkitVideoDecodedByteCount || 0;

    var simdi = Date.now(), fps = null, bit = null;
    if (vsOnceki) {
      var dt = (simdi - vsOnceki.ts) / 1000;
      if (dt > 0.2) {
        if (toplam > vsOnceki.kare) fps = Math.round((toplam - vsOnceki.kare) / dt);
        if (bayt > vsOnceki.bayt) bit = Math.round(((bayt - vsOnceki.bayt) * 8) / dt / 1000);   // kbps
      }
    }
    vsOnceki = { ts: simdi, kare: toplam, bayt: bayt };

    var b = v.buffered;
    var sonUc = b.length ? b.end(b.length - 1) : 0;
    var tampon = b.length ? Math.max(0, sonUc - v.currentTime) : 0;
    var HAZIR = ['HAVE_NOTHING', 'HAVE_METADATA', 'HAVE_CURRENT_DATA', 'HAVE_FUTURE_DATA', 'HAVE_ENOUGH_DATA'];
    var AG = ['NETWORK_EMPTY', 'NETWORK_IDLE', 'NETWORK_LOADING', 'NETWORK_NO_SOURCE'];

    return {
      cozunurluk: v.videoWidth ? (v.videoWidth + '×' + v.videoHeight) : '—',
      fps: fps == null ? '—' : fps + ' fps',
      bit: bit == null ? '—' : (bit >= 1000 ? (Math.round(bit / 100) / 10) + ' Mbps' : bit + ' kbps'),
      kare: toplam ? (dusen + '/' + toplam + '  (%' + (Math.round((dusen / toplam) * 1000) / 10) + ')') : '—',
      tampon: tampon.toFixed(1) + ' sn',
      gecikme: (!vodSayfasi() && b.length) ? tampon.toFixed(1) + ' sn' : '—',
      hiz: (Math.round(v.playbackRate * 100) / 100) + 'x',
      ses: v.muted ? t('pkKapali') : ('%' + Math.round(v.volume * 100)),
      sure: vodSayfasi() && v.duration ? (Math.floor(v.currentTime) + ' / ' + Math.floor(v.duration) + ' sn') : '—',
      hazir: HAZIR[v.readyState] || String(v.readyState),
      ag: AG[v.networkState] || String(v.networkState),
      duraklatildi: t(v.paused ? 'pkEvet' : 'pkHayir')
    };
  }

  function vistatCiz() {
    var p = document.getElementById(VISTAT_ID); if (!p) return;
    /* Panel açılışta oynatıcının sol üstüne oturuyordu ama orada ÇAKILI
       kalıyordu: tiyatro moduna geçince ya da sohbet paneli sürüklenince
       oynatıcı kayıyor, panel havada kalıyordu. Saniyelik tazelemede konumu da
       güncelliyoruz. */
    var v0 = oynaticiVideosu();
    if (v0) {
      var r0 = v0.getBoundingClientRect();
      if (r0.width > 100) {
        var sl = Math.round(Math.max(8, r0.left + 12)), st = Math.round(Math.max(8, r0.top + 12));
        if (p.style.left !== sl + 'px') p.style.left = sl + 'px';
        if (p.style.top !== st + 'px') p.style.top = st + 'px';
      }
    }
    var d = vsOku();
    var govde = p.querySelector('[data-pk-vs]'); if (!govde) return;
    govde.textContent = '';
    if (!d) { govde.appendChild(el('div', 'text-sm text-subtle', t('pkTOynaticiYokNokta'))); return; }
    [[t('pkVsCozunurluk'), d.cozunurluk], ['FPS', d.fps], [t('pkVsBitHizi'), d.bit],
     [t('pkVsDusenKare'), d.kare], [t('pkVsTampon'), d.tampon], [t('pkVsCanliUzaklik'), d.gecikme],
     [t('pkOynatmaHizi'), d.hiz], [t('pkSes'), d.ses], [t('pkVsKonum'), d.sure],
     [t('pkVsHazir'), d.hazir], [t('pkVsAg'), d.ag], [t('pkVsDuraklatildi'), d.duraklatildi]
    ].forEach(function (p2) {
      var r = el('div', 'flex items-center justify-between gap-3');
      r.appendChild(el('div', 'text-xs text-subtle', p2[0]));
      r.appendChild(el('div', 'text-xs font-bold text-white', String(p2[1])));
      govde.appendChild(r);
    });
  }

  function vistatAc() {
    if (document.getElementById(VISTAT_ID)) { vistatKapat(); return; }
    var v = oynaticiVideosu(); if (!v) { toast(t('pkTOynaticiYok')); return; }
    var vr = v.getBoundingClientRect();

    var p = el('div', 'bg-surface-base rounded shadow-lg flex flex-col gap-2 ' +
      'will-change-[opacity,transform] animate-slideUpAndFade');
    p.id = VISTAT_ID;
    p.style.cssText = 'position:fixed;z-index:2147483080;width:260px;padding:12px;' +
      'border:1px solid rgba(240,241,242,.16)';
    var bas = el('div', 'flex items-center justify-between gap-2');
    bas.appendChild(el('div', 'text-sm font-bold text-white', t('pkVideoIstatistikleri')));
    var kapat = el('button', 'inline-flex items-center justify-center rounded state-layer-surface text-white shrink-0');
    kapat.style.cssText = 'width:24px;height:24px;padding:0;background:none;border:0;cursor:pointer';
    kapat.innerHTML = I_KAPAT;
    kapat.addEventListener('click', function (e) { e.stopPropagation(); vistatKapat(); });
    bas.appendChild(kapat);
    p.appendChild(bas);
    var govde = el('div', 'flex flex-col gap-1');
    govde.setAttribute('data-pk-vs', '1');
    p.appendChild(govde);
    pkKatmanaEkle(p);

    // oynatıcının sol üstüne otur
    p.style.left = Math.round(Math.max(8, vr.left + 12)) + 'px';
    p.style.top = Math.round(Math.max(8, vr.top + 12)) + 'px';

    vsOnceki = null;
    vistatCiz();
    vsZaman = setInterval(vistatCiz, 1000);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — SOHBETTE KİŞİ GİZLEME · ÖNCELİKLİ KİŞİLER · EMOTE ÖNİZLEME
   * Üçü de tamamen yerel; liste chrome.storage.local'da durur.
   *
   * GİZLEME: Kick'in "sustur" özelliği sunucuya gider ve hesabına bağlıdır;
   * bu ise yalnız SENİN ekranında mesajı saklar, kimseye bildirilmez.
   * ÖNCELİK: seçtiğin kişilerin mesajları soluk değil, kenarında renkli çizgi.
   * ══════════════════════════════════════════════════════════════════════ */
  var gizliKisi = Object.create(null);      // nick(küçük) -> 1
  var oncelikli = Object.create(null);      // nick(küçük) -> 1

  function kisiKaydet() {
    try {
      chrome.storage.local.set({ pkGizliKisi: Object.keys(gizliKisi), pkOncelikli: Object.keys(oncelikli) });
    } catch (e) {}
  }
  function kisiGizle(ad) {
    var k = String(ad || '').toLowerCase(); if (!k) return;
    gizliKisi[k] = 1; kisiKaydet(); kisiUygula();
  }
  function kisiGoster(ad) {
    var k = String(ad || '').toLowerCase(); if (!k) return;
    delete gizliKisi[k]; kisiKaydet();
    var els = document.querySelectorAll('[data-pk-kisigizli="' + k + '"]');
    for (var i = 0; i < els.length; i++) { els[i].style.removeProperty('display'); els[i].removeAttribute('data-pk-kisigizli'); }
  }
  function oncelikDegis(ad) {
    var k = String(ad || '').toLowerCase(); if (!k) return false;
    if (oncelikli[k]) { delete oncelikli[k]; } else { oncelikli[k] = 1; }
    kisiKaydet(); kisiUygula();
    return !!oncelikli[k];
  }

  /* Her turda sohbet balonlarını gezip gizle/öne çıkar. Mesaj kimliği yerine
     görünen kullanıcı adı kullanılır — sanal liste düğümleri geri dönüştürdüğü
     için işaret her turda yeniden doğrulanır. */
  function kisiUygula() {
    if (!settings.consented) return;
    var msgs = sohbetSatirlari();
    for (var i = 0; i < msgs.length; i++) {
      var m = msgs[i];
      var ub = usernameButton(m); if (!ub) continue;
      var k = (ub.textContent || '').trim().toLowerCase(); if (!k) continue;

      // gizleme
      if (settings.kisiGizle !== false && gizliKisi[k]) {
        if (m.style.display !== 'none') {
          m.style.setProperty('display', 'none', 'important');
          m.setAttribute('data-pk-kisigizli', k);
        }
        continue;
      }
      if (m.getAttribute('data-pk-kisigizli')) {
        m.style.removeProperty('display'); m.removeAttribute('data-pk-kisigizli');
      }

      rolBoya(m, k);                                   // 10.5 rol vurgusu
      ozelAdUygula(ub, k);                             // hesaba özel akan isim

      /* YEŞİL ŞERİT — iki kaynağı var: öncelikli kişi ve takip edilen kelime.
         İkisi de AYNI işareti kullanıyor; ayrı öznitelik tutsaydık biri
         diğerinin şeridini söker, aynı mesajda ikisi birden varken satır
         her turda bir yanıp bir sönerdi. */
      var oncelikVar = (settings.oncelik !== false && !!oncelikli[k]) ||
                       (settings.kelimeVurgu !== false && kelimeVurgusuVar(m));
      var isaretli = m.getAttribute('data-pk-oncelik') === '1';
      if (oncelikVar && !isaretli) {
        m.setAttribute('data-pk-oncelik', '1');
        m.style.setProperty('border-left', '3px solid rgb(83,252,24)', 'important');
        m.style.setProperty('padding-left', '5px', 'important');
      } else if (!oncelikVar && isaretli) {
        m.removeAttribute('data-pk-oncelik');
        m.style.removeProperty('border-left'); m.style.removeProperty('padding-left');
      }
    }
  }

  /* Satır bu kullanıcıya aitse ismini akan gradyanla boyar, değilse söker.
     Her turda yeniden doğrulanıyor çünkü Kick satır düğümlerini geri
     dönüştürüyor: aynı düğüm bir sonraki turda başkasının mesajı olabilir.

     SATIRIN KUTU ÖLÇÜSÜNE DOKUNULMUYOR. Sohbet sanallaştırılmış liste:
     satırlar `position:absolute` + `translateY(...)` ile, ölçülmüş
     yüksekliklerden hesaplanan yerlere konuyor. Dolgu/kenarlık/dış boşluk
     eklemek Kick'in kaydettiği yükseklikle boyananı ayırıyor ve mesajlar
     arasında boşluk açıyor. `rolBoya` da bu yüzden yalnız `box-shadow` ve
     `background-color` kullanıyor — ikisi de yüksekliği değiştirmez. */
  /* `ub` ÇAĞIRANDAN GELİYOR. Eskiden burada `usernameButton(m)` yeniden
     çağrılıyordu; `kisiUygula` onu zaten bulmuş oluyor, yani ekrandaki her
     mesaj için her turda iki fazladan DOM sorgusu boşuna dönüyordu — hem de
     listede olmayan (yani neredeyse tüm) kullanıcılar için. */
  /* ══════════════════════════════════════════════════════════════════════
   * AKAN GÖRÜNÜMÜN TEK KAYNAĞI
   *
   * Sohbetteki akan isim/çerçeve ile panelin tanıtım demosu AYNI koddan
   * besleniyor. Ayrı ayrı yazılsaydı biri değişince diğeri sessizce eski
   * kalırdı; demo "gerçekte ne göreceksin" diyorsa birebir aynı olmalı.
   * Sitedeki önizleme (dashboard.php) de bu değerlerin kopyası.
   *
   * SON SATIR ÖNEMLİ VE ÖNCELİKSİZ (ikisinde de):
   * Yukarıdaki `background-*` özellikleri `!important` ile yazılınca tarayıcı
   * bunları `background` KISALTMASINA sıkıştırıyor ve hiç yazmadığımız
   * `background-position`ı da `initial !important` olarak bildirime koyuyor.
   * `!important` bildirimleri sıralamada animasyonların ÜSTÜNDE olduğu için
   * `@keyframes` konumu süremiyordu: gradyan görünüyor ama DONUK kalıyordu —
   * `animationName` ve `getAnimations()` doğru, hata sessiz.
   * Canlıda ölçüldü (`getPropertyPriority('background-position')`):
   *   önce  → "important" / "initial", konum `0% 0%`ta çakılı
   *   sonra → ""          / "0% 50%",  konum `163% → 223% → 298% → 74%`
   * Öncelik boş verilince o özelliğin `!important` bayrağı kalkıyor.
   * ══════════════════════════════════════════════════════════════════════ */
  function akanAdBoya(ub, renkler) {
    if (!ub || !renkler || !renkler.length) return;
    msStilKur();                                   // @keyframes pk-ozel-ad
    /* Satır içi yazılıyor: `applyNameGradient` aynı özelliklere `!important`
       basıyor ve satır içi `!important` bir sınıf kuralını her zaman yener —
       stil sayfasına koyarsak rozet gradyanı bunu ezerdi. */
    ub.style.setProperty('background-image', 'linear-gradient(90deg,' + renkler.join(',') + ')', 'important');
    /* Gradyan metinden geniş olmalı, yoksa kaydıracak yer kalmıyor. */
    ub.style.setProperty('background-size', '300% 100%', 'important');
    /* Döşeme şart: dikişsiz döngü bir görüntü genişliği kaydırmaya dayanıyor. */
    ub.style.setProperty('background-repeat', 'repeat', 'important');
    ub.style.setProperty('-webkit-background-clip', 'text', 'important');
    ub.style.setProperty('background-clip', 'text', 'important');
    ub.style.setProperty('-webkit-text-fill-color', 'transparent', 'important');
    ub.style.setProperty('color', 'transparent', 'important');
    ub.style.setProperty('animation', 'pk-ozel-ad 4s linear infinite', 'important');
    ub.style.setProperty('background-position', '0% 50%');      // bkz. üstteki not
  }

  function akanSatirBoya(m, renk) {
    if (!m || !renk) return;
    msStilKur();                                 // @keyframes pk-cerceve-isik
    m.style.setProperty('box-shadow',
      'inset 0 0 0 1px ' + renk + '6b, inset 3px 0 0 ' + renk, 'important');
    m.style.setProperty('background-color', renk + '14', 'important');   // %8
    m.style.setProperty('background-image',
      'linear-gradient(100deg,transparent 18%,' + renk + '4d 50%,transparent 82%)', 'important');
    m.style.setProperty('background-size', '300% 100%', 'important');
    m.style.setProperty('background-repeat', 'no-repeat', 'important');
    m.style.setProperty('animation', 'pk-cerceve-isik 2.6s ease-in-out infinite alternate', 'important');
    m.style.setProperty('background-position', '0% 0%');        // bkz. üstteki not
  }

  function ozelAdUygula(ub, k) {
    if (!ub) return;
    /* 10.15 — önce SUNUCUDAN gelen akan renk (abonelik hakkı), yoksa koda
       gömülü eski harita (sunucu ulaşılamazken kurucu hesap kaybolmasın). */
    var renkler = (badgeMaps && badgeMaps.akanByName && badgeMaps.akanByName[k]) || PK_OZEL_AD[k];
    var simdiki = ub.getAttribute('data-pk-ozelad') || '';
    /* İŞARETE TEK BAŞINA GÜVENİLMEZ. Kick düğümü yeniden çizerken satır içi
       stili siliyor ama niteliğimiz kalıyor; yalnız işarete bakan bir kapı
       "zaten boyalı" deyip bir daha hiç boyamıyordu (canlıda beş isimden
       biri boyasız kalmıştı). Stil gerçekten duruyor mu diye de bakıyoruz —
       satır içi değeri okumak ucuz, yerleşim tetiklemiyor. */
    if (renkler && (simdiki !== k || !ub.style.getPropertyValue('animation'))) {
      ub.setAttribute('data-pk-ozelad', k);
      akanAdBoya(ub, renkler);
      return;
    }
    if (!renkler && simdiki) ozelAdSok(ub);
  }

  /* Ayrı duruyor çünkü kendini toplarken elimizde satır değil doğrudan isim
     düğümü oluyor (`[data-pk-ozelad]` ile bulunuyor). */
  function ozelAdSok(ub) {
    ub.removeAttribute('data-pk-ozelad');
    ['background', 'background-image', 'background-size', 'background-repeat',
     'background-position', '-webkit-background-clip', 'background-clip',
     '-webkit-text-fill-color', 'color', 'animation']
      .forEach(function (p) { ub.style.removeProperty(p); });
    /* Düğüm geri dönüştürülüp rozetli birine geçebilir; imzayı da siliyoruz ki
       `applyNameGradient` "zaten boyalı" sanıp ismi boyasız bırakmasın. */
    ub.removeAttribute('data-pk-grad');
  }

  /* ---------- Profil kartına gizle / öncelik butonları ---------- */
  function injectPopupKisi(kart, ad, zorla) {
    var eski = kart.querySelector(':scope > .pk-kisi');
    if (settings.kisiGizle === false && settings.oncelik === false) { if (eski) eski.remove(); return; }
    /* `zorla` OLMADAN erken çıkış: tur her saniye çağırıyor, kutu duruyorsa
       yeniden çizmeye gerek yok. TIKLAMADAN sonra ise ŞART — durum değişti,
       düğme yazısı da değişmeli. Bu bayrak eklenene kadar "Öncelikli yap"a
       basınca yazı olduğu gibi kalıyordu. */
    if (!zorla && eski && eski.getAttribute('data-pk-kisi') === ad) return;
    if (eski) eski.remove();
    var k = String(ad || '').toLowerCase(); if (!k) return;

    var kutu = el('div', 'pk-kisi flex gap-2');
    kutu.setAttribute('data-pk-kisi', ad);

    function dugme(metin, aktif, tikla) {
      var b = el('button', 'flex-1 min-w-0 h-8 rounded text-xs font-semibold transition-all ' +
        'flex items-center justify-center whitespace-nowrap ' +
        (aktif ? 'bg-primary-base text-black' : 'bg-secondary-base text-white hover:brightness-125'), metin);
      b.style.cssText = 'border:0;cursor:pointer';
      b.addEventListener('click', function (e) { e.preventDefault(); e.stopPropagation(); tikla(); });
      return b;
    }
    if (settings.kisiGizle !== false) {
      var gizliMi = !!gizliKisi[k];
      kutu.appendChild(dugme(gizliMi ? t('pkMesajlariGoster') : t('pkMesajlariniGizle'), gizliMi, function () {
        if (gizliKisi[k]) { kisiGoster(k); toast(t('pkTKisiGorunur', [ad])); }
        else { kisiGizle(k); toast(t('pkTKisiGizlendi', [ad])); }
        injectPopupKisi(kart, ad, true);   // durum değişti → yazılar yenilensin
      }));
    }
    if (settings.oncelik !== false) {
      var oncMi = !!oncelikli[k];
      kutu.appendChild(dugme(oncMi ? t('pkOncelikCikar') : t('pkOncelikliYap'), oncMi, function () {
        var yeni = oncelikDegis(k);
        toast(yeni ? t('pkTKisiOncelikli', [ad]) : t('pkTOncelikKalkti', [ad]));
        injectPopupKisi(kart, ad, true);   // durum değişti → yazılar yenilensin
      }));
    }
    var pm = kart.querySelector(':scope > .pk-not') || kart.querySelector(':scope > .pk-pm');
    if (pm) kart.insertBefore(kutu, pm); else kart.appendChild(kutu);
  }

  /* ---------- Emote önizleme ---------- */
  var EMOTE_ID = 'pk-emote-oniz';
  /* Önizlemenin kaynağı olan <img>. Kaydırmada kapatmak yerine bunu takip
     ediyoruz; sohbetin kendini dibe alması önizlemeyi kapatmasın. */
  var emoteKaynak = null;
  function emoteKapat() {
    var p = document.getElementById(EMOTE_ID); if (p) p.remove();
    emoteKaynak = null;
  }

  /* Önizlemeyi kaynağın güncel yerine göre konumlar. Kaynak koptuysa ya da
     ekrandan çıktıysa kapatır. */
  function emoteKonumla() {
    var p = document.getElementById(EMOTE_ID);
    if (!p) return;
    if (!emoteKaynak || !emoteKaynak.isConnected) { emoteKapat(); return; }
    var r = emoteKaynak.getBoundingClientRect();
    /* Ölçüsü kalmadıysa ya da görünür alandan tamamen çıktıysa asılı
       kalmasın. */
    if (!r.width || r.bottom < 0 || r.top > innerHeight) { emoteKapat(); return; }
    var pr = p.getBoundingClientRect();
    var x = r.left + r.width / 2 - pr.width / 2;
    var y = r.top - pr.height - 8;
    if (y < 8) y = r.bottom + 8;
    if (x < 8) x = 8;
    if (x + pr.width > innerWidth - 8) x = innerWidth - pr.width - 8;
    p.style.left = Math.round(x) + 'px';
    p.style.top = Math.round(y) + 'px';
  }

  function emoteHover(e) {
    if (!settings.consented || settings.emoteOniz === false) return;
    var t = e.target;
    if (!t || t.tagName !== 'IMG') { emoteKapat(); return; }
    var src = t.getAttribute('src') || '';
    /* Yalnız emote görselleri: Kick emoteleri /emotes/ yolundan gelir.
       Avatar, rozet ve küçük resimler bu desene uymaz. */
    if (!/emote/i.test(src)) { emoteKapat(); return; }
    var ad = (t.getAttribute('alt') || '').trim();
    var r = t.getBoundingClientRect();
    if (!r.width) { emoteKapat(); return; }
    emoteKapat();

    var p = el('div', 'bg-surface-base rounded shadow-lg flex flex-col items-center gap-1 ' +
      'will-change-[opacity,transform] animate-slideUpAndFade');
    p.id = EMOTE_ID;
    p.style.cssText = 'position:fixed;z-index:2147483090;padding:8px;pointer-events:none;' +
      'border:1px solid rgba(240,241,242,.16)';
    var im = el('img', '');
    im.src = src; im.referrerPolicy = 'no-referrer';
    var emBoyut = Math.max(16, Math.min(128, Number(settings.emoteBoyut) || 48));
    im.style.cssText = 'width:' + emBoyut + 'px;height:' + emBoyut + 'px;object-fit:contain;display:block';
    p.appendChild(im);
    if (ad) p.appendChild(el('div', 'text-xs font-semibold text-white', ad));
    pkKatmanaEkle(p);
    /* Kaynağı ÖNCE yaz: `emoteKonumla` onsuz kapatır. */
    emoteKaynak = t;
    emoteKonumla();
  }


  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — ROL BAZLI MESAJ VURGULAMA
   * Rol, DOM'dan TAHMİN EDİLMEZ: Kick'in kendi Pusher olayındaki
   * sender.identity.badges listesinden okunur (tür alanı sabit ve dilden
   * bağımsız). Bu yüzden ancak sayfa açıldıktan SONRA yazan kişilerin rolü
   * bilinir — zaten ekranda kayan mesajlar onlar.
   *
   * Vurgulama iki parçalı: solda rol rengiyle çizgi + hafif zemin. Kick'in
   * kendi mesaj düzenine dokunulmaz, yalnız iki stil özelliği eklenir.
   * ══════════════════════════════════════════════════════════════════════ */
  var kisiRol = Object.create(null);        // nick(küçük) -> rol
  var kisiRolSira = [];
  var ROL_VARSAYILAN = {
    broadcaster: '#fa2d2d', staff: '#3aa1ff', moderator: '#00c7ff', verified: '#1da1f2',
    vip: '#e6a800', founder: '#ff7ac6', og: '#9b6bff', sub_gifter: '#ff8a3d',
    subscriber: '#53fc18', bot: '#8b969d'
  };
  /* Rol sırası = öncelik sırası. Bir kişide birden çok rozet olabilir; en üstteki kazanır. */
  /* ── KICK ROL ROZETLERİ ──────────────────────────────────────────────
   * Kick'in kendi rozet çizimleri. Figma'dan kalan işlevsiz tam boy
   * clipPath'ler atıldı, gradyan kimlikleri `pkrg-<rol>-<n>` şemasına
   * çevrildi (sayfada birkaç rozet aynı anda çizilebiliyor, kimlikler
   * çakışmasın), ölçü CSS'e bırakıldı.
   * Anahtarlar ROL_SIRASI ile birebir aynı.
   * ──────────────────────────────────────────────────────────────────── */
  var KICK_ROZET = {
    broadcaster: '<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M15.6773 22.1533C17.3698 22.1533 18.8182 21.5507 20.0233 20.3461C21.2282 19.1415 21.8307 17.6924 21.8307 16V6.15401C21.8307 4.46162 21.2286 3.01305 20.0233 1.80784C18.8182 0.602907 17.3698 0 15.6773 0C13.9849 0 12.5363 0.602907 11.3311 1.80784C10.1259 3.01285 9.52344 4.46162 9.52344 6.15401V16C9.52344 17.6923 10.1262 19.1415 11.3311 20.3461C12.5361 21.5507 13.9849 22.1533 15.6773 22.1533Z" fill="url(#pkrg-broadcaster-0)"></path><path d="M15.6773 22.1533C17.3698 22.1533 18.8182 21.5507 20.0233 20.3461C21.2282 19.1415 21.8307 17.6924 21.8307 16V6.15401C21.8307 4.46162 21.2286 3.01305 20.0233 1.80784C18.8182 0.602907 17.3698 0 15.6773 0C13.9849 0 12.5363 0.602907 11.3311 1.80784C10.1259 3.01285 9.52344 4.46162 9.52344 6.15401V16C9.52344 17.6923 10.1262 19.1415 11.3311 20.3461C12.5361 21.5507 13.9849 22.1533 15.6773 22.1533Z" fill="white" fill-opacity="0.3"></path><path d="M26.3888 12.6731C26.1459 12.4295 25.8568 12.3076 25.5234 12.3076C25.1904 12.3076 24.902 12.4295 24.6581 12.6731C24.4147 12.9167 24.293 13.2051 24.293 13.5383V16C24.293 18.3718 23.4498 20.4006 21.7639 22.0864C20.0785 23.7723 18.0495 24.6153 15.6775 24.6153C13.3057 24.6153 11.2769 23.7723 9.59089 22.0864C7.90509 20.401 7.06226 18.3719 7.06226 16V13.5383C7.06226 13.2051 6.94041 12.9167 6.69692 12.6731C6.45329 12.4295 6.16514 12.3076 5.83159 12.3076C5.49804 12.3076 5.20956 12.4295 4.96606 12.6731C4.72237 12.9167 4.60059 13.2051 4.60059 13.5383V16C4.60059 18.8333 5.54627 21.2981 7.4371 23.3941C9.32799 25.4901 11.6645 26.6919 14.4467 26.9994V29.5381H9.52373C9.19038 29.5381 8.90196 29.6601 8.6584 29.9037C8.41477 30.1472 8.29293 30.4357 8.29293 30.7691C8.29293 31.1019 8.41477 31.391 8.6584 31.6344C8.90196 31.8778 9.19038 32 9.52373 32H21.831C22.1643 32 22.4531 31.8779 22.6963 31.6344C22.9402 31.391 23.0622 31.1019 23.0622 30.7691C23.0622 30.4358 22.9402 30.1472 22.6963 29.9037C22.4532 29.6601 22.1644 29.5381 21.831 29.5381H16.9086V26.9994C19.6904 26.6919 22.0267 25.4901 23.9178 23.3941C25.8089 21.2981 26.7548 18.8333 26.7548 16V13.5383C26.7548 13.2051 26.6327 12.9169 26.3888 12.6731Z" fill="url(#pkrg-broadcaster-1)"></path><defs><linearGradient id="pkrg-broadcaster-0" x1="5.22969e-08" y1="-5.22969e-08" x2="32" y2="32" gradientUnits="userSpaceOnUse"><stop stop-color="#FF1CD2"></stop><stop offset="1" stop-color="#B20DFF"></stop></linearGradient><linearGradient id="pkrg-broadcaster-1" x1="-5.88081e-07" y1="-8.98822e-07" x2="4.72839" y2="35.6202" gradientUnits="userSpaceOnUse"><stop stop-color="#FF1CD2"></stop><stop offset="1" stop-color="#B20DFF"></stop></linearGradient></defs></svg>',
    staff: '<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M30 0C31.1046 0 32 0.895431 32 2V30C32 31.1046 31.1046 32 30 32H2C0.895431 32 0 31.1046 0 30V2C0 0.895431 0.895431 4.10637e-08 2 0H30ZM6 5V27H13.5029V22.1162H16V24.5576H18.4971V27H26V19.6631H23.5029V17.2207H20.9941V14.7793H23.5029V12.3369H26V5H18.4971V7.44238H16V9.88379H13.5029V5H6Z" fill="url(#pkrg-staff-0)"></path><defs><linearGradient id="pkrg-staff-0" x1="8.99888" y1="34.208" x2="20.0805" y2="-2.27173" gradientUnits="userSpaceOnUse"><stop stop-color="#1EFF00"></stop><stop offset="0.99" stop-color="#00FF8C"></stop></linearGradient></defs></svg>',
    moderator: '<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M30 0C31.1046 0 32 0.895431 32 2V30C32 31.1046 31.1046 32 30 32H2C0.895431 32 0 31.1046 0 30V2C0 0.895431 0.895431 0 2 0H30ZM16.2197 2.99316C15.8292 2.60266 15.1962 2.60265 14.8057 2.99316L8.36328 9.43555C7.97294 9.82608 7.97284 10.4591 8.36328 10.8496L10.0918 12.5781C10.4823 12.9686 11.1153 12.9685 11.5059 12.5781L11.585 12.499L13.9414 14.8564L3.57129 25.2275C2.70357 26.0954 2.7035 27.5023 3.57129 28.3701C4.43911 29.2376 5.84612 29.2377 6.71387 28.3701L17.084 17.999L19.4414 20.3564L19.3633 20.4346C18.9728 20.8251 18.9728 21.4581 19.3633 21.8486L21.0918 23.5771C21.4823 23.9676 22.1154 23.9676 22.5059 23.5771L28.9482 17.1348C29.3386 16.7443 29.3386 16.1112 28.9482 15.7207L27.2197 13.9922C26.8293 13.6017 26.1962 13.6018 25.8057 13.9922L25.7266 14.0703L23.3701 11.7139C24.2377 10.8461 24.2376 9.4391 23.3701 8.57129C22.5023 7.7035 21.0954 7.70357 20.2275 8.57129L17.8701 6.21387L17.9482 6.13574C18.3388 5.74522 18.3388 5.11221 17.9482 4.72168L16.2197 2.99316Z" fill="url(#pkrg-moderator-0)"></path><path d="M30 0C31.1046 0 32 0.895431 32 2V30C32 31.1046 31.1046 32 30 32H2C0.895431 32 0 31.1046 0 30V2C0 0.895431 0.895431 0 2 0H30ZM16.2197 2.99316C15.8292 2.60266 15.1962 2.60265 14.8057 2.99316L8.36328 9.43555C7.97294 9.82608 7.97284 10.4591 8.36328 10.8496L10.0918 12.5781C10.4823 12.9686 11.1153 12.9685 11.5059 12.5781L11.585 12.499L13.9414 14.8564L3.57129 25.2275C2.70357 26.0954 2.7035 27.5023 3.57129 28.3701C4.43911 29.2376 5.84612 29.2377 6.71387 28.3701L17.084 17.999L19.4414 20.3564L19.3633 20.4346C18.9728 20.8251 18.9728 21.4581 19.3633 21.8486L21.0918 23.5771C21.4823 23.9676 22.1154 23.9676 22.5059 23.5771L28.9482 17.1348C29.3386 16.7443 29.3386 16.1112 28.9482 15.7207L27.2197 13.9922C26.8293 13.6017 26.1962 13.6018 25.8057 13.9922L25.7266 14.0703L23.3701 11.7139C24.2377 10.8461 24.2376 9.4391 23.3701 8.57129C22.5023 7.7035 21.0954 7.70357 20.2275 8.57129L17.8701 6.21387L17.9482 6.13574C18.3388 5.74522 18.3388 5.11221 17.9482 4.72168L16.2197 2.99316Z" fill="url(#pkrg-moderator-1)"></path><path d="M30 0C31.1046 0 32 0.895431 32 2V30C32 31.1046 31.1046 32 30 32H2C0.895431 32 0 31.1046 0 30V2C0 0.895431 0.895431 0 2 0H30ZM16.2197 2.99316C15.8292 2.60266 15.1962 2.60265 14.8057 2.99316L8.36328 9.43555C7.97294 9.82608 7.97284 10.4591 8.36328 10.8496L10.0918 12.5781C10.4823 12.9686 11.1153 12.9685 11.5059 12.5781L11.585 12.499L13.9414 14.8564L3.57129 25.2275C2.70357 26.0954 2.7035 27.5023 3.57129 28.3701C4.43911 29.2376 5.84612 29.2377 6.71387 28.3701L17.084 17.999L19.4414 20.3564L19.3633 20.4346C18.9728 20.8251 18.9728 21.4581 19.3633 21.8486L21.0918 23.5771C21.4823 23.9676 22.1154 23.9676 22.5059 23.5771L28.9482 17.1348C29.3386 16.7443 29.3386 16.1112 28.9482 15.7207L27.2197 13.9922C26.8293 13.6017 26.1962 13.6018 25.8057 13.9922L25.7266 14.0703L23.3701 11.7139C24.2377 10.8461 24.2376 9.4391 23.3701 8.57129C22.5023 7.7035 21.0954 7.70357 20.2275 8.57129L17.8701 6.21387L17.9482 6.13574C18.3388 5.74522 18.3388 5.11221 17.9482 4.72168L16.2197 2.99316Z" fill="url(#pkrg-moderator-2)"></path><defs><linearGradient id="pkrg-moderator-0" x1="18.8102" y1="-12.7222" x2="2.88536" y2="39.1063" gradientUnits="userSpaceOnUse"><stop stop-color="#FF6A4A"></stop><stop offset="1" stop-color="#C70C00"></stop></linearGradient><linearGradient id="pkrg-moderator-1" x1="15.7467" y1="-4.75575" x2="16.321" y2="39.0672" gradientUnits="userSpaceOnUse"><stop stop-color="#FFC900"></stop><stop offset="0.99" stop-color="#FF9500"></stop></linearGradient><linearGradient id="pkrg-moderator-2" x1="-14.9543" y1="46.9544" x2="32.0001" y2="-0.000509222" gradientUnits="userSpaceOnUse"><stop stop-color="#0095FF"></stop><stop offset="0.99" stop-color="#00C7FF"></stop></linearGradient></defs></svg>',
    verified: '<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M30.8598 19.2368C30.1977 18.2069 29.5356 17.2138 28.8736 16.1839C28.7264 15.9632 28.7264 15.8161 28.8736 15.5954C29.5356 14.6023 30.1609 13.6092 30.823 12.6161C31.5954 11.4391 31.1908 10.2989 29.8667 9.82069C28.7632 9.41609 27.6598 8.97471 26.5563 8.57012C26.3356 8.49656 26.2253 8.34943 26.2253 8.09196C26.1885 6.87816 26.1149 5.66437 26.0414 4.48736C25.9678 3.2 24.9747 2.46437 23.7241 2.7954C22.5471 3.08966 21.3701 3.42069 20.2299 3.75173C19.9724 3.82529 19.8253 3.75173 19.6414 3.56782C18.9057 2.61149 18.1333 1.69195 17.3977 0.772414C16.5885 -0.257472 15.3379 -0.257472 14.492 0.772414C13.7563 1.69195 12.9839 2.61149 12.2851 3.53103C12.1012 3.7885 11.9172 3.82529 11.623 3.75173C10.4828 3.42069 9.34253 3.12644 8.53334 2.90575C6.95173 2.53793 5.99541 3.16322 5.92184 4.48736C5.84828 5.70115 5.77472 6.91495 5.73794 8.16552C5.73794 8.42299 5.62759 8.53333 5.4069 8.64368C4.26667 9.08506 3.12644 9.52644 1.98621 9.96782C0.809203 10.446 0.441387 11.5862 1.14023 12.6529C1.8023 13.6828 2.46437 14.6759 3.12644 15.7057C3.27356 15.9264 3.27356 16.0736 3.12644 16.331C2.42759 17.3609 1.76552 18.3908 1.10345 19.4575C0.478165 20.4506 0.882759 21.6276 1.98621 22.069C3.12644 22.5104 4.30345 22.9517 5.44368 23.3931C5.70115 23.4667 5.77471 23.6138 5.77471 23.8713C5.81149 25.0483 5.95862 26.1885 5.95862 27.3655C5.95862 28.5425 6.9885 29.6092 8.42298 29.1678C9.56321 28.8 10.7034 28.5425 11.8437 28.2115C12.0644 28.1379 12.2115 28.1747 12.3586 28.3954C13.131 29.3517 13.8667 30.2713 14.6391 31.2276C15.485 32.2575 16.6988 32.2575 17.508 31.2276C18.2805 30.2713 19.0161 29.3517 19.7885 28.3954C19.9356 28.2115 20.046 28.1379 20.3034 28.2115C21.4804 28.5425 22.6575 28.8368 23.8345 29.1678C25.0483 29.4988 26.0781 28.7632 26.1149 27.5126C26.1885 26.2989 26.2621 25.0851 26.2988 23.8345C26.2988 23.5402 26.446 23.4299 26.6667 23.3563C27.7701 22.9517 28.9103 22.5104 30.0138 22.069C31.1908 21.4805 31.5586 20.3034 30.8598 19.2368ZM22.069 13.2046L14.7127 20.5609C14.5287 20.7448 14.2713 20.892 14.0138 20.9287C13.9402 20.9287 13.8299 20.9655 13.7563 20.9655C13.4253 20.9655 13.0575 20.8184 12.8 20.5609L9.78392 17.5448C9.26898 17.0299 9.26898 16.1839 9.78392 15.669C10.2989 15.154 11.1448 15.154 11.6598 15.669L13.7196 17.7287L20.1196 11.3287C20.6345 10.8138 21.4805 10.8138 21.9954 11.3287C22.5839 11.8437 22.5839 12.6897 22.069 13.2046Z" fill="url(#pkrg-verified-0)"></path><defs><linearGradient id="pkrg-verified-0" x1="8.14138" y1="32.3591" x2="24.4968" y2="0.904884" gradientUnits="userSpaceOnUse"><stop stop-color="#1EFF00"></stop><stop offset="0.99" stop-color="#00FF8C"></stop></linearGradient></defs></svg>',
    vip: '<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M30 0C31.1046 0 32 0.895431 32 2V30C32 31.1046 31.1046 32 30 32H2C0.895431 32 0 31.1046 0 30V2C0 0.895431 0.895431 4.10637e-08 2 0H30ZM15.9648 5C15.7748 5.00005 15.588 5.05204 15.4238 5.15039C15.2596 5.24878 15.124 5.39057 15.0303 5.56055L9.82812 15.0176L3.55078 11.8906C3.36913 11.7985 3.16534 11.7607 2.96387 11.7822C2.76241 11.8038 2.57048 11.8842 2.41113 12.0127C2.25235 12.1408 2.13185 12.3126 2.06348 12.5078C1.99511 12.7031 1.98143 12.9144 2.02441 13.1172L4.58301 25.127C4.63544 25.3782 4.77165 25.6034 4.96777 25.7627C5.16376 25.9217 5.40762 26.0056 5.65723 26H26.251C26.5009 26.0057 26.7453 25.9219 26.9414 25.7627C27.1376 25.6034 27.2737 25.3782 27.3262 25.127L29.9697 13.1172C30.0187 12.9103 30.0086 12.6932 29.9404 12.4922C29.8722 12.2912 29.7485 12.1151 29.585 11.9844C29.4215 11.8537 29.2249 11.7743 29.0186 11.7559C28.8122 11.7374 28.6049 11.7802 28.4219 11.8799L22.1025 15.0283L16.9004 5.56055C16.8066 5.39054 16.6701 5.24878 16.5059 5.15039C16.3416 5.05207 16.1549 5 15.9648 5Z" fill="url(#pkrg-vip-0)"></path><path d="M30 0C31.1046 0 32 0.895431 32 2V30C32 31.1046 31.1046 32 30 32H2C0.895431 32 0 31.1046 0 30V2C0 0.895431 0.895431 4.10637e-08 2 0H30ZM15.9648 5C15.7748 5.00005 15.588 5.05204 15.4238 5.15039C15.2596 5.24878 15.124 5.39057 15.0303 5.56055L9.82812 15.0176L3.55078 11.8906C3.36913 11.7985 3.16534 11.7607 2.96387 11.7822C2.76241 11.8038 2.57048 11.8842 2.41113 12.0127C2.25235 12.1408 2.13185 12.3126 2.06348 12.5078C1.99511 12.7031 1.98143 12.9144 2.02441 13.1172L4.58301 25.127C4.63544 25.3782 4.77165 25.6034 4.96777 25.7627C5.16376 25.9217 5.40762 26.0056 5.65723 26H26.251C26.5009 26.0057 26.7453 25.9219 26.9414 25.7627C27.1376 25.6034 27.2737 25.3782 27.3262 25.127L29.9697 13.1172C30.0187 12.9103 30.0086 12.6932 29.9404 12.4922C29.8722 12.2912 29.7485 12.1151 29.585 11.9844C29.4215 11.8537 29.2249 11.7743 29.0186 11.7559C28.8122 11.7374 28.6049 11.7802 28.4219 11.8799L22.1025 15.0283L16.9004 5.56055C16.8066 5.39054 16.6701 5.24878 16.5059 5.15039C16.3416 5.05207 16.1549 5 15.9648 5Z" fill="url(#pkrg-vip-1)"></path><defs><linearGradient id="pkrg-vip-0" x1="18.8102" y1="-12.7222" x2="2.88536" y2="39.1063" gradientUnits="userSpaceOnUse"><stop stop-color="#FF6A4A"></stop><stop offset="1" stop-color="#C70C00"></stop></linearGradient><linearGradient id="pkrg-vip-1" x1="15.7467" y1="-4.75575" x2="16.321" y2="39.0672" gradientUnits="userSpaceOnUse"><stop stop-color="#FFC900"></stop><stop offset="0.99" stop-color="#FF9500"></stop></linearGradient></defs></svg>',
    founder: '<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M16 32C24.8366 32 32 24.8366 32 16C32 7.16344 24.8366 0 16 0C7.16344 0 0 7.16344 0 16C0 24.8366 7.16344 32 16 32Z" fill="url(#pkrg-founder-0)"></path><path d="M16 32C24.8366 32 32 24.8366 32 16C32 7.16344 24.8366 0 16 0C7.16344 0 0 7.16344 0 16C0 24.8366 7.16344 32 16 32Z" fill="url(#pkrg-founder-1)"></path><path d="M16 29.0375C23.2004 29.0375 29.0375 23.2004 29.0375 16C29.0375 8.79958 23.2004 2.96249 16 2.96249C8.79959 2.96249 2.9625 8.79958 2.9625 16C2.9625 23.2004 8.79959 29.0375 16 29.0375Z" fill="#FEB635"></path><path d="M16 29.0375C23.2004 29.0375 29.0375 23.2004 29.0375 16C29.0375 8.79958 23.2004 2.96249 16 2.96249C8.79959 2.96249 2.9625 8.79958 2.9625 16C2.9625 23.2004 8.79959 29.0375 16 29.0375Z" fill="url(#pkrg-founder-2)"></path><path d="M29.0375 16C29.0375 23.1875 23.1875 29.0375 16 29.0375C13.6563 29.0375 11.4625 28.4187 9.5625 27.3312C11.3125 28.2062 13.2875 28.7 15.375 28.7C22.5625 28.7 28.4125 22.85 28.4125 15.6625C28.4125 10.8188 25.75 6.58125 21.8125 4.3375C26.0938 6.475 29.0375 10.8938 29.0375 16ZM16.8875 3.575C19.4563 3.575 21.85 4.325 23.8625 5.60625C21.675 3.95625 18.95 2.96875 16 2.96875C8.8125 2.96875 2.9625 8.8125 2.9625 16.0063C2.9625 20.6437 5.4 24.7313 9.0625 27.0312C5.9 24.65 3.85 20.8687 3.85 16.6125C3.85 9.425 9.7 3.575 16.8875 3.575Z" fill="black" fill-opacity="0.05"></path><path d="M18.5966 9.45456V24H14.6477V13.0909H14.5625L11.3807 14.9943V11.6421L14.9602 9.45456H18.5966Z" fill="black" fill-opacity="0.8"></path><path d="M18.5966 9.45456V24H14.6477V13.0909H14.5625L11.3807 14.9943V11.6421L14.9602 9.45456H18.5966Z" fill="url(#pkrg-founder-3)" fill-opacity="0.5"></path><path d="M18.5966 9.45456V24H14.6477V13.0909H14.5625L11.3807 14.9943V11.6421L14.9602 9.45456H18.5966Z" stroke="black" stroke-opacity="0.1" stroke-width="0.350269"></path><defs><linearGradient id="pkrg-founder-0" x1="15.7467" y1="-4.46667" x2="16.2533" y2="36.6933" gradientUnits="userSpaceOnUse"><stop stop-color="#FFC900"></stop><stop offset="0.99" stop-color="#FF9500"></stop></linearGradient><linearGradient id="pkrg-founder-1" x1="16" y1="0" x2="16" y2="32" gradientUnits="userSpaceOnUse"><stop stop-color="white" stop-opacity="0.3"></stop><stop offset="1" stop-color="white" stop-opacity="0.15"></stop></linearGradient><linearGradient id="pkrg-founder-2" x1="15.7936" y1="-0.677142" x2="16.2064" y2="32.8618" gradientUnits="userSpaceOnUse"><stop stop-color="#FFC900"></stop><stop offset="0.99" stop-color="#FF9500"></stop></linearGradient><linearGradient id="pkrg-founder-3" x1="18.5966" y1="16.7273" x2="11.3807" y2="16.7273" gradientUnits="userSpaceOnUse"><stop stop-color="white" stop-opacity="0.1"></stop><stop offset="0.3" stop-color="white" stop-opacity="0.2"></stop><stop offset="0.65" stop-color="white" stop-opacity="0.05"></stop><stop offset="1" stop-color="white" stop-opacity="0.2"></stop></linearGradient></defs></svg>',
    og: '<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M22.8226 17.2693V28.0037C22.8226 28.2177 22.8929 28.383 23.0336 28.4996C23.1742 28.5969 23.3969 28.6455 23.7017 28.6455H24.5104V32H21.838C19.9627 32 18.6265 31.6694 17.8294 31.0082C17.0559 30.347 16.6691 29.472 16.6691 28.383V16.8901C16.6691 15.8011 17.0559 14.926 17.8294 14.2648C18.6265 13.6036 19.9627 13.273 21.838 13.273H24.6511V16.6276H23.7017C23.3969 16.6276 23.1742 16.6859 23.0336 16.8026C22.8929 16.8998 22.8226 17.0554 22.8226 17.2693ZM32.0002 21.6447V24.8826H24.0885V21.6447H32.0002ZM25.8466 19.6904V17.2693C25.8466 17.0554 25.7763 16.8998 25.6357 16.8026C25.495 16.6859 25.2723 16.6276 24.9676 16.6276H24.0182V13.273H26.8312C28.7066 13.273 30.031 13.6036 30.8046 14.2648C31.6017 14.926 32.0002 15.8011 32.0002 16.8901V19.6904H25.8466ZM25.8466 28.0037V23.8908H32.0002V28.383C32.0002 29.472 31.6017 30.347 30.8046 31.0082C30.031 31.6694 28.7066 32 26.8312 32H24.1588V28.6455H24.9676C25.2723 28.6455 25.495 28.5969 25.6357 28.4996C25.7763 28.383 25.8466 28.2177 25.8466 28.0037Z" fill="white"></path><path d="M22.8226 17.2693V28.0037C22.8226 28.2177 22.8929 28.383 23.0336 28.4996C23.1742 28.5969 23.3969 28.6455 23.7017 28.6455H24.5104V32H21.838C19.9627 32 18.6265 31.6694 17.8294 31.0082C17.0559 30.347 16.6691 29.472 16.6691 28.383V16.8901C16.6691 15.8011 17.0559 14.926 17.8294 14.2648C18.6265 13.6036 19.9627 13.273 21.838 13.273H24.6511V16.6276H23.7017C23.3969 16.6276 23.1742 16.6859 23.0336 16.8026C22.8929 16.8998 22.8226 17.0554 22.8226 17.2693ZM32.0002 21.6447V24.8826H24.0885V21.6447H32.0002ZM25.8466 19.6904V17.2693C25.8466 17.0554 25.7763 16.8998 25.6357 16.8026C25.495 16.6859 25.2723 16.6276 24.9676 16.6276H24.0182V13.273H26.8312C28.7066 13.273 30.031 13.6036 30.8046 14.2648C31.6017 14.926 32.0002 15.8011 32.0002 16.8901V19.6904H25.8466ZM25.8466 28.0037V23.8908H32.0002V28.383C32.0002 29.472 31.6017 30.347 30.8046 31.0082C30.031 31.6694 28.7066 32 26.8312 32H24.1588V28.6455H24.9676C25.2723 28.6455 25.495 28.5969 25.6357 28.4996C25.7763 28.383 25.8466 28.2177 25.8466 28.0037Z" fill="url(#pkrg-og-0)"></path><path d="M22.8228 3.99625V14.7307C22.8228 14.9446 22.8931 15.1099 23.0338 15.2266C23.1744 15.3238 23.3971 15.3724 23.7019 15.3724H24.5106V18.727H21.8382C19.9629 18.727 18.6267 18.3964 17.8296 17.7352C17.056 17.074 16.6693 16.1989 16.6693 15.1099V3.61704C16.6693 2.52804 17.056 1.65295 17.8296 0.99177C18.6267 0.33059 19.9629 0 21.8382 0H24.6513V3.35452H23.7019C23.3971 3.35452 23.1744 3.41286 23.0338 3.52953C22.8931 3.62677 22.8228 3.78234 22.8228 3.99625ZM32.0004 8.37171V11.6095H24.0887V8.37171H32.0004ZM25.8468 6.41734V3.99625C25.8468 3.78234 25.7765 3.62677 25.6358 3.52953C25.4952 3.41286 25.2725 3.35452 24.9677 3.35452H24.0183V0H26.8314C28.7067 0 30.0312 0.33059 30.8048 0.99177C31.6018 1.65295 32.0004 2.52804 32.0004 3.61704V6.41734H25.8468ZM25.8468 14.7307V10.6178H32.0004V15.1099C32.0004 16.1989 31.6018 17.074 30.8048 17.7352C30.0312 18.3964 28.7067 18.727 26.8314 18.727H24.159V15.3724H24.9677C25.2725 15.3724 25.4952 15.3238 25.6358 15.2266C25.7765 15.1099 25.8468 14.9446 25.8468 14.7307Z" fill="#00FFF2"></path><path d="M9.38855 7.81748V4.28795C9.38855 4.07404 9.31822 3.91846 9.17757 3.82123C9.03691 3.70455 8.81421 3.64621 8.50947 3.64621H7.34909V0H10.3731C12.2485 0 13.573 0.33059 14.3465 0.99177C15.1436 1.65295 15.5421 2.52804 15.5421 3.61704V7.81748H9.38855ZM9.38855 14.439V7.43828H15.5421V15.1099C15.5421 16.1989 15.1436 17.074 14.3465 17.7352C13.573 18.3964 12.2485 18.727 10.3731 18.727H7.34909V15.0807H8.50947C8.81421 15.0807 9.03691 15.0321 9.17757 14.9349C9.31822 14.8182 9.38855 14.6529 9.38855 14.439ZM6.15354 4.28795V7.81748H0V3.61704C0 2.52804 0.386794 1.65295 1.16038 0.99177C1.95741 0.33059 3.29361 0 5.16897 0H8.193V3.64621H7.03262C6.72787 3.64621 6.50517 3.70455 6.36452 3.82123C6.22387 3.91846 6.15354 4.07404 6.15354 4.28795ZM6.15354 7.43828V14.439C6.15354 14.6529 6.22387 14.8182 6.36452 14.9349C6.50517 15.0321 6.72787 15.0807 7.03262 15.0807H8.193V18.727H5.16897C3.29361 18.727 1.95741 18.3964 1.16038 17.7352C0.386794 17.074 0 16.1989 0 15.1099V7.43828H6.15354Z" fill="white"></path><path d="M9.38855 7.81748V4.28795C9.38855 4.07404 9.31822 3.91846 9.17757 3.82123C9.03691 3.70455 8.81421 3.64621 8.50947 3.64621H7.34909V0H10.3731C12.2485 0 13.573 0.33059 14.3465 0.99177C15.1436 1.65295 15.5421 2.52804 15.5421 3.61704V7.81748H9.38855ZM9.38855 14.439V7.43828H15.5421V15.1099C15.5421 16.1989 15.1436 17.074 14.3465 17.7352C13.573 18.3964 12.2485 18.727 10.3731 18.727H7.34909V15.0807H8.50947C8.81421 15.0807 9.03691 15.0321 9.17757 14.9349C9.31822 14.8182 9.38855 14.6529 9.38855 14.439ZM6.15354 4.28795V7.81748H0V3.61704C0 2.52804 0.386794 1.65295 1.16038 0.99177C1.95741 0.33059 3.29361 0 5.16897 0H8.193V3.64621H7.03262C6.72787 3.64621 6.50517 3.70455 6.36452 3.82123C6.22387 3.91846 6.15354 4.07404 6.15354 4.28795ZM6.15354 7.43828V14.439C6.15354 14.6529 6.22387 14.8182 6.36452 14.9349C6.50517 15.0321 6.72787 15.0807 7.03262 15.0807H8.193V18.727H5.16897C3.29361 18.727 1.95741 18.3964 1.16038 17.7352C0.386794 17.074 0 16.1989 0 15.1099V7.43828H6.15354Z" fill="url(#pkrg-og-1)"></path><path d="M9.38839 21.0905V17.561C9.38839 17.3471 9.31807 17.1915 9.17741 17.0943C9.03676 16.9776 8.81406 16.9193 8.50932 16.9193H7.34893V13.273H10.373C12.2483 13.273 13.5728 13.6036 14.3464 14.2648C15.1434 14.926 15.5419 15.8011 15.5419 16.8901V21.0905H9.38839ZM9.38839 27.712V20.7113H15.5419V28.383C15.5419 29.472 15.1434 30.347 14.3464 31.0082C13.5728 31.6694 12.2483 32 10.373 32H7.34893V28.3538H8.50932C8.81406 28.3538 9.03676 28.3052 9.17741 28.2079C9.31807 28.0913 9.38839 27.926 9.38839 27.712ZM6.15339 17.561V21.0905H-0.000152588V16.8901C-0.000152588 15.8011 0.386641 14.926 1.16023 14.2648C1.95726 13.6036 3.29346 13.273 5.16882 13.273H8.19285V16.9193H7.03247C6.72772 16.9193 6.50502 16.9776 6.36437 17.0943C6.22371 17.1915 6.15339 17.3471 6.15339 17.561ZM6.15339 20.7113V27.712C6.15339 27.926 6.22371 28.0913 6.36437 28.2079C6.50502 28.3052 6.72772 28.3538 7.03247 28.3538H8.19285V32H5.16882C3.29346 32 1.95726 31.6694 1.16023 31.0082C0.386641 30.347 -0.000152588 29.472 -0.000152588 28.383V20.7113H6.15339Z" fill="#00FFF2"></path><defs><linearGradient id="pkrg-og-0" x1="23.9622" y1="0.695162" x2="24.4274" y2="31.9986" gradientUnits="userSpaceOnUse"><stop stop-color="#00FFF2"></stop><stop offset="1" stop-color="#006399"></stop></linearGradient><linearGradient id="pkrg-og-1" x1="7.77104" y1="0" x2="7.91062" y2="32.567" gradientUnits="userSpaceOnUse"><stop stop-color="#00FFF2"></stop><stop offset="1" stop-color="#006399"></stop></linearGradient></defs></svg>',
    sub_gifter: '<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg"><g clip-path="url(#pkrg-subgifter-0)"><g clip-path="url(#pkrg-subgifter-1)"><path d="M22.34 9.5L26 4H18L16 7L14 4H6L9.66 9.5H4V15.1H28V9.5H22.34Z" fill="#53FC18"></path><path d="M26.0799 19.0996H5.8999V28.4996H26.0799V19.0996Z" fill="#53FC18"></path><path d="M26.0799 15.0996H5.8999V19.0996H26.0799V15.0996Z" fill="#32970E"></path></g></g><defs><clipPath id="pkrg-subgifter-0"><rect width="24" height="24.5" fill="white" transform="translate(4 4)"></rect></clipPath><clipPath id="pkrg-subgifter-1"><rect width="24" height="24.5" fill="white" transform="translate(4 4)"></rect></clipPath></defs></svg>',
    subscriber: '<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M17.0284 2.91378L16.2357 0.667951C16.1573 0.445558 15.8427 0.445558 15.7643 0.667951L14.9716 2.91378C12.9003 8.78263 8.78263 12.9003 2.91378 14.9716L0.667951 15.7643C0.445558 15.8427 0.445558 16.1573 0.667951 16.2357L2.91378 17.0284C8.78263 19.0998 12.9003 23.2174 14.9716 29.0862L15.7643 31.3321C15.8427 31.5544 16.1573 31.5544 16.2357 31.3321L17.0284 29.0862C19.0998 23.2174 23.2174 19.0998 29.0862 17.0284L31.3321 16.2357C31.5544 16.1573 31.5544 15.8427 31.3321 15.7643L29.0862 14.9716C23.2174 12.9003 19.0998 8.78263 17.0284 2.91378Z" fill="black"></path><path d="M17.0284 2.91378L16.2357 0.667951C16.1573 0.445558 15.8427 0.445558 15.7643 0.667951L14.9716 2.91378C12.9003 8.78263 8.78263 12.9003 2.91378 14.9716L0.667951 15.7643C0.445558 15.8427 0.445558 16.1573 0.667951 16.2357L2.91378 17.0284C8.78263 19.0998 12.9003 23.2174 14.9716 29.0862L15.7643 31.3321C15.8427 31.5544 16.1573 31.5544 16.2357 31.3321L17.0284 29.0862C19.0998 23.2174 23.2174 19.0998 29.0862 17.0284L31.3321 16.2357C31.5544 16.1573 31.5544 15.8427 31.3321 15.7643L29.0862 14.9716C23.2174 12.9003 19.0998 8.78263 17.0284 2.91378Z" fill="url(#pkrg-subscriber-0)"></path><defs><radialGradient id="pkrg-subscriber-0" cx="0" cy="0" r="1" gradientUnits="userSpaceOnUse" gradientTransform="translate(16 16) rotate(90) scale(16)"><stop stop-color="#E1FF00"></stop><stop offset="1" stop-color="#2AA300"></stop></radialGradient></defs></svg>',
    bot: '<svg viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M17.56 0H14.4533C13.717 0 13.12 0.597452 13.12 1.33445V4.4437C13.12 5.1807 13.717 5.77815 14.4533 5.77815H17.56C18.2964 5.77815 18.8933 5.1807 18.8933 4.4437V1.33445C18.8933 0.597452 18.2964 0 17.56 0Z" fill="url(#pkrg-bot-0)"></path><path d="M17.3333 5.77815H14.6667V8.44704H17.3333V5.77815Z" fill="url(#pkrg-bot-1)"></path><path d="M5.33333 14.8257C5.33333 14.8257 0 14.8257 0 20.1635C0 25.5013 5.33333 25.5013 5.33333 25.5013V14.8257Z" fill="url(#pkrg-bot-2)"></path><path d="M26.6667 14.8257C26.6667 14.8257 32 14.8257 32 20.1635C32 25.5013 26.6667 25.5013 26.6667 25.5013V14.8257Z" fill="url(#pkrg-bot-3)"></path><path d="M26.6667 10.8224H5.33333V28.1701H26.6667V10.8224Z" fill="#4FD8FF"></path><path d="M15.76 11.2761C20.4133 11.2761 24.0933 12.3036 25.24 13.2911C26.3867 14.8657 26.1733 24.4737 24.9067 26.0751C24.2533 26.849 21.0667 28.01 16.28 28.01C11.24 28.01 7.73333 26.7556 6.94667 25.9283C5.70667 24.367 5.65333 14.7189 6.84 13.211C7.58667 12.4637 10.6667 11.2761 15.76 11.2761ZM15.76 7.27273C10.9067 7.27273 6.12 8.28691 4.01333 10.382C1.25333 13.1309 1.34667 25.7948 4.01333 28.6372C6.08 30.8524 11.2133 32 16.28 32C21.3467 32 26.08 30.9058 27.9867 28.6372C30.48 25.648 30.8667 12.9975 27.9867 10.382C25.7467 8.34028 20.72 7.27273 15.76 7.27273Z" fill="url(#pkrg-bot-4)"></path><path d="M23 17.975C23 16.6852 21.9553 15.6397 20.6667 15.6397C19.378 15.6397 18.3333 16.6852 18.3333 17.975V21.3111C18.3333 22.6008 19.378 23.6464 20.6667 23.6464C21.9553 23.6464 23 22.6008 23 21.3111V17.975Z" fill="black"></path><path d="M13.6667 17.975C13.6667 16.6852 12.622 15.6397 11.3333 15.6397C10.0447 15.6397 9 16.6852 9 17.975V21.3111C9 22.6008 10.0447 23.6464 11.3333 23.6464C12.622 23.6464 13.6667 22.6008 13.6667 21.3111V17.975Z" fill="black"></path><defs><linearGradient id="pkrg-bot-0" x1="15.963" y1="0.886836" x2="15.963" y2="43.3072" gradientUnits="userSpaceOnUse"><stop stop-color="#00C7FF"></stop><stop offset="0.99" stop-color="#006399"></stop></linearGradient><linearGradient id="pkrg-bot-1" x1="16" y1="-69.28" x2="16" y2="-69.28" gradientUnits="userSpaceOnUse"><stop stop-color="#00C7FF"></stop><stop offset="0.99" stop-color="#006399"></stop></linearGradient><linearGradient id="pkrg-bot-2" x1="17.28" y1="-0.2" x2="16.8598" y2="31.7766" gradientUnits="userSpaceOnUse"><stop stop-color="#00C7FF"></stop><stop offset="0.99" stop-color="#006399"></stop></linearGradient><linearGradient id="pkrg-bot-3" x1="14.72" y1="-0.2" x2="15.1402" y2="31.7766" gradientUnits="userSpaceOnUse"><stop stop-color="#00C7FF"></stop><stop offset="0.99" stop-color="#006399"></stop></linearGradient><linearGradient id="pkrg-bot-4" x1="5.14015" y1="0.587156" x2="36.6592" y2="34.8544" gradientUnits="userSpaceOnUse"><stop stop-color="#00C7FF"></stop><stop offset="0.99" stop-color="#006399"></stop></linearGradient></defs></svg>'
  };

  var ROL_SIRASI = ['broadcaster', 'staff', 'moderator', 'verified', 'vip', 'founder', 'og',
                    'sub_gifter', 'subscriber', 'bot'];
  var rolRenk = Object.create(null);          // kullanıcının seçtikleri (yoksa varsayılan)
  var vurguMuaf = Object.create(null);        // vurgulanmayacak kişiler

  /* Kullanıcının KAPATTIĞI roller. Boş nesne = hepsi açık. */
  var rolKapali = Object.create(null);

  /* Ham renk — ayarlardaki renk kutusu için: rol kapalı olsa da gerçek
     rengini göstermeli, yoksa kutu boş görünür. */
  function rolRengiHam(r) { return rolRenk[r] || ROL_VARSAYILAN[r] || ''; }

  /* Vurgulamada kullanılan renk. Kapalı rol renk döndürmez; şerit çizen her
     yer zaten rengin varlığına bakıyor, tek çıkış noktası burası. */
  function rolRengi(r) { return rolKapali[r] ? '' : rolRengiHam(r); }

  function rolAcikMi(r) { return !rolKapali[r]; }
  function rolAcKapa(r, acik) {
    if (acik) delete rolKapali[r]; else rolKapali[r] = 1;
    try { chrome.storage.local.set({ pkRolKapali: Object.keys(rolKapali) }); } catch (e) {}
    kisiUygula();
  }
  function rolAdi(r) { return t('pkRol_' + r); }


  function rolYaz(ad, rol) {
    var k = String(ad || '').toLowerCase(); if (!k) return;
    if (!rol) { if (kisiRol[k]) return; rol = ''; }        // rolsüzü de yaz ama rolü olanı ezme
    if (kisiRol[k] === rol) return;
    if (!(k in kisiRol)) { kisiRolSira.push(k); if (kisiRolSira.length > 3000) delete kisiRol[kisiRolSira.shift()]; }
    kisiRol[k] = rol;
  }

  /* Vurgulamayı sohbet balonlarına uygular. kisiUygula ile aynı turda çağrılır;
     ayrı DOM taraması yapmamak için o döngüden geçirilir. */
  /* Belirli hesaplara özel akan isim gradyanı — kodda sabit, sunucudan gelmez.
     Anahtar küçük harf kullanıcı adı, değer durak renkleri. Dizi BAŞA DÖNMELİ
     (ilk renk sonda da var), yoksa animasyon her turda geri sıçrıyor.
     Yeşil Kick'in kendi vurgu tonu (ölçüldü: rgb(83,252,24)). */
  /* Hesaba özel ÇERÇEVE: anahtar küçük harf kullanıcı adı, değer renk.
     Satırın kutu ölçüsüne DOKUNULMUYOR (bkz. rolBoya) — çerçeve
     `box-shadow: inset`, ışık taraması `background-position` animasyonu. */
  /* 10.15 — BOŞ. Eskiden burada tek hesap sabitliydi; artık satır rengi
     SUNUCUDAN geliyor (abonelik hakkı ya da panelden elle atama).
     Dizi duruyor ki `rolBoya` yedek olarak okumaya devam edebilsin —
     acil bir durumda buraya elle satır eklemek hâlâ mümkün. */
  var PK_OZEL_CERCEVE = {};

  /* Hesaba özel BAŞ/SON simgesi: [ön, arka]. Metne yazılmıyor, sözde
     öğeyle çiziliyor — ada dayalı aramalar bozulmasın (bkz. msStilKur). */
  var PK_OZEL_AD_SIM = {
    pumpzera: ['\u231E', '\u231D']          // ⌞ ⌝
  };

  /* 10.15 — BOŞ. Akan isim renkleri de sunucudan geliyor (bkz. akanByName).
     Yedek okuma yolu `ozelAdUygula` içinde duruyor. */
  var PK_OZEL_AD = {};

  function rolBoya(m, k) {
    /* HESABA ÖZEL ÇERÇEVE rol renginin ÖNÜNDE. Aynı özelliklere (box-shadow,
       background-*) yazan TEK yer burası olsun — iki ayrı işlev yazarsa
       sırayla birbirini ezer. */
    /* 10.15 — önce SUNUCUDAN gelen satır rengi (abonelik hakkı), yoksa koda
       gömülü eski harita. Gömülü harita yedek olarak duruyor ki sunucu
       ulaşılamazken kurucu hesabın görünümü kaybolmasın. */
    var cerceveRenk = (badgeMaps && badgeMaps.satirByName && badgeMaps.satirByName[k]) || PK_OZEL_CERCEVE[k];
    var cerceveVar = m.getAttribute('data-pk-cerceve') || '';
    if (cerceveRenk) {
      /* Animasyon gerçekten duruyor mu diye de bakıyoruz: Kick satırı yeniden
         çizerken satır içi stili siliyor ama niteliğimiz kalabiliyor —
         `ozelAdUygula`da aynı tuzağa düşülmüştü. */
      if (cerceveVar !== k || !m.style.getPropertyValue('animation')) {
        m.setAttribute('data-pk-cerceve', k);
        m.removeAttribute('data-pk-rol');            // çerçeve rolü değiştiriyor
        akanSatirBoya(m, cerceveRenk);
      }
      return;
    }
    if (cerceveVar) {
      m.removeAttribute('data-pk-cerceve');
      ['box-shadow', 'background-color', 'background-image', 'background-size',
       'background-repeat', 'background-position', 'animation']
        .forEach(function (x) { m.style.removeProperty(x); });
    }
    var rol = (settings.rolVurgu !== false) ? kisiRol[k] : '';
    /* Muaf listesi ve bot yoksayma: rol BİLİNİYOR ama boyanmıyor. Rolü
       silmiyoruz — kullanıcı listeden çıkarınca renk hemen geri gelsin. */
    if (rol && vurguMuaf[k]) rol = '';
    if (rol === 'bot' && settings.botYoksay !== false) rol = '';
    var renk = rol ? rolRengi(rol) : '';
    var simdiki = m.getAttribute('data-pk-rol') || '';
    if (renk && simdiki !== rol) {
      m.setAttribute('data-pk-rol', rol);
      m.style.setProperty('box-shadow', 'inset 3px 0 0 ' + renk, 'important');
      m.style.setProperty('background-color', renk + '14', 'important');   // %8 opaklık
    } else if (!renk && simdiki) {
      m.removeAttribute('data-pk-rol');
      m.style.removeProperty('box-shadow');
      m.style.removeProperty('background-color');
    }
  }

  /* Panelde gösterilecek özet: hangi rolden kaç kişi yazdı. */


  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — OTOMATİK KALİTE
   * Kick her kanal açılışında kaliteyi "Otomatik"e döndürüyor; bağlantı bir an
   * dalgalanınca 360p'ye düşüp orada kalabiliyor. Bu modül tercih ettiğin
   * kaliteyi kanal başına BİR KEZ uygular.
   *
   * Nasıl: oynatıcının kendi kalite menüsü açılır, seçenek metinleri okunur
   * (720p60, 1080p…), hedefe en yakını tıklanır, menü kapatılır. Bizim ayrı bir
   * akış açmamız ya da m3u8'e dokunmamız GEREKMEZ — Kick'in kendi düğmeleri.
   *
   * Menü metni dile göre değişmez (rakam + "p"), o yüzden dil-bağımsızdır;
   * yalnız "Otomatik/Auto" seçeneği için desen kullanılır.
   * ══════════════════════════════════════════════════════════════════════ */
  var KALITE_SECENEK = ['oto', '1080', '720', '480', '360', '160'];
  var kaliteYapildi = '';        // kanal başına tek deneme
  var kaliteMesgul = false;

  function kaliteYazi(k) {
    if (k === 'oto') return 'Otomatik';
    return k + 'p';
  }

  /* Menüdeki seçenekleri okur. Menü AÇIKKEN çağrılmalı.
     Dönen: [{ el, p, oto }] — p sayısal yükseklik (1080), oto=Otomatik satırı. */
  /* Kick seçenekleri `[role="menuitemradio"]` olarak veriyor ve SEÇİLİ
     olanı `data-state="checked"` ile işaretliyor — tahmin gerektirmeyen
     gerçek bir başarı ölçüsü. Etiketler "1080p60" gibi olabiliyor. */
  function kaliteSecenekleri(kok) {
    var cikti = [];
    var adaylar = kok.querySelectorAll('[role="menuitemradio"], [role="menuitem"], [role="option"], button, li');
    for (var i = 0; i < adaylar.length; i++) {
      var a = adaylar[i];
      if (a.querySelector('[role="menuitemradio"], [role="menuitem"], button')) continue;   // yalnız yaprak
      var t = (a.textContent || '').trim();
      if (!t || t.length > 24) continue;
      var secili = a.getAttribute('data-state') === 'checked';
      var m = t.match(/(\d{3,4})\s*p/i);
      if (m) { cikti.push({ el: a, p: parseInt(m[1], 10), oto: false, secili: secili }); continue; }
      if (/^(otomatik|auto|automático|automatique|automatisch|автоматически)$/i.test(t)) {
        cikti.push({ el: a, p: 0, oto: true, secili: secili });
      }
    }
    return cikti;
  }

  /* Hedefe en uygun seçeneği seçer:
     - 'oto'  → Otomatik satırı
     - sayı   → o değer varsa o, yoksa ALTINDAKİ en yüksek (istenenden fazlasını
                zorlamayalım; kullanıcı veri tasarrufu için düşük seçmiş olabilir) */
  function kaliteEsle(liste, hedef) {
    if (hedef === 'oto') { for (var i = 0; i < liste.length; i++) if (liste[i].oto) return liste[i]; return null; }
    var h = parseInt(hedef, 10); if (!h) return null;
    var tam = null, alt = null;
    for (var j = 0; j < liste.length; j++) {
      var o = liste[j]; if (o.oto) continue;
      if (o.p === h) tam = o;
      else if (o.p < h && (!alt || o.p > alt.p)) alt = o;
    }
    return tam || alt;
  }

  /* Kick kontrol şeridini boştayken DOM'dan söküyor (ölçüldü). Şerit React
     durumuyla gidiyor, CSS ile değil — bu yüzden kabarcıklı fare olayı
     React'in kök dinleyicisine ulaşıp şeridi geri getirebiliyor. */
  function oynaticiSeridiUyandir() {
    var v = oynaticiVideosu(); if (!v) return;
    var r = v.getBoundingClientRect();
    if (!r.width || !r.height) return;
    var o = { bubbles: true, cancelable: true, composed: true,
              clientX: Math.round(r.left + r.width / 2),
              clientY: Math.round(r.bottom - 30),          // şeridin durduğu bant
              pointerId: 1, pointerType: 'mouse', isPrimary: true, buttons: 0 };
    var hedef = [v, v.parentElement, oynaticiKabi(v, r)];
    for (var i = 0; i < hedef.length; i++) {
      if (!hedef[i]) continue;
      try { hedef[i].dispatchEvent(new PointerEvent('pointermove', o)); } catch (e) {}
      try { hedef[i].dispatchEvent(new MouseEvent('mousemove', o)); } catch (e2) {}
    }
  }

  /* Şerit görünene kadar ARALIKLA dürter. Tek dürtme çoğu zaman yetmiyor:
     Kick şeridi React durumuyla söküyor ve geri gelmesi bir tur alıyor.
     `bitti(dugme|null)`. */
  function oynaticiAyarDugmesiniBekle(bitti) {
    var b0 = oynaticiButonu(null, 'Settings');
    if (b0) { bitti(b0); return; }
    var kalan = 20;                                   // 20 × 150ms ≈ 3sn
    var zaman = setInterval(function () {
      oynaticiSeridiUyandir();
      var b = oynaticiButonu(null, 'Settings');
      if (b || --kalan <= 0) { clearInterval(zaman); bitti(b || null); }
    }, 150);
    oynaticiSeridiUyandir();
  }

  /* Radix tetikleyicisini gerçek fare gibi aç: önce hover, sonra basış,
     bırakma AYRI GÖREVDE (Radix pointerdown'ı eşzamansız işliyor). */
  function pkRadixAc(btn, bitti) {
    var y = { bubbles: true, cancelable: true, composed: true,
              pointerId: 1, pointerType: 'mouse', isPrimary: true };
    ['pointerover', 'pointerenter'].forEach(function (n) {
      try { btn.dispatchEvent(new PointerEvent(n, y)); } catch (e) {}
    });
    ['mouseover', 'mouseenter'].forEach(function (n) {
      try { btn.dispatchEvent(new MouseEvent(n, y)); } catch (e) {}
    });
    try { btn.dispatchEvent(new PointerEvent('pointerdown', Object.assign({ button: 0, buttons: 1 }, y))); } catch (e) {}
    try { btn.dispatchEvent(new MouseEvent('mousedown', Object.assign({ button: 0, buttons: 1 }, y))); } catch (e) {}
    try { btn.dispatchEvent(new PointerEvent('pointerup', Object.assign({ button: 0, buttons: 0 }, y))); } catch (e) {}
    setTimeout(function () {
      try { btn.dispatchEvent(new MouseEvent('mouseup', Object.assign({ button: 0, buttons: 0 }, y))); } catch (e) {}
      try { btn.dispatchEvent(new MouseEvent('click', Object.assign({ button: 0, buttons: 0 }, y))); } catch (e) {}
      bitti();
    }, 1);
  }

  /* Seçeneği etkinleştir: basış, bırakma AYRI GÖREVDE. */
  function pkRadixSec(oge, bitti) {
    var y = { bubbles: true, cancelable: true, composed: true,
              pointerId: 1, pointerType: 'mouse', isPrimary: true };
    try { oge.dispatchEvent(new PointerEvent('pointerdown', y)); } catch (e) {}
    try { oge.dispatchEvent(new MouseEvent('mousedown', y)); } catch (e) {}
    setTimeout(function () {
      try { oge.dispatchEvent(new PointerEvent('pointerup', y)); } catch (e) {}
      try { oge.dispatchEvent(new MouseEvent('mouseup', y)); } catch (e) {}
      try { oge.dispatchEvent(new MouseEvent('click', y)); } catch (e) {}
      bitti();
    }, 1);
  }

  /* İşlem bitince hover durumunu bırak — yoksa şerit açık takılı kalıyor. */
  function pkHoverBirak(el) {
    if (!el) return;
    var y = { bubbles: true, cancelable: true, composed: true,
              pointerId: 1, pointerType: 'mouse', isPrimary: true };
    ['pointerout', 'pointerleave'].forEach(function (n) {
      try { el.dispatchEvent(new PointerEvent(n, y)); } catch (e) {}
    });
    ['mouseout', 'mouseleave'].forEach(function (n) {
      try { el.dispatchEvent(new MouseEvent(n, y)); } catch (e) {}
    });
  }

  /* Düğme yoksa şeridi uyandırıp ~600ms bekler; sonra `bitti(düğme|null)`.
     Şerit React durumuyla söküldüğü için kabarcıklı fare olayı onu geri
     getirebiliyor — ama anında değil, o yüzden beklemek şart.
     TAM EKRANA GİRİŞ İÇİN KULLANMA: beklemek user activation'ı öldürür. */
  function oynaticiButonunuBekle(desen, ikon, bitti) {
    var b0 = oynaticiButonu(desen, ikon);
    if (b0) { bitti(b0); return; }
    oynaticiSeridiUyandir();
    var kalan = 12;                                        // 12 × 50ms
    (function bekle() {
      var b = oynaticiButonu(desen, ikon);
      if (b) { bitti(b); return; }
      if (--kalan <= 0) { bitti(null); return; }
      setTimeout(bekle, 50);
    })();
  }

  /* `bitti(basarili)` her yolda bir kez çağrılır. */
  function oynaticiButonunaBas(desen, ikon, bitti) {
    oynaticiButonunuBekle(desen, ikon, function (b) {
      if (b) { try { b.click(); } catch (e) {} }
      if (bitti) bitti(!!b);
    });
  }

  /* Menü kabı: düğmeye basınca açılan katman. Kick'in menüsü body'ye
     taşınabildiği için düğmenin içinde ARAMAYIZ — sayfadaki YENİ açılmış
     rol=menu/dialog katmanına bakarız. */
  /* Menüyü TETİKLEYİCİNİN KİMLİĞİNE göre bul: Radix açılan içeriğe
     `aria-labelledby=<düğme id>` koyuyor. Eski "görünen herhangi bir
     role=menu" araması yanlış düğümü (kendi panelimiz, başka bir açılır
     liste) yakalayabiliyordu. */
  function kaliteMenuKabi(btn) {
    var id = btn && btn.getAttribute && btn.getAttribute('id');
    if (id) {
      var k = document.querySelector('[data-radix-menu-content][aria-labelledby="' + id + '"]');
      if (k) return k;
    }
    var aday = document.querySelectorAll('[data-radix-menu-content], [role="menu"], [role="listbox"]');
    for (var i = aday.length - 1; i >= 0; i--) {
      if (aday[i].querySelector('[role="menuitemradio"], [role="menuitem"]')) return aday[i];
    }
    return null;
  }

  /* Tek seferlik uygulama. Menüyü açar, seçer, kapatır. Hiçbir adımda
     kullanıcının açtığı bir menüyü kapatmaz: kendi açtığımız menü değilse
     dokunmadan çıkarız. */
  /* `bitti(uygulandiMi)` — otoKaliteKur buna bakıp "yapıldı" işaretliyor.
     Sessiz başarısızlık kanal başına tek denemeyi harcıyordu. */
  /* `bitti(ok, sebep)` — sebep otoKaliteKur'un ne yapacağını belirliyor:
       'dugme-yok'     oynatıcı şeridi daha ayakta değil → deneme HAKKI YANMAZ
       'menu-yok'      menü açılmadı → hak yanar
       'yok'           bu yayında o çözünürlük yok → kanalda bırak
       'dogrulanmadi'  tıklandı ama video o yüksekliğe geçmedi → hak yanar */
  function kaliteUygula(hedef, elle, bitti) {
    function son(ok, sebep) { kaliteMesgul = false; if (bitti) bitti(!!ok, sebep || ''); }
    if (kaliteMesgul) { if (bitti) bitti(false, 'mesgul'); return; }
    kaliteMesgul = true;

    oynaticiAyarDugmesiniBekle(function (btn) {
      if (!btn) { if (elle) toast(t('pkTKaliteMenusuYok')); son(false, 'dugme-yok'); return; }

      /* Kullanıcı bu gidip gelmeyi görmesin: şerit işlem boyunca saydam. */
      var serit = btn.closest ? btn.closest('.z-controls') : null;
      var eskiOp = serit ? serit.style.opacity : null;
      if (serit) serit.style.opacity = '0';
      function temizle() {
        if (serit) { if (eskiOp) serit.style.opacity = eskiOp; else serit.style.removeProperty('opacity'); }
        pkHoverBirak(btn);
        var v = oynaticiVideosu(); if (v) pkHoverBirak(v.parentElement);
      }

      if (kaliteMenuKabi(btn)) { if (elle) toast(t('pkTAcikMenuKapat')); temizle(); son(false, 'acik-menu'); return; }

      pkRadixAc(btn, function () {
        /* Menü DOM'a girene kadar kısa aralıklarla bak. */
        var kalan = 16;                                  // 16 × 60ms ≈ 1sn
        (function bekle() {
          var kok = kaliteMenuKabi(btn);
          if (kok) { kaliteSec(btn, kok, hedef, elle, function (ok, sb) { temizle(); son(ok, sb); }); return; }
          if (--kalan <= 0) {
            if (elle) toast(t('pkTKaliteMenusuAcilmadi'));
            temizle(); son(false, 'menu-yok'); return;
          }
          setTimeout(bekle, 60);
        })();
      });
    });
  }

  /* Menü açıkken: seçeneği bul, gerekiyorsa etkinleştir, menüyü kapat. */
  function kaliteSec(btn, kok, hedef, elle, son) {
    var eskiOp = kok.style.opacity;
    kok.style.opacity = '0';                            // seçim titremesi görünmesin
    function kapat(ok, sebep) {
      if (eskiOp) kok.style.opacity = eskiOp; else kok.style.removeProperty('opacity');
      kaliteMenuKapat(btn);
      son(ok, sebep || '');
    }
    try {
      var liste = kaliteSecenekleri(kok);
      var sec = kaliteEsle(liste, hedef);
      if (!sec) {
        if (elle) toast(t('pkTKaliteYok', [kaliteYazi(hedef)]));
        kapat(false, 'yok');
        return;
      }
      /* ZATEN SEÇİLİ görünüyor (Kick `data-state="checked"` veriyor).
         AMA İŞARET TEK BAŞINA KANIT DEĞİL: kanal değiştirdikten hemen sonra
         menü bir süre ÖNCEKİ yayının seçimini taşıyor, video ise yeni yayında
         otomatik kalitede oynuyor. Buna "oldu" dersek kanal bir daha hiç
         denenmiyordu. Video gerçekten o yükseklikteyse kabul ediyoruz;
         değilse tıklayıp uyguluyoruz. */
      if (sec.secili) {
        var vs = oynaticiVideosu();
        var ys = vs ? (vs.videoHeight || 0) : 0;
        var uyuyor = sec.oto ? ys > 0 : Math.abs(ys - sec.p) <= 2;
        if (uyuyor || !ys) {              // ys yoksa henüz ölçemiyoruz, işarete uy
          if (elle) toast('Kalite: ' + (sec.oto ? 'Otomatik' : sec.p + 'p'));
          kapat(uyuyor, uyuyor ? '' : 'dogrulanmadi');
          return;
        }
        /* İşaret duruyor ama video başka yükseklikte → seçimi tazele. */
      }
      pkRadixSec(sec.el, function () {
        if (elle) toast('Kalite: ' + (sec.oto ? 'Otomatik' : sec.p + 'p'));
        /* Doğrulama: video gerçekten o çözünürlüğe geçti mi? Tıklamanın
           kendisi kanıt değil. Geçmediyse otoKaliteKur sonraki turda
           yeniden dener ve o turda `data-state` kesin cevabı verir. */
        setTimeout(function () {
          var v2 = oynaticiVideosu();
          var y = v2 ? (v2.videoHeight || 0) : 0;
          var oldu = sec.oto ? y > 0 : Math.abs(y - sec.p) <= 2;
          kapat(oldu, oldu ? '' : 'dogrulanmadi');
        }, 1200);
      });
    } catch (e) { kapat(false); }
  }

  /* Seçim menüyü kapatmadıysa Esc, o da olmazsa tetikleyiciye tekrar bas. */
  function kaliteMenuKapat(btn) {
    if (!kaliteMenuKabi(btn)) return;
    try { document.body.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape', bubbles: true })); } catch (e) {}
    if (kaliteMenuKabi(btn)) { try { btn.click(); } catch (e2) {} }
  }

  /* Kanal değişince bir kez dener. Oynatıcı hazır değilse tur tekrar çağırır. */
  var kaliteDeneme = 0, kaliteDenemeSlug = '', kaliteBekliyor = false;
  var kaliteHedefYok = '';       // bu yayında o çözünürlük yok → kanalda bırak
  var kaliteSapmaBasi = 0;       // sapmanın kesintisiz ne zamandır sürdüğü
  var kaliteSapmaHak = 0;        // kanal başına yeniden uygulama tavanı
  var KALITE_DENEME_TAVAN = 20;  // ilk kurulumda kaç kez denensin
  /* Sapmadan sonra bütçe SIFIRDAN başlamaz. Başlarsa kötü senaryoda (yayın
     1080p'yi tutamıyor) 3 × 21 ≈ 80 kez menü açılıp kapanıyor; kullanıcı bunu
     oynatıcı çubuğunun sürekli titremesi olarak görüyor. Sapma düzeltmesi
     kısa olmalı: tutuyorsa ilk denemede tutar. */
  var KALITE_SAPMA_DENEME = 3;
  /* Kanal başına dener; UYGULANANA KADAR bırakmaz.
     Eskiden denemeden önce "yapıldı" işaretliyordu: sessiz bir başarısızlık
     (şerit kapalı, menü açılmadı) o kanalda otomatik kaliteyi tamamen
     öldürüyordu. Kullanıcının "her yayına girdiğimde kendisi alsın" dediği
     davranış buydu. */
  /* Uygulandı sanılan kalite GERÇEKTEN duruyor mu?
     Kanal değiştirince Kick oynatıcıyı yeni yayın için baştan kuruyor ve
     seçimi otomatiğe döndürebiliyor; biz "yapıldı" işaretlediğimiz için bir
     daha hiç bakmıyorduk. Anlık geçişleri sapma saymamak için yükseklik
     KESİNTİSİZ birkaç saniye tutmazsa sapma sayılıyor. */
  function kaliteSapti() {
    var hedef = parseInt(settings.otoKalite, 10);
    if (!hedef) return false;
    var v = oynaticiVideosu();
    if (!v || v.readyState < 3 || !v.videoHeight) { kaliteSapmaBasi = 0; return false; }
    if (Math.abs(v.videoHeight - hedef) <= 2) { kaliteSapmaBasi = 0; return false; }
    if (!kaliteSapmaBasi) { kaliteSapmaBasi = Date.now(); return false; }
    return (Date.now() - kaliteSapmaBasi) > 4000;
  }

  function otoKaliteKur() {
    if (!settings.consented || !settings.otoKalite || settings.otoKalite === 'oto') return;
    var slug = sayfaSlug();
    if (!slug) { kaliteYapildi = ''; kaliteDenemeSlug = ''; kaliteHedefYok = ''; return; }
    /* Bu yayında o çözünürlük hiç yok (kaynak 720p olabilir) — menüyü sürekli
       açıp kapatmanın anlamı yok. */
    if (kaliteHedefYok === slug) return;
    if (kaliteYapildi === slug) {
      if (!kaliteSapti()) return;
      if (++kaliteSapmaHak > 3) return;              // sonsuz döngüye karşı tavan
      kaliteYapildi = '';
      kaliteDeneme = KALITE_DENEME_TAVAN - KALITE_SAPMA_DENEME;   // kısa bütçe
      kaliteSapmaBasi = 0;
    }
    if (kaliteDenemeSlug !== slug) {
      kaliteDenemeSlug = slug; kaliteDeneme = 0;
      kaliteSapmaHak = 0; kaliteSapmaBasi = 0; kaliteHedefYok = '';
    }
    if (kaliteBekliyor) return;                       // süren deneme var
    var v = oynaticiVideosu(); if (!v || !v.videoWidth || v.readyState < 3) return;
    if (kaliteDeneme > KALITE_DENEME_TAVAN) { kaliteYapildi = slug; return; }   // pes
    kaliteBekliyor = true;
    kaliteUygula(settings.otoKalite, false, function (ok, sebep) {
      kaliteBekliyor = false;
      if (ok) { kaliteYapildi = slug; kaliteDeneme = 0; kaliteSapmaBasi = 0; return; }
      /* Bu yayında hedef çözünürlük yoksa bir daha deneme. */
      if (sebep === 'yok') { kaliteHedefYok = slug; return; }
      /* ŞERİT HENÜZ AYAKTA DEĞİLSE HAK YANMAZ. Kanal değişiminden hemen sonra
         Kick oynatıcı şeridini söküyor ve yayına katılmak saniyeler alıyor;
         eskiden bu bekleyiş 20 denemeyi saniyeler içinde tüketiyor, kanal
         kalıcı olarak "pes edildi" damgası yiyordu — 1080p'nin kanal
         değişiminde tutmamasının sebebi buydu. */
      if (sebep === 'dugme-yok' || sebep === 'mesgul') return;
      kaliteDeneme++;
    });
  }


  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — SOHBETİ YAYININ ÜSTÜNE BİNDİRME
   *
   * Kick'in KENDİ sohbet sütununu taşımıyoruz: React'in yönettiği bir düğümü
   * DOM'da başka yere almak, tam ekranda kırpılmaya ve yeniden çizimde
   * kopmalara yol açıyor. Onun yerine KENDİ katmanımızı çiziyoruz; içerik
   * zaten dinlediğimiz Pusher olaylarından geliyor (yeni bağlantı yok, ağ
   * isteği yok). Emoteler mesaj metnindeki [emote:ID:ad] kodundan üretilir —
   * görsel Kick'in kendi CDN'inden gelir, bizim sunucumuza uğramaz.
   *
   * Tam ekran: position:fixed katman, tam ekrandaki elemanın ALTINDA kalır.
   * Bu yüzden fullscreenchange'te katmanı tam ekran elemanının İÇİNE taşıyoruz.
   * ══════════════════════════════════════════════════════════════════════ */
  var BINDIR_ID = 'pk-bindir';
  var bindirMesajlar = [];                   // { kim, rol, pr, ts }
  var BINDIR_SINIR = 60;

  /* [emote:1730752:pepeLaugh] → mesajGovdesi'nin beklediği parça listesi. */
  function emoteAyristir(metin) {
    var pr = [], kalan = String(metin || ''), d = /\[emote:(\d+):([^\]]*)\]/;
    for (var g = 0; g < 60; g++) {
      var m = kalan.match(d);
      if (!m) break;
      if (m.index > 0) pr.push({ t: 'y', v: kalan.slice(0, m.index) });
      pr.push({ s: 'https://files.kick.com/emotes/' + m[1] + '/fullsize', a: m[2] || '' });
      kalan = kalan.slice(m.index + m[0].length);
    }
    if (kalan) pr.push({ t: 'y', v: kalan });
    return pr;
  }

  function bindirEkle(kim, rol, metin, ts, renk) {
    if (settings.bindir !== true) return;
    if (!kim || !metin) return;
    if (gizliKisi[String(kim).toLowerCase()]) return;      // gizlenen kişi burada da görünmesin
    var m = { kim: kim, rol: rol || '', pr: emoteAyristir(metin), ts: ts || Date.now(), renk: renk || '' };
    bindirMesajlar.push(m);
    while (bindirMesajlar.length > BINDIR_SINIR) bindirMesajlar.shift();
    /* Her mesajda tüm listeyi yeniden çizmiyoruz: 60 satırın emote görselleri
       baştan yükleniyor ve katman gözle görülür şekilde titriyordu. Yeni satırı
       ekleyip en eskiyi düşürmek yeterli. */
    var p = document.getElementById(BINDIR_ID);
    var ic = p && p.querySelector('[data-pk-bindir]');
    if (!ic) return;
    ic.appendChild(bindirSatir(m));
    while (ic.children.length > BINDIR_SINIR) ic.removeChild(ic.firstChild);
  }

  function bindirSatir(m) {
    var r = el('div', 'text-sm');
    r.style.cssText = 'line-height:1.35;text-shadow:0 1px 3px rgba(0,0,0,.95),0 0 2px rgba(0,0,0,.9);word-break:break-word';
    /* Kick'in kullanıcıya verdiği renk önce gelir: kullanıcı "sohbette
       hangi renkteyse bindirmede de o renk" istedi. Kick renk vermezse
       eski davranış (rol rengi, sonra beyaz). */
    var renk = m.renk || (m.rol && rolRengi(m.rol)) || '#ffffff';
    var ad = el('span', 'font-bold', m.kim + ': ');
    ad.style.color = renk;
    r.appendChild(ad);
    r.appendChild(mesajGovdesi(m.pr, 18));
    return r;
  }

  function bindirKapat() {
    var p = document.getElementById(BINDIR_ID); if (p) p.remove();
  }

  /* Katmanı doğru ebeveyne koyar: tam ekrandayken tam ekran elemanının içine,
     değilken body'ye. Yeri değiştiyse taşır; aynıysa dokunmaz. */
  function bindirKap() { return pkKatman(); }

  function bindirKur() {
    if (settings.bindir !== true || !settings.consented) { bindirKapat(); return; }
    /* Ayar panelimiz açıkken gizle: panel videonun önünü kapatıyor, bindirme
       onun da üstünde kalıp panel yazılarıyla iç içe giriyordu. */
    if (panelAcikMi()) { bindirKapat(); return; }
    var v = oynaticiVideosu(); if (!v) { bindirKapat(); return; }
    var vr = v.getBoundingClientRect();
    if (vr.width < 200 || vr.height < 100) { bindirKapat(); return; }

    var p = document.getElementById(BINDIR_ID);
    var kap = bindirKap();
    if (p && p.parentElement !== kap) { p.remove(); p = null; }     // tam ekrana girdi/çıktı
    if (!p) {
      p = el('div', 'flex flex-col');
      p.id = BINDIR_ID;
      /* KATMAN SIRASI — Kick'in ölçülmüş ölçeğine göre:
           202 oynatıcı şeridi · 300 video kabı · 401 kenar çubuğu/mini
           oynatıcı · 402 navbar · 501 profil kartı · 801 ipucu
         302–400 aralığı boş; 350 videonun üstünde ama Kick'in AÇTIĞI hiçbir
         şeyin üstünde değil. Eskiden 2147483040 idi ve profil kartının önüne
         geçiyordu. */
      p.style.cssText = 'position:fixed;z-index:350;pointer-events:none;' +
        'gap:2px;overflow:hidden;padding:8px;border-radius:6px;' +
        'mask-image:linear-gradient(to bottom,transparent,#000 22%);' +
        '-webkit-mask-image:linear-gradient(to bottom,transparent,#000 22%)';
      var ic = el('div', 'flex flex-col');
      ic.setAttribute('data-pk-bindir', '1');
      ic.style.cssText = 'gap:2px;margin-top:auto';
      p.appendChild(ic);
      kap.appendChild(p);
      bindirSatirCiz();
    }
    /* Konum: videonun sol altı. Kick'in kontrol şeridi altta ~72px yer
       kapladığı için o kadar yukarıdan başlatıyoruz, düğmeleri örtmesin.

       `bindirKur` her DOM turunda çağrılıyor (sohbet akarken saniyede birkaç
       kez). Her seferinde altı stil özelliği yazmak gereksiz yeniden düzenleme
       demek; imza değişmediyse dokunmuyoruz. */
    var gen = Math.max(220, Math.min(360, Math.round(vr.width * 0.26)));
    var yuk = Math.max(120, Math.round(vr.height * (settings.bindirBoy || 55) / 100));
    var saydam = (settings.bindirSaydam == null ? 90 : settings.bindirSaydam);
    var konum = settings.bindirKonum || 'sol-alt';
    var ustte = konum === 'sol-ust' || konum === 'sag-ust';
    var solda = konum === 'sol-alt' || konum === 'sol-ust';
    var imza = Math.round(vr.left) + ':' + Math.round(vr.top) + ':' + Math.round(vr.right) +
               ':' + Math.round(vr.bottom) + ':' + gen + ':' + yuk + ':' + saydam + ':' + konum;
    if (p.getAttribute('data-pk-imza') === imza) return;
    p.setAttribute('data-pk-imza', imza);

    p.style.width = gen + 'px';
    p.style.height = yuk + 'px';
    if (solda) p.style.left = Math.round(vr.left + 12) + 'px';
    else p.style.left = Math.round(vr.right - 12 - gen) + 'px';
    /* Altta Kick'in kontrol şeridi ~72px yer kaplıyor, düğmeleri örtmeyelim;
       üstte böyle bir engel yok. */
    if (ustte) p.style.top = Math.round(vr.top + 12) + 'px';
    else p.style.top = Math.round(vr.bottom - 76 - yuk) + 'px';

    /* SAYDAMLIK YALNIZ ZEMİNE: eskiden tüm öğenin opacity'siydi ve yazıyı da
       soluklaştırıyordu. */
    p.style.opacity = '1';
    p.style.background = 'rgba(0,0,0,' + (saydam / 100) + ')';

    /* Solma yönü konuma göre: alt köşelerde yeni satır alttan gelir, üstte
       tersi. */
    var maske = ustte
      ? 'linear-gradient(to top,transparent,#000 22%)'
      : 'linear-gradient(to bottom,transparent,#000 22%)';
    p.style.maskImage = maske;
    p.style.webkitMaskImage = maske;
    /* Üstteyken satırlar yukarıdan aşağı yığılsın. */
    var ic0 = p.querySelector('[data-pk-bindir]');
    if (ic0) ic0.style.marginTop = ustte ? '0' : 'auto';
  }

  // Tam yeniden çizim — yalnız katman ilk kurulduğunda / tam ekrana taşındığında.
  function bindirSatirCiz() {
    var p = document.getElementById(BINDIR_ID); if (!p) return;
    var ic = p.querySelector('[data-pk-bindir]'); if (!ic) return;
    ic.textContent = '';
    for (var i = 0; i < bindirMesajlar.length; i++) ic.appendChild(bindirSatir(bindirMesajlar[i]));
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — OYNATICI SAĞ TIK MENÜSÜ
   * Yalnız videonun ÜSTÜNDE devreye girer; sayfanın geri kalanında tarayıcının
   * kendi menüsü çalışmaya devam eder. Shift ile sağ tıklarsan yine tarayıcı
   * menüsü açılır (kaçış yolu) — kimseyi kendi menüsünden mahrum bırakmayalım.
   * ══════════════════════════════════════════════════════════════════════ */
  var SAGTIK_ID = 'pk-sagtik';
  function sagTikKapat() { var m = document.getElementById(SAGTIK_ID); if (m) m.remove(); }

  function sagTikOgeleri() {
    var o = [];
    if (settings.ekranGoruntusu !== false) o.push([t('pkEkranGoruntusuAl'), function () { ekranGoruntusuAl(); }]);
    if (settings.videoIstat !== false) o.push(['Video istatistikleri', function () { vistatAc(); }]);
    o.push([t('pkBindir') + (settings.bindir === true ? ' ✓' : ''), function () {
      var y = settings.bindir !== true;
      sendBg({ type: 'setSettings', payload: { bindir: y } }); settings.bindir = y;
      if (y) bindirKur(); else bindirKapat();
      toast(ozellikToast(y, 'pkBindir'));
    }]);
    o.push(['Tiyatro modu', function () {
      /* Şerit sökülmüşse uyandırıp bekle; yine de gelmezse sessiz kalma,
         söyle — eskiden hiçbir şey olmuyordu ve kullanıcı düğme bozuk
         sanıyordu. */
      oynaticiButonunaBas(/tiyatro|theat/, 'ViewTheatre', function (ok) {
        if (!ok) toast(t('pkTSeritYok'));
      });
    }]);
    o.push(['Tam ekran', function () {
      var b = oynaticiButonu(/tam ekran|fullscreen/, 'FullscreenExpand');
      if (b) { b.click(); return; }
      /* Kick kontrol şeridi gizliyken düğmeyi DOM'dan söküyor; o an menüden
         gelen istek boşa gidiyordu. Doğrudan API yedeği: kullanıcının kendi
         tıklamasının İÇİNDE çağrıldığı için etkinleştirme (user activation)
         geçerli — sonraya bırakılırsa tarayıcı reddeder. */
      try {
        if (document.fullscreenElement) { document.exitFullscreen(); return; }
        var v = oynaticiVideosu(); if (!v) return;
        var k = oynaticiKabi(v, v.getBoundingClientRect());
        if (k && k.requestFullscreen) k.requestFullscreen();
      } catch (e) {}
    }]);
    o.push([t('pkAyarlar'), function () { panelAc(); }]);
    return o;
  }

  function sagTikAc(x, y) {
    sagTikKapat();
    var m = el('div', 'z-dropdown flex h-fit min-w-fit flex-col rounded bg-surface-base text-sm shadow-lg ' +
      'will-change-[opacity,transform] animate-slideUpAndFade');
    m.id = SAGTIK_ID;
    m.style.cssText = 'position:fixed;z-index:2147483100;padding:6px;min-width:200px;' +
      'border:1px solid rgba(240,241,242,.16)';
    sagTikOgeleri().forEach(function (o) {
      var b = el('button', 'state-layer-surface flex w-full items-center rounded text-left text-white ' +
        'font-semibold text-sm', o[0]);
      b.style.cssText = 'padding:8px 10px;background:none;border:0;cursor:pointer';
      b.addEventListener('click', function (e) { e.stopPropagation(); sagTikKapat(); try { o[1](); } catch (er) {} });
      m.appendChild(b);
    });
    pkKatmanaEkle(m);
    var r = m.getBoundingClientRect();
    var sx = Math.min(x, innerWidth - r.width - 8), sy = Math.min(y, innerHeight - r.height - 8);
    m.style.left = Math.round(Math.max(8, sx)) + 'px';
    m.style.top = Math.round(Math.max(8, sy)) + 'px';
  }

  function sagTikKur() {
    if (window.__pkSagTik) return;
    window.__pkSagTik = true;
    document.addEventListener('contextmenu', function (e) {
      if (settings.sagTik !== true) return;
      if (e.shiftKey) return; // kaçış yolu: tarayıcı menüsü
      var v = oynaticiVideosu() || e.target.closest('video') || e.target.closest('[class*="thumbnail"], [class*="card"]');
      if (!v) return;
      var vr = v.getBoundingClientRect();
      if (e.clientX < vr.left || e.clientX > vr.right || e.clientY < vr.top || e.clientY > vr.bottom) return;
      e.preventDefault();
      sagTikAc(e.clientX, e.clientY);
    }, true);
    document.addEventListener('click', function () { sagTikKapat(); }, true);
    document.addEventListener('keydown', function (e) { if (e.key === 'Escape') sagTikKapat(); }, true);
  }


  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — AYAR YÖNETİMİ (dışa aktar · içe aktar · sıfırla)
   *
   * Tek bir JSON dosyası: ayarlar + tüm yerel listeler (notlar, gizlenenler,
   * kayıt noktaları, filtreler…). Sunucuya hiçbir şey gitmez, dosya kullanıcının
   * diskinde kalır. Cihaz değiştirirken ya da tarayıcı sıfırlarken her şeyi
   * elle kurmak zorunda kalmasın diye var.
   *
   * İçe aktarmada iki yol: BİRLEŞTİR (yalnız dosyadaki anahtarlar yazılır,
   * gerisi kalır) ve SIFIRDAN (mevcut her şey silinip dosya uygulanır).
   * ══════════════════════════════════════════════════════════════════════ */
  var YEDEK_SURUM = 1;
  /* Yedeğe giren yerel anahtarlar. Buraya eklenmeyen bir şey yedeklenmez —
     yeni bir liste eklenirse BURAYA DA eklenmeli.

     BİLEREK DIŞARIDA:
       pkLink   — giriş jetonu; dışa aktarılan dosya paylaşılabiliyor,
                  kimlik bilgisi içine koymuyoruz
       badges · drops · seenDrops · hbLast — sunucudan yeniden gelen önbellek
       stats    — sayaçlar, tercih değil
       pkYeni   — yenilik noktalarının okunma durumu, cihaza özel */
  var YEDEK_ANAHTAR = ['settings', 'theme', 'blockedChannels', 'playbackRate',
                       'pkNotlar', 'pkVodNokta', 'pkGizliKat',
                       'pkGizliKisi', 'pkOncelikli', 'pkRolRenk', 'pkVurguMuaf',
                       'pkGizliMesaj', 'pkRolKapali', 'pkSabitKanal', 'pkKapaliAcik', 'pkHakkindaAcik', 'pkSes',
                       'pkBanSebep'];

  function yedekTopla(cb) {
    try {
      chrome.storage.local.get(YEDEK_ANAHTAR, function (res) {
        var v = { pk: 'purekick', surum: YEDEK_SURUM, tarih: new Date().toISOString(), veri: {} };
        try { v.eklenti = chrome.runtime.getManifest().version; } catch (e) {}
        YEDEK_ANAHTAR.forEach(function (k) { if (res && res[k] !== undefined) v.veri[k] = res[k]; });
        cb(v);
      });
    } catch (e) { cb(null); }
  }

  function yedekIndir() {
    yedekTopla(function (v) {
      if (!v) { toast(t('pkTYedekAlinamadi')); return; }
      try {
        var b = new Blob([JSON.stringify(v, null, 2)], { type: 'application/json' });
        var url = URL.createObjectURL(b);
        var a = document.createElement('a');
        var d = new Date();
        function ik(n) { return (n < 10 ? '0' : '') + n; }
        a.href = url;
        a.download = 'purekick-ayarlar-' + d.getFullYear() + ik(d.getMonth() + 1) + ik(d.getDate()) + '.json';
        document.body.appendChild(a); a.click(); a.remove();
        setTimeout(function () { URL.revokeObjectURL(url); }, 4000);
        toast(t('pkTAyarlarIndirildi'));
      } catch (e) { toast(t('pkTYedekAlinamadi')); }
    });
  }

  /* Dosyayı doğrular. Yalnız BİZİM ürettiğimiz biçimi kabul eder — rastgele bir
     JSON'u uygulamak ayarları çöple doldururdu. */
  function yedekDogrula(metin) {
    var j;
    try { j = JSON.parse(metin); } catch (e) { return { hata: 'pkYedekBozuk' }; }
    if (!j || typeof j !== 'object' || j.pk !== 'purekick' || !j.veri || typeof j.veri !== 'object') {
      return { hata: 'pkYedekYabanci' };
    }
    var kabul = {}, sayi = 0;
    YEDEK_ANAHTAR.forEach(function (k) { if (j.veri[k] !== undefined) { kabul[k] = j.veri[k]; sayi++; } });
    if (!sayi) return { hata: 'pkYedekBos' };
    return { veri: kabul, sayi: sayi, tarih: j.tarih || '', eklenti: j.eklenti || '' };
  }

  function yedekUygula(kabul, sifirdan, cb) {
    try {
      if (!sifirdan) {
        chrome.storage.local.set(kabul, function () { cb(true); });
        return;
      }
      chrome.storage.local.remove(YEDEK_ANAHTAR, function () {
        chrome.storage.local.set(kabul, function () { cb(true); });
      });
    } catch (e) { cb(false); }
  }

  /* SIFIRLAMA SONRASI HEDEF: yalnız reklam engelleyici açık.
     DEFAULTS üzerinden hesaplanıyor (mevcut ayarlardan DEĞİL) — yoksa
     sıfırlama kullanıcının eski kalite/yazı boyu gibi tercihlerini taşırdı.
     Sayı/metin ayarları varsayılanına döner, boole özellikler kapanır,
     reklam engelleme ve onay açık kalır. */
  function sifirlamaAyari() {
    var y = {};
    Object.keys(DEFAULTS).forEach(function (k) {
      if (k === 'consented') { y[k] = true; return; }
      if (PK_REKLAM_AYARI.indexOf(k) >= 0) { y[k] = true; return; }
      y[k] = (typeof DEFAULTS[k] === 'boolean') ? false : DEFAULTS[k];
    });
    return y;
  }

  function hersiSifirla(cb) {
    try {
      chrome.storage.local.remove(YEDEK_ANAHTAR, function () {
        /* SİLMEK YETMEZ: kayıt yokken arka plan DEFAULTS'a düşer ve bütün
           özellikler yeniden AÇILIR — sıfırlama tam ters sonuç verirdi.
           Hedef durumu açıkça yazıyoruz. */
        sendBg({ type: 'setSettings', payload: sifirlamaAyari() })
          .then(function () { cb(true); }, function () { cb(true); });
      });
    } catch (e) { cb(false); }
  }


  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — MESAJ ÜSTÜ HIZLI EYLEMLER
   *
   * Bir sohbet balonunun üstüne gelince sağ üstte küçük bir şerit çıkar:
   * kopyala · bahset · saati göster · gizle · daralt.
   *
   * Şerit her mesaja BASILMAZ — Kick sohbeti sanallaştırıyor, her satıra düğme
   * eklemek yüzlerce düğüm demek. Tek bir şerit var, imleç hangi balonun
   * üstündeyse oraya konumlanıyor (kanal gizlemedeki ✕ düğmesiyle aynı yaklaşım).
   * ══════════════════════════════════════════════════════════════════════ */
  var MEYLEM_ID = 'pk-meylem';
  var meylemHedef = null;
  var gizliMesaj = Object.create(null);      // mesaj anahtarı -> 1
  var darMesaj = Object.create(null);

  /* Mesajı kimlikleyen anahtar: kim + metnin ilk 60 karakteri. data-index
     KULLANILMAZ — Kick tamponu kırpınca indeksler kayıyor. */
  function mesajAnahtar(m) {
    var ub = usernameButton(m); if (!ub) return '';
    var ad = (ub.textContent || '').trim().toLowerCase();
    var mt = (m.textContent || '').replace(/\s+/g, ' ').trim().slice(0, 60);
    return ad ? (ad + '|' + mt) : '';
  }

  function mesajMetniAl(m) {
    var pr = scParcala(m), o = '';
    for (var i = 0; i < pr.length; i++) o += (pr[i].t === 'y') ? pr[i].v : (pr[i].a ? (' ' + pr[i].a + ' ') : '');
    return o.replace(/\s+/g, ' ').trim();
  }

  function meylemKapat() {
    var e = document.getElementById(MEYLEM_ID); if (e) e.remove();
    meylemHedef = null;
  }

  function meylemDugme(baslik, ikon, tikla) {
    var b = el('button', 'state-layer-surface inline-flex items-center justify-center rounded text-white shrink-0');
    b.style.cssText = 'width:24px;height:24px;padding:0;background:none;border:0;cursor:pointer';
    b.title = baslik;
    b.innerHTML = ikon;
    b.addEventListener('click', function (e) { e.preventDefault(); e.stopPropagation(); tikla(); });
    return b;
  }

  function meylemAc(m) {
    if (meylemHedef === m && document.getElementById(MEYLEM_ID)) return;
    meylemKapat();
    var anah = mesajAnahtar(m); if (!anah) return;
    var mr = m.getBoundingClientRect();
    if (mr.width < 60 || mr.height < 8) return;
    meylemHedef = m;

    var kutu = el('div', 'bg-surface-base rounded shadow-lg flex items-center gap-0.5');
    kutu.id = MEYLEM_ID;
    kutu.style.cssText = 'position:fixed;z-index:2147483070;padding:2px;' +
      'border:1px solid rgba(240,241,242,.16)';

    kutu.appendChild(meylemDugme(t('pkMKopyala'), I_KOPYA, function () {
      var metin = mesajMetniAl(m);
      if (!metin) { toast(t('pkTKopyalanamadi')); return; }
      panoyaYaz(metin, function (ok) { toast(t(ok ? 'pkTKopyalandi' : 'pkTKopyalanamadi')); });
    }));

    kutu.appendChild(meylemDugme(t('pkMBahset'), I_BAHSET, function () {
      var ub = usernameButton(m);
      var ad = ub ? (ub.textContent || '').trim() : '';
      if (!ad) return;
      sohbeteYaz('@' + ad + ' ');
      meylemKapat();
    }));

    kutu.appendChild(meylemDugme(darMesaj[anah] ? t('pkMGenislet') : t('pkMDaralt'), I_DARALT, function () {
      if (darMesaj[anah]) delete darMesaj[anah]; else darMesaj[anah] = 1;
      mesajDurumUygula(); meylemKapat();
    }));

    kutu.appendChild(meylemDugme(t('pkMGizle'), I_GOZ, function () {
      gizliMesaj[anah] = 1;
      /* KALICI VE SINIRSIZDI: her gizlemede depoya bir kayıt daha ekleniyor,
         hiç temizlenmiyordu. En eskiler düşsün. */
      var gm = Object.keys(gizliMesaj);
      if (gm.length > GIZLI_MESAJ_SINIR) {
        for (var gx = 0; gx < gm.length - GIZLI_MESAJ_SINIR; gx++) delete gizliMesaj[gm[gx]];
      }
      try { chrome.storage.local.set({ pkGizliMesaj: Object.keys(gizliMesaj) }); } catch (e) {}
      mesajDurumUygula(); meylemKapat();
    }));

    pkKatmanaEkle(kutu);
    var kr = kutu.getBoundingClientRect();
    var x = mr.right - kr.width - 4, y = mr.top - kr.height + 2;
    if (y < 8) y = mr.top + 2;
    if (x < 8) x = 8;
    kutu.style.left = Math.round(x) + 'px';
    kutu.style.top = Math.round(y) + 'px';
  }

  /* Gizlenen / daraltılan / saatli mesajları her turda uygular. */
  function mesajDurumUygula() {
    if (!settings.consented || settings.mesajEylem === false) return;
    var msgs = sohbetSatirlari();
    for (var i = 0; i < msgs.length; i++) {
      var m = msgs[i], anah = mesajAnahtar(m);
      if (!anah) continue;

      // gizleme — yerine "gizlendi" satırı
      if (gizliMesaj[anah]) {
        if (m.getAttribute('data-pk-mgizli') !== '1') {
          m.setAttribute('data-pk-mgizli', '1');
          m.style.setProperty('opacity', '.35', 'important');
          m.style.setProperty('filter', 'blur(3px)', 'important');
          m.title = t('pkMesajGizli');
        }
      } else if (m.getAttribute('data-pk-mgizli')) {
        m.removeAttribute('data-pk-mgizli');
        m.style.removeProperty('opacity'); m.style.removeProperty('filter');
        m.removeAttribute('title');
      }

      // daraltma — tek satıra indir
      if (darMesaj[anah]) {
        if (m.getAttribute('data-pk-mdar') !== '1') {
          m.setAttribute('data-pk-mdar', '1');
          m.style.setProperty('max-height', '22px', 'important');
          m.style.setProperty('overflow', 'hidden', 'important');
        }
      } else if (m.getAttribute('data-pk-mdar')) {
        m.removeAttribute('data-pk-mdar');
        m.style.removeProperty('max-height'); m.style.removeProperty('overflow');
      }

    }
  }

  /* Pano yazımı: modern API kısıtlıysa (odak yoksa, izin yoksa) gizli bir
     textarea + execCommand ile yedek yol. İkisi de tutmazsa false döner. */
  function panoyaYaz(metin, cb) {
    try {
      if (navigator.clipboard && navigator.clipboard.writeText) {
        navigator.clipboard.writeText(metin).then(function () { cb(true); }, function () { panoYedek(metin, cb); });
        return;
      }
    } catch (e) {}
    panoYedek(metin, cb);
  }
  function panoYedek(metin, cb) {
    try {
      var ta = document.createElement('textarea');
      ta.value = metin;
      ta.style.cssText = 'position:fixed;left:-9999px;top:0;opacity:0';
      document.body.appendChild(ta);
      ta.select();
      var ok = document.execCommand('copy');
      ta.remove();
      cb(!!ok);
    } catch (e) { cb(false); }
  }

  /* Sohbet kutusuna metin yazar (React'in kendi girdisine). */
  function sohbeteYaz(metin) {
    var yuva = araYuva();
    var kutu = null;
    if (yuva) {
      var kap = yuva.parentElement;
      for (var d = 0; kap && d < 4; kap = kap.parentElement, d++) {
        kutu = kap.querySelector('[contenteditable="true"], textarea, input[type="text"]');
        if (kutu) break;
      }
    }
    if (!kutu) kutu = document.querySelector('[contenteditable="true"]');
    if (!kutu) { toast(t('pkTSohbetKutusuYok')); return; }
    try {
      kutu.focus();
      if (kutu.isContentEditable) {
        document.execCommand('insertText', false, metin);
      } else {
        var d2 = Object.getOwnPropertyDescriptor(Object.getPrototypeOf(kutu), 'value');
        if (d2 && d2.set) d2.set.call(kutu, (kutu.value || '') + metin);
        else kutu.value = (kutu.value || '') + metin;
        kutu.dispatchEvent(new Event('input', { bubbles: true }));
      }
    } catch (e) {}
  }

  /* ---------- Tıklanabilir @bahsetme ----------
     Mesaj metnindeki @ad parçasını tıklanabilir yapar; tıklayınca o kişinin
     profil kartını açmak yerine adı sohbet kutusuna yazar (kart Kick'in kendi
     davranışı, biz onu taklit etmiyoruz). */
  function bahsetTiklanabilir() {
    if (!settings.consented || settings.bahsetTikla === false) return;
    var msgs = sohbetSatirlari();
    for (var i = 0; i < msgs.length; i++) {
      var m = msgs[i];
      if (m.getAttribute('data-pk-btik') === '1') continue;
      m.setAttribute('data-pk-btik', '1');
      var dugumler = [], yr = document.createTreeWalker(m, NodeFilter.SHOW_TEXT, null);
      var d;
      while ((d = yr.nextNode())) { if (/@[A-Za-z0-9_]{2,25}/.test(d.nodeValue || '')) dugumler.push(d); }
      for (var k = 0; k < dugumler.length; k++) bahsetSar(dugumler[k]);
    }
  }
  function bahsetSar(dugum) {
    var metin = dugum.nodeValue || '';
    if (dugum.parentElement && dugum.parentElement.classList &&
        dugum.parentElement.classList.contains('pk-bahset')) return;
    var parca = document.createDocumentFragment();
    var re = /@([A-Za-z0-9_]{2,25})/g, son = 0, m;
    while ((m = re.exec(metin))) {
      if (m.index > son) parca.appendChild(document.createTextNode(metin.slice(son, m.index)));
      var sp = document.createElement('span');
      sp.className = 'pk-bahset';
      sp.textContent = m[0];
      sp.style.cssText = 'color:rgb(83,252,24);font-weight:700;cursor:pointer';
      (function (ad) {
        sp.addEventListener('click', function (e) { e.preventDefault(); e.stopPropagation(); sohbeteYaz('@' + ad + ' '); });
      })(m[1]);
      parca.appendChild(sp);
      son = m.index + m[0].length;
    }
    if (son < metin.length) parca.appendChild(document.createTextNode(metin.slice(son)));
    if (dugum.parentNode) dugum.parentNode.replaceChild(parca, dugum);
  }

  /* ---------- Sohbet yazı boyutu ---------- */
  /* SOHBET YAZI BOYUTU KALDIRILDI (kullanıcı kararı). Sayfaya `#pk-yazi-boyu`
     stili yazıp `[data-index]` balonlarını ölçekliyordu. Tarayıcının kendi
     yakınlaştırması aynı işi zaten görüyor; ayarlarda yer kaplıyordu.
     `settings.yaziBoyu` eski kullanıcıların kaydında kalabilir — kimse
     okumadığı için zararsız, göç yazmaya değmez. */

  /* ---------- Rol renkleri: kaydet / sıfırla ---------- */
  function rolRenkKaydet() {
    try { chrome.storage.local.set({ pkRolRenk: rolRenk }); } catch (e) {}
  }
  function rolRenkAta(r, renk) {
    if (!ROL_VARSAYILAN[r]) return;
    if (!renk || renk.toLowerCase() === String(ROL_VARSAYILAN[r]).toLowerCase()) delete rolRenk[r];
    else rolRenk[r] = renk;
    rolRenkKaydet(); kisiUygula();
  }
  function rolRenkSifirla() { rolRenk = Object.create(null); rolRenkKaydet(); kisiUygula(); }

  function muafKaydet() {
    try { chrome.storage.local.set({ pkVurguMuaf: Object.keys(vurguMuaf) }); } catch (e) {}
  }
  function muafEkle(ad) {
    var k = String(ad || '').trim().toLowerCase(); if (!k) return false;
    if (vurguMuaf[k]) return false;
    vurguMuaf[k] = 1; muafKaydet(); kisiUygula(); return true;
  }
  function muafCikar(ad) {
    var k = String(ad || '').toLowerCase();
    delete vurguMuaf[k]; muafKaydet(); kisiUygula();
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — SOHBET BAĞLANTI DURUMU
   * Moderasyon günlüğü ve bindirme Kick'in Pusher bağlantısını dinliyor.
   * Bağlantı kopunca o özellikler sessizce çalışmaz hâle geliyor ve kullanıcı
   * bunu anlamıyordu. Sohbet başlığındaki küçük nokta durumu gösteriyor.
   * ══════════════════════════════════════════════════════════════════════ */
  var sonOlayZamani = 0;
  function baglantiDurumu() {
    if (!sonOlayZamani) return 'bekliyor';
    var f = Date.now() - sonOlayZamani;
    if (f < 90000) return 'acik';              // 90 sn içinde olay geldi
    if (f < 300000) return 'sessiz';           // sohbet sakin olabilir
    return 'kopuk';
  }
  var BAGLANTI_RENK = { acik: '#53fc18', sessiz: '#e6a800', kopuk: '#fa2d2d', bekliyor: '#8b969d' };

  /* "Sohbet" başlık yazısı — sohbet sütunundaki mutlak konumlu, ortalanmış
     yaprak span. Metne BAKMIYORUZ (7 dil), konumlanışına bakıyoruz. */
  function sohbetBaslikYazisi() {
    var sut = pkIlkGorunur('#channel-chatroom'); if (!sut) return null;
    var l = sut.querySelectorAll('span');
    for (var i = 0; i < l.length; i++) {
      var e = l[i];
      if (!pkGorunur(e)) continue;
      if (!(e.textContent || '').trim()) continue;
      /* Kendi eklediğimiz düğüm dışında çocuğu olmamalı — yaprak başlık. */
      var c = 0;
      for (var q = 0; q < e.children.length; q++) {
        if (!e.children[q].hasAttribute('data-pk-baglanti')) c++;
      }
      if (c) continue;
      var cs; try { cs = getComputedStyle(e); } catch (x) { continue; }
      if (cs.position !== 'absolute') continue;
      return e;
    }
    return null;
  }

  function baglantiNoktasiKur() {
    if (!settings.consented || settings.baglantiDurum !== true) {
      var e0 = document.querySelectorAll('[data-pk-baglanti]');
      for (var q = 0; q < e0.length; q++) e0[q].remove();
      return;
    }
    /* Yuva: üstteki "Sohbet" başlığı. Bulunamazsa eski yerine (gönder
       satırının solu) düşüyoruz — bir sürüm sonra başlık değişse bile
       gösterge kaybolmasın. */
    var yuva = sohbetBaslikYazisi(), basliktaMi = true;
    if (!yuva) { yuva = araYuva(); basliktaMi = false; }
    if (!yuva) return;
    var n = yuva.querySelector(':scope > [data-pk-baglanti]');
    if (!n) {
      /* Başka bir yuvada kalmış eski nokta varsa taşı, ikinci kopya olmasın. */
      var eski = document.querySelectorAll('[data-pk-baglanti]');
      for (var w = 0; w < eski.length; w++) eski[w].remove();
      n = el('span', 'shrink-0');
      n.setAttribute('data-pk-baglanti', '1');
      n.style.cssText = basliktaMi
        ? 'width:8px;height:8px;border-radius:999px;display:inline-block;margin-left:6px;vertical-align:middle'
        : 'width:8px;height:8px;border-radius:999px;align-self:center;margin:0 4px';
      if (basliktaMi) yuva.appendChild(n);
      else yuva.insertBefore(n, yuva.firstChild);
    }
    var d = baglantiDurumu();
    if (n.getAttribute('data-pk-durum') !== d) {
      n.setAttribute('data-pk-durum', d);
      n.style.background = BAGLANTI_RENK[d];
      n.title = t('pkBaglanti_' + d);
    }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — SOHBET TEMİZLENMESİNİ ENGELLE
   * Moderatör "sohbeti temizle" dediğinde Kick tüm balonları siliyor.
   * Bu ayar açıkken biz DOM'a dokunmuyoruz — Kick zaten kendi listesini
   * boşaltıyor, engelleyemeyiz — ama temizlemeden HEMEN ÖNCEKİ son mesajları
   * saklayıp sohbetin üstüne bir "kurtarılan mesajlar" kutusu koyuyoruz.
   * Böylece ne yazıldığını okumaya devam edebiliyorsun.
   * ══════════════════════════════════════════════════════════════════════ */
  var KURTAR_ID = 'pk-kurtarilan';
  var kurtarilan = [];

  function kurtarmaKap() {
    // Kick'in kendi sohbet sutunu; bulunamazsa eski tirmanma yolu.
    return sohbetSutunu();
  }

  function sohbetTemizlendi() {
    if (settings.temizlemeKoru !== true) return;
    /* Ekrandaki son 40 mesajı metin olarak al — DOM düğümlerini tutmak
       React'in geri dönüştürdüğü düğümleri tutmak demek olurdu. */
    var msgs = sohbetSatirlari();
    var bas = Math.max(0, msgs.length - 40);
    kurtarilan = [];
    for (var i = bas; i < msgs.length; i++) {
      var ub = usernameButton(msgs[i]); if (!ub) continue;
      var ad = (ub.textContent || '').trim(); if (!ad) continue;
      kurtarilan.push({ kim: ad, rol: kisiRol[ad.toLowerCase()] || '', metin: mesajMetniAl(msgs[i]) });
    }
    if (kurtarilan.length) kurtarmaCiz();
  }

  function kurtarmaKapat() { var e = document.getElementById(KURTAR_ID); if (e) e.remove(); }

  function kurtarmaCiz() {
    kurtarmaKapat();
    var sut = kurtarmaKap(); if (!sut) return;
    var k = el('div', 'bg-surface-base border-outline-decorative rounded border flex flex-col gap-1');
    k.id = KURTAR_ID;
    k.style.cssText = 'margin:6px;padding:8px;max-height:200px;overflow-y:auto';
    var bas = el('div', 'flex items-center justify-between gap-2 shrink-0');
    bas.appendChild(el('div', 'text-xs font-bold text-white', t('pkKurtarilanBaslik', [String(kurtarilan.length)])));
    var kb = el('button', 'state-layer-surface inline-flex items-center justify-center rounded text-white shrink-0');
    kb.style.cssText = 'width:20px;height:20px;padding:0;background:none;border:0;cursor:pointer';
    kb.innerHTML = I_KAPAT;
    kb.addEventListener('click', function () { kurtarmaKapat(); });
    bas.appendChild(kb);
    k.appendChild(bas);
    kurtarilan.forEach(function (m) {
      var r = el('div', 'text-xs');
      r.style.cssText = 'word-break:break-word';
      var ad = el('span', 'font-bold', m.kim + ': ');
      ad.style.color = m.rol ? rolRengi(m.rol) : '#ffffff';
      r.appendChild(ad);
      r.appendChild(document.createTextNode(m.metin));
      k.appendChild(r);
    });
    sut.insertBefore(k, sut.firstChild);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — CANLIYA ATLA
   * Canlı yayında geri sarınca ya da tamponlama olunca canlı kenardan
   * kopuyorsun. Kick bunu geri almak için bir düğme koymuyor; bu düğme
   * doğrudan tamponun sonuna atlar.
   * ══════════════════════════════════════════════════════════════════════ */

  /* Canlıya uzaklık — "gelişmiş canlı göstergesi" için. */
  function canliUzaklik() {
    var v = oynaticiVideosu(); if (!v || vodSayfasi()) return -1;
    var b = v.buffered;
    if (!b || !b.length) return -1;
    return Math.max(0, b.end(b.length - 1) - v.currentTime);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — UYARLANABİLİR HIZ
   * Canlı kenardan geride kaldıysan oynatma hızını artırıp (1.25x) farkı
   * kapatır, kenara varınca 1x'e döner. Sıçrama yapmaz —
   * currentTime'a dokunmak görüntüyü zıplatıyor, hız değişimi fark edilmiyor.
   *
   * Eşikler: 6 sn üstü gecikmede hızlan, 2 sn altında normale dön. Aradaki
   * boşluk BİLEREK geniş — dar olsaydı hız sürekli açılıp kapanırdı.
   *
   * 1.05 pratikte yakalamıyordu: 6 sn'lik farkı kapatması 120 sn sürüyordu,
   * o sürede yayın yeniden geride kalıyordu. 1.25'te aynı fark 24 sn'de
   * kapanıyor.
   * ══════════════════════════════════════════════════════════════════════ */
  var UYAR_HIZ = 1.25;          // yakalama hızı
  var uyarHizAcik = false;
  function uyarlanabilirHiz() {
    if (!settings.consented || settings.uyarHiz !== true || vodSayfasi()) {
      if (uyarHizAcik) { uyarHizAcik = false; var v0 = oynaticiVideosu(); if (v0) try { v0.playbackRate = 1; } catch (e) {} }
      return;
    }
    var v = oynaticiVideosu(); if (!v || v.paused) return;
    var u = canliUzaklik(); if (u < 0) return;
    if (!uyarHizAcik && u > 6) {
      uyarHizAcik = true;
      try { v.playbackRate = UYAR_HIZ; } catch (e) {}
    } else if (uyarHizAcik && u < 2) {
      uyarHizAcik = false;
      try { v.playbackRate = 1; } catch (e) {}
    } else if (uyarHizAcik && v.playbackRate !== UYAR_HIZ) {
      /* TAZELEME. Kick atlama/tamponlama anında hızı 1'e çekiyor (ölçülmüş
         davranış). Eski kod hızı yalnız eşik geçilirken yazdığı için o
         andan sonra bayrak açık kalıyor ama gerçek hız 1 oluyordu —
         özellik sessizce duruyordu. */
      try { v.playbackRate = UYAR_HIZ; } catch (e) {}
    }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — TAM EKRAN DÜĞMESİNİ EN SONA AL
   * Kick tam ekran düğmesini şeridin sonuna koymuyor; bizim eklediğimiz
   * düğmelerle birlikte sağ uç karışıyor. Bu ayar açıkken tam ekran düğmesi
   * en sağa taşınır — Kick'in düğümü DOM'da yer değiştirir, kopyalanmaz.
   * ══════════════════════════════════════════════════════════════════════ */
  function tamEkranSona() {
    if (!settings.consented) return;
    var b = oynaticiButonu(/tam ekran|fullscreen|vollbild|pantalla completa|plein écran|tela cheia|полн/, 'FullscreenExpand');
    if (!b) return;
    /* DOM AGACINA DOKUNMUYORUZ — CSS `order` ile kaydiriyoruz.
       Eskiden Kick'in KENDI dugumu baska bir siraya aliniyordu. React o
       dugumun yerini kendi tutuyor; disaridan tasimak
       "removeChild: The node to be removed is not a child of this node"
       cokmesinin bilinen sebebi. Serit zaten flex (canlida olculdu). */
    if (settings.tamEkranSon === true) b.style.setProperty('order', '999');
    else b.style.removeProperty('order');                  // ayar kapandiysa geri al
  }


  /* Bir yazida kelimenin TAM olarak gecip gecmedigi. Türkçe karakterler  ile
     güvenilir çalışmadığı için sınır elle kontrol edilir: "ali" araması
     "kalitesiz" içinde eşleşmez. */
  /* TÜRKÇE KÜÇÜLTME. `String.toLowerCase()` Türkçe'de iki yerde yanlış sonuç
     veriyor ve ikisi de sohbette çok yaygın:
       · 'İ' (U+0130) → 'i' + BİRLEŞEN NOKTA (U+0307). Yani
         "ÇEKİLİŞ".toLowerCase() ile "çekiliş" EŞLEŞMİYOR.
       · 'ı' büyütülünce 'I' oluyor, 'I' küçültülünce 'i'. Büyük harfle
         yazan kullanıcıda ı/i ayrımı zaten kayboluyor.
     Sonuç: büyük harfle yazılmış bir mesajda ne adın ne de takip ettiğin
     kelime yakalanıyordu — hatasız, sessizce. Birleşen noktayı atıp 'ı'yı
     'i'ye katlıyoruz. Eşleşme GENİŞLİYOR, daralmıyor: eskiden bulunan her
     şey yine bulunuyor. */
  function pkKucult(s) {
    return String(s || '').toLowerCase().replace(/̇/g, '').replace(/ı/g, 'i');
  }

  function kelimeGecti(yazi, kelime) {
    var y = pkKucult(yazi), a = pkKucult(kelime);
    if (!y || !a) return false;
    var p = -1;
    while ((p = y.indexOf(a, p + 1)) >= 0) {
      var onc = p > 0 ? y.charAt(p - 1) : ' ';
      var son = y.charAt(p + a.length) || ' ';
      if (!/[a-z0-9_çğıöşü]/.test(onc) && !/[a-z0-9_çğıöşü]/.test(son)) return true;
    }
    return false;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — PROFİL KARTINI SABİTLEME
   *
   * Kick'in kartı ilk tıklamada kapanıyor: birinin geçmişini okurken sohbete
   * bir şey yazmak istesen kart kayboluyor. Sabitleyince kartın bir KOPYASINI
   * ayrı bir katmana alıyoruz — Kick'in kendi düğümünü taşımıyoruz, çünkü
  /* ══════════════════════════════════════════════════════════════════════
   * KAPATMA (✕) DÜĞMESİNİ SAĞ KENARA OTURT
   *
   * Kartı genişlettikten sonra ✕ ile sağ kenar arasında boşluk kalıyordu.
   *
   * ✕ KARTIN İÇİNDE DEĞİL — canlıda ölçüldü: kartın üst 60px'inde hiç düğme
   * yok ve kartın kendi konumu `static`. Kick kapatma düğmesini kartın
   * DIŞINA, konumlandırılmış kabın içine koyuyor. Bu yüzden arama kaptan
   * başlıyor, eleme kartın ölçüsüne göre yapılıyor.
   *
   * DOM'DA TAŞIMIYORUZ: React kendi düğümünün yerini tutuyor, dışarıdan
   * taşımak `removeChild` çökmesinin bilinen sebebi. Yalnız satır içi `right`
   * değeri kaydırılıyor — mevcut değer üzerinden FARK alınarak, ki kabın
   * hangi öğeye göre konumlandığını bilmemize gerek kalmasın.
   * ══════════════════════════════════════════════════════════════════════ */
  var KAPAT_IM = 'data-pk-kapat-hiza';

  function kartKapatDugmesi(kart) {
    var cr = kart.getBoundingClientRect();
    if (!cr.width) return null;
    var kap = kart;
    try { kap = tasinacakKap(kart) || kart; } catch (e) {}
    if (kap === kart && kart.parentElement) kap = kart.parentElement;
    var btn = kap.querySelectorAll('button');
    for (var i = 0; i < btn.length; i++) {
      var b = btn[i];
      /* Simge düğmesi: ya svg taşıyor ya da tek karakterlik kapatma imi. */
      var yazi = (b.textContent || '').trim();
      if (!b.querySelector('svg') && yazi.length > 2) continue;
      var r = b.getBoundingClientRect();
      if (!r.width || r.width > 60) continue;
      var dy = r.top - cr.top;
      if (dy > 48 || dy < -48) continue;              // üst hizada (kart dışı olabilir)
      if (cr.right - r.right > 80) continue;          // sağ kenara yakın
      return b;
    }
    return null;
  }

  function kapatHizala(kart) {
    if (!kart || settings.kartGenis === false) return;
    var kapat = kartKapatDugmesi(kart);
    if (!kapat || kapat.getAttribute(KAPAT_IM)) return;   // bir kez yeter
    var konum = '';
    try { konum = getComputedStyle(kapat).position; } catch (e) {}
    if (konum !== 'absolute' && konum !== 'fixed') return;
    try {
      var cr = kart.getBoundingClientRect(), kr = kapat.getBoundingClientRect();
      var bosluk = cr.right - kr.right;
      var mevcut = parseFloat(getComputedStyle(kapat).right);
      if (!isFinite(mevcut) || bosluk <= 12) return;
      kapat.setAttribute(KAPAT_IM, '1');
      kapat.style.setProperty('right', Math.max(0, mevcut - (bosluk - 10)) + 'px');
    } catch (e) {}
  }
  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — KARTA GELEN YANITLAR
   * "Son Mesajlar" o kişinin YAZDIKLARINI gösteriyor. Bu blok tersini
   * gösteriyor: sohbette ONA yanıt verilen ya da ondan bahsedilen mesajlar.
   * Veri yine yerel toplayıcıdan; ek istek yok.
   * ══════════════════════════════════════════════════════════════════════ */
  function kisiyeYanitlar(ad) {
    var k = String(ad || '').toLowerCase();
    if (!k) return [];
    var o = [], adlar = Object.keys(scListe);
    for (var i = 0; i < adlar.length; i++) {
      var v = scListe[adlar[i]];
      if (!v || !v.mesajlar) continue;
      for (var j = 0; j < v.mesajlar.length; j++) {
        var m = v.mesajlar[j];
        var duz = '';
        for (var p = 0; p < m.p.length; p++) duz += (m.p[p].t === 'y') ? m.p[p].v : ' ';
        if (kelimeGecti(duz, k)) o.push({ kim: v.ad, p: m.p, ts: m.ts });
      }
    }
    o.sort(function (a, b) { return b.ts - a.ts; });
    return o.slice(0, 15);
  }

  /* Kartın alt bölümü: iki sekme + tek gövde. Aynı kap iki çizici tarafından
     paylaşılıyor, böylece bloklar alt alta yığılmıyor. */
  var pkKartSekme = 'mesaj';        // 'mesaj' | 'yanit'
  /* Gövde açık mı. KAPALI BAŞLIYOR (kullanıcı isteği): kart açılır açılmaz
     mesaj listesi basılınca kart uzuyor ve profilin geri kalanı aşağı
     kayıyordu. Her YENİ kartta kapalıya dönüyor (bkz. pkKartSekmeKabi). */
  var pkKartAcik = false;

  function pkKartSekmeKabi(kart) {
    var kap = kart.querySelector(':scope > .pk-sekme-kap');
    if (kap) return kap;
    /* YENİ KART = KAPALI. Değişken modül düzeyinde; sıfırlamazsak bir kartta
       açan kullanıcı sonraki her kartı açık görürdü. */
    pkKartAcik = false;
    kap = el('div', 'pk-sekme-kap flex flex-col gap-2');
    /* Şeridin altında ince ayraç — Kick'in kendi sekme şeridindeki çizginin
       aynısı; sekme alt çizgisi onun üstünde duruyor. `gap` KALDIRILDI:
       altı çizili sekmelerde araya boşluk koymak çizgiyi bölüyor. */
    var bar = el('div', 'pk-sekme-bar flex');
    bar.style.cssText = 'border-bottom:1px solid rgba(255,255,255,.10)';
    kap.appendChild(bar);
    kap.appendChild(el('div', 'pk-sekme-govde flex flex-col gap-2'));
    kart.appendChild(kap);
    return kap;
  }

  /* Sekme düğmesi — Kick'in kendi düğme görünümüyle. */
  function pkSekmeDugmesi(bar, anahtar, metin, tikla) {
    var eski = bar.querySelector('[data-pk-sekme="' + anahtar + '"]');
    if (eski) { pkSekmeBoya(eski, pkKartSekme === anahtar); return eski; }
    var b = el('button', 'flex-1 min-w-0 h-8 rounded text-xs font-semibold transition-all ' +
      'flex items-center justify-center whitespace-nowrap', metin);
    b.setAttribute('data-pk-sekme', anahtar);
    b.style.cssText = 'border:0;cursor:pointer';
    pkSekmeBoya(b, pkKartSekme === anahtar);
    b.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      /* Seçili sekmeye tekrar basmak KAPATIR; başka sekmeye basmak ona geçip
         AÇAR. Akordeon davranışı — kullanıcı isteği. */
      if (pkKartSekme === anahtar) pkKartAcik = !pkKartAcik;
      else { pkKartSekme = anahtar; pkKartAcik = true; }
      try { tikla(); } catch (er) {}
    });
    bar.appendChild(b);
    return b;
  }
  function pkSekmeBoya(b, aktif) {
    /* KICK'İN KENDİ SEKME GÖRÜNÜMÜ: dolgulu düğme değil, altı çizili başlık
       (sohbetteki "Mesajlar / Yanıtlar" şeridiyle aynı dil).
       Çizgi YALNIZ GÖVDE AÇIKKEN: kapalıyken hiçbir sekme vurgulu değil,
       böylece "şu an bir şey açık değil" tek bakışta anlaşılıyor. */
    var secili = aktif && pkKartAcik;
    b.className = 'flex-1 min-w-0 h-9 text-sm font-semibold transition-all ' +
      'flex items-center justify-center whitespace-nowrap ' +
      (secili ? 'text-white' : 'text-subtle betterhover:hover:text-white');
    b.style.setProperty('border-bottom', secili ? '2px solid #53fc18' : '2px solid transparent');
    b.style.setProperty('background', 'none');
    b.style.setProperty('border-radius', '0');
  }

  /* Kartın sekmelerini kurar ve seçili olanın içeriğini çizer. */
  function pkKartSekmeCiz(kart, ad) {
    /* 10.14 — Son Mesajlar + Ona yazılanlar TEK ayara bağlı: açılınca ikisi de
       gelir, kapanınca ikisi de gider. Eskiden "Son Mesajlar" ayarsızdı (hep
       açıktı), "Ona yazılanlar" ayrı anahtardaydı — kullanıcı ikisini birden
       kapatamıyordu. */
    var yanitAcik = settings.kartMesajlar !== false;
    if (!yanitAcik) { pkKartSekme = 'mesaj'; }

    var kap = pkKartSekmeKabi(kart);
    var bar = kap.querySelector(':scope > .pk-sekme-bar');
    var govde = kap.querySelector(':scope > .pk-sekme-govde');

    /* Tek sekme varsa şerit gereksiz — yer kaplamasın.
       ŞERİT GİZLİYSE GÖVDE AÇIK KALMALI: aç/kapa denetimi sekmenin kendisi,
       gizlenince kapalı kalan içeriği açacak bir şey kalmazdı. (Bu dal bugün
       ulaşılmıyor — kartMesajlar kapalıyken kap tamamen siliniyor — ama
       yeniden bağlanırsa tuzağa düşmesin.) */
    bar.style.display = yanitAcik ? '' : 'none';
    if (!yanitAcik) pkKartAcik = true;

    pkSekmeDugmesi(bar, 'mesaj', t('pkSonMesajlar'), function () { pkKartSekmeCiz(kart, ad); });
    if (yanitAcik) pkSekmeDugmesi(bar, 'yanit', t('pkGelenYanitlar'), function () { pkKartSekmeCiz(kart, ad); });
    else { var y = bar.querySelector('[data-pk-sekme="yanit"]'); if (y) y.remove(); }

    var b1 = bar.querySelector('[data-pk-sekme="mesaj"]'); if (b1) pkSekmeBoya(b1, pkKartSekme === 'mesaj');
    var b2 = bar.querySelector('[data-pk-sekme="yanit"]'); if (b2) pkSekmeBoya(b2, pkKartSekme === 'yanit');

    govde.textContent = '';
    /* KAPALIYKEN İÇERİK HİÇ ÜRETİLMİYOR: yalnız gizlemek, her kart açılışında
       mesaj listesini boşuna kurmak olurdu. */
    govde.style.display = pkKartAcik ? '' : 'none';
    if (pkKartAcik) {
      if (pkKartSekme === 'yanit' && yanitAcik) pkYanitlariCiz(govde, ad);
      else pkMesajlariCiz(govde, ad);
    }
    return kap;
  }

  /* Artık kartın altına ayrı blok eklemiyor; sekme gövdesine çiziyor. */
  function pkYanitlariCiz(govde, ad) {
    var liste = kisiyeYanitlar(ad);
    if (!liste.length) {
      govde.appendChild(el('div', 'text-sm text-subtle', t('pkGelenYanitYok')));
      return;
    }
    var ic = el('div', 'flex flex-col gap-2');
    /* Mesajlar sekmesiyle aynı boyda kutu, aynı davranış: hepsi basılır,
       taşan kaydırılır. Burada da kesme yok. */
    ic.style.cssText = 'max-height:' + (PK_KART_SATIR * PK_KART_SATIR_PX) +
      'px;overflow-y:auto;overscroll-behavior:contain';
    liste.forEach(function (m) {
      var r = el('div', 'bg-surface-base rounded flex flex-col gap-1');
      r.style.cssText = 'padding:6px 8px';
      var ust = el('div', 'flex items-center gap-2');
      ust.appendChild(el('div', 'text-xs font-semibold text-primary-base', m.kim));
      ust.appendChild(el('div', 'text-xs text-subtle', scSaat(m.ts)));
      r.appendChild(ust);
      r.appendChild(mesajGovdesi(m.p, 16));
      ic.appendChild(r);
    });
    govde.appendChild(ic);
    ic.scrollTop = ic.scrollHeight;            // en yeni altta, mesajlar sekmesi gibi
  }

  function injectPopupYanitlar(kart, ad) {
    /* Çizim artık pkKartSekmeCiz üzerinden; burada yalnız eski ayrı blok
       kaldıysa temizleniyor (sürüm yükseltmesinde sayfada kalmış olabilir). */
    var eski = kart.querySelector(':scope > .pk-yanitlar');
    if (eski) eski.remove();
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — KARTTA HIZLI MODERASYON
   * Kick'in kendi sustur/yasakla düğmeleri kartta var ama süre seçtirmiyor.
   * Burada seçtiğin süreyi sohbet kutusuna KOMUT olarak yazıyoruz
   * (/timeout ad dakika). Komutu BİZ GÖNDERMİYORUZ — kutuya yazılıyor,
   * göndermek sende. Böylece yanlışlıkla ceza verilmiyor ve bizim adımıza
   * moderasyon isteği atılmış olmuyor.
   * ══════════════════════════════════════════════════════════════════════ */
  var HIZLI_SURE = [1, 5, 10, 30, 60, 1440];


  /* ══════════════════════════════════════════════════════════════════════════
   * CROSS-BAN — moderatör olunan tüm kanallarda yasakla / sustur / affet
   *
   * KICK'TE TOPLU İŞLEM YOK. Sunucuda "hepsine uygula" diye bir bayrak
   * bulunmuyor; kanal listesi alınıp HER KANALA AYRI istek atılıyor.
   *
   * Uç noktalar (canlıda doğrulandı — çıkışta 401, yani rota gerçek):
   *   GET    /api/internal/v1/user/moderators      → moderatör olunan kanallar
   *   POST   /api/v2/channels/{slug}/bans          → yasakla / sustur
   *   DELETE /api/v2/channels/{slug}/bans/{ad}     → yasağı kaldır
   *
   * Hepsi kick.com kökeninde, oturum çerezi + Bearer ile. İçerik betiğinden
   * doğrudan çağrılabiliyor (aynı köken), arka plana gerek yok.
   *
   * KULLANICI ADI: ban gövdesi tire kabul etmiyor, slug'da tire olabiliyor —
   * '-' → '_' çevriliyor.
   * ══════════════════════════════════════════════════════════════════════════ */
  var CROSS_KOK = 'https://kick.com/api/v2/channels/';
  var CROSS_LISTE_UC = 'https://kick.com/api/internal/v1/user/moderators';
  var crossKanallar = null;          // [{slug, ad}]
  var crossKanalZaman = 0;
  var CROSS_LISTE_TTL = 10 * 60 * 1000;
  var crossHaric = Object.create(null);   // kalıcı dışlama listesi (slug → 1)
  var crossCalisiyor = false, crossDur = false;

  function crossAdNormal(ad) { return String(ad || '').trim().replace(/-/g, '_'); }

  /* TOPLU kipte kanallar arası en kısa bekleme. Tek kişide 1 sn yeterli;
     toplu işlem kişi×kanal kadar istek demek (10 kişi × 20 kanal = 200) ve
     hızlı gönderim hem hız sınırına takılıyor hem kötüye kullanım gibi
     görünüyor. Alt sınır arayüzde de zorlanıyor, girilen değer kırpılıyor. */
  var CROSS_TOPLU_MIN_BEKLEME = 10;

  /* Kullanıcı adı listesini çözer.
     Kick kullanıcı adında BOŞLUK YOK — harf, rakam, alt çizgi (ve tire).
     Bu yüzden ayıraçları tek tek saymak yerine "ada ait olmayan her şey
     ayıraçtır" diyoruz: virgül, satır sonu, boşluk, noktalı virgül, `@`,
     hepsi çalışıyor. Kullanıcı ister `x,y,z` yazsın ister alt alta, ister
     ikisini karıştırsın — sonuç aynı. Tekrar eden adlar da eleniyor. */
  function crossAdlariCoz(metin) {
    var ham = String(metin || '').split(/[^A-Za-z0-9_-]+/);
    var out = [], gor = Object.create(null);
    for (var i = 0; i < ham.length; i++) {
      var a = crossAdNormal(ham[i]);
      if (!a) continue;
      var k = a.toLowerCase();
      if (gor[k]) continue;
      gor[k] = 1; out.push(a);
    }
    return out;
  }

  function crossBasliklar(jeton) {
    var h = { 'Accept': 'application/json', 'Content-Type': 'application/json' };
    if (jeton) h.Authorization = 'Bearer ' + jeton;
    return h;
  }

  /* Moderatör olunan kanallar. 10 dakika önbellekleniyor: pencere her
     açıldığında listeyi yeniden çekmenin anlamı yok, mod yetkisi sık
     değişmiyor. */
  function crossKanallariCek(bitince) {
    if (crossKanallar && Date.now() - crossKanalZaman < CROSS_LISTE_TTL) { bitince(crossKanallar); return; }
    var jeton = '';
    try { jeton = rosterJeton(); } catch (e) {}
    fetch(CROSS_LISTE_UC, { credentials: 'include', cache: 'no-store', headers: crossBasliklar(jeton) })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        var dizi = (j && j.data && j.data.channels) || [];
        var out = [];
        for (var i = 0; i < dizi.length; i++) {
          var c = dizi[i] || {};
          var s = String(c.channel_slug || c.slug || '').toLowerCase();
          if (s) out.push({ slug: s, ad: c.channel_slug || c.user_username || s });
        }
        out.sort(function (a, b) { return a.slug < b.slug ? -1 : 1; });
        crossKanallar = out; crossKanalZaman = Date.now();
        bitince(out);
      })
      .catch(function () { bitince(null); });
  }

  /* Tek kanalda tek işlem. Söz döndürüyor: {ok, durum}. */
  function crossIslem(tur, slug, ad, sure, sebep) {
    var jeton = '';
    try { jeton = rosterJeton(); } catch (e) {}
    var kullanici = crossAdNormal(ad);
    if (tur === 'kaldir') {
      return fetch(CROSS_KOK + encodeURIComponent(slug) + '/bans/' + encodeURIComponent(kullanici),
                   { method: 'DELETE', credentials: 'include', headers: crossBasliklar(jeton) })
        .then(function (r) { return { ok: r.ok, durum: r.status }; })
        .catch(function () { return { ok: false, durum: 0 }; });
    }
    var kalici = (tur === 'yasak');
    var govde = { banned_username: kullanici, permanent: kalici, reason: sebep || null };
    /* `duration` YALNIZ susturmada gönderiliyor — kalıcı yasakta anlamsız. */
    if (!kalici) govde.duration = Math.max(1, parseInt(sure, 10) || 5);
    return fetch(CROSS_KOK + encodeURIComponent(slug) + '/bans',
                 { method: 'POST', credentials: 'include', headers: crossBasliklar(jeton),
                   body: JSON.stringify(govde) })
      .then(function (r) { return { ok: r.ok, durum: r.status }; })
      .catch(function () { return { ok: false, durum: 0 }; });
  }

  /* Dışlama listesi kalıcı: kullanıcı bir kanalın kutusunu boşaltınca bir
     dahaki sefere de boş geliyor. */
  /* ══════════════════════════════════════════════════════════════════════
   * PARALEL GÖNDERİM — kendini ayarlayan öbekler
   *
   * Kanallar SIRAYLA gönderilince 50 kanal 50 ardışık gidiş-dönüş demek;
   * kullanıcı bir kişinin banlanmasını dakikalarca bekliyordu. HTTP/2 tek
   * bağlantı üzerinde çoklama yaptığı için onlarca isteği aynı anda
   * göndermek Kick tarafında sorun değil.
   *
   * ASIL SINIR KICK'İN HIZ SINIRI DEĞİL, TARAYICININ BAĞLANTI HAVUZU.
   * Havuz dolunca `fetch` HTTP durumu olmayan bir ağ hatası veriyor;
   * `crossIslem` bunu `durum: 0` diye bildiriyor. 4xx/5xx ise gerçek bir
   * yanıttır (yetki yok, zaten yasaklı) ve tekrar denemek düzeltmez.
   * Ayrım bu yüzden önemli:
   *   durum 0  → öbek çok büyük, KÜÇÜLT ve AYNI dilimi yinele
   *   durum >0 → gerçek başarısızlık, kaydet ve devam et
   * Art arda temiz turlarda öbek yeniden büyüyor: küçük listede boşuna
   * yavaşlamayalım, büyük listede kendini bulsun.
   * ══════════════════════════════════════════════════════════════════════ */
  var CROSS_OBEK_BAS = 200;          // başlangıç öbek boyu
  var CROSS_OBEK_MIN = 20;
  var CROSS_OBEK_MAX = 500;
  var CROSS_OBEK_ARA = 200;          // öbekler arası nefes (ms)
  var CROSS_DAR_BEKLE = 500;         // havuz dolunca küçültmeden önce
  var CROSS_TEMIZ_ESIK = 3;          // kaç temiz öbekten sonra büyüt
  var CROSS_YENIDEN_BEKLE = 2000;    // başarısızları yeniden denemeden önce
  /* Küçültüp yinelemenin ÜST SINIRI. Ağ tümden koptuğunda her istek
     `durum: 0` döner ve motor bunu "öbek çok büyük" sanıp küçülterek
     yeniden dener; öbek tabana inene kadar hedef sayısının katı kadar
     istek harcanıyordu (ölçüldü: 100 hedef için 443 istek). Bu sayıdan
     sonra `durum: 0` gerçek başarısızlık sayılıyor. */
  var CROSS_DAR_SINIR = 3;

  function crossBekle(ms) { return new Promise(function (c) { setTimeout(c, ms); }); }

  /* `hedefler`i öbek öbek paralel gönderir.
       calistir(h)          → Promise<{ok, durum}>
       ilerle(bitti, toplam) → her öbekten sonra
       durMu()              → true dönerse yarıda bırakır
     Sonuç: Promise<{basarili, hatali, basarisizlar}> */
  function crossObekGonder(hedefler, calistir, ilerle, durMu) {
    var i = 0, bitti = 0, basarili = 0, temizTur = 0, darSayac = 0;
    var basarisizlar = [];
    var obek = Math.max(1, Math.min(CROSS_OBEK_BAS, hedefler.length));
    function guvenli(h) {
      return calistir(h).catch(function () { return { ok: false, durum: 0 }; });
    }
    function tur() {
      if ((durMu && durMu()) || i >= hedefler.length) return Promise.resolve();
      var dilim = hedefler.slice(i, i + obek);
      return Promise.all(dilim.map(guvenli)).then(function (sonuc) {
        /* TEK bir ağ hatası bile öbeğin havuzu doldurduğunu gösteriyor.
           O dilimdeki istekler sunucuya ULAŞMADI sayılır: sayaçlara
           dokunmadan küçültüp aynı dilimi yineliyoruz. */
        var havuzDoldu = sonuc.some(function (r) { return !r.ok && r.durum === 0; });
        if (havuzDoldu && obek > CROSS_OBEK_MIN && darSayac < CROSS_DAR_SINIR) {
          darSayac++;
          obek = Math.max(CROSS_OBEK_MIN, Math.floor(obek * 0.6));
          temizTur = 0;
          return crossBekle(CROSS_DAR_BEKLE).then(tur);
        }
        for (var j = 0; j < sonuc.length; j++) {
          if (sonuc[j].ok) basarili++; else basarisizlar.push(dilim[j]);
        }
        i += dilim.length; bitti += dilim.length;
        if (ilerle) ilerle(bitti, hedefler.length);
        /* Temiz bir öbek geçti: daralma bütçesi tazeleniyor. Sınır ARDIŞIK
           başarısızlıklar için — uzun listede bir kez daralmak sonrakini
           yasaklamamalı. */
        darSayac = 0;
        if (++temizTur >= CROSS_TEMIZ_ESIK && obek < CROSS_OBEK_MAX) {
          obek = Math.min(CROSS_OBEK_MAX, Math.ceil(obek * 1.3));
          temizTur = 0;
        }
        if (i >= hedefler.length) return Promise.resolve();
        return crossBekle(CROSS_OBEK_ARA).then(tur);
      });
    }
    function sonuclandir() {
      return { basarili: basarili, hatali: basarisizlar.length, basarisizlar: basarisizlar };
    }
    return tur().then(function () {
      /* TEK yeniden deneme turu. İlk turda düşenlerin bir kısmı anlık ağ
         dalgalanması; ikinci bir tur atmıyoruz çünkü kalıcı hatada (yetki
         yok, zaten yasaklı) sonsuza kadar dönerdi. */
      if (!basarisizlar.length || (durMu && durMu())) return sonuclandir();
      var yeniden = basarisizlar; basarisizlar = [];
      return crossBekle(CROSS_YENIDEN_BEKLE)
        .then(function () { return Promise.all(yeniden.map(guvenli)); })
        .then(function (sonuc) {
          for (var j = 0; j < sonuc.length; j++) {
            if (sonuc[j].ok) basarili++; else basarisizlar.push(yeniden[j]);
          }
          return sonuclandir();
        });
    });
  }


  /* ══════════════════════════════════════════════════════════════════════
   * ROZET YETKİLENDİRME — VIP · OG · Moderatör
   *
   * Profil kartında rozetlerin yanına bir kalem düğmesi koyar; açılan pencerede
   * kişiye bu üç yetkiyi verip alabilirsin.
   *
   * YALNIZ KENDİ KANALINDA. Kick'in bu uçları KANAL SAHİBİNE açık — moderatör
   * olarak girdiğin başka bir kanalda 403 dönüyor (aynı kısıt kanalın mod
   * listesini okumakta da var). Düğmeyi orada göstermek "bastım ama olmadı"
   * demek olurdu; sahiplik doğrulanmadan hiç çizilmiyor.
   *
   * Sahiplik `/api/v2/channels/{slug}/me` → `is_broadcaster` ile soruluyor,
   * KANAL BAŞINA BİR KEZ: kart her açıldığında sormak boş istek olurdu.
   * ══════════════════════════════════════════════════════════════════════ */
  var PK_YETKI_ID = 'pk-yetki';
  var PK_YETKI_TIP = ['vip', 'og', 'moderator'];

  var I_KALEM = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:14px;height:14px;display:block">' +
    '<path d="M3 17.25V21h3.75L17.81 9.94l-3.75-3.75L3 17.25zM20.71 7.04a1 1 0 0 0 0-1.41l-2.34-2.34a1 1 0 0 0-1.41 0l-1.83 1.83 3.75 3.75 1.83-1.83z"/></svg>';

  function pkYetkiAdi(tip) {
    if (tip === 'vip') return t('pkYetkiVip');
    if (tip === 'og') return t('pkYetkiOg');
    return t('pkYetkiMod');
  }

  var pkSahipKanal = '', pkSahipMi = false, pkSahipIstek = false;
  function pkSahipMiyiz() {
    var k = scKanal;
    if (!k) return false;
    if (pkSahipKanal === k) return pkSahipMi;
    if (pkSahipIstek) return false;
    pkSahipIstek = true;
    var jeton = ''; try { jeton = rosterJeton(); } catch (e) {}
    fetch('https://kick.com/api/v2/channels/' + encodeURIComponent(k) + '/me',
          { credentials: 'include', cache: 'no-store', headers: crossBasliklar(jeton) })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        pkSahipKanal = k; pkSahipMi = !!(j && j.is_broadcaster); pkSahipIstek = false;
      })
      .catch(function () { pkSahipIstek = false; });
    return false;
  }

  /* Kişinin BU kanaldaki yetkileri. Kimlik doğrulaması gerekmiyor; yanıttaki
     `badges` dizisinde `{type}` alanı var (broadcaster, vip, og, moderator…). */
  function pkYetkiDurumu(slug, ad) {
    return fetch('https://kick.com/api/v2/channels/' + encodeURIComponent(slug) +
                 '/users/' + encodeURIComponent(crossAdNormal(ad)),
                 { credentials: 'include', cache: 'no-store', headers: { accept: 'application/json' } })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        if (!j) return null;
        var v = Object.create(null), b = j.badges || [];
        for (var i = 0; i < b.length; i++) if (b[i] && b[i].type) v[b[i].type] = 1;
        /* `is_moderator` ayrı bir alan; moderatör rozeti listede olmasa da
           yetki duruyor olabiliyor. İkisini birleştiriyoruz. */
        if (j.is_moderator) v.moderator = 1;
        return v;
      })
      .catch(function () { return null; });
  }

  /* Tek yetki ver / al.
       ver → POST   …/community/{tip}s        gövde {username}
       al  → DELETE …/community/{tip}s/{ad}
     Çoğul takı uçta: vip→vips, og→ogs, moderator→moderators. */
  function pkYetkiIslem(ver, slug, tip, ad) {
    var jeton = ''; try { jeton = rosterJeton(); } catch (e) {}
    var bas = { accept: 'application/json' };
    if (jeton) bas.Authorization = 'Bearer ' + jeton;
    var kok = 'https://kick.com/api/internal/v1/channels/' + encodeURIComponent(slug) +
              '/community/' + tip + 's';
    var istek;
    if (ver) {
      bas['content-type'] = 'application/json';
      istek = fetch(kok, { method: 'POST', credentials: 'include', headers: bas,
                           body: JSON.stringify({ username: crossAdNormal(ad) }) });
    } else {
      istek = fetch(kok + '/' + encodeURIComponent(crossAdNormal(ad)),
                    { method: 'DELETE', credentials: 'include', headers: bas });
    }
    return istek.then(function (r) { return { ok: r.ok, durum: r.status }; })
                .catch(function () { return { ok: false, durum: 0 }; });
  }

  function pkYetkiKapat() { var p = document.getElementById(PK_YETKI_ID); if (p) p.remove(); }

  function pkYetkiPenceresi(ad) {
    if (document.getElementById(PK_YETKI_ID)) return;
    var slug = scKanal; if (!slug) { toast(t('pkTSohbetAlaniYok')); return; }

    var perde = el('div', '');
    perde.id = PK_YETKI_ID;
    /* Perde ölçüsü `pkPencereSerbest` içinde veriliyor — karartma yok. */
    var kart2 = el('div', 'bg-surface-base flex h-fit flex-col rounded text-sm shadow-lg gap-3 p-4');
    kart2.style.cssText = 'width:380px;max-width:100%;max-height:82vh;overflow-y:auto;color:#fff';

    var bas2 = el('div', 'flex items-center justify-between gap-2');
    bas2.appendChild(el('div', 'min-w-0 truncate text-base font-bold text-white', t('pkYetkiBaslik', [ad])));
    var kapatB = el('button', 'shrink-0 inline-flex items-center justify-center rounded state-layer-surface text-white');
    kapatB.style.cssText = 'width:32px;height:32px;padding:0;background:none;border:0;cursor:pointer';
    kapatB.innerHTML = I_KAPAT;
    kapatB.addEventListener('click', function (e) { e.stopPropagation(); pkYetkiKapat(); });
    bas2.appendChild(kapatB);
    kart2.appendChild(bas2);
    kart2.appendChild(el('div', 'text-xs text-subtle', t('pkYetkiAciklama')));
    kart2.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));

    var govde2 = el('div', 'flex flex-col gap-2');
    govde2.appendChild(el('div', 'text-sm text-subtle', t('pkCrossYukleniyor')));
    kart2.appendChild(govde2);

    pkPencereSerbest(perde, kart2, bas2);
    perde.appendChild(kart2);
    pkKatmanaEkle(perde);

    pkYetkiDurumu(slug, ad).then(function (durum) {
      govde2.textContent = '';
      if (!durum) {
        govde2.appendChild(el('div', 'text-sm text-danger', t('pkYetkiOkunamadi')));
        return;
      }
      /* BAŞLANGIÇ durumu ayrı tutuluyor: kaydederken yalnız DEĞİŞENLER
         gönderiliyor. Hepsini göndermek, dokunulmamış yetkiyi de yeniden
         yazıp gereksiz istek (ve gereksiz bildirim) üretirdi. */
      var baslangic = Object.create(null), hedef = Object.create(null);
      PK_YETKI_TIP.forEach(function (tip) {
        baslangic[tip] = !!durum[tip];
        hedef[tip] = baslangic[tip];
        /* Satırı `satir()` ile değil ELLE kuruyoruz: rozetin adın ÖNÜNE
           gelmesi gerekiyor, `satir()` ise yalnız düz metin başlık alıyor.
           Rozet Kick'in kendi çizimi (`KICK_ROZET`) — kullanıcı yetkiyi
           sohbette hangi simgeyle tanıyorsa burada da onu görüyor. */
        var r = el('div', 'flex items-center justify-between gap-4 py-2');
        var sl = el('div', 'flex items-center gap-2 min-w-0');
        sl.appendChild(rolRozeti(tip));
        sl.appendChild(el('div', 'text-sm font-semibold text-white truncate', pkYetkiAdi(tip)));
        r.appendChild(sl);
        r.appendChild(anahtar(baslangic[tip], function (v) { hedef[tip] = v; }));
        govde2.appendChild(r);
      });

      var durumY2 = el('div', 'text-xs text-subtle', '');
      kart2.appendChild(durumY2);
      var kaydetB = btnBirincil(t('pkKaydet'));
      kart2.appendChild(kaydetB);

      kaydetB.addEventListener('click', function (e) {
        e.preventDefault(); e.stopPropagation();
        var isler = PK_YETKI_TIP.filter(function (tip) { return hedef[tip] !== baslangic[tip]; });
        if (!isler.length) { pkYetkiKapat(); return; }
        kaydetB.disabled = true;
        var i = 0, hata = 0;
        function sonraki() {
          if (i >= isler.length) {
            kaydetB.disabled = false;
            /* Kart açık kalırsa eski rozetleri gösterir; kapatıp bir sonraki
               açılışta Kick'in taze verisini okumak en temizi. */
            toast(hata ? t('pkTYetkiKismi', [String(hata)]) : t('pkTYetkiKaydedildi'));
            pkYetkiKapat();
            return;
          }
          var tip = isler[i];
          durumY2.textContent = t('pkCrossIlerleme', [String(i + 1), String(isler.length), pkYetkiAdi(tip)]);
          pkYetkiIslem(hedef[tip], slug, tip, ad).then(function (r) {
            if (!r.ok) hata++;
            i++; sonraki();
          });
        }
        sonraki();
      });
    });
  }

  /* Kalem düğmesi — Kick'in rozet kutusunun SONUNA. */
  function pkYetkiIkonu(kart2, ad) {
    var eski = kart2.querySelector('.pk-yetki-btn');
    if (!ad || !pkSahipMiyiz()) { if (eski) eski.remove(); return; }
    if (eski) {
      if (eski.getAttribute('data-pk-yad') === ad) return;
      eski.remove();
    }
    var kutu = popupBadgeBox(kart2); if (!kutu) return;
    var b = el('button', 'pk-yetki-btn inline-flex items-center justify-center rounded shrink-0');
    b.setAttribute('data-pk-yad', ad);
    b.type = 'button';
    b.title = t('pkYetkiDugme');
    b.setAttribute('aria-label', b.title);
    b.style.cssText = 'width:18px;height:18px;padding:0;margin-left:2px;background:transparent;' +
      'border:0;color:#9fa6ad;cursor:pointer;transition:color .12s ease';
    b.innerHTML = I_KALEM;
    b.addEventListener('mouseenter', function () { b.style.color = '#fff'; });
    b.addEventListener('mouseleave', function () { b.style.color = '#9fa6ad'; });
    b.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      try { pkYetkiPenceresi(ad); } catch (er) {}
    });
    kutu.appendChild(b);
  }


  /* ══════════════════════════════════════════════════════════════════════
   * TOPLU TIMEOUT — tetikleyici metinle otomatik susturma
   *
   * Bir kelime/cümle listesi veriliyor; o metni yazan herkes seçilen süreyle
   * susturuluyor. İki kip birlikte kullanılabiliyor:
   *   · GEÇMİŞ  → oturumda toplanan sohbette (`scListe`) eşleşenler taranır
   *   · CANLI   → başlatıldıktan sonra gelen her mesaj anında denetlenir
   * Baskın (raid) anında saniyeler önemli: canlı kip mesaj işlenirken doğrudan
   * çağrılıyor, ayrı bir tur beklemiyor.
   *
   * KORUMALAR — yanlış susturma geri alması zor, o yüzden dört kapı var:
   *   1) Kendini asla susturmaz.
   *   2) Yayıncı ve moderatörler atlanır (baskına karşı açtığın araç kendi
   *      ekibini susturmamalı).
   *   3) Aynı kişi oturumda bir kez işlenir — tekrar tekrar süre eklemez.
   *   4) Tetikleyici listesi boşken hiç başlamaz.
   * ══════════════════════════════════════════════════════════════════════ */
  var TOPLU_TO_ID = 'pk-toplu-to';
  var topluToAcik = false;            // canlı kip çalışıyor mu
  var topluToTetik = [];              // aranan metinler (küçültülmüş)
  var topluToTamKelime = true;
  var topluToDk = 5;
  var topluToSebep = '';
  var topluToGorulen = Object.create(null);   // nick → 1, tekrar susturmayı önler
  var topluToSayac = 0;
  var topluToYaz = null;              // pencere açıksa sayacı güncelleyen geri çağrı

  /* Tetikleyiciler SATIR SATIR ayrılıyor, virgülle DEĞİL: aranan şey bir cümle
     olabiliyor ve cümlede virgül geçmesi normal. */
  function topluToCoz(metin) {
    var ham = String(metin || '').split(/\r?\n/);
    var out = [], gor = Object.create(null);
    for (var i = 0; i < ham.length; i++) {
      var a = pkKucult(ham[i].trim());
      if (!a || gor[a]) continue;
      gor[a] = 1; out.push(a);
    }
    return out;
  }

  /* Parça dizisinden düz metin — emote adları da metne katılıyor, spam çoğu
     zaman emote dizisi oluyor. */
  function topluToMetin(pr) {
    var o = '';
    for (var i = 0; i < (pr || []).length; i++) {
      if (pr[i].t === 'y') o += ' ' + pr[i].v;
      else if (pr[i].a) o += ' ' + pr[i].a;
    }
    return pkKucult(o);
  }

  /* Eşleştirme ORTAK: Toplu TimeOut ile Toplu Mesaj Silme aynı kuralı
     kullanıyor. İki kopya tutmak, birinde düzeltilen bir davranışın ötekinde
     kalmasına yol açardı. */
  function tetikEslesti(pr, liste, tamKelime) {
    if (!liste || !liste.length) return false;
    var yazi = topluToMetin(pr);
    if (!yazi.trim()) return false;
    for (var i = 0; i < liste.length; i++) {
      if (tamKelime ? kelimeGecti(yazi, liste[i]) : yazi.indexOf(liste[i]) >= 0) return true;
    }
    return false;
  }
  function topluToEslesti(pr) {
    return tetikEslesti(pr, topluToTetik, topluToTamKelime);
  }

  /* Bu kişiye dokunulur mu? */
  function topluToUygunMu(ad) {
    var k = pkKucult(ad); if (!k) return false;
    if (topluToGorulen[k]) return false;
    var ben = pkKucult(pkKendiAdim());
    if (ben && k === ben) return false;
    if (pkKucult(scKanal) === k) return false;             // yayıncı
    var rol = kisiRol[k];
    if (rol === 'broadcaster' || rol === 'moderator' || rol === 'staff') return false;
    return true;
  }

  function topluToSustur(ad) {
    var k = pkKucult(ad);
    topluToGorulen[k] = 1;
    return crossIslem('sustur', scKanal, ad, topluToDk, topluToSebep || null)
      .then(function (r) {
        if (r.ok) { topluToSayac++; if (topluToYaz) try { topluToYaz(); } catch (e) {} }
        return r;
      });
  }

  /* CANLI KİP — her mesaj işlenirken çağrılıyor. Kapalıyken tek karşılaştırma
     yapıp çıkıyor; sohbet turuna yük bindirmiyor. */
  function topluToBak(ad, pr) {
    if (!topluToAcik) return;
    if (!topluToUygunMu(ad)) return;
    if (!topluToEslesti(pr)) return;
    try { topluToSustur(ad); } catch (e) {}
  }

  /* GEÇMİŞ KİP — oturumda toplanan sohbette eşleşen kişileri bulur. */
  function topluToGecmisAdlar() {
    var out = [];
    for (var k in scListe) {
      var v = scListe[k]; if (!v || !v.mesajlar) continue;
      if (!topluToUygunMu(v.ad || k)) continue;
      for (var i = 0; i < v.mesajlar.length; i++) {
        if (topluToEslesti(v.mesajlar[i].p)) { out.push(v.ad || k); break; }
      }
    }
    return out;
  }

  /* KANAL DEĞİŞİNCE CANLI KİP DURUR. Yeni girdiğin kanalda susturmaya devam
     etmesi kabul edilemez: orada muhtemelen yetkin bile yok ve tetikleyici
     listesi o topluluk için düşünülmemiş. Görülenler de sıfırlanıyor —
     sayaç kanala ait. */
  function topluToSifirla() {
    topluToAcik = false;
    topluToGorulen = Object.create(null);
    topluToSayac = 0;
    topluToKapat();
  }


  /* ══════════════════════════════════════════════════════════════════════
   * TOPLU MESAJ SİLME
   *
   * Toplu TimeOut'un kardeşi: aynı tetikleyici mantığı, ama kişiyi
   * susturmak yerine EŞLEŞEN MESAJI siliyor. Baskında ikisi ayrı işe yarıyor
   * — susturma kişiyi durduruyor, silme ekranda kalan çöpü topluyor.
   *
   *   DELETE /api/v2/chatrooms/{oda}/messages/{mesaj_kimliği}
   *
   * Uç TARAYICIDAN ÖLÇÜLDÜ (28.08.2026): var olmayan bir kimlikle 200 +
   * {"status":{"error":false}} dönüyor, yani yol geçerli. Denenen ötekiler
   * (channels/{id}/messages, internal/v1/chatrooms/…, web.kick.com/…) "route
   * yok" 404'ü verdi.
   *
   * KORUMALAR — Toplu TimeOut'takilerle aynı mantık, biri farklı:
   *   1) Kendi mesajını silmez.
   *   2) Yayıncı/moderatör/staff mesajlarına dokunmaz.
   *   3) Aynı mesaj bir kez işlenir — "görülen" burada KİŞİ değil MESAJ
   *      kimliği; bir kişinin on eşleşen mesajı varsa onu da silmeli.
   *   4) Tetikleyici listesi boşken hiç başlamaz.
   * ══════════════════════════════════════════════════════════════════════ */
  var TOPLU_SIL_ID = 'pk-toplu-sil';
  var topluSilAcik = false;           // canlı kip çalışıyor mu
  var topluSilTetik = [];             // aranan metinler (küçültülmüş)
  var topluSilTamKelime = true;
  var topluSilGorulen = Object.create(null);   // mesaj kimliği → 1
  var topluSilSayac = 0;
  var topluSilYaz = null;             // pencere açıksa sayacı güncelleyen geri çağrı

  /* YETKİ: yalnız moderatör olunan ve kendi kanalında. Otomatik mesajla AYNI
     liste kullanılıyor (bkz. omYetkiliMi) — sıradan bir izleyicinin eline
     toplu silme aracı vermek anlamsız, uç zaten reddederdi ama isteği hiç
     atmamak daha doğru. Liste gelmeden çalışmıyor. */
  function topluSilYetkiliMi() {
    if (!scKanal) return false;
    if (!omYetkiliMi(scKanal)) { omModListesi(null); return false; }
    return true;
  }

  /* Bu mesaja dokunulur mu?

     KENDİ MESAJIN ATLANIYOR. Bir ara silinebilir yapılmıştı — özelliği
     denerken kullanıcı kendi yazdığı spam'ı hedefliyordu ve "eşleşen mesaj
     yok" alıyordu. Deneme bitince geri alındı (kullanıcı kararı): araç
     başkasının çöpünü toplamak için, kendi mesajını toplu bir tetikleyiciyle
     silmek istemeden olduğunda geri dönüşü yok.

     Kendi mesajını silmek isteyen sohbetteki tek tek silme düğmesini
     kullanıyor; orada ne sildiğini görüyor. */
  function topluSilUygunMu(ad, id) {
    if (!id || topluSilGorulen[id]) return false;
    var k = pkKucult(ad); if (!k) return false;
    var ben = pkKucult(pkKendiAdim());
    if (ben && k === ben) return false;                    // kendi mesajım
    if (pkKucult(scKanal) === k) return false;             // yayıncı
    var rol = kisiRol[k];
    if (rol === 'broadcaster' || rol === 'moderator' || rol === 'staff') return false;
    return true;
  }

  function topluSilIstek(oda, id) {
    var jeton = ''; try { jeton = rosterJeton(); } catch (e) {}
    var bas = { accept: 'application/json' };
    if (jeton) bas.Authorization = 'Bearer ' + jeton;
    return fetch('https://kick.com/api/v2/chatrooms/' + encodeURIComponent(oda) +
                 '/messages/' + encodeURIComponent(id),
                 { method: 'DELETE', credentials: 'include', headers: bas })
      .then(function (r) { return { ok: r.ok, durum: r.status }; })
      .catch(function () { return { ok: false, durum: 0 }; });
  }

  /* Oda kimliği otomatik mesajınkiyle AYNI önbellekten geliyor (omOdaCoz):
     iki özellik de aynı sayıya ihtiyaç duyuyor, ikinci bir çözücü yazmak
     aynı isteği iki kez atmak olurdu. */
  function topluSilMesaj(id) {
    topluSilGorulen[id] = 1;
    return new Promise(function (bitir) {
      omOdaCoz(scKanal, function (kim) {
        var oda = kim && kim.oda;
        if (!oda) { bitir({ ok: false, durum: 0 }); return; }
        topluSilIstek(oda, id).then(function (r) {
          if (r.ok) { topluSilSayac++; if (topluSilYaz) try { topluSilYaz(); } catch (e) {} }
          bitir(r);
        });
      });
    });
  }

  /* CANLI KİP — her mesaj işlenirken çağrılıyor. Kapalıyken tek karşılaştırma
     yapıp çıkıyor; sohbet turuna yük bindirmiyor. */
  function topluSilBak(ad, pr, id) {
    if (!topluSilAcik) return;
    if (!topluSilYetkiliMi()) return;
    if (!topluSilUygunMu(ad, id)) return;
    if (!tetikEslesti(pr, topluSilTetik, topluSilTamKelime)) return;
    try { topluSilMesaj(id); } catch (e) {}
  }

  /* GEÇMİŞ KİP — oturumda toplanan sohbette eşleşen MESAJLARI bulur. */
  function topluSilGecmisKimlikler() {
    var out = [];
    if (!topluSilYetkiliMi()) return out;
    for (var k in scListe) {
      var v = scListe[k]; if (!v || !v.mesajlar) continue;
      var ad = v.ad || k;
      for (var i = 0; i < v.mesajlar.length; i++) {
        var m = v.mesajlar[i];
        if (!m.id || !topluSilUygunMu(ad, m.id)) continue;
        if (tetikEslesti(m.p, topluSilTetik, topluSilTamKelime)) out.push(m.id);
      }
    }
    return out;
  }

  /* KANAL DEĞİŞİNCE CANLI KİP DURUR — Toplu TimeOut'takiyle aynı gerekçe:
     yeni kanalda yetkin olmayabilir ve tetikleyici listesi oraya göre
     düşünülmemiştir. */
  function topluSilSifirla() {
    topluSilAcik = false;
    topluSilGorulen = Object.create(null);
    topluSilSayac = 0;
    topluSilKapat();
  }

  /* Pencere Toplu TimeOut'unkinin aynısı — bilerek: ikisi kardeş araç,
     farklı görünmeleri "bu başka bir şey mi" diye düşündürürdü. Ayrılan tek
     yer süre/sebep alanları: silmede karşılığı yok. */
  function topluSilPencereAc() {
    if (document.getElementById(TOPLU_SIL_ID)) return;
    if (!scKanal) { toast(t('pkTSohbetAlaniYok')); return; }

    var perde = el('div', '');
    perde.id = TOPLU_SIL_ID;
    /* Perde ölçüsü `pkPencereSerbest` içinde veriliyor — karartma yok. */
    var kk = el('div', 'bg-surface-base flex h-fit flex-col rounded text-sm shadow-lg gap-3 p-4');
    kk.style.cssText = 'width:460px;max-width:100%;max-height:82vh;overflow-y:auto;color:#fff';

    var bas = el('div', 'flex items-center justify-between gap-2');
    bas.appendChild(el('div', 'min-w-0 truncate text-base font-bold text-white', t('pkSilBaslik')));
    var kapatB = el('button', 'shrink-0 inline-flex items-center justify-center rounded state-layer-surface text-white');
    kapatB.style.cssText = 'width:32px;height:32px;padding:0;background:none;border:0;cursor:pointer';
    kapatB.innerHTML = I_KAPAT;
    kapatB.addEventListener('click', function (e) { e.stopPropagation(); topluSilKapat(); });
    bas.appendChild(kapatB);
    kk.appendChild(bas);
    kk.appendChild(el('div', 'text-xs text-subtle', t('pkSilAciklama')));

    /* Yetki yoksa sebebini yazıp denetimleri kilitliyoruz — düğmeye basıp
       hiçbir şey olmaması en kötü geri bildirimdi. */
    var yetkili = !omModKanal || omYetkiliMi(scKanal);
    if (omModKanal && !yetkili) {
      kk.appendChild(el('div', 'text-xs text-danger', t('pkSilYetkiYok')));
    }
    if (!omModKanal) { try { omModListesi(null); } catch (e) {} }
    kk.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));

    /* ---- tetikleyiciler ---- */
    var govde = el('div', 'flex flex-col gap-3');
    kk.appendChild(govde);
    govde.appendChild(el('div', 'text-sm font-semibold text-white', t('pkToTetik')));
    govde.appendChild(el('div', 'text-xs text-subtle', t('pkSilTetikD')));
    var alan = el('textarea', 'bg-surface-lower rounded text-sm text-white');
    alan.rows = 4;
    alan.placeholder = t('pkToTetikIpucu');
    alan.value = topluSilTetik.join('\n');
    alan.style.cssText = 'padding:6px 8px;border:1px solid rgba(255,255,255,.12);' +
      'outline:none;resize:vertical;min-height:64px;font-family:inherit';
    govde.appendChild(alan);

    govde.appendChild(satir(t('pkToTamKelime'), t('pkToTamKelimeD'),
      anahtar(topluSilTamKelime, function (v) { topluSilTamKelime = v; })));

    govde.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));

    /* ---- sayaç + çubuk ---- */
    var durumY = el('div', 'text-xs text-subtle', t('pkSilSayac', [String(topluSilSayac)]));
    govde.appendChild(durumY);
    var barKap = el('div', 'w-full rounded overflow-hidden shrink-0');
    barKap.style.cssText = 'height:8px;background:rgba(255,255,255,.10)';
    var barDolgu = el('div', '');
    barDolgu.style.cssText = 'height:100%;width:0%;background:rgb(83,252,24);transition:width .18s ease';
    barKap.appendChild(barDolgu);
    govde.appendChild(barKap);
    topluSilYaz = function () { durumY.textContent = t('pkSilSayac', [String(topluSilSayac)]); };

    function ayarlariAl() {
      topluSilTetik = topluToCoz(alan.value);
      return topluSilTetik.length;
    }

    /* ---- geçmişi tara ---- */
    var gecmisB = el('button', 'bg-secondary-base hover:brightness-125 text-white h-9 rounded font-semibold text-sm', t('pkSilGecmis'));
    gecmisB.style.cssText = 'border:0;cursor:pointer';
    gecmisB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (!ayarlariAl()) { toast(t('pkTToTetikGir')); return; }
      var kimlikler = topluSilGecmisKimlikler();
      if (!kimlikler.length) { toast(t('pkTSilEslesmeYok')); return; }
      gecmisB.disabled = true;
      barDolgu.style.width = '0%';
      /* Cross-ban'ın uyarlanabilir öbek göndericisi burada da kullanılıyor:
         yüzlerce mesajı tek tek sıraya dizmek dakikalar sürerdi, hepsini
         birden atmak 429 getirirdi. */
      crossObekGonder(kimlikler,
        function (id) { return topluSilMesaj(id); },
        function (bitti, toplam) { barDolgu.style.width = Math.round(bitti / toplam * 100) + '%'; },
        function () { return false; }
      ).then(function (r) {
        gecmisB.disabled = false;
        barDolgu.style.width = '100%';
        durumY.textContent = t('pkCrossBitti', [String(r.basarili), String(r.hatali)]);
      });
    });
    govde.appendChild(gecmisB);

    /* ---- canlı kip ---- */
    var canliB = el('button', '', '');
    canliB.style.cssText = 'border:0;cursor:pointer';
    function canliBoya() {
      canliB.textContent = topluSilAcik ? t('pkToDurdur') : t('pkToBaslat');
      canliB.className = 'h-9 rounded font-semibold text-sm hover:brightness-110 transition-all';
      /* RENK SATIR İÇİ (bkz. Toplu TimeOut): `bg-danger-base` diye bir sınıf
         Kick'te yok, yazınca düğme zeminsiz kalıyor. */
      canliB.style.background = topluSilAcik ? '#fb1818' : 'rgb(83,252,24)';
      canliB.style.color = topluSilAcik ? '#fff' : '#000';
    }
    canliBoya();
    canliB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (topluSilAcik) { topluSilAcik = false; canliBoya(); toast(t('pkTSilDurdu')); return; }
      if (!ayarlariAl()) { toast(t('pkTToTetikGir')); return; }
      topluSilAcik = true; canliBoya();
      toast(t('pkTSilBasladi', [String(topluSilTetik.length)]));
    });
    govde.appendChild(canliB);
    govde.appendChild(el('div', 'text-xs text-subtle', t('pkSilNot')));

    if (omModKanal && !yetkili) {
      govde.style.pointerEvents = 'none'; govde.style.opacity = '.35';
    }

    pkPencereSerbest(perde, kk, bas);
    perde.appendChild(kk);
    pkKatmanaEkle(perde);
  }

  function topluSilKapat() {
    var p = document.getElementById(TOPLU_SIL_ID);
    if (p) p.remove();
    topluSilYaz = null;
    /* Pencere kapanınca sayaç ve görülenler sıfırlanıyor — Toplu TimeOut'ta
       da böyle: aynı mesaj yeniden gelirse (düzenleme/yeniden gönderim)
       tekrar yakalanabilsin. */
    topluSilGorulen = Object.create(null);
    topluSilSayac = 0;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * OTOMATİK MESAJ — aralıklı duyuru + komut cevabı
   *
   * İKİ BAĞIMSIZ PARÇA, ayrı ayrı açılıp kapanıyor:
   *   ARALIKLI → belirli dakikada bir ya da belirli mesaj sayısında bir,
   *              listedeki metinleri SIRAYLA gönderir.
   *   KOMUT    → sohbete "!komut" yazan olursa kayıtlı cevabı gönderir.
   *
   * GÖNDERİM API İLE, sohbet kutusuna yazarak DEĞİL: kutuya yazmak kullanıcının
   * o an yazmakta olduğu yarım mesajı ezer ve odağı çalardı.
   *
   * SONSUZ DÖNGÜ KORUMASI en kritik nokta: kendi gönderdiğimiz mesaj sohbete
   * düşünce onu da okuyoruz. Kendi adımızdan gelen mesajlara komut cevabı
   * vermiyoruz — yoksa "!test" cevabı içinde "!test" geçerse sonsuza kadar
   * kendine cevap verirdi.
   * ══════════════════════════════════════════════════════════════════════ */
  var OM_ID = 'pk-otomesaj';
  /* ÜST SINIR YOK — aralığı kullanıcı belirliyor (isteği üzerine). Önceki
     3–60 dakika / 5–300 mesar aralıkları bizim tercihimizdi; kanalını en iyi
     yayıncı biliyor.

     ALT SINIR 1 KALIYOR ve bu bir tercih DEĞİL, doğruluk meselesi: 0 dakika
     "her 30 saniyede bir" (turun kendi periyodu), 0 mesaj ise "her sohbet
     mesajına cevap" demek — ikisi de ayar değil, arıza. Kick bunu spam sayıp
     hesabı kısıtlar.

     Üst sınır için `Infinity` kullanılıyor: `omKirp` içindeki
     `Math.min(Infinity, n)` değeri olduğu gibi geçiriyor, `sayiAlani` ise
     sonsuz olmayan bir sınır yoksa `max` niteliğini hiç yazmıyor. */
  var OM_DK_MIN = 1, OM_DK_MAX = Infinity;
  var OM_ADET_MIN = 1, OM_ADET_MAX = Infinity;

  function omKirp(deger, alt, ust, varsayilan) {
    var n = parseInt(deger, 10);
    if (!isFinite(n)) return varsayilan;
    return Math.max(alt, Math.min(ust, n));
  }
  var OM_KOMUT_BEKLEME = 10000;     // aynı komuta en sık bu aralıkla cevap

  var omOnaySus = false;                // "bir daha gösterme" işaretlendi mi
  var OM_TASIMA = '*';                  // kanal slug'ı olamaz → çakışmaz
  var omTum = Object.create(null);      // kanal → ayar
  var omAyar = omBosAyar();             // BU sekmenin kanalına ait ayar
  var omSonGonderim = 0;            // aralıklı kip: son gönderim anı
  var omMesajSayaci = 0;            // mesaj kipi: son gönderimden beri sayılan
  var omSira = 0;                   // listede sıradaki metin
  var omKomutSon = Object.create(null);   // komut → son cevap anı
  var omOda = Object.create(null);  // kanal → { oda, kanal } kimlikleri

  /* ══════════════════════════════════════════════════════════════════════
   * AYARLAR KANAL BAŞINA
   *
   * Eskiden tek bir `omAyar` vardı ve her sekme aynı kaydı okuyordu: kullanıcı
   * bir kanalda duyuruyu başlatınca, yan sekmede açık olan BAŞKA kanala da
   * mesaj gidiyordu (kullanıcı bildirdi). Artık kayıt `{ kanal: ayar }`
   * haritası; her sekme `scKanal`'ına bakıp yalnız kendi kanalınınkini
   * yürütüyor. Kanal kapsamlaması böylece kendiliğinden çıkıyor — ayrıca bir
   * "hangi kanal için açıktı" bayrağı tutmuyoruz, o bayrak kanal değişiminde
   * kaymaya açık olurdu.
   * ══════════════════════════════════════════════════════════════════════ */
  function omBosAyar() {
    return { aralikAcik: false, komutAcik: false, kip: 'dk',
             dk: 10, adet: 50, metinler: [], komutlar: [] };
  }
  /* Eksik alanlar varsayılandan tamamlanıyor ki eski bir kayıt yeni alanları
     olmadan gelince kod undefined okumasın. */
  function omAyarNormal(o) {
    o = o || {};
    return {
      aralikAcik: o.aralikAcik === true,
      komutAcik: o.komutAcik === true,
      kip: o.kip === 'mesaj' ? 'mesaj' : 'dk',
      dk: omKirp(o.dk, OM_DK_MIN, OM_DK_MAX, 10),
      adet: omKirp(o.adet, OM_ADET_MIN, OM_ADET_MAX, 50),
      metinler: Array.isArray(o.metinler) ? o.metinler.slice(0, 20) : [],
      komutlar: Array.isArray(o.komutlar) ? o.komutlar.filter(function (x) {
        return x && x.k && x.c;
      }).slice(0, 40) : []
    };
  }
  /* Eski biçimde ayarların KENDİSİ kökte duruyordu, yenisinde kök bir kanal
     haritası. Ayar alanlarına bakarak ayırıyoruz; bir kanal slug'ı bu adları
     taşıyamaz. */
  function omEskiBicimMi(o) {
    return Array.isArray(o.metinler) || Array.isArray(o.komutlar) ||
           typeof o.aralikAcik === 'boolean' || typeof o.komutAcik === 'boolean';
  }
  function omKanalAyari(k) {
    if (!k) return omBosAyar();
    if (!omTum[k]) omTum[k] = omBosAyar();
    return omTum[k];
  }
  function omKanalaGec() { omAyar = omKanalAyari(scKanal); }

  /* ══════════════════════════════════════════════════════════════════════
   * BOT YALNIZ POPOUT PENCERESİNDE KOŞAR
   *
   * Kullanıcı kuralı: "popup kapatılınca bot tamamen durmalı". Pencereyi
   * kapatmak botu durdurmanın yolu olduğuna göre, kanalın SIRADAN sekmesi de
   * göndermemeli — yoksa pencere kapatıldığı hâlde duyuru gitmeye devam
   * ederdi ve kullanıcı botu durduramazdı.
   *
   * Model böylece tek cümleye iniyor: pencere açık = bot çalışıyor.
   * AYARLAR SİLİNMİYOR; pencereyi yeniden açmak kaldığı yerden sürdürüyor.
   * ══════════════════════════════════════════════════════════════════════ */
  function omPopoutMu() {
    var p = (location.pathname || '').split('/').filter(Boolean);
    return p.length === 3 && pkKucult(p[0]) === 'popout' && pkKucult(p[2]) === 'chat';
  }

  /* ══════════════════════════════════════════════════════════════════════
   * ONAY PENCERESİ
   *
   * Kaydet artık yalnız ayar yazmıyor: İLK DUYURUYU HEMEN GÖNDERİYOR ve bot
   * penceresini açıyor. Sohbete mesaj düşüren, geri alınamaz bir eylem —
   * onay istiyor.
   *
   * "Bir daha gösterme" var: ayarını sık değiştiren kullanıcıya her kaydette
   * sormak eziyet olurdu. Tercih depoda (`pkOmOnaySus`), bütün kanallar için
   * ortak — soru "bu kanalda emin misin" değil, "bunu her seferinde sorayım
   * mı" sorusu.
   * ══════════════════════════════════════════════════════════════════════ */
  function omOnayPenceresi(metin, bitince) {
    var perde = el('div', '');
    /* Bu perde KARARTIYOR — araç pencerelerinin aksine burada bir karar
       bekleniyor, arkasıyla iş yapılamaz. */
    perde.style.cssText = 'position:fixed;inset:0;z-index:2147483215;background:rgba(0,0,0,.55);' +
      'display:flex;align-items:center;justify-content:center;padding:16px';
    var kart = el('div', 'bg-surface-base flex h-fit flex-col rounded text-sm shadow-lg gap-3 p-4');
    kart.style.cssText = 'width:420px;max-width:100%;color:#fff;border:1px solid rgba(255,255,255,.10)';
    kart.appendChild(el('div', 'text-base font-bold text-white', t('pkOmOnayBaslik')));
    kart.appendChild(el('div', 'text-xs text-subtle', metin));

    var susSatir = el('label', 'flex items-center gap-2');
    susSatir.style.cssText = 'cursor:pointer';
    var kutu = el('input', '');
    kutu.type = 'checkbox';
    kutu.style.cssText = 'width:15px;height:15px;accent-color:rgb(83,252,24);cursor:pointer';
    susSatir.appendChild(kutu);
    susSatir.appendChild(el('span', 'text-xs text-subtle', t('pkOmOnayGizle')));
    kart.appendChild(susSatir);

    var sr = el('div', 'flex items-center justify-end gap-2');
    function kapat() { try { perde.remove(); } catch (e) {} }
    var vaz = btnIkincil(t('pkIptal'));
    vaz.addEventListener('click', function (e) { e.preventDefault(); e.stopPropagation(); kapat(); });
    var tamam = btnBirincil(t('pkOmBaslat'));
    tamam.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (kutu.checked) {
        omOnaySus = true;
        try { chrome.storage.local.set({ pkOmOnaySus: true }); } catch (er) {}
      }
      kapat();
      /* `bitince` DOĞRUDAN burada çağrılıyor: içinde `window.open` var ve
         tarayıcı onu ancak kullanıcı tıklamasının zincirinde çalıştırıyor.
         setTimeout'a alsaydık pencere engellenirdi. */
      bitince();
    });
    sr.appendChild(vaz); sr.appendChild(tamam);
    kart.appendChild(sr);
    perde.appendChild(kart);
    pkKatmanaEkle(perde);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * BOT CANLI MI — KALP ATIŞI
   *
   * Bot yalnız popout penceresinde koşuyor, pencere kapanınca duruyor. Ama
   * `aralikAcik` DEPODA açık kalıyordu: kullanıcı ayarları açtığında bot
   * çalışıyormuş gibi görünüyordu, oysa çoktan durmuştu (kullanıcı bildirdi).
   *
   * Popout açık olduğu sürece kendi kanalına damga atıyor. Ayar penceresi
   * damgaya bakıyor; taze değilse anahtarları KAPATIYOR ve diske yazıyor —
   * ekranda görünen şey gerçeği söylesin.
   *
   * İKİ YOL BİRDEN:
   *   · `pagehide` damgayı hemen siliyor — HIZLI yol, ama kapanış sırasında
   *     depoya yazmanın tamamlanacağı garanti değil.
   *   · Damga eskiyince ölü sayılıyor — GARANTİ yol; çökme, askıda kalma ve
   *     yazamadan kapanma da bunun içinde.
   * `storage.onChanged` sayesinde öteki sekmeler anında haberdar oluyor;
   * yoklama (polling) yok.
   * ══════════════════════════════════════════════════════════════════════ */
  /* ÖMÜR UZUN (5 dk) ÇÜNKÜ ASIL SİNYAL `pagehide`: pencere kapanınca kayıt
     hemen siliniyor. Eskime yalnız çökme/yazamadan kapanma ağı. Kısa tutmak
     zararlıydı — arka plandaki pencerenin damgası Chrome kısıtı yüzünden
     gecikince bot ölü sanılıyordu (kullanıcı bildirdi). */
  var OM_BOT_OMUR = 300000;
  var omBot = null;                 // kanal → bot kaydı; null = depo daha okunmadı

  /* Bu sekme popout’u AZ ÖNCE açtıysa, kanal → an. Bkz. omBotCanliMi. */
  var omBaslatilan = Object.create(null);
  var OM_BASLATMA_PAYI = 120000;    // 2 dk: pencerenin açılıp ilk damgayı atması

  /* ⚠ BU FONKSİYONU YALNIZ BOT PENCERESİ ÇAĞIRIR.
     `omBot` haritasının TAMAMI yazılıyor; popout dışındaki bir sekme çağırınca
     kendi (eski) kopyasını diske basıp ÇALIŞAN botun kaydını siliyordu. SW de
     kaydı görmeyince `pkOmDurum`u budayıp duyuru sayacını sıfırdan başlatıyor,
     yani kullanıcı başka sekmelerde gezindikçe duyuru hiç gitmiyordu
     (kullanıcı bildirdi). Çağrı yerleri: omBotDamga ve pagehide — ikisi de
     `omPopoutMu()` kapısının arkasında. */
  function omBotYaz(kanal, kayit) {
    if (!kanal) return;
    var h = omBot || {};
    if (kayit) h[kanal] = kayit; else delete h[kanal];
    omBot = h;
    /* TEK ATIŞ, get+set DEĞİL: `pagehide` sırasında iki adımlık zincir
       tamamlanmaya yetişmiyor. Harita bellekte tutuluyor, doğrudan yazılıyor. */
    try { chrome.storage.local.set({ pkOmBot: h }); } catch (e) {}
  }
  function omBotCanliMi(kanal) {
    if (!kanal) return false;
    /* BU SEKME AZ ÖNCE BAŞLATTIYSA canlı sayılıyor: pencere ilk damgasını
       ~1,5 sn sonra atıyor ve depo turu daha geç geliyor. Eskiden bunun için
       buradan sahte bir kayıt yazılıyordu — asıl hata oydu, artık hiçbir şey
       yazmıyoruz, yalnız BU SEKMENİN hafızasına bakıyoruz. */
    if (Date.now() - (omBaslatilan[kanal] || 0) < OM_BASLATMA_PAYI) return true;
    if (!omBot) return false;
    var b = omBot[kanal];
    return !!b && (Date.now() - (b.beat || 0)) < OM_BOT_OMUR;
  }

  /* SERVICE WORKER'A DURUM BİLDİRİMİ. Gönderimi artık SW yapıyor (bkz.
     background.js): arka plandaki pencerenin zamanlayıcısı Chrome tarafından
     kısılıyor, alarm kısılmıyor. Pencerenin işi "yaşıyorum" demek ve gönderim
     için gerekenleri tazelemek — jeton süresi dolabiliyor, oda kimliği kanal
     değişiminde başkalaşıyor.
     `sira`/`son` BURADA TUTULMUYOR; onlar SW'nin `pkOmDurum` kaydında. İki
     yazar aynı alanı ezseydi duyuru iki kez giderdi. */
  function omBotDamga() {
    if (!omPopoutMu() || !scKanal) return;
    var k = scKanal;
    /* YETKİ YOKSA LİSTEYİ TAZELE: kullanıcı bu kanala yeni moderatör
       yapılmış olabilir ve elimizdeki liste eski. Bu tazeleme eskiden
       `otoMesajTur` içindeydi; zamanlayıcı service worker'a taşınınca o
       işlev çağrılmaz oldu ve tazeleme SESSİZCE kayboldu — sessiz kanalda
       bot bir daha hiç açılmazdı. (oluislev.js yakaladı.) */
    var yetkili = omYetkiliMi(k);
    if (!yetkili) omModListesi(null);
    var duyuru = settings.otoMesaj === true && yetkili &&
                 omAyar.aralikAcik && omAyar.kip === 'dk' && !!omAyar.metinler.length;
    var jeton = ''; try { jeton = rosterJeton(); } catch (e) {}
    var eski = (omBot && omBot[k]) || {};
    function yaz(kim) {
      omBotYaz(k, {
        beat: Date.now(),
        jeton: jeton,
        oda: (kim && (kim.oda || kim.kanal)) || eski.oda || 0,
        dk: omAyar.dk || 10,
        /* Duyuru kapalıysa liste BOŞ gider: kayıt yine de yazılıyor, çünkü
           "pencere yaşıyor mu" bilgisi duyurudan bağımsız (yalnız komut
           cevabı açık olabilir). SW boş listeyi atlıyor. */
        metinler: duyuru ? omAyar.metinler.slice(0, 20) : []
      });
    }
    if (duyuru) omOdaCoz(k, yaz); else yaz(null);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * İKİ AŞAMALI KURULUM
   *
   * Normal Kick sayfasında Otomatik Mesaj YALNIZ KANAL SORAR. Kanalı seçince
   * o kanalın bot penceresi açılıyor ve ayarların KALANI orada yapılıyor.
   *
   * Neden: bot zaten yalnız popout'ta koşuyor. Ayarı bir sayfada kurup başka
   * bir sayfada çalıştırmak, "kurdum ama çalışmıyor" durumlarının kaynağıydı;
   * artık ayarın kurulduğu yer ile koştuğu yer AYNI.
   *
   * Bot penceresinde ayar penceresi HEP AÇIK ve kapatma tuşu YOK: o pencere
   * botun kendisi, kapatma yolu sekmeyi kapatmak. Sürüklenebilir olduğu için
   * sohbeti görmek isteyen kenara çekebiliyor.
   *
   * Pencereler arası nota gerek YOK — popout kendi kanalını adresinden
   * biliyor (/popout/<kanal>/chat), yüklenince açıyor. Önce `pkOmAcilis` diye
   * bir depo notu tasarlamıştım; pencere zaten hep açık olunca gereksizdi.
   * ══════════════════════════════════════════════════════════════════════ */
  var omAcilisDeneme = 0;
  function omAcilisBak() {
    if (!omPopoutMu()) return;
    if (document.getElementById(OM_ID)) return;
    /* `scKanal` tur tarafından çözülüyor; sayfa yeni yüklendiyse henüz boş
       olabilir. Birkaç saniye deneyip bırakıyoruz. */
    if (!scKanal) {
      if (++omAcilisDeneme < 20) setTimeout(omAcilisBak, 500);
      return;
    }
    try { omPencereAc(); } catch (e) {}
  }

  /* Botun evi. Pencere ADLI: ikinci çağrı yenisini açmıyor, açık olanı öne
     getiriyor — kaydete her basışta yeni pencere yığılmasın. */
  function omPopoutAc(slug) {
    try {
      return window.open('https://kick.com/popout/' + encodeURIComponent(slug) + '/chat',
                         'pk-om-' + slug, 'width=460,height=820');
    } catch (e) { return null; }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * YETKİ — YALNIZ MOD OLUNAN VE KENDİ KANALINDA
   *
   * Otomatik mesaj sıradan bir izleyicinin elinde spam aracıdır; özellik
   * kanalda yetkisi olanlara kapalı tutuluyor (kullanıcı isteği).
   *
   * Kick bu listeyi TEK uçtan veriyor:
   *   GET /api/internal/v1/user/moderators
   * ÇEREZ YETMİYOR — `Authorization: Bearer <session_token>` şart, yoksa 401
   * dönüyor. Gelen `data.channels[]` içinde kullanıcının KENDİ kanalı da var,
   * bu yüzden ayrıca sahiplik sorgusu gerekmiyor.
   *
   * (Ters yön hâlâ yok: bir kanalın moderatör listesi hiçbir uçtan
   * alınamıyor, yalnız tek tek `is_moderator` sorulabiliyor.)
   * ══════════════════════════════════════════════════════════════════════ */
  var OM_MOD_TAZE = 1800000;              // 30 dk
  var omModKanal = null;                  // null = daha okunmadı, [] = boş
  var omModZaman = 0, omModIstek = false, omModBekleyen = [];

  function omModListesi(bitince) {
    if (bitince) omModBekleyen.push(bitince);
    if (omModKanal && Date.now() - omModZaman < OM_MOD_TAZE) return omModHaber();
    if (omModIstek) return;                // uçuştaki istek varsa sıraya girdik
    var jeton = ''; try { jeton = rosterJeton(); } catch (e) {}
    if (!jeton) { omModKanal = omModKanal || []; return omModHaber(); }
    omModIstek = true;
    fetch('https://kick.com/api/internal/v1/user/moderators',
          { credentials: 'include', cache: 'no-store',
            headers: { accept: 'application/json', Authorization: 'Bearer ' + jeton } })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        omModIstek = false;
        var l = (j && j.data && j.data.channels) || null;
        /* İstek başarısızsa ESKİ LİSTE KORUNUYOR: geçici bir ağ hatası
           yüzünden çalışan duyuruyu susturmak, yanlış kanala mesaj atmaktan
           farklı olarak sessiz bir kayıp olurdu. */
        if (l) {
          var out = [];
          for (var i = 0; i < l.length; i++) {
            var sl = l[i] && l[i].channel_slug;
            if (sl) out.push({ slug: pkKucult(sl), ad: String(l[i].user_username || sl) });
          }
          omModKanal = out; omModZaman = Date.now();
        }
        omModHaber();
      })
      .catch(function () { omModIstek = false; omModHaber(); });
  }
  function omModHaber() {
    var l = omModBekleyen; omModBekleyen = [];
    for (var i = 0; i < l.length; i++) { try { l[i](omModKanal || []); } catch (e) {} }
  }
  /* Liste GELMEDEN gönderim yok: bilinmeyen yetkiyi "var" saymak, yetkisiz
     kanalda mesaj atmak demek olurdu. En fazla bir tur gecikiyor. */
  function omYetkiliMi(k) {
    if (!k || !omModKanal) return false;
    for (var i = 0; i < omModKanal.length; i++) if (omModKanal[i].slug === k) return true;
    return false;
  }

  /* Eski tek kanallı kaydı BU kanala bir kereliğine taşır. Hangi kanal için
     yazıldığı bilinmediğinden hiçbir kanala kendiliğinden bağlanamaz; metinler
     de silinmesin diye taşıma yuvasında bekletiliyor. Anahtarlar KAPALI
     geliyor: taşınan bir ayarın habersiz mesaj atması en kötü sonuç olurdu. */
  function omTasimaUygula(hedef) {
    var e = omTum[OM_TASIMA];
    if (!e || !hedef) return;
    delete omTum[OM_TASIMA];
    if (!hedef.metinler.length && !hedef.komutlar.length) {
      hedef.metinler = e.metinler.slice();
      hedef.komutlar = e.komutlar.slice();
      hedef.kip = e.kip; hedef.dk = e.dk; hedef.adet = e.adet;
    }
    omKaydet();
  }

  /* Hiç dokunulmamış ayar. `omKanalAyari` girilen HER kanal için bir kayıt
     açıyor (ayar okunabilsin diye); bunlar diske de yazılsaydı gezilen her
     kanal depoda boş bir satır bırakırdı. */
  function omBosMu(a) {
    return !a || (!a.aralikAcik && !a.komutAcik &&
                  !a.metinler.length && !a.komutlar.length);
  }

  function omKaydet() {
    if (scKanal) omTum[scKanal] = omAyar;
    /* Boşlar elenip DÜZ nesneye kopyalanıyor: `omTum` prototipsiz, depoya
       olduğu gibi vermek yerine sıradan bir nesne göndermek daha güvenli. */
    var yaz = {};
    for (var k in omTum) { if (!omBosMu(omTum[k])) yaz[k] = omTum[k]; }
    try { chrome.storage.local.set({ pkOtoMesaj: yaz }); } catch (e) {}
  }

  /* Komut listesi artık METİNDEN AYRIŞTIRILMIYOR: arayüzde komut ve cevap
     ayrı alanlarda. "!k = c" biçimini kullanıcıya ezberletmek gereksizdi ve
     cevabın içinde eşittir geçince kafa karıştırıyordu. */
  function omMetinCoz(metin) {
    var ham = String(metin || '').split(/\r?\n/), out = [];
    for (var i = 0; i < ham.length; i++) { var a = ham[i].trim(); if (a) out.push(a); }
    return out;
  }

  /* Sohbet odası kimliği — gönderim adresinde bu var. KANAL BAŞINA çözülüp
     saklanıyor: gönderim artık "açık sekmenin kanalı"na değil, ADRESLENEN
     kanala gidiyor. Gönderim isteğinin kendisi zaten düz bir POST, o kanalın
     sekmesinin açık olmasına hiç gerek yok — eski kod sadece adresi mevcut
     sekmeden okuduğu için öyle sanılıyordu. */
  function omOdaCoz(kanal, bitince) {
    var k = pkKucult(kanal || '');
    if (!k) { bitince(null); return; }
    if (omOda[k]) { bitince(omOda[k]); return; }
    fetch('https://kick.com/api/v2/channels/' + encodeURIComponent(k),
          { credentials: 'include', cache: 'no-store', headers: { accept: 'application/json' } })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        omOda[k] = { oda: (j && j.chatroom && j.chatroom.id) || 0, kanal: (j && j.id) || 0 };
        bitince(omOda[k]);
      })
      .catch(function () { bitince(null); });
  }

  /* Tek mesaj gönderir. 404 gelirse İKİNCİ kimlikle bir kez daha dener:
     uçtaki sayının sohbet odası mı kanal mı olduğu Kick tarafında zaman zaman
     değişti; iki kimliği de elimizde tutup ölçmek yerine düşmeye bırakıyoruz. */
  function omGonderIstek(kimlik, metin) {
    var jeton = ''; try { jeton = rosterJeton(); } catch (e) {}
    var bas = { accept: 'application/json', 'content-type': 'application/json' };
    if (jeton) bas.Authorization = 'Bearer ' + jeton;
    return fetch('https://kick.com/api/v2/messages/send/' + encodeURIComponent(kimlik),
                 { method: 'POST', credentials: 'include', headers: bas,
                   body: JSON.stringify({ content: String(metin).slice(0, 500), type: 'message' }) })
      .then(function (r) { return { ok: r.ok, durum: r.status }; })
      .catch(function () { return { ok: false, durum: 0 }; });
  }
  function omGonder(metin, kanal, bitince) {
    if (!metin || !kanal) { if (bitince) bitince(false); return; }
    omOdaCoz(kanal, function (kim) {
      if (!kim || (!kim.oda && !kim.kanal)) { if (bitince) bitince(false); return; }
      omGonderIstek(kim.oda || kim.kanal, metin).then(function (r) {
        if (r.ok || r.durum !== 404 || !kim.kanal || kim.kanal === kim.oda) {
          if (bitince) bitince(r.ok);
          return;
        }
        omGonderIstek(kim.kanal, metin).then(function (r2) {
          if (r2.ok) kim.oda = kim.kanal;           // doğru kimliği öğrendik
          if (bitince) bitince(r2.ok);
        });
      });
    });
  }

  /* ══════════════════════════════════════════════════════════════════════
   * SEKMELER ARASI TEKİLLEŞTİRME
   *
   * Popout desteğiyle "aynı kanalın iki sekmesi" sıradan bir durum oldu:
   * kanalın kendi sayfası + popout penceresi. İkisi de aynı ayarı okuyup
   * duyuruyu ATTIĞI için mesaj iki kere giderdi.
   *
   * Damga `chrome.storage.local`'da, yani bütün sekmelerde ORTAK. Damga
   * GÖNDERMEDEN ÖNCE yazılıyor: ikinci sekme okuduğunda tazesini görüp geri
   * çekiliyor. Sonra yazsaydık ikisi de eskisini görüp aynı anda gönderirdi.
   * Kilit değil, çok kısa bir yarış penceresi kalıyor — turlar zaten farklı
   * anlarda başladığı için pratikte çakışmıyorlar, çakışsa da bedeli bir
   * fazladan mesaj; alternatifi her turda sistemli çift gönderimdi.
   * ══════════════════════════════════════════════════════════════════════ */
  function omKilitAl(kanal, anahtar, aralik, bitince) {
    try {
      chrome.storage.local.get('pkOmSon', function (res) {
        var h = (res && res.pkOmSon) || {};
        var kayit = h[kanal] || {};
        var simdi = Date.now();
        if (simdi - (kayit[anahtar] || 0) < aralik) { bitince(false); return; }
        kayit[anahtar] = simdi; h[kanal] = kayit;
        try { chrome.storage.local.set({ pkOmSon: h }); } catch (e) {}
        bitince(true);
      });
    } catch (e) { bitince(true); }      // depo yoksa tek sekme varsayımıyla çalış
  }

  /* Sıradaki duyuruyu gönder. Liste boşsa hiçbir şey yapmaz. */
  function omSiradakiniGonder() {
    var l = omAyar.metinler;
    if (!l.length) return;
    var metin = l[omSira % l.length];
    omSira = (omSira + 1) % l.length;
    omSonGonderim = Date.now();
    omMesajSayaci = 0;
    omGonder(metin, scKanal, null);
  }

  /* DAKİKA KİPİ BU SAYFADA GÖNDERMİYOR — bkz. background.js'teki alarm.
     Kapıların tamamı `omBotDamga` içinde; bir ara aynı kapıları tekrarlayan
     `otoMesajTur` ara işlevi vardı, zamanlayıcı SW'a taşınınca çağrılmaz
     olup ölü kaldı ve içindeki liste tazelemesini de yanında götürdü.
     (Mesaj sayısı kipi ayrı: sohbet olayına bağlı — bkz. otoMesajBak.) */

  /* Her sohbet mesajında çağrılıyor: mesaj sayacı ve komut cevabı. */
  function otoMesajBak(ad, pr) {
    if (settings.otoMesaj !== true) return;
    if (!scKanal) return;
    if (!omPopoutMu()) return;                 // bot yalnız popout'ta koşar
    /* KOMUT CEVABI VE SAYAÇ DA YETKİYE BAĞLI (bkz. omYetkiliMi). */
    if (!omYetkiliMi(scKanal)) { omModListesi(null); return; }
    /* KENDİ MESAJIMIZ hiçbir şeyi tetiklemiyor: ne sayaç ne komut. Sayaçta
       kendi duyurumuzu saymak aralığı kaydırır; komutta ise cevabın kendine
       cevap vermesi sonsuz döngü olur. */
    var ben = pkKucult(pkKendiAdim());
    var kendisi = !!(ad && ben && pkKucult(ad) === ben);

    if (omAyar.aralikAcik && omAyar.kip === 'mesaj' && omAyar.metinler.length && !kendisi) {
      omMesajSayaci++;
      if (omMesajSayaci >= (omAyar.adet || 50)) omSiradakiniGonder();
    }

    if (!omAyar.komutAcik || !omAyar.komutlar.length || kendisi) return;
    var yazi = pkKucult(topluToMetin(pr)).trim();
    if (!yazi) return;
    /* Komut mesajın BAŞINDA olmalı: cümle içinde geçen "!test" sohbet
       konusudur, komut değil. */
    for (var i = 0; i < omAyar.komutlar.length; i++) {
      var k = omAyar.komutlar[i].k;
      if (yazi !== k && yazi.indexOf(k + ' ') !== 0) continue;
      var simdi = Date.now();
      /* Aynı komuta arka arkaya cevap vermiyoruz — biri komutu spamlarsa
         bizi de spamcı yapardı. */
      if (simdi - (omKomutSon[k] || 0) < OM_KOMUT_BEKLEME) return;
      omKomutSon[k] = simdi;
      /* Komut cevabı da sekmeler arası tekil: iki pencere aynı komutu
         görüyor, cevap bir kere gitmeli. */
      var cevap = omAyar.komutlar[i].c, hangi = k;
      omKilitAl(scKanal, 'k:' + hangi, OM_KOMUT_BEKLEME, function (izin) {
        if (izin) omGonder(cevap, scKanal, null);
      });
      return;
    }
  }

  /* Kanal değişince sayaçlar sıfırlanır; ayarlar kalır. */
  function omSifirla() {
    omSonGonderim = 0; omMesajSayaci = 0; omSira = 0;
    omKomutSon = Object.create(null);
    /* `omOda` SİLİNMİYOR: kanal → kimlik eşlemesi kanal değişiminden bağımsız
       bir gerçek, yeniden sormak boşuna istek olurdu. */
    /* KANAL DEĞİŞTİ → AYAR DA DEĞİŞTİ. Pencere açık kalırsa üstünde eski
       kanalın değerleri durur ve Kaydet'e basılınca YENİ kanala yazardı. */
    omKanalaGec();
    omKapat();
  }

  function omKapat() { var p = document.getElementById(OM_ID); if (p) p.remove(); }

  /* Pencerede düzenlenecek kanal. Yetkisi olmayan kanal SEÇİLMİYOR — boş
     dönerse pencere "burada kullanamazsın" diyip adımları hiç açmıyor.
     Kullanıcıyı sessizce başka bir kanala geçirmiyoruz: ayarı yanlış kanala
     yazdığını fark etmeden kaydedebilirdi. */
  function omPencereKanal(istek) {
    if (istek && omYetkiliMi(istek)) return istek;
    return omYetkiliMi(scKanal) ? scKanal : '';
  }

  /* ══════════════════════════════════════════════════════════════════════
   * OTOMATİK MESAJ PENCERESİ
   *
   * Eskiden bütün denetimler aynı anda ekrandaydı: iki sütun, altı ayrı
   * kutu, hangisinin önce doldurulacağı belli değildi. Şimdi sıra belli:
   *   1) kanal → 2) nasıl gönderilsin → 3) aralık → 4) metin → Kaydet
   *
   * SIRA NUMARALI, AMA KİLİTLİ DEĞİL. Bir ara her adımı ayrı ayrı açan bir
   * sayaç (`omAdim`) ve bir "Devam" düğmesi vardı; kullanıcı gereksiz buldu —
   * bölümü açmak zaten bir adım, içinde ikinci bir tıklama zinciri fazladan
   * engeldi. Şimdi bölüm açılınca metin kutusu da geliyor.
   *
   * TEK GERÇEK BAĞIMLILIK aralık alanı: dakika mı mesaj sayısı mı olduğu kip
   * seçilmeden belli olmuyor, o yüzden 3. adım kip seçilene kadar duruyor.
   *
   * İki bölüm (aralıklı duyuru / komut cevapları) AÇILIR-KAPANIR: ikisi
   * bağımsız araç, biriyle işi olan ötekini kapatabiliyor.
   * ══════════════════════════════════════════════════════════════════════ */
  function omPencereAc(kanal) {
    if (document.getElementById(OM_ID)) return;
    omKanalaGec();
    /* DÜZENLENEN ayar ÇALIŞAN ayardan ayrı tutuluyor: `omAyar` bu sekmenin
       kanalını yürütüyor, pencere başka bir kanalı düzenliyor olabilir.
       `omAyar`ı oraya çevirseydik sekme öteki kanalın duyurusunu atardı.
       Aynı kanal seçiliyse ikisi zaten AYNI nesne (omKanalAyari haritadan
       veriyor), yani mevcut kanalda düzenleme yine anında yürürlüğe giriyor. */
    var listeVardi = !!omModKanal;
    /* BU SAYFA BOT PENCERESİ Mİ? Değilse pencere yalnız kanal soruyor:
       `omSecili` boş kalıyor ve `adimUygula` geri kalan her şeyi gizliyor. */
    var buPopout = omPopoutMu();
    var omSecili = buPopout ? omPencereKanal(scKanal) : '';
    var omDuzen = omSecili ? omKanalAyari(omSecili) : omBosAyar();
    omTasimaUygula(omSecili ? omDuzen : null);

    /* BOT ÇALIŞMIYORSA ANAHTARLAR KAPALI GELİR — ve öyle kaydedilir. Depodaki
       açık değer, kapatılmış bir botu çalışıyor gösteriyordu (kullanıcı
       bildirdi). Metinler ve komutlar DURUYOR; sıfırlanan yalnız "çalışıyor"
       bilgisi, kurulum değil.
       Pencerenin kendisi bot penceresiyse sıfırlama yok: canlı olduğu apaçık
       ve ilk damga henüz atılmamış olabilir. */
    var kendiBot = omPopoutMu() && omSecili && omSecili === scKanal;
    if (omSecili && !kendiBot && omBot && !omBotCanliMi(omSecili) &&
        (omDuzen.aralikAcik || omDuzen.komutAcik)) {
      omDuzen.aralikAcik = false; omDuzen.komutAcik = false;
      omKaydet();
    }

    /* Kurulu = daha önce kaydedilmiş bir şey var. Yeni kanalda kip seçili
       gelmesin diye kullanılıyor (bkz. kipSecildi). */
    var omKurulu = !!(omDuzen.metinler.length || omDuzen.komutlar.length);

    var perde = el('div', '');
    perde.id = OM_ID;
    /* Perde ölçüsü `pkPencereSerbest` içinde veriliyor — karartma yok. */
    var kk = el('div', 'bg-surface-base flex h-fit flex-col rounded text-sm shadow-lg gap-3 p-4');
    kk.style.cssText = 'width:820px;max-width:100%;max-height:86vh;overflow-y:auto;color:#fff';

    var bas = el('div', 'flex items-center justify-between gap-2');
    bas.appendChild(el('div', 'min-w-0 truncate text-base font-bold text-white', t('pkOmBaslik')));
    var kapatB = el('button', 'shrink-0 inline-flex items-center justify-center rounded state-layer-surface text-white');
    kapatB.style.cssText = 'width:32px;height:32px;padding:0;background:none;border:0;cursor:pointer';
    kapatB.innerHTML = I_KAPAT;
    kapatB.addEventListener('click', function (e) { e.stopPropagation(); omKapat(); });
    /* BOT PENCERESİNDE KAPATMA TUŞU YOK: bu pencere botun kendisi, kapatma
       yolu sekmeyi kapatmak (kullanıcı kararı). Kapatılabilseydi "bot açık mı
       kapalı mı" sorusunun iki ayrı cevabı olurdu. */
    if (!buPopout) bas.appendChild(kapatB);
    kk.appendChild(bas);
    kk.appendChild(el('div', 'text-xs text-subtle', t('pkOmAciklama')));
    if (!buPopout) kk.appendChild(el('div', 'text-xs text-subtle', t('pkOmSecAciklama')));

    /* ══════════ ADIM 1 — KANAL ══════════
       En üstte, çünkü geri kalan her şey seçilen kanala yazılıyor. Yalnız
       moderatör olunan ve kendi kanalları listeleniyor (bkz. omModListesi);
       açılır kutu yerine sarmalı düğme şeridi — pencerenin geri kalanı da
       aynı biçimde ve iki kanalda da yirmi kanalda da düzgün duruyor. */
    var knKap = el('div', 'bg-surface-lower rounded flex flex-col gap-2');
    knKap.style.cssText = 'padding:10px 12px';
    knKap.appendChild(el('div', 'text-sm font-semibold text-white', t('pkOmAdim1')));
    knKap.appendChild(el('div', 'text-xs text-subtle', t('pkOmKanalSecD')));
    /* AÇILIR LİSTE + FİLTRE. Önce sarmalı düğme şeridiydi; çok kanalda
       moderatörlüğü olan kullanıcıda şerit pencerenin yarısını kaplıyordu.
       Kapalıyken tek satır (seçili kanalın adı), açılınca arama kutusu ve
       liste. Seçim yoksa AÇIK başlıyor — ilk iş kanal seçmek. */
    var knListe = omModKanal || [];
    /* KAPALI BAŞLIYOR (kullanıcı isteği): kanal adları alt alta serilince
       pencere uzuyordu. Açılır satır tek satır yer kaplıyor. */
    var knAcik = false;

    var knAcJ = el('button', 'bg-surface-lower rounded flex w-full items-center justify-between gap-2');
    knAcJ.style.cssText = 'border:1px solid rgba(255,255,255,.12);cursor:pointer;padding:7px 10px';
    var knEtiket = el('span', 'text-sm font-semibold text-white truncate min-w-0', '');
    knAcJ.appendChild(knEtiket);
    var knOk = el('span', 'inline-flex shrink-0 text-subtle');
    knOk.style.cssText = 'width:16px;height:16px';
    knOk.innerHTML = I_CHEV;
    var knSv = knOk.querySelector('svg');
    if (knSv) { knSv.style.width = '16px'; knSv.style.height = '16px'; knSv.style.transition = 'transform .15s'; }
    knAcJ.appendChild(knOk);
    knKap.appendChild(knAcJ);

    var knGovde = el('div', 'flex flex-col gap-2');
    var knSer = el('div', 'flex flex-col gap-1');
    /* Yükseklik SATIR SAYISINA göre: en çok 7 kanal görünüyor, kalanı
       kaydırmayla geliyor (kullanıcı isteği).
       `tabindex` KLAVYE ARAMASI İÇİN: liste odak alabilsin ki harflere
       basılınca tuşlar buraya düşsün. */
    knSer.style.cssText = 'overflow-y:auto;overscroll-behavior:contain;outline:none';
    knSer.setAttribute('tabindex', '0');
    knGovde.appendChild(knSer);
    knKap.appendChild(knGovde);

    /* ══════════ KLAVİYEYLE ARAMA ══════════
       Arama kutusu kaldırıldı (kullanıcı isteği): iki kanalı olan kullanıcıda
       boşa yer kaplıyordu. Yerine harfe basınca o harfle başlayana atlama —
       dosya yöneticilerinin alışılmış davranışı.

       AYNI HARFE ARKA ARKAYA basmak o harfle başlayanlar arasında geziyor;
       tur başa sarıyor. Eşleşen düğme ODAKLANIYOR, seçilmiyor: odak hem
       görünür hale getiriyor (tarayıcı kaydırıyor) hem Enter'ı bedavaya
       getiriyor — odaklı düğme Enter'la kendiliğinden tıklanıyor.

       Türkçe küçültme `pkKucult` ile: JS'in `toLowerCase`'i 'İ' ve ı/i'de
       bozuk, "İbrahim" kanalına 'i' ile ulaşılamazdı. */
    var knSonHarf = '', knSonSira = -1;
    knSer.addEventListener('keydown', function (e) {
      var h = e.key;
      if (!h || h.length !== 1 || e.ctrlKey || e.altKey || e.metaKey) return;
      h = pkKucult(h);
      var dl = [].slice.call(knSer.querySelectorAll('[data-pk-kanal]'));
      if (!dl.length) return;
      var bas = (h === knSonHarf) ? knSonSira + 1 : 0;
      var bulundu = -1;
      for (var i = 0; i < dl.length; i++) {
        var j = (bas + i) % dl.length;
        if (pkKucult(dl[j].getAttribute('data-pk-kanal')).indexOf(h) === 0) { bulundu = j; break; }
      }
      /* Eşleşme yoksa hafızayı temizliyoruz: sonraki basış baştan arasın. */
      if (bulundu < 0) { knSonHarf = ''; knSonSira = -1; return; }
      e.preventDefault();
      knSonHarf = h; knSonSira = bulundu;
      try { dl[bulundu].focus(); } catch (er) {}
    });

    /* YÜKSEKLİK ÖLÇÜLEREK KURULUYOR, sabit piksel yazılmıyor: satır boyu
       Kick'in `text-sm` belirtecine bağlı ve değişebiliyor. Cross-ban
       listesinde sabit piksel kullanılmıştı ve satır arası boşluk hesaba
       katılmadığı için 5 yerine 4,6 satır görünüyordu — aynı hataya
       düşmemek için gerçek satırdan ölçüyoruz.
       ÖLÇÜM AÇILINCA yapılıyor: kapalıyken `display:none` ve yükseklik 0. */
    var KN_GORUNEN = 7;
    function knYukseklikKur() {
      var ilk = knSer.firstElementChild;
      if (!ilk || !ilk.offsetHeight) return;
      var bos = 4;
      try { bos = parseFloat(getComputedStyle(knSer).rowGap) || 4; } catch (e) {}
      knSer.style.maxHeight =
        Math.round(ilk.offsetHeight * KN_GORUNEN + bos * (KN_GORUNEN - 1)) + 'px';
    }

    function knSecAd() {
      for (var i = 0; i < knListe.length; i++) {
        if (knListe[i].slug === omSecili) return knListe[i].ad;
      }
      return omSecili;
    }
    function knCiz() {
      knSer.textContent = '';
      knListe.forEach(function (kn) {
        var se = kn.slug === omSecili;
        var kb = el('button', 'flex w-full items-center rounded text-sm font-semibold transition-all ' +
          (se ? 'bg-primary-base text-black' : 'bg-secondary-base text-white hover:brightness-125'), kn.ad);
        /* Klavye araması GÖRÜNEN ADA bakıyor: kullanıcı ekranda ne
           okuyorsa ona basıyor, slug'a değil. */
        kb.setAttribute('data-pk-kanal', kn.ad);
        kb.style.cssText = 'border:0;cursor:pointer;padding:6px 10px;text-align:left';
        kb.addEventListener('click', function (e) {
          e.preventDefault(); e.stopPropagation();
          /* SEÇİM = BAŞLAT. Bot penceresi açılıyor, ayarın kalanı orada
             yapılıyor. Burada devam etseydik ayarı bir sayfada kurup başka
             sayfada çalıştırmış olurduk — asıl karışıklık oydu. */
          omPopoutAc(kn.slug);
          omBaslatilan[kn.slug] = Date.now();
          omKapat();
          toast(t('pkTOmPencereAcildi', [kn.ad]));
        });
        knSer.appendChild(kb);
      });
      if (!knListe.length) {
        knSer.appendChild(el('div', 'text-xs text-subtle',
          t(listeVardi ? 'pkOmKanalYok' : 'pkCrossYukleniyor')));
      }
    }
    function knAcKapa(v) {
      knAcik = v;
      knGovde.style.display = v ? '' : 'none';
      if (knSv) knSv.style.transform = v ? 'rotate(90deg)' : '';
      knEtiket.textContent = omSecili ? knSecAd() : t('pkOmKanalSecin');
      /* Açılınca ODAK LİSTEYE: kullanıcı hemen harfe basabilsin. */
      if (v) {
        knYukseklikKur();                      // görünür olunca ölçülebiliyor
        try { knSer.focus(); } catch (e) {}
        knSonHarf = ''; knSonSira = -1;
      }
    }
    knAcJ.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      knAcKapa(!knAcik);
    });
    knCiz(); knAcKapa(knAcik);
    /* Bot penceresinde seçici görünmüyor: kanal pencerenin adresinden belli
       (/popout/<kanal>/chat), ikinci kez sormak yanıltıcı olurdu. */
    if (buPopout) knKap.style.display = 'none';
    if (omSecili && omSecili !== scKanal) {
      /* Başka kanalı düzenlerken gönderimin BURADAN çıkmayacağını söylüyoruz;
         yoksa kullanıcı ayarı kurup mesaj beklerken hiçbir şey olmuyor. */
      knKap.appendChild(el('div', 'text-xs text-danger', t('pkOmBaskaKanal', [omSecili])));
    } else if (buPopout && !omSecili && listeVardi) {
      /* Yalnız BOT PENCERESİNDE anlamlı: normal sayfada kullanıcı henüz kanal
         seçmedi, orada "yetkin yok" demek yanlış olurdu. */
      kk.appendChild(el('div', 'text-xs text-danger', t('pkOmYetkiYok')));
    }
    /* AYRI "arka planda çalıştır" DÜĞMESİ YOK: Kaydet zaten pencereyi açıyor,
       iki düğme aynı işi yapıyordu (kullanıcı kaldırılmasını istedi).
       Açıklaması Kaydet'in yanına taşındı — pencereyi açan şeyin dibinde. */
    kk.appendChild(knKap);

    /* ══════════ AÇILIR-KAPANIR BÖLÜM ══════════
       AÇ/KAPA ANAHTARI AYNI ZAMANDA KATLIYOR. Önce ayrı bir ok düğmesi vardı;
       anahtar ile ok aynı soruyu iki kere soruyordu ("özellik açık mı" /
       "ayarları görüyor muyum") ve ikisi ayrışabiliyordu. Tek kural kaldı:
       ANAHTAR AÇIKSA AYARLAR GÖRÜNÜR, kapalıysa bölüm hiç açılmıyor.

       Anahtar ANINDA KAYDEDİYOR: eskiden yalnız bellekte değişiyordu, sayfa
       yenilenince eski hâline dönüyordu ve kullanıcı kapattığını sanıyordu. */
    function katlanir(baslik, aciklama, deger, degisti) {
      var kap = el('div', 'bg-surface-lower rounded flex flex-col gap-2 min-w-0');
      kap.style.cssText = 'padding:10px 12px';
      var bsr = el('div', 'flex items-center justify-between gap-3');
      var yz = el('div', 'flex flex-col gap-0.5 min-w-0');
      yz.appendChild(el('div', 'text-sm font-semibold text-white', baslik));
      yz.appendChild(el('div', 'text-xs text-subtle', aciklama));
      bsr.appendChild(yz);
      var govde = el('div', 'flex flex-col gap-2 min-w-0');
      govde.style.cssText = 'margin-top:2px';
      function ciz(v) { govde.style.display = v ? '' : 'none'; }
      var sg = el('div', 'shrink-0');
      sg.appendChild(anahtar(deger, function (v) {
        degisti(v); ciz(v); omKaydet(); adimUygula();
      }));
      bsr.appendChild(sg);
      ciz(deger);
      kap.appendChild(bsr); kap.appendChild(govde);
      return { kap: kap, govde: govde };
    }

    /* Dar ekranda alt alta düşsün diye grid: sütun en az 300px, sığmazsa
       tek sütuna iniyor. Sabit iki sütun verseydik küçük pencerede taşardı. */
    var izgara = el('div', '');
    izgara.style.cssText = 'display:grid;gap:16px;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));' +
      'align-items:start';
    kk.appendChild(izgara);

    /* ══════════ ARALIKLI DUYURU ══════════ */
    var bolA = katlanir(t('pkOmAralik'), t('pkOmAralikD'), omDuzen.aralikAcik,
      function (v) { omDuzen.aralikAcik = v; });
    izgara.appendChild(bolA.kap);
    var sol = bolA.govde;

    /* ---- ADIM 2: nasıl gönderilsin ---- */
    var adim2 = el('div', 'flex flex-col gap-2');
    adim2.appendChild(el('div', 'text-xs font-semibold text-white', t('pkOmAdim2')));
    var kipSer = el('div', 'flex gap-2');
    /* YENİ KANALDA HİÇBİRİ SEÇİLİ DEĞİL. `kip` alanının varsayılanı 'dk'
       olduğu için ilk düğme seçili görünüyordu ve kullanıcı adımı atlanmış
       sanıyordu; seçim ayrıca izleniyor. */
    var kipSecildi = omKurulu;
    function kipBoya() {
      var b2 = kipSer.querySelectorAll('[data-pk-om-kip]');
      for (var i = 0; i < b2.length; i++) {
        var se = kipSecildi && b2[i].getAttribute('data-pk-om-kip') === omDuzen.kip;
        b2[i].className = 'flex-1 h-8 rounded text-xs font-semibold transition-all ' +
          (se ? 'bg-primary-base text-black' : 'bg-secondary-base text-white hover:brightness-125');
      }
      dkSer.style.display = kipSecildi && omDuzen.kip === 'dk' ? '' : 'none';
      adSer.style.display = kipSecildi && omDuzen.kip === 'mesaj' ? '' : 'none';
    }
    [['dk', t('pkOmKipDk')], ['mesaj', t('pkOmKipMesaj')]].forEach(function (p) {
      var b2 = el('button', '', p[1]);
      b2.setAttribute('data-pk-om-kip', p[0]);
      b2.style.cssText = 'border:0;cursor:pointer';
      b2.addEventListener('click', function (e) {
        e.preventDefault(); e.stopPropagation();
        omDuzen.kip = p[0]; kipSecildi = true;
        kipBoya(); adimUygula();
      });
      kipSer.appendChild(b2);
    });
    adim2.appendChild(kipSer);
    sol.appendChild(adim2);

    /* ---- ADIM 3: aralık ---- */
    var adim3 = el('div', 'flex flex-col gap-2');
    adim3.appendChild(el('div', 'text-xs font-semibold text-white', t('pkOmAdim3')));
    var dkSer = satir(t('pkOmDk'), t('pkOmDkD', [String(OM_DK_MIN)]),
      sayiAlani(omDuzen.dk, OM_DK_MIN, OM_DK_MAX, function (v) { omDuzen.dk = v; }));
    var adSer = satir(t('pkOmAdet'), t('pkOmAdetD', [String(OM_ADET_MIN)]),
      sayiAlani(omDuzen.adet, OM_ADET_MIN, OM_ADET_MAX, function (v) { omDuzen.adet = v; }));
    adim3.appendChild(dkSer); adim3.appendChild(adSer);
    sol.appendChild(adim3);

    /* ---- ADIM 4: duyuru metni ---- */
    var adim4 = el('div', 'flex flex-col gap-2');
    adim4.appendChild(el('div', 'text-xs font-semibold text-white', t('pkOmAdim4')));
    adim4.appendChild(el('div', 'text-xs text-subtle', t('pkOmMetinD')));
    var metinAlan = el('textarea', 'bg-surface-lower rounded text-sm text-white');
    metinAlan.rows = 5;
    metinAlan.placeholder = t('pkOmMetinIpucu');
    metinAlan.value = omDuzen.metinler.join('\n');
    metinAlan.style.cssText = 'padding:6px 8px;border:1px solid rgba(255,255,255,.12);' +
      'outline:none;resize:vertical;min-height:96px;font-family:inherit';
    adim4.appendChild(metinAlan);

    /* AYRI "İLK DUYURUYU ŞİMDİ GÖNDER" DÜĞMESİ YOK: kurulumu bitirip kaydeden
       kullanıcı zaten ilk duyurunun gitmesini bekliyor, ikinci bir düğmeye
       basması gerekmiyordu (kullanıcı isteği). Gönderim artık Kaydet'in içinde. */
    metinAlan.addEventListener('input', function () { adimUygula(); });
    sol.appendChild(adim4);

    /* ══════════ KOMUT CEVAPLARI ══════════ */
    var bolK = katlanir(t('pkOmKomut'), t('pkOmKomutD'), omDuzen.komutAcik,
      function (v) { omDuzen.komutAcik = v; });
    izgara.appendChild(bolK.kap);
    var sag = bolK.govde;

    /* ÖNCE KOMUT, ALTINDA CEVABI — iki ayrı alan. Tek satırda "!k = c"
       yazdırmak kullanıcıya bir biçim ezberletiyordu. */
    var kAd = el('input', 'bg-surface-lower rounded text-sm text-white');
    kAd.type = 'text'; kAd.maxLength = 40;
    kAd.placeholder = t('pkOmKomutAdIpucu');
    kAd.style.cssText = 'padding:6px 8px;border:1px solid rgba(255,255,255,.12);outline:none';
    sag.appendChild(el('div', 'text-xs text-subtle', t('pkOmKomutAd')));
    sag.appendChild(kAd);

    var kCevap = el('textarea', 'bg-surface-lower rounded text-sm text-white');
    kCevap.rows = 2; kCevap.maxLength = 400;
    kCevap.placeholder = t('pkOmKomutCevapIpucu');
    kCevap.style.cssText = 'padding:6px 8px;border:1px solid rgba(255,255,255,.12);' +
      'outline:none;resize:vertical;min-height:44px;font-family:inherit';
    sag.appendChild(el('div', 'text-xs text-subtle', t('pkOmKomutCevap')));
    sag.appendChild(kCevap);

    var ekleB = btnIkincil(t('pkOmEkle'));
    sag.appendChild(ekleB);

    /* KAYITLI KOMUTLAR EN ALTTA, kendi başlığıyla: eklediğin şeyin nereye
       gittiği görünsün. Liste kaydırmalı — 40 komutta pencere uzamasın. */
    sag.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));
    /* KURAL METNİ TAM BURADA: pencerenin en altındaydı, komutla ilgisi olan
       tek yer burası olduğu hâlde araya iki bölüm giriyordu (kullanıcı
       taşınmasını istedi). */
    sag.appendChild(el('div', 'text-xs text-subtle', t('pkOmNot')));
    sag.appendChild(el('div', 'text-sm font-semibold text-white', t('pkOmKayitli')));
    var kListe = el('div', 'flex flex-col gap-1');
    kListe.style.cssText = 'max-height:190px;overflow-y:auto;overscroll-behavior:contain';
    sag.appendChild(kListe);

    function komutListeCiz() {
      kListe.textContent = '';
      if (!omDuzen.komutlar.length) {
        kListe.appendChild(el('div', 'text-xs text-subtle', t('pkOmKomutYok')));
        return;
      }
      omDuzen.komutlar.forEach(function (x, i) {
        var r = el('div', 'bg-surface-highest rounded flex items-start gap-2');
        r.style.cssText = 'padding:5px 8px';
        var ic = el('div', 'flex flex-col gap-0.5 min-w-0 flex-1');
        ic.appendChild(el('div', 'text-sm font-semibold text-primary-base', x.k));
        var cv = el('div', 'text-xs text-subtle', x.c);
        cv.style.cssText = 'word-break:break-word';
        ic.appendChild(cv);
        r.appendChild(ic);
        var sil = el('button', 'shrink-0 inline-flex items-center justify-center rounded state-layer-surface text-white');
        sil.style.cssText = 'width:22px;height:22px;padding:0;background:none;border:0;cursor:pointer';
        sil.innerHTML = I_KAPAT;
        sil.title = t('pkOmKomutSil');
        sil.addEventListener('click', function (e) {
          e.preventDefault(); e.stopPropagation();
          omDuzen.komutlar.splice(i, 1);
          omKaydet(); komutListeCiz(); ozet(); adimUygula();
        });
        r.appendChild(sil);
        kListe.appendChild(r);
      });
    }

    function komutEkle() {
      var k = pkKucult(String(kAd.value || '').trim());
      var c = String(kCevap.value || '').trim();
      if (!k || !c) { toast(t('pkTOmKomutEksik')); return; }
      /* Komut BOŞLUK İÇEREMEZ: eşleştirme mesajın başındaki ilk sözcüğe
         bakıyor, boşluklu bir komut hiç eşleşmezdi. */
      if (/\s/.test(k)) { toast(t('pkTOmKomutBosluk')); return; }
      /* Aynı komut varsa CEVABI GÜNCELLENİYOR. İkinci bir kayıt eklemek
         hangisinin geçerli olduğunu belirsiz bırakırdı. */
      var eski = null;
      for (var i = 0; i < omDuzen.komutlar.length; i++) {
        if (omDuzen.komutlar[i].k === k) { eski = omDuzen.komutlar[i]; break; }
      }
      if (eski) eski.c = c; else omDuzen.komutlar.push({ k: k, c: c });
      kAd.value = ''; kCevap.value = '';
      omKaydet(); komutListeCiz(); ozet(); adimUygula();
      toast(eski ? t('pkTOmKomutGuncellendi') : t('pkTOmKomutEklendi'));
    }
    ekleB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation(); komutEkle();
    });
    /* Komut alanında Enter da ekliyor — cevap alanında Enter satır atlıyor,
       çok satırlı cevap yazabilmek için. */
    kAd.addEventListener('keydown', function (ev) {
      if (ev.key === 'Enter') { ev.preventDefault(); komutEkle(); }
    });

    /* ══════════ ALT ŞERİT ══════════ */
    var ayrac = el('div', 'h-px shrink-0 bg-outline-decorative w-full');
    kk.appendChild(ayrac);
    var altSerit = el('div', 'flex items-center justify-between gap-3 flex-wrap');
    var ozetY = el('div', 'text-xs text-subtle', '');
    function ozet() {
      ozetY.textContent = t('pkOmOzet',
        [String(omDuzen.metinler.length), String(omDuzen.komutlar.length)]);
    }
    altSerit.appendChild(ozetY);

    var kaydetB = btnBirincil(t('pkKaydet'));

    /* KAYDET = BOTU BAŞLAT. Ayarı yazıyor, ilk duyuruyu gönderiyor ve bot
       penceresini açıyor. */
    function kaydetUygula() {
      /* Komutlar EKLE düğmesiyle zaten kaydedildi; burada yalnız duyuru
         listesi ve kip/aralık seçimleri diske iniyor. */
      omDuzen.metinler = omMetinCoz(metinAlan.value);
      metinAlan.value = omDuzen.metinler.join('\n');
      /* Sayaçlar YALNIZ çalışan kanal düzenlenirken sıfırlanıyor; başka bir
         kanalın ayarını kaydetmek bu sekmenin duyuru saatini kaydırmamalı. */
      if (omSecili === scKanal) { omSonGonderim = Date.now(); omMesajSayaci = 0; omSira = 0; }
      omKaydet(); ozet(); adimUygula();
      toast(t('pkTOmKaydedildi'));

      /* KAYDETTE MESAJ GİTMİYOR (kullanıcı isteği). Bir ara ilk duyuru
         hemen atılıyordu — "kurdum, çalışıyor mu?" sorusunu anında
         cevaplıyordu ama ayarı her düzeltişte sohbete mesaj düşürüyordu.
         İlk duyuru artık ilk aralığın sonunda gidiyor: SW kanalı ilk
         gördüğünde sayacı kuruyor, göndermiyor (bkz. background.js). */
      /* PENCERE AÇILMIYOR — Kaydet yalnız bot penceresinin içinde görünüyor,
         yani ZATEN oradayız. `window.open` aynı pencere adıyla çağrılınca bu
         pencerenin KENDİSİNİ yeniden yükler ve botu kesintiye uğratırdı. */
      /* KAYDEDİNCE AYAR PENCERESİ KAPANIYOR: işi bitti, altındaki sohbet
         görünsün. Bot DURMUYOR — botu durduran şey sekmenin kapanması, bu
         pencerenin değil. Yeniden açmak gerekirse PureKick menüsündeki
         'Otomatik Mesaj' satırı burada da duruyor. */
      omKapat();
    }

    kaydetB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (kaydetEksik()) return;                 // düğme zaten soluk; emniyet
      if (omOnaySus) { kaydetUygula(); return; }
      var duyurulu = omDuzen.aralikAcik && omMetinCoz(metinAlan.value).length;
      omOnayPenceresi(t(duyurulu ? 'pkOmOnayMetin' : 'pkOmOnayKomut', [omSecili]),
                      kaydetUygula);
    });
    altSerit.appendChild(kaydetB);
    kk.appendChild(altSerit);
    /* Düğme neden soluk — hemen altında. Sebebi söylemeden kilitlemek
       kullanıcıyı "bozuk mu?" diye bırakırdı. */
    var eksikY = el('div', 'text-xs text-danger', '');
    kk.appendChild(eksikY);
    /* Pencereyi AÇAN şeyin dibinde duruyor: Kaydet'in botu başlattığını ve
       pencereyi kapatmanın onu durdurduğunu burada söylüyoruz. */
    /* Kaydet ile BİRLİKTE görünüyor: düğme yokken "Kaydet'e basınca..."
       diye anlatmak olmayan bir düğmeyi tarif etmek olurdu. */
    var popNot = el('div', 'text-xs text-subtle', t('pkOmPopoutD'));
    kk.appendChild(popNot);

    /* Hangi adımlar görünür. Kaydet komut eklendiğinde de çıkıyor: yalnız
       komut cevabı kuran kullanıcı duyuru sihirbazını bitirmeden kaydedebilsin
       diye — yoksa kaydet düğmesine hiç ulaşamazdı. */
    /* KAYDET NE ZAMAN ÇIKAR, NE ZAMAN ÇALIŞIR
       · İKİ BÖLÜM DE KAPALI → düğme hiç yok. Ortada başlatılacak bir bot
         yok; "kaydet" göstermek bir şey yaptığı izlenimi verirdi.
       · AÇIK BÖLÜMÜN İÇİ BOŞ → düğme soluk ve tıklanmıyor. Boş listeyle
         kaydetmek hiç mesaj atmayan bir bot demek; eksiğin ne olduğu düğmenin
         altında yazıyor.
       · İÇİ DOLU → etkin. */
    function kaydetEksik() {
      /* KİP SEÇİMİ ÖNCE. `kip` alanının varsayılanı 'dk' olduğu için hiç
         seçilmese bile geçerli bir değer duruyordu ve kaydetmek çalışıyordu;
         kullanıcı seçmediği bir aralık kipiyle bot başlatmış oluyordu. Seçim
         `kipSecildi` ile ayrıca izleniyor — bkz. kipBoya. */
      if (omDuzen.aralikAcik && !kipSecildi) return t('pkOmEksikKip');
      if (omDuzen.aralikAcik && !omMetinCoz(metinAlan.value).length) return t('pkOmEksikMetin');
      if (omDuzen.komutAcik && !omDuzen.komutlar.length) return t('pkOmEksikKomut');
      return '';
    }

    function adimUygula() {
      izgara.style.display = omSecili ? '' : 'none';
      /* 3. adım TEK İSTİSNA: hangi birimin (dakika / mesaj sayısı) sorulacağı
         kip seçilmeden bilinmiyor. 2 ve 4 bölüm açılır açılmaz duruyor. */
      adim3.style.display = kipSecildi ? '' : 'none';

      var acikVar = !!(omDuzen.aralikAcik || omDuzen.komutAcik);
      var goster = !!omSecili && acikVar;
      altSerit.style.display = goster ? '' : 'none';
      ayrac.style.display = goster ? '' : 'none';
      popNot.style.display = goster ? '' : 'none';

      var eksik = goster ? kaydetEksik() : '';
      kaydetB.disabled = !!eksik;
      kaydetB.style.opacity = eksik ? '.4' : '';
      kaydetB.style.cursor = eksik ? 'not-allowed' : '';
      eksikY.textContent = eksik;
      eksikY.style.display = eksik ? '' : 'none';
    }

    komutListeCiz();
    ozet();
    kipBoya();
    adimUygula();

    /* Liste açılışta yoksa gelince pencere BİR KEZ yeniden çiziliyor.
       `listeVardi` kapısı şart: yeniden çizimde liste artık var, yoksa
       istek → çizim → istek diye sonsuz döngü olurdu. */
    if (!listeVardi) {
      omModListesi(function () {
        if (document.getElementById(OM_ID)) { omKapat(); omPencereAc(omSecili); }
      });
    }

    pkPencereSerbest(perde, kk, bas);
    perde.appendChild(kk);
    pkKatmanaEkle(perde);
  }

  function topluToKapat() {
    var p = document.getElementById(TOPLU_TO_ID);
    if (p) p.remove();
    topluToYaz = null;
    /* PENCERE KAPANINCA SAYAÇ VE "GÖRÜLENLER" SIFIRLANIYOR.
       Görülenler listesi aynı kişiyi üst üste susturmamak içindi ama oturum
       boyunca kalıcıydı: birinin cezasını kaldırıp o kişi yeniden spam
       attığında BİR DAHA susturulmuyordu (kullanıcı bildirdi). Pencereyi
       kapatmak artık temiz sayfa demek — canlı izleme sürüyorsa aynı kişiyi
       yeniden yakalayabiliyor. Tekrar koruması pencere açık kaldığı sürece
       geçerli; asıl işi orada zaten görüyor. */
    topluToGorulen = Object.create(null);
    topluToSayac = 0;
  }

  function topluToPencereAc() {
    if (document.getElementById(TOPLU_TO_ID)) return;
    if (!scKanal) { toast(t('pkTSohbetAlaniYok')); return; }

    var perde = el('div', '');
    perde.id = TOPLU_TO_ID;
    /* Perde ölçüsü `pkPencereSerbest` içinde veriliyor — karartma yok. */
    var kk = el('div', 'bg-surface-base flex h-fit flex-col rounded text-sm shadow-lg gap-3 p-4');
    kk.style.cssText = 'width:460px;max-width:100%;max-height:82vh;overflow-y:auto;color:#fff';

    var bas = el('div', 'flex items-center justify-between gap-2');
    bas.appendChild(el('div', 'min-w-0 truncate text-base font-bold text-white', t('pkToBaslik')));
    var kapatB = el('button', 'shrink-0 inline-flex items-center justify-center rounded state-layer-surface text-white');
    kapatB.style.cssText = 'width:32px;height:32px;padding:0;background:none;border:0;cursor:pointer';
    kapatB.innerHTML = I_KAPAT;
    kapatB.addEventListener('click', function (e) { e.stopPropagation(); topluToKapat(); });
    bas.appendChild(kapatB);
    kk.appendChild(bas);
    kk.appendChild(el('div', 'text-xs text-subtle', t('pkToAciklama')));
    kk.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));

    /* ---- tetikleyiciler ---- */
    kk.appendChild(el('div', 'text-sm font-semibold text-white', t('pkToTetik')));
    kk.appendChild(el('div', 'text-xs text-subtle', t('pkToTetikD')));
    var alan = el('textarea', 'bg-surface-lower rounded text-sm text-white');
    alan.rows = 4;
    alan.placeholder = t('pkToTetikIpucu');
    alan.value = topluToTetik.join('\n');
    alan.style.cssText = 'padding:6px 8px;border:1px solid rgba(255,255,255,.12);' +
      'outline:none;resize:vertical;min-height:64px;font-family:inherit';
    kk.appendChild(alan);

    kk.appendChild(satir(t('pkToTamKelime'), t('pkToTamKelimeD'),
      anahtar(topluToTamKelime, function (v) { topluToTamKelime = v; })));

    /* ---- süre: hızlı susturma listesinden ---- */
    kk.appendChild(el('div', 'text-sm font-semibold text-white', t('pkToSure')));
    kk.appendChild(el('div', 'text-xs text-subtle', t('pkToSureD')));
    var sureSer = el('div', 'flex flex-wrap gap-2');
    var sureler = susSureListesi();
    if (sureler.indexOf(topluToDk) < 0) topluToDk = sureler[0];
    function sureBoya() {
      var b2 = sureSer.querySelectorAll('[data-pk-to-dk]');
      for (var i = 0; i < b2.length; i++) {
        var se = Number(b2[i].getAttribute('data-pk-to-dk')) === topluToDk;
        b2[i].className = 'h-9 px-3 rounded font-semibold text-sm shrink-0 transition-all ' +
          (se ? 'bg-primary-base text-black' : 'bg-secondary-base text-white hover:brightness-125');
      }
    }
    sureler.forEach(function (dk) {
      var b2 = el('button', '', sureKisa(dk));
      b2.setAttribute('data-pk-to-dk', String(dk));
      b2.style.cssText = 'border:0;cursor:pointer';
      b2.addEventListener('click', function (e) {
        e.preventDefault(); e.stopPropagation();
        topluToDk = dk; sureBoya();
      });
      sureSer.appendChild(b2);
    });
    sureBoya();
    kk.appendChild(sureSer);

    /* ---- sebep ---- */
    var sr = el('div', 'flex flex-col gap-1');
    sr.appendChild(el('div', 'text-xs text-subtle', t('pkCrossSebep')));
    var sg = el('input', 'bg-surface-lower rounded text-sm text-white');
    sg.type = 'text'; sg.maxLength = 100;
    sg.placeholder = t('pkCrossSebepIpucu');
    sg.value = topluToSebep;
    sg.style.cssText = 'padding:6px 8px;border:1px solid rgba(255,255,255,.12);outline:none';
    sr.appendChild(sg);
    kk.appendChild(sr);

    kk.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));

    /* ---- sayaç + çubuk ---- */
    var durumY = el('div', 'text-xs text-subtle', t('pkToSayac', [String(topluToSayac)]));
    kk.appendChild(durumY);
    var barKap = el('div', 'w-full rounded overflow-hidden shrink-0');
    barKap.style.cssText = 'height:8px;background:rgba(255,255,255,.10)';
    var barDolgu = el('div', '');
    barDolgu.style.cssText = 'height:100%;width:0%;background:rgb(83,252,24);transition:width .18s ease';
    barKap.appendChild(barDolgu);
    kk.appendChild(barKap);
    topluToYaz = function () { durumY.textContent = t('pkToSayac', [String(topluToSayac)]); };

    /* ---- düğmeler ---- */
    function ayarlariAl() {
      topluToTetik = topluToCoz(alan.value);
      topluToSebep = String(sg.value || '').trim();
      return topluToTetik.length;
    }

    var gecmisB = el('button', 'bg-secondary-base hover:brightness-125 text-white h-9 rounded font-semibold text-sm', t('pkToGecmis'));
    gecmisB.style.cssText = 'border:0;cursor:pointer';
    gecmisB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (!ayarlariAl()) { toast(t('pkTToTetikGir')); return; }
      var adlar = topluToGecmisAdlar();
      if (!adlar.length) { toast(t('pkTToEslesmeYok')); return; }
      gecmisB.disabled = true;
      barDolgu.style.width = '0%';
      crossObekGonder(adlar,
        function (a) { return topluToSustur(a); },
        function (bitti, toplam) { barDolgu.style.width = Math.round(bitti / toplam * 100) + '%'; },
        function () { return false; }
      ).then(function (r) {
        gecmisB.disabled = false;
        barDolgu.style.width = '100%';
        durumY.textContent = t('pkCrossBitti', [String(r.basarili), String(r.hatali)]);
      });
    });
    kk.appendChild(gecmisB);

    var canliB = el('button', '', '');
    canliB.style.cssText = 'border:0;cursor:pointer';
    function canliBoya() {
      canliB.textContent = topluToAcik ? t('pkToDurdur') : t('pkToBaslat');
      canliB.className = 'h-9 rounded font-semibold text-sm hover:brightness-110 transition-all';
      /* RENK SATIR İÇİ, sınıfla DEĞİL. `bg-danger-base` diye bir sınıf Kick'in
         yapısında yok — yazınca düğme zeminsiz kalıyordu (kullanıcı bildirdi).
         Kırmızı, Kick'in `text-danger-base` tonundan ölçülmüş #fb1818;
         yeşil, birincil renk. Durdurma düğmesinin kırmızı olması şart:
         canlı kip arka planda susturma yapıyor, "bu açık" uyarısı görünür
         olmalı. */
      canliB.style.background = topluToAcik ? '#fb1818' : 'rgb(83,252,24)';
      canliB.style.color = topluToAcik ? '#fff' : '#000';
    }
    canliBoya();
    canliB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (topluToAcik) { topluToAcik = false; canliBoya(); toast(t('pkTToDurdu')); return; }
      if (!ayarlariAl()) { toast(t('pkTToTetikGir')); return; }
      topluToAcik = true; canliBoya();
      toast(t('pkTToBasladi', [String(topluToTetik.length)]));
    });
    kk.appendChild(canliB);
    kk.appendChild(el('div', 'text-xs text-subtle', t('pkToNot')));

    pkPencereSerbest(perde, kk, bas);
    perde.appendChild(kk);
    pkKatmanaEkle(perde);
  }

  function crossHaricKaydet() {
    try { chrome.storage.local.set({ pkCrossHaric: Object.keys(crossHaric) }); } catch (e) {}
  }

  /* ══════════════════════════════════════════════════════════════════════════
   * CROSS-BAN PENCERESİ
   *
   * Tek ekranda: işlem türü · süre · sebep · kanallar arası bekleme · kanal
   * listesi · ilerleme. Onay ayrı bir adım DEĞİL — kullanıcı kanalları görüp
   * "Başlat"a basıyor, yani gördüğünü onaylamış oluyor.
   *
   * BEKLEME NEDEN VAR: N kanal = N istek. Art arda atılırsa Kick 429 veriyor
   * (bu projede daha önce yaşandı, bkz. rozet API'si). Varsayılan 1 sn;
   * kullanıcı ister düşürür ister yükseltir.
   * ══════════════════════════════════════════════════════════════════════════ */
  var CROSS_ID = 'pk-cross';
  /* İMZA — ban kaydında bu işlemin PureKick'ten geldiği görünsün. Hedef
     kanalın öteki moderatörleri sebep alanından başka bir şey görmüyor;
     nereden geldiğini ve kimin yaptığını buraya yazmazsak ban gökten
     inmiş gibi duruyor. */
  var PK_CROSS_IMZA = 'Automated by PureKick.';
  /* Sebep yazılmadığında kullanılan uzun biçim. Kaynak kanal ve işlemi
     yapan kişi ayrı ayrı yazılıyor: ikisi çoğu zaman farklı (moderatör,
     modu olduğu bir kanaldan başlatıyor). */
  function crossImzaTam(kaynakKanal, yapan) {
    return 'Cross-Ban. Ban origin is ' + (kaynakKanal || '?') +
           '. Banned by ' + (yapan || '?') + '. ' + PK_CROSS_IMZA;
  }
  /* Listeler SATIR SAYISINA göre sınırlı, piksele göre değil: 100 kanalda
     modluğu olan kullanıcıda pencere ekranı aşıyordu. Kalanı kaydırma ile
     geliyor — hepsi DOM'da duruyor, yalnız görünen yükseklik sabit. */
  var CROSS_GORUNEN = 5;                  // aynı anda görünen satır sayısı
  /* ÖLÇÜLDÜ (tarayıcıda): kanal satırı 27px, günlük satırı 18px, aradaki
     boşluk 4px (`gap-1`). Boşluğu hesaba katmayan ilk sürüm 5 yerine 4,6
     satır gösteriyordu. */
  var CROSS_SATIR_PX = 27;
  var CROSS_LOG_PX = 18;
  var CROSS_ARA_PX = 4;
  function crossYukseklik(satirPx) {
    return (CROSS_GORUNEN * satirPx) + ((CROSS_GORUNEN - 1) * CROSS_ARA_PX);
  }

  function crossKapat() {
    var p = document.getElementById(CROSS_ID);
    if (p) p.remove();
    crossDur = true;
  }

  /* `ad` verilirse TEK KİŞİ kipi (profil kartından), verilmezse TOPLU kip
     (ayar menüsünden). İkisi aynı pencereyi kullanıyor: aynı işlem türleri,
     aynı kanal listesi, aynı ilerleme günlüğü. Ayrı bir pencere yazmak iki
     yerde bakım demekti ve ikisi zamanla ayrışırdı. */
  function crossPencereAc(ad) {
    if (document.getElementById(CROSS_ID)) return;
    var toplu = !ad;
    var perde = el('div', '');
    perde.id = CROSS_ID;
    /* Perde ölçüsü `pkPencereSerbest` içinde veriliyor — karartma yok. */
    var kart = el('div', 'bg-surface-base flex h-fit flex-col rounded text-sm shadow-lg gap-3 p-4');
    kart.style.cssText = 'width:460px;max-width:100%;max-height:82vh;overflow-y:auto;color:#fff';

    var bas = el('div', 'flex items-center justify-between gap-2');
    bas.appendChild(el('div', 'min-w-0 truncate text-base font-bold text-white',
      toplu ? t('pkCrossTopluBaslik') : t('pkCrossBaslik', [ad])));
    var kapatB = el('button', 'shrink-0 inline-flex items-center justify-center rounded state-layer-surface text-white');
    kapatB.style.cssText = 'width:32px;height:32px;padding:0;background:none;border:0;cursor:pointer';
    kapatB.innerHTML = I_KAPAT;
    kapatB.addEventListener('click', function (e) { e.stopPropagation(); crossKapat(); });
    bas.appendChild(kapatB);
    kart.appendChild(bas);
    kart.appendChild(el('div', 'text-xs text-subtle',
      toplu ? t('pkCrossTopluAciklama') : t('pkCrossAciklama')));
    kart.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));

    /* ---- TOPLU: kullanıcı adları ---- */
    var adAlani = null, adSayacY = null;
    function topluAdlar() { return adAlani ? crossAdlariCoz(adAlani.value) : [ad]; }
    if (toplu) {
      var as = el('div', 'flex flex-col gap-1');
      as.appendChild(el('div', 'text-xs text-subtle', t('pkCrossTopluAdlar')));
      adAlani = el('textarea', 'bg-surface-lower rounded text-sm text-white');
      adAlani.rows = 4;
      adAlani.placeholder = t('pkCrossTopluIpucu');
      adAlani.style.cssText = 'padding:6px 8px;border:1px solid rgba(255,255,255,.12);' +
        'outline:none;resize:vertical;min-height:64px;font-family:inherit';
      as.appendChild(adAlani);
      /* Kaç ad anladığımızı ANINDA gösteriyoruz: kullanıcı virgülü mü satır
         sonunu mu kullandığını merak etmesin, sayıyı görüp emin olsun. */
      adSayacY = el('div', 'text-xs text-subtle', '');
      as.appendChild(adSayacY);
      adAlani.addEventListener('input', function () {
        var n = topluAdlar().length;
        adSayacY.textContent = n ? t('pkCrossTopluSayi', [String(n)]) : '';
      });
      kart.appendChild(as);
      kart.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));
    }

    /* ---- işlem türü ---- */
    var tur = 'yasak';
    var turSatir = el('div', 'flex gap-2');
    [['yasak', t('pkCrossYasakla')], ['sustur', t('pkCrossSustur')], ['kaldir', t('pkCrossKaldir')]]
      .forEach(function (p2) {
        var b = el('button', 'flex-1 h-8 rounded text-xs font-semibold transition-all', p2[1]);
        b.setAttribute('data-pk-cross-tur', p2[0]);
        b.style.cssText = 'border:0;cursor:pointer';
        b.addEventListener('click', function (e) {
          e.preventDefault(); e.stopPropagation();
          tur = p2[0]; turBoya(); sureSatir.style.display = (tur === 'sustur') ? '' : 'none';
          sebepSatir.style.display = (tur === 'kaldir') ? 'none' : '';
        });
        turSatir.appendChild(b);
      });
    function turBoya() {
      var hepsi = turSatir.querySelectorAll('[data-pk-cross-tur]');
      for (var i = 0; i < hepsi.length; i++) {
        var s = hepsi[i].getAttribute('data-pk-cross-tur') === tur;
        hepsi[i].className = 'flex-1 h-8 rounded text-xs font-semibold transition-all ' +
          (s ? 'bg-primary-base text-black' : 'bg-secondary-base text-white hover:brightness-125');
      }
    }
    turBoya();
    kart.appendChild(turSatir);

    function alan(etiket, tip, deger, ipucu) {
      var s = el('div', 'flex flex-col gap-1');
      s.appendChild(el('div', 'text-xs text-subtle', etiket));
      var g = el('input', 'bg-surface-lower rounded text-sm text-white');
      g.type = tip; g.value = deger; if (ipucu) g.placeholder = ipucu;
      g.style.cssText = 'padding:6px 8px;border:1px solid rgba(255,255,255,.12);outline:none';
      s.appendChild(g);
      s.girdi = g;
      return s;
    }

    var sureSatir = alan(t('pkCrossSure'), 'number', '5');
    sureSatir.girdi.min = '1';
    sureSatir.style.display = 'none';
    kart.appendChild(sureSatir);

    var sebepSatir = alan(t('pkCrossSebep'), 'text', '', t('pkCrossSebepIpucu'));
    sebepSatir.girdi.maxLength = 100;
    kart.appendChild(sebepSatir);

    /* ETİKET KİPE GÖRE. Tek kişide bekleme KANALLAR arasında (tek kişi var,
       başka bekleyecek yer yok); toplu kipte KULLANICILAR arasında. */
    var beklemeSatir = alan(toplu ? t('pkCrossTopluBekleme') : t('pkCrossBekleme'), 'number',
      toplu ? String(CROSS_TOPLU_MIN_BEKLEME) : '1');
    beklemeSatir.girdi.min = toplu ? String(CROSS_TOPLU_MIN_BEKLEME) : '0';
    beklemeSatir.girdi.step = toplu ? '1' : '0.5';
    kart.appendChild(beklemeSatir);
    if (toplu) {
      kart.appendChild(el('div', 'text-xs text-subtle',
        t('pkCrossTopluBeklemeNot', [String(CROSS_TOPLU_MIN_BEKLEME)])));
    }

    /* ---- kanal listesi ---- */
    kart.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));
    var kbas = el('div', 'flex items-center justify-between gap-2');
    var sayacY = el('div', 'text-xs text-subtle', t('pkCrossKanallar'));
    kbas.appendChild(sayacY);
    var hepsiB = el('button', 'text-xs font-semibold text-primary-base', t('pkCrossHepsi'));
    hepsiB.style.cssText = 'background:none;border:0;cursor:pointer;padding:0';
    kbas.appendChild(hepsiB);
    kart.appendChild(kbas);

    /* Kanal süzgeci. Çok kanallı modlarda liste uzuyor ve tek tek kutu
       aramak zorlaşıyor. SÜZGEÇ YALNIZ GÖRÜNÜRLÜĞÜ değiştiriyor: işleme
       giren kanalları KUTU belirliyor, süzgeç değil — süzülüp gizlenen ama
       tikli bir kanal yine işlenir. Sayaç bunu görünür kılıyor. */
    var suzgec = el('input', 'bg-surface-lower rounded text-sm text-white');
    suzgec.type = 'search';
    suzgec.placeholder = t('pkCrossAra');
    suzgec.style.cssText = 'padding:5px 8px;border:1px solid rgba(255,255,255,.12);outline:none';
    kart.appendChild(suzgec);

    var kliste = el('div', 'flex flex-col gap-1');
    kliste.style.cssText = 'max-height:' + crossYukseklik(CROSS_SATIR_PX) + 'px;' +
      'overflow-y:auto;overscroll-behavior:contain';
    kart.appendChild(kliste);
    kliste.appendChild(el('div', 'text-xs text-subtle', t('pkCrossYukleniyor')));

    var kutular = [];
    crossKanallariCek(function (liste) {
      kliste.textContent = '';
      if (!liste) { kliste.appendChild(el('div', 'text-xs text-subtle', t('pkCrossListeYok'))); return; }
      if (!liste.length) { kliste.appendChild(el('div', 'text-xs text-subtle', t('pkCrossModYok'))); return; }
      liste.forEach(function (k) {
        var r = el('label', 'flex items-center gap-2 text-sm rounded');
        r.style.cssText = 'padding:3px 6px;cursor:pointer';
        var c = el('input', '');
        c.type = 'checkbox';
        c.checked = !crossHaric[k.slug];
        c.style.cssText = 'width:auto;cursor:pointer';
        c.addEventListener('change', function () {
          /* Kutu boşaltmak KALICI dışlama: bir dahaki açılışta da boş gelir. */
          if (c.checked) delete crossHaric[k.slug]; else crossHaric[k.slug] = 1;
          crossHaricKaydet();
          sayacTazele();
        });
        r.appendChild(c);
        r.appendChild(el('span', 'min-w-0 truncate text-white', k.ad));
        kliste.appendChild(r);
        kutular.push({ kutu: c, slug: k.slug, ad: k.ad, satir: r });
      });
      sayacTazele();
    });

    function gorunurler() {
      return kutular.filter(function (x) { return x.satir.style.display !== 'none'; });
    }
    function sayacTazele() {
      var secili = kutular.filter(function (x) { return x.kutu.checked; }).length;
      sayacY.textContent = t('pkCrossKanallar') + ' · ' +
        t('pkCrossSecili', [String(secili), String(kutular.length)]);
    }
    function kanalSuz() {
      var q2 = String(suzgec.value || '').trim().toLowerCase();
      kutular.forEach(function (x) {
        var uyar = !q2 || x.slug.indexOf(q2) >= 0 || String(x.ad).toLowerCase().indexOf(q2) >= 0;
        x.satir.style.display = uyar ? '' : 'none';
      });
    }
    suzgec.addEventListener('input', kanalSuz);

    hepsiB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      /* YALNIZ GÖRÜNENLERE uygulanıyor. Süzgeçle daraltıp "hepsini bırak"
         demek, süzülen kanalları toplu çıkarmanın en kısa yolu; gizli
         kanalları da kapatsaydı süzgeç işe yaramaz olurdu. */
      var hedef = gorunurler();
      if (!hedef.length) return;
      var acik = hedef.some(function (x) { return !x.kutu.checked; });
      hedef.forEach(function (x) {
        x.kutu.checked = acik;
        if (acik) delete crossHaric[x.slug]; else crossHaric[x.slug] = 1;
      });
      crossHaricKaydet();
      sayacTazele();
    });

    /* ---- ilerleme + başlat ---- */
    kart.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));
    var durumY = el('div', 'text-xs text-subtle', '');
    kart.appendChild(durumY);
    /* TEK TEK SATIR YERİNE ÇUBUK. 10 kişi × 50 kanal = 500 satır demekti;
       akan listede ne olduğunu kimse takip edemiyordu. Çubuk "ne kadar
       kaldı"yı bir bakışta veriyor. Ayrıntı yalnız BAŞARISIZLAR için
       tutuluyor — başarılı işlemin adını tek tek okumanın değeri yok. */
    var barKap = el('div', 'w-full rounded overflow-hidden shrink-0');
    barKap.style.cssText = 'height:8px;background:rgba(255,255,255,.10)';
    var barDolgu = el('div', '');
    barDolgu.style.cssText = 'height:100%;width:0%;background:rgb(83,252,24);' +
      'transition:width .18s ease';
    barKap.appendChild(barDolgu);
    kart.appendChild(barKap);

    var hataY = el('div', 'text-xs text-danger', '');
    hataY.style.cssText = 'max-height:' + crossYukseklik(CROSS_LOG_PX) + 'px;' +
      'overflow-y:auto;overscroll-behavior:contain;white-space:pre-line';
    kart.appendChild(hataY);

    function barCiz(bitti, toplam) {
      barDolgu.style.width = (toplam ? Math.round(bitti / toplam * 100) : 0) + '%';
    }

    var basB = el('button', 'bg-primary-base hover:brightness-110 text-black h-9 rounded font-semibold text-sm', t('pkCrossBaslat'));
    basB.style.cssText = 'border:0;cursor:pointer';
    kart.appendChild(basB);

    basB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (crossCalisiyor) { crossDur = true; return; }
      var secili = kutular.filter(function (x) { return x.kutu.checked; });
      if (!secili.length) { toast(t('pkTCrossKanalSec')); return; }
      var adlar = topluAdlar();
      if (!adlar.length) { toast(t('pkTCrossAdGir')); return; }
      var sure = parseInt(sureSatir.girdi.value, 10) || 5;
      var saniye = parseFloat(beklemeSatir.girdi.value);
      if (!isFinite(saniye) || saniye < 0) saniye = 0;
      /* ALT SINIR BURADA DA ZORLANIYOR: `min` özniteliği yalnız tarayıcının
         kendi doğrulamasını etkiliyor, elle yazılan küçük değeri engellemiyor.
         Kırpılan değeri kutuya geri yazıyoruz ki kullanıcı ne uygulanacağını
         görsün — sessizce başka bir hızda çalışmasın. */
      if (toplu && saniye < CROSS_TOPLU_MIN_BEKLEME) {
        saniye = CROSS_TOPLU_MIN_BEKLEME;
        beklemeSatir.girdi.value = String(saniye);
      }
      var bekleme = saniye * 1000;
      /* SEBEP BİÇİMİ (kullanıcı kararı):
           yazdıysa  → "spam - Cross-Ban, Automated by PureKick."
           yazmadıysa → "Cross-Ban. Ban origin is <kanal>. Banned by <ad>.
                         Automated by PureKick."
         Sebep varsa kaynak/yapan tekrar edilmiyor: sebep alanı zaten kısa,
         uzatmak kullanıcının yazdığını gözden kaçırtırdı. */
      var hamSebep = String(sebepSatir.girdi.value || '').trim();
      var sebep = hamSebep
        ? (hamSebep + ' - Cross-Ban, ' + PK_CROSS_IMZA)
        : crossImzaTam(scKanal, pkKendiAdim());

      crossCalisiyor = true; crossDur = false;
      basB.textContent = t('pkCrossDurdur');
      hataY.textContent = ''; barCiz(0, 1);
      /* İşlem sürerken sekme kapanmasın: yarım kalan toplu işlem, hangi
         kanalda kaldığı bilinmeyen bir durum bırakır. */
      var kapanmaKoru = function (ev) { ev.preventDefault(); ev.returnValue = ''; };
      window.addEventListener('beforeunload', kapanmaKoru);

      /* AKIŞ: kişiler SIRAYLA, o kişinin kanalları PARALEL.
         Kullanıcının modeli bu: bir kişiyi bütün kanallarda takır takır
         işle, sonra bekle, sıradaki kişiye geç. Tek kişi kipinde de aynı
         yol çalışıyor — tek turluk bir döngü oluyor, kanallar yine paralel. */
      var toplamIs = adlar.length * secili.length;
      var yapilan = 0, toplamBasarili = 0, toplamHatali = 0;
      var hataMetni = [];
      var ai = 0;

      function bitir() {
        crossCalisiyor = false;
        window.removeEventListener('beforeunload', kapanmaKoru);
        basB.textContent = t('pkCrossBaslat');
        durumY.textContent = t('pkCrossBitti', [String(toplamBasarili), String(toplamHatali)]);
      }

      function sonrakiKisi() {
        if (crossDur || ai >= adlar.length) { bitir(); return; }
        var kisi = adlar[ai];
        crossObekGonder(
          secili,
          function (k) { return crossIslem(tur, k.slug, kisi, sure, sebep); },
          function (bitti, toplam) {
            durumY.textContent = t('pkCrossIlerleme',
              [String(yapilan + bitti), String(toplamIs), kisi]);
            barCiz(yapilan + bitti, toplamIs);
          },
          function () { return crossDur; }
        ).then(function (r) {
          yapilan += secili.length;
          toplamBasarili += r.basarili; toplamHatali += r.hatali;
          /* Başarısızlar kişi başına TEK satırda toplanıyor: 40 kanalın
             40 ayrı satırı yerine "ahmet: a, b, c". */
          if (r.basarisizlar.length) {
            hataMetni.push(kisi + ': ' +
              r.basarisizlar.map(function (x) { return x.ad; }).join(', '));
            hataY.textContent = hataMetni.join('\n');
          }
          barCiz(yapilan, toplamIs);
          ai++;
          if (crossDur || ai >= adlar.length) { bitir(); return; }
          setTimeout(sonrakiKisi, bekleme);
        });
      }
      sonrakiKisi();
    });

    /* PERDEYE TIKLAMAK KAPATMIYOR (kullanıcı kararı). Pencere doldurulması
       gereken bir form: adlar, sebep, süre, kanal seçimi. Yanına denk gelen
       tek tıkla hepsinin gitmesi pahalı bir kaza. Kapatma yolu tek ve
       açık: sağ üstteki ✕. */
    pkPencereSerbest(perde, kart, bas);
    perde.appendChild(kart);
    pkKatmanaEkle(perde);
  }


  /* ══════════════════════════════════════════════════════════════════════════
   * HIZLI SUSTURMA — sohbetteki kum saati düğmesinden
   *
   * Kick her mesajın yanına üç mod düğmesi koyuyor: Trash · Hourglass ·
   * Prohibited. Kum saatine basınca Kick kendi süre penceresini açıyor.
   * Biz o tıklamayı YAKALAYIP kendi küçük süre şeridimizi açıyoruz — tek
   * tıkla susturma yerine önce süreyi seçtiriyoruz.
   *
   * YAKALAMA AŞAMASINDA (capture) dinleniyor ve yayılım durduruluyor:
   * React'in kendi işleyicisi kabarma aşamasında; capture'da durdurmazsak
   * Kick'in penceresi de açılır ve iki arayüz üst üste biner.
   *
   * ÖZELLİK KAPALIYKEN HİÇ DOKUNULMUYOR: dinleyici yalnız "hizliMod" açıkken
   * bağlanıyor, kapatılınca işaret ve dinleyici sökülüyor — Kick'in kendi
   * davranışı geri geliyor.
   * ══════════════════════════════════════════════════════════════════════════ */
  var SUS_IM = 'data-pk-sustur';
  var SUS_MENU_ID = 'pk-sustur-menu';
  var SUS_VARSAYILAN = [5, 10, 30, 60, 1440];
  var susSureler = null;                       // kullanıcının seçtiği süreler

  function susSureListesi() {
    return (susSureler && susSureler.length) ? susSureler : SUS_VARSAYILAN;
  }
  function susSureKaydet(dizi) {
    susSureler = dizi;
    try { chrome.storage.local.set({ pkHizliSure: dizi }); } catch (e) {}
  }
  /* "5, 10, 30, 1440" → [5,10,30,1440]. Sıfır/negatif/harf elenir, en çok 8
     süre alınır — şerit sohbetin üstünde duruyor, uzarsa ekrandan taşar. */
  function susSureCoz(metin) {
    var p = String(metin || '').split(/[,;\s]+/), o = [];
    for (var i = 0; i < p.length && o.length < 8; i++) {
      var n = parseInt(p[i], 10);
      if (n > 0 && o.indexOf(n) < 0) o.push(n);
    }
    o.sort(function (a, b) { return a - b; });
    return o;
  }

  function susMenuKapat() {
    var e = document.getElementById(SUS_MENU_ID);
    if (e) e.remove();
    document.removeEventListener('mousedown', susDisTik, true);
    document.removeEventListener('keydown', susEsc, true);
  }
  function susDisTik(e) {
    var m = document.getElementById(SUS_MENU_ID);
    if (m && !m.contains(e.target)) susMenuKapat();
  }
  function susEsc(e) { if (e.key === 'Escape') susMenuKapat(); }

  /* Susturmayı UYGULAR. Karttaki eski blok komutu yalnız sohbet kutusuna
     yazıyordu; burada menünün kendisi onay adımı olduğu için doğrudan
     uygulanıyor. İstek "crossIslem" ile — cross-ban'deki uç noktanın aynısı,
     tek kanal. */
  function susUygula(ad, dk) {
    var slug = sayfaSlug();
    if (!slug) { toast(t('pkTSusturmaHata', ['-'])); return; }
    crossIslem('sustur', slug, ad, dk, null).then(function (r) {
      if (r.ok) toast(t('pkTSusturuldu', [ad, sureKisa(dk)]));
      else toast(t('pkTSusturmaHata', [String(r.durum || 0)]));
    });
  }

  function susMenuAc(dugme, ad) {
    susMenuKapat();
    if (!ad) return;
    var m = el('div', 'pk-sus-menu');
    m.id = SUS_MENU_ID;
    m.appendChild(el('span', 'pk-sus-ad', ad));
    susSureListesi().forEach(function (dk) {
      var b = el('button', '', sureKisa(dk));
      b.addEventListener('click', function (e) {
        e.preventDefault(); e.stopPropagation();
        susMenuKapat();
        susUygula(ad, dk);
      });
      m.appendChild(b);
    });
    pkKatmanaEkle(m);

    /* Konum: düğmenin ÜSTÜNE, sağ kenarları hizalı; ekrandan taşarsa içeri
       çekilir. Sohbet sağda ve dar olduğu için taşmaya en yatkın yer orası.
       Yukarıda yer yoksa altına geçiyor. */
    var r = dugme.getBoundingClientRect(), mr = m.getBoundingClientRect();
    var sol = Math.min(r.right - mr.width, window.innerWidth - mr.width - 8);
    var ust = r.top - mr.height - 6;
    if (ust < 8) ust = r.bottom + 6;
    m.style.left = Math.round(Math.max(8, sol)) + 'px';
    m.style.top = Math.round(ust) + 'px';

    document.addEventListener('mousedown', susDisTik, true);
    document.addEventListener('keydown', susEsc, true);
  }

  function susTikla(e) {
    e.preventDefault();
    e.stopPropagation();
    if (e.stopImmediatePropagation) e.stopImmediatePropagation();
    var b = e.currentTarget;
    var satir = pkSatirBul(b);
    var ub = satir && usernameButton(satir);
    var ad = ub ? (ub.textContent || '').trim() : '';
    susMenuAc(b, ad);
  }

  function susturKur() {
    var ikon = document.querySelectorAll('svg[data-ds-icon="Hourglass"]');
    var acik = settings.hizliMod === true;
    for (var i = 0; i < ikon.length; i++) {
      var b = ikon[i].closest ? ikon[i].closest('button') : null;
      if (!b) continue;
      var bagli = b.getAttribute(SUS_IM) === '1';
      if (acik && !bagli) {
        b.setAttribute(SUS_IM, '1');
        b.addEventListener('click', susTikla, true);    // capture: React'ten önce
      } else if (!acik && bagli) {
        b.removeAttribute(SUS_IM);
        b.removeEventListener('click', susTikla, true);
      }
    }
    if (!acik) susMenuKapat();
  }

  function susStilKur() {
    return '.pk-sus-menu{position:fixed;z-index:2147483150;display:flex;align-items:center;' +
        'gap:2px;padding:4px;border-radius:8px;background:rgb(30,33,36);' +
        'border:1px solid rgba(255,255,255,.12);box-shadow:0 8px 24px rgba(0,0,0,.5)}' +
      '.pk-sus-menu .pk-sus-ad{max-width:110px;overflow:hidden;text-overflow:ellipsis;' +
        'white-space:nowrap;padding:0 8px 0 4px;font-size:12px;font-weight:700;color:#8f9ba1}' +
      '.pk-sus-menu button{height:28px;padding:0 9px;border:0;border-radius:5px;' +
        'background:none;color:#fff;font-size:12px;font-weight:600;line-height:1;' +
        'cursor:pointer;white-space:nowrap}' +
      '.pk-sus-menu button:hover{background:rgba(255,255,255,.10)}';
  }

  /* Kart üzerindeki hızlı susturma bloğu KALDIRILDI: aynı iş artık sohbette,
     her mesajın yanındaki kum saati düğmesinden yapılıyor (bkz. susturKur).
     Kartta ikinci bir kopya tutmak hem yer kaplıyor hem iki ayrı yerde aynı
     ayarı yönetmek demekti. Ban sebebi şablonları da oraya taşınmadı —
     onlar komut yazma akışına aitti, menü doğrudan uyguluyor.
     Sayfada kalmış eski blokları topluyoruz. */
  function injectPopupModer(kart) {
    var eski = kart.querySelector(':scope > .pk-hmod');
    if (eski) eski.remove();
  }

  function sureKisa(dk) {
    if (dk >= 1440) return Math.round(dk / 1440) + t('pkKisaGun');
    if (dk >= 60) return Math.round(dk / 60) + t('pkKisaSaat');
    return dk + t('pkKisaDakika');
  }

  /* `injectPopupTakipci` KALDIRILDI: takipçi sayısı artık kartın üstündeki
     bilgi kutusunda bir satır (bkz. injectPopupBilgi). Eski blok kartın
     ortasında ayrı duruyordu. Sayfada kalmış eski düğümleri topluyoruz. */
  function takipciBloguTemizle(kart) {
    var e = kart.querySelector(':scope > .pk-takipci');
    if (e) e.remove();
  }


  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — SEKME İÇİ BAĞLANTI
   *
   * Sohbette paylaşılan bir klip/video bağlantısına tıklayınca yeni sekme
   * açılıyor ve yayından kopuyorsun. Bu özellik açıkken bağlantı SAYFA İÇİNDE
   * küçük bir oynatıcıda açılıyor.
   *
   * Yalnız BİLİNEN ve gömme adresi olan siteler açılır — rastgele bir adresi
   * iframe'e koymak hem güvenli değil hem de çoğu site X-Frame-Options ile
   * reddediyor. Listede olmayan bağlantı normal davranışını sürdürür
   * (yeni sekme), hiçbir şeyi bozmayız.
   *
   * Gömme adresleri sitelerin KENDİ herkese açık gömme uçları; bizim
   * sunucumuza uğramaz.
   * ══════════════════════════════════════════════════════════════════════ */
  var SEKME_ID = 'pk-sekme-ici';

  /* Adresi gömülebilir bir kaynağa çevirir. Tanımıyorsak null döner. */
  /* Kick kullanıcı adı biçimi. */
  var KICK_SLUG = /^[A-Za-z0-9_]{3,25}$/;
  /* Kick'in KENDİ üst düzey sayfaları. Slug kalıbı bunları elemeye yetmiyor
     ("browse" da harf ve 6 karakter); Kick bu adları kullanıcıya vermediği için
     açıkça dışlıyoruz. */
  var KICK_AYRILMIS = ['browse', 'categories', 'category', 'following', 'search', 'help',
                       'about', 'subscriptions', 'messages', 'settings', 'dashboard',
                       'clips', 'drops', 'wallet', 'store', 'legal', 'privacy', 'terms',
                       'api', 'login', 'signup', 'verify', 'password', 'community'];
  function kickKanalSlugu(x) {
    return KICK_SLUG.test(x) && KICK_AYRILMIS.indexOf(String(x).toLowerCase()) < 0;
  }

  function gommeAdresi(url) {
    var u;
    /* TABAN ADRES VERİLMİYOR — bu bilerek. Önce `new URL(url, location.href)`
       yazılmıştı ve göreli her şey mevcut sayfaya göre çözülüyordu: boş dize
       o anki kanalı, "bu bir adres degil" gibi bir metin ise
       kick.com/bu%20bir%20adres%20degil adresini "kanal" sanıp gömüyordu.
       Taban olmadan göreli girdi hata verir ve elenir. */
    try { u = new URL(String(url || '')); } catch (e) { return null; }
    if (u.protocol !== 'https:' && u.protocol !== 'http:') return null;
    var h = u.hostname.replace(/^www\./, '').toLowerCase();
    var y = u.pathname;

    // YouTube
    if (h === 'youtu.be') {
      var vid1 = y.slice(1).split('/')[0];
      if (vid1) return { tur: 'youtube', src: 'https://www.youtube-nocookie.com/embed/' + encodeURIComponent(vid1) };
    }
    if (h === 'youtube.com' || h === 'm.youtube.com' || h === 'youtube-nocookie.com') {
      var vid2 = u.searchParams.get('v');
      if (vid2) return { tur: 'youtube', src: 'https://www.youtube-nocookie.com/embed/' + encodeURIComponent(vid2) };
      var m2 = y.match(/^\/(?:embed|shorts|live)\/([\w-]+)/);
      if (m2) return { tur: 'youtube', src: 'https://www.youtube-nocookie.com/embed/' + encodeURIComponent(m2[1]) };
    }
    // Kick klip ve kanal
    if (h === 'kick.com') {
      var mk = y.match(/^\/([^\/]+)\/clips\/([\w-]+)/);
      if (mk && kickKanalSlugu(mk[1])) {
        return { tur: 'kick', src: 'https://player.kick.com/' + encodeURIComponent(mk[1]) + '?clip=' + encodeURIComponent(mk[2]) };
      }
      var mc = u.searchParams.get('clip');
      var ms = y.match(/^\/([^\/?#]+)\/?$/);
      /* Slug kalıbı ŞART. Kalıpsız hâlde /browse, /categories, /help gibi
         normal Kick sayfaları da "kanal" sanılıp oynatıcıya gömülüyordu. */
      if (!ms || !kickKanalSlugu(ms[1])) return null;
      if (mc) return { tur: 'kick', src: 'https://player.kick.com/' + encodeURIComponent(ms[1]) + '?clip=' + encodeURIComponent(mc) };
      return { tur: 'kick', src: 'https://player.kick.com/' + encodeURIComponent(ms[1]) };
    }
    // Streamable
    if (h === 'streamable.com') {
      var mst = y.match(/^\/([\w-]+)/);
      if (mst) return { tur: 'streamable', src: 'https://streamable.com/e/' + encodeURIComponent(mst[1]) };
    }
    // Twitch klip
    if (h === 'clips.twitch.tv') {
      var mt = y.match(/^\/([\w-]+)/);
      if (mt) return { tur: 'twitch', src: 'https://clips.twitch.tv/embed?clip=' + encodeURIComponent(mt[1]) + '&parent=' + location.hostname };
    }
    return null;
  }

  function sekmeIciKapat() { var e = document.getElementById(SEKME_ID); if (e) e.remove(); }

  function sekmeIciAc(bilgi, baslik) {
    sekmeIciKapat();
    var ort = el('div', '');
    ort.id = SEKME_ID;
    ort.style.cssText = 'position:fixed;inset:0;z-index:2147483210;background:rgba(0,0,0,.72);' +
      'display:flex;align-items:center;justify-content:center;padding:24px';
    var k = el('div', 'bg-surface-base rounded shadow-lg flex flex-col gap-2 ' +
      'will-change-[opacity,transform] animate-slideUpAndFade');
    k.style.cssText = 'width:min(900px,94vw);padding:12px;border:1px solid rgba(240,241,242,.16)';
    var bas = el('div', 'flex items-center justify-between gap-2');
    bas.appendChild(el('div', 'text-sm font-bold text-white truncate', baslik || ''));
    var sag = el('div', 'flex items-center gap-2 shrink-0');
    var dis = el('a', 'text-xs text-subtle hover:text-white transition-all', t('pkYeniSekmedeAc'));
    dis.href = baslik; dis.target = '_blank'; dis.rel = 'noopener noreferrer';
    sag.appendChild(dis);
    var kb = el('button', 'state-layer-surface inline-flex items-center justify-center rounded text-white shrink-0');
    kb.style.cssText = 'width:24px;height:24px;padding:0;background:none;border:0;cursor:pointer';
    kb.innerHTML = I_KAPAT;
    kb.addEventListener('click', sekmeIciKapat);
    sag.appendChild(kb);
    bas.appendChild(sag);
    k.appendChild(bas);

    var cerc = el('iframe', '');
    cerc.src = bilgi.src;
    cerc.setAttribute('allow', 'autoplay; fullscreen; encrypted-media; picture-in-picture');
    /* `no-referrer` YouTube gömmesini bozuyordu: oynatıcı "Hata 153 — video
       oynatıcı yapılandırma hatası" veriyor, çünkü YouTube gömülü oynatıcıda
       geçerli bir referrer bekliyor. `origin` yalnızca "https://kick.com"
       gönderir — tam adres sızmaz, YouTube da kabul eder.
       (Aynı hata bağlantı önizleme penceresinde de vardı, orada düzeltilmişti;
       sekme içi açıcıda gözden kaçmıştı.) */
    cerc.setAttribute('referrerpolicy', 'origin');
    /* Sandbox: gömülü sayfa üst pencereye gezinemesin, açılır pencere açmasın. */
    cerc.setAttribute('sandbox', 'allow-scripts allow-same-origin allow-presentation');
    cerc.style.cssText = 'width:100%;aspect-ratio:16/9;border:0;border-radius:4px;background:#000;display:block';
    k.appendChild(cerc);
    ort.appendChild(k);
    ort.addEventListener('click', function (e) { if (e.target === ort) sekmeIciKapat(); });
    pkKatmanaEkle(ort);
  }

  /* Sohbetteki bağlantı tıklamasını yakalar. Kapsam DAR: yalnız sohbet
     balonlarının içindeki bağlantılar — sayfanın geri kalanı normal çalışır. */
  function sekmeIciTikla(e) {
    if (!settings.consented || settings.sekmeIci !== true) return;
    if (e.button !== 0 || e.ctrlKey || e.metaKey || e.shiftKey || e.altKey) return;   // kaçış yolları
    var a = e.target && e.target.closest ? e.target.closest('a[href]') : null;
    if (!a) return;
    if (!pkSatirBul(a)) return;                                // yalnız sohbet mesajları
    var bilgi = gommeAdresi(a.href);
    if (!bilgi) return;                                        // tanımadığımız adres → dokunma
    e.preventDefault(); e.stopPropagation();
    sekmeIciAc(bilgi, a.href);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — YAYIN KARTLARINDA SAĞ TIK MENÜSÜ
   *
   * Ana sayfada / kategoride bir yayın kartına sağ tıklayınca kanala özel
   * kısayollar. Oynatıcıdaki menünün kart karşılığı; aynı kaçış yolu geçerli:
   * Shift + sağ tık her zaman tarayıcının kendi menüsünü açar.
   * ══════════════════════════════════════════════════════════════════════ */
  var KMENU_ID = 'pk-kart-menu';
  function kartMenuKapat() { var e = document.getElementById(KMENU_ID); if (e) e.remove(); }

  function kartMenuOgeleri(slug) {
    var o = [];
    o.push([t('pkYeniSekmedeAc'), function () {
      window.open('https://kick.com/' + encodeURIComponent(slug), '_blank', 'noopener');
    }]);
    o.push([t('pkBaglantiyiKopyala'), function () {
      panoyaYaz('https://kick.com/' + slug, function (ok) { toast(t(ok ? 'pkTBaglantiKopyalandi' : 'pkTKopyalanamadi')); });
    }]);
    if (settings.onizleme !== false) {
      o.push([t('pkKanalBilgisi'), function () {
        kanalBilgiCek(slug, function (v) {
          if (!v) return;
          toast(v.canli
            ? t('pkKanalCanli', [v.baslik || '—', String(v.izleyici || 0)])
            : t('pkKanalCevrimdisi', [String(v.takipci || 0)]));
        });
      }]);
    }
    var engelli = !!blocked[slug];
    o.push([engelli ? t('pkKanalEngeliKaldir') : t('pkKanaliGizle'), function () {
      if (engelli) unblockChannel(slug); else blockChannel(slug);
      applyChannelBlocks();
    }]);
    return o;
  }

  function kartMenuAc(x, y, slug) {
    kartMenuKapat();
    var m = el('div', 'z-dropdown flex h-fit min-w-fit flex-col rounded bg-surface-base text-sm shadow-lg ' +
      'will-change-[opacity,transform] animate-slideUpAndFade');
    m.id = KMENU_ID;
    m.style.cssText = 'position:fixed;z-index:2147483100;padding:6px;min-width:210px;' +
      'border:1px solid rgba(240,241,242,.16)';
    var bas = el('div', 'text-xs text-subtle truncate');
    bas.style.cssText = 'padding:4px 10px';
    bas.textContent = slug;
    m.appendChild(bas);
    kartMenuOgeleri(slug).forEach(function (o) {
      var b = el('button', 'state-layer-surface flex w-full items-center rounded text-left text-white font-semibold text-sm', o[0]);
      b.style.cssText = 'padding:8px 10px;background:none;border:0;cursor:pointer';
      b.addEventListener('click', function (e) { e.stopPropagation(); kartMenuKapat(); try { o[1](); } catch (er) {} });
      m.appendChild(b);
    });
    pkKatmanaEkle(m);
    var r = m.getBoundingClientRect();
    m.style.left = Math.round(Math.max(8, Math.min(x, innerWidth - r.width - 8))) + 'px';
    m.style.top = Math.round(Math.max(8, Math.min(y, innerHeight - r.height - 8))) + 'px';
  }

  function kartSagTik(e) {
    if (!settings.consented || settings.kartSagTik !== true) return;
    if (e.shiftKey) return;                                  // kaçış yolu
    var a = e.target && e.target.closest ? e.target.closest('a[href^="/"]') : null;
    if (!a) return;
    if (pkSatirBul(a)) return;                               // sohbet mesajı değil
    var slug = chanSlug(a);
    if (!slug || inSkipZone(a)) return;
    e.preventDefault();
    kartMenuAc(e.clientX, e.clientY, slug);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — TİYATRO MODUNDA ÜST MENÜ
   * Tiyatro modunda Kick üst gezinme çubuğunu gizliyor; arama ve menü için
   * moddan çıkmak gerekiyor. Bu özellik açıkken ekranın en üstüne gelince
   * çubuk geri geliyor — Kick'in kendi düğümü, kopya değil.
   * ══════════════════════════════════════════════════════════════════════ */
  /* İlk yazışta ekranın en üstüne şeffaf bir yakalama katmanı konuyordu; iki
     kusuru vardı ve ikisi de kodu okuyunca çıktı:
       1. Katman body'nin SONUNA ekleniyordu, dolayısıyla `#pk-tiyatro-alan:hover
          ~ * nav` kuralının seçeceği bir "sonraki kardeş" hiç yoktu — kural ölüydü.
       2. `pointer-events:auto` ile sayfanın en üst 8 pikselindeki tıklamaları
          yutuyordu, üstelik tiyatro modunda değilken bile.
     Katman tamamen kaldırıldı: imleci `mousemove` ile izliyoruz. Sayfaya hiçbir
     şey eklenmiyor, hiçbir tıklama engellenmiyor. */
  var tiyatroNavAcik = false, tiyatroNavZaman = 0;

  /* Kick'in üst çubuğu. Tiyatro modunda yüksekliği sıfıra iniyor ya da
     görünmez oluyor; ölçüyle anlıyoruz — Kick'te okunabilir bir "tiyatro açık"
     işareti yok. */
  function tiyatroNavOgesi() {
    return document.querySelector('nav') || document.querySelector('header');
  }
  function tiyatroNavGizliMi(n) {
    if (!n) return false;
    if (n.getAttribute('data-pk-nav') === '1') return true;   // biz açtıysak zaten gizliydi
    if (tiyatroModuMu() === false) return false;              // tiyatroda degiliz, cubuk zaten acik
    var r = n.getBoundingClientRect();
    return r.height < 8;
  }

  function tiyatroNavGoster(ac) {
    var n = tiyatroNavOgesi();
    if (!n) return;
    if (ac) {
      if (n.getAttribute('data-pk-nav') === '1') return;
      n.setAttribute('data-pk-nav', '1');
      n.style.setProperty('opacity', '1', 'important');
      n.style.setProperty('visibility', 'visible', 'important');
      n.style.setProperty('height', 'auto', 'important');
      n.style.setProperty('overflow', 'visible', 'important');
      tiyatroNavAcik = true;
    } else if (n.getAttribute('data-pk-nav') === '1') {
      n.removeAttribute('data-pk-nav');
      ['opacity', 'visibility', 'height', 'overflow'].forEach(function (p) { n.style.removeProperty(p); });
      tiyatroNavAcik = false;
    }
  }

  /* İmleç üst 4 piksele girince aç, 120 pikselin altına inince kapat.
     Aradaki geniş boşluk bilerek: dar olsaydı çubuk açılıp kapanıp titrerdi. */
  function tiyatroNavHareket(e) {
    if (!settings.consented || settings.tiyatroNav !== true) {
      if (tiyatroNavAcik) tiyatroNavGoster(false);
      return;
    }
    if (e.clientY <= 4) {
      clearTimeout(tiyatroNavZaman);
      var n = tiyatroNavOgesi();
      if (tiyatroNavGizliMi(n)) tiyatroNavGoster(true);
      return;
    }
    if (tiyatroNavAcik && e.clientY > 120) {
      clearTimeout(tiyatroNavZaman);
      tiyatroNavZaman = setTimeout(function () { tiyatroNavGoster(false); }, 600);
    }
  }

  /* Ayar kapatılınca açık bırakılmış çubuğu geri al. */
  function tiyatroNavKur() {
    if (!settings.consented || settings.tiyatroNav !== true) {
      if (tiyatroNavAcik) tiyatroNavGoster(false);
    }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — GÖRSELİ BÜYÜT
   * Sohbetteki emote, rozet ve profil görselleri çok küçük. CTRL + tık ile
   * görsel tam boyutta bir katmanda açılıyor.
   * ══════════════════════════════════════════════════════════════════════ */
  var BUYUT_ID = 'pk-gorsel-buyut';
  function gorselBuyutKapat() { var e = document.getElementById(BUYUT_ID); if (e) e.remove(); }

  function gorselBuyut(src, ad) {
    gorselBuyutKapat();
    var ort = el('div', '');
    ort.id = BUYUT_ID;
    ort.style.cssText = 'position:fixed;inset:0;z-index:2147483220;background:rgba(0,0,0,.8);' +
      'display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;padding:24px;cursor:zoom-out';
    var im = el('img', '');
    im.src = src; im.referrerPolicy = 'no-referrer';
    im.style.cssText = 'max-width:min(80vw,520px);max-height:70vh;object-fit:contain;' +
      'image-rendering:pixelated;background:rgba(255,255,255,.04);border-radius:8px;padding:12px';
    ort.appendChild(im);
    if (ad) ort.appendChild(el('div', 'text-sm font-semibold text-white', ad));
    ort.addEventListener('click', gorselBuyutKapat);
    pkKatmanaEkle(ort);
  }

  function gorselTikla(e) {
    if (!settings.consented || settings.gorselBuyut !== true) return;
    if (!e.ctrlKey && !e.metaKey) return;
    var t0 = e.target;
    if (!t0 || t0.tagName !== 'IMG') return;
    var src = t0.getAttribute('src'); if (!src) return;
    e.preventDefault(); e.stopPropagation();
    gorselBuyut(src, (t0.getAttribute('alt') || '').trim());
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — DETAYLI BAN BİLGİSİ
   *
   * Günlükte "X yasakladı" yazıyordu ama sebep, süre ve itiraz durumu
   * görünmüyordu. Bu blok kaydın tüm alanlarını okunur hâle getiriyor.
   * Alanlar Kick'in kendi olayından geliyor; olmayan alan sessizce atlanır.
   * ══════════════════════════════════════════════════════════════════════ */
  function banDetay(x) {
    var d = [];
    if (x.sebep) d.push({ e: t('pkBanSebep'), v: x.sebep });
    if (x.tur === 'sustur' && x.bitis) {
      var sn = Math.max(0, Math.round((x.bitis - x.ts) / 1000));
      d.push({ e: t('pkBanSure'), v: pkSureYazi(sn) });
      var kalan = Math.max(0, Math.round((x.bitis - Date.now()) / 1000));
      d.push({ e: t('pkBanKalan'), v: kalan > 0 ? pkSureYazi(kalan) : t('pkBanDoldu') });
    }
    if (x.tur === 'yasak') d.push({ e: t('pkBanSure'), v: t('pkBanKalici') });
    if (x.yapan) d.push({ e: t('pkBanYapan'), v: x.yapan });
    else if (x.tur === 'sil') d.push({ e: t('pkBanYapan'), v: t('pkBanOtomatik') });
    if (x.itiraz) d.push({ e: t('pkBanItiraz'), v: t('pkBanItirazVar') });
    if (x.metin) d.push({ e: t('pkBanMesaj'), v: x.metin });
    return d;
  }

  /* Günlük satırını genişletilebilir yapar: tıklayınca detay açılır/kapanır. */
  function modSatirDetayKur(satirEl, kayit) {
    if (settings.banDetay !== true) return;
    var d = banDetay(kayit);
    if (!d.length) return;
    satirEl.style.cursor = 'pointer';
    satirEl.setAttribute('data-pk-detayli', '1');
    satirEl.addEventListener('click', function (e) {
      e.stopPropagation();
      var acik = satirEl.querySelector(':scope > .pk-bandetay');
      if (acik) { acik.remove(); return; }
      var kutu = el('div', 'pk-bandetay flex flex-col gap-1');
      kutu.style.cssText = 'margin-top:6px;padding-left:8px;border-left:2px solid rgba(240,241,242,.16)';
      d.forEach(function (p) {
        var r = el('div', 'flex items-start gap-2');
        r.appendChild(el('div', 'text-xs text-subtle shrink-0', p.e));
        r.appendChild(el('div', 'text-xs text-white', p.v));
        kutu.appendChild(r);
      });
      satirEl.appendChild(kutu);
    });
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — BAN SEBEBİ ŞABLONLARI
   * Ceza verirken sebebi elle yazmak zaman alıyor. Hazır sebepler tek tıkla
   * sohbet kutusuna komut olarak yazılır — GÖNDERİLMEZ, göndermek sende.
   * Şablonlar yereldir, istediğini ekleyip silebilirsin.
   * ══════════════════════════════════════════════════════════════════════ */
  var banSebepleri = [];

  function sebepKaydet() {
    try { chrome.storage.local.set({ pkBanSebep: banSebepleri }); } catch (e) {}
  }
  function sebepEkle(metin) {
    var m = String(metin || '').trim().slice(0, 120);
    if (!m || banSebepleri.indexOf(m) >= 0) return false;
    banSebepleri.push(m);
    if (banSebepleri.length > 20) banSebepleri.shift();
    sebepKaydet(); return true;
  }
  function sebepSil(metin) {
    var i = banSebepleri.indexOf(metin);
    if (i >= 0) { banSebepleri.splice(i, 1); sebepKaydet(); }
  }


  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — YAYIN KAYDEDİCİ
   *
   * İzlerken kaydeder, durdurunca dosyayı indirir. Kaynak, oynatıcının
   * KENDİ görüntüsü: `video.captureStream()` → MediaRecorder. Akışa ikinci
   * kez bağlanmıyoruz, ağdan tek bayt fazladan inmiyor — ekranda zaten
   * çözülmüş olan kareler yazılıyor.
   *
   * SINIRLAR (kullanıcıya da söyleniyor):
   *  · Sekme kapanırsa kayıt gider — kayıt bellekte tutuluyor.
   *  · DRM'li akışta captureStream boş döner; o durumda uyarı verilir.
   *  · Çıktı WebM. Tarayıcı hangi kodeği destekliyorsa o seçilir.
   *  · Uzun kayıtlar RAM yer — 2 saat sınırı ve boyut uyarısı var.
   * ══════════════════════════════════════════════════════════════════════ */
  /* Firefox aynı işi `mozCaptureStream` adıyla veriyor. Yalnız `captureStream`
     aranınca Firefox'ta özellik "desteklenmiyor" görünüyordu. */





  /* Kaydederken oynatıcının üstünde küçük bir gösterge: süre + boyut. */


  /* "X açıldı" / "X kapatıldı" bildirimi.
     Önceden her özellik için iki ayrı Türkçe dize yazılıydı (44 dize); çeviriye
     geçerken hepsini ayrı ayrı çevirmek yerine özellik ADINI yer tutucuya
     veriyoruz — özellik adları zaten sözlükte var. */
  function ozellikToast(acikMi, adAnahtari) {
    return t(acikMi ? 'pkTOzellikAcildi' : 'pkTOzellikKapatildi', [t(adAnahtari)]);
  }


  /* ---------- Kick'in kendi kimlikleri: ortak yardımcılar ----------
     Kick aynı kimlikten İKİ tane basıyor (masaüstü + mobil düzeni); biri
     0×0. Bu yüzden her seçim görünürlükten geçer. */
  function pkGorunur(e) {
    if (!e) return false;
    var r = e.getBoundingClientRect();
    return r.width > 0 && r.height > 0;
  }
  function pkIlkGorunur(sec, kok) {
    try {
      var l = (kok || document).querySelectorAll(sec);
      for (var i = 0; i < l.length; i++) if (pkGorunur(l[i])) return l[i];
    } catch (e) {}
    return null;
  }

  /* Tiyatro modu — Kick kök kapta data-theatre bayrağı tutuyor.
     Eskiden video/pencere oranı ölçülüyordu (%70 eşiği); bayrak kesin bilgi. */
  function tiyatroModuMu() {
    var e = document.querySelector('[data-theatre]');
    if (e) return e.getAttribute('data-theatre') === 'true';
    var d = document.querySelector('[data-dashboard-theatre]');
    if (d) return d.getAttribute('data-dashboard-theatre') === 'true';
    return null;                                   // bilinmiyor → çağıran eski yola düşsün
  }


  /* ---------- Sohbet düğme yuvaları ---------- */

  /* Başlıktaki kendi düğme grubumuz; sağ uca yapışır. */
  /* Başlıktaki kendi düğme grubumuz — Kick'in "aktif sohbetçiler" düğmesinin
     HEMEN SOLUNA, bitişik.

     Ölçüm: o düğmenin kendi `ml-auto`'su var (canlıda 244 px'e çözülüyor) ve
     boş alanın tamamını yutuyor. Grubumuzu araya koyunca aramızda 208 px boşluk
     kalıyordu; gruba da `margin-left:auto` verince boşluk ikiye bölünüp 104 px
     oluyordu. Bitişik durması için Kick'in düğmesindeki otomatik kenar boşluğu
  /* Grup kaldırılırken Kick'in kenar boşluğunu geri koy. */
  function sohbetBaslikGrubuKaldir() {
    var g = document.querySelector('[data-pk-bgrup]');
    if (g) g.remove();
    var k = document.querySelectorAll('[data-pk-ml]');
    for (var i = 0; i < k.length; i++) {
      var v = k[i].getAttribute('data-pk-ml');
      if (v) k[i].style.marginLeft = v; else k[i].style.removeProperty('margin-left');
      k[i].removeAttribute('data-pk-ml');
    }
  }

  /* Bir tur adımını korumalı çalıştırır. Aynı adımın hatası bir kez bildirilir. */
  /* ══════════════════════════════════════════════════════════════════════
   * EKLENTİ BAĞLAMI ÖLDÜĞÜNDE TEMİZ ÇIKIŞ
   * ══════════════════════════════════════════════════════════════════════ */

  /* `chrome.runtime.id` bağlam geçersizleşince undefined olur; erişim bile
     fırlatabilir, o yüzden try içinde. */
  function pkBaglamCanli() {
    try { return !!(chrome && chrome.runtime && chrome.runtime.id); }
    catch (e) { return false; }
  }

  /* Kick'in KENDİ düğümlerine bastığımız işaretler. Bu düğümler silinmez —
     yalnız işaret ve bizim verdiğimiz satır içi stiller geri alınır. */
  /* `data-pk-rol` ve `data-pk-cerceve` BURADA OLMALI: ikisi de Kick'in kendi
     satırına satır içi stil yazıyor. `data-pk-rol` eksikti — rol vurgusu
     eklenti kapatılınca sayfada kalıyordu. */
  var PK_KICK_ISARET = ['data-pk-mid', 'data-pk-mid-ix', 'data-pk-sil-gizle', 'data-pk-genis',
                        'data-pk-kapat-hiza',
                        'data-pk-silindi', 'data-pk-oncelik', 'data-pk-mdar',
                        'data-pk-rol', 'data-pk-cerceve',
                        'data-pk-kisigizli', 'data-pk-grad', 'data-pk-title', 'data-pk-processed',
                        'data-pk-kanalsabit', 'data-pk-takipliste', 'data-pk-grup',
                        'data-pk-grupli', 'data-pk-kapali-gizli', 'data-pk-gizli-grup',
                        'data-pk-perde', 'data-pk-altlik'];

  var pkToplandi = false;
  var pkGozcuZaman = 0;                  // 800ms'lik arayüz gözcüsü
  function pkKendiniTopla() {
    if (pkToplandi) return;
    pkToplandi = true;
    /* Gözcüyü de durdur: bayrak onu zaten susturuyor ama boşuna dönmesin. */
    if (pkGozcuZaman) { try { clearInterval(pkGozcuZaman); } catch (e) {} pkGozcuZaman = 0; }
    try {
      /* 1. Gizlediğimiz Kick düğümlerini geri aç. */
      /* Erken perde kapısı: eklenti çekilirken kenar çubuğu örtülü kalmasın. */
      try { document.documentElement.removeAttribute('data-pk-kenar-hazir'); } catch (e) {}
      try { document.documentElement.style.removeProperty('--pk-sabit-renk'); } catch (e) {}
      var g = document.querySelectorAll('[data-pk-gizli],[data-pk-gizli-grup]');
      for (var i = 0; i < g.length; i++) {
        g[i].style.removeProperty('display');
        g[i].removeAttribute('data-pk-gizli');
        g[i].removeAttribute('data-pk-gizli-grup');
      }
      /* Kick'in kendi tarih bloğunu gizlemiştik (bilgiyi biz yukarıda
         yazdığımız için) — düğüm Kick'in, geri verilmeli. */
      var gt = document.querySelectorAll('[data-pk-gizli-tarih]');
      for (var gi = 0; gi < gt.length; gi++) {
        gt[gi].style.removeProperty('display');
        gt[gi].removeAttribute('data-pk-gizli-tarih');
      }
      /* 2. Kenar boşluğunu değiştirdiğimiz düğümleri geri al. */
      var m = document.querySelectorAll('[data-pk-ml]');
      for (var j = 0; j < m.length; j++) {
        var v = m[j].getAttribute('data-pk-ml');
        if (v) m[j].style.marginLeft = v; else m[j].style.removeProperty('margin-left');
        m[j].removeAttribute('data-pk-ml');
      }
      /* 3. Kick düğümlerindeki işaretlerimizi ve verdiğimiz stilleri kaldır. */
      for (var k = 0; k < PK_KICK_ISARET.length; k++) {
        var l = document.querySelectorAll('[' + PK_KICK_ISARET[k] + ']');
        for (var n = 0; n < l.length; n++) {
          l[n].removeAttribute(PK_KICK_ISARET[k]);
          /* `background` KISALTMASI, `setProperty('background-color', …)` ile
             yazılanı her tarayıcıda kaldırmıyor — longhand'leri açıkça
             sayıyoruz. `box-shadow` ve `animation` da eksikti. */
          /* `width`/`max-width`/`margin-left`: profil kartını genişletirken
             Kick'in düğümüne yazıyoruz; kaldırılmazsa eklenti çekildikten
             sonra kart geniş kalıyordu. */
          ['opacity', 'filter', 'max-height', 'overflow', 'border-left', 'padding-left',
           'width', 'max-width', 'margin-left', 'right',
           'border', 'border-radius',
           'background', 'background-color', 'background-image', 'background-size',
           'background-repeat', 'background-position',
           'box-shadow', 'animation',
           '-webkit-background-clip', 'background-clip',
           '-webkit-text-fill-color', 'color', 'display'].forEach(function (p) {
            try { l[n].style.removeProperty(p); } catch (e) {}
          });
        }
      }
      /* 4. BİZİM ürettiğimiz düğümleri sil. `pk-` sınıfı ya da kap
            işaretlerimizden birini taşıyanlar bize aittir. */
      var bizim = document.querySelectorAll(
        '[data-pk-satir],[data-pk-mod-kutu],[data-pk-baglanti],[data-pk-ayar-sayfa],' +
        '[data-pk-ayar-bas],[data-pk-sabit],[data-pk-bilgi],[data-pk-kisi],[data-pk-pm],' +
        '[data-pk-not],[data-pk-takipci],[data-pk-yanit],[data-pk-hmod],[data-pk-mod-stil],' +
        '[data-pk-demo],[data-pk-yenilik],[data-pk-sabit-stil],[data-pk-erken-perde],' +
        '[data-pk-hakkinda],[data-pk-hk-stil],.pk-hk-kural-govde,[data-pk-oniz-kutu],' +
        '#pk-oniz-bastir,#pk-emote-oniz,#' + MOD_ID + ',[' + MOD_SERIT + '],' +
        '[data-pk-grupbas],.pk-iskelet,' +
        '.pk-badge,.pk-sc,' +
        '.pk-sil-et,.pk-sil-goster,.pk-sil-icerik,.pk-sabit-dugme,.pk-sabit-oluk,' +
        '.pk-stream-badge,' +
        /* KATMAN DÜĞÜMLERİ TEK KURALLA: `pkKatmanaEkle` ile body'ye (ya da
           tam ekran ögesine) eklediğimiz HER şey `data-pk-katman` taşıyor —
           toplu araç pencereleri, rozet yetkilendirme, ipuçları, bildirimler.
           Dördü kimlik kimlik sayılmıştı ve DÖRDÜ DE listede yoktu: eklenti
           kapatılınca açık pencere sayfada asılı kalıyordu. Kimlik saymak
           yerine işaretten gitmek yeni pencerede de kendiliğinden çalışır. */
        '[data-pk-katman],' +
        '[data-pk-ayarbtn],#' + PK_PANEL_ID + ',' +
        '.pk-mesaj-liste,.pk-lp,.pk-bilgi,.pk-kisi,.pk-pm,.pk-not');
      for (var q = 0; q < bizim.length; q++) { try { bizim[q].remove(); } catch (e) {} }
      /* GİRİŞ ALANININ ZEMİNİ Kick'in düğümüne yazılıyor: panelimiz açıkken
         onu `bg-surface-base` yapıyoruz. Çekilirken geri almazsak sayfada
         boyalı kalırdı — düğüm Kick'in, silinmiyor, yalnız stil sökülüyor. */
      try { pkAltZeminBoya(false); } catch (e) {}
      /* Özel isim gradyanı KICK'İN kendi düğümüne yazılıyor — düğümü
         silmiyoruz, yalnız stili geri alıyoruz. */
      var oz = document.querySelectorAll('[data-pk-ozelad]');
      for (var oi = 0; oi < oz.length; oi++) {
        try { ozelAdSok(oz[oi]); } catch (e) {}
      }
      /* 5. Enjekte ettiğimiz stil etiketleri. */
      var st = document.querySelectorAll('style[data-pk-mod-stil],style[data-pk-stil]');
      for (var w = 0; w < st.length; w++) { try { st[w].remove(); } catch (e) {} }
    } catch (e) {}
  }

  var pkTurHata = Object.create(null);
  /* TUR PROFİLLEYİCİSİ. Yalıtılmış dünyada çalıştığımız için tur adımlarının
     maliyeti dışarıdan ölçülemiyor; uzun görev kaydı "bir şey uzun sürdü"
     diyor ama neyin sürdüğünü söylemiyor. Açmak için sayfada:
         localStorage.pkProfil = '1'   → sonra yenile
     Sonuç 2 sn'de bir <html data-pk-profil> içine JSON olarak yazılır.
     Kapalıyken maliyeti: adım başına bir boole denetimi. */
  var pkProfilAcik = false;
  try { pkProfilAcik = localStorage.getItem('pkProfil') === '1'; } catch (e0) {}
  var pkProfil = Object.create(null), pkProfilTur = 0, pkProfilSonYazim = 0;

  function pkAdim(ad, fn) {
    var t0 = pkProfilAcik ? performance.now() : 0;
    try { fn(); }
    catch (e) {
      if (!pkTurHata[ad]) {
        pkTurHata[ad] = 1;
        try { if (window.__pkDebug) console.warn('[PK] tur adimi hata verdi: ' + ad, e); } catch (x) {}
      }
    }
    if (pkProfilAcik) {
      var d = performance.now() - t0;
      var kayit = pkProfil[ad] || (pkProfil[ad] = { n: 0, ms: 0, en: 0 });
      kayit.n++; kayit.ms += d; if (d > kayit.en) kayit.en = d;
    }
  }

  function pkProfilBildir() {
    pkProfilTur++;
    var simdi = performance.now();
    if (simdi - pkProfilSonYazim < 2000) return;
    pkProfilSonYazim = simdi;
    var liste = Object.keys(pkProfil).map(function (a) {
      var k = pkProfil[a];
      return { ad: a, cagri: k.n, toplamMs: Math.round(k.ms), enUzunMs: +k.en.toFixed(1) };
    }).sort(function (a, b) { return b.toplamMs - a.toplamMs; });
    try {
      document.documentElement.setAttribute('data-pk-profil', JSON.stringify({
        tur: pkProfilTur,
        gecenSn: Math.round(simdi / 1000),
        toplamMs: liste.reduce(function (t, x) { return t + x.toplamMs; }, 0),
        ilk15: liste.slice(0, 15)
      }));
    } catch (e2) {}
  }
  /* Ad listesini sırayla, her biri korumalı olarak çalıştırır. */
  function pkTur(adlar) {
    /* Eklenti yeniden yüklendiyse/güncellendiyse bağlam ölür: chrome.i18n
       patlar ve t() ham anahtar döndürür ("pkSonMesajlar" gibi). Bu hâlde
       çalışmaya devam etmek arayüzü bozuk bırakır — çekiliyoruz. */
    if (!pkBaglamCanli()) { pkKendiniTopla(); return; }
    /* Kanal dışındayken sohbet/oynatıcı adımlarını hiç çalıştırma. Ana sayfa
       öne çıkan yayının canlı sohbetini basıyor; onu işlemek boşuna iş ve
       ana sayfayı en pahalı sayfa hâline getiriyordu. */
    var kanalda = kanalSayfasi();
    for (var i = 0; i < adlar.length; i++) {
      var ad = adlar[i], fn = pkTurFn[ad];
      if (!fn) continue;
      if (!kanalda && PK_TUR_KANAL[ad]) continue;
      /* KAPALI ÖZELLİĞİN İŞLEVİ HİÇ ÇAĞRILMAZ (bkz. PK_TUR_AYAR). Kapandıktan
         sonraki İLK turda bir kez daha çağrılıyor ki kendi "kapalı" dalı
         bıraktığı düğümleri toplasın; ondan sonra sessiz. */
      if (!pkAdimAcik(ad)) {
        if (turTemiz[ad]) continue;
        turTemiz[ad] = 1;
      } else if (turTemiz[ad]) turTemiz[ad] = 0;
      pkAdim(ad, fn);
    }
    if (pkProfilAcik) pkProfilBildir();
  }


  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — KICK'İN SOHBET AYARLARI PANELİNE ENTEGRASYON
   * ══════════════════════════════════════════════════════════════════════ */

  /* Kick'in ayar satır listesi. İşaret: içinde ChevronRight ikonlu li'ler olan
     bir ul. Metne bakmıyoruz — 7 dilde çalışsın. */
  function kickAyarListesi() {
    var uls = document.querySelectorAll('ul');
    for (var i = 0; i < uls.length; i++) {
      var u = uls[i];
      if (!pkGorunur(u)) continue;
      var li = u.querySelector(':scope > li');
      if (!li) continue;
      if (!li.querySelector('svg[data-ds-icon="ChevronRight"]')) continue;
      /* Sohbet sütununun içinde olmalı — sayfadaki başka bir liste olmasın. */
      var sut = pkIlkGorunur('#channel-chatroom');
      if (sut && !sut.contains(u)) continue;
      return u;
    }
    return null;
  }

  /* Klonlanacak örnek satır: Kick'in kendi satırlarından ilki. */
  function kickAyarOrnekSatiri(ul) {
    var li = ul.querySelectorAll(':scope > li');
    for (var i = 0; i < li.length; i++) {
      if (li[i].getAttribute('data-pk-satir')) continue;      // kendi satırımız değil
      if (li[i].querySelector('span')) return li[i];
    }
    return null;
  }

  /* Kick'in satırından bir kopya üretir: metni değiştirir, ok ikonunu
     isteğe göre bırakır/atar, kendi tıklamamızı bağlar. */
  function kickAyarSatiri(ornek, metin, okVar, tikla) {
    var li = ornek.cloneNode(true);
    li.setAttribute('data-pk-satir', '1');
    /* Örnek satır o sırada GİZLENMİŞ olabilir (alt menüye geçerken Kick'in
       satırlarını display:none yapıyoruz) — kopya o stili miras alır ve bizim
       satırlar da görünmez olur. Canlı denemede tam olarak bu oldu: liste
       4 piksele çöktü. Kopyada satır içi display'ı her hâlükârda temizliyoruz. */
    li.style.removeProperty('display');
    var sp = li.querySelector('span');
    if (sp) sp.textContent = metin; else li.textContent = metin;
    var ok = li.querySelector('svg[data-ds-icon]');
    if (ok && !okVar) ok.remove();
    /* KALICI VURGU: satır Kick'in kendi listesinde göze çarpsın.
       Renk uydurmuyoruz — Kick'in kendi yüzey tonu `bg-surface-highest`
       (ölçüldü: rgb(35,38,41)); Kick temasını değiştirirse bizimki de değişir.
       `bg-transparent` ÖNCE KALKMALI: ikisi de background-color yazıyor ve
       hangisinin kazanacağı sınıf sırasına değil stil sayfasındaki sıraya
       bağlı — çakışmayı bırakmak yerine kaldırıyoruz. */
    li.classList.remove('bg-transparent');
    li.classList.add('bg-surface-highest');
    /* Kopya, örnekteki tüm veri niteliklerini de taşıyor olabilir; React'in
       kendi işaretlerini temizleyelim ki bize ait olduğu belli olsun. */
    var nit = li.attributes, sil = [];
    for (var n = 0; n < nit.length; n++) {
      if (/^data-(radix|state|ds-)/.test(nit[n].name)) sil.push(nit[n].name);
    }
    for (var q = 0; q < sil.length; q++) li.removeAttribute(sil[q]);
    li.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      try { tikla(); } catch (er) {}
    });
    return li;
  }

  /* Alt menümüz açıkken Kick'in kendi satırları gizlenir (silinmez — React
     onları yönetiyor, silmek yeniden çizimde soruna yol açar). */
  var pkAyarAcik = false;

  function kickAyarKickSatirlari(ul, goster) {
    var li = ul.querySelectorAll(':scope > li');
    for (var i = 0; i < li.length; i++) {
      if (li[i].getAttribute('data-pk-satir')) continue;
      if (goster) li[i].style.removeProperty('display');
      else li[i].style.setProperty('display', 'none', 'important');
    }
  }
  function kickAyarBizimSatirlar(ul) {
    var e = ul.querySelectorAll(':scope > [data-pk-satir]');
    for (var i = 0; i < e.length; i++) e[i].remove();
  }

  /* Alt menü: PureKick'in sohbet araçları, Kick'in satır tasarımıyla. */
  /* PureKick araçları — Kick'in LİSTESİNE DEĞİL, panelin gövdesine çizilir.
     Eskiden Kick'in satırları gizlenip yerine 6 klon konuyordu; React o
     listeyi yeniden çizerken çakılıyor ve sohbet "something went wrong"
     ile değişiyordu. Burada çizdiğimiz kap bizim, React'e temas yok. */
  function pkAyarMenuCiz(kap) {
    /* MENÜ KENARDAN KENARA. Kick'in kendi ayar listesi de öyle: `ul` yatay
       dolgusuz (`px-0 pb-1`), satır arası 2px. Sayfa kabının nefes payını
       burada sıfırlıyoruz — negatif kenar boşluğu gerekmiyor. */
    kap.style.padding = '0 0 4px';
    kap.style.gap = '2px';
    [[t('pkBahsedilmeler'), bolumBahsedilme, 'ayar/bahset'],
     [t('pkSohbetIstatistikleriBaslik'), bolumIstatistik, 'ayar/istat'],
     [t('pkKullaniciBul'), pkAyarKullaniciBul, 'ayar/bul'],
     /* MODERASYON SATIRI KALDIRILDI: aynı liste artık sohbetin ÜSTÜNDEKİ
        şeritten açılıyor (modSeritKur/modPanelAc). İki giriş noktası hem
        gereksizdi hem de şerit paneli açıkken PureKick menüsü onun içine
        gömülüyordu — bkz. pkAyarKickGovde. */
     [t('pkSohbetKaydi'), pkAyarSohbetKaydi, 'ayar/kayit']
    ].concat(
      /* TOPLU CROSS-BAN yalnız özellik açıkken listede. Kendi SAYFASI yok:
         dördüncü öge `1` → satır doğrudan cross penceresini açıyor. Aynı
         pencere profil kartından tek kişi için de açılıyor. */
      settings.crossBan === true
        ? [[t('pkCrossToplu'), function () { crossPencereAc(null); }, 'ayar/toplucross', 1]]
        : []
    ).concat(
      settings.topluTo === true
        ? [[t('pkToMenu'), function () { topluToPencereAc(); }, 'ayar/topluto', 1]]
        : []
    ).concat(
      settings.topluSil === true
        ? [[t('pkSilMenu'), function () { topluSilPencereAc(); }, 'ayar/toplusil', 1]]
        : []
    ).concat(
      settings.otoMesaj === true
        ? [[t('pkOmMenu'), function () { omPencereAc(); }, 'ayar/otomesaj', 1]]
        : []
    ).forEach(function (p) {
      var b = pkAyarMenuSatiri(kap, p[0], function () {
        pkYeniEkGoruldu(p[2]);                  // açıldı → nokta sönsün
        if (p[3]) { try { p[1](); } catch (er) {} return; }   // sayfa değil, doğrudan eylem
        pkAyarSayfaAc(p[0], p[1], pkAyarMenuAc);
      });
      /* Nokta metnin YANINA, ok işaretinin soluna. */
      if (pkYeniEkVar(p[2])) {
        var sp = b.querySelector('span');
        if (sp) sp.appendChild(pkYeniNoktasi());
      }
    });
  }

  /* Kick'in liste satırı görünümünde, ama kendi düğümümüz. */
  /* Satır ölçüleri Kick'in kendi `li`sinden ÖLÇÜLDÜ:
       h-10 (40px) · padding 6px 10px · rounded-xs (2px) · zemin şeffaf
       hover #2A2D32 · yazı text-sm font-medium
     Kick hover'ı `betterhover:hover:bg-[#2A2D32]!` sınıfıyla veriyor; biz
     kendi dinleyicimizle veriyoruz ki sınıf adı değişse de bozulmasın. */
  function pkAyarMenuSatiri(kap, metin, tikla) {
    var b = el('button', 'flex w-full items-center justify-between rounded-xs ' +
                         'text-white select-none transition-colors');
    b.style.cssText = 'height:40px;padding:6px 10px;background:transparent;border:0;' +
      'cursor:pointer;text-align:left;transition-duration:200ms';
    b.addEventListener('mouseenter', function () { b.style.background = '#2A2D32'; });
    b.addEventListener('mouseleave', function () { b.style.background = 'transparent'; });
    b.appendChild(el('span', 'text-sm font-medium', metin));
    var ok = el('span', 'inline-flex shrink-0');
    ok.style.cssText = 'width:16px;height:16px';
    ok.innerHTML = I_CHEV;
    var sv = ok.querySelector('svg');
    if (sv) { sv.style.width = '16px'; sv.style.height = '16px'; }
    b.appendChild(ok);
    b.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      try { tikla(); } catch (er) {}
    });
    kap.appendChild(b);
    return b;
  }

  function pkAyarMenuAc() {
    pkAyarSayfaAc(t('pkAyarSatiri'), pkAyarMenuCiz, null);
  }

  /* Kök görünüm: Kick'in satırları geri, bizim tek giriş satırımız en altta. */
  /* Kick'in listesine TEK bir satır ekler. Kick'in kendi satırları asla
     gizlenmez — listede ne kadar az oynarsak React o kadar sakin. */
  function pkAyarKok(ul, ornek) {
    pkAyarAcik = false;
    kickAyarBizimSatirlar(ul);
    kickAyarKickSatirlari(ul, true);
    var satir = kickAyarSatiri(ornek, t('pkAyarSatiri'), true, pkAyarMenuAc);
    /* Güncelleme sonrası menüde okunmamış yenilik varsa burada da belli olsun:
       kullanıcı paneli açmadan önce çoğunlukla bu satırı görüyor. */
    if (pkYeniVarMi()) {
      var sp2 = satir.querySelector('span');
      (sp2 || satir).appendChild(pkYeniNoktasi());
    }
    ul.appendChild(satir);
  }

  /* Her turda çağrılır: panel açıldıysa satırımızı ekler, kapandıysa bir şey
     yapmaz. React paneli yeniden çizerse satır da yeniden eklenir. */
  /* Bizim satırlarımızı YALNIZ görünür listede tut. Gizli ikizde ya da
     React'in attığı eski listede kalan artıklar temizlenir; oralarda
     gizlenmiş Kick satırı varsa geri açılır.

     Bu temizlik şart: eskiden panel kapanınca hiçbir şey geri alınmıyordu,
     Kick'in satırları `display:none` kalıyor ve bizim klonlar listede
     duruyordu — React yeniden çizerken çakılmasının sebebi buydu. */
  function kickAyarArtikTemizle(hedef) {
    var uls = document.querySelectorAll('ul');
    for (var i = 0; i < uls.length; i++) {
      var u = uls[i];
      if (u === hedef) continue;
      if (!u.querySelector(':scope > [data-pk-satir]')) continue;
      kickAyarBizimSatirlar(u);
      kickAyarKickSatirlari(u, true);
    }
  }

  function kickAyarSatiriKur() {
    if (!settings.consented) return;
    var ul = kickAyarListesi();
    kickAyarArtikTemizle(ul);
    if (!ul) { pkAyarAcik = false; return; }
    if (ul.querySelector(':scope > [data-pk-satir]')) return;   // zaten duruyor
    var ornek = kickAyarOrnekSatiri(ul);
    if (!ornek) return;
    pkAyarKok(ul, ornek);
  }


  /* ---------- Kick ayar panelinin parçaları ---------- */
  /* KICK'İN ayar panelinin kaydırılan gövdesi.

     BİZİMKİLER ELENMELİ: moderasyon panelinin gövdesi `flex-1 min-h-0
     overflow-y-auto` sınıflarını taşıyor — sınıf seçicisi sıraya bakmadığı
     için `.min-h-0.flex-1.overflow-y-auto` ona da uyuyor. Panel sohbet
     sütununda Kick'in panelinden ÖNCE geldiğinden `pkIlkGorunur` onu
     buluyordu: moderasyon paneli açıkken PK düğmesine basınca menü Kick'in
     paneli yerine BİZİM panelimizin içine çiziliyordu (kullanıcı bildirdi).

     Sınıf eklemek yerine sahiplik denetimi: Kick sınıf adlarını değiştirse
     de kendi düğümümüzü kendi işaretimizden tanımaya devam ediyoruz. */
  /* ══════════════════════════════════════════════════════════════════════
   * KENDİ AYAR PANELİMİZ
   *
   * Eskiden sayfalarımızı KİCK'İN ayar panelinin gövdesine çiziyor, onun
   * içeriğini display:none ile gizliyorduk. Bir hata sınıfının tamamı oradan
   * çıktı ve teker teker yamamak işe yaramadı:
   *   · React panelde sayfa değiştirince kendi içeriğini geri getiriyor,
   *     bizimki altında kalıyordu (kalkan → "bizim başlık, Kick'in içeriği").
   *   · Panelin gerçekten açık mı olduğunu anlamak ayrı bir uğraştı: kapalı
   *     panel DOM'dan silinmiyor, max-height:0 ile kırpılıyor.
   *   · Başlık ve geri oku iki sahip arasında kalıyordu.
   *
   * ARTIK PANEL BİZİM. Kick'in düğümlerine hiç dokunmuyoruz, hiçbir şeyini
   * gizlemiyoruz. Görünüm Kick'in kendi panelinden ÖLÇÜLDÜ:
   *   kap : absolute inset-x-0 z-popover h-fit bottom-0 -translate-y-[103px]
   *         (#chatroom-footer içinde, z-index 401)
   *   kök : rounded-t bg-surface-base … min-h-[50px] max-h-[454px]
   *         zemin rgb(23,26,28), üst köşe 4px
   * Kabın sınıfı CANLI PANELDEN KOPYALANIYOR: -translate-y-[103px] gibi
   * ölçüler Kick'in düzenine bağlı, sabit sayı gömmek yanlış olurdu. Panel
   * bulunamazsa ölçülmüş yedek dizgeye düşüyoruz.
   * ══════════════════════════════════════════════════════════════════════ */
  var PK_PANEL_ID = 'pk-ayar-panel';
  var PK_PANEL_KAP_YEDEK = 'absolute inset-x-0 z-popover h-fit bottom-0 -translate-y-[103px]';
  /* break-all BİLEREK YOK: Kick'in kökünde var ama bizim etiketlerimizi
     kelime ortasından bölerdi. */
  var PK_PANEL_KOK_SINIF = 'relative rounded-t bg-surface-base flex h-auto w-full flex-col ' +
                           'overflow-hidden min-h-[50px] max-h-[454px]';

  function pkPanelYuva() { return pkIlkGorunur('#chatroom-footer'); }

  function pkPanelKapSinif() {
    var k = document.getElementById('chat-settings-panel');
    return (k && (k.className || '').toString()) || PK_PANEL_KAP_YEDEK;
  }

  function pkPanelVar() { return document.getElementById(PK_PANEL_ID); }

  /* SOHBET GİRİŞ ALANININ KABI — panel açıkken zemini boyanan yer.
     Kick kendi ayar paneli açılınca buraya `bg-surface-base` veriyor; panel
     ile giriş alanı tek bir yüzey gibi görünüyor. Bizim panelimizde orası
     siyah kalıyordu (kullanıcı bildirdi; ölçüldü: kapalıyken
     rgba(0,0,0,0), Kick'in paneli açıkken rgb(23,26,28)).
     Sınıf adıyla (`z-common`) DEĞİL, İÇERİĞİYLE buluyoruz: mesaj kutusunu
     barındıran doğrudan çocuk. Kick sınıfı değiştirse de tutar. */
  function pkAltZeminKabi() {
    var f = pkPanelYuva(); if (!f) return null;
    for (var i = 0; i < f.children.length; i++) {
      var c = f.children[i];
      if (c.id === PK_PANEL_ID) continue;
      if (c.querySelector && c.querySelector('[contenteditable="true"], textarea')) return c;
    }
    return null;
  }
  function pkAltZeminBoya(ac) {
    if (ac) {
      var k = pkAltZeminKabi(); if (!k) return;
      k.setAttribute('data-pk-altzemin', '1');
      k.style.setProperty('background-color', 'rgb(23,26,28)');
      return;
    }
    var l = document.querySelectorAll('[data-pk-altzemin]');
    for (var i = 0; i < l.length; i++) {
      l[i].style.removeProperty('background-color');
      l[i].removeAttribute('data-pk-altzemin');
    }
  }

  /* Kick'in KENDİ ayar paneli açık mı? İkisi aynı yere denk geliyor, biri
     açılırken öteki kapanmalı. Kapalı panel silinmediği için gövde ölçüsüne
     bakıyoruz: kapalıyken sıfır. */
  function kickPaneliAcikMi() {
    var k = document.getElementById('chat-settings-panel');
    if (!k) return false;
    var g = k.querySelector('.min-h-0.flex-1.overflow-y-auto');
    return !!(g && pkGorunur(g));
  }

  /* Kick'in geri oku — kendi ikon setinden, klonlanarak. Yoksa kendi okumuz. */
  function pkGeriOku() {
    var k = document.querySelector('svg[data-ds-icon="ChevronLeft"]');
    if (k) { var c = k.cloneNode(true); c.removeAttribute('data-ds-icon'); return c; }
    var w = el('span', 'inline-flex shrink-0');
    w.innerHTML = I_GERI;
    return w;
  }

  function pkAyarSayfaKapat() {
    pkMentionOkunduUygula();  // liste görüldüyse rozet ANCAK ŞİMDİ düşüyor
    pkAyarSayfaAcik = false;
    pkAyarSonCiz = null; pkAyarSonKap = null;
    var p = pkPanelVar();
    if (p) p.remove();
    pkAltZeminBoya(false);  // giriş alanı kendi şeffaf hâline dönsün
    /* SÜRÜM YÜKSELTME ARTIĞI: eski sürüm Kick'in düğümlerini gizleyip kendi
       başlığını onun başlık satırına koyuyordu. Güncelleme sırasında sayfada
       kalmış olabilir — bir kez toplayıp gizlenenleri geri açıyoruz. */
    var e = document.querySelectorAll('[data-pk-ayar-bas]');
    for (var i = 0; i < e.length; i++) { try { e[i].remove(); } catch (er) {} }
    var g = document.querySelectorAll('[data-pk-gizli="1"]');
    for (var j = 0; j < g.length; j++) {
      g[j].style.removeProperty('display');
      g[j].removeAttribute('data-pk-gizli');
    }
  }

  /* Panelimiz yerinde duruyor mu — turda denetleniyor.
     İki durumda kapanıyor: sohbet altlığı gitmiş (kanal değişti, tiyatro
     kipi…) ya da Kick kendi ayar panelini açmış. */
  /* SOHBET ALTLIĞINDAKİ KICK DÜĞMELERİ PANELİMİZİ KAPATIR.

     Emote seçici, kalkan (kimlik/rozet), dişli — hepsi Kick'in panelini
     BİZİM panelimizle aynı yere açıyor. Kapatmayı turdaki denetime
     bırakmak yetmedi: canlıda ölçüldü, ikisi saniyelerce aynı anda açık
     kalabiliyor (bizim 340×339, Kick'in 340×428, üst üste).

     Bu yüzden KAYNAĞINDA kapatıyoruz: altlığa yakalama aşamasında tek bir
     dinleyici. Kick'in kendi işleyicisi kabarma aşamasında olduğu için biz
     ondan ÖNCE davranıyoruz — yarış diye bir şey kalmıyor.

     Üç istisna: kendi panelimizin içi (orada gezinmek kapatmamalı), PK
     düğmesi (kendi aç/kapa mantığı var) ve düğme olmayan tıklamalar
     (yazı kutusu, boşluk). */
  function pkAltlikDinleKur() {
    var f = pkPanelYuva();
    if (!f || f.getAttribute('data-pk-altlik') === '1') return;
    f.setAttribute('data-pk-altlik', '1');
    f.addEventListener('click', function (e) {
      if (!pkPanelVar()) return;
      var t0 = e.target;
      if (!t0 || !t0.closest) return;
      if (t0.closest('#' + PK_PANEL_ID)) return;      // kendi panelimizin içi
      if (t0.closest('[' + PK_AYAR_IM + ']')) return; // PK düğmesi kendi işini görüyor
      if (!t0.closest('button')) return;              // yazı kutusu, boşluk…
      pkAyarSayfaKapat();
    }, true);
  }

  function pkAyarCakismaDenetle() {
    pkAltlikDinleKur();   // altlık React ile yeniden çizilirse dinleyici geri bağlanır
    var p = pkPanelVar();
    if (!p) {
      /* Panel yok ama bayrak açık kalmışsa durumu temizle: PK düğmesi
         "açık" sanıp kapatmaya çalışırdı. */
      if (pkAyarSayfaAcik) pkAyarSayfaKapat();
      return;
    }
    if (!document.contains(p) || !pkPanelYuva() || kickPaneliAcikMi()) pkAyarSayfaKapat();
  }

  /* ciz(kap) içeriği doldurur. geriye geri okuna basınca çağrılır;
     verilmezse ok çizilmez (kök menüde gidecek yer yok). */
  function pkAyarSayfaAc(baslik, ciz, geriye) {
    var yuva = pkPanelYuva(); if (!yuva) return;
    pkAyarSayfaKapat();
    pkAyarSayfaAcik = true;

    var kap = el('div', pkPanelKapSinif());
    kap.id = PK_PANEL_ID;

    var kok = el('div', PK_PANEL_KOK_SINIF);
    kap.appendChild(kok);

    /* Başlık şeridi — Kick'inkiyle aynı 42px ve aynı hizalama. */
    var bas = el('div', 'flex w-full items-center justify-between shrink-0');
    bas.style.cssText = 'height:42px;padding:0 8px 0 12px';
    var sol = el('div', 'flex items-center gap-2 min-w-0');
    if (geriye) {
      var gb = el('button', '');
      gb.type = 'button';
      gb.title = t('pkGeri');
      gb.setAttribute('aria-label', t('pkGeri'));
      gb.style.cssText = 'width:26px;height:26px;flex:0 0 auto;padding:0;margin:0;' +
        'display:inline-flex;align-items:center;justify-content:center;' +
        'background:transparent;border:0;border-radius:6px;color:#fff;' +
        'cursor:pointer;transition:background-color .12s ease';
      gb.addEventListener('mouseenter', function () { gb.style.backgroundColor = 'rgba(255,255,255,.10)'; });
      gb.addEventListener('mouseleave', function () { gb.style.backgroundColor = 'transparent'; });
      gb.appendChild(pkGeriOku());
      gb.addEventListener('click', function (ev) {
        ev.preventDefault(); ev.stopPropagation();
        try { geriye(); } catch (er) {}
      });
      sol.appendChild(gb);
    }
    sol.appendChild(el('div', 'text-base font-semibold text-white truncate', baslik));
    bas.appendChild(sol);

    var kapatB = el('button', '');
    kapatB.type = 'button';
    kapatB.title = t('pkKapat');
    kapatB.setAttribute('aria-label', t('pkKapat'));
    kapatB.style.cssText = 'width:26px;height:26px;flex:0 0 auto;padding:0;margin:0;' +
      'display:inline-flex;align-items:center;justify-content:center;' +
      'background:transparent;border:0;border-radius:6px;color:#fff;' +
      'cursor:pointer;transition:background-color .12s ease';
    kapatB.addEventListener('mouseenter', function () { kapatB.style.backgroundColor = 'rgba(255,255,255,.10)'; });
    kapatB.addEventListener('mouseleave', function () { kapatB.style.backgroundColor = 'transparent'; });
    kapatB.innerHTML = I_KAPAT;
    kapatB.addEventListener('click', function (ev) {
      ev.preventDefault(); ev.stopPropagation();
      pkAyarSayfaKapat();
    });
    bas.appendChild(kapatB);
    kok.appendChild(bas);
    kok.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));

    /* GÖVDEDE DOLGU YOK — Kick'in kendi gövdesinde de yok (ölçüldü:
       padding 0). Nefes payını içerik kabı veriyor; menü kendi çizicisinde
       onu sıfırlayıp kenardan kenara uzanıyor, Kick'in listesi gibi. */
    var govde = el('div', 'min-h-0 flex-1 overflow-y-auto');
    govde.style.cssText = 'overscroll-behavior:contain';
    kok.appendChild(govde);

    /* İçerik kabı AYRI: bölüm çizicileri data-pk-ayar-sayfa taşıyan bu
       düğüme yazıyor, gövdenin kendisine değil. Böylece sayfayı yenilerken
       (pkAyarTazele) yalnız içerik siliniyor, kaydırma kabı kalıyor. */
    var icerik = el('div', 'flex flex-col gap-2.5');
    icerik.style.cssText = 'padding:10px 12px 12px';
    icerik.setAttribute('data-pk-ayar-sayfa', '1');
    govde.appendChild(icerik);

    pkAyarSonCiz = ciz; pkAyarSonKap = icerik;
    try { ciz(icerik); } catch (er) {}

    yuva.appendChild(kap);
    pkAltZeminBoya(true);   // giriş alanı da panelle aynı yüzey olsun
  }

  /* ---------- Panel içi çiziciler ---------- */

  /* Kick'in ayar bloğu görünümü: etiket + içerik. */
  /* Çubuğu kabın EN SONUNA koyup alta yapıştırır.
     `top:0` yerine `bottom:0`: liste kısaldığında üstteki çubuk kapla
     birlikte yukarı-aşağı zıplıyordu. Kap kapsayıcı kadar uzatılıp liste
     boşluğu doldurunca çubuk her durumda altta kalır. */
  function pkAyarAltaSabitle(kap, cubuk, liste) {
    if (!kap || !cubuk) return;
    kap.style.setProperty('min-height', '100%');
    if (liste) liste.style.setProperty('flex', '1 1 auto');
    cubuk.style.setProperty('position', 'sticky');
    cubuk.style.setProperty('bottom', '0');
    cubuk.style.setProperty('z-index', '2');
    cubuk.style.setProperty('background', 'rgb(23,26,28)');   // Kick bg-surface-base
    cubuk.style.setProperty('padding-top', '6px');
    kap.appendChild(cubuk);           // en sona taşı
  }

  /* Şerit: çift sıra gri, tek sıra zeminsiz. Kick'in kendi yüzey tonu. */
  /* ŞERİT SINIFLARI — ZEMİNE GÖRE İKİ TANE.
     `pkSeritSinif` ayarlar panelindeki listeler için: panelin zemini koyu,
     `bg-surface-highest` (35,38,41) orada AÇIK şerit oluyor.
     `pkSeritSinifKoyu` profil kartı için: kartın zemini ZATEN
     `bg-surface-highest` (ölçüldü), orada aynı sınıf görünmez kalırdı;
     bir tık koyu olan `bg-surface-base` (23,26,28) gerekiyor.
     İkisi de Kick'in kendi belirteçleri. */
  function pkSeritSinif(sira) { return sira % 2 ? '' : ' bg-surface-highest'; }
  function pkSeritSinifKoyu(sira) { return sira % 2 ? '' : ' bg-surface-base'; }

  function pkAyarBlok(kap, etiket) {
    var b = el('div', 'flex flex-col gap-1 px-2');
    if (etiket) b.appendChild(el('span', 'text-sm font-semibold', etiket));
    kap.appendChild(b);
    return b;
  }

  /* Kick'in kendi girdi kutusu ölçüleriyle. */
  function pkAyarGirdi(kap, yertutucu, deger, degisti) {
    var i = el('input', 'bg-surface-highest rounded text-sm text-white w-full');
    i.type = 'text';
    i.placeholder = yertutucu;
    i.value = deger || '';
    i.style.cssText = 'padding:7px 10px;border:1px solid rgba(240,241,242,.16);outline:none';
    i.addEventListener('input', function () { degisti(i.value); });
    kap.appendChild(i);
    return i;
  }

  /* Sohbet kaydı — panel içi. Veri araSonuclari()'ndan; ayrı kaynak yok. */
  function pkAyarSohbetKaydi(kap) {
    var b = pkAyarBlok(kap, null);
    pkAyarGirdi(b, t('pkKullaniciAdiBos'), araAdFiltre, function (v) { araAdFiltre = v; pkAyarKayitCiz(); });
    pkAyarGirdi(b, t('pkIcerikBos'), araSorgu, function (v) { araSorgu = v; pkAyarKayitCiz(); });
    var liste = el('div', 'flex flex-col px-2');
    liste.setAttribute('data-pk-kayit-liste', '1');
    kap.appendChild(liste);
    pkAyarAltaSabitle(kap, b, liste);
    pkAyarKayitCiz();
  }
  function pkAyarKayitCiz() {
    var liste = document.querySelector('[data-pk-kayit-liste]'); if (!liste) return;
    liste.textContent = '';
    var son = araSonuclari();
    if (!son.length) { liste.appendChild(el('div', 'text-sm text-subtle', t('pkSonucYok'))); return; }
    liste.appendChild(el('div', 'text-xs text-subtle', t('pkSonucSayisiN', [String(son.length)])));
    var n = Math.min(son.length, 150);
    /* YENİ OLAN ÜSTTE — moderasyon listesiyle aynı davranış. */
    for (var i = son.length - 1, sira = 0; i >= son.length - n; i--, sira++) {
      var r = el('div', 'flex gap-2 text-sm rounded' + pkSeritSinif(sira));
      r.style.cssText = 'padding:3px 6px';
      r.appendChild(el('span', 'text-subtle shrink-0', scSaat(son[i].m.ts)));
      var sag = el('span', 'min-w-0 break-words');
      sag.appendChild(pkAdOgesi(son[i].v.ad));
      sag.appendChild(document.createTextNode(': '));
      var g = mesajGovdesi(son[i].m.p || [], 18);
      while (g.firstChild) sag.appendChild(g.firstChild);
      r.appendChild(sag);
      liste.appendChild(r);
    }
  }

  /* Kullanıcı bul — panel içi. Sohbette yazanlar arasında ada göre süzer. */
  function pkAyarKullaniciBul(kap) {
    var b = pkAyarBlok(kap, null);
    pkAyarGirdi(b, t('pkKullaniciAdi'), '', function (v) { pkAyarKisiCiz(v); });
    var liste = el('div', 'flex flex-col px-2');
    liste.setAttribute('data-pk-kisi-liste', '1');
    kap.appendChild(liste);
    pkAyarAltaSabitle(kap, b, liste);
    pkAyarKisiCiz('');
  }
  /* Sohbette o kişinin son mesajındaki isim düğmesine basar. Bulamazsa false. */
  /* ---------- Kişi avatarı/bio: /api/v2/channels/<kişi> ---------- */
  /* Kullanıcı adı -> Kick kanal adresi. `_` ve `.` tireye dönüyor
     (canlıda ölçüldü: snap_exe -> snap-exe). YALNIZ kanal ucu ve kanal
     bağlantısı için; `/users/<ad>` ucu HAM adı istiyor. */
  function pkKanalSlug(nick) {
    return String(nick || '').toLowerCase().replace(/[_.]/g, '-');
  }

  /* Profil resmi olmayan kullanıcı için yedek avatar: adın ilk harfi,
     addan türetilen sabit bir renk. Daire hiç boş kalmasın. */

  var pkKisiKanal = Object.create(null);     // nick -> {ts…} | 'bekliyor' | 'yok'

  var pkKisiBekleyen = Object.create(null);   // nick -> [geri çağrı]
  function pkKisiKanalCek(nick, bitince) {
    var k = String(nick || '').toLowerCase(); if (!k) return;
    var v = pkKisiKanal[k];
    if (v === 'yok') return;
    /* İstek uçuyorsa çağrıyı KUYRUĞA AL — eskiden sessizce düşüyordu ve
       kart açma isteği kayboluyordu. */
    if (v === 'bekliyor') { (pkKisiBekleyen[k] = pkKisiBekleyen[k] || []).push(bitince); return; }
    if (v) { bitince(v); return; }
    pkKisiKanal[k] = 'bekliyor';
    /* Önbellek HAM adla, istek TİRELİ slug ile. */
    fetch('https://kick.com/api/v2/channels/' + encodeURIComponent(pkKanalSlug(nick)), { credentials: 'omit' })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        if (!j) { pkKisiKanal[k] = 'yok'; return; }
        var u = j.user || {};
        /* `user_id`: Kick'in kartını açarken `sender.id` olarak gerekiyor. */
        pkKisiKanal[k] = { ad: u.username || nick, avatar: u.profile_pic || '',
                           bio: String(u.bio || '').slice(0, 200),
                           takipci: j.followers_count || 0, dogrulanmis: !!j.verified,
                           slug: j.slug || k, kimlik: j.user_id || (u && u.id) || 0 };
        bitince(pkKisiKanal[k]);
        var bek = pkKisiBekleyen[k]; delete pkKisiBekleyen[k];
        if (bek) for (var b = 0; b < bek.length; b++) { try { bek[b](pkKisiKanal[k]); } catch (e) {} }
      })
      .catch(function () { pkKisiKanal[k] = 'yok'; delete pkKisiBekleyen[k]; });
  }





  /* Önce Kick'in KENDİ kartı (moderatör düğmeleri, rozetler onda). Mesaj
     DOM'dan gitmişse — banlanan kişide neredeyse her zaman öyle — kendi
     kartımızı açıyoruz. Artık hiçbir durumda "mesajı kalmadı" demiyoruz. */
  /* Kick'in kartını sayfa dünyasındaki açıcıya açtır. */
  function kickKartiniAcUzaktan(isim) {
    pkKisiKanalCek(isim, function (v) {
      if (!v) return;
      try {
        window.postMessage({ source: 'kab-kart', n: pkNonce,
                             kimlik: v.kimlik || 0, ad: v.ad || isim, slug: v.slug || '' }, '*');
      } catch (e) {}
    });
  }

  /* HER YERDE KICK'İN KENDİ KARTI. Kendi kartımızı çizmiyoruz — Kick'inki
     rozetleri, moderatör düğmelerini, notu, "Ona yazılanlar"ı ve sabitlemeyi
     zaten getiriyor; taklidi hem eksik hem bakımı zor.

     1) Mesaj ekranda ise doğrudan o satıra bas (istek yok).
     2) Değilse kimliği Kick'in kanal ucundan al, sayfa dünyası açsın —
        banlanan kişinin mesajı silinmiş olsa bile çalışır. */
  function profilKartiniAc(isim) {
    var k = String(isim || '').toLowerCase(); if (!k) return false;
    var msgs = sohbetSatirlari();
    for (var i = msgs.length - 1; i >= 0; i--) {
      var ub = usernameButton(msgs[i]); if (!ub) continue;
      if ((ub.textContent || '').trim().toLowerCase() !== k) continue;
      try { ub.click(); return true; } catch (e) { break; }
    }
    kickKartiniAcUzaktan(isim);
    return true;
  }

  /* SOHBETTE OLMAYAN KULLANICI İÇİN SATIR.
     Arama listesi yalnız sohbete YAZANLARI taşıyor (`scListe`); aranan ad
     orada yoksa eskiden "sonuç yok" deyip bırakıyorduk. Oysa kartı uzaktan
     açan altyapı zaten var (`kickKartiniAcUzaktan` → worker-hook `kartAc`).
     Kartı KICK ÇİZİYOR: sohbetteki bir gönderenin bilgisi geçici olarak
     hedef kullanıcıyla değiştirilip düğmesine basılıyor, hemen geri alınıyor.
     Böylece rozetler, mod düğmeleri, biyografi — hepsi Kick'in kendi
     kartından geliyor, taklit çizmiyoruz. */
  function pkDisKisiSatiri(ad) {
    var r = el('div', 'flex items-center justify-between gap-2 text-sm rounded');
    r.style.cssText = 'padding:3px 6px;cursor:pointer';
    var sol = el('div', 'flex items-center gap-2 min-w-0');
    sol.appendChild(pkAdOgesi(ad));
    r.appendChild(sol);
    r.appendChild(el('span', 'text-xs text-primary-base shrink-0', t('pkProfiliAc')));
    r.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      /* Kanal sorgusu daha önce boş döndüyse tekrar denemeye gerek yok. */
      if (pkKisiKanal[String(ad).toLowerCase()] === 'yok') { toast(t('pkTKullaniciBulunamadi')); return; }
      try { kickKartiniAcUzaktan(ad); } catch (er) {}
    });
    return r;
  }

  function pkAyarKisiCiz(sorgu) {
    var liste = document.querySelector('[data-pk-kisi-liste]'); if (!liste) return;
    liste.textContent = '';
    var q = String(sorgu || '').trim().toLowerCase();
    var adlar = [];
    for (var k in scListe) {
      if (!Object.prototype.hasOwnProperty.call(scListe, k)) continue;
      if (q && k.indexOf(q) < 0) continue;
      adlar.push(scListe[k]);
    }
    adlar.sort(function (a, b) { return b.adet - a.adet; });

    /* Aranan ad sohbette YOKSA doğrudan açma satırı. Tam eşleşme varsa
       eklemiyoruz — aynı kişi iki kez listelenmesin. Kalıp Kick'in kullanıcı
       adı kuralı; rastgele metinle boşuna istek atmayalım. */
    var tamVar = false;
    for (var e0 = 0; e0 < adlar.length; e0++) {
      if (String(adlar[e0].ad).toLowerCase() === q) { tamVar = true; break; }
    }
    var disSatir = (q && !tamVar && /^[a-z0-9_]{3,25}$/.test(q)) ? pkDisKisiSatiri(sorgu.trim()) : null;
    if (disSatir) liste.appendChild(disSatir);

    if (!adlar.length) {
      /* Dış satır zaten basıldıysa "sonuç yok" demek yanlış olurdu. */
      if (!disSatir) liste.appendChild(el('div', 'text-sm text-subtle', t('pkSonucYok')));
      return;
    }
    liste.appendChild(el('div', 'text-xs text-subtle', t('pkSonucSayisiN', [String(adlar.length)])));
    var n = Math.min(adlar.length, 100);
    for (var i = 0; i < n; i++) {
      var v = adlar[i];
      var r = el('div', 'flex items-center justify-between gap-2 text-sm rounded' + pkSeritSinif(i));
      r.style.cssText = 'padding:3px 6px';
      var sol = el('div', 'flex items-center gap-2 min-w-0');
      sol.appendChild(pkAdOgesi(v.ad));
      r.appendChild(sol);
      r.appendChild(el('span', 'text-xs text-subtle shrink-0', t('pkMesajSayisi', [String(v.adet)])));
      /* Tıklayınca Kick'in KENDİ profil kartını açıyoruz: sohbetteki o kişinin
         isim düğmesine basıyoruz. Kartı biz çizmiyoruz — Kick'inki açılıyor,
         rozetler ve düğmeler olduğu gibi geliyor. */
      r.style.cursor = 'pointer';
      (function (isim) {
        r.addEventListener('click', function (e) {
          e.preventDefault(); e.stopPropagation();
          if (!profilKartiniAc(isim)) toast(t('pkTKisiSohbetteYok'));
        });
      })(v.ad);
      liste.appendChild(r);
    }
  }


  /* ══════════════════════════════════════════════════════════════════════
   * KICK'TE AÇIK OLAN HESABIN ADI
   *
   * Site bağlantısı (pkBenimAd) varsa o öncelikli. Yoksa Kick'in KENDİ
   * uç noktasından okunuyor: `/api/v1/user` — aynı köken, çerezle çalışıyor,
   * çıkış yapılmışsa 200 + boş nesne döndürüyor.
   *
   * DOM TARAMASI NEDEN BIRAKILDI: ad eskiden üst çubuktaki avatarın `alt`
   * metninde duruyordu. Kick avatarı `<nav>` DIŞINA taşımış; `<nav>` içinde
   * artık yalnız "Kick Logo" var (canlıda ölçüldü) ve arama boş dönüyordu.
   * Sayfadaki diğer `img[alt]` değerleri KENAR ÇUBUĞUNDAKİ KANAL adları —
   * onlara bakmak kullanıcıya başkasının adını verirdi.
   *
   * BUNA BAĞLI OLAN: bahsedilmeler (kendi adını arıyor) ve panelin abonelik
   * demosu. Siteye bağlanmamış kullanıcıda ikisi de sessizce çalışmıyordu.
   * ══════════════════════════════════════════════════════════════════════ */
  var pkSayfaAdi = '';
  var pkAdIstek = false, pkAdBos = 0;
  var PK_AD_BEKLE = 5 * 60 * 1000;        // boş döndüyse bu kadar yeniden deneme

  function pkKendiAdimCek(bitince) {
    var v = pkBenimAd || pkSayfaAdi;
    if (v) { if (bitince) bitince(v); return; }
    if (pkAdIstek) return;
    /* Çıkış yapılmışken uç nokta boş nesne dönüyor; geri çekilmezsek her
       turda boşuna istek atardık. */
    if (pkAdBos && Date.now() - pkAdBos < PK_AD_BEKLE) { if (bitince) bitince(''); return; }
    pkAdIstek = true;
    /* BEARER ŞART. Uç nokta çerezle de 200 dönüyor ama gövde BOŞ NESNE ({}) —
       yani "çıkış yapılmış" gibi görünüyor ve hata da vermiyor. Jeton
       başlığıyla gerçek kullanıcı geliyor. Canlıda ölçüldü; ilk sürüm
       başlıksız istek attığı için ad HİÇBİR ZAMAN gelmiyordu ve buna bağlı
       olan bahsedilmeler sessizce çalışmıyordu.
       Yanıt e-posta da taşıyor; YALNIZ `username` okunuyor, gerisi hiçbir
       yere yazılmıyor. */
    var jeton = '';
    try { jeton = rosterJeton(); } catch (e) {}
    var basliklar = { accept: 'application/json' };
    if (jeton) basliklar.Authorization = 'Bearer ' + jeton;
    fetch('https://kick.com/api/v1/user', { credentials: 'include', cache: 'no-store',
                                            headers: basliklar })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        var ad = (j && (j.username || (j.user && j.user.username))) || '';
        if (ad) { pkSayfaAdi = String(ad); pkAdBos = 0; } else pkAdBos = Date.now();
        pkAdIstek = false;
        if (bitince) bitince(pkSayfaAdi);
      })
      .catch(function () { pkAdIstek = false; pkAdBos = Date.now(); if (bitince) bitince(''); });
  }

  /* Eşzamanlı okuyucu: elde olan adı verir, yoksa arka planda çekmeyi
     başlatır. Bahsedilmeler her turda çağırdığı için bir sonraki turda
     kendiliğinden doğru ada kavuşuyor. */
  function pkKendiAdim() {
    if (pkBenimAd) return pkBenimAd;
    if (pkSayfaAdi) return pkSayfaAdi;
    try { pkKendiAdimCek(null); } catch (e) {}
    return '';
  }


  /* Gömülü sayfanın son çizicisi — sekme değişince yerinde yeniden çizmek için.
     Eskiden sekmeye basınca panelCiz() çağrılıyordu; o BİZİM paneli tazeliyor,
     Kick'in içine gömülü sayfayı değil. Sonuç: sekme değişiyor ama ekran
     değişmiyordu. */
  var pkAyarSayfaAcik = false;
  var pkAyarSonCiz = null, pkAyarSonKap = null;
  function pkAyarTazele() {
    if (!pkAyarSonCiz || !pkAyarSonKap || !document.contains(pkAyarSonKap)) return false;
    pkAyarSonKap.textContent = '';
    try { pkAyarSonCiz(pkAyarSonKap); } catch (e) {}
    return true;
  }
  /* Panel ya da gömülü sayfa — hangisi açıksa onu tazele. */
  function pkNereyiTazele() { if (!pkAyarTazele()) panelCiz(); }

  var I_MENU = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:16px;height:16px;display:block">' +
    '<rect x="3" y="12" width="4.4" height="9" rx="1.2"/><rect x="9.8" y="7" width="4.4" height="14" rx="1.2"/><rect x="16.6" y="3" width="4.4" height="18" rx="1.2"/></svg>';

  /* Sürüm yükseltmesinde sayfada kalmış eski kanal menüsü düğmelerini topla. */
  function eskiKanalMenusuTemizle() {
    var e = document.querySelectorAll('[data-pk-kmenu]');
    for (var i = 0; i < e.length; i++) e[i].remove();
  }

  /* Sohbet başlığındaki ayrı PureKick düğmesi KALDIRILDI: araçlar artık Kick'in
     kendi "Sohbet Ayarları" panelinde, "PureKick Sohbet Ayarları" satırının
     altında. İki ayrı giriş noktası olmasın. Sürüm yükseltmesinde sayfada
     kalmış düğmeyi ve kenar boşluğu düzeltmesini geri alıyoruz. */
  function sohbetMenuButonuKur() {
    var e = document.querySelectorAll('[data-pk-smenu]');
    for (var i = 0; i < e.length; i++) e[i].remove();
    sohbetBaslikGrubuKaldir();
  }

  /* Menü ikonundaki kırmızı nokta — her turda tazelenir. */
  function sohbetMenuNokta() {
    var n = document.querySelector('[data-pk-smenu-nokta]');
    if (n) n.style.display = (settings.bahsedilme !== false && pkMentionYeni > 0) ? 'block' : 'none';
  }

  /* ---------- Bahsedilmeler paneli (sohbet alanını kaplar) ---------- */
  var MENTION_ID = 'pk-mention-panel';
  function mentionPanelKapat() { var p = document.getElementById(MENTION_ID); if (p) p.remove(); }

  /* Arama artık ayrı bir düğme değil, araç menüsünün bir maddesi. Yazma
     satırındaki eski düğme varsa temizlenir (sürüm yükseltmesinde kalmasın). */
  function araButonuKur() {
    var e = document.querySelectorAll('[data-pk-ara]');
    for (var i = 0; i < e.length; i++) e[i].remove();
  }

  /* Panel yalnızca MESAJ ALANINI kaplar, sohbet sütununun tamamını değil —
     Kick'in kendi tam ekran panelleri de mesaj yazma kutusunu açıkta bırakıyor.
     Sütunun çocukları (ölçüldü): üst şerit 33px · mesaj alanı 546px (zaten
     `relative`) · giriş+araç çubuğu 166px. Doğru çocuğu ANLAMCA seçiyoruz:
     içinde sohbet mesajı (`[data-index]`) olan; yoksa en uzun olan. */
  /* SÜTUNU ANLAMCA BUL, ÖLÇÜYLE DEĞİL.
     Eskiden sohbet sütunu "genişliği 300-460 px arasında" bir ata aranarak
     bulunuyordu. Kick'in sohbet paneli SÜRÜKLENEREK YENİDEN BOYUTLANDIRILABİLİYOR;
     kullanıcı paneli genişletince (canlıda ölçüldü: 487 px) hiçbir ata eşleşmiyor,
     fonksiyon null dönüyor ve arama paneli hiç açılmıyordu — sessizce, hatasız.
     Doğru tanım ölçü değil İLİŞKİ: sohbet sütunu, hem araç çubuğunu hem mesaj
     listesini kapsayan en küçük atadır. Panel genişliğinden bağımsız çalışır. */
  function araAlan() {
    /* Sohbet mesaj listesi — Kick'te #chatroom-messages. Eski yol (gönder
       düğmesinden yukarı tırmanıp [data-index] içeren atayı bulmak) yedek. */
    var m = pkIlkGorunur('#chatroom-messages');
    if (m) return m;
    var yuva = araYuva(); if (!yuva) return null;
    var sut = null;
    for (var e = yuva.parentElement, i = 0; e && e !== document.body && i < 10; e = e.parentElement, i++) {
      if (e.querySelector('[data-index]')) { sut = e; break; }
    }
    if (!sut) return null;
    var enUzun = null, enH = 0;
    for (var c = 0; c < sut.children.length; c++) {
      var ch = sut.children[c], h = ch.getBoundingClientRect().height;
      if (ch.querySelector('[data-index]')) return ch;
      if (!ch.contains(yuva) && h > enH) { enH = h; enUzun = ch; }
    }
    return enUzun || sut;
  }

  /* Sohbet sütunu (mesaj listesi + alt şerit birlikte) — bindirme kurtarma
     kutusu gibi şeyler buraya konuyor. */
  function sohbetSutunu() {
    return pkIlkGorunur('#channel-chatroom') || (function () {
      var m = araAlan();
      return m ? m.parentElement : null;
    })();
  }

  function araKapat() { var p = document.getElementById(ARA_ID); if (p) p.remove(); }

  /* 10.5 — İKİ FİLTRE BİRLİKTE. Eskiden "kelime" ya da "kullanıcı" modundan
     biri seçiliyordu; artık ikisi aynı anda uygulanabiliyor (boş bırakılan
     filtre hepsini kabul eder). Kick'in kendi sohbet kaydı da böyle çalışıyor. */
  function araSonuclari() {
    var ad = (araAdFiltre || '').trim().toLowerCase();
    var ic = (araSorgu || '').trim().toLowerCase();
    var out = [];
    for (var k in scListe) {
      if (!Object.prototype.hasOwnProperty.call(scListe, k)) continue;
      var v = scListe[k];
      if (ad && k.indexOf(ad) < 0) continue;              // kullanıcı filtresi
      for (var i = 0; i < v.mesajlar.length; i++) {
        var m = v.mesajlar[i];
        if (ic) {
          var yazi = '';
          for (var j = 0; j < m.p.length; j++) yazi += ' ' + (m.p[j].t === 'y' ? m.p[j].v : m.p[j].a);
          if (yazi.toLowerCase().indexOf(ic) < 0) continue;
        }
        out.push({ v: v, m: m });
      }
    }
    out.sort(function (a, b) { return a.m.ts - b.m.ts; });   // eskiden yeniye
    return out;
  }

  /* ---- Profil kartındaki eylem butonlarını SADECE İKON yap ----
   * "Takip Et / Abonelik Hediye Edin / Kullanıcıyı Sustur" yazılarıyla birlikte
   * 324px'lik karta sığmayıp alt alta diziliyordu; ikon olunca yan yana tek
   * satıra iniyorlar. Kick'in DOM'unu bozmuyoruz: yalnızca etiketi gizleyip
   * butonu kareye çekiyoruz, metin `title` olarak kalıyor (üzerine gelince
   * görünür). React yeniden çizerse bir sonraki turda tekrar uygulanır.
   *
   * Ölçüm: buton `svg + SPAN(etiket)`, 40px yüksek, ikon 16×16, kap `gap-1`.
   * `font-size:0` da veriyoruz ki span'e sarılmamış çıplak metin varsa o da
   * kaybolsun; ikon `em` tabanlı olabileceği için boyutu AÇIKÇA yazılıyor,
   * yoksa yazı tipi sıfırlanınca ikon da yok oluyor. */
  /* Kick'in EYLEM ŞERİDİ: kartın doğrudan çocuğu olup ilk çocuğu ikonlu bir
     düğme olan kap (♡, ekran görüntüsü…). İki yerden kullanılıyor, tek
     bulucu. */
  function kartEylemKabi(kart) {
    var ck = kart.children;
    for (var k = 0; k < ck.length; k++) {
      var b0 = ck[k].children && ck[k].children[0];
      if (b0 && b0.tagName === 'BUTTON' && b0.querySelector('svg')) return ck[k];
    }
    return null;
  }

  /* Cross-ban düğmesi Kick'in KENDİ eylem şeridine giriyor (kullanıcı isteği:
     "kickteki yapı gibi"). Sınıfı komşu düğmeden kopyalanıyor ki durum
     katmanı/hover davranışı birebir aynı olsun.

     SİMGE DEĞİL, YAZILI VE GENİŞ: 40×40 bir ikon olarak ne olduğu
     anlaşılmıyordu (kullanıcı bildirdi). Şeritteki boş yeri `flex:1` ile
     dolduruyor, içinde simge + "Cross-Ban" yazısı var.
     `font-size` AÇIKÇA VERİLİYOR: `profilKartiIkonlari` komşu düğmelerin
     yazısını `font-size:0` ile gizliyor, kopyaladığımız sınıfla o miras
     kalırsa bizim yazımız da görünmezdi. */
  function crossKartIkonu(kart, ad) {
    var eski = kart.querySelector('.pk-cross-btn');
    if (settings.crossBan !== true || !ad) { if (eski) eski.remove(); return; }
    if (eski) {
      if (eski.getAttribute('data-pk-cad') === ad) return;
      eski.remove();
    }
    var kap = kartEylemKabi(kart); if (!kap) return;
    var ornek = kap.querySelector('button');
    var b = el('button', 'pk-cross-btn ' + (ornek ? (ornek.className || '') : ''));
    b.setAttribute('data-pk-cad', ad);
    b.style.cssText = 'flex:1 1 auto;min-width:0;height:40px;padding:0 10px;' +
      'display:inline-flex;align-items:center;justify-content:center;gap:6px;' +
      'font-size:13px;font-weight:600;line-height:1;white-space:nowrap;' +
      'cursor:pointer;color:#ff8f8f';
    var ikonKap = el('span', '');
    ikonKap.style.cssText = 'display:inline-flex;flex:none';
    ikonKap.innerHTML = I_MS_BAN;
    var sv = ikonKap.querySelector('svg');
    if (sv) { sv.style.width = '15px'; sv.style.height = '15px'; sv.style.display = 'block'; }
    b.appendChild(ikonKap);
    b.appendChild(el('span', '', t('pkCrossDugme')));
    b.title = t('pkCrossBaslik', [ad]);
    b.setAttribute('aria-label', b.title);
    b.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      try { crossPencereAc(ad); } catch (er) {}
    });
    kap.appendChild(b);
  }

  function profilKartiIkonlari(kart) {
    var kap = kartEylemKabi(kart);
    if (!kap) return;
    for (var i = 0; i < kap.children.length; i++) {
      var b = kap.children[i];
      if (b.tagName !== 'BUTTON') continue;
      if (b.classList.contains('pk-cross-btn')) continue;   // bizimki, ölçüsü hazır
      var svg = b.querySelector('svg');
      if (!svg) continue;
      var etiket = (b.textContent || '').trim();
      if (etiket && !b.title) b.title = etiket;
      for (var c = 0; c < b.children.length; c++) {
        var ch = b.children[c];
        if (ch.tagName.toLowerCase() === 'svg' || ch.querySelector('svg')) continue;
        if (ch.style.display !== 'none') ch.style.display = 'none';
      }
      if (svg.style.width !== '16px') { svg.style.width = '16px'; svg.style.height = '16px'; }
      if (b.style.width !== '40px') {
        b.style.width = '40px'; b.style.minWidth = '40px'; b.style.padding = '0';
        /* Yükseklik AÇIKÇA 40px: `font-size:0` içerik yüksekliğini de düşürüp
           butonu 32px'e indiriyordu (ölçüldü) → Kick'in kendi buton boyu korunur. */
        b.style.height = '40px';
        b.style.justifyContent = 'center'; b.style.fontSize = '0';
      }
    }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * VOD İNDİR — geçmiş yayını diske indirir
   *
   * ÖLÇÜLEN GERÇEKLER (Kuzi kanalında, 3,9 saatlik yayın):
   *  · Kaynak: `api/v2/channels/<slug>/videos` → `.source` (temiz master.m3u8).
   *    Sayfa adresindeki kimlik API'deki `video.uuid` ile EŞLEŞMİYOR ve tekil
   *    video uç noktası yok (v1/v2 → 404). Bu yüzden eşleme OYNATICI SÜRESİYLE
   *    yapılır (ölçüm: oynatıcı 14033 sn ↔ kayıt 14032 sn, sonraki aday 2533 sn
   *    uzakta — belirsizlik yok). VOD reklam engellememiz de aynı yöntemi kullanıyor.
   *  · 5 kalite: 1080p 4,77 Mbps … 160p 0,23 Mbps. 1080p'de 1123 parça ≈ 8,4 GB.
   *
   * Bu boyut Blob'a SIĞMAZ (sekme çöker) → File System Access API ile parçalar
   * geldikçe diske akıtılır. `showSaveFilePicker` kullanıcı hareketi ister, bu
   * yüzden kalite düğmesine basıldığı anda ÇAĞRILIR (önce await edilmez).
   * Çıktı ham .ts birleşimidir: VLC/MPV oynatır, mp4 için remux gerekir.
   * ══════════════════════════════════════════════════════════════════════ */
  var vodIndirmeAktif = false;
  var VOD_ESZAMAN = 12;   // aynı anda indirilen parça — ölçümle seçildi, bkz. vodAkit
  var vodSonUrl = null;   // { url, yol } — worker-hook'un duyurduğu, oynatıcının kullandığı adres

  function vodSayfasi() {
    var p = (location.pathname || '').split('/').filter(Boolean);
    return (p.length === 3 && p[1] === 'videos') ? { slug: p[0].toLowerCase(), id: p[2] } : null;
  }

  /* Oynatıcı süresiyle eşleşen kaydın temiz master.m3u8 adresi. */
  function vodKaynakBul() {
    var s = vodSayfasi(); if (!s) return Promise.resolve(null);
    /* 1) ÖNCE oynatıcının fiilen kullandığı adres (worker-hook duyurur). Bu yol
       her VOD'da çalışır. 2) Olmazsa süre eşlemesine düşeriz — ama o yalnızca
       son ~13 yayını kapsayan listeye bakabildiği için eski yayınlarda boş döner
       (kullanıcı "kaynak bulunamadı" hatasını böyle almıştı). */
    if (vodSonUrl && vodSonUrl.yol === location.pathname && vodSonUrl.url) {
      return Promise.resolve({ url: vodSonUrl.url, baslik: vodBaslikBul(s.slug), tarih: '' });
    }
    var vid = document.querySelector('video');
    var sure = vid && isFinite(vid.duration) ? vid.duration : 0;
    if (!sure) return Promise.resolve(null);
    return fetch('https://kick.com/api/v2/channels/' + encodeURIComponent(s.slug) + '/videos',
      { credentials: 'include' })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        if (!Array.isArray(j)) return null;
        var en = null, enFark = 1e9;
        for (var i = 0; i < j.length; i++) {
          if (!j[i].source) continue;
          var f = Math.abs(Math.round((j[i].duration || 0) / 1000) - Math.round(sure));
          if (f < enFark) { enFark = f; en = j[i]; }
        }
        if (!en || enFark > 5) return null;             // güvenli eşleşme yoksa indirme
        return { url: en.source, baslik: en.session_title || s.slug, tarih: (en.created_at || '').slice(0, 10) };
      })
      .catch(function () { return null; });
  }

  /* Yayın başlığı — GÖRÜNÜR bir başlık öğesinden.
     `document.querySelector('h1,h2')` KULLANILMAZ: sayfadaki ilk başlık Kick'in
     GİZLİ çerez panelindeki <h2> oluyordu ve indirme penceresinin başlığında
     "Your Privacy Choices" yazıyordu (canlı görüldü). Bu yüzden hem görünürlük
     hem de o panelin dışında olma şartı aranıyor. */
  function vodBaslikBul(slug) {
    var h = document.querySelectorAll('h1, h2');
    for (var i = 0; i < h.length; i++) {
      if (h[i].closest('#onetrust-consent-sdk')) continue;   // çerez/gizlilik paneli
      var r = h[i].getBoundingClientRect();
      if (r.width < 40 || r.height < 10) continue;           // gizli → atla
      var t = (h[i].textContent || '').trim();
      if (t) return t.slice(0, 60);
    }
    return slug;
  }

  /* master.m3u8 → kalite listesi (yüksekten düşüğe). */
  function vodKaliteler(masterUrl) {
    return fetch(masterUrl).then(function (r) { return r.text(); }).then(function (txt) {
      var sat = txt.split('\n'), taban = masterUrl.slice(0, masterUrl.lastIndexOf('/') + 1), out = [];
      for (var i = 0; i < sat.length; i++) {
        if (!/^#EXT-X-STREAM-INF/i.test(sat[i])) continue;
        var res = (sat[i].match(/RESOLUTION=(\d+)x(\d+)/i) || [])[2] || '?';
        var bw = parseInt((sat[i].match(/BANDWIDTH=(\d+)/i) || [])[1] || '0', 10);
        var yol = (sat[i + 1] || '').trim();
        if (!yol) continue;
        out.push({ ad: res === '?' ? 'Bilinmiyor' : res + 'p', bw: bw,
                   url: /^https?:/i.test(yol) ? yol : taban + yol });
      }
      out.sort(function (a, b) { return b.bw - a.bw; });
      return out;
    });
  }

  function vodBoyutYazi(bayt) {
    if (bayt >= 1e9) return (bayt / 1e9).toFixed(2) + ' GB';
    if (bayt >= 1e6) return (bayt / 1e6).toFixed(0) + ' MB';
    return (bayt / 1e3).toFixed(0) + ' KB';
  }

  /* Kick'in "Abone Ol" penceresiyle AYNI kalıp (ölçüldü): ortada, `bg-surface-base`,
     4px köşe, `p-4`, `gap-2`, 408px genişlik; başlık satırı `flex items-center
     justify-between`, altında `text-xs` açıklama ve `h-px bg-outline-decorative`
     ayraç. Arkada karartma perdesi var.
     İndirme sürerken perdeye tıklamak KAPATMAZ — yanlışlıkla saatlik indirmeyi
     iptal ettirmemek için; kapatmak isteyen ✕ ile bilerek iptal eder. */
  function vodKapat() {
    var p = document.getElementById('pk-vod'); if (p) p.remove();
    vodIndirmeAktif = false;
  }
  function vodPencere(baslik, aciklama) {
    vodKapat();
    var perde = el('div', '');
    perde.id = 'pk-vod';
    perde.style.cssText = 'position:fixed;inset:0;z-index:2147483200;background:rgba(0,0,0,.6);' +
      'display:flex;align-items:center;justify-content:center;padding:16px';
    var kart = el('div', 'bg-surface-base flex h-fit flex-col rounded text-sm shadow-lg gap-2 p-4');
    kart.style.cssText = 'width:408px;max-width:100%;max-height:80vh;overflow-y:auto;color:#fff';
    var bas = el('div', 'flex items-center justify-between gap-2');
    bas.appendChild(el('div', 'min-w-0 truncate text-base font-bold text-white', baslik));
    var kapat = el('button', 'shrink-0 inline-flex items-center justify-center rounded state-layer-surface text-white');
    kapat.style.cssText = 'width:32px;height:32px;padding:0;background:none;border:0;cursor:pointer';
    kapat.innerHTML = I_KAPAT;
    kapat.addEventListener('click', function (e) { e.stopPropagation(); vodKapat(); });
    bas.appendChild(kapat);
    kart.appendChild(bas);
    if (aciklama) kart.appendChild(el('p', 'text-surface-onSurfaceSecondary text-xs', aciklama));
    kart.appendChild(el('div', 'h-px min-h-px shrink-0 bg-outline-decorative w-full grow-0'));
    perde.addEventListener('click', function (e) { if (e.target === perde && !vodIndirmeAktif) vodKapat(); });
    perde.appendChild(kart);
    pkKatmanaEkle(perde);
    return kart;
  }

  function vodIndirBaslat() {
    if (vodIndirmeAktif) { toast(t('pkTIndirmeSuruyor')); return; }
    if (typeof window.showSaveFilePicker !== 'function') {
      toast(t('pkTIndirmeDesteklenmiyor')); return;
    }
    var kart = vodPencere(t('pkVodIndirBaslik'),
      t('pkVodIndirNot'));
    var govde = el('div', 'flex flex-col gap-2');
    kart.appendChild(govde);
    var bilgi = el('div', 'text-sm text-subtle', t('pkKaynakBulunuyor'));
    govde.appendChild(bilgi);
    vodKaynakBul().then(function (k) {
      if (!k) { bilgi.textContent = t('pkKaynakYok'); return null; }
      kart.firstChild.firstChild.textContent = k.baslik;
      bilgi.textContent = t('pkKaliteSecin');
      return vodKaliteler(k.url).then(function (kal) { return { k: k, kal: kal }; });
    }).then(function (d) {
      if (!d) return;
      if (!d.kal.length) { bilgi.textContent = t('pkKaliteOkunamadi'); return; }
      var sure = (document.querySelector('video') || {}).duration || 0;
      var toplamDk = Math.max(1, Math.round(sure / 60));
      /* ARALIK SEÇİMİ: tüm yayın yerine yalnızca bir dakika aralığı indirilebilir.
         Uzun yayınlarda (ölçüm: 3,9 saat = 8,4 GB) tamamını indirmek çoğu zaman
         gereksiz. Parçalar `#EXTINF` süreleriyle seçildiği için kesim isabetli. */
      var tumu = true;                                    // başlangıçta "Tümü" seçili

      var arKutu = el('div', 'flex flex-col gap-2');
      var modSatir = el('div', 'flex items-center gap-1.5');
      var girSatir = el('div', 'flex items-center gap-2');
      var kaliteKutu = el('div', 'flex flex-col gap-1');

      /* ARALIK ÇUBUĞU — iki tutamaklı, sürüklenebilir. Hazır kütüphane yok;
         izleyici (pointer) olaylarıyla kendi çizimimiz. Oranlar yayın süresine
         göre; tutamaklar birbirini geçemez (en az 10 sn aralık kalır). */
      var oranBas = 0, oranBit = 1;                       // 0..1
      var cubuk = el('div', 'bg-surface-highest rounded relative');
      cubuk.style.cssText = 'height:40px;cursor:pointer;overflow:hidden;user-select:none';
      var bant = el('div', 'bg-secondary-base absolute');
      bant.style.cssText = 'top:0;bottom:0;left:0;right:0';
      cubuk.appendChild(bant);
      function tutamak() {
        var t = el('div', 'absolute rounded');
        t.style.cssText = 'top:3px;bottom:3px;width:10px;background:#fcfcfc;cursor:ew-resize;' +
          'box-shadow:0 2px 6px rgba(0,0,0,.5)';
        cubuk.appendChild(t); return t;
      }
      var tBas = tutamak(), tBit = tutamak();
      var etiketSatir = el('div', 'flex items-center justify-between');
      var etBas = el('span', 'text-surface-onSurfaceSecondary text-xs', '');
      var etOrta = el('span', 'text-white text-xs font-semibold', '');
      var etBit = el('span', 'text-surface-onSurfaceSecondary text-xs', '');
      etiketSatir.appendChild(etBas); etiketSatir.appendChild(etOrta); etiketSatir.appendChild(etBit);
      girSatir.style.flexDirection = 'column'; girSatir.style.alignItems = 'stretch';
      girSatir.appendChild(cubuk); girSatir.appendChild(etiketSatir);

      function sureYaz(sn) {
        sn = Math.max(0, Math.round(sn));
        var s = sn % 60, d = Math.floor(sn / 60) % 60, sa = Math.floor(sn / 3600);
        var ik = function (x) { return (x < 10 ? '0' : '') + x; };
        return sa ? (sa + ':' + ik(d) + ':' + ik(s)) : (d + ':' + ik(s));
      }
      function cubukTazele() {
        bant.style.left = (oranBas * 100) + '%';
        bant.style.right = ((1 - oranBit) * 100) + '%';
        tBas.style.left = 'calc(' + (oranBas * 100) + '% - 5px)';
        tBit.style.left = 'calc(' + (oranBit * 100) + '% - 5px)';
        etBas.textContent = sureYaz(oranBas * sure);
        etBit.textContent = sureYaz(oranBit * sure);
        etOrta.textContent = t('pkSecili', [sureYaz((oranBit - oranBas) * sure)]);
      }
      /* Parametre `kol` — `t` DEĞİL: `t` çeviri fonksiyonunun adı ve burada
         gölgelenirse gövdeye sonradan eklenecek ilk çeviri çağrısı sessizce
         patlar. (Yorumda bile örnek çağrı yazma: dil tarayıcıları ham kaynağı
         okuyor, uydurma anahtarı sözlükte eksik sanıyor.) */
      function surukle(kol, hangi) {
        kol.addEventListener('pointerdown', function (e) {
          e.preventDefault(); e.stopPropagation();
          kol.setPointerCapture(e.pointerId);
          var oynat = function (ev) {
            var r = cubuk.getBoundingClientRect();
            var o = Math.min(1, Math.max(0, (ev.clientX - r.left) / r.width));
            var enAz = sure > 0 ? (10 / sure) : 0.01;      // en az ~10 sn
            if (hangi === 'bas') oranBas = Math.min(o, oranBit - enAz);
            else oranBit = Math.max(o, oranBas + enAz);
            cubukTazele(); kaliteCiz();
          };
          var birak = function () {
            kol.removeEventListener('pointermove', oynat);
            kol.removeEventListener('pointerup', birak);
          };
          kol.addEventListener('pointermove', oynat);
          kol.addEventListener('pointerup', birak);
        });
      }
      surukle(tBas, 'bas'); surukle(tBit, 'bit');
      cubukTazele();

      [{ id: true, ad: t('pkTumu') }, { id: false, ad: t('pkAralikSec') }].forEach(function (m) {
        var b = el('button', 'flex-1 min-w-0 h-8 px-1 rounded text-xs font-semibold transition-all ' +
          'flex items-center justify-center whitespace-nowrap', m.ad);
        b.setAttribute('data-pk-mod', m.id ? '1' : '0');
        b.addEventListener('click', function (e) {
          e.preventDefault(); e.stopPropagation();
          tumu = m.id; kaliteCiz();
        });
        modSatir.appendChild(b);
      });
      arKutu.appendChild(modSatir); arKutu.appendChild(girSatir);

      function aralikSn() {
        if (tumu || !sure) return { bas: 0, bit: sure || 1e9 };
        return { bas: oranBas * sure, bit: oranBit * sure };
      }

      function kaliteCiz() {
        // mod düğmelerinin görünümü
        [].forEach.call(modSatir.children, function (b) {
          var secili = (b.getAttribute('data-pk-mod') === '1') === tumu;
          b.className = 'flex-1 min-w-0 h-8 px-1 rounded text-xs font-semibold transition-all ' +
            'flex items-center justify-center whitespace-nowrap ' +
            (secili ? 'bg-secondary-base text-white' : 'text-subtle betterhover:hover:bg-surface-highest');
        });
        girSatir.style.display = tumu ? 'none' : 'flex';
        if (!tumu) cubukTazele();
        var ar = aralikSn();
        var sn = Math.max(1, ar.bit - ar.bas);
        kaliteKutu.textContent = '';
        d.kal.forEach(function (q) {
          var tahmin = vodBoyutYazi(q.bw * sn / 8);
          var b = el('button', 'bg-secondary-base hover:brightness-125 text-white rounded text-sm font-semibold ' +
            'flex items-center justify-between transition-all');
          b.style.cssText = 'height:40px;padding:0 12px;border:0;cursor:pointer';
          b.appendChild(el('span', '', q.ad));
          b.appendChild(el('span', 'text-surface-onSurfaceSecondary text-xs', '~' + tahmin));
          /* showSaveFilePicker KULLANICI HAREKETİ ister → tıklamanın ilk işi o olmalı,
             öncesinde await edersek tarayıcı "user gesture yok" diye reddediyor. */
          b.addEventListener('click', function () {
            var a2 = aralikSn();
            var adAl = (d.k.baslik || 'kick-vod').replace(/[\\/:*?"<>|]+/g, '-').slice(0, 60);
            var ek = tumu ? '' : (' ' + Math.round(a2.bas / 60) + '-' + Math.round(a2.bit / 60) + 'dk');
            window.showSaveFilePicker({
              suggestedName: adAl + ' ' + (d.k.tarih || '') + ' ' + q.ad + ek + '.ts',
              types: [{ description: 'Video (TS)', accept: { 'video/mp2t': ['.ts'] } }]
            }).then(function (fh) {
              return fh.createWritable().then(function (ws) { vodAkit(govde, q, ws, a2); });
            }).catch(function () { /* kullanıcı iptal etti */ });
          });
          kaliteKutu.appendChild(b);
        });
      }

      govde.appendChild(arKutu);
      govde.appendChild(kaliteKutu);
      govde.appendChild(el('div', 'text-surface-onSurfaceSecondary text-xs',
        t('pkVodTsNotN', [String(toplamDk)])));
      kaliteCiz();
    }).catch(function () { bilgi.textContent = t('pkHazirlanamadi'); });
  }

  /* Parçaları sırayla indirip diske yazar. Bellekte biriktirme YOK. */
  function vodAkit(govde, kalite, ws, aralik) {
    vodIndirmeAktif = true;
    govde.textContent = '';
    /* İndirme sürerken sağ üstteki ✕ gizlenir: aynı işi yapan iki düğme
       (✕ ve "İptal") kafa karıştırıyordu. Bitince/iptal edilince geri gelir
       ki pencere kapatılabilsin. */
    var kart = govde.parentElement;
    var basSatir = kart && kart.firstElementChild;
    var kapatX = basSatir && basSatir.lastElementChild;
    function kapatXGoster() { if (kapatX) kapatX.style.display = ''; }
    if (kapatX) kapatX.style.display = 'none';
    var ust = el('div', 'flex items-center gap-2');
    ust.appendChild(el('div', 'flex-1 min-w-0 truncate text-sm font-semibold', t('pkIndiriliyorEki', [kalite.ad])));
    var iptalBtn = el('button', 'shrink-0 text-sm font-semibold text-surface-onSurfaceSecondary hover:text-white');
    iptalBtn.style.cssText = 'background:none;border:0;cursor:pointer;padding:0';
    iptalBtn.textContent = t('pkIptal');
    ust.appendChild(iptalBtn);
    govde.appendChild(ust);
    var cubukKap = el('div', 'rounded'); cubukKap.style.cssText = 'height:6px;background:rgba(255,255,255,.14)';
    var cubuk = el('div', 'bg-primary-base rounded'); cubuk.style.cssText = 'height:6px;width:0%;transition:width .2s';
    cubukKap.appendChild(cubuk); govde.appendChild(cubukKap);
    var durum = el('div', 'text-sm text-surface-onSurfaceSecondary', t('pkParcaListesi'));
    govde.appendChild(durum);

    var iptal = false;
    iptalBtn.addEventListener('click', function () { iptal = true; });

    var yazilan = 0;
    fetch(kalite.url).then(function (r) { return r.text(); }).then(function (txt) {
      var taban = kalite.url.slice(0, kalite.url.lastIndexOf('/') + 1);
      /* Parçaları SÜRELERİYLE birlikte oku: aralık seçimi `#EXTINF` değerlerine
         göre kesiliyor, böylece kesim yayının gerçek zamanına oturuyor. */
      var sat = txt.split('\n'), parcalar = [], sureler = [], sonSure = 0;
      for (var s = 0; s < sat.length; s++) {
        var l = sat[s].trim();
        if (/^#EXTINF/i.test(l)) { sonSure = parseFloat(l.split(':')[1]) || 0; continue; }
        if (!l || l.charAt(0) === '#') continue;
        parcalar.push(/^https?:/i.test(l) ? l : taban + l);
        sureler.push(sonSure);
      }
      if (!parcalar.length) throw new Error(t('pkParcaYok'));
      if (aralik && (aralik.bas > 0 || aralik.bit < 1e9)) {
        /* DEĞİŞKEN ADI `t` OLAMAZ — `t` çeviri fonksiyonu. `var` bildirimi bu
           geri çağrının TEPESİNE çıktığı için aralık seçilmese bile gövdenin
           tamamında `t` bir sayı/undefined oluyordu; ilk parça diske yazılıp
           ilerleme satırı yazılırken `t(...)` "t is not a function" diye
           patlıyor ve indirme her seferinde duruyordu. */
        var secili = [], sn = 0;
        for (var q = 0; q < parcalar.length; q++) {
          var b0 = sn, b1 = sn + (sureler[q] || 0); sn = b1;
          if (b1 > aralik.bas && b0 < aralik.bit) secili.push(parcalar[q]);
        }
        if (secili.length) parcalar = secili;
      }
      var n = parcalar.length, bekleyen = new Array(n), i = 0;
      var basladi = performance.now();
      /* KAYAN PENCERE: parçalar ÖNDEN paralel indirilir, diske SIRAYLA yazılır.
         Tek tek indirirken bağlantı boş kalıyordu — ölçüm (720p, 100 Mbps hat):
         sıralı 13 Mbps · 4 eşzamanlı 58 · 8 → 78 · 12 → 88,7 · 16 → 88,8.
         12'de hat doyuyor, fazlası yalnızca bellek harcıyor. Bellekte en fazla
         12 parça durur (~1080p'de 80 MB), tamamı DEĞİL. */
      function basla(idx) {
        if (idx >= n || bekleyen[idx]) return;
        var p = vodParcaGetir(parcalar[idx], 3);
        p.catch(function () {});          // hatayı sonraki() yakalayacak; "unhandled" uyarısı çıkmasın
        bekleyen[idx] = p;
      }
      for (var s = 0; s < VOD_ESZAMAN && s < n; s++) basla(s);
      function sonraki() {
        if (iptal || !vodIndirmeAktif) {
          vodIndirmeAktif = false;
          kapatXGoster();
          durum.textContent = t('pkIndirmeIptal', [vodBoyutYazi(yazilan)]);
          iptalBtn.style.display = 'none';
          return ws.abort ? ws.abort() : ws.close();
        }
        if (i >= n) {
          return ws.close().then(function () {
            vodIndirmeAktif = false;
            kapatXGoster();
            iptalBtn.textContent = t('pkKapat');
            iptalBtn.addEventListener('click', vodKapat);
            cubuk.style.width = '100%';
            durum.textContent = t('pkIndirmeBitti', [vodBoyutYazi(yazilan)]);
            toast(t('pkTVodIndirildi'));
          });
        }
        return bekleyen[i].then(function (buf) {
          return ws.write(buf).then(function () {
            bekleyen[i] = null;                 // yazılan parçayı bellekten bırak
            yazilan += buf.byteLength; i++;
            basla(i + VOD_ESZAMAN - 1);         // pencereyi bir ileri kaydır
            var yuzde = Math.round(i / n * 100);
            var gecen = (performance.now() - basladi) / 1000;
            var hiz = gecen > 0 ? (yazilan * 8 / 1e6) / gecen : 0;
            cubuk.style.width = yuzde + '%';
            durum.textContent = t('pkIndirmeIlerleme', [String(yuzde), i + '/' + n]) + ' · ' + vodBoyutYazi(yazilan) + ' · ' + hiz.toFixed(0) + ' Mbps';
            return sonraki();
          });
        });
      }
      return sonraki();
    }).catch(function (e) {
      vodIndirmeAktif = false;
      kapatXGoster();
      iptalBtn.style.display = 'none';
      /* Bozuk/yarım dosya bırakmaktansa açıkça hata göster. */
      durum.textContent = t('pkIndirmeDurdu', [(e && e.message) ? e.message : t('pkHata')]);
      try { ws.abort ? ws.abort() : ws.close(); } catch (er) {}
    });
  }

  /* Tek parça + yeniden deneme; sessizce atlamayız, yoksa dosya bozulur. */
  function vodParcaGetir(url, kalanDeneme) {
    return fetch(url).then(function (r) {
      if (!r.ok) throw new Error('HTTP ' + r.status);
      return r.arrayBuffer();
    }).catch(function (e) {
      if (kalanDeneme <= 1) throw e;
      return new Promise(function (res) { setTimeout(res, 800); })
        .then(function () { return vodParcaGetir(url, kalanDeneme - 1); });
    });
  }

  /* Hedef hızı uygular. İKİ ÖLÇÜLMÜŞ DAVRANIŞ buna şekil verdi:
     1) Kick oynatıcısı ATLAMA/YENİDEN TAMPONLAMA anında `playbackRate`i 1'e
        çekiyor (~200 ms içinde). Kalıcı değil — ileride veri varken 2x'i
        tutuyor ve gerçekten hızlandırıyor (ölçüm: ileri tampon 10 sn'de
        25,08 → 15,08). Bu yüzden sıfırlamayı geri alıyoruz.
     2) Canlı kenara yetişildiyse hızlı oynatmanın yakıtı bitmiştir; ısrar
        edersek tampon kurur ve yayın donar. Orada hedef 1x olur. */

  /* Videonun KENDİ oynatıcı kabını bulur: videodan yukarı çıkıp, içinde bu ölçüde
     en az 2 buton barındıran ilk atayı alır — o kap kontrol şeridini içeren kaptır.
     Sınıf adına bağlı değil, Kick arayüzü değişse de ayakta kalır. */
  function oynaticiKabi(v, vr) {
    /* Kick'in kendi oynatıcı kabı. Eski yol (videodan yukarı tırmanıp iki
       düğmeli kap arama) yedek olarak duruyor. */
    var k = pkIlkGorunur('#injected-channel-player') || pkIlkGorunur('#injected-embedded-channel-player-video');
    if (k && k.contains(v)) return k;
    var e = v.parentElement, kat = 0;
    while (e && e !== document.body && kat++ < 8) {
      var er = e.getBoundingClientRect();
      if (er.height > vr.height + 160 || er.width > vr.width + 160) return null;
      var n = 0, bs = e.querySelectorAll('button');
      for (var i = 0; i < bs.length; i++) {
        var r = bs[i].getBoundingClientRect();
        if (r.width >= 24 && r.width <= 70 && r.height >= 24 && r.height <= 70) n++;
        if (n >= 2) return e;
      }
      e = e.parentElement;
    }
    return null;
  }

  /* Kontrol şeridi — Kick `z-controls` sınıfını kullanıyor. Bulunamazsa
     çağıran eski geometrik yola düşer. */
  function oynaticiSeridi(kap) {
    if (!kap) return null;
    var a = kap.querySelectorAll('.z-controls');
    for (var i = 0; i < a.length; i++) {
      if (!pkGorunur(a[i])) continue;
      if (a[i].querySelectorAll('button').length >= 2) return a[i];
    }
    return null;
  }

  /* Sayfadaki EN BÜYÜK görünür video = oynatıcı.
     `document.querySelector('video')` DOM sırasındaki ilkini verir; sohbete gömülü
     bir klip ya da önizleme videosu oynatıcıdan önce gelirse butonu onun etrafına
     kurmaya çalışırdık. Boyuta bakmak bu belirsizliği tamamen kaldırıyor. */
  /* SONUÇ ÖNBELLEĞİ — YERLEŞİM ÇAKMASINI KESER.
     Bu işlev ana turda ve 24 ayrı yerden çağrılıyor; içindeki
     `getBoundingClientRect` eşzamanlı yeniden yerleşim zorluyor. Tur zaten
     DOM değişikliğiyle uyandığı için yerleşim hep kirli ve her çağrı tam bir
     reflow'a mal oluyordu.
     PROFİLLEYİCİYLE ÖLÇÜLDÜ: `sesUygula` 942 ms / 91 çağrı (~10 ms) —
     bütün tur maliyetimizin %56'sı, ve o işlevin kendi işi yok denecek kadar
     azdı; masraf buydu.
     `isConnected` yerleşim istemez, bedavadır. */
  var oynVideoOnbellek = null;
  function oynaticiVideosu() {
    if (oynVideoOnbellek && oynVideoOnbellek.isConnected) return oynVideoOnbellek;
    /* Bulunamazsa ÖNBELLEĞE YAZMIYORUZ: video henüz yüklenmemişken null'ı
       saklasak, video gelince bir daha hiç aramazdık. */
    var bulunan = oynaticiVideosuAra();
    if (bulunan) oynVideoOnbellek = bulunan;
    return bulunan;
  }

  function oynaticiVideosuAra() {
    /* Kick videoya doğrudan #video-player kimliğini veriyor. Önce onu dene;
       yoksa eski yol: sayfadaki EN BÜYÜK video. */
    var k = pkIlkGorunur('#video-player');
    if (k && k.tagName === 'VIDEO') return k;
    var vs = document.querySelectorAll('video'), en = null, enAlan = 0;
    for (var i = 0; i < vs.length; i++) {
      var r = vs[i].getBoundingClientRect();
      var alan = r.width * r.height;
      if (alan > enAlan) { enAlan = alan; en = vs[i]; }
    }
    return enAlan > 0 ? en : null;
  }


  /* Oynatıcının altındaki eylem satırına "VOD İndir" butonu (Kick ölçüleriyle). */
  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — OYNATICI EKLERİ  (klavye · ekran görüntüsü · görüntü filtreleri)
   * Üçü de tamamen yerel; hiçbiri ağ isteği yapmaz.
   * ══════════════════════════════════════════════════════════════════════ */

  /* Kick'in kendi kontrol butonlarını etikete göre bulur (tiyatro/tam ekran).
     Doğrudan tıklarız — kendi mantığımızı yazmak yerine Kick'inkini kullanmak
     hem daha az kod hem sürüm değişince kırılmaya daha dayanıklı. */
  /* `ikon`: Kick'in tasarım sistemindeki `data-ds-icon` adı — dilden
     bağımsız ve kalıcı. `desen` metin yedeği: Kick ikon adını değiştirirse
     eski yolla bulmayı dener. */
  function oynaticiButonu(desen, ikon) {
    var v = oynaticiVideosu(); if (!v) return null;
    var vr = v.getBoundingClientRect();
    var kap = oynaticiKabi(v, vr); if (!kap) return null;
    if (ikon) {
      var sv = kap.querySelectorAll('svg[data-ds-icon="' + ikon + '"]');
      for (var q = 0; q < sv.length; q++) {
        var b = sv[q].closest ? sv[q].closest('button') : null;
        if (b) return b;
      }
    }
    if (!desen) return null;
    var bs = kap.querySelectorAll('button');
    for (var i = 0; i < bs.length; i++) {
      var et = (bs[i].getAttribute('aria-label') || bs[i].title || '').toLowerCase();
      if (et && desen.test(et)) return bs[i];
    }
    return null;
  }


  /* ---------- Geri / ileri sar ----------
     Hem kontrol şeridindeki düğmeler hem ← → kısayolu buradan geçer.
     Canlı yayında ileri sarma TAMPONLA sınırlı: tamponun sonundan ötesine
     gidilirse oynatıcı donuyor. `buffered` son aralığın sonu = canlı kenar;
     yarım saniye pay bırakıyoruz. Geri sarma da tamponun başından öteye
     inemez — Kick canlıda geçmişi tutmuyor.
     Döndürdüğü değer: sarma gerçekten yapıldı mı (kısayolda preventDefault için). */

  /* ---------- Hızlı ekran görüntüsü ----------
     Video karesini canvas'a çizip PNG indirir. Ağ yok, sunucu yok.
     Not: DRM'li akışlarda canvas "kirlenir" ve toBlob güvenlik hatası verir —
     o durumda sessizce uyarı gösterilir. */
  /* Kareyi canvas'a çizer ve blob döndürür.
     `filtreli` true ise ekranda gördüğün filtreler görüntüye de işlenir —
     canvas.filter, video çizilmeden ÖNCE kurulmalı, sonra kurmak hiçbir şey
     yapmıyor. DRM'li akışta canvas kirlenir ve toBlob güvenlik hatası verir. */
  /* `filtreli` parametresi kaldırıldı: görüntü filtreleri özelliği silindi,
     ekran görüntüsü artık her zaman videonun ham hâlini alıyor. */
  function ekranKaresi(cb) {
    var v = oynaticiVideosu();
    if (!v || !v.videoWidth) { cb(null, 'pkTGoruntuHazirDegil'); return; }
    try {
      var c = document.createElement('canvas');
      c.width = v.videoWidth; c.height = v.videoHeight;
      var ctx = c.getContext('2d');
      ctx.drawImage(v, 0, 0, c.width, c.height);
      c.toBlob(function (blob) {
        if (!blob) { cb(null, 'pkTSsAlinamadi'); return; }
        cb(blob, null);
      }, 'image/png');
    } catch (e) { cb(null, 'pkTSsYasak'); }
  }

  function ekranGoruntusuIndir() {
    ekranKaresi(function (blob, hata) {
      if (!blob) { toast(t(hata)); return; }
      try {
        var url = URL.createObjectURL(blob);
        var a = document.createElement('a');
        var slug = sayfaSlug() || 'kick';
        var d = new Date();
        function ik(n) { return (n < 10 ? '0' : '') + n; }
        a.href = url;
        a.download = 'purekick-' + slug + '-' + d.getFullYear() + ik(d.getMonth() + 1) + ik(d.getDate()) +
                     '-' + ik(d.getHours()) + ik(d.getMinutes()) + ik(d.getSeconds()) + '.png';
        document.body.appendChild(a); a.click(); a.remove();
        setTimeout(function () { URL.revokeObjectURL(url); }, 5000);
        toast(t('pkTSsKaydedildi'));
      } catch (e) { toast(t('pkTSsAlinamadi')); }
    });
  }

  /* Panoya kopyalama: ClipboardItem gerekiyor. Firefox'ta ve eski Chrome'da
     yok — o durumda sessizce indirmeye düşüyoruz, kullanıcı eli boş kalmasın. */
  function ekranGoruntusuKopyala() {
    ekranKaresi(function (blob, hata) {
      if (!blob) { toast(t(hata)); return; }
      try {
        if (!window.ClipboardItem || !navigator.clipboard || !navigator.clipboard.write) {
          toast(t('pkTPanoDesteklenmiyor')); ekranGoruntusuIndir(); return;
        }
        var oge = {}; oge[blob.type] = blob;
        navigator.clipboard.write([new ClipboardItem(oge)]).then(
          function () { toast(t('pkTSsPanoya')); },
          function () { toast(t('pkTPanoDesteklenmiyor')); ekranGoruntusuIndir(); }
        );
      } catch (e) { toast(t('pkTPanoDesteklenmiyor')); ekranGoruntusuIndir(); }
    });
  }

  /* Düğmeye normal tık = birincil eylem, CTRL/⌘ + tık = ikincil eylem. */
  function ekranGoruntusuAl(ikincilMi) {
    var e = ikincilMi ? (settings.ssIkincil || 'pano') : (settings.ssBirincil || 'indir');
    if (e === 'pano') ekranGoruntusuKopyala(); else ekranGoruntusuIndir();
  }

  /* ---------- Görüntü filtreleri ----------
     Saf CSS `filter`. Video elementine uygulanır, ayarlar kalıcıdır. */

  /* ---------- Geçmiş yayın kayıt noktası: 10.8'de KALDIRILDI ---------- */


  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — OYNATICI BUTONLARI (ekran görüntüsü · filtreler)
   * Kontrol şeridine, hız butonuyla AYNI yerleştirme mantığıyla girer:
   * önce videonun kendi kabı bulunur, aday butonlar yalnız orada aranır.
   * ══════════════════════════════════════════════════════════════════════ */
  var OYN_BTN = 'group relative box-border flex shrink-0 grow-0 select-none items-center ' +
                'justify-center gap-2 rounded bg-transparent text-white p-0! size-11';

  /* ---------- İkon boyutunu Kick'inkine eşitle ----------
     İkonlarımız 16–18 px sabit yazılmıştı; Kick'in kendi ikonları daha büyük
     olduğu için bizimkiler cılız duruyordu. Sabit bir sayı yazmak yerine
     KOMŞU KICK İKONLARINI ÖLÇÜP aynısını uyguluyoruz — Kick boyutu değiştirirse
     kendiliğinden uyar, ayrıca her yüzey (oynatıcı şeridi, sohbet başlığı,
     kanal eylem satırı) kendi komşusuna göre ölçülür.

     Ölçüm ORTANCA (ortalama değil): şeritte bir tane devasa "Clip" logosu ya da
     minik bir nokta varsa ortalamayı bozardı, ortanca bozmaz.
     Hiç uygun komşu yoksa 24 px.

     Önbellek kabın kendisinde durur; `ikonCagi` artınca (yeniden boyutlanma,
     tam ekran) tüm kapların ölçümü baştan alınır. */
  var ikonCagi = 1;
  function ikonBoyu(kap) {
    if (!kap) return 24;
    if (kap.__pkIkonCag === ikonCagi && kap.__pkIkonBoy) return kap.__pkIkonBoy;
    var olcum = [], svgler = kap.querySelectorAll('button svg, a svg');
    for (var i = 0; i < svgler.length; i++) {
      var s = svgler[i];
      if (s.closest('[data-pk-oyn],[data-pk-smenu],[data-pk-block2],[data-pk-kmenu]')) continue;  // kendi düğmelerimiz sayılmaz
      var r = s.getBoundingClientRect();
      var b = Math.max(r.width, r.height);
      if (b >= 12 && b <= 40) olcum.push(b);                 // makul aralık dışını ele
    }
    if (!olcum.length) return 24;
    olcum.sort(function (a, b) { return a - b; });
    kap.__pkIkonBoy = Math.round(olcum[Math.floor(olcum.length / 2)]);
    kap.__pkIkonCag = ikonCagi;
    return kap.__pkIkonBoy;
  }
  function ikonEsitle(btn, kap) {
    var s = btn.querySelector('svg'); if (!s) return;
    var b = ikonBoyu(kap);
    s.style.setProperty('width', b + 'px', 'important');
    s.style.setProperty('height', b + 'px', 'important');
  }
  var I_KOPYA = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:15px;height:15px;display:block">' +
    '<path d="M15.6 1.2H5.4A2.4 2.4 0 0 0 3 3.6v13.2h2.4V3.6h10.2zm3.6 4.8H10.2a2.4 2.4 0 0 0-2.4 2.4v12a2.4 2.4 0 0 0 2.4 2.4h9a2.4 2.4 0 0 0 2.4-2.4v-12a2.4 2.4 0 0 0-2.4-2.4zm0 14.4h-9v-12h9z"/></svg>';
  var I_DARALT = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:15px;height:15px;display:block">' +
    '<rect x="3" y="10.6" width="18" height="2.8" rx="1.4"/></svg>';
  var I_GOZ = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:15px;height:15px;display:block">' +
    '<path d="M2.1 3.6 3.6 2.1l18.3 18.3-1.5 1.5-3.3-3.3A11 11 0 0 1 12 19.5C6.6 19.5 2.2 16 .9 12a12 12 0 0 1 4.3-5.4zM12 7.5a4.5 4.5 0 0 1 4.5 4.5 4.4 4.4 0 0 1-.4 1.8l-5.9-5.9a4.4 4.4 0 0 1 1.8-.4zm0-3c5.4 0 9.8 3.5 11.1 7.5a12 12 0 0 1-3 4.3l-2.2-2.2a4.5 4.5 0 0 0-5.6-5.6L9.9 5.1A11.4 11.4 0 0 1 12 4.5z"/></svg>';
  var I_CANLI = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:18px;height:18px;display:block">' +
    '<path d="M4 4.6a1 1 0 0 1 1.55-.83l9 6.6a1 1 0 0 1 0 1.66l-9 6.6A1 1 0 0 1 4 17.8z"/>' +
    '<rect x="16.6" y="4" width="3.2" height="16" rx="1.6"/></svg>';
  var I_KAMERA = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:18px;height:18px;display:block">' +
    '<path d="M9.2 4l-1.4 2H4.4A2.4 2.4 0 0 0 2 8.4v9.2A2.4 2.4 0 0 0 4.4 20h15.2a2.4 2.4 0 0 0 2.4-2.4V8.4A2.4 2.4 0 0 0 19.6 6h-3.4l-1.4-2zm2.8 4.9a4.6 4.6 0 1 1-4.6 4.6A4.6 4.6 0 0 1 12 8.9zm0 2.2a2.4 2.4 0 1 0 2.4 2.4A2.4 2.4 0 0 0 12 11.1z"/></svg>';
  var I_FILTRE = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:18px;height:18px;display:block">' +
    '<rect x="2" y="5" width="20" height="2.6" rx="1.3"/><rect x="2" y="10.7" width="20" height="2.6" rx="1.3"/><rect x="2" y="16.4" width="20" height="2.6" rx="1.3"/><circle cx="8" cy="6.3" r="3"/><circle cx="16" cy="12" r="3"/><circle cx="6.5" cy="17.7" r="3"/></svg>';
  /* Kontrol şeridine butonları basar. Şerit gizliyken (fare oynatıcının üstünde
     değilken) aday buton bulunmaz ve sessizce çıkılır — kontroller belirince
     bir sonraki tur basar. */
  /* Şu an CANLI YAYIN oynatılıyor mu? Ekran görüntüsü ve istatistik
     düğmeleri yalnız burada anlamlı: klipte/VOD'da istatistik canlı ölçüm
     değil, bindirme oynatıcımızda ise Kick'in şeridi bile yok. */
  function canliOynaticiMi() {
    if (vodSayfasi()) return false;
    /* Klip ve VOD adresleri birden çok parça taşıyor; kanal sayfası tek. */
    var pr = (location.pathname || '').split('/').filter(Boolean);
    if (pr.length !== 1) return false;
    /* Kick'in ANA oynatıcısı olmalı. `pkIlkGorunur` — Kick görünmez ikizler
       tutuyor, ilk eşleşen 0x0 olabiliyor. */
    var v = pkIlkGorunur('#video-player');
    if (!v) return false;
    var kap = pkIlkGorunur('#injected-channel-player');
    if (!kap || !kap.contains(v)) return false;   // gömülü/bindirme oynatıcı
    /* SONLU süre = kayıt (öne çıkan klip). Süre bilinmiyorsa (NaN) ya da
       sonsuzsa engellemiyoruz: canlıda sonsuz, yüklenirken NaN geliyor.
       Yani bu şart yalnız EMİN olunca eliyor. */
    if (isFinite(v.duration) && v.duration > 0) return false;
    return true;
  }

  function oynaticiButonlariKur() {
    if (!settings.consented) return;
    var v = oynaticiVideosu(); if (!v) return;
    var vr = v.getBoundingClientRect();
    if (vr.width < 200 || vr.height < 100) return;
    var kap = oynaticiKabi(v, vr);
    if (!kap) {
      var art = document.querySelectorAll('[data-pk-oyn]');
      for (var a = 0; a < art.length; a++) art[a].remove();
      return;
    }
    // yanlış kaba girmiş butonları topla
    var kacak = document.querySelectorAll('[data-pk-oyn]');
    for (var k = 0; k < kacak.length; k++) if (!kap.contains(kacak[k])) kacak[k].remove();

    /* Kontrol şeridi: Kick kendi şeridine z-controls sınıfı veriyor. Önce onu
       dene — geometrik tarama (24–70 px düğme, videonun alt 80 px bandı) Kick
       düzeni değiştirdiğinde sessizce yanlış kabı seçebiliyordu. Bulunamazsa
       eski tarama yedek olarak duruyor. */
    var bar = null, son = null;
    var zs = oynaticiSeridi(kap);
    if (zs) {
      /* Şeridin İÇİNDEKİ en sağdaki Kick düğmesi; bizimkiler onun soluna
         girecek. Kendi düğmelerimiz ve gizli ikizler sayılmaz. */
      var zb = zs.querySelectorAll(':scope > button');
      for (var zi = 0; zi < zb.length; zi++) {
        if (zb[zi].getAttribute('data-pk-oyn')) continue;
        if (!pkGorunur(zb[zi])) continue;
        if (!son || zb[zi].getBoundingClientRect().left > son.getBoundingClientRect().left) son = zb[zi];
      }
      if (son) bar = zs;
    }
    if (!bar) {
      var btns = kap.querySelectorAll('button');
      for (var i = 0; i < btns.length; i++) {
        var r = btns[i].getBoundingClientRect();
        if (r.width < 24 || r.width > 70 || r.height < 24 || r.height > 70) continue;
        if (r.left < vr.left - 5 || r.right > vr.right + 5) continue;
        if (r.top < vr.bottom - 80 || r.top > vr.bottom + 10) continue;
        if (btns[i].getAttribute('data-pk-oyn')) continue;
        if (!son || r.left > son.getBoundingClientRect().left) son = btns[i];
      }
      if (!son || !son.parentElement) return;
      bar = son.parentElement;
    }

    /* Yayın süresi — kontrol şeridinde metin. Kick "LIVE" yazıyor ama ne
       zamandır yayında olduğunu göstermiyor; veri API'den geliyor (zaten
       önizleme için çekiliyor), ayrı istek yok. */

    function ekle(anahtar, baslik, ikon, tikla) {
      if (bar.querySelector(':scope > [data-pk-oyn="' + anahtar + '"]')) return;
      var b = el('button', OYN_BTN);
      b.setAttribute('data-pk-oyn', anahtar);
      b.type = 'button';
      /* Ham `title` yerine Kick'in kendi ipucu görünümü (bkz. bindTip →
         KICK_TIP_CLASS). `title` bırakılsaydı ikisi üst üste binerdi. */
      b.setAttribute('aria-label', baslik);
      bindTip(b, baslik, true);        // oynatıcı şeridi görünümü
      b.innerHTML = ikon;
      b.addEventListener('click', function (e) { e.preventDefault(); e.stopPropagation(); tikla(b, e); });
      bar.insertBefore(b, son);
      ikonEsitle(b, bar);
    }

    /* Geri/ileri sar — şeridin BAŞINA, Kick'in oynat düğmesinin yanına.
       Diğerleri sağ uca (`son`un soluna) giriyor; sarma düğmeleri oynatma
       kontrolü olduğu için orada değil, solda duruyor. */
  
    /* Yalnız canlı yayında. Klipten canlıya (ya da tersine) geçişte düğmeler
       eskiden kalmasın diye "eklemiyoruz" yetmiyor, SİLİYORUZ da. */
    var canli = canliOynaticiMi();
    if (settings.ekranGoruntusu !== false && canli) {
      ekle('ss', t('pkEkranGoruntusuAl'), I_KAMERA, function (b, ev) { ekranGoruntusuAl(!!(ev && (ev.ctrlKey || ev.metaKey))); });
    } else {
      var e1 = bar.querySelector(':scope > [data-pk-oyn="ss"]'); if (e1) e1.remove();
    }
    if (settings.videoIstat !== false && canli) {
      ekle('vistat', t('pkVideoIstatistikleri'), I_MENU, function () { vistatAc(); });
    } else {
      var e0 = bar.querySelector(':scope > [data-pk-oyn="vistat"]'); if (e0) e0.remove();
      /* Panel açıkken klibe geçilirse ekranda asılı kalmasın. */
      vistatKapat();
    }

    /* Zaten yerinde olan düğmeleri de her turda eşitle: tiyatro/tam ekrana
       geçişte Kick ikonlarını büyütüyor, bizimkiler küçük kalmasın. Ölçüm
       önbellekli, bu döngü yalnızca stil yazıyor. */
    var bizim = bar.querySelectorAll(':scope > [data-pk-oyn]');
    for (var bi = 0; bi < bizim.length; bi++) ikonEsitle(bizim[bi], bar);

    tamEkranSona();                              // 10.5

    // Süre yazısı da Kick'in şerit yazısıyla aynı boyda dursun.
    var sy = bar.querySelector(':scope > [data-pk-sure]');
    if (sy) sy.style.fontSize = Math.max(12, Math.round(ikonBoyu(bar) * 0.62)) + 'px';
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — OTOMATİK TİYATRO MODU  (varsayılan KAPALI)
   * Kanala girince oynatıcıyı bir kez tiyatro moduna alır.
   *
   * DURUM TESPİTİ: Kick'in tiyatro butonunda okunabilir bir "açık/kapalı"
   * işareti yok (aria-label boş, tooltip özel bir katmanda). Bu yüzden duruma
   * ÖLÇÜYLE karar veriyoruz: normal düzende video pencerenin ~%36'sı (sohbet +
   * kenar çubuğu yanında), tiyatroda neredeyse tamamı. %70 eşiğinin altındaysa
   * "tiyatro kapalı" kabul edip bir kez tıklıyoruz. Kanal başına tek deneme —
   * yanlış ölçümde açıp kapatan bir döngüye girmesin.
   * ══════════════════════════════════════════════════════════════════════ */
  var otoTiyatroYapildi = '', otoTiyatroDeneme = 0, otoTiyatroDenemeSlug = '';
  function otoTiyatroKur() {
    if (!settings.consented || settings.otoTiyatro !== true) return;
    var slug = sayfaSlug(); if (!slug) { otoTiyatroYapildi = ''; return; }
    if (otoTiyatroYapildi === slug) return;
    var v = oynaticiVideosu(); if (!v) return;
    var vr = v.getBoundingClientRect();
    if (vr.width < 200 || !v.videoWidth) return;          // oynatıcı hazır değil
    if (otoTiyatroDenemeSlug !== slug) { otoTiyatroDenemeSlug = slug; otoTiyatroDeneme = 0; }
    /* Kick tiyatro durumunu kok kapta data-theatre bayraginda tutuyor —
       kesin bilgi. Bayrak yoksa eski olcume duseriz (%70 genislik esigi). */
    var tm = tiyatroModuMu();
    if (tm === true) { otoTiyatroYapildi = slug; return; }              // zaten tiyatroda
    if (tm === null && vr.width / (window.innerWidth || 1) >= 0.7) { otoTiyatroYapildi = slug; return; }
    var b = oynaticiButonu(/tiyatro|theat/, 'ViewTheatre');
    if (b) { otoTiyatroYapildi = slug; try { b.click(); } catch (e) {} return; }
    /* DÜĞME YOKSA "DENENDİ" SAYMA. Eskiden bayrak burada değil, en başta
       konuyordu: şerit o an sökülüyse otomatik tiyatro o kanalda bir daha
       HİÇ denenmiyordu. Şimdi şeridi uyandırıp sonraki turda yeniden
       deneniyor; makul sayıda denemeden sonra pes ediyor. */
    if (++otoTiyatroDeneme > 20) { otoTiyatroYapildi = slug; return; }
    oynaticiSeridiUyandir();
  }

  function vodButonuKur() {
    if (!settings.consented || !vodSayfasi()) return;
    var abone = null, btns = document.querySelectorAll('button');
    for (var i = 0; i < btns.length; i++) {
      // `t` DEĞİL: çeviri fonksiyonunu tüm fonksiyon boyunca gölgeler (var yükselir).
      var yz = (btns[i].textContent || '').trim();
      if (/^(abone ol|abonesin|subscribe|subscribed)$/i.test(yz) && btns[i].getBoundingClientRect().width > 0) { abone = btns[i]; break; }
    }
    if (!abone || !abone.parentElement) return;
    var bar = abone.parentElement;
    if (bar.querySelector('[data-pk-vod]')) return;
    var b = el('button', BTN_BASE + ' h-10 px-3 text-base bg-secondary-base text-white hover:brightness-125');
    b.setAttribute('data-pk-vod', '1');
    b.title = t('pkVodIndir');
    var ik = el('span', 'inline-flex shrink-0');
    ik.innerHTML = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:16px;height:16px;display:block">' +
      '<path d="M13.4 3v8.2h3.3a.9.9 0 0 1 .7 1.5l-4.7 5.4a.9.9 0 0 1-1.4 0l-4.7-5.4a.9.9 0 0 1 .7-1.5h3.3V3zM4 19.2h16a1.4 1.4 0 0 1 0 2.8H4a1.4 1.4 0 0 1 0-2.8z"/></svg>';
    b.appendChild(ik);
    b.appendChild(document.createTextNode(t('pkVodIndirKisa')));
    b.addEventListener('click', function (e) { e.preventDefault(); e.stopPropagation(); vodIndirBaslat(); });
    bar.insertBefore(b, bar.firstChild);   // ikonların EN BAŞINA
  }

  function profilKartiIcinde(elm) {
    var k = elm && elm.closest && elm.closest('div.bg-surface-highest');
    return !!(k && k.querySelector('a.select-text.font-semibold'));
  }

  /* NOT — kartın KENDİSİNE kaydırma VERİLMEZ (bir kez denendi, geri alındı):
     kart da kayınca ekranda İKİ kaydırma çubuğu oluyordu ve Kick'in kapatma (✕)
     düğmesi kartla birlikte kayıp yerinden çıkıyordu (✕ kartın çocuğu değil,
     ayrı konumlanıyor — ölçüldü). Tek kaydırma "Son Mesajlar" listesinde;
     liste sınırlı olduğu için kart zaten kontrolsüz uzamıyor. */

  /* ---- Yüzen kutuları taşıma (profil kartı, Yan Sohbet…) ----
   * Kick'in konumlandırmasıyla kavga etmemek için `left/top`'a DOKUNMUYORUZ;
   * yalnızca `transform: translate` uygulanır. Kutu kapanıp yeniden açıldığında
   * yeni bir DOM düğümü geldiği için konum kendiliğinden sıfırlanır.
   *
   * Sürükleme buton/link/giriş alanından BAŞLAMAZ (tıklamayı bozmamak için) ve
   * kendi içinde kayan listelerden de başlamaz (`data-pk-ickaydir`). */
  /* Taşınacak asıl kutu: Kick'in KAPATMA (✕) düğmesi kartın çocuğu DEĞİL —
     kartla aynı kapsayıcıda duran `position:absolute` bir kardeş (ölçüldü:
     kart 1240,177 324x475 · ✕ 1524,185 32x32 · ortak ata `z-modal absolute
     w-[356px]` 356x507). Kartı taşıyınca ✕ yerinde kalıyordu. Bu yüzden
     kartın en yakın KONUMLANDIRILMIŞ atası taşınır → ✕ de birlikte gider.
     Ata makul boyutta değilse (tüm sayfa gibi) kartın kendisine düşülür. */
  function tasinacakKap(kart) {
    for (var e = kart, i = 0; e && i < 6; e = e.parentElement, i++) {
      var c = getComputedStyle(e);
      if (c.position !== 'absolute' && c.position !== 'fixed') continue;
      var r = e.getBoundingClientRect();
      if (r.width > 0 && r.width < window.innerWidth * 0.9 && r.height < window.innerHeight * 0.95) return e;
      break;
    }
    return kart;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * SERBEST PENCERE — karartmayan, sürüklenebilir
   *
   * Toplu araçların pencereleri tam ekran koyu bir perdeyle açılıyordu:
   * moderatör pencere açıkken ne yayını izleyebiliyor ne sohbeti okuyabiliyordu
   * (kullanıcı bildirdi). Oysa hepsi UZUN işlemler — toplu susturma dakikalarca
   * sürebiliyor ve bu sırada sohbeti takip etmek işin kendisi.
   *
   * Perde artık KARARTMIYOR ve tıklamayı GEÇİRİYOR (`pointer-events:none`);
   * yalnız kartın kendisi tıklama alıyor. Kart başlığından sürüklenebiliyor,
   * yayının önünü kapatıyorsa kenara çekiliyor.
   *
   * Taşıma imleci YALNIZ BAŞLIKTA: `surukleYap` kutunun tamamına `move`
   * yazıyor, form alanlarının üstünde bu yanlış bir ipucu olurdu.
   * ══════════════════════════════════════════════════════════════════════ */
  function pkPencereSerbest(perde, kart, baslik) {
    perde.style.cssText = 'position:fixed;inset:0;z-index:2147483210;pointer-events:none;' +
      'display:flex;align-items:center;justify-content:center;padding:16px';
    kart.style.pointerEvents = 'auto';
    kart.style.boxShadow = '0 14px 40px rgba(0,0,0,.6)';
    kart.style.border = '1px solid rgba(255,255,255,.10)';
    try { surukleYap(kart); } catch (e) {}
    kart.style.cursor = '';
    if (baslik) baslik.style.cursor = 'move';
  }

  var pkTasi = null;
  function surukleYap(kutu) {
    if (!kutu || kutu.getAttribute('data-pk-tasi')) return;
    kutu.setAttribute('data-pk-tasi', '1');
    kutu.style.cursor = 'move';
    kutu.addEventListener('mousedown', function (e) {
      if (e.button !== 0) return;
      /* 10.14 — dinleyici kaldırılamıyor (isimsiz), bu yüzden ETKİSİZLEŞTİRME
         işaretle yapılıyor. İşareti yalnız yan sohbet koyuyor; profil kartı
         sürüklemesi (aynı fonksiyon) etkilenmez. */
      if (kutu.getAttribute('data-pk-tasi-kapali')) return;
      if (e.target.closest && e.target.closest('button, a, input, textarea, select, [role="button"]')) return;
      /* İçinde KAYAN bir alandan sürükleme başlatma — yoksa listeyi kaydırmak
         kutuyu taşırdı. Genel kontrol: hedeften kutuya kadar yükselip gerçekten
         kaydırılabilen bir ata var mı bak (Yan Sohbet'in kendi kaydırıcısı ve
         bizim mesaj listemiz bu sayede tek kuralla kapsanıyor). */
      for (var n = e.target; n && n !== kutu; n = n.parentElement) {
        if (n.hasAttribute && n.hasAttribute('data-pk-ickaydir')) return;
        var oy = getComputedStyle(n).overflowY;
        if ((oy === 'auto' || oy === 'scroll') && n.scrollHeight > n.clientHeight + 4) return;
      }
      var o = kutu.__pkOfs || { x: 0, y: 0 };
      pkTasi = { kutu: kutu, sx: e.clientX - o.x, sy: e.clientY - o.y };
      e.preventDefault();                       // metin seçimini bastır → sürükleme akıcı
    });
  }

  /* Yan Sohbet paneli. Başlık metni dile göre değiştiği için SINIF İZİNDEN
     bulunur: `bg-surface-base size-full flex-col` + panele özgü gölge sınıfı.
     Gövdesinde Kick'in kendi kaydırıcısı zaten var (ölçüldü), eksik olan taşıma. */
  function yanSohbetKur() {
    var a = document.querySelectorAll('div.bg-surface-base.size-full.flex-col');
    var kapali = settings.yanSohbet === false;
    for (var i = 0; i < a.length; i++) {
      if (!/shadow-\[0px_4px_4px/.test(String(a[i].className || ''))) continue;
      /* İşaret KİMSE TARAFINDAN OKUNMUYOR. Eski yorum "sohbetTara kapsamı
         ayırt etsin" diyordu ama o çakışma başka türlü çözüldü: anahtar
         kim+dakika+metin, `data-index` hiç kullanılmıyor. Yalnız kendini
         toplama listesinde geçiyor. Kaldırmadım — Kick'in kutusunu bizim
         dokunduğumuz hâliyle işaretlemek hata ayıklamada işe yarıyor. */
      a[i].setAttribute('data-pk-yan', '1');
      var k = tasinacakKap(a[i]);
      if (!k) continue;
      /* 10.14 — kapalıyken: yeni bağlama yapılmaz, bağlı olan işaretle
         etkisizleşir, taşınmış kutu da eski yerine döner. */
      if (kapali) {
        k.setAttribute('data-pk-tasi-kapali', '1');
        if (k.style.cursor === 'move') k.style.cursor = '';
        if (k.__pkOfs) { k.style.transform = ''; k.__pkOfs = null; }
        continue;
      }
      k.removeAttribute('data-pk-tasi-kapali');
      /* İmleci BURADA geri koy: kutu zaten bağlıysa surukleYap erken döner ve
         kapatırken sildiğimiz `cursor:move` bir daha yazılmazdı. */
      if (k.style.cursor !== 'move') k.style.cursor = 'move';
      surukleYap(k);
    }
  }
  document.addEventListener('mousemove', function (e) {
    if (!pkTasi) return;
    var k = pkTasi.kutu, x = e.clientX - pkTasi.sx, y = e.clientY - pkTasi.sy;
    k.__pkOfs = { x: x, y: y };
    k.style.transform = 'translate(' + Math.round(x) + 'px,' + Math.round(y) + 'px)';
  });
  document.addEventListener('mouseup', function () { pkTasi = null; });

  function scSaat(ts) {
    var d = new Date(ts);
    return ('0' + d.getHours()).slice(-2) + ':' + ('0' + d.getMinutes()).slice(-2);
  }
  /* Mesaj parçalarından gövde düğümü (yazı + emote görselleri). Hem profil
     kartı hem sohbet araması aynı çizimi kullanır. Emote sohbette 30px
     (ölçüldü); dar listelerde satırı şişirmesin diye 22px. */
  function mesajGovdesi(pr, boy) {
    var gov = el('span', 'min-w-0 break-words text-white');
    for (var j = 0; j < pr.length; j++) {
      if (pr[j].t === 'y') { gov.appendChild(document.createTextNode(pr[j].v)); continue; }
      /* Adres yoksa kırık resim yerine emote adını yaz. */
      if (!pr[j].s) { gov.appendChild(document.createTextNode(pr[j].a || '')); continue; }
      var im = document.createElement('img');
      im.src = pr[j].s; im.alt = pr[j].a || ''; im.title = pr[j].a || '';
      im.referrerPolicy = 'no-referrer';
      /* Profil kartındaki liste kişinin YAYIN BOYUNCA yazdığı her mesajı
         basıyor (kullanıcı kesilmesini istemedi). Emoteler istekli yüklenirse
         kart açılırken yüzlerce görsel isteği aynı anda çıkıyordu; tembel
         yüklemede yalnız görünen satırlar iniyor, gerisi kaydırdıkça geliyor. */
      im.loading = 'lazy';
      im.decoding = 'async';
      im.style.cssText = 'height:' + (boy || 22) + 'px;width:auto;vertical-align:middle;display:inline-block';
      gov.appendChild(im);
    }
    return gov;
  }
  /* "Son Mesajlar" bloğu — kartın EN ALTINA eklenir ki kart kaydırılırken
     Engelle/Takip Et/Sustur butonları görünürde kalsın. Başlık, Kick'in kendi
     "Rozetler" başlığıyla aynı sınıflar (ölçüldü: 16px/600). */
  /* 10.5 — profil kartına kişiye özel NOT kutusu.
     Tamamen yerel: chrome.storage.local'da durur, sunucuya gitmez.
     Mesaj listesinin ÜSTÜNE konur ki kart açılınca hemen görünsün. */
  function injectPopupNot(kart, ad) {
    var eski = kart.querySelector(':scope > .pk-not');
    if (!settings.notlar) { if (eski) eski.remove(); return; }
    if (eski && eski.getAttribute('data-pk-not') === (ad || '')) return;   // aynı kişi → dokunma
    if (eski) eski.remove();

    var kutu = el('div', 'pk-not flex flex-col gap-2');
    kutu.setAttribute('data-pk-not', ad || '');
    var bas = el('div', 'flex items-center justify-between gap-2');
    bas.appendChild(el('div', 'text-md font-bold text-white lg:font-semibold', t('pkNotun')));
    var durum = el('div', 'text-xs text-subtle', '');
    bas.appendChild(durum);
    kutu.appendChild(bas);

    var alan = el('textarea', 'bg-surface-highest border-outline-decorative rounded border px-3 py-2 text-sm text-white');
    alan.placeholder = t('pkNotPlaceholder');
    alan.value = notOku(ad);
    alan.style.cssText = 'width:100%;min-height:64px;max-height:120px;resize:vertical;outline:none;font-family:inherit';
    /* Her tuşta storage'a yazmayalım — yazmayı bıraktıktan 600ms sonra kaydeder. */
    var zaman = 0;
    alan.addEventListener('input', function () {
      clearTimeout(zaman);
      durum.textContent = t('pkYaziliyor');
      zaman = setTimeout(function () {
        notYaz(ad, alan.value);
        durum.textContent = t('pkKaydedildi');
        setTimeout(function () { if (durum) durum.textContent = ''; }, 1500);
      }, 600);
    });
    // Kartın sürüklenmesi not yazarken tetiklenmesin
    ['mousedown', 'pointerdown'].forEach(function (ev) {
      alan.addEventListener(ev, function (e) { e.stopPropagation(); });
    });
    kutu.appendChild(alan);

    var pm = kart.querySelector(':scope > .pk-pm');
    if (pm) kart.insertBefore(kutu, pm); else kart.appendChild(kutu);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — PROFİL KARTINDA KULLANICI BİLGİSİ
   * Kick'in kendi ucu: /api/v2/channels/<kanal>/users/<kişi>
   * Canlıda doğrulandı — çerez GÖNDERİLMEDEN de çalışıyor (credentials:'omit'),
   * yani kimliğimizi paylaşmıyoruz. Dönen alanlar:
   *   created_at       → Kick'e katılma tarihi
   *   following_since  → bu kanalı ne zamandır takip ediyor
   *   subscribed_for   → kaç aydır abone
   * Kişi başına BİR kez sorulur ve bellekte tutulur; kart her açıldığında
   * yeniden istek atılmaz. Bizim sunucumuza hiçbir şey gitmez.
   * ══════════════════════════════════════════════════════════════════════ */
  var kartBilgi = Object.create(null);      // "kanal|nick" -> {ts, veri} | 'bekliyor' | 'yok'

  /* Ay adları sabit Türkçe bir diziden geliyordu; 12 ayı 7 dile çevirmek yerine
     tarayıcının kendi yerelleştirmesini kullanıyoruz — kullanıcının dilinde ve
     o dilin kendi tarih sırasıyla yazılır. `ts` adı önemli: `t` çeviri
     fonksiyonunu gölgelerdi. */
  function tarihYaz(iso) {
    var ts = Date.parse(iso);
    if (!isFinite(ts)) return '';
    var d = new Date(ts);
    try {
      return d.toLocaleDateString(undefined, { day: 'numeric', month: 'short', year: 'numeric' });
    } catch (e) {
      return d.getDate() + '.' + (d.getMonth() + 1) + '.' + d.getFullYear();
    }
  }
  /* ADI DEĞİŞTİ (10.5): eskiden `sureYaz` idi ve VOD ilerleme çubuğundaki
     iç içe `sureYaz(saniye)` ile aynı adı taşıyordu. İkisi tamamen farklı iş
     yapıyor (biri ISO tarih → "3 aydır", öteki saniye → "1:23:45"); dış
     kapsamdan çağıran yeni kod yanlışlıkla bunu alıyordu. */
  function gecenSureYazi(iso) {
    var ts = Date.parse(iso); if (!isFinite(ts)) return '';   // `t` DEĞİL: çeviri fonksiyonunu gölgeler
    var gun = Math.floor((Date.now() - ts) / 86400000);
    if (gun < 1) return t('pkBugun');
    if (gun < 30) return t('pkGundur', [String(gun)]);
    var ay = Math.floor(gun / 30);
    if (ay < 12) return t('pkAydir', [String(ay)]);
    var yil = Math.floor(gun / 365);
    return t('pkYildir', [String(yil)]);
  }

  function kartBilgiCek(kanal, nick, bitince) {
    var k = kanal + '|' + nick.toLowerCase();
    var v = kartBilgi[k];
    if (v === 'bekliyor') return;
    if (v && v !== 'yok') { bitince(v); return; }
    if (v === 'yok') return;
    kartBilgi[k] = 'bekliyor';
    fetch('https://kick.com/api/v2/channels/' + encodeURIComponent(kanal) +
          '/users/' + encodeURIComponent(nick), { credentials: 'omit' })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        if (!j) { kartBilgi[k] = 'yok'; return; }
        kartBilgi[k] = { created: j.created_at || '', takip: j.following_since || '',
                         abone: j.subscribed_for || 0, mod: !!j.is_moderator, sahip: !!j.is_channel_owner };
        bitince(kartBilgi[k]);
      })
      .catch(function () { kartBilgi[k] = 'yok'; });
  }

  /* Kartın üstündeki bilgi kutusu: Kick'e katıldı · takipçi · kanalı takip ·
     abonelik · rol.

     TAKİPÇİ SAYISI ARTIK BURADA. Eskiden kartın ortasında ayrı bir blok
     (`.pk-takipci`) olarak duruyordu; kullanıcı isteğiyle bu satıra taşındı.
     İki AYRI KAYNAK besliyor: kanal-kullanıcı ilişkisi `kartBilgiCek`ten,
     takipçi sayısı `kanalBilgi`den. İkisi ayrı zamanlarda geliyor.

     BU YÜZDEN ÇİZİM TEK YERDE (`ciz`) ve HER İKİ KAYNAK İÇİN DE ÇAĞRILIYOR:
     · `kartBilgiCek` BAŞARISIZ OLURSA geri çağrısı HİÇ çalışmıyor
       (`kartBilgi[k]='yok'` deyip çıkıyor) — çizimi yalnız oraya bağlasaydık
       API'siz durumda takipçi satırı da kaybolurdu.
     · İmza iki verinin de gelip gelmediğini taşıyor; biri sonradan gelince
       erken çıkış tetiklenmiyor ve kutu tazeleniyor. */
  function injectPopupBilgi(kart, ad) {
    var eski = kart.querySelector(':scope > .pk-bilgi');
    var bilgiAcik = settings.kartBilgi !== false;
    var tkAcik = settings.kartTakipci === true;
    /* İKİSİ DE KAPALIYSA kutu hiç durmasın. Ayrı ayrı kapatılabiliyorlar:
       biri kapalı diğeri açıkken kutu yine gerekiyor. */
    if (!bilgiAcik && !tkAcik) { if (eski) eski.remove(); return; }
    var kanal = sayfaSlug(); if (!kanal || !ad) return;
    var slug = String(ad).toLowerCase();

    /* Takipçi verisi elde mi; yoksa iste (gelince processProfilePopups çağırır). */
    var tk = null;
    if (tkAcik) {
      var kb = kanalBilgi[slug];
      if (kb === undefined) kanalBilgiCek(slug, function () { processProfilePopups(); });
      else if (kb !== 'bekliyor') tk = kb;
    }
    var v = bilgiAcik ? kartBilgi[kanal + '|' + slug] : null;
    if (v === 'bekliyor' || v === 'yok') v = null;

    var imza = ad + '|' + (v ? 'v' : '-') + (tk ? 't' : '-');
    if (eski && eski.getAttribute('data-pk-bilgi') === imza) return;
    if (eski) eski.remove();

    var kutu = el('div', 'pk-bilgi flex flex-col gap-1');
    kutu.setAttribute('data-pk-bilgi', imza);
    /* ÜST ŞERİDİ KICK'E BIRAK. Blok kartın ilk çocuğuyken ilk satırın değeri
       sağ üstteki mutlak konumlu ✕/raptiye düğmelerinin altına giriyor ve
       kapatma düğmesi okunmaz oluyordu (kullanıcı bildirdi). Sağdan boşluk
       bırakmak kart genişleyince yetmedi. Blok artık KULLANICI ADI
       SATIRININ ALTINA giriyor; üst şerit tamamen Kick'in kalıyor.
       Ad satırı bulunamazsa eski yere düşüyor — blok hiç görünmemesindense. */
    var adKok = kart.querySelector('a.select-text.font-semibold');
    while (adKok && adKok.parentElement && adKok.parentElement !== kart) adKok = adKok.parentElement;
    if (adKok && adKok.parentElement === kart) {
      if (adKok.nextSibling) kart.insertBefore(kutu, adKok.nextSibling);
      else kart.appendChild(kutu);
    } else kart.insertBefore(kutu, kart.firstChild);

    function satirci(etiket, deger, alt) {
      var r = el('div', 'flex items-center justify-between gap-3');
      r.appendChild(el('div', 'text-xs text-subtle', etiket));
      var sp = el('div', 'text-xs font-semibold text-white', deger);
      if (alt) sp.title = alt;
      r.appendChild(sp);
      return r;
    }

    function ciz() {
      kutu.textContent = '';
      /* Bu satırlar çeviri geçişinde ATLANMIŞTI: etiketler doğrudan
         `satirci(...)` çağrısına yazılıydı, tarayıcı yalnız bilinen çağrı
         biçimlerine bakıyordu. */
      if (v && v.created) kutu.appendChild(satirci(t('pkKickeKatildi'), tarihYaz(v.created), gecenSureYazi(v.created)));
      if (tk) kutu.appendChild(satirci(t('pkTakipciSayisi'), (tk.takipci || 0).toLocaleString()));
      /* Sağdan boşluk kaldırıldı: blok artık üst şeridin altında, ✕ ile
         çakışma yok. */
      if (v) {
        /* TARİH ASIL DEĞER, geçen süre İPUCU — bir üstteki "Kick'e katıldı"
           satırıyla aynı düzen. İkisi ters kurulmuştu: biri "3 Haz 2024",
           öteki "2 yıldır" gösteriyordu ve yan yana tutarsız duruyordu.
           `tarihYaz` tarayıcının yerelleştirmesini kullanıyor: ay adı ve gün/ay
           sırası kullanıcının diline göre geliyor (TR "26 Oca 2024",
           EN "Jan 26, 2024", RU "26 янв. 2024 г."). */
        if (v.takip) kutu.appendChild(satirci(t('pkKanaliTakip'), tarihYaz(v.takip), gecenSureYazi(v.takip)));
        if (v.abone > 0) kutu.appendChild(satirci(t('pkAbonelik'), t('pkAySayisi', [String(v.abone)])));
        if (v.sahip) kutu.appendChild(satirci(t('pkRolEtiket'), t('pkKanalSahibi')));
        else if (v.mod) kutu.appendChild(satirci(t('pkRolEtiket'), t('pkRol_moderator')));
      }
      if (kutu.children.length) kutu.appendChild(el('div', 'h-px bg-outline-decorative w-full'));
      /* Aynı bilgiyi Kick de kartın ortasında yazıyor; ikisi üst üste
         duruyordu. YALNIZ katılma tarihini gerçekten bastıysak gizliyoruz —
         API'den veri gelmediyse Kick'inki tek kaynak, dokunmuyoruz. */
      if (v && v.created) kickTarihBloguGizle(kart);
    }

    ciz();                                    // elde ne varsa hemen bas
    if (bilgiAcik && !v) {
      kartBilgiCek(kanal, ad, function (yeni) {
        if (!kutu.isConnected) return;
        v = yeni; kutu.setAttribute('data-pk-bilgi', ad + '|v' + (tk ? 't' : '-'));
        ciz();
      });
    }
  }

  /* Kick'in "Katılma tarihi / Takip Etme Tarihi" bloğunu gizler.
     METNE BAKMIYOR: kart 7 dilde açılıyor, Türkçe etiket aramak yanlış olur.
     İmza yapısal — kartın içinde, bizim olmayan, yıl içeren `select-text`
     sarmalayıcı. Düğüm SİLİNMİYOR, yalnız gizleniyor: Kick'in kendi düğümü,
     eklenti kapanınca geri verilmeli. */
  function kickTarihBloguGizle(kart) {
    if (!kart) return;
    var blok = kart.querySelector('div[class*="gap-x-6"][class*="select-text"]');
    if (!blok) {
      /* Sınıf adları değişirse yapıdan bul: etiket+değer çiftleri, yıl geçiyor. */
      var aday = kart.querySelectorAll('div.select-text, div[class*="flex-wrap"]');
      for (var i = 0; i < aday.length; i++) {
        var a = aday[i];
        if (a.closest('[data-pk-bilgi]')) continue;              // bizimki değil
        if (!a.children.length || a.children.length > 3) continue;
        if (!/\b(19|20)\d{2}\b/.test(a.textContent || '')) continue;
        blok = a; break;
      }
    }
    if (!blok || blok.closest('[data-pk-bilgi]')) return;
    if (blok.getAttribute('data-pk-gizli-tarih')) return;        // zaten gizli
    blok.setAttribute('data-pk-gizli-tarih', '1');
    blok.style.setProperty('display', 'none', 'important');
  }

  function injectPopupMesajlar(kart, ad) {
    try { takipciBloguTemizle(kart); } catch (e) {}
    try { injectPopupBilgi(kart, ad); } catch (e) {}
    try { injectPopupKisi(kart, ad); } catch (e) {}
    try { injectPopupNot(kart, ad); } catch (e) {}
    try { injectPopupYanitlar(kart, ad); } catch (e) {}     // eski ayrı bloğu temizler
    /* 10.14 — sekme şeridinin tamamı (Son Mesajlar + Ona yazılanlar) ayara
       bağlı; kapalıyken kartta hiç görünmez. */
    if (settings.kartMesajlar === false) {
      var esk = kart.querySelector(':scope > .pk-sekme-kap'); if (esk) esk.remove();
      var epm = kart.querySelector(':scope > .pk-pm'); if (epm) epm.remove();
      return;
    }
    /* Son Mesajlar + Ona yazılanlar TEK şeritte, yan yana iki sekme.
       Eskiden ikisi ayrı blok olarak alt alta ekleniyor ve kartı aşağı
       kaydırıyordu. */
    var v = scListe[(ad || '').toLowerCase()];
    var msj = (v && v.mesajlar) || [];
    var imza = (ad || '') + '|' + msj.length + '|' + (msj.length ? msj[msj.length - 1].ts : '0') +
               '|' + pkKartSekme;
    var kap = kart.querySelector(':scope > .pk-sekme-kap');
    if (kap && kap.getAttribute('data-pk-sekme-imza') === imza) return;   // değişmediyse dokunma
    kap = pkKartSekmeCiz(kart, ad);
    kap.setAttribute('data-pk-sekme-imza', imza);
    /* Eski tek-blok sürümünden kalmış kap varsa temizle. */
    var pm = kart.querySelector(':scope > .pk-pm'); if (pm) pm.remove();
  }

  /* Kişinin mesajları — sekme gövdesine. Sohbet görünümünde. */
  function pkMesajlariCiz(govde, ad) {
    var v = scListe[(ad || '').toLowerCase()];
    var msj = (v && v.mesajlar) || [];
    if (!msj.length) {
      govde.appendChild(el('div', 'text-sm text-subtle', t('pkKisiMesajYok')));
      return;
    }
    /* Mesajlar KENDİ kutusunda kayar — yoksa 100 mesajlı biri kartı uzatıyor.
       `data-pk-ickaydir` sürüklemenin buradan başlamasını engeller. */
    msStilKur();
    var liste = el('div', 'pk-mesaj-liste flex flex-col');
    liste.setAttribute('data-pk-ickaydir', '1');
    /* Kutu PK_KART_SATIR satır boyunda; MESAJLARIN HEPSİ basılıyor, fazlası
       kaydırılıyor. (Bir ara son 6 mesajla kesiliyordu — istenen o değildi:
       6 satır GÖRÜNSÜN, gerisi kaydırmayla gelsin.) */
    liste.style.cssText = 'max-height:' + (PK_KART_SATIR * PK_KART_SATIR_PX) +
      'px;overflow-y:auto;overscroll-behavior:contain';
    /* ZAMANA GÖRE SIRALA. Depoya geliş sırasıyla yazılıyor ve DOM taraması
       sayfaya girmeden önceki ESKİ mesajları sonradan ekleyebiliyor; liste
       o zaman karışık görünüyordu (08:06'dan sonra 08:05). Depoyu bozmadan,
       yalnız çizim için kopya sıralanıyor. */
    msj = msj.slice().sort(function (a, b) { return a.ts - b.ts; });
    for (var i = 0; i < msj.length; i++) {                                        // eskiden yeniye
      /* Bir açık bir koyu. Sınıfla veriliyor: satır içi stil yazsaydık
         `.pk-mesaj-satir:hover` kuralını ezer, fareyle gezinme geri
         bildirimi ölürdü. */
      var r = el('div', 'pk-mesaj-satir w-full min-w-0 rounded-lg px-2 break-words' + pkSeritSinifKoyu(i));
      r.appendChild(el('span', 'pk-mesaj-saat text-subtle pr-1 font-semibold', scSaat(msj[i].ts)));
      var g = mesajGovdesi(msj[i].p || [], 26);
      while (g.firstChild) r.appendChild(g.firstChild);
      liste.appendChild(r);
    }
    govde.appendChild(liste);
    liste.scrollTop = liste.scrollHeight;      // en yeni altta
  }

  // React sanal-listesi mesajı yeniden çizince badge silinir; geri eklemeyi
  // requestAnimationFrame ile (boyamadan ÖNCE, kare başına toplu) yaparız →
  // silinme gözükmez, flicker olmaz. setTimeout kullanılmaz (paint sonrası çalışır).
  /* ---- Yayıncı başlığı (video altındaki isim) rozeti ----
   * Yayıncı adı badges.json'da varsa isminin/verified rozetinin yanına badge basılır
   * (sevdiğin yayıncılara özel rozet). Eşleştirme: h1 metni VEYA kanal slug'ı (URL).
   * Boyut verified rozetine (yoksa isim font'una) eşitlenir. */
  /* `ad` PARAMETRESİ ŞART: rozetIpucu kıdemi ondan buluyor. 10.15'te ipucu
     gerçek kıdeme bağlanırken makeBadge ve makePopupBadge'e eklenmiş ama
     burası atlanmıştı; `ad` tanımsız kaldığı için her çağrı ReferenceError
     fırlatıyordu. Çağıran processStreamerHeader try/catch içinde olduğu için
     hata yutuluyor ve KANAL BAŞLIĞINDA ROZET HİÇ ÇIKMIYORDU (sohbet, profil
     kartı ve arama ayrı fonksiyonlar olduğu için oralar sağlamdı). */
  function makeStreamerBadge(def, sz, key, ad) {
    var w = document.createElement('span');
    w.className = 'pk-stream-badge shrink-0';
    w.style.cssText = 'display:inline-flex;align-items:center;margin-left:0;cursor:inherit;vertical-align:middle';
    var fallback = function () {
      var s = document.createElement('span');
      s.style.cssText = 'width:' + sz + 'px;height:' + sz + 'px;border-radius:4px;display:block;background:' + ((Array.isArray(def.color) ? def.color[0] : def.color) || '#53fc18');
      return s;
    };
    var src = def.img || def.image;               // önce gömülü data-URI (anlık), yoksa URL
    if (src) {
      var img = document.createElement('img'); img.src = src; img.alt = def.label || ''; img.referrerPolicy = 'no-referrer';
      img.style.cssText = 'height:' + sz + 'px;width:' + sz + 'px;object-fit:contain;display:block';
      img.addEventListener('error', function () { if (img.parentNode) img.parentNode.replaceChild(fallback(), img); }, { once: true });
      w.appendChild(img);
    } else { w.appendChild(fallback()); }
    bindTip(w, rozetIpucu(def, key, ad));   // Kick gibi hover tooltip
    return w;
  }
  function injectStreamerBadge(h1, slug) {
    var btn = h1.parentElement; if (!btn) return;
    var name = (h1.textContent || '').trim(); if (!name) return;
    noteUser(name); if (slug) noteUser(slug);   // yayıncı adını/slug'ını sorguya al
    /* Rozet HANGİ ANAHTARLA bulunduysa ipucu da onu kullanmalı: kıdem
       (ayByName) o anahtarla saklanıyor. Görünen ad ile slug farklı
       olabiliyor, yanlışını göndermek ipucunu kademe eşiğine düşürürdü. */
    var keys = badgeKeysFor(name), adAnahtar = String(name || '').toLowerCase();
    if (!keys.length && slug) { keys = badgeKeysFor(slug); adAnahtar = String(slug).toLowerCase(); }
    // Kanal/yayıncı başlığında SADECE hideOnChannel olmayan rozetler görünür
    // (supporter chat + popup'a özel → kanalda gizli).
    keys = keys.filter(function (k) { return badgeMaps.defs[k] && !badgeMaps.defs[k].hideOnChannel; });
    // KURAL: rozetin olduğu her yerde RENK de olur → profil/kanal başlığındaki isim,
    // görünen rozetlere tanımlı gradyanla boyanır. (Rozet yoksa [] → varsa eski
    // boyamamız temizlenir.) hideOnChannel rozetin rengi burada kullanılmaz.
    var grad = null;
    for (var g = 0; g < keys.length; g++) {
      var gd = badgeMaps.defs[keys[g]];
      if (gd && Array.isArray(gd.gradient) && gd.gradient.length) grad = gd.gradient;
    }
    /* 10.15 — kanal sahibinin AKAN ismi varsa o kazanır (isim her yüzeyde aynı
       görünmeli); yoksa görünen rozetlerin gradyanı.
       ANAHTAR SIRASI yukarıdaki rozet aramasıyla AYNI olmalı: önce görünen ad,
       sonra slug. Kick'te ikisi farklı olabiliyor (görünen ad büyük harfli ya
       da tamamen başka); yalnız slug'a bakan bir kontrol, adıyla kayıtlı
       aboneyi ıskalardı. */
    var akanKey = '';
    if (badgeMaps.akanByName) {
      var n1 = (name || '').toLowerCase(), n2 = String(slug || '').toLowerCase();
      if (n1 && badgeMaps.akanByName[n1]) akanKey = n1;
      else if (n2 && badgeMaps.akanByName[n2]) akanKey = n2;
    }
    if (akanKey) {
      isimBoya(h1, akanKey);
    } else {
      try { if (h1.getAttribute('data-pk-ozelad')) ozelAdSok(h1); } catch (e) {}
      applyNameGradient(h1, grad || []);
    }
    /* ERKEN DONUS TEMIZLIKTEN SONRA. Once "rozet yoksa cik" deniyordu ve
       onceki kanalin rozeti dugumde asili kaliyordu: Kick gezinmede h1/butonu
       yeniden kullaniyor, biz de rozetsiz kanalda hic silmeye ugramiyorduk.
       Canlida olculdu: sematall -> chips gecisinde isim "Chips" oldu, gradyan
       temizlendi (o zaten erken donusten once yapiliyor) ama semoshiro rozeti
       durdu; gezindikce de birikti. */
    var existing = btn.querySelectorAll(':scope > .pk-stream-badge');
    var have = []; for (var i = 0; i < existing.length; i++) have.push(existing[i].getAttribute('data-pk-key') || '');
    if (have.join('|') === keys.join('|')) return;               // dogru rozet(ler) zaten yerinde (ikisi de bos olabilir)
    for (var j = 0; j < existing.length; j++) existing[j].remove();
    if (!keys.length) return;                                    // temizlendi, eklenecek rozet yok
    var svg = btn.querySelector('svg');                          // verified rozeti (varsa)
    var sz = svg ? Math.round(svg.getBoundingClientRect().height) : 0;
    if (!sz) sz = Math.round(parseFloat(getComputedStyle(h1).fontSize)) || 20;
    var after = svg || h1;                                        // verified'dan (yoksa isimden) sonra
    for (var m = 0; m < keys.length; m++) {
      var def = badgeMaps.defs[keys[m]]; if (!def) continue;
      var el = makeStreamerBadge(def, sz, keys[m], adAnahtar); el.setAttribute('data-pk-key', keys[m]);
      if (after.nextSibling) btn.insertBefore(el, after.nextSibling); else btn.appendChild(el);
      after = el;
    }
  }
  function processStreamerHeader() {
    if (!badgeActive() || !badgeMaps) return;
    var slug = (location.pathname.split('/').filter(Boolean)[0] || '').toLowerCase();
    var h1s = document.querySelectorAll('h1');
    for (var i = 0; i < h1s.length; i++) { try { injectStreamerBadge(h1s[i], slug); } catch (e) {} }
  }

  /* ---- ARAMA SONUÇLARI (üstteki arama kutusunun açılır listesi) ----
   * Satır yapısı:  a[href="/slug"] > [avatar] [isim kutusu] [sağ]
   * Kullanıcı adı METİNDEN değil href SLUG'ından alınır (görünen ad farklı
   * yazılabiliyor: "PuPZera" ↔ /pupzera).
   * İsim satırı:  span > [span.truncate "İsim"]  [svg = doğrulanmış rozeti]
   * Rozetimiz DOĞRULANMIŞ ROZETİN ARDINA gelir (yoksa ismin ardına) — Kick'in
   * kendi düzenine uyar. Gradyan yalnız isim span'ine uygulanır (svg etkilenmesin).
   * Kenar çubuğundaki kanal listesi bu selector'a takılmaz (canlı doğrulandı). */
  function processSearchResults() {
    if (!badgeActive() || !badgeMaps) return;
    var rows = document.querySelectorAll('a[href^="/"] div[class*="text-surface-onSurface"] > span');
    for (var i = 0; i < rows.length; i++) {
      try {
        var row = rows[i], a = row.closest('a'); if (!a) continue;
        var path = (a.getAttribute('href') || '').replace(/^\//, '');
        if (!path || path.indexOf('/') > -1) continue;                // /category/… gibi kanal olmayan satırları atla
        var slug = path.split(/[?#]/)[0].toLowerCase();
        noteUser(slug);
        var nameEl = row.querySelector('span') || row;                // isim metni
        isimBoya(nameEl, slug);            // KURAL: renk de gelsin (akan varsa akan)
        var keys = badgeKeysFor(slug);
        var existing = row.querySelectorAll(':scope > .pk-badge');
        var have = []; for (var q = 0; q < existing.length; q++) have.push(existing[q].getAttribute('data-pk-key') || '');
        if (have.join('|') === keys.join('|')) continue;              // doğru rozet(ler) zaten yerinde
        for (var j = 0; j < existing.length; j++) existing[j].remove();
        var anchor = row.querySelector('svg') || nameEl;              // doğrulanmış rozetinden SONRA
        for (var m = 0; m < keys.length; m++) {
          var def = badgeMaps.defs[keys[m]]; if (!def) continue;
          var el = makeBadge(def, keys[m], slug);                     // tooltip makeBadge içinde bağlanır
          el.setAttribute('data-pk-key', keys[m]);
          el.setAttribute('data-pk-for', slug);
          el.style.marginRight = '0';                                 // satırda zaten gap var → çift boşluk olmasın
          if (anchor.nextSibling) row.insertBefore(el, anchor.nextSibling); else row.appendChild(el);
          anchor = el;
        }
      } catch (e) {}
    }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * KANAL GİZLEME — istemediğin yayıncıyı listelerden kaldırır
   *
   * NEREDE: kenar çubuğu (takip edilen/önerilen), ana sayfa kartları,
   *         kategori + /browse sayfaları, arama sonuçları.
   * NEREDE DEĞİL: sohbet mesajları, profil kartı, üst menü, kanalın kendi
   *         sayfası — oralara hiç dokunulmaz (bilerek).
   *
   * KABI BULMA: Bir kanalın sayfada birden çok linki olur (kart görseli,
   * avatar, isim, kenar çubuğu satırı). Tek tek gizlemek kartın iskeletini
   * bırakır. Bu yüzden linkten yukarı tırmanıp, BAŞKA bir kanalın linki
   * kapsama girmeden önceki son kabı alırız = kartın tam sınırı.
   * Güvenlik freni: ekranın %70'inden büyük bir kabı asla gizlemeyiz.
   * ══════════════════════════════════════════════════════════════════════ */
  var blocked = {};                 // slug -> 1
  /* NON_CHANNEL yukarıya (pkErkenPerde öncesine) taşındı — bkz. dosya başı. */
  // Bu bölgelerdeki linkler ASLA işlenmez: sohbet + oynatıcı.
  var BLOCK_SKIP = '[data-index], #chatroom, [class*="z-player"]';
  /* Profil kartı da atlanmalı AMA class'ıyla ayırt edilemiyor: arama açılır listesi
     de aynı `div.bg-surface-highest` kabını kullanıyor (canlı doğrulandı). Ayırt edici
     işaret: profil kartında kullanıcı adı `a.select-text.font-semibold` olarak bulunur,
     arama satırlarında bulunmaz. `nav/header` kullanmıyoruz — arama kutusu orada. */
  function inSkipZone(a) {
    try {
      if (a.closest(BLOCK_SKIP)) return true;
      var card = a.closest('div.bg-surface-highest');
      return !!(card && card.querySelector('a.select-text.font-semibold'));
    } catch (e) { return false; }
  }

  function chanSlug(a) {
    try {
      var h = a.getAttribute('href') || '';
      if (h.charAt(0) !== '/') return '';
      var p = h.slice(1).split(/[?#]/)[0];
      if (p.indexOf('/') > -1) return '';        // /category/slots gibi çok parçalı → kanal değil
      p = p.toLowerCase();
      return (p && !NON_CHANNEL[p]) ? p : '';
    } catch (e) { return ''; }
  }

  function blockContainer(a) {
    var el = a, best = a, lim = window.innerHeight * 0.7;
    for (var i = 0; i < 10 && el.parentElement && el.parentElement !== document.body; i++) {
      el = el.parentElement;
      var seen = {}, n = 0, links = el.querySelectorAll('a[href^="/"]');
      for (var j = 0; j < links.length; j++) {
        var s = chanSlug(links[j]);
        if (s && !seen[s]) { seen[s] = 1; n++; if (n > 1) break; }
      }
      if (n > 1) break;                          // başka kanal da girdi → fazla yukarı çıktık
      if (el.getBoundingClientRect().height > lim) break;   // güvenlik freni
      best = el;
    }
    return best;
  }

  function applyChannelBlocks() {
    if (!settings.consented) return;
    var links = document.querySelectorAll('a[href^="/"]');
    for (var i = 0; i < links.length; i++) {
      var a = links[i];
      try {
        var s = chanSlug(a); if (!s || !blocked[s]) continue;
        if (inSkipZone(a)) continue;     // sohbet/profil kartı/menü → dokunma
        var box = blockContainer(a);
        if (box && box.style.display !== 'none') {
          box.style.setProperty('display', 'none', 'important');
          box.setAttribute('data-pk-hidden', s);
        }
      } catch (e) {}
    }
  }

  function unhideChannel(slug) {                 // engel kalkınca gizlenenleri geri getir
    var els = document.querySelectorAll('[data-pk-hidden="' + slug + '"]');
    for (var i = 0; i < els.length; i++) { els[i].style.removeProperty('display'); els[i].removeAttribute('data-pk-hidden'); }
  }


  /* ══════════════════════════════════════════════════════════════════════
   * 10.6 — TAKİP EDİLEN KANALLARI SABİTLEME
   *
   * Kenar çubuğundaki takip listesinde bir satırın üstüne gelince raptiye
   * çıkar; sabitlenen kanal listenin en üstünde kalır.
   *
   * ÖLÇÜLEN GERÇEKLER (canlı DOM, kick.com):
   *   · Satırlar `a[data-testid^="sidebar-following-channel-"]` taşıyor.
   *     Metne ya da sınıfa bakmıyoruz: bu nitelik dile ve temaya bağlı değil.
   *   · Her satır bir `<button>` içinde ve o düğmeler kabın DOĞRUDAN
   *     çocukları. Kap `display:flex; flex-direction:column`.
   *   · Liste SANALLAŞTIRILMIŞ DEĞİL (mutlak konum/transform yok) — sohbetin
   *     aksine burada sıralamaya dokunmak güvenli.
   *   · Avatar adresi `/images/user/<id>/` taşıyor: kanalın SAYISAL kimliği.
   *
   * NEDEN DOM'U TAŞIMIYORUZ: React bu düğümleri kendi yönetiyor; yerlerini
   * değiştirirsek ilk yeniden çizimde geri alır ve satırlar zıplar. Bunun
   * yerine CSS `order` kullanılıyor — düğüm yerinde kalıyor, yalnız çizim
   * sırası değişiyor. Kural kendi niteliğimize bağlı, satır içi stile değil:
   * Kick satır içi stilleri siliyor ama `data-*` niteliklerimizi bırakıyor.
   *
   * SIRALAMA: başlık DIV'i kabın ilk çocuğu ve `order:0`. Sabitlenenler de
   * 0'da kalıyor (DOM sırası gereği başlıktan sonra), sabitlenmeyenler 1'e
   * itiliyor. Böylece başlığa hiç dokunmadan sabitler yukarı çıkıyor.
   *
   * KİMLİK: kanal adı değişebiliyor. Sabit, avatardaki sayısal kimlikle
   * tutuluyor; ad değişirse kayıt sessizce yeni ada güncelleniyor. Avatarı
   * olmayan satırda ada düşülüyor.
   * ══════════════════════════════════════════════════════════════════════ */
  var SABIT_LISTE = 'data-pk-takipliste';
  /* DİKKAT: `data-pk-sabit` BAŞKA bir özelliğin (sabitlenen profil kartı)
     işareti ve teardown o niteliği taşıyan düğümleri siliyor. Bu işaret
     Kick'in KENDİ düğmesine yazıldığı için ayrı bir ad şart. */
  var SABIT_IM    = 'data-pk-kanalsabit';
  var sabitler = Object.create(null);        // kimlik -> slug
  var sabitStil = false;

  function sabitKaydet() {
    try {
      var d = [];
      Object.keys(sabitler).forEach(function (k) { d.push({ k: k, s: sabitler[k] }); });
      chrome.storage.local.set({ pkSabitKanal: d });
    } catch (e) {}
  }

  function sabitYukle(dizi) {
    sabitler = Object.create(null);
    (Array.isArray(dizi) ? dizi : []).forEach(function (x) {
      if (x && x.k) sabitler[String(x.k)] = String(x.s || '');
    });
  }

  /* Satırın kalıcı kimliği. Avatardaki sayısal kimlik ada göre daha sağlam:
     kanal adını değiştirse bile sabit kaybolmaz. Avatar yoksa ada düşülür. */
  function sabitAnahtar(a) {
    try {
      var img = a.querySelector('img');
      var src = img ? (img.getAttribute('src') || '') : '';
      var m = /\/images\/user\/(\d+)\//.exec(src.split('?')[0]);
      if (m) return m[1];
    } catch (e) {}
    var s = chanSlug(a);
    return s ? 's:' + s : '';
  }

  /* Tema rengini CSS değişkenine yazar; tema değişince yeniden çağrılıyor. */
  function sabitRenkUygula() {
    try {
      document.documentElement.style.setProperty('--pk-sabit-renk',
        /^#[0-9a-f]{6}$/i.test(themeColor || '') ? themeColor : '#53fc18');
    } catch (e) {}
  }

  function sabitStilKur() {
    /* Bayrak yetmez: teardown ya da Kick'in head'i yeniden kurması stil
       düğümünü alıp götürebiliyor. Bayrak açık kalıp düğüm gittiğinde
       raptiyeler stilsiz kalıyor ve her satırda görünüyor (canlıda ölçüldü).
       Bu yüzden DÜĞÜMÜN VARLIĞINA bakıyoruz. */
    if (sabitStil && document.querySelector('[data-pk-sabit-stil]')) return;
    sabitStil = true;
    var st = document.createElement('style');
    st.setAttribute('data-pk-sabit-stil', '1');
    st.textContent =
      /* GRUPSUZ hâl: sabitlenmeyenler aşağı, başlık ve sabitler 0'da kalır. */
      '[' + SABIT_LISTE + ']:not([' + PK_GRUPLI + '])>button:not([' + SABIT_IM + ']){order:1}' +
      /* GRUPLU hâl: Kick başlığı 0 · Çevrimiçi 1 · sabitler 2 · canlılar 3 ·
         Çevrimdışı 4 · çevrimdışılar 5. */
      '[' + PK_GRUPLI + ']>[data-pk-grupbas="sekme"]{order:1}' +
      '[' + PK_GRUPLI + ']>button[' + PK_GRUP + '="canli"]{order:3}' +
      '[' + PK_GRUPLI + ']>button[' + PK_GRUP + '="canli"][' + SABIT_IM + ']{order:2}' +
      '[' + PK_GRUPLI + ']>button[' + PK_GRUP + '="kapali"]{order:5}' +
      '[' + PK_KAPALI_G + ']>button[' + PK_GRUP + '="kapali"]{display:none}' +
      /* Çevrimdışı sekmesi seçiliyken CANLI satırlar gizlenir — yukarıdaki
         kuralın tersi. İki kural birlikte "tek sekme görünür" demek. */
      '[' + PK_CANLI_G + ']>button[' + PK_GRUP + '="canli"]{display:none}' +
      '[' + PK_GRUPLI + ']>[' + PK_GRUP + '="bos"]{display:none}' +
      /* Çizim bizdeyken Kick'in kendi satırları ve başlığı gizli kalır. */
      '[' + PK_BIZIM + ']>*:not([' + PK_SATIR + ']):not([data-pk-grupbas]):not(.pk-iskelet){display:none}' +
      /* Perde: yalnız iskeletler görünür, gerçek içerik beklerken örtülü. */
      '[' + PK_PERDE + ']>*:not(.pk-iskelet){display:none}' +
      '.pk-iskelet{order:0}' +
      /* Raptiye KANAL ADININ SOLUNDA, satır içinde akıyor.
         Satırın sağ kenarına konduğunda izleyici sayısının ("17,8 B",
         "LIVE") üstüne biniyordu — kalıcı bir gösterge bilgi örtmemeli.
         `flex:0 0 auto`: ad uzun olup kırpılsa bile raptiye kırpılmasın. */
      /* SABİT DEĞİLKEN: adın SAĞINDA, yalnız satırın üstüne gelince. */
      '.pk-sabit-dugme{display:none;align-items:center;justify-content:center;' +
        'flex:0 0 auto;width:14px;height:14px;margin-left:4px;vertical-align:middle;' +
        'border-radius:3px;cursor:pointer;color:#fff;opacity:.6}' +
      '.pk-sabit-dugme:hover{opacity:1}' +
      'button:hover:not([' + SABIT_IM + ']) .pk-sabit-dugme{display:inline-flex}' +
      /* SABİTKEN: AVATARIN SOLUNDAKİ oluk, kalıcı. Negatif kenar boşluğu
         satırın kendi iç boşluğuna çekiyor ki avatar ve isim kaymasın. */
      '.pk-sabit-oluk{display:none;align-items:center;justify-content:flex-start;' +
        'flex:0 0 auto;width:12px;height:14px;margin-left:-8px;margin-right:2px;' +
        'cursor:pointer;color:var(--pk-sabit-renk,#53fc18);opacity:1}' +
      '.pk-sabit-oluk:hover{opacity:.55}' +
      'button[' + SABIT_IM + '] .pk-sabit-oluk{display:flex}';
    (document.head || document.documentElement).appendChild(st);
    sabitRenkUygula();
  }

  /* Kanal adını taşıyan SPAN. Satırda tek `flex-col` sütun var: üstte ad,
     altta kategori. Metne bakmıyoruz ki dile bağlı olmasın. */
  function sabitAdOgesi(a) {
    var sut = a.querySelector('div[class*="flex-col"]');
    var sp = sut && sut.firstElementChild;
    return (sp && sp.tagName === 'SPAN') ? sp : null;
  }

  function sabitIkonu(dolu) {
    var ns = 'http://www.w3.org/2000/svg';
    var svg = document.createElementNS(ns, 'svg');
    svg.setAttribute('viewBox', '0 0 24 24');
    svg.setAttribute('width', '13'); svg.setAttribute('height', '13');
    svg.setAttribute('fill', dolu ? 'currentColor' : 'none');
    svg.setAttribute('stroke', 'currentColor');
    svg.setAttribute('stroke-width', '2');
    svg.setAttribute('stroke-linecap', 'round');
    svg.setAttribute('stroke-linejoin', 'round');
    var p = document.createElementNS(ns, 'path');
    /* Raptiye: başlık + gövde + iğne. Kendi çizimimiz. */
    p.setAttribute('d', 'M9 3h6l-1 5 3 3v2H7v-2l3-3-1-5z');
    svg.appendChild(p);
    var l = document.createElementNS(ns, 'line');
    l.setAttribute('x1', '12'); l.setAttribute('y1', '13');
    l.setAttribute('x2', '12'); l.setAttribute('y2', '21');
    svg.appendChild(l);
    return svg;
  }

  function sabitDegis(anahtar, slug) {
    if (!anahtar) return;
    if (sabitler[anahtar] !== undefined) delete sabitler[anahtar];
    else sabitler[anahtar] = slug || '';
    sabitKaydet();
    sabitKanalUygula();
  }

  /* Özellik kapatılınca bıraktığımız her şeyi topla. */
  function sabitTemizle() {
    var d = document.querySelectorAll('.pk-sabit-dugme,.pk-sabit-oluk');
    for (var i = 0; i < d.length; i++) d[i].remove();
    var im = document.querySelectorAll('[' + SABIT_IM + ']');
    for (var j = 0; j < im.length; j++) im[j].removeAttribute(SABIT_IM);
    var lk = document.querySelectorAll('[' + SABIT_LISTE + ']');
    for (var k = 0; k < lk.length; k++) lk[k].removeAttribute(SABIT_LISTE);
  }


  /* ══════════════════════════════════════════════════════════════════════
   * ÇEVRİMİÇİ KANALLARIN HEPSİNİ GÖSTER
   *
   * Kick takip listesini 10 satırda kesiyor ve "daha az" denince fazlaları
   * DOM'dan SİLİYOR (gizlemiyor — ölçüldü). Sabitlenen kanal kesme
   * noktasının altındaysa ortada eleman kalmıyor ve sıralanamıyor.
   *
   * Kick'in kendi düğmesi kademeli açıyor (+5) ve liste bitince düğmeyi
   * KALDIRIYOR — bitişi böyle anlıyoruz (ölçüldü: 35 satırda düğme yok).
   * Hepsini açıyoruz ki çevrimdışı bölümü de dolu olsun; çevrimdışı satırlar
   * varsayılan olarak kapalı durduğu için ekranda yer kaplamıyorlar.
   * ══════════════════════════════════════════════════════════════════════ */
  var SABIT_AC_DUGME = '[data-testid="sidebar-show-more-following"]';
  var SABIT_AC_TAVAN = 20;
  var sabitAcSayac = 0, sabitAcSonSayi = 0, sabitAcZaman = 0;

  /* Satır canlı mı? Kick canlı satırda yeşil nokta + izleyici sayısı taşıyan
     bir DIV koyuyor; çevrimdışı satırda onun yerine bir SVG var (ölçüldü).
     Renge ya da metne bakmıyoruz — ikisi de değişebilir, yapı değişmez. */
  function sabitSatirCanli(a) {
    var sut = a.querySelector('div[class*="flex-col"]');
    var sag = sut && sut.parentElement && sut.parentElement.children[1];
    if (!sag) return false;
    return String(sag.tagName || '').toLowerCase() !== 'svg';
  }

  function sabitListeAc(kap, satirlar) {
    if (settings.kenarGrup === false || !kap || !satirlar.length) return;
    /* Liste küçüldüyse (kullanıcı daralttı ya da başka kanala geçildi)
       sayaçlar sıfırlanır, yoksa bir daha hiç açılmaz. */
    if (satirlar.length < sabitAcSonSayi) {
      sabitAcSayac = 0; sabitAcSonSayi = 0;
      perdeBasla = 0; perdeBitti = false;        // yeniden açılacak, perde de yeniden
    }
    if (sabitAcSayac >= SABIT_AC_TAVAN) return;              // sonsuz tıklama freni
    /* Tıkladık ama sayı artmadıysa Kick daha fazlasını vermiyor. */
    if (sabitAcSayac && satirlar.length <= sabitAcSonSayi) return;
    var simdi = Date.now();
    if (simdi - sabitAcZaman < 300) return;                  // React çizsin diye ara ver
    var d = document.querySelector(SABIT_AC_DUGME);
    if (!d) return;                    // düğme yok = liste bitti
    sabitAcSayac++; sabitAcSonSayi = satirlar.length; sabitAcZaman = simdi;
    try { d.click(); } catch (e) {}
  }






  /* ══════════════════════════════════════════════════════════════════════
   * OTOMATİK SES
   * ══════════════════════════════════════════════════════════════════════ */
  /* Yalnız iki mod. Sabit yüzde YOK: `video.volume` doğrusal, Kick'in
     oynatıcısı algısal bir yüzde gösteriyor (0.25 -> %54). Kendi
     yüzdemizi basmak kullanıcıya yalan söylerdi; sesi oynatıcının kendi
     kaydırıcısından alıp hatırlıyoruz. */
  var SES_SECENEK = ['kapali', 'oto'];
  var sesSeviye = -1;          // öğrenilen seviye (0..1); -1 = henüz bilinmiyor
  var sesSessiz = false;
  var sesYaziyoruz = false;    // kendi yazdığımızı kullanıcı değişikliği sanmayalım
  var sesKanal = '';           // bu kanalda uygulandı mı
  var sesVideo = null;

  function sesKaydet() {
    try { chrome.storage.local.set({ pkSes: { s: sesSeviye, m: sesSessiz } }); } catch (e) {}
  }

  /* Kullanıcının seviyesini öğren. Bayrak şart: kendi uyguladığımız değeri
     yeniden öğrenirsek ayar hiçbir işe yaramaz. */
  function sesDinle(v) {
    if (sesVideo === v) return;
    sesVideo = v;
    v.addEventListener('volumechange', function () {
      if (sesYaziyoruz) return;
      sesSeviye = v.volume; sesSessiz = !!v.muted;
      sesKaydet();
    });
  }

  function sesUygula() {
    if (!settings.consented) return;
    /* Bilinmeyen mod (eski yapıdan kalan '25' gibi) 'oto' sayılıyor;
       yoksa ayar sessizce hiç uygulanmazdı. */
    var mod = (settings.otoSes === 'kapali') ? 'kapali' : 'oto';
    if (mod === 'kapali') return;
    var v = oynaticiVideosu();
    if (!v) return;
    sesDinle(v);
    var yol = location.pathname || '';
    /* Kanal başına bir kez: yayın ortasında kullanıcı sesi değiştirirse
       üstüne yazmıyoruz. */
    if (sesKanal === yol) return;
    /* Kullanıcının oynatıcıda bıraktığı DOĞRUSAL değer; çevrim yok. */
    var hedef = sesSeviye;
    if (!(hedef >= 0)) return;              // henüz bir şey öğrenmedik
    sesKanal = yol;
    sesYaziyoruz = true;
    try {
      v.volume = Math.max(0, Math.min(1, hedef));
      v.muted = sesSessiz;
    } catch (e) {}
    /* Bayrağı hemen değil, olay kuyruğu boşalınca indiriyoruz:
       `volumechange` eşzamanlı gelmiyor. */
    setTimeout(function () { sesYaziyoruz = false; }, 80);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * SOHBET KURALLARINI OTOMATİK KABUL
   * ══════════════════════════════════════════════════════════════════════ */
  var KURAL_PANEL = 'chat-rules-panel';
  var KURAL_TAVAN = 5;                    // kanal başına en fazla deneme
  var KURAL_BEKLE = 1200;                 // tıkladıktan sonra tekrar bakma
  var kuralYol = '', kuralSayac = 0, kuralBitti = '', kuralMesgul = false;

  /* Kabul düğmesi. Panelde TEK düğme var (ölçüldü) — metne bakmıyoruz,
     dile bağlı olmasın. İkinci bir düğme çıkarsa null dönüyoruz: yanlış
     düğmeye basmaktansa hiç basmamak doğrusu. */
  function kuralDugmesi() {
    var p2 = document.getElementById(KURAL_PANEL);
    if (!p2 || p2.getBoundingClientRect().height < 1) return null;
    var hepsi = p2.querySelectorAll('button');
    var uygun = [];
    for (var i = 0; i < hepsi.length; i++) {
      var b = hepsi[i];
      if (b.disabled || b.getAttribute('aria-disabled') === 'true') continue;
      if (b.getBoundingClientRect().width < 1) continue;
      uygun.push(b);
    }
    return uygun.length === 1 ? uygun[0] : null;
  }

  function kuralKabulUygula() {
    if (!settings.consented || settings.kuralKabul !== true) return;
    var yol = location.pathname || '';
    /* Kanal değişti: sayaçlar sıfırlanır. */
    if (yol !== kuralYol) { kuralYol = yol; kuralSayac = 0; kuralBitti = ''; }
    if (kuralMesgul || kuralBitti === yol || kuralSayac >= KURAL_TAVAN) return;
    var d = kuralDugmesi();
    if (!d) return;
    kuralMesgul = true; kuralSayac++;
    try { d.click(); } catch (e) {}
    /* Panel gerçekten kapandı mı? Kapandıysa bu kanalda işimiz bitti;
       kapanmadıysa tavana kadar yeniden denenir. */
    setTimeout(function () {
      kuralMesgul = false;
      try { if (!kuralDugmesi()) kuralBitti = yol; } catch (e) {}
    }, KURAL_BEKLE);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * KANAL HAKKINDA — EK BİLGİ
   * ══════════════════════════════════════════════════════════════════════ */
  var HAKKINDA_NOKTA = '#channel-content section > .flex.grow.items-center.justify-between';
  var HAKKINDA_IM    = 'data-pk-hakkinda';
  var HAKKINDA_TAZE  = 300000;               // 5 dk
  var HAKKINDA_EMOTE_GOSTER = 6;             // satırda kaç emote, kalanı "+N"
  var HAKKINDA_KATEGORI = 3;
  var hakkindaVeri = Object.create(null);    // slug -> { ts, rozet, emote, kategori, kural }
  /* AÇILAN bölümler. Ters tutmak önemli: kayıt yokken hepsi kapalı gelir,
     ileride bölüm eklersek o da kapalı doğar. */
  var hakkindaAcik = Object.create(null);

  /* Üç istek paralel; hepsi tamamlanınca bir kez haber veriyoruz. Biri
     patlarsa o parça boş kalır, diğerleri yine gösterilir. */
  function hakkindaCek(slug, bitince) {
    var v = hakkindaVeri[slug];
    if (v && v.ts && Date.now() - v.ts < HAKKINDA_TAZE) { bitince(v); return; }
    if (v && v.bekliyor) return;
    var yeni = { ts: 0, bekliyor: true, rozet: [], emote: [], kategori: [], kural: '' };
    hakkindaVeri[slug] = yeni;
    var kalan = 3;
    function bitti() {
      if (--kalan > 0) return;
      yeni.bekliyor = false; yeni.ts = Date.now();
      bitince(yeni);
    }
    var kok = 'https://kick.com/api/v2/channels/' + encodeURIComponent(slug);

    fetch(kok, { credentials: 'omit' })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        if (!j) return;
        (j.subscriber_badges || []).forEach(function (b) {
          var src = b && b.badge_image && (b.badge_image.src || b.badge_image.url);
          if (src) yeni.rozet.push({ ay: parseInt(b.months, 10) || 0, src: src });
        });
        yeni.rozet.sort(function (a, b2) { return a.ay - b2.ay; });
        (j.recent_categories || []).forEach(function (k) {
          if (k && k.name) yeni.kategori.push({ ad: k.name, slug: k.slug || '' });
        });
      })
      .catch(function () {})
      .then(bitti, bitti);

    /* Emote uçları kanal grubunu sayısal kanal kimliğiyle adlandırıyor;
       Global ve Emoji gruplarını almıyoruz — onlar kanala ait değil. */
    fetch('https://kick.com/emotes/' + encodeURIComponent(slug), { credentials: 'omit' })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        if (!Array.isArray(j)) return;
        for (var i = 0; i < j.length; i++) {
          var g = j[i];
          /* Kanal grubu: emotelerinde `channel_id` DOLU. Global ve Emoji
             gruplarında null (ölçüldü). Grup adına bakmıyoruz — "Global"
             ve "Emojis" İngilizce sabitler, dile göre değişebilir. */
          var kanalGrubu = g && g.emotes && g.emotes[0] && g.emotes[0].channel_id != null;
          if (!kanalGrubu) continue;
          g.emotes.forEach(function (e) {
            if (e && e.id && e.name) {
              yeni.emote.push({ ad: e.name, src: 'https://files.kick.com/emotes/' + e.id + '/fullsize',
                                aboneye: e.subscribers_only === true });
            }
          });
          break;
        }
      })
      .catch(function () {})
      .then(bitti, bitti);

    fetch(kok + '/chatroom/rules', { credentials: 'omit' })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        var k = j && j.data && j.data.rules;
        if (typeof k === 'string' && k.trim()) yeni.kural = k.trim();
      })
      .catch(function () {})
      .then(bitti, bitti);
  }


  function hakkindaStilKur() {
    if (document.querySelector('[data-pk-hk-stil]')) return;
    var st = document.createElement('style');
    st.setAttribute('data-pk-hk-stil', '1');
    st.textContent =
      '[' + HAKKINDA_IM + ']{display:flex;flex-direction:column;gap:4px;margin-top:10px}' +
      /* Başlık: tıklanınca bölüm katlanır. */
      '.pk-hk-bas{display:flex;align-items:center;gap:5px;cursor:pointer;user-select:none;' +
        'width:max-content;font-size:13px;font-weight:600}' +
      '.pk-hk-ok{flex:0 0 auto;transition:transform .15s ease}' +
      '.pk-hk-bolum[data-pk-kapali] .pk-hk-ok{transform:rotate(-90deg)}' +
      '.pk-hk-bolum[data-pk-kapali] .pk-hk-satir{display:none}' +
      '.pk-hk-bolum{display:flex;flex-direction:column;gap:6px;margin-top:6px}' +
      /* IZGARA: eşit sütunlar, uzun adlar hizayı bozmuyor. Çok öğede
         (56 emote gibi) kendi içinde kayıyor, sayfayı uzatmıyor. */
      '.pk-hk-satir{display:grid;grid-template-columns:repeat(auto-fill,minmax(72px,1fr));' +
        'gap:10px;max-height:260px;overflow-y:auto;padding:10px;border-radius:8px}' +
      /* Kural gövdesi ızgara değil, tek blok. */
      '.pk-hk-bolum[data-pk-hk-bolum="kural"]:not([data-pk-kapali]) .pk-hk-satir{display:block;padding:0;' +
        'max-height:none;overflow:visible}' +
      /* Kategoriler kutucuk değil ETİKET: ızgara hücresine sıkışınca ad
         satır atlıyordu ("Slots &" / "Casino"). Yatay akıyorlar. */
      '.pk-hk-bolum[data-pk-hk-bolum="kategori"]:not([data-pk-kapali]) .pk-hk-satir{display:flex;' +
        'flex-wrap:wrap;gap:8px;max-height:none;overflow:visible}' +
      '.pk-hk-kat{white-space:nowrap;flex:0 0 auto;padding:4px 10px;border-radius:6px;' +
        'font-size:13px;line-height:1.2}' +
      '.pk-hk-kat:hover{filter:brightness(1.25)}' +
      /* Kutucuk: görsel üstte, etiket altında. */
      '.pk-hk-kutu{display:flex;flex-direction:column;align-items:center;gap:5px;' +
        'min-width:0;padding:6px 2px;border-radius:6px;cursor:zoom-in}' +
      '.pk-hk-kutu:hover{background:rgba(255,255,255,.06)}' +
      '.pk-hk-gorsel{width:32px;height:32px;border-radius:5px;object-fit:contain}' +
      '.pk-hk-etiket{font-size:11px;font-weight:600;text-align:center;white-space:nowrap;' +
        'line-height:1.2;max-width:100%;overflow:hidden;text-overflow:ellipsis}' +
      '.pk-hk-kural-govde{display:block;white-space:pre-wrap;overflow-wrap:anywhere;' +
        'max-height:180px;overflow-y:auto;padding:8px 10px;border-radius:6px;' +
        'font-size:13px;line-height:1.45}';
    (document.head || document.documentElement).appendChild(st);
  }

  function hkOk() {
    var ns = 'http://www.w3.org/2000/svg';
    var svg = document.createElementNS(ns, 'svg');
    svg.setAttribute('viewBox', '0 0 24 24');
    svg.setAttribute('width', '13'); svg.setAttribute('height', '13');
    svg.setAttribute('fill', 'none'); svg.setAttribute('stroke', 'currentColor');
    svg.setAttribute('stroke-width', '2.5');
    svg.setAttribute('stroke-linecap', 'round'); svg.setAttribute('stroke-linejoin', 'round');
    svg.setAttribute('class', 'pk-hk-ok');
    var pl = document.createElementNS(ns, 'polyline');
    pl.setAttribute('points', '6 9 12 15 18 9');
    svg.appendChild(pl);
    return svg;
  }

  /* Bir bölüm: katlanabilir başlık + kutucuk satırı. */
  function hkBolum(anahtar, baslik) {
    var b = el('div', 'pk-hk-bolum');
    b.setAttribute('data-pk-hk-bolum', anahtar);
    if (!hakkindaAcik[anahtar]) b.setAttribute('data-pk-kapali', '1');
    var h = el('div', 'pk-hk-bas text-subtle');
    h.appendChild(hkOk());
    h.appendChild(el('span', '', baslik));
    h.setAttribute('role', 'button');
    h.setAttribute('tabindex', '0');
    h.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      var kapali = b.getAttribute('data-pk-kapali');
      if (kapali) { b.removeAttribute('data-pk-kapali'); hakkindaAcik[anahtar] = 1; }
      else { b.setAttribute('data-pk-kapali', '1'); delete hakkindaAcik[anahtar]; }
      hakkindaAcikKaydet();
    });
    b.appendChild(h);
    var satir = el('div', 'pk-hk-satir bg-surface-highest');
    b.appendChild(satir);
    b.pkSatir = satir;
    return b;
  }

  /* Kutucuk: görsel üstte, etiket altında. */
  function hkKutu(src, etiket, ipucu) {
    var k = el('div', 'pk-hk-kutu');
    var im = el('img', 'pk-hk-gorsel');
    im.src = src; im.alt = etiket || '';
    im.setAttribute('loading', 'lazy');
    im.setAttribute('decoding', 'async');
    /* Yüklenemeyen görsel kutuyu boş bırakmasın. */
    im.addEventListener('error', function () { try { k.remove(); } catch (e) {} }, { once: true });
    k.appendChild(im);
    if (etiket) k.appendChild(el('span', 'pk-hk-etiket text-subtle', etiket));
    if (ipucu) k.setAttribute('title', ipucu);
    /* Tıklayınca büyür — profil resmindeki katmanın aynısı, yenisini
       yazmıyoruz. Bölüm başlığına değil kutucuğa bağlı, katlamayı bozmasın. */
    k.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      try { gorselBuyut(src, etiket || ''); } catch (x) {}
    });
    return k;
  }

  function hakkindaCiz(kap, slug, v) {
    hakkindaStilKur();
    var imza = slug + '|' + v.rozet.length + '|' + v.emote.length + '|' +
               v.kategori.length + '|' + (v.kural ? v.kural.length : 0);
    /* ÇİFT PANEL: kanal yayına geçerken iki kopya görülebiliyordu. Kaç mount
       noktası çıkarsa çıksın belgedeki bütün kopyalar siliniyor. */
    var mevcut = document.querySelectorAll('[' + HAKKINDA_IM + ']');
    if (mevcut.length === 1 && mevcut[0].parentElement === kap &&
        mevcut[0].getAttribute('data-pk-imza') === imza) return;
    for (var m = 0; m < mevcut.length; m++) mevcut[m].remove();

    var dis = el('div', '');
    dis.setAttribute(HAKKINDA_IM, '1');
    dis.setAttribute('data-pk-imza', imza);

    if (v.rozet.length) {
      var br = hkBolum('rozet', t('pkHkRozetler'));
      v.rozet.forEach(function (r) {
        br.pkSatir.appendChild(hkKutu(r.src, r.ay ? t('pkHkAy', [String(r.ay)]) : '', ''));
      });
      dis.appendChild(br);
    }
    if (v.emote.length) {
      var be = hkBolum('emote', t('pkHkEmoteler'));
      v.emote.forEach(function (e2) {
        be.pkSatir.appendChild(hkKutu(e2.src, e2.ad,
          e2.ad + (e2.aboneye ? ' · ' + t('pkHkAboneye') : '')));
      });
      dis.appendChild(be);
    }
    if (v.kategori.length) {
      var bk = hkBolum('kategori', t('pkHkKategoriler'));
      v.kategori.forEach(function (k2) {
        /* Panel `bg-surface-highest`; etiket bir ton KOYU (`bg-surface-base`)
           ki iç içe olduğu okunsun. İkisi de Kick'in kendi tokenı. */
        var a = el('a', 'pk-hk-kat bg-surface-base text-subtle');
        a.textContent = k2.ad;
        if (k2.slug) a.setAttribute('href', '/category/' + k2.slug);
        bk.pkSatir.appendChild(a);
      });
      dis.appendChild(bk);
    }
    /* Kural çoğu kanalda boş; boşken bölüm hiç açılmıyor. */
    if (v.kural) {
      var bq = hkBolum('kural', t('pkHkKurallar'));
      var g = el('div', 'pk-hk-kural-govde bg-surface-highest text-subtle');
      g.textContent = v.kural;
      bq.pkSatir.appendChild(g);
      dis.appendChild(bq);
    }

    if (dis.children.length) kap.appendChild(dis);
  }

  function hakkindaAcikKaydet() {
    try {
      chrome.storage.local.set({ pkHakkindaAcik: Object.keys(hakkindaAcik) });
    } catch (e) {}
  }

  function hakkindaUygula() {
    if (!settings.consented) return;
    if (settings.hakkindaEk === false) { hakkindaTemizle(); return; }
    var satir = document.querySelector(HAKKINDA_NOKTA);
    /* Bölümler "Hakkında" satırının ALTINA, kabın içine giriyor. */
    var kap = satir && satir.parentElement;
    if (!kap) return;
    var slug = (location.pathname || '').split('/').filter(Boolean)[0] || '';
    if (!slug) return;
    slug = slug.toLowerCase();
    hakkindaCek(slug, function (v) {
      try {
        var s2 = document.querySelector(HAKKINDA_NOKTA);
        var k2 = s2 && s2.parentElement;
        if (k2) hakkindaCiz(k2, slug, v);
      } catch (e) {}
    });
  }
  function hakkindaTemizle() {
    var q = document.querySelectorAll('[' + HAKKINDA_IM + '],.pk-hk-kural-govde');
    for (var i = 0; i < q.length; i++) q[i].remove();
  }

  /* ══════════════════════════════════════════════════════════════════════
   * TAKİP LİSTESİ VERİSİ
   * ══════════════════════════════════════════════════════════════════════ */
  var ROSTER_UC   = 'https://kick.com/api/v2/channels/followed-page';
  var ROSTER_SAYFA_TAVAN = 8;              // 8 x 20 = 160 kanal
  var ROSTER_TAZE = 45000;                 // canlı durum tazeleme aralığı
  var roster = null;                       // [{slug, ad, avatar, canli, izleyici, sayiGoster, kategori}]
  var rosterZaman = 0, rosterIstek = false;
  /* Boş dönen denemeden sonra beklenecek süre. 10.14'te jeton kontrolü
     kaldırıldığı için (Brave düzeltmesi) ÇIKIŞ YAPMIŞ kullanıcıda istek artık
     gerçekten atılıyor; geri çekilme olmasa 30 sn'lik tur bunu sonsuza dek
     tekrarlardı. İlk deneme her zaman yapılır — Brave'de liste böyle geliyor. */
  var ROSTER_BOS_BEKLE = 300000;           // 5 dk
  var rosterBosZaman = 0;

  /* Kick oturum jetonu. HttpOnly DEĞİL — sitenin kendi arayüzü de böyle
     okuyor. Yoksa (çıkış yapılmış) roster kullanılamaz, Kick'in kendi
     listesine düşülür. */
  function rosterJeton() {
    try {
      var c = document.cookie.split(';');
      for (var i = 0; i < c.length; i++) {
        var t = c[i].trim(), e = t.indexOf('=');
        if (e > 0 && t.slice(0, e) === 'session_token') {
          try { return decodeURIComponent(t.slice(e + 1)); } catch (x) { return t.slice(e + 1); }
        }
      }
    } catch (e) {}
    return '';
  }

  function rosterKayit(k) {
    var slug = k && typeof k.channel_slug === 'string' ? k.channel_slug : '';
    if (!slug) return null;
    return {
      slug: slug,
      ad: k.user_username || slug,
      avatar: typeof k.profile_picture === 'string' ? k.profile_picture : '',
      canli: k.is_live === true,
      izleyici: parseInt(k.viewer_count, 10) || 0,
      /* Kick bazı kanallarda sayıyı gizliyor; orada sayı yerine "LIVE" var. */
      sayiGoster: k.show_view_count !== false,
      kategori: typeof k.category_name === 'string' ? k.category_name : ''
    };
  }

  /* Sayfaları sırayla toplar. Tavan var: çok kanal takip edende sonsuz
     istek atmayalım. */
  function rosterCek(bitince) {
    if (rosterIstek) return;
    /* 10.14 — BRAVE DÜZELTMESİ: jeton okunamasa da istek atılır.
       session_token çerezi document.cookie ile okunuyordu; Brave çerez
       erişimini kısıtladığı için jeton BOŞ dönüyor ve çevrimiçi kanal listesi
       hiç çekilmiyordu (Chrome'da sorun yok). Oysa credentials:'include'
       çerezi tarayıcıya zaten gönderttiriyor — Authorization yalnızca yedek.
       Jeton varsa başlığa eklenir, yoksa istek çerezle gider. */
    var jeton = rosterJeton();
    /* Geri çekilme yalnız jeton YOKKEN sürer; jeton belirdiyse (kullanıcı bu
       arada giriş yaptı) beklemeden yeniden denenir. */
    if (!jeton && rosterBosZaman && Date.now() - rosterBosZaman < ROSTER_BOS_BEKLE) {
      if (bitince) bitince(null); return;   // yakında boş döndü, boşuna deneme
    }
    rosterIstek = true;
    var toplam = [], sayfa = 0;
    function al(imlec) {
      var url = ROSTER_UC + (imlec ? ('?cursor=' + encodeURIComponent(imlec)) : '');
      var basliklar = { accept: 'application/json' };
      if (jeton) basliklar.Authorization = 'Bearer ' + jeton;
      fetch(url, { credentials: 'include', headers: basliklar })
        .then(function (r) { return r.ok ? r.json() : null; })
        .then(function (j) {
          var dizi = j && (j.channels || j.data);
          if (!Array.isArray(dizi)) { rosterBitir(toplam, bitince); return; }
          for (var i = 0; i < dizi.length; i++) {
            var kk = rosterKayit(dizi[i]);
            if (kk) toplam.push(kk);
          }
          sayfa++;
          if (j.nextCursor && dizi.length && sayfa < ROSTER_SAYFA_TAVAN) al(j.nextCursor);
          else rosterBitir(toplam, bitince);
        })
        .catch(function () { rosterBitir(toplam, bitince); });
    }
    al('');
  }

  function rosterBitir(liste, bitince) {
    rosterIstek = false;
    if (!liste.length) {
      rosterBosZaman = Date.now();          // geri çekil: 5 dk yeniden deneme yok
      if (bitince) bitince(null); return;
    }
    rosterBosZaman = 0;                     // liste geldi → geri çekilme sıfırlanır
    /* Kick'in kenar çubuğu sırası: önce canlılar, izleyiciye göre azalan;
       sonra çevrimdışılar. Aynı sırayı kuruyoruz. */
    liste.sort(function (a, b) {
      if (a.canli !== b.canli) return a.canli ? -1 : 1;
      return (b.izleyici || 0) - (a.izleyici || 0);
    });
    roster = liste; rosterZaman = Date.now();
    try { chrome.storage.local.set({ pkRoster: { ts: rosterZaman, k: liste } }); } catch (e) {}
    if (bitince) bitince(liste);
  }

  /* Süresi dolduysa tazele. Sekme arka plandayken boşuna istek atmıyoruz. */
  /* ══════════════════════════════════════════════════════════════════════
   * GÜNLÜK ÖDÜL — otomatik alma (ayar: otoClaim)
   *
   * Jetonu buradan gönderiyoruz (`document.cookie` yalnız içerik betiğinde
   * okunabiliyor), sorgu ve claim arka planda — `web.kick.com` içerik betiği
   * için çapraz köken.
   *
   * ARALIK 5 DAKİKA: görev izleme süresine dayanıyor, saniye saniye bakmanın
   * faydası yok. Arka planda ayrıca 4 dakikalık bir gem var — çok sekme açık
   * olsa da Kick'e dakikada bir istekten fazlası gitmiyor.
   * SEKME GİZLİYKEN SORMUYORUZ: izleme süresi zaten işlemiyor.
   * ══════════════════════════════════════════════════════════════════════ */
  var ODUL_ARA_MS = 5 * 60 * 1000;
  var odulZaman = 0, odulSonDurum = null;

  /* Panelde gösterilen durum satırı. Veri yoksa hiç basılmıyor — boş bir
     "—" satırı bilgi vermiyor. Süreler dakika geliyor, pkSureYazi saniye
     istiyor (60 ile çarpılıyor). */
  function odulDurumYazi() {
    if (settings.otoClaim !== true) return null;
    var o = odulSonDurum;
    if (!o) return el('div', 'text-xs text-subtle', t('pkOdulKontrol'));
    var yazi = '';
    if (o.durum === 'yeni' || o.durum === 'alindi') yazi = t('pkOdulAlindi');
    else if (o.durum === 'yok') yazi = t('pkOdulYok');
    else if (o.durum === 'bekliyor' && o.hedef > 0) {
      yazi = t('pkOdulIlerleme', [pkSureYazi((o.ilerleme || 0) * 60), pkSureYazi(o.hedef * 60)]);
    } else if (o.durum === 'jetonyok') yazi = t('pkOdulGirisGerek');
    else yazi = t('pkOdulHata');
    return el('div', 'text-xs text-subtle', yazi);
  }

  /* Ödül kartı açıksa yeniden çiz. Ayrı işlev: üç yerden çağrılıyor ve
     hepsinde aynı koşul geçerli. */
  function odulPaneliTazele() {
    try {
      if (panelEl && document.contains(panelEl) &&
          panelYol[panelYol.length - 1] === 'drops') panelCiz();
    } catch (e) {}
  }

  function odulSor(zorla) {
    if (settings.otoClaim !== true) return;
    if (!zorla && document.hidden) return;
    if (!zorla && Date.now() - odulZaman < ODUL_ARA_MS) return;
    odulZaman = Date.now();
    var jeton = '';
    try { jeton = rosterJeton(); } catch (e) {}
    if (!jeton) {
      /* Sessizce çıkmıyoruz: durum satırı "kontrol ediliyor" da asılı
         kalırdı. Kullanıcı giriş yapması gerektiğini görsün. */
      odulSonDurum = { durum: 'jetonyok' };
      odulPaneliTazele();
      return;
    }
    sendBg({ type: 'pk-odul', jeton: jeton, zorla: !!zorla }).then(function (o) {
      /* 'erken' ve 'kapali' BİLGİ DEĞİL: eski durumu ezmiyoruz. */
      if (!o || !o.durum || o.durum === 'erken' || o.durum === 'kapali') return;
      odulSonDurum = o;
      /* Toast YALNIZ yeni alındığında: her turda "zaten alınmış" demek
         gürültü olurdu. */
      if (o.durum === 'yeni') { try { toast(t('pkTOdulAlindi')); } catch (e) {} }
      odulPaneliTazele();
    }).catch(function () {
      odulSonDurum = { durum: 'hata' };
      odulPaneliTazele();
    });
  }

  function rosterTazele() {
    if (document.hidden) return;
    if (rosterZaman && Date.now() - rosterZaman < ROSTER_TAZE) return;
    rosterCek(function () { try { sabitKanalUygula(); } catch (e) {} });
  }


  /* ══════════════════════════════════════════════════════════════════════
   * KENAR ÇUBUĞUNU KENDİMİZ ÇİZİYORUZ
   * ══════════════════════════════════════════════════════════════════════ */
  var PK_BIZIM  = 'data-pk-bizim';        // kap: çizim bizde
  var PK_SATIR  = 'data-pk-satirimiz';
  var PK_SLUG   = 'data-pk-slug';
  var rosterSablon = null;                 // Kick satırından klon kalıbı
  var rosterCanliEtiket = '';              // sayısı gizli kanallarda yazan söz ("LIVE")
  /* İzlenen kanalın satırı vurgulu: Kick bu iki işareti koyuyor. Kalıba
     bunlar bulaşırsa BÜTÜN satırlar vurgulu çıkar (canlıda öyle oldu). */
  var PK_VURGU_SINIF = 'bg-surface-highest';

  /* Kalıp: Kick'in bastığı gerçek bir satır. Bir kez görüp saklıyoruz;
     önbelleğe de yazıyoruz ki sonraki açılışta beklemeden basalım. */
  function sablonKur(kap) {
    /* "LIVE" sözünü Kick'in kendi satırından yakala: sayı yerine yazı olan
       satır sayısı gizli olandır. Metni uydurmuyoruz, dile de bağlı kalmıyoruz. */
    if (!rosterCanliEtiket) {
      var hepsi = kap.querySelectorAll('a[data-testid^="sidebar-following-channel-"]');
      for (var h = 0; h < hepsi.length; h++) {
        var sut0 = hepsi[h].querySelector('div[class*="flex-col"]');
        var sag0 = sut0 && sut0.parentElement && sut0.parentElement.children[1];
        var m0 = sag0 ? (sag0.textContent || '').trim() : '';
        if (m0 && !/[0-9]/.test(m0)) { rosterCanliEtiket = m0; break; }
      }
    }
    if (rosterSablon) return rosterSablon;
    var a = kap.querySelector('a[data-testid^="sidebar-following-channel-"]');
    var btn = a && a.parentElement;
    if (btn && btn.tagName === 'BUTTON') {
      rosterSablon = sablonNormalle(btn.cloneNode(true));
      try { chrome.storage.local.set({ pkRosterSablon: rosterSablon.outerHTML }); } catch (e) {}
    }
    return rosterSablon;
  }

  /* Vurguyu kalıptan söker: kalıp hangi satırdan gelirse gelsin nötr olsun. */
  function sablonNormalle(btn) {
    try {
      btn.className = String(btn.className || '')
        .split(/\s+/).filter(function (c) { return c && c !== PK_VURGU_SINIF; }).join(' ');
      var a = btn.querySelector('a');
      if (a) a.setAttribute('data-state', 'false');
    } catch (e) {}
    return btn;
  }

  /* Önbellekteki kalıbı diriltir. İçerik Kick'in kendi işaretlemesi. */
  function sablonYukle(html) {
    if (rosterSablon || !html) return;
    try {
      var d = document.createElement('div');
      d.innerHTML = html;
      var b = d.querySelector('button');
      if (b) rosterSablon = sablonNormalle(b);
    } catch (e) {}
  }

  /* Adres çubuğundaki kanal. Kanal değişince vurgu da taşınmalı. */
  function rosterSuankiSlug() {
    try {
      var s0 = (location.pathname || '').split('/').filter(Boolean)[0] || '';
      return s0.toLowerCase();
    } catch (e) { return ''; }
  }

  function rosterSayiBicim(n) {
    try {
      return new Intl.NumberFormat(undefined, { notation: 'compact', maximumFractionDigits: 1 }).format(n);
    } catch (e) { return String(n); }
  }

  /* Kalıptan bir satır üretir ve verisini yazar. */
  function rosterSatir(k) {
    if (!rosterSablon) return null;
    var btn = rosterSablon.cloneNode(true);
    var a = btn.querySelector('a');
    if (!a) return null;
    btn.setAttribute(PK_SATIR, '1');
    btn.setAttribute(PK_SLUG, k.slug);
    btn.setAttribute(PK_GRUP, k.canli ? 'canli' : 'kapali');
    if (sabitSlugMu(k.slug)) btn.setAttribute(SABIT_IM, '1');
    /* Kick'in kimlik niteliği KALMAMALI: kendi satırımızı Kick'inki sanmayalım. */
    a.removeAttribute('data-testid');
    a.setAttribute('href', '/' + k.slug);
    var eskiPin = btn.querySelectorAll('.pk-sabit-dugme,.pk-sabit-oluk');
    for (var i = 0; i < eskiPin.length; i++) eskiPin[i].remove();
    var img = a.querySelector('img');
    if (img) {
      img.removeAttribute('srcset');
      if (k.avatar) img.setAttribute('src', k.avatar); else img.removeAttribute('src');
      img.alt = k.ad;
    }
    var adSpan = sabitAdOgesi(a);
    if (!adSpan) return null;
    adSpan.textContent = k.ad;
    var sut = adSpan.parentElement;
    var kat = sut && sut.children[1];
    if (kat) kat.textContent = k.canli ? (k.kategori || '') : '';
    /* Sağ blok: canlıda nokta + sayı, çevrimdışında Kick kendi ikonunu
       koyuyor — kalıptan gelen neyse ona dokunmuyoruz, yalnız sayıyı
       yazıyoruz. Sayı gizliyse (show_view_count false) rakam basmıyoruz. */
    var sag = sut && sut.parentElement && sut.parentElement.children[1];
    if (sag && sag.children) {
      /* Noktayı sayıdan ÖNCE al: aşağıda silinince sıra kayıyor. */
      var nokta = sag.children[0];
      var sayi = sag.children[1];
      if (sayi) {
        if (k.canli && k.sayiGoster && k.izleyici > 0) sayi.textContent = rosterSayiBicim(k.izleyici);
        /* Sayı gizliyse Kick "LIVE" yazıyor — boş bırakmıyoruz. */
        else if (k.canli) sayi.textContent = rosterCanliEtiket || 'LIVE';
        else sayi.textContent = '';
      }
      /* ÇEVRİMDIŞI: kalıp CANLI bir satırdan geldiği için yeşil canlı noktası
         da geliyor ve kapalı kanallar canlı gibi görünüyordu. Kick çevrimdışı
         satırda o noktayı hiç koymuyor. */
      if (!k.canli && nokta) nokta.remove();
    }
    /* RAPTİYE. Kendi satırımızda `data-testid` bilerek yok (kendimizi Kick
       sanmayalım), o yüzden Kick satırlarını gezen raptiye döngüsü buraya
       uğramıyor — pinleme kaybolmasının sebebi buydu. Burada koyuyoruz. */
    if (settings.sabitKanal !== false) {
      sabitYerlestir(a, adSpan, rosterAnahtar(k), k.slug, sabitSlugMu(k.slug));
    }
    /* İZLENEN KANAL: Kick vurgusunu yalnız ona koyuyoruz. */
    if (k.slug === rosterSuankiSlug()) {
      btn.className = String(btn.className || '') + ' ' + PK_VURGU_SINIF;
      a.setAttribute('data-state', 'active');
    }
    /* ÖNİZLEME: Kick'in ipucu bizim klon satırlarımızda açılmıyor (React
       ağacının dışındalar), o yüzden kendi kutumuzu biz açıyoruz. */
    try { onizBizimBagla(btn, k); } catch (e) {}
    /* Kendi satırımız: Next'in bağlantı işleyicisi üzerinde yok, düz
       bağlantı tam sayfa yenilerdi. Gezinme sayfa dünyasına devrediliyor. */
    btn.addEventListener('click', function (e) {
      e.preventDefault();
      try { window.postMessage({ source: 'kab-git', n: pkNonce, yol: '/' + k.slug }, '*'); }
      catch (x) { try { location.href = '/' + k.slug; } catch (y) {} }
    });
    return btn;
  }

  /* Listeyi kur. Yalnız DEĞİŞENİ dokunuyoruz: her turda hepsini yeniden
     basmak titreme ve kaydırma sıçraması yapar. */
  function kenarCiz(kap) {
    /* AYAR KAPALIYSA DEVRALMA YOK. Eskiden burada kapı yoktu: kullanıcı
       "çevrimiçi/çevrimdışı ayır"ı kapatsa da liste bizde kalıyor ve
       roster'ın tamamı basılıyordu; Kick'in kademeli listesi ve Önerilen
       Kanallar geri gelmiyordu. */
    if (settings.kenarGrup === false) return false;
    if (!kap || !roster || !roster.length) return false;
    if (!sablonKur(kap)) return false;
    kap.setAttribute(PK_BIZIM, '1');
    var mevcut = Object.create(null);
    var q = kap.querySelectorAll(':scope > [' + PK_SATIR + ']');
    for (var i = 0; i < q.length; i++) mevcut[q[i].getAttribute(PK_SLUG)] = q[i];
    var canliSayi = 0, kapaliSayi = 0;
    for (var j = 0; j < roster.length; j++) {
      var k = roster[j];
      if (k.canli) canliSayi++; else kapaliSayi++;
      var imza = (k.canli ? 1 : 0) + '|' + k.ad + '|' + k.kategori + '|' +
                 (k.sayiGoster ? k.izleyici : -1) + '|' + (sabitSlugMu(k.slug) ? 1 : 0) +
                 '|' + (k.slug === rosterSuankiSlug() ? 1 : 0);
      var eski = mevcut[k.slug];
      if (eski && eski.getAttribute('data-pk-imza') === imza) { delete mevcut[k.slug]; continue; }
      var yeni = rosterSatir(k);
      if (!yeni) continue;
      yeni.setAttribute('data-pk-imza', imza);
      if (eski) { kap.replaceChild(yeni, eski); delete mevcut[k.slug]; }
      else kap.appendChild(yeni);
    }
    /* Listeden düşen kanalların satırlarını kaldır. */
    Object.keys(mevcut).forEach(function (sl) { mevcut[sl].remove(); });
    return { canli: canliSayi, kapali: kapaliSayi };
  }

  /* Raptiye anahtarı: avatar adresindeki sayısal kimlik. Kick'in satırlarında
     da aynı kaynak kullanılıyordu, böylece eski sabitler tutmaya devam eder.
     Adres kimlik taşımıyorsa ada düşülür. */
  function rosterAnahtar(k) {
    try {
      var m = /\/images\/user\/(\d+)\//.exec(String(k.avatar || '').split('?')[0]);
      if (m) return m[1];
    } catch (e) {}
    return 's:' + k.slug;
  }

  /* Sabit mi — roster satırları için (kimlik yok, ada bakıyoruz). */
  function sabitSlugMu(slug) {
    var k = Object.keys(sabitler);
    for (var i = 0; i < k.length; i++) if (sabitler[k[i]] === slug) return true;
    return false;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * ÇEVRİMİÇİ / ÇEVRİMDIŞI BÖLÜMLERİ
   *
   * Kick takip listesini tek yığın hâlinde basıyor: önce canlılar (izleyiciye
   * göre azalan), sonra çevrimdışılar. Biz araya kendi başlıklarımızı koyup
   * çevrimdışı bölümünü kapatıyoruz.
   *
   * SATIRLAR KICK'İN KENDİ SATIRLARI. Taşımıyoruz da, klonlamıyoruz da:
   * React düğümleri geri alır, klon da bayatlar. Yalnız `order` veriyoruz —
   * kural kendi `data-*` niteliğimize bağlı, satır içi stile değil, çünkü
   * Kick satır içi stilleri siliyor ama niteliklerimizi bırakıyor.
   *
   * SIRA: Kick başlığı 0 · bizim "Çevrimiçi" 1 · sabitler 2 · canlılar 3 ·
   * bizim "Çevrimdışı" 4 · çevrimdışılar 5.
   * ══════════════════════════════════════════════════════════════════════ */
  var PK_GRUP     = 'data-pk-grup';
  var PK_GRUPLI   = 'data-pk-grupli';
  var PK_KAPALI_G = 'data-pk-kapali-gizli';
  /* Çevrimdışı sekmesi seçiliyken canlı satırları gizler. */
  var PK_CANLI_G  = 'data-pk-canli-gizli';
  /* Gizlediğimiz KICK düğümleri. `data-pk-gizli` ayar paneli özelliğiyle
     ortak olduğu için ayrı bir işaret kullanıyoruz — toplu geri açma
     onun düğümlerine dokunmasın. */
  var PK_GIZLI_G  = 'data-pk-gizli-grup';
  var kapaliAcik = false;              // çevrimdışı bölümü açık mı

  function kapaliKaydet() {
    try { chrome.storage.local.set({ pkKapaliAcik: !!kapaliAcik }); } catch (e) {}
  }

  /* Başlık: Kick'in KENDİ başlık satırını klonluyoruz ki yazı tipi, yükseklik
     ve iç boşluk birebir aynı olsun — sınıf listesi yazmıyoruz. */
  var PK_PERDE = 'data-pk-perde';
  var SABIT_PERDE_MS = 4000;              // perde en fazla bu kadar durur
  var SABIT_PERDE_SATIR = 8;
  var perdeBasla = 0, perdeBitti = false;

  /* Tek iskelet satır: Kick'in kendi ölçü ve yüzey sınıflarıyla. */
  function perdeSatiri() {
    var r = el('div', 'pk-iskelet flex h-11 w-full flex-row items-center gap-2 rounded px-1.5');
    r.appendChild(el('div', 'size-7 shrink-0 rounded-full bg-surface-highest animate-pulse'));
    var g = el('div', 'flex w-full flex-col gap-1');
    g.appendChild(el('div', 'h-3 w-2/3 rounded bg-surface-highest animate-pulse'));
    g.appendChild(el('div', 'h-2 w-1/3 rounded bg-surface-highest animate-pulse'));
    r.appendChild(g);
    return r;
  }

  function perdeKur(kap, adet) {
    if (kap.getAttribute(PK_PERDE)) return;          // zaten çekili
    kap.setAttribute(PK_PERDE, '1');
    for (var i = 0; i < adet; i++) kap.appendChild(perdeSatiri());
  }

  function perdeKaldir(kap) {
    var q = document.querySelectorAll('.pk-iskelet');
    for (var i = 0; i < q.length; i++) q[i].remove();
    if (kap) kap.removeAttribute(PK_PERDE);
  }

  function sabitPerdeUygula(kap, satirlar) {
    if (!kap) return;
    if (settings.kenarGrup === false) { perdeKaldir(kap); pkKenarHazir(); return; }
    if (perdeBitti) { pkKenarHazir(); return; }
    if (!perdeBasla) perdeBasla = Date.now();
    /* Canlılar listenin başında; çevrimdışı satır göründüyse hepsi geldi. */
    var canliTamam = satirlar.length && !sabitSatirCanli(satirlar[satirlar.length - 1]);
    var listeBitti = !document.querySelector(SABIT_AC_DUGME);
    if (canliTamam || listeBitti || Date.now() - perdeBasla > SABIT_PERDE_MS) {
      perdeBitti = true; perdeKaldir(kap); pkKenarHazir(); return;
    }
    perdeKur(kap, Math.max(satirlar.length, SABIT_PERDE_SATIR));
  }

  function sabitGrupUygula(kap, satirlar, sayim) {
    if (!kap) return;
    if (settings.kenarGrup === false) { grupTemizle(kap); return; }
    /* Kick'in kendi başlığı: kabın kanal olmayan ilk çocuğu. Klon örneği
       ve sıralamada 0 olarak kalacak öğe o. */
    var kickBas = null;
    for (var i = 0; i < kap.children.length; i++) {
      var c = kap.children[i];
      if (c.tagName !== 'BUTTON' && !c.getAttribute('data-pk-grupbas')) { kickBas = c; break; }
    }
    if (!kickBas) return;                     // başlık yoksa dokunma
    var canliSayi = 0, kapaliSayi = 0;
    /* Çizim bizdeyse sayılar roster'dan KESİN geliyor; Kick'in o an kaç
       satır bastığına bakmıyoruz (tırmanan sayacın sebebi buydu). */
    if (sayim) { canliSayi = sayim.canli; kapaliSayi = sayim.kapali; }
    for (var j = 0; !sayim && j < satirlar.length; j++) {
      var a = satirlar[j];
      var btn = a.parentElement;
      if (!btn || btn.tagName !== 'BUTTON') continue;
      var tur = sabitSatirCanli(a) ? 'canli' : 'kapali';
      if (btn.getAttribute(PK_GRUP) !== tur) btn.setAttribute(PK_GRUP, tur);
      if (tur === 'canli') canliSayi++; else kapaliSayi++;
    }
    /* Kick'in kendi başlığı gizleniyor: bölüm adları artık bizim iki
       başlığımız. Düğüm SİLİNMİYOR — teardown `data-pk-gizli` taşıyanları
       geri açıyor, ayrıca klon örneği olarak lazım. */
    if (!kickBas.getAttribute(PK_GIZLI_G)) {
      kickBas.setAttribute(PK_GIZLI_G, '1');
      kickBas.style.setProperty('display', 'none', 'important');
    }
    /* YÜKLEME YER TUTUCULARI. Kick sıradaki grubu getirirken içi boş satırlar
       basıyor: kanal bağlantısı yok, metni yok. Gruplanmadıkları için sıraları
       varsayılan 0'da kalıyor ve başlığın ÜSTÜNE düşüyorlardı. Metin şartı
       önemli: Kick'in kendi başlığında metin var, onu yer tutucu sanmayalım. */
    for (var y = 0; y < kap.children.length; y++) {
      var c2 = kap.children[y];
      if (c2.getAttribute('data-pk-grupbas') || c2 === kickBas) continue;
      /* KENDİ perde iskeletlerimiz: onlarda da bağlantı ve metin yok,
         aynı kurala takılıp gizleniyorlardı. */
      if ((c2.className || '').indexOf('pk-iskelet') >= 0) continue;
      var g2 = c2.getAttribute(PK_GRUP);
      if (g2 === 'canli' || g2 === 'kapali') continue;        // bu turda gerçek satır sayıldı
      /* `bos` işareti HER TURDA yeniden değerlendirilir: React düğümleri
         yeniden kullanıyor, yer tutucu gerçek satıra dönüşebiliyor. Bir kez
         işaretleyip bir daha bakmasaydık o satır kalıcı görünmez kalırdı. */
      var bosMu = !c2.querySelector('a[href^="/"]') && !(c2.textContent || '').trim();
      if (bosMu) { if (g2 !== 'bos') c2.setAttribute(PK_GRUP, 'bos'); }
      else if (g2 === 'bos') c2.removeAttribute(PK_GRUP);
    }
    kap.setAttribute(PK_GRUPLI, '1');
    /* Tek sekme görünür: seçili olmayan grubun satırları gizlenir. */
    if (kapaliAcik) { kap.removeAttribute(PK_KAPALI_G); kap.setAttribute(PK_CANLI_G, '1'); }
    else { kap.setAttribute(PK_KAPALI_G, '1'); kap.removeAttribute(PK_CANLI_G); }

    /* Başlıklar: eksikse kur, metni güncel tut. React kabı yeniden çizince
       düşebiliyorlar, her turda bakıyoruz. */
    /* Liste sonuna gelindiyse Kick "daha fazla" düğmesini kaldırıyor.
       Gelinmediyse çevrimdışı sayısı EKSİK — sonuna `+` koyuyoruz. */
    /* Roster'dan geliyorsa sayı kesin; Kick'in listesinden geliyorsa
       açılma bitmemiş olabilir, o zaman `+` ile eksikliği belli ediyoruz. */
    var tamam = !!sayim || !document.querySelector(SABIT_AC_DUGME);
    kenarSekmeYaz(kap, canliSayi, kapaliSayi, tamam);

    /* Kick'in "daha fazla/az" kutusu artık anlamsız: listeyi biz açıyoruz.
       Kutuyu DÜĞMELERİNDEN buluyoruz. Eskiden kabın bir sonraki kardeşi
       alınıyordu; liste tamamen açılınca Kick o kutuyu kaldırıyor ve sıradaki
       kardeş "Önerilen Kanallar" bölümü oluyordu — canlıda onu gizledik. */
    var dug = document.querySelector('[data-testid="sidebar-show-more-following"],' +
                                     '[data-testid="sidebar-show-less-following"]');
    var kutu = dug && dug.parentElement;
    if (kutu && !kutu.getAttribute(PK_GIZLI_G)) {
      kutu.setAttribute(PK_GIZLI_G, '1');
      kutu.style.setProperty('display', 'none', 'important');
    }
  }

  /* Daralmış kenar çubuğu (yalnız ikon modu) için sekme ikonları.
     Dolu daire = çevrimiçi, boş daire = çevrimdışı — Kick'in kendi nokta
     dilinin aynısı, 56px'lik sütunda da okunuyor. */
  var I_KENAR_CANLI = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:15px;height:15px;display:block">' +
    '<circle cx="12" cy="12" r="6"/></svg>';
  var I_KENAR_KAPALI = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" style="width:15px;height:15px;display:block">' +
    '<circle cx="12" cy="12" r="6"/></svg>';

  /* Kenar çubuğu daralmış mı. SABİT PİKSEL DEĞİL, KABIN KENDİ GENİŞLİĞİ:
     Kick daralmış modda ~56px, açıkken 240px+ veriyor. Ölçüye bakmak
     Kick'in sınıf adlarına bakmaktan dayanıklı — sınıflar sürekli değişiyor. */
  function kenarDarMi(kap) {
    try { return !!kap && kap.getBoundingClientRect().width < 120; } catch (e) { return false; }
  }

  /* Sohbetteki filtre düğmesinin aynısı: tek satırda iki segment.
     `data-pk-grupbas` işareti KORUNUYOR — sıralama, gizleme ve söküm
     kuralları o işarete bağlı. */
  function kenarSekmeYaz(kap, canliSayi, kapaliSayi, tamam) {
    var kutu = kap.querySelector(':scope > [data-pk-grupbas="sekme"]');
    if (!kutu) {
      kutu = el('div', 'flex gap-1');
      kutu.setAttribute('data-pk-grupbas', 'sekme');
      kutu.style.cssText = 'padding:0 0 8px';
      kap.appendChild(kutu);
    }
    /* Her turda yeniden çizilirse tıklanmak üzere olan düğme altından kayar.
       DAR DURUMU DA İMZADA: kullanıcı kenar çubuğunu daraltıp açtığında
       sekmelerin biçim değiştirmesi gerekiyor, yoksa eski çizim kalıyordu. */
    var dar = kenarDarMi(kap);
    var imza = (kapaliAcik ? 'k' : 'c') + '|' + canliSayi + '|' + kapaliSayi + (tamam ? '' : '+') + (dar ? '|d' : '');
    if (kutu.getAttribute('data-pk-imza') === imza) return;
    kutu.setAttribute('data-pk-imza', imza);
    kutu.textContent = '';
    [['canli',  t('pkGrupCanli',  [String(canliSayi)]),  I_KENAR_CANLI],
     ['kapali', t('pkGrupKapali', [String(kapaliSayi) + (tamam ? '' : '+')]), I_KENAR_KAPALI]
    ].forEach(function (par) {
      var secili = (par[0] === 'kapali') === !!kapaliAcik;
      var d = el('button', 'flex-1 min-w-0 h-8 px-1 rounded text-sm font-semibold transition-all ' +
        'flex items-center justify-center whitespace-nowrap ' +
        (secili ? 'bg-secondary-base text-white' : 'text-subtle betterhover:hover:bg-surface-highest'),
        dar ? '' : par[1]);
      /* DAR: metin sığmıyor ve "erimiçi (9" gibi kırpılıyordu. İkon + ipucu.
         innerHTML yalnız KENDİ sabit ikon metnimizle çağrılıyor. */
      if (dar) {
        d.innerHTML = par[2];
        d.title = par[1];
        d.setAttribute('aria-label', par[1]);
        var sv = d.querySelector('svg');
        if (sv && par[0] === 'canli' && secili) sv.style.color = '#53fc18';
      }
      d.addEventListener('click', function (e) {
        e.preventDefault(); e.stopPropagation();
        kapaliAcik = (par[0] === 'kapali');
        kapaliKaydet();
        sabitKanalUygula();
      });
      kutu.appendChild(d);
    });
  }

  function grupTemizle(kap) {
    perdeKaldir(kap);
    pkKenarHazir();
    /* BİZİM ÇİZDİĞİMİZ SATIRLARI DA TOPLA. Kick'in kendi satırlarını gizleyen
       kural `PK_BIZIM` işaretine bağlı; işaret kalırsa Kick'in listesi gizli
       kalır ve ekranda donmuş bizim listemiz görünür. */
    var bizimSatir = document.querySelectorAll('[' + PK_SATIR + ']');
    for (var b0 = 0; b0 < bizimSatir.length; b0++) bizimSatir[b0].remove();
    var bizimKap = document.querySelectorAll('[' + PK_BIZIM + ']');
    for (var b1 = 0; b1 < bizimKap.length; b1++) bizimKap[b1].removeAttribute(PK_BIZIM);
    var b = document.querySelectorAll('[data-pk-grupbas]');
    for (var i = 0; i < b.length; i++) b[i].remove();
    var g = document.querySelectorAll('[' + PK_GRUP + ']');
    for (var j = 0; j < g.length; j++) g[j].removeAttribute(PK_GRUP);
    if (kap) { kap.removeAttribute(PK_GRUPLI); kap.removeAttribute(PK_KAPALI_G); kap.removeAttribute(PK_CANLI_G); }
    var k = document.querySelectorAll('[' + PK_GIZLI_G + ']');
    for (var q = 0; q < k.length; q++) {
      k[q].style.removeProperty('display'); k[q].removeAttribute(PK_GIZLI_G);
    }
  }

  /* Raptiyeyi doğru yere koyar. Yerindeyse DOKUNMAZ (false döner): her turda
     silip yeniden basmak titreme yapar. */
  function sabitYerlestir(a, adSpan, anahtar, slug, sabitMi) {
    var istenen = sabitMi ? 'oluk' : 'dugme';
    var mevcut = a.querySelectorAll('.pk-sabit-dugme,.pk-sabit-oluk');
    var tut = null, fazla = [];
    for (var i = 0; i < mevcut.length; i++) {
      var tur = String(mevcut[i].className || '').indexOf('pk-sabit-oluk') >= 0 ? 'oluk' : 'dugme';
      if (!tut && tur === istenen) tut = mevcut[i]; else fazla.push(mevcut[i]);
    }
    for (var j = 0; j < fazla.length; j++) fazla[j].remove();
    if (tut) return false;                       // doğru yerde, dokunma
    if (sabitMi) {
      /* Avatarın SOLUNA: bağlantının ilk çocuğu. */
      a.insertBefore(sabitDugmesi(anahtar, slug, true, 'pk-sabit-oluk'), a.firstChild);
    } else if (adSpan) {
      adSpan.appendChild(sabitDugmesi(anahtar, slug, false, 'pk-sabit-dugme'));
    }
    return true;
  }

  function sabitKanalUygula() {
    /* PERDEYİ İNDİRMEDEN ÇIKMA. Bu iki erken çıkış `pkKenarHazir()`
       çağırmıyordu: özellik kapatılınca perde kimse tarafından
       kaldırılmıyor, kenar çubuğu PK_ERKEN_TAVAN dolana kadar boş kalıyordu.
       Kullanıcının bildirdiği "kapatınca geç yükleniyor" hatası buydu. */
    if (!settings.consented) { pkKenarHazir(); return; }
    var satirlar = document.querySelectorAll('a[data-testid^="sidebar-following-channel-"]');
    if (settings.sabitKanal === false) { if (satirlar.length) sabitTemizle(); pkKenarHazir(); return; }
    /* Liste henüz boyanmadı: perde görevini yapıyor, tavan zaten sınırlıyor. */
    if (!satirlar.length) return;
    sabitStilKur();
    var degisti = false, basilan = [], kap = null;
    for (var i = 0; i < satirlar.length; i++) {
      var a = satirlar[i];
      var btn = a.parentElement;
      if (!btn || btn.tagName !== 'BUTTON') continue;      // Kick yapıyı değiştirmiş → dokunma
      if (btn.parentElement) {
        kap = btn.parentElement;
        if (!kap.getAttribute(SABIT_LISTE)) kap.setAttribute(SABIT_LISTE, '1');
      }
      var anahtar = sabitAnahtar(a);
      if (!anahtar) continue;
      var slug = chanSlug(a);
      if (slug) basilan.push(slug);              // vekil gerekip gerekmediğini bu belirliyor
      var sabitMi = sabitler[anahtar] !== undefined;
      /* AD DEĞİŞİMİ: kimlik aynı ama ad farklıysa kaydı sessizce güncelle. */
      if (sabitMi && slug && sabitler[anahtar] !== slug) { sabitler[anahtar] = slug; degisti = true; }
      /* İşaret: sıralamayı bu sürüyor. */
      if (sabitMi && !btn.getAttribute(SABIT_IM)) btn.setAttribute(SABIT_IM, '1');
      else if (!sabitMi && btn.getAttribute(SABIT_IM)) btn.removeAttribute(SABIT_IM);
      /* Düğme: React satırı yeniden çizince düşüyor — canlıda ölçüldü,
         Kick izleyici sayısı değiştikçe listeyi yeniden kuruyor. Her turda
         bakıp eksikse geri koyuyoruz; işaretler de aynı turda yenileniyor. */
      var adSpan = sabitAdOgesi(a);
      if (!adSpan) continue;
      /* Satırdaki BÜTÜN raptiyeleri gözden geçirir: doğru yerde duran varsa
         tutar, kalan her şeyi atar. Önceki bağlamdan kalan düğümler birikip
         satırı bozuyordu (canlıda 35 satıra 92 raptiye). */
      sabitYerlestir(a, adSpan, anahtar, slug, sabitMi);
    }
    if (degisti) sabitKaydet();
    /* ÇİZİM BİZDEYSE Kick'in kademeli listesine hiç dokunmuyoruz: sayım da
       sıralama da roster'dan geliyor, tek geçişte. */
    var ciz = kenarCiz(kap);
    if (ciz) {
      perdeBitti = true; perdeKaldir(kap); pkKenarHazir();
      sabitGrupUygula(kap, satirlar, ciz);
      return;
    }
    /* Roster yok (çıkış yapılmış / ağ hatası): eski yola düş. */
    sabitListeAc(kap, satirlar);
    sabitPerdeUygula(kap, satirlar);
    sabitGrupUygula(kap, satirlar);
  }

  function sabitDugmesi(anahtar, slug, sabitMi, sinif) {
    var b = el('span', sinif || 'pk-sabit-dugme');
    b.setAttribute('data-pk-durum', sabitMi ? '1' : '0');
    b.setAttribute('title', t(sabitMi ? 'pkSabitKaldir' : 'pkSabitle'));
    b.appendChild(sabitIkonu(sabitMi));
    /* Satır bir bağlantı: tıklama yayılırsa kanala gideriz. Hem `click` hem
       `mousedown` durduruluyor — Kick gezinmeyi mousedown'da başlatıyor. */
    function dur(e) { e.preventDefault(); e.stopPropagation(); }
    b.addEventListener('mousedown', dur);
    b.addEventListener('click', function (e) {
      dur(e);
      sabitDegis(anahtar, slug);
    });
    return b;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — KATEGORİ GİZLEME
   * Kanal gizlemenin aynısı, tek fark bağlantı deseni: /category/<ad>.
   * Gizlenen kategori hem kartlarda hem gözat listelerinde kaybolur; o
   * kategorideki yayın kartları da (kart içinde kategori linki taşır) gider.
   * ══════════════════════════════════════════════════════════════════════ */
  var gizliKat = {};                // kategori adı (küçük harf) -> 1

  function katSlug(a) {
    var h = a.getAttribute('href') || '';
    var m = h.match(/^\/category\/([^/?#]+)/i);
    return m ? decodeURIComponent(m[1]).toLowerCase() : '';
  }
  function katKaydet() {
    try { chrome.storage.local.set({ pkGizliKat: Object.keys(gizliKat) }); } catch (e) {}
  }
  function katGizle(ad) {
    ad = String(ad || '').toLowerCase(); if (!ad || gizliKat[ad]) return;
    gizliKat[ad] = 1; katKaydet(); katUygula();
  }
  function katGoster(ad) {
    ad = String(ad || '').toLowerCase(); if (!ad || !gizliKat[ad]) return;
    delete gizliKat[ad]; katKaydet();
    var els = document.querySelectorAll('[data-pk-kathidden="' + ad + '"]');
    for (var i = 0; i < els.length; i++) { els[i].style.removeProperty('display'); els[i].removeAttribute('data-pk-kathidden'); }
  }
  function katUygula() {
    if (!settings.consented || settings.kategoriGizle === false) return;
    var links = document.querySelectorAll('a[href^="/category/"]');
    for (var i = 0; i < links.length; i++) {
      var a = links[i];
      try {
        var ad = katSlug(a); if (!ad || !gizliKat[ad]) continue;
        if (inSkipZone(a)) continue;
        /* Kanal gizlemedeki kap bulucuyu kullanıyoruz: birden çok linki olan
           kartın tam sınırını buluyor, yalnız etiketi değil kartı gizliyor. */
        var box = blockContainer(a) || a;
        if (box && box.style.display !== 'none') {
          box.style.setProperty('display', 'none', 'important');
          box.setAttribute('data-pk-kathidden', ad);
        }
      } catch (e) {}
    }
  }

  /* Kategori sayfasındayken başlığın yanına "Gizle" düğmesi. */
  function katSayfaSlug() {
    var m = location.pathname.match(/^\/category\/([^/?#]+)/i);
    return m ? decodeURIComponent(m[1]).toLowerCase() : '';
  }
  function katButonuKur() {
    if (!settings.consented || settings.kategoriGizle === false) return;
    var ad = katSayfaSlug();
    // `t` DEĞİL: var yükseldiği için aşağıdaki t('...') çağrıları bozuluyordu.
    if (!ad) { var es = document.querySelectorAll('[data-pk-kat]'); for (var q = 0; q < es.length; q++) es[q].remove(); return; }
    var h1 = null, hs = document.querySelectorAll('h1, h2');
    for (var i = 0; i < hs.length; i++) {
      var r = hs[i].getBoundingClientRect();
      if (r.width > 0 && r.height > 0 && r.top < 400) { h1 = hs[i]; break; }
    }
    if (!h1 || !h1.parentElement) return;
    var bar = h1.parentElement;
    var gizliMi = !!gizliKat[ad];
    var mevcut = bar.querySelector(':scope > [data-pk-kat]');
    if (mevcut) {
      if (mevcut.getAttribute('data-pk-kat') === (gizliMi ? '1' : '0')) return;
      mevcut.remove();
    }
    var b = el('button', BTN_BASE + ' h-8 px-3 text-sm ' +
      (gizliMi ? 'bg-primary-base text-black' : 'bg-secondary-base text-white hover:brightness-125'));
    b.setAttribute('data-pk-kat', gizliMi ? '1' : '0');
    b.type = 'button';
    b.title = gizliMi ? t('pkKategoriGoster') : 'Bu kategoriyi listelerde gizle';
    var ik = el('span', 'inline-flex shrink-0'); ik.innerHTML = I_BLOCK;
    b.appendChild(ik);
    b.appendChild(document.createTextNode(gizliMi ? t('pkGizlemeyiKaldir') : 'Kategoriyi Gizle'));
    b.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (gizliKat[ad]) { katGoster(ad); toast(t('pkTKategoriGorunur')); }
      else { katGizle(ad); toast(t('pkTKategoriGizlendi')); }
      katButonuKur();
    });
    bar.appendChild(b);
  }

  function saveBlocked() {
    try { chrome.storage.local.set({ blockedChannels: Object.keys(blocked) }); } catch (e) {}
  }
  function blockChannel(slug) {
    if (!slug || blocked[slug]) return;
    blocked[slug] = 1; saveBlocked(); applyChannelBlocks();
    toast(slug + ' gizlendi', { ad: 'Geri al', fn: function () { unblockChannel(slug); } });
  }
  function unblockChannel(slug) {
    if (!blocked[slug]) return;
    delete blocked[slug]; saveBlocked(); unhideChannel(slug);
    toast(t('pkTKanalEngelKalkti', [slug]));
  }

  /* ══════════════════════════════════════════════════════════════════════
   * BİLDİRİM (toast) — Kick'in KENDİ bildirimi canlı ölçülerek birebir taklit
   * edildi ("Profil bilgileriniz güncellendi!" bildirimi yakalanıp incelendi).
   *
   * ÖLÇÜLEN DEĞERLER (tahmin değil — hepsi getComputedStyle çıktısı):
   *   kap   : fixed · bottom 24px · left 50% + translateX(-50%) · width 356px
   *           · bildirimler arası boşluk 14px · z-index 999999999
   *   kart  : 356×56 · zemin rgb(35,38,41) · köşe 4px · padding 16px
   *           · KENARLIK YOK · iç boşluk 6px · gölge 0 4px 12px rgba(0,0,0,.1)
   *   yazı  : 14px/500 (text-sm font-medium) · renk #fcfcfc (beyaz DEĞİL)
   *   ikon  : 24px (size-6) · viewBox 0 0 32 32 · ikon kabı marj 0 4px 0 -3px
   *
   * Kabı kendimiz kuruyoruz çünkü Kick'inki (`[data-sonner-toaster]`) sayfada
   * hazır durmuyor, ancak Kick kendi bildirimini gösterdiği an kuruluyor.
   * Zemin/vurgu için Kick sınıfları kullanılır → tema rengini kendiliğinden
   * takip eder. Ölçüler bizim, çizim bizim; birebir görünür.
   * ══════════════════════════════════════════════════════════════════════ */
  var toastKap = null;
  var I_OK = '<svg viewBox="0 0 32 32" fill="currentColor" style="width:24px;height:24px;display:block">' +
             '<path fill-rule="evenodd" d="M16 30C23.7 30 30 23.7 30 16S23.7 2 16 2 2 8.3 2 16s6.3 14 14 14zM6.6 16.3l6.9 6.9 11.2-11.2-2.8-2.8-8.4 8.4-4.1-4.1-2.8 2.8z"/></svg>';

  function toastKur() {
    if (toastKap && document.contains(toastKap)) return;
    toastKap = el('div', '');
    toastKap.style.cssText = 'position:fixed;left:50%;bottom:24px;transform:translateX(-50%);' +
      'width:356px;max-width:calc(100vw - 32px);z-index:999999999;' +
      'display:flex;flex-direction:column;gap:14px;pointer-events:none';
    pkKatmanaEkle(toastKap);
  }

  /* mesaj = gösterilecek yazı · eylem = {ad, fn} (isteğe bağlı "Geri al" gibi) */
  /* ══════════════════════════════════════════════════════════════════════
   * TANITIM — ilk kurulumda bir kez
   *
   * Sayfa sayfa geziliyor: sağ altta İleri, sol altta Geri, sağ üstte Kapat.
   * DIŞARI TIKLAMA KAPATMIYOR (kullanıcı kararı): tanıtım tek sefer
   * gösteriliyor, yanlışlıkla kapanınca bir daha görünmezdi.
   *
   * İÇERİK PANELDEN GELİYOR. Her madde, ayar panelindeki başlık/açıklama
   * çiftinin anahtarını kullanıyor (`pkX` + `pkXD`) — burada sadece anahtar
   * adı yazılı. Kazancı: yeni bir özellik eklerken çeviri yapılmıyor (zaten
   * panelde var) ve tanıtım ile panel hiçbir zaman ayrışmıyor. 46 anahtarlık
   * ikinci bir sözlük yazmak, ilk güncellemede eskiyecek bir kopya olurdu.
   *
   * SÜRÜMLÜ. Her sayfanın eklendiği tanıtım sürümü var; kullanıcı gördüğü en
   * son sürümü saklıyor. Sonraki güncellemede YALNIZ YENİ sayfalar açılıyor,
   * tanıtımın tamamı baştan gösterilmiyor — "sonraki güncellemelerde eklenen
   * özelliği kolayca tanıtalım" isteği tam olarak bu.
   * ══════════════════════════════════════════════════════════════════════ */
  /* YEŞİL ÇERÇEVE — "buraya bak". Tanıtım kapandıktan sonra kullanıcı
     ayarların nerede olduğunu bilmiyor; iki PK düğmesi de (sohbet şeridi ve
     üst çubuk) bir kez çerçeveleniyor. İLK TIKLAMADA kalkıyor ve bir daha
     gelmiyor: amacı yol göstermek, süs değil. */
  var PK_ISARET_IM = 'data-pk-isaret';
  var pkUstIsaret = null;              // üst çubuk düğmesinin kendi tazeleyicisi
  var pkIsaretAcik = false;            // depodan; tanıtım görülünce açılıyor
  function pkIsaretUygula(b) {
    if (!b) return;
    if (!pkIsaretAcik) {
      if (b.getAttribute(PK_ISARET_IM)) {
        b.removeAttribute(PK_ISARET_IM);
        b.style.removeProperty('box-shadow');
      }
      return;
    }
    if (b.getAttribute(PK_ISARET_IM)) return;
    b.setAttribute(PK_ISARET_IM, '1');
    /* `box-shadow` KULLANILIYOR, `border` DEĞİL: kenarlık düğmenin ölçüsünü
       büyütüp şeritteki hizayı bozuyordu. */
    b.style.setProperty('box-shadow', '0 0 0 2px rgb(83,252,24)');
    b.style.setProperty('border-radius', '4px');
  }
  function pkIsaretKapat() {
    if (!pkIsaretAcik) return;
    pkIsaretAcik = false;
    try { chrome.storage.local.set({ pkIsaret: 0 }); } catch (e) {}
    if (pkUstIsaret) { try { pkUstIsaret(); } catch (e) {} }
    var l = document.querySelectorAll('[' + PK_ISARET_IM + ']');
    for (var i = 0; i < l.length; i++) {
      l[i].removeAttribute(PK_ISARET_IM);
      l[i].style.removeProperty('box-shadow');
    }
  }

  var PK_TANITIM_ID = 'pk-tanitim';
  var PK_TANITIM_SURUM = 1;            // YENİ SAYFA EKLERKEN ARTIR
  var pkTanitimGorulen = -1;           // -1 = depo daha okunmadı

  /* `s` = sayfanın eklendiği tanıtım sürümü. `m` = panel anahtarları. */
  var PK_TANITIM_SAYFA = [
    { s: 1, ad: 'pkTanHos',    giris: 'pkTanHosD',    m: [] },
    { s: 1, ad: 'pkTanReklam', giris: 'pkTanReklamD', m: ['pkVideoReklamlari', 'pkVodReklamlari', 'pkSayfaReklamlari'] },
    { s: 1, ad: 'pkTanSohbet', giris: 'pkTanSohbetD', m: ['pkOncelikliKisiler', 'pkKisiGizleme', 'pkKelimeVurgu', 'pkBahsedilmeler', 'pkSekmeIci'] },
    { s: 1, ad: 'pkTanListe',  giris: 'pkTanListeD',  m: ['pkScSayac', 'pkScEtiket', 'pkScFiltre', 'pkYanSohbet', 'pkSohbetIstatistikleri'] },
    { s: 1, ad: 'pkTanKart',   giris: 'pkTanKartD',   m: ['pkKatilmaTakip', 'pkKartTakipci', 'pkKartGenis', 'pkKartMesajlar', 'pkKullaniciNotlari'] },
    { s: 1, ad: 'pkTanMod',    giris: 'pkTanModD',    m: ['pkModIzle', 'pkModSerit', 'pkModSohbette', 'pkSilinenGoster', 'pkAiMod', 'pkBanDetay'] },
    { s: 1, ad: 'pkTanArac',   giris: 'pkTanAracD',   m: ['pkHizliMod', 'pkCrossAyar', 'pkToAyar', 'pkSilAyar', 'pkOmAyar'] },
    { s: 1, ad: 'pkTanVideo',  giris: 'pkTanVideoD',  m: ['pkUyarHiz', 'pkVideoIstatistikleri', 'pkTamEkranSon', 'pkEkranGoruntusu', 'pkOtomatikKalite'] },
    { s: 1, ad: 'pkTanKenar',  giris: 'pkTanKenarD',  m: ['pkSabitKanallar', 'pkKenarGrup', 'pkOnizlemeKarti', 'pkHakkindaEk'] },
    { s: 1, ad: 'pkTanSon',    giris: 'pkTanSonD',    m: [] }
  ];

  function pkTanitimKapat() {
    var p = document.getElementById(PK_TANITIM_ID);
    if (p) p.remove();
  }
  function pkTanitimGorulduYaz(surum) {
    pkTanitimGorulen = surum;
    /* Tanıtım bitince yeşil çerçeve açılıyor: "ayarlar şurada" demenin
       yeri tam burası. İlk tıklamada sönüyor. */
    pkIsaretAcik = true;
    try { chrome.storage.local.set({ pkTanitim: surum, pkIsaret: 1 }); } catch (e) {}
    if (pkUstIsaret) { try { pkUstIsaret(); } catch (e) {} }
  }

  /* `hepsi` true ise sürüm bakılmadan bütün sayfalar (panelden yeniden
     gösterme). Değilse yalnız kullanıcının görmediği sayfalar. */
  function pkTanitimAc(hepsi) {
    if (document.getElementById(PK_TANITIM_ID)) return;
    var sayfalar = PK_TANITIM_SAYFA.filter(function (x) {
      return hepsi || x.s > pkTanitimGorulen;
    });
    if (!sayfalar.length) return;
    var i = 0;

    var perde = el('div', '');
    perde.id = PK_TANITIM_ID;
    /* Bu perde KARARTIYOR ve tıklamayı YUTUYOR ama kapatmıyor: karar
       bekleniyor, arkasıyla iş yapılamaz, yanlışlıkla da kapanmıyor. */
    perde.style.cssText = 'position:fixed;inset:0;z-index:2147483220;background:rgba(0,0,0,.66);' +
      'display:flex;align-items:center;justify-content:center;padding:16px';
    perde.addEventListener('click', function (e) { e.stopPropagation(); });

    var kk = el('div', 'bg-surface-base flex h-fit flex-col rounded text-sm shadow-lg gap-3 p-4');
    kk.style.cssText = 'width:560px;max-width:100%;max-height:88vh;overflow-y:auto;color:#fff;' +
      'border:1px solid rgba(255,255,255,.10)';

    /* ---- başlık + kapat ---- */
    var bas = el('div', 'flex items-start justify-between gap-2');
    var baslikY = el('div', 'min-w-0 text-base font-bold text-white', '');
    bas.appendChild(baslikY);
    var kapatB = el('button', 'shrink-0 inline-flex items-center justify-center rounded state-layer-surface text-white');
    kapatB.style.cssText = 'width:32px;height:32px;padding:0;background:none;border:0;cursor:pointer';
    kapatB.innerHTML = I_KAPAT;
    kapatB.title = t('pkKapat');
    kapatB.setAttribute('aria-label', t('pkKapat'));
    kapatB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      /* KAPATMAK DA GÖRÜLDÜ SAYILIYOR: kullanıcı "gördüm, yeter" diyor;
         her açılışta yeniden çıkarsak tanıtım rahatsızlığa dönüşür. */
      pkTanitimGorulduYaz(PK_TANITIM_SURUM);
      pkTanitimKapat();
    });
    bas.appendChild(kapatB);
    kk.appendChild(bas);

    /* GİRİŞ METNİ `text-sm`: `text-xs` idi, maddesiz sayfalarda (ilk ve son)
       koca kartın içinde kaybolmuş görünüyordu. Maddesi olmayan sayfada bir
       tık daha büyüyor — orada okunacak tek şey o. */
    var girisY = el('div', 'text-subtle', '');
    kk.appendChild(girisY);
    var listeY = el('div', 'flex flex-col gap-2');
    kk.appendChild(listeY);

    kk.appendChild(el('div', 'h-px shrink-0 bg-outline-decorative w-full'));

    /* ---- alt şerit: Geri · noktalar · İleri ---- */
    var alt = el('div', 'flex items-center justify-between gap-3');
    var geriB = btnIkincil(t('pkGeri'));
    geriB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (i > 0) { i--; ciz(); }
    });
    alt.appendChild(geriB);

    var noktaY = el('div', 'flex items-center gap-1');
    alt.appendChild(noktaY);

    var ileriB = btnBirincil(t('pkIleri'));
    ileriB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (i < sayfalar.length - 1) { i++; ciz(); return; }
      pkTanitimGorulduYaz(PK_TANITIM_SURUM);
      pkTanitimKapat();
    });
    alt.appendChild(ileriB);
    kk.appendChild(alt);

    function ciz() {
      var p = sayfalar[i];
      baslikY.textContent = t(p.ad);
      girisY.textContent = p.giris ? t(p.giris) : '';
      girisY.style.display = p.giris ? '' : 'none';
      var yalniz = !(p.m && p.m.length);
      girisY.className = yalniz ? 'text-base text-white' : 'text-sm text-subtle';
      girisY.style.lineHeight = yalniz ? '1.55' : '1.5';
      girisY.style.padding = yalniz ? '6px 0 10px' : '0';

      listeY.textContent = '';
      (p.m || []).forEach(function (anahtar) {
        var sat = el('div', 'bg-surface-lower rounded flex flex-col gap-0.5');
        sat.style.cssText = 'padding:8px 10px';
        sat.appendChild(el('div', 'text-sm font-semibold text-white', t(anahtar)));
        sat.appendChild(el('div', 'text-xs text-subtle', t(anahtar + 'D')));
        listeY.appendChild(sat);
      });

      /* İlk sayfada geri gidilecek yer yok — düğme duruyor ama sönük, yer
         değiştirmesin: her sayfada düğmeler aynı yerde kalsın. */
      geriB.disabled = i === 0;
      geriB.style.opacity = i === 0 ? '.35' : '';
      geriB.style.cursor = i === 0 ? 'default' : 'pointer';
      ileriB.textContent = (i === sayfalar.length - 1) ? t('pkTanBitir') : t('pkIleri');

      noktaY.textContent = '';
      for (var q = 0; q < sayfalar.length; q++) {
        var n = el('span', '');
        n.style.cssText = 'width:6px;height:6px;border-radius:50%;background:' +
          (q === i ? 'rgb(83,252,24)' : 'rgba(255,255,255,.25)');
        noktaY.appendChild(n);
      }
      try { kk.scrollTop = 0; } catch (e) {}
    }
    ciz();

    perde.appendChild(kk);
    pkKatmanaEkle(perde);
  }

  /* İlk açılışta gösterim. BOT PENCERESİNDE GÖSTERİLMİYOR: orası araç
     penceresi, tanıtım orada yolu kapatırdı. */
  function pkTanitimBak() {
    if (!settings.consented) return;
    if (omPopoutMu()) return;
    if (pkTanitimGorulen < 0) return;              // depo daha okunmadı
    if (pkTanitimGorulen >= PK_TANITIM_SURUM) return;
    try { pkTanitimAc(false); } catch (e) {}
  }

  function toast(mesaj, eylem) {
    toastKur();
    while (toastKap.children.length >= 3) toastKap.removeChild(toastKap.firstChild);   // en fazla 3 bildirim
    var t = el('div', 'bg-surface-highest rounded');
    /* min-height: uzun kanal adlarında kırpılmasın diye sabit 56 yerine en-az 56 */
    t.style.cssText += ';pointer-events:auto;display:flex;align-items:center;gap:6px;padding:16px;' +
      'width:100%;min-height:56px;box-shadow:0 4px 12px rgba(0,0,0,.1);color:#fcfcfc;' +
      'opacity:0;transform:translateY(10px);transition:opacity .18s ease,transform .18s ease';
    /* ikon · yazı · eylem — hepsi BEYAZ (tema rengi kullanılmaz) */
    var ik = el('span', 'inline-flex shrink-0');
    ik.style.cssText = 'margin:0 4px 0 -3px;flex:0 0 auto;color:#fcfcfc';
    ik.innerHTML = I_OK;
    t.appendChild(ik);
    /* Kick yazıyı kalan alanda ORTALIYOR (ölçüldü: flex 1 1 auto · text-align center) */
    var yz = el('span', 'text-sm font-medium', mesaj);
    yz.style.cssText = 'color:#fcfcfc;flex:1 1 auto;text-align:center';
    t.appendChild(yz);
    if (eylem && eylem.ad) {
      var a = el('button', 'text-sm font-semibold shrink-0');
      a.style.cssText = 'background:none;border:0;cursor:pointer;padding:0;margin-left:auto;color:#fcfcfc';
      a.textContent = eylem.ad;
      a.addEventListener('click', function () { try { eylem.fn(); } catch (e) {} kapat(); });
      t.appendChild(a);
    }
    toastKap.appendChild(t);
    requestAnimationFrame(function () { t.style.opacity = '1'; t.style.transform = 'translateY(0)'; });
    var zt = setTimeout(kapat, eylem ? 6000 : 3500);      // eylemli bildirim daha uzun durur
    function kapat() {
      clearTimeout(zt);
      t.style.opacity = '0'; t.style.transform = 'translateY(10px)';
      setTimeout(function () { if (t.parentNode) t.parentNode.removeChild(t); }, 200);
    }
    return t;
  }

  /* ---- Kartın üstüne gelince çıkan "engelle" butonu ----
   * Buton KARTIN İÇİNE demirlenir (`position:absolute`, kartın sağ üst köşesi).
   *
   * ÖNCEDEN `position:fixed` idi ve imlecin altındaki kartın ekran koordinatına
   * konumlanıyordu. Takip Edilen Kanallar listesi izleyici sayısına göre KENDİNİ
   * CANLI YENİDEN SIRALIYOR: satır kayıyor, buton ekranda çakılı kaldığı için
   * satırın altında/üstünde bir yerde asılı kalıyordu (fare hiç kıpırdamadan
   * ölçüldü: imleç satırdayken buton 70px aşağıdaydı). Fare her kımıldadığında
   * doğru yere sıçrayıp yine kayıyordu → "gidip geliyor". Üstelik yanlış satırın
   * üstünde durduğu için tıklanınca YANLIŞ kanalı gizleyebiliyordu.
   *
   * Karta demirlenince satırla birlikte hareket eder: sıralama değişse de,
   * liste kaydırılsa da, sanal liste satırı taşısa da hep aynı kartın köşesinde.
   * Ayrıca buton artık kartın İÇİNDE olduğundan üstüne gelmek "karttan çıkıldı"
   * sayılmaz → titreme döngüsü de yapısal olarak imkânsız. */
  var hideBtn = null, hoverBox = null, hoverSlug = '';


  function ensureHideBtn() {
    if (hideBtn) return;
    hideBtn = document.createElement('button');
    hideBtn.type = 'button';
    hideBtn.title = t('pkKanaliGizle');
    /* ✕ METİN DEĞİL SVG: '✕' karakteri yazı tipine göre birkaç piksel kayıyordu
       (dikeyde tam ortalanmıyordu). SVG ile her yazı tipinde piksel-hassas ortada. */
    hideBtn.innerHTML = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:15px;height:15px;display:block">' +
      '<path d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zm0 2.8a7.1 7.1 0 0 1 4.1 1.3L6.1 16.1A7.2 7.2 0 0 1 12 4.8zm0 14.4a7.1 7.1 0 0 1-4.1-1.3l10-10A7.2 7.2 0 0 1 12 19.2z"/></svg>';
    /* Görünüm: PureKick araç-çubuğu butonuyla aynı gri (#42474d) + keskin köşe (4px).
       Kartın sağ üst köşesinde durur. Koyu küçük resim üzerinde de seçilsin diye
       ince kenarlık + gölge var. Üstüne gelince kırmızı. */
    hideBtn.style.cssText = 'position:absolute;top:4px;right:4px;z-index:40;display:flex;' +
      'width:28px;height:28px;border-radius:4px;border:1px solid rgba(255,255,255,.18);' +
      'background:#42474d;color:#fff;cursor:pointer;padding:0;line-height:0;' +
      'align-items:center;justify-content:center;' +
      'box-shadow:0 2px 8px rgba(0,0,0,.5);transition:background .12s';
    hideBtn.addEventListener('mouseenter', function () { hideBtn.style.background = '#c0392b'; });
    hideBtn.addEventListener('mouseleave', function () { hideBtn.style.background = '#42474d'; });
    hideBtn.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      var s = hoverSlug; hideHideBtn();
      blockChannel(s);
    }, true);
  }
  /* Butonu DOM'dan çıkarmak = gizlemek (kartın içinde durduğu için display:none yetmez,
     kartta yer kaplamaya devam etmesin diye tamamen sökülür). */
  function hideHideBtn() {
    if (hideBtn && hideBtn.parentNode) hideBtn.parentNode.removeChild(hideBtn);
    hoverBox = null; hoverSlug = '';
  }

  function onHover(e) {
    if (!settings.consented) return;
    /* Varsayılan kapalı: kartların üstüne gelince çıkan gizle düğmesi.
       Ayar sırasında düğme takılı kalmışsa da sökülüyor. Kanal sayfasındaki
       paylaş düğmesinin yanındaki gizle düğmesi BUNDAN BAĞIMSIZ. */
    if (settings.gizleIkonu !== true) { if (hoverBox) hideHideBtn(); return; }
    var t = e.target;
    /* TİTREME DÜZELTMESİ: imleç butonun ÜSTÜNE gelince olayın hedefi buton değil,
       içindeki <svg>/<path> oluyordu. `t === hideBtn` tutmadığı için akış devam edip
       "kart dışına çıkıldı" sanıyor ve butonu gizliyordu; fare yine satırın üstünde
       kaldığından hemen tekrar gösteriliyordu → göz kırpma döngüsü (ve butona
       tıklamak da zorlaşıyordu). Butonun İÇİ de buton sayılır: */
    if (!t || t.nodeType !== 1 || (hideBtn && hideBtn.contains(t))) return;
    var a = t.closest && t.closest('a[href^="/"]');
    if (!a) { if (hoverBox && !hoverBox.contains(t)) hideHideBtn(); return; }
    var s = chanSlug(a);
    if (!s || inSkipZone(a)) { hideHideBtn(); return; }
    /* Arama açılır listesinde (ve benzeri yüzen menülerde) gizle butonu ÇIKMAZ:
       dar satırda üstteki ipucu balonuyla çakışıyordu. Yapısal işaret — dile
       bağlı değil: açılır liste Radix portalında duruyor (ölçüldü:
       `[data-radix-popper-content-wrapper]`, fixed, z-701, içi `role="dialog"`).
       DİKKAT: bu kontrol yalnızca BUTON için; engellenen kanalların aramada
       gizlenmesi (applyChannelBlocks) eskisi gibi sürüyor. */
    if (a.closest('[data-radix-popper-content-wrapper]')) { hideHideBtn(); return; }
    var box = blockContainer(a);
    if (!box) return;
    var r = box.getBoundingClientRect();
    if (r.width < 40 || r.height < 20) return;
    ensureHideBtn();
    hoverSlug = s;
    if (hoverBox !== box || hideBtn.parentNode !== box) {
      /* `position:absolute` kartı referans alsın diye kart konumlandırılmış olmalı.
         Statikse `relative` yapıyoruz — akış dışı olduğu için kart düzeni bozulmaz. */
      if (getComputedStyle(box).position === 'static') box.style.position = 'relative';
      box.appendChild(hideBtn);
      hoverBox = box;
    }
  }

  /* Kart React tarafından yeniden oluşturulursa buton da onunla DOM'dan düşer;
     takılı kalan referansı temizle (bir sonraki hover yeniden yerleştirir). */
  function tazeleHideBtn() {
    if (hoverBox && !hoverBox.isConnected) hideHideBtn();
  }

  /* ══════════════════════════════════════════════════════════════════════
   * PUREKICK PANELİ — tam sayfa yönetim ekranı
   *
   * TASARIM KURALI: Kick'in KENDİ sınıflarını kullanır (bg-surface-base,
   * text-subtle, border-outline-decorative, rounded, font-semibold…). Bu yüzden
   * panel SHADOW DOM'DA DEĞİL, normal DOM'da durur — shadow root'a Kick'in CSS'i
   * geçmez. Böylece renkler/yazı tipi/kenarlar Kick ile birebir aynı olur ve
   * Kick temasını değiştirdiğinde panel de kendiliğinden uyar.
   *
   * YERLEŞİM: Kick'in üst barı ve sol menüsü DURUR; panel sadece içerik alanını
   * kaplar. Ölçüler sabit yazılmaz, canlı okunur (sol menü açık/kapalı olabilir).
   * ══════════════════════════════════════════════════════════════════════ */
  var PANEL_ID = 'pk-panel';
  var panelEl = null;


  /* İçerik alanının ölçüleri — CANLI okunur, sabit yazılmaz.
     DİKKAT: Kick'in DOM'unda sol menü için birden çok eleman var (gerçek menü +
     "collapsed-width" yer tutucu). Tekini seçmek yanlış sonuç veriyordu (yer
     tutucuyu bulup panel menünün ÜSTÜNÜ kapatıyordu). Bu yüzden sola yapışık,
     tam yükseklikteki adayların EN SAĞA uzananını alıyoruz → menü açıkken 256,
     kapalıyken 56, gizliyse 0. Kendi panelimiz sayıma dahil edilmez. */
  function panelGeo() {
    var top = 60, left = 0;
    try {
      var nav = document.querySelector('[class*="navbar-height"]');
      if (nav) top = Math.round(nav.getBoundingClientRect().height) || 60;
      var c = document.querySelectorAll('[class*="sidebar-expanded-width"], [class*="sidebar-collapsed-width"], aside');
      for (var i = 0; i < c.length; i++) {
        var e = c[i];
        if (panelEl && panelEl.contains(e)) continue;          // kendi menümüz sayılmasın
        var r = e.getBoundingClientRect();
        if (r.left < 5 && r.width > 20 && r.height > window.innerHeight * 0.4) left = Math.max(left, Math.round(r.right));
      }
    } catch (e) {}
    return { top: top, left: left };
  }
  function panelKonumla() {                 // menü açılıp kapanınca panel kayar
    if (!panelAcikMi()) return;
    var g = panelGeo();
    if (panelEl.style.top !== g.top + 'px') panelEl.style.top = g.top + 'px';
    if (panelEl.style.left !== g.left + 'px') panelEl.style.left = g.left + 'px';
  }

  function el(tag, cls, txt) {
    var n = document.createElement(tag);
    if (cls) n.className = cls;
    if (txt != null) n.textContent = txt;
    return n;
  }

  /* --- ortak parçalar (hepsi Kick sınıflarıyla) --- */
  function kart(baslik, aciklama) {
    var c = el('div', 'bg-surface-base border-outline-decorative rounded border p-4 flex flex-col gap-3');
    if (baslik) {
      var h = el('div', 'flex flex-col gap-1');
      h.appendChild(el('div', 'text-base font-bold text-white', baslik));
      if (aciklama) h.appendChild(el('div', 'text-sm text-subtle', aciklama));
      c.appendChild(h);
    }
    return c;
  }
  function btnBirincil(txt) {
    return el('button', 'bg-primary-base hover:brightness-110 text-black h-9 px-4 rounded font-semibold text-sm transition-all betterhover:active:scale-[0.98]', txt);
  }
  function btnIkincil(txt) {
    return el('button', 'bg-secondary-base hover:brightness-125 text-white h-9 px-4 rounded font-semibold text-sm transition-all betterhover:active:scale-[0.98]', txt);
  }
  /* AÇ/KAPA DÜĞMESİ. Durumu KENDİ tutar ve görselini KENDİ günceller.

     Eskiden durum yalnız ilk çizimde yazılıyor, `acikMi` hiç değişmiyordu:
       · tik güncellenmiyordu (yenilenmesi çağıranın `panelCiz()` demesine
         bağlıydı),
       · ikinci tıklamada AYNI değer gidiyordu, yani kapattığın ayarı bir
         daha açamıyordun.
     46 anahtarın 45'i `panelCiz()` çağırdığı için gizliydi; çağırmayan tek
     anahtar (kanalın üstüne gelince gizle) hatayı açığa çıkardı. Çağrı
     yerlerini tek tek yamamak yerine kök burada kapatıldı. */
  function anahtar(acikMi, onDegis) {
    var w = el('button', 'relative shrink-0 rounded-full transition-all');
    var k = el('span', 'absolute rounded-full bg-white transition-all');
    /* Görsel TEK yerden kuruluyor: ilk çizim ile tıklama aynı yolu
       kullanmazsa ikisi zamanla ayrışır. */
    function ciz(a) {
      w.style.cssText = 'width:44px;height:24px;background:' + (a ? 'rgb(83,252,24)' : 'rgb(66,71,77)');
      k.style.cssText = 'width:18px;height:18px;top:3px;left:' + (a ? '23px' : '3px');
    }
    ciz(acikMi);
    w.appendChild(k);
    w.addEventListener('click', function () {
      acikMi = !acikMi;
      ciz(acikMi);
      onDegis(acikMi);
    });
    return w;
  }
  function satir(baslik, aciklama, sagEl) {
    var r = el('div', 'flex items-center justify-between gap-4 py-2');
    var sol = el('div', 'flex flex-col gap-0.5 min-w-0');
    sol.appendChild(el('div', 'text-sm font-semibold text-white', baslik));
    if (aciklama) sol.appendChild(el('div', 'text-xs text-subtle', aciklama));
    r.appendChild(sol);
    if (sagEl) r.appendChild(sagEl);
    return r;
  }
  function istatKutu(sayi, etiket) {
    var b = el('div', 'bg-surface-base border-outline-decorative rounded border p-4 flex flex-col gap-1 flex-1');
    b.appendChild(el('div', 'text-2xl font-bold text-primary-base', String(sayi)));
    b.appendChild(el('div', 'text-xs text-subtle', etiket));
    return b;
  }

  /* --- bölümler --- */
  function bolumGenel(body) {
    var aktif = isActive();
    var durum = kart();
    var d = el('div', 'flex items-center justify-between gap-4');
    var sol = el('div', 'flex flex-col gap-1');
    sol.appendChild(el('div', 'text-lg font-bold ' + (aktif ? 'text-primary-base' : 'text-subtle'),
      aktif ? t('statusActive') : t('statusPaused')));
    sol.appendChild(el('div', 'text-sm text-subtle', aktif ? t('pkKorumaAcikMetin') : t('pkKorumaKapaliMetin')));
    d.appendChild(sol);
    var b = aktif ? btnIkincil(t('pauseBtn')) : btnBirincil(t('startBtn'));
    // setAll -> setMaster: fonksiyon yeniden adlandırılırken bu çağrı atlanmış,
    // buton tıklanınca ReferenceError atıp hiçbir şey yapmıyordu.
    b.addEventListener('click', function () { toast(t(aktif ? 'pkTKorumaDuraklatildi' : 'pkTKorumaAcildi')); setMaster(!aktif); });
    d.appendChild(b);
    durum.appendChild(d);
    body.appendChild(durum);

    var sr = el('div', 'flex gap-3');
    body.appendChild(sr);

    /* SADECE REKLAM ENGELLEYİCİ — istatistiklerin altında, tek tık.
       Durum ayarlardan hesaplanıyor (bkz. `reklamModuMu`): kullanıcı başka
       bir özelliği açar açmaz buton yeniden tıklanabilir hâle gelir. */
    var rmAktif = reklamModuMu();
    /* DİKKAT ÇEKME: yalnız bu sürümün yeniliği HENÜZ GÖRÜLMEMİŞKEN.
       `pkYeniGoruldu` bölüm çizildikten SONRA çalıştığı için burada sorulan
       değer ilk görüntülemede doğru, sonrakilerde yanlış — çerçeve tam bir
       kez görünüp kendiliğinden kalkıyor. Sürüm atlayınca da sıfırlanıyor. */
    var rmVurgu = false;
    try { rmVurgu = !rmAktif && menuYeni(menuDugum(['genel']), ['genel']); } catch (e) {}
    var rmKart = kart();
    if (rmVurgu) {
      /* Kick'in vurgu yeşili (ölçüldü: rgb(83,252,24)). Kutu ölçüsü
         DEĞİŞMİYOR — kart zaten 1px kenarlıklı, yalnız rengi değişiyor. */
      rmKart.style.borderColor = 'rgb(83,252,24)';
      rmKart.style.boxShadow = '0 0 0 3px rgba(83,252,24,.14)';
    }
    var rmSatir = el('div', 'flex items-center justify-between gap-4');
    var rmSol = el('div', 'flex flex-col gap-1 min-w-0');
    rmSol.appendChild(el('div', 'text-base font-bold text-white', t('pkSadeceReklam')));
    rmSol.appendChild(el('div', 'text-sm text-subtle',
      rmAktif ? t('pkSadeceReklamAcik') : t('pkSadeceReklamD')));
    rmSatir.appendChild(rmSol);
    if (rmAktif) {
      rmSatir.appendChild(el('div', 'text-sm font-semibold text-primary-base shrink-0',
        t('pkSadeceReklamRozet')));
    } else {
      var rmBtn = btnBirincil(t('pkSadeceReklamBtn'));
      rmBtn.addEventListener('click', function () {
        toast(t('pkTSadeceReklam')); reklamModuUygula();
      });
      rmSatir.appendChild(rmBtn);
    }
    rmKart.appendChild(rmSatir);
    body.appendChild(rmKart);
    try {
      chrome.storage.local.get('stats', function (res) {
        var s = (res && res.stats) || { videoAdsBlocked: 0, domHidden: 0 };
        sr.appendChild(istatKutu(s.videoAdsBlocked || 0, t('pkEngellenenVideo')));
        sr.appendChild(istatKutu(s.domHidden || 0, t('pkTemizlenenAlan')));
        sr.appendChild(istatKutu(Object.keys(blocked).length, 'Gizlenen kanal'));
      });
    } catch (e) {}

    /* 8. Madde: Rozetler & Bağlı Hesap Bilgisi / Giriş-Çıkış */
    var hKart = kart('Bağlı Kick Hesabı & Rozet', 'PureKick sistemine bağlı Kick hesabınız ve rozet durumunuz.');
    var hWrap = el('div', 'flex items-center justify-between gap-4 mt-2');
    var hSol = el('div', 'flex items-center gap-3');
    
    var hAvatar = el('div', 'w-10 h-10 rounded-full bg-surface-highest flex items-center justify-center font-bold text-white text-base', '👤');
    var hBilgi = el('div', 'flex flex-col');
    var kAd = (settings.mentionAd || localStorage.getItem('pk_user_name') || 'Giriş Yapılmadı');
    hBilgi.appendChild(el('div', 'font-bold text-white text-sm', kAd));
    hBilgi.appendChild(el('div', 'text-xs text-primary-base font-semibold', kAd !== 'Giriş Yapılmadı' ? '✓ Rozet ve Hesap Bağlı' : 'Misafir Kullanıcı'));
    hSol.appendChild(hAvatar);
    hSol.appendChild(hBilgi);
    hWrap.appendChild(hSol);

    var hSag = el('div', 'flex items-center gap-2');
    if (kAd !== 'Giriş Yapılmadı') {
      var btnCikis = btnIkincil('Bağlantıyı Kes');
      btnCikis.addEventListener('click', function () {
        sendBg({ type: 'setSettings', payload: { mentionAd: '' } });
        settings.mentionAd = '';
        localStorage.removeItem('pk_user_name');
        toast('Hesap bağlantısı kesildi');
        panelCiz();
      });
      hSag.appendChild(btnCikis);
    } else {
      var btnBagla = btnBirincil('Hesap Bağla');
      btnBagla.addEventListener('click', function () {
        var ad = prompt('Kick kullanıcı adınızı girin:');
        if (ad && ad.trim()) {
          sendBg({ type: 'setSettings', payload: { mentionAd: ad.trim() } });
          settings.mentionAd = ad.trim();
          localStorage.setItem('pk_user_name', ad.trim());
          toast('Hesap başarıyla bağlandı: ' + ad.trim());
          panelCiz();
        }
      });
      hSag.appendChild(btnBagla);
    }
    hWrap.appendChild(hSag);
    hKart.appendChild(hWrap);
    body.appendChild(hKart);
  }

  function bolumReklam(body) {
var c = kart(t('pkEngellemeAyarlari'), t('pkAninda'));
    c.appendChild(satir(t('pkVideoReklamlari'), t('pkVideoReklamlariD'),
      anahtar(settings.blockVideoAds !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { blockVideoAds: v } }); settings.blockVideoAds = v; panelCiz();
        toast(ozellikToast(v, 'pkVideoReklamlari'));
      })));
    /* Geçmiş yayın reklamı AYRI anahtar: canlıdaki reklam ayrı bir istekle gelir,
       VOD'daki ise akışın içine dikilidir (SSAI) ve temiz kaynağa geçilerek
       atlanır — iki mekanizma bağımsız, kullanıcı birini kapatıp diğerini
       açık tutabilmeli. */
    c.appendChild(satir(t('pkVodReklamlari'), t('pkVodReklamlariD'),
      anahtar(settings.blockVodAds !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { blockVodAds: v } }); settings.blockVodAds = v;
        pushConfigToPage();                       // MAIN-world hook anında haberdar olsun
        panelCiz();
        toast(ozellikToast(v, 'pkVodReklamlari'));
      })));
    c.appendChild(satir(t('pkSayfaReklamlari'), t('pkSayfaReklamlariD'),
      anahtar(settings.blockDom !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { blockDom: v } }); settings.blockDom = v; panelCiz();
        toast(ozellikToast(v, 'pkSayfaReklamlari'));
      })));
    body.appendChild(c);
  }



  /* Kanal Hakkında ek bilgi ayarı.
     ADI DİKKATLE SEÇİLDİ: `bolumHakkinda` zaten "PureKick Hakkında"
     sayfasının adı. Aynı adı kullanınca fonksiyon bildirimi yükseltmesi
     yüzünden sonraki kazanıyor ve bu sayfa yanlış içerik gösteriyordu. */
  function bolumKanalHakkinda(body) {
    var c = kart(t('pkHakkindaEk'), t('pkHakkindaEkD'));
    c.appendChild(satir(t('pkHakkindaEk'), t('pkHakkindaEkD'),
      anahtar(settings.hakkindaEk !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { hakkindaEk: v } }); settings.hakkindaEk = v;
        if (v) hakkindaUygula(); else hakkindaTemizle();
        panelCiz();
        toast(ozellikToast(v, 'pkHakkindaEk'));
      })));
    body.appendChild(c);
  }

  /* Kenar Çubuğu › Kanal listesi. */
  function bolumKenarListe(body) {
    var sb = kart(t('pkSabitKanallar'), t('pkSabitKanallarD'));
    sb.appendChild(satir(t('pkSabitKanalAyar'), t('pkSabitKanalAyarD'),
      anahtar(settings.sabitKanal !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { sabitKanal: v } }); settings.sabitKanal = v;
        /* Kapatılınca raptiyeleri ve sıralama işaretlerini topla. */
        if (!v) sabitTemizle(); else sabitKanalUygula();
        panelCiz();
        toast(ozellikToast(v, 'pkSabitKanalAyar'));
      })));
    sb.appendChild(satir(t('pkKenarGrup'), t('pkKenarGrupD'),
      anahtar(settings.kenarGrup !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { kenarGrup: v } }); settings.kenarGrup = v;
        /* Kapatınca başlıklarımızı ve işaretleri topluyoruz; listeyi geri
           DARALTMIYORUZ — Kick'in kendi durumu, altından çekmek olur.
           Sonraki sayfa yüklemesinde Kick zaten 10'da başlıyor. */
        if (v) { sabitAcSayac = 0; sabitAcSonSayi = 0; sabitKanalUygula(); }
        else grupTemizle(document.querySelector('[' + SABIT_LISTE + ']'));
        panelCiz();
        toast(ozellikToast(v, 'pkKenarGrup'));
      })));
    
    /* 9. Madde: Çevrimdışı Kanalları Tamamen Gizle/Göster */
    sb.appendChild(satir('Çevrimdışı Kanalları Gizle', 'Kenar çubuğundaki yayında olmayan kanalları tamamen gizleyerek yer tasarrufu sağlar.',
      anahtar(settings.kenarCevrimdisiGizle === true, function (v) {
        sendBg({ type: 'setSettings', payload: { kenarCevrimdisiGizle: v } }); settings.kenarCevrimdisiGizle = v;
        pkCevrimdisiGizleUygula();
        sabitKanalUygula();
        panelCiz();
        toast(v ? 'Çevrimdışı kanallar gizlendi' : 'Çevrimdışı kanallar görünür yapıldı');
      })));
    body.appendChild(sb);
  }

  function bolumOnizleme(body) {
var oz = kart(t('pkOnizleme'), t('pkOnizlemeD'));
    oz.appendChild(satir(t('pkOnizlemeKarti'), t('pkOnizlemeKartiD'),
      anahtar(settings.onizleme !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { onizleme: v } }); settings.onizleme = v;
        /* Kapatılınca ekranda duran kutuyu topla. `onizBizimKapat` kutuyu,
           ipucu bastırma stilini ve BEKLEYEN AÇILMA ZAMANLAYICISINI birden
           halleder — sonuncusu olmasa ayar kapatıldıktan yarım saniye sonra
           kutu yine açılırdı. */
        if (!v) { try { onizBizimKapat(); } catch (e) {} }
        panelCiz();
        toast(ozellikToast(v, 'pkOnizlemeKarti'));
      })));
    body.appendChild(oz);

  }


  function bolumTema(body) {
    /* 10.13 — BAKIM: Tema Rengi geçici olarak devre dışı (ana sayfadaki
       performans/kasma sorunları). Seçtirme kapatıldı; recolor.js'te isNoop()
       zorla true → herkes varsayılan (Kick yeşili), recolor hiç çalışmaz → kasma
       yok. Kod DURUYOR — geri açmak için: (1) buradaki eski buton döngüsünü
       (THEME_PRESETS.forEach + reset) geri koy, (2) recolor.js'te isNoop'u eski
       haline döndür. */
    var c = kart(t('themeColor'), t('pkTemaAciklama'));
    var uyari = el('div', 'text-sm');
    uyari.style.cssText = 'padding:12px 14px;border:1px solid rgba(255,193,7,.42);border-radius:8px;background:rgba(255,193,7,.10);color:#ffd479;line-height:1.55';
    uyari.textContent = '🔧 Tema Rengi şu anda bakımda. Ana sayfadaki performans (kasma) sorunları nedeniyle geçici olarak devre dışı bırakıldı ve tüm kullanıcılar varsayılan (Kick) renge döndürüldü. En kısa sürede yeniden aktif edilecektir.';
    c.appendChild(uyari);
    body.appendChild(c);
  }

  function bolumRozet(body) {
    var c = kart(linked ? t('badgeLinkedTitle') : t('badgeCtaTitle'),
                 linked ? t('badgeLinkedSub') : t('badgeCtaSub'));
    var a = el('a', 'bg-primary-base hover:brightness-110 text-black h-9 px-4 rounded font-semibold text-sm inline-flex items-center w-fit',
               linked ? t('pkPaneliAc') : t('pkKickIleGiris'));
    a.href = SITE_URL; a.target = '_blank'; a.rel = 'noopener noreferrer';
    c.appendChild(a);
    body.appendChild(c);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.15 — ABONELİK BÖLÜMÜ
   *
   * Dört durum, üçü farklı düğme gerektiriyor:
   *   giriş yapılmamış  -> "Kick ile giriş yap" (üstteki kart zaten veriyor)
   *   giriş var, abone değil, geçmiş YOK  -> "Abone Ol"
   *   giriş var, abonelik BİTMİŞ          -> "Aboneliği Yenile"
   *   aktif abone                         -> düğme yok, kıdem + rozet yazar
   *
   * "Hiç abone olmadı" ile "aboneliği bitti" ayrımı sunucudan geliyor
   * (gecmis alanı): ikisine aynı metni yazmak, bir yıl destek olmuş
   * kullanıcıya "abone ol" demek olurdu.
   * ══════════════════════════════════════════════════════════════════════ */
  /* Örnek gösterim: gerçek kimsenin ayarı DEĞİL. İSİM rastgele değil —
     kullanıcının kendi adı, o okunamazsa sabit "PureKick". Yalnız renkler
     her açılışta değişiyor. */
  var PK_AB_RENK = [
    ['#ff3d9a', '#a855f7'], ['#53fc18', '#00e5ff'], ['#ff8a00', '#ff2d55'],
    ['#00e5ff', '#7c3aed'], ['#ffd60a', '#ff6b00'], ['#22d3ee', '#a3e635'],
    ['#f472b6', '#60a5fa'], ['#34d399', '#facc15']
  ];
  function pkAbSec(a) { return a[Math.floor(Math.random() * a.length)]; }
  function pkAbRgba(hex, alfa) {
    var n = parseInt(String(hex).slice(1), 16);
    if (isNaN(n)) return 'rgba(83,252,24,' + alfa + ')';
    return 'rgba(' + ((n >> 16) & 255) + ',' + ((n >> 8) & 255) + ',' + (n & 255) + ',' + alfa + ')';
  }

  /* Kademe rozetleri — GÖRSELLER EKLENTİYE GÖMÜLÜ (`rozetler/subN.png`).

     NEDEN SUNUCUDAN DEĞİL: demo şeridi canlıda hiç görünmedi. Sebep, rozet
     tanımlarının 6 saat önbelleklenmesi — kademe rozetleri sunucuya sonradan
     eklendiği için eski önbellekte o anahtarlar HİÇ YOK; `ay` alanını
     kurtarmak da yetmiyor. Demo tanıtım içeriği, canlı veri değil: gömülü
     dosya her koşulda (önbellek eski, ağ yok, giriş yapılmamış) anında
     çiziliyor. Toplam ~84 KB.

     Liste sabit: kademeler `badges` tablosunda değişse bile demo bozulmaz;
     gerçek rozetler zaten sunucudan geliyor, burası yalnız vitrin. */
  var PK_AB_KADEME = [1, 3, 6, 12, 24, 36, 48];
  function aboneKademeGorselleri() {
    var out = [];
    for (var i = 0; i < PK_AB_KADEME.length; i++) {
      var ay = PK_AB_KADEME[i], anahtar = 'sub' + ay, src = '';
      try { src = chrome.runtime.getURL('rozetler/' + anahtar + '.png'); } catch (e) {}
      if (src) out.push({ key: anahtar, ay: ay, src: src });
    }
    return out;
  }

  function abonePaneliTazele() {
    try {
      if (panelEl && document.contains(panelEl) &&
          panelYol[panelYol.length - 1] === 'abone') panelCiz();
    } catch (e) {}
  }

  function aboneDemo(renkler) {
    var kap = el('div', 'flex flex-col gap-2');
    var kademeler = aboneKademeGorselleri();

    kap.appendChild(el('div', 'text-xs text-subtle', t('pkAboneDemoBaslik')));

    if (kademeler.length) {
      var serit = el('div', 'pk-ab-rozetler');
      kademeler.forEach(function (r) {
        var im = document.createElement('img');
        im.src = r.src; im.alt = ''; im.referrerPolicy = 'no-referrer';
        im.title = t(r.ay === 1 ? 'pkAboneAy1' : 'pkAboneAy', [String(r.ay)]);
        serit.appendChild(im);
      });
      kap.appendChild(serit);
    }

    /* Örnek sohbet satırı: sohbetteki görünümün BİREBİR aynısı — rozet +
       AKAN isim + AKAN satır çerçevesi. Üçü birden, çünkü sohbette de üçü
       birlikte çıkıyor; boyamayı `akanAdBoya`/`akanSatirBoya` yapıyor, yani
       gerçek sohbetle aynı koddan. */
    var satir = el('div', 'pk-ab-satir');
    akanSatirBoya(satir, renkler[1]);
    if (kademeler.length) {
      var rz = pkAbSec(kademeler);
      var im2 = document.createElement('img');
      im2.src = rz.src; im2.alt = ''; im2.referrerPolicy = 'no-referrer';
      satir.appendChild(im2);
    }
    /* İSİM KULLANICININ KENDİ ADI: "sen böyle görüneceksin" demek uydurma bir
       ad göstermekten çok daha anlaşılır. Ad Kick sayfasından okunuyor
       (pkKendiAdim), siteye bağlanmaya gerek yok; okunamazsa örnek ada düşer. */
    var benim = '';
    try { benim = pkKendiAdim(); } catch (e) {}
    /* Ad henüz gelmediyse örnek adla çiziyoruz ama gelince yeniden çizip
       gerçek adı koyuyoruz — kullanıcı "bu benim adım değil" demesin. */
    if (!benim) {
      try { pkKendiAdimCek(function (a) { if (a) abonePaneliTazele(); }); } catch (e) {}
    }
    /* Gerçek ad yoksa SABİT "PureKick" — rastgele ad kullanıcıya "bu benim
       adım değil" dedirtiyordu ve her açılışta değişmesi kafa karıştırıyordu. */
    var ad = el('span', 'pk-ab-ad', benim || 'PureKick');
    akanAdBoya(ad, akanPalet(renkler) || renkler);
    satir.appendChild(ad);
    satir.appendChild(el('span', 'pk-ab-mesaj', t('pkAboneDemoMesaj')));
    kap.appendChild(satir);
    return kap;
  }

  function bolumAbone(body) {
    /* GİRİŞ YOKSA: önce siteye. Abonelik işlemlerinin tamamı panelde
       yapılıyor (bkz. abonePanelAdresi) — sıra: giriş → bağla → abone ol. */
    if (!linked) {
      var t0 = kart(t('pkAbonelik'), t('pkAbonelikTanit'));
      /* Demo GİRİŞ DÜĞMESİNİN ÜSTÜNDE: kullanıcı "ne kazanacağım" sorusunun
         cevabını görmeden giriş yapmaya davet edilmemeli. */
      var dr = pkAbSec(PK_AB_RENK);
      t0.appendChild(aboneDemo(dr));
      t0.appendChild(el('div', 'text-xs text-subtle', t('pkAboneOnceGiris')));
      t0.appendChild(pkAboneDugme(t('pkKickIleGiris'), SITE_URL));
      body.appendChild(t0);
      return;
    }

    /* Durum SUNUCUDAN gelir (heartbeat yanıtı) ve sayfa her açıldığında
       tazesi isteniyor: elle verilen abonelik ya da yeni ödeme, 6 saatlik
       heartbeat turunu beklemeden görünsün. Yanıt gelince storage.onChanged
       bu sayfayı yeniden çiziyor. */
    aboneTazele();

    var k = kart(t('pkAbonelik'), null);

    /* HENÜZ HİÇ DURUM OKUNMADI (yeni kurulum, ya da 10.15'e ilk geçişte
       heartbeat turu daha gelmemiş). Burada "Abone Ol" basmak abone olan
       kullanıcıya "abone değilsin" demek olurdu — yanıt gelene kadar bekle. */
    if (!abone.ts) {
      k.appendChild(el('div', 'text-sm text-subtle', t('pkAboneYukleniyor')));
      body.appendChild(k);
      return;
    }

    if (abone.abone) {
      /* AKTİF: rozet + kıdem + tarihler. Düğme yok — satacak bir şey kalmadı. */
      var d = badgeMaps && badgeMaps.defs ? badgeMaps.defs[abone.rozet] : null;
      var sat = el('div', 'flex items-center gap-2');
      if (d && (d.img || d.image)) {
        var im = document.createElement('img');
        im.src = d.img || d.image; im.alt = '';
        im.style.cssText = 'width:22px;height:22px;object-fit:contain;flex:none';
        im.referrerPolicy = 'no-referrer';
        sat.appendChild(im);
      }
      var ay = abone.ay > 0 ? abone.ay : 1;
      sat.appendChild(el('div', 'text-sm font-semibold text-white',
        t(ay === 1 ? 'pkAboneAy1' : 'pkAboneAy', [String(ay)])));
      k.appendChild(sat);

      var bas = pkAboneTarih(abone.bas);
      if (bas) k.appendChild(pkAboneSatir(t('pkAboneBaslangic'), bas));
      var bit = pkAboneTarih(abone.bitis);
      if (bit) k.appendChild(pkAboneSatir(t('pkAboneYenileme'), bit));

      k.appendChild(el('div', 'text-xs text-subtle', t('pkAboneAktifNot')));
      k.appendChild(pkAboneBaglanti(t('pkPaneliAc'), SITE_URL));
    } else if (abone.gecmis) {
      /* BİTMİŞ: yenileme de panelden yapılıyor (bkz. abonePanelAdresi). */
      k.appendChild(el('div', 'text-sm text-white', t('pkAboneBitti')));
      var sonB = pkAboneTarih(abone.bitis);
      if (sonB) k.appendChild(pkAboneSatir(t('pkAboneSonaErdi'), sonB));
      if (abone.ay > 0) k.appendChild(pkAboneSatir(t('pkAboneKidem'),
        t(abone.ay === 1 ? 'pkAboneAy1' : 'pkAboneAy', [String(abone.ay)])));
      k.appendChild(pkAboneDugme(t('pkAboneYenile'), abonePanelAdresi()));
    } else {
      /* Bağlı ama abone değil: burada da ne kazanacağını göstermek gerekiyor,
         yoksa "Abone Ol" düğmesi tek başına ne verdiğini anlatmıyor. */
      k.appendChild(aboneDemo(pkAbSec(PK_AB_RENK)));
      k.appendChild(el('div', 'text-sm text-subtle', t('pkAbonelikTanit')));
      k.appendChild(pkAboneDugme(t('pkAboneOl'), abonePanelAdresi()));
    }
    body.appendChild(k);
  }

  /* Abonelik durumunu arka plandan tazele. 30 sn'den sık istenmiyor:
     heartbeat sunucunun önbelleklenemeyen tek yazma uç noktası, paneli açıp
     kapatan kullanıcı oraya yük bindirmesin. */
  var aboneTazeleSon = 0;
  function aboneTazele() {
    if (Date.now() - aboneTazeleSon < 30000) return;
    aboneTazeleSon = Date.now();
    try {
      chrome.runtime.sendMessage({ type: 'pk-abone-tazele' }, function () {
        void chrome.runtime.lastError;   // yanıt dinlemiyoruz; storage'dan okunacak
      });
    } catch (e) {}
  }

  /* ISO tarihi kullanıcının kendi diline göre yazar. Geçersiz/boş değerde ''
     döner ki satır hiç basılmasın (sunucu alanı göndermemiş olabilir). */
  function pkAboneTarih(iso) {
    if (!iso) return '';
    var d = new Date(iso);
    if (isNaN(d.getTime())) return '';
    try { return d.toLocaleDateString(undefined, { year: 'numeric', month: 'long', day: 'numeric' }); }
    catch (e) { return d.toISOString().slice(0, 10); }
  }

  function pkAboneSatir(etiket, deger) {
    var r = el('div', 'flex items-center justify-between gap-3 text-xs');
    r.appendChild(el('span', 'text-subtle', etiket));
    r.appendChild(el('span', 'text-white font-semibold', deger));
    return r;
  }

  function pkAboneBaglanti(yazi, adres) {
    var a = el('a', 'text-xs text-subtle hover:text-white transition-all w-fit', yazi + ' ↗');
    a.href = adres; a.target = '_blank'; a.rel = 'noopener noreferrer';
    a.style.cssText = 'text-decoration:none';
    return a;
  }

  /* Kick'in kendi birincil düğme sınıflarıyla — panelin geri kalanıyla aynı. */
  function pkAboneDugme(yazi, adres) {
    var kap = el('div', 'flex');
    var a = el('a', 'bg-primary-base hover:brightness-110 text-black h-9 px-4 rounded ' +
                    'font-semibold text-sm inline-flex items-center w-fit', yazi);
    a.href = adres; a.target = '_blank'; a.rel = 'noopener noreferrer';
    kap.appendChild(a);
    return kap;
  }

  /* Günlük ödül kartı. Drops bölümüne konuldu çünkü ikisi de "Kick'ten ödül
     al" işi; Gelişmiş'e gömseydik kimse bulamazdı. */
  function bolumDrops(body) {
    var o = kart(t('pkOdulBaslik'), t('pkOtoClaimD'));
    o.appendChild(satir(t('pkOtoClaim'), null,
      anahtar(settings.otoClaim === true, function (v) {
        settings.otoClaim = v;
        /* SORGU AYAR KAYDEDİLDİKTEN SONRA. İkisi aynı anda gönderilince
           yarışıyorlardı: arka plan sorguyu alırken ayar henüz yazılmamış
           oluyor, `otoClaim` false görünüyor ve 'kapalı' dönüyordu — durum
           satırı sonsuza dek "kontrol ediliyor" kalıyordu. */
        sendBg({ type: 'setSettings', payload: { otoClaim: v } })
          .then(function () { if (v) { try { odulSor(true); } catch (e) {} } })
          .catch(function () {});
        panelCiz();
        toast(ozellikToast(v, 'pkOtoClaim'));
      })));
    /* Kart açıldığında elde durum yoksa bir kez sor — kullanıcı paneli
       açtığında "kontrol ediliyor" görüp bekletilmesin. Gem zaten
       arka planda. */
    if (settings.otoClaim === true && !odulSonDurum) { try { odulSor(true); } catch (e) {} }
    var dr = odulDurumYazi();
    if (dr) o.appendChild(dr);
    body.appendChild(o);

    var c = kart(t('dropsTitle'), t('pkDropsAciklama'));
    var liste = el('div', 'flex flex-col gap-2');
    c.appendChild(liste); body.appendChild(c);
    sendBg({ type: 'getDrops', force: false }).then(function (d) {
      dropsData = d || dropsData;
      markDropsSeen();          // bu bölüm görüldü → menüdeki "1" bildirimi düşsün
      /* YALNIZ menü tazelenir. Eskiden burada tam çizim (`panelCiz`) vardı ve
         o da bu fonksiyonu yeniden çağırıyordu → sonsuz döngü, panel donuyordu. */
      panelNavCiz();
      /* Yanıt geç geldiyse kullanıcı başka bölüme geçmiş olabilir; kopmuş
         listeye yazmanın anlamı yok. */
      if (!liste.isConnected) return;
      var arr = (d && Array.isArray(d.active)) ? d.active : [];
      if (!arr.length) { liste.appendChild(el('div', 'text-sm text-subtle', t('dropsEmpty'))); return; }
      arr.forEach(function (x) {
        // Alan adları menüdeki renderDrops ile AYNI olmalı: category|name + endsAt + image
        var baslik = x.category || x.name || '—';
        /* Tarih + kalan süre: "23 Ağu · 5 saat kaldı". Biri boşsa öbürü
           tek başına yazılıyor, araya boş ayraç girmiyor. */
        var kalan  = [fmtEnd(x.endsAt), fmtLeft(x.endsAt)].filter(Boolean).join(' · ');
        var r = el('a', 'bg-surface-highest hover:brightness-125 transition-all rounded p-3 flex items-center gap-3 no-underline');
        r.href = DROPS_URL; r.target = '_blank'; r.rel = 'noopener noreferrer'; r.title = t('dropTitle');
        var yedekGorsel = function () {
          var ph = el('div', 'rounded shrink-0 flex items-center justify-center bg-surface-base', '🎁');
          ph.style.cssText += ';width:40px;height:40px;font-size:20px';
          return ph;
        };
        if (x.image) {
          var im = el('img', 'rounded shrink-0');
          im.src = x.image; im.alt = ''; im.referrerPolicy = 'no-referrer';
          im.style.cssText = 'width:40px;height:40px;object-fit:cover';
          // Görsel yüklenmezse kırık ikon kalmasın → 🎁'ye düş
          im.addEventListener('error', function () { if (im.parentNode) im.parentNode.replaceChild(yedekGorsel(), im); }, { once: true });
          r.appendChild(im);
        } else {
          r.appendChild(yedekGorsel());
        }
        var tx = el('div', 'flex flex-col min-w-0 flex-1 gap-0.5');
        tx.appendChild(el('div', 'text-sm font-semibold text-white truncate', baslik));
        if (kalan) tx.appendChild(el('div', 'text-xs text-subtle truncate', kalan));
        r.appendChild(tx);
        r.appendChild(el('div', 'text-subtle shrink-0', '›'));
        liste.appendChild(r);
      });
    });
  }



  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — SOHBET İSTATİSTİKLERİ
   * Veriler sohbetçiler motorundan gelir; hiçbir ağ isteği yapılmaz.
   * Görünüm Kick'in kendi sınıflarıyla kurulur (kart / istatKutu / satır).
   * ══════════════════════════════════════════════════════════════════════ */
  var istatSekme = 'ozet';                 // ozet | kisiler | mesajlar

  function istatSure() {
    if (!scBaslangic || !scSonMesajTs) return 0;
    return Math.max(0, scSonMesajTs - scBaslangic);
  }
  function istatSureYazi() {
    var ms = istatSure(), sn = Math.floor(ms / 1000);
    var s = Math.floor(sn / 3600), d = Math.floor((sn % 3600) / 60), k = sn % 60;
    function ik(n) { return (n < 10 ? '0' : '') + n; }
    return ik(s) + ':' + ik(d) + ':' + ik(k);
  }
  function istatDakikaBasi(toplam) {
    var dk = istatSure() / 60000;
    if (dk < 0.5) return 0;
    var v = toplam / dk;
    /* Sakin sohbette dakikada 1'in altına düşüyor ve tam sayıya yuvarlayınca
       "0" görünüyordu (canlıda 48 mesaj / 133 dk). 10'un altında bir ondalık. */
    return v < 10 ? Math.round(v * 10) / 10 : Math.round(v);
  }
  function istatSirala(nesne, alan) {
    var out = [];
    for (var k in nesne) if (Object.prototype.hasOwnProperty.call(nesne, k)) out.push(nesne[k]);
    out.sort(function (a, b) { return (b[alan] || 0) - (a[alan] || 0); });
    return out;
  }

  /* Sıralı liste satırı — #1 ad ....... değer */
  function istatSiraSatir(sira, ad, deger, alt) {
    var r = el('div', 'bg-surface-highest rounded px-3 py-2 flex items-center gap-3');
    var no = el('div', 'text-sm font-bold shrink-0', '#' + sira);
    no.style.cssText = 'min-width:28px;color:' + (sira <= 3 ? 'rgb(83,252,24)' : 'rgb(146,158,166)');
    r.appendChild(no);
    var orta = el('div', 'flex flex-col min-w-0 flex-1');
    orta.appendChild(el('div', 'text-sm font-semibold text-white truncate', ad));
    if (alt) orta.appendChild(el('div', 'text-xs text-subtle truncate', alt));
    r.appendChild(orta);
    r.appendChild(el('div', 'text-sm font-bold text-white shrink-0', String(deger)));
    return r;
  }

  function istatSekmeCubugu(sec) {
    var bar = el('div', 'flex gap-1 border-outline-decorative border-b');
    /* Kaydırırken başlıklar kaybolmasın: şeridi gövdenin üstüne yapıştır.
       Zemin şart — altından geçen satırlar okunmaz hâle gelmesin. */
    bar.style.cssText = 'position:sticky;top:0;z-index:2;background:rgb(23,26,28)';
    [['ozet', t('pkSekmeGenel')], ['kisiler', t('pkSohbetEdenler')], ['mesajlar', t('pkMesajlar')], ['bagis', '💰 Bağış & Kicks']].forEach(function (p) {
      var b = el('button', TAB_BASE + (istatSekme === p[0] ? TAB_ACTIVE : TAB_IDLE) + ' text-sm', p[1]);
      b.addEventListener('click', function (e) {
        e.preventDefault(); e.stopPropagation();
        istatSekme = p[0];
        pkNereyiTazele();          // gömülü sayfa açıksa onu, değilse paneli
      });
      bar.appendChild(b);
    });
    return bar;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — İSTATİSTİK PENCERESİ (sohbetten açılır, ortalanmış modal)
   * Ayarlar panelinde DEĞİL: özellik kullanıldığı yerde durmalı.
   * İskelet VOD penceresiyle aynı kalıp; genişlik ve başlık farklı.
   * ══════════════════════════════════════════════════════════════════════ */
  var ISTAT_ID = 'pk-istat';
  function istatKapat() { var p = document.getElementById(ISTAT_ID); if (p) p.remove(); }

  /* Kanalın kapak görseli + avatarı.
     ÖNCE DOM'dan kazınıyordu ve yanlış kanalın görselini alıyordu: sayfada
     önerilen kanalların banner'ları da duruyor, ilk eşleşme onlardan biri
     olabiliyor (canlıda görüldü — Cavs sayfasında xQc'nin kapağı çıktı).
     Artık Kick'in kendi ucundan alınıyor: /api/v2/channels/<slug>
     → banner_image.url · user.profile_pic · followers_count · verified.
     Kanal başına BİR istek, sonuç bellekte; çerez gönderilmez. */
  var kanalBilgi = Object.create(null);      // slug -> {ts, veri} | 'bekliyor'

  /* ══════════════════════════════════════════════════════════════════════
   * KENAR ÇUBUĞU ÖNİZLEMESİ — KICK'İN KENDİ İPUCUNUN İÇİNDE
   * ══════════════════════════════════════════════════════════════════════ */


  /* Oynatıcı iframe'i. İKİ yol da bunu kullanıyor: Kick'in ipucuna giren
     eski yol ve kendi satırlarımızdaki yeni yol. Tek yerde durması şart —
     iki kopya olsaydı biri güncellenip diğeri unutulurdu. */
  function onizOynatici(slug) {
    var ifr = el('iframe', '');
    /* `muted=false&autoplay=true`: `muted=true` ile oynatıcı oynat düğmesiyle
       bekliyor. Tarayıcı otomatik oynatmaya izin vermezse tek tıkla başlar. */
    ifr.src = 'https://player.kick.com/' + encodeURIComponent(slug) + '?muted=false&autoplay=true';
    ifr.setAttribute('allow', 'autoplay; encrypted-media; picture-in-picture');
    ifr.setAttribute('referrerpolicy', 'origin');
    ifr.setAttribute('frameborder', '0');
    ifr.setAttribute('scrolling', 'no');
    ifr.style.cssText = 'position:absolute;inset:0;width:100%;height:100%;border:0;display:block';
    return ifr;
  }


  /* ══════════════════════════════════════════════════════════════════════
   * KENDİ SATIRLARIMIZDA ÖNİZLEME
   * Kick'in ipucu React'e bağlı; bizim satırlarımız klon olduğu için (ölçüldü:
   * klonda `__reactFiber` yok) o ipucu hiç açılmıyor. Satırlar bizim olduğuna
   * göre hover da bizim: kendi kutumuzu kuruyoruz.
   * ══════════════════════════════════════════════════════════════════════ */
  var ONIZ_KUTU_IM = 'data-pk-oniz-kutu';
  var ONIZ_GECIKME = 450;      // hover niyeti: listede gezerken açılmasın
  var ONIZ_PAY     = 180;      // ayrılışta pay: fare kutuya geçebilsin
  var onizAcZaman = 0, onizKapatZaman = 0, onizKutu = null, onizSatir = null;

  /* Kutumuz açıkken Kick'in ipucu gizleniyor — yan yana iki kart olmasın.
     Silmiyoruz, yalnız bir stil kuralıyla gizliyoruz; kutu kapanınca kural
     kalkıyor ve ipucu aynen geri geliyor. */
  var ONIZ_BASTIR_ID = 'pk-oniz-bastir';
  function onizIpucuBastir(ac) {
    var st = document.getElementById(ONIZ_BASTIR_ID);
    if (!ac) { if (st) st.remove(); return; }
    if (st) return;
    st = el('style', '');
    st.id = ONIZ_BASTIR_ID;
    st.textContent = '.z-tooltip{display:none !important}';
    pkKatmanaEkle(st);
  }

  /* Yayın başlığı önbelleği: slug -> { ts, baslik }. Başlık yayın sırasında
     değişebildiği için kısa ömürlü. `bekliyor` alanı aynı kanala iki istek
     gitmesini engelliyor (fare girip çıkınca tekrar tetikleniyordu). */
  var ONIZ_BASLIK_TAZE = 120000;              // 2 dk
  var onizBaslikVeri = Object.create(null);

  function onizBaslikCek(slug, bitince) {
    if (!slug) return;
    var v = onizBaslikVeri[slug];
    if (v && v.bekliyor) return;
    if (v && Date.now() - v.ts < ONIZ_BASLIK_TAZE) { bitince(v.baslik); return; }
    onizBaslikVeri[slug] = { ts: 0, bekliyor: true, baslik: '' };
    fetch('https://kick.com/api/v2/channels/' + encodeURIComponent(slug), { credentials: 'omit' })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        var b = (j && j.livestream && j.livestream.session_title) || '';
        onizBaslikVeri[slug] = { ts: Date.now(), bekliyor: false, baslik: String(b) };
        bitince(onizBaslikVeri[slug].baslik);
      })
      .catch(function () {
        /* Başarısızlığı da ÖNBELLEĞE YAZ: yoksa her hover'da yeniden denerdi. */
        onizBaslikVeri[slug] = { ts: Date.now(), bekliyor: false, baslik: '' };
        bitince('');
      });
  }

  function onizBizimKapat() {
    onizIpucuBastir(false);
    if (onizAcZaman) { clearTimeout(onizAcZaman); onizAcZaman = 0; }
    if (onizKapatZaman) { clearTimeout(onizKapatZaman); onizKapatZaman = 0; }
    /* Belgedeki BÜTÜN kopyalar: kutu bir turda kopmuş olabilir. */
    var q = document.querySelectorAll('[' + ONIZ_KUTU_IM + ']');
    for (var i = 0; i < q.length; i++) { try { q[i].remove(); } catch (e) {} }
    onizKutu = null; onizSatir = null;
  }

  /* Kutuyu satırın sağına, dikeyde ortalayarak koyar; ekrandan taşmaz. */
  function onizKonumla(kutu, satir) {
    var r = satir.getBoundingClientRect();
    var g = kutu.getBoundingClientRect();
    var sol = r.right + 8;
    if (sol + g.width > innerWidth - 8) sol = Math.max(8, r.left - g.width - 8);
    var ust = r.top + (r.height / 2) - (g.height / 2);
    ust = Math.max(8, Math.min(ust, innerHeight - g.height - 8));
    kutu.style.left = Math.round(sol) + 'px';
    kutu.style.top = Math.round(ust) + 'px';
  }

  function onizBizimAc(satir, k) {
    if (!satir.isConnected) return;      // satır bu arada yeniden çizilmiş olabilir
    onizBizimKapat();
    var kutu = el('div', 'bg-surface-highest rounded overflow-hidden');
    kutu.setAttribute(ONIZ_KUTU_IM, '1');
    kutu.style.cssText = 'position:fixed;width:340px;z-index:2147483120;' +
      'box-shadow:0 20px 32px rgba(0,0,0,.2);left:-9999px;top:-9999px';

    var cer = el('div', '');
    cer.style.cssText = 'position:relative;width:100%;aspect-ratio:16/9;background:#000';
    cer.appendChild(onizOynatici(k.slug));
    kutu.appendChild(cer);

    var alt = el('div', 'flex flex-col gap-0.5');
    alt.style.cssText = 'padding:8px 10px';
    /* Başlık en üstte ve belirgin; kullanıcı adı onun altında. Başlık henüz
       gelmediyse satır BOŞ DURUYOR ve gelince doluyor — kutunun açılmasını
       beklettirmiyoruz. Hiç gelmezse gizli kalıyor, düzen eskisi gibi. */
    var bsl = el('div', 'text-sm font-semibold text-white truncate');
    bsl.style.display = 'none';
    alt.appendChild(bsl);
    var adEl = el('div', 'text-sm font-semibold text-white truncate', k.ad || k.slug);
    alt.appendChild(adEl);
    if (k.kategori) alt.appendChild(el('div', 'text-xs text-subtle truncate', k.kategori));
    kutu.appendChild(alt);

    onizBaslikCek(k.slug, function (b) {
      /* Kutu bu arada kapanmış ya da başka kanala geçilmiş olabilir. */
      if (!bsl.isConnected || onizKutu !== kutu) return;
      if (!b) return;
      bsl.textContent = b;
      bsl.style.display = '';
      bsl.title = b;
      /* Başlık gelince ad ikincil satıra düşüyor — iki satır aynı ağırlıkta
         olursa hangisinin yayın adı olduğu anlaşılmıyor. */
      adEl.className = 'text-xs font-semibold text-subtle truncate';
      /* Kutu uzadı: konumu tazele, yoksa ekranın altından taşabiliyor. */
      if (onizSatir) onizKonumla(kutu, onizSatir);
    });

    /* Fare kutunun üstündeyken kapanmasın — oynatıcıya tıklanabilsin. */
    kutu.addEventListener('mouseenter', function () {
      if (onizKapatZaman) { clearTimeout(onizKapatZaman); onizKapatZaman = 0; }
    });
    kutu.addEventListener('mouseleave', onizBizimGecikmeliKapat);

    pkKatmanaEkle(kutu);
    onizIpucuBastir(true);
    onizKutu = kutu; onizSatir = satir;
    onizKonumla(kutu, satir);
  }

  function onizBizimGecikmeliKapat() {
    if (onizKapatZaman) clearTimeout(onizKapatZaman);
    onizKapatZaman = setTimeout(onizBizimKapat, ONIZ_PAY);
  }

  /* Satır kurulurken bir kez bağlanır. `rosterSatir` her çağrıda YENİ düğüm
     ürettiği için dinleyiciler üst üste binmiyor. */
  /* `kaynak` ya doğrudan veri nesnesi ya da `(cb)` alan bir işlev. Kendi
     satırlarımızda roster verisi elde hazır; Kick'in satırlarında kanalın
     canlı olup olmadığını hover niyeti dolunca öğreniyoruz. */
  function onizBizimBagla(satir, kaynak) {
    var dogrudan = (typeof kaynak === 'function') ? null : kaynak;
    /* Çevrimdışı kanalda oynatılacak yayın yok; siyah kutu bozukmuş gibi
       görünürdü. Veriyi önceden biliyorsak hiç bağlamıyoruz. */
    if (dogrudan && !dogrudan.canli) return;
    satir.addEventListener('mouseenter', function () {
      if (settings.onizleme === false) return;   // ayar turda değişmiş olabilir
      if (onizKapatZaman) { clearTimeout(onizKapatZaman); onizKapatZaman = 0; }
      if (onizSatir === satir && onizKutu) return;        // zaten açık
      if (onizAcZaman) clearTimeout(onizAcZaman);
      onizAcZaman = setTimeout(function () {
        onizAcZaman = 0;
        if (dogrudan) { onizBizimAc(satir, dogrudan); return; }
        kaynak(function (v) { if (v && v.canli) onizBizimAc(satir, v); });
      }, ONIZ_GECIKME);
    });
    satir.addEventListener('mouseleave', function () {
      if (onizAcZaman) { clearTimeout(onizAcZaman); onizAcZaman = 0; }
      if (onizKutu) onizBizimGecikmeliKapat();
    });
    /* Kanala geçilince kutu ekranda kalmasın. */
    satir.addEventListener('click', onizBizimKapat);
  }

  /* KICK'İN KENDİ KENAR ÇUBUĞU SATIRLARI (önerilen kanallar). Kendi
     satırlarımızda `data-testid` bilerek yok, o yüzden bu seçici yalnız
     Kick'inkileri yakalıyor — çift bağlama olmuyor. */
  var ONIZ_BAGLI = 'data-pk-oniz-bagli';
  function onizKickBagla() {
    if (!settings.consented || settings.onizleme === false) return;
    var l = document.querySelectorAll('a[data-testid*="-channel-"]:not([' + ONIZ_BAGLI + '])');
    for (var i = 0; i < l.length; i++) {
      var a = l[i];
      a.setAttribute(ONIZ_BAGLI, '1');
      (function (a) {
        onizBizimBagla(a, function (cb) {
          /* Slug HOVER ANINDA okunuyor: React aynı düğümü başka bir kanal
             için yeniden kullanabiliyor; bağlama anında okusaydık zamanla
             yanlış kanalın yayınını gösterirdik. */
          var yol = (a.getAttribute('href') || '').split('?')[0];
          var slug = yol.replace(/^\//, '').toLowerCase();
          if (!/^[a-z0-9_-]{2,30}$/.test(slug)) return;
          kanalBilgiCek(slug, function (v) {
            if (!v) return;
            cb({ slug: slug, ad: v.ad || slug, kategori: v.kategori || '', canli: !!v.canli });
          });
        });
      })(a);
    }
  }


  function kanalBilgiCek(slug, bitince) {
    var v = kanalBilgi[slug];
    if (v === 'bekliyor') return;
    if (v) { bitince(v); return; }
    kanalBilgi[slug] = 'bekliyor';
    fetch('https://kick.com/api/v2/channels/' + encodeURIComponent(slug), { credentials: 'omit' })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (j) {
        if (!j) { kanalBilgi[slug] = { ad: slug, avatar: '', kapak: '', takipci: 0, onayli: false, canli: false, baslik: '', izleyici: 0, kategori: '', kucukResim: '' }; return; }
        var ls = j.livestream || null;
        kanalBilgi[slug] = {
          ad: (j.user && j.user.username) || slug,
          avatar: (j.user && j.user.profile_pic) || '',
          kapak: (j.banner_image && j.banner_image.url) ||
                 (j.offline_banner_image && (j.offline_banner_image.src || j.offline_banner_image.url)) || '',
          takipci: parseInt(j.followers_count, 10) || 0,
          onayli: !!j.verified,
          /* 10.5 — kenar çubuğu önizlemesi için canlı yayın bilgisi */
          canli: !!(ls && ls.is_live),
          baslik: (ls && ls.session_title) || '',
          izleyici: (ls && ls.viewer_count) || 0,
          kategori: (ls && ls.categories && ls.categories[0] && ls.categories[0].name) || '',
          /* Küçük resim: API'nin verdiği `livestream.thumbnail.url` adresi
             (stream.kick.com/thumbnails/…) tarayıcıdan çekilince 403 dönüyor —
             referrer politikasından bağımsız olarak, canlı sayfada üç ayrı
             politikayla denendi, üçü de reddedildi. Kick'in KENDİ sitesinde
             kullandığı uç ise çalışıyor ve kanal kimliğinden üretiliyor.
             Kimlik yoksa API'nin adresine düşüyoruz; o da tutmazsa görsel
             onerror ile karttan çıkarılıyor. */
          kucukResim: (ls && ls.is_live && (j.id || ls.channel_id))
            ? ('https://web.kick.com/api/v1/channels/' + (j.id || ls.channel_id) + '/thumbnail.jpeg')
            : ((ls && ls.thumbnail && (ls.thumbnail.url || ls.thumbnail.src)) || ''),
          basladi: (ls && ls.start_time) ? (Date.parse(ls.start_time) || 0) : 0
        };
        bitince(kanalBilgi[slug]);
      })
      .catch(function () { kanalBilgi[slug] = { ad: slug, avatar: '', kapak: '', takipci: 0, onayli: false, canli: false, baslik: '', izleyici: 0, kategori: '', kucukResim: '' }; });
  }

  function kanalGorselleri() {
    var slug = sayfaSlug();
    var v = kanalBilgi[slug];
    if (!v || v === 'bekliyor') return { slug: slug, ad: slug, avatar: '', kapak: '', takipci: 0, onayli: false };
    return { slug: slug, ad: v.ad, avatar: v.avatar, kapak: v.kapak, takipci: v.takipci, onayli: v.onayli };
  }

  /* Üç metrikli sütun kartı — ekran görüntüsündeki düzenin karşılığı. */
  function istatSutun(satirlar) {
    var c = el('div', 'bg-surface-base border-outline-decorative rounded border p-4 flex flex-col gap-4 flex-1 min-w-0');
    satirlar.forEach(function (p) {
      var g = el('div', 'flex flex-col gap-0.5');
      g.appendChild(el('div', 'text-xs text-subtle', p[0]));
      g.appendChild(el('div', 'text-2xl font-bold text-white', String(p[1])));
      c.appendChild(g);
    });
    return c;
  }

  function istatYorum() {
    if (scTop.mesaj < 25) return t('pkYorumVeriYok');
    var p = [];
    var mdk = istatDakikaBasi(scTop.mesaj);
    p.push(mdk >= 30 ? t('pkYorumHizli') : mdk >= 10 ? 'Sohbet rahat bir ritimde' : 'Sohbet sakin');
    var oran = scTop.mesaj ? (scTop.solo / scTop.mesaj) : 0;
    if (oran > 0.4) p.push(t('pkYorumEmoteCok'));
    else if (scTop.emote > scTop.kelime) p.push(t('pkYorumEmoteFazla'));
    else p.push(t('pkYorumDengeli'));
    if (scZirve.dk) p.push(t('pkYorumZirve', [scZirve.dk]) + ' (' + scZirve.adet + ' mesaj)');
    return p.join(', ') + '.';
  }

  function istatPencereCiz() {
    var eski = document.getElementById(ISTAT_ID);
    var kaydir = 0;
    if (eski) { var g = eski.querySelector('[data-pk-istat-govde]'); if (g) kaydir = g.scrollTop; eski.remove(); }

    var g2 = kanalGorselleri();
    /* Kanal bilgisi henüz gelmediyse iste; gelince pencereyi bir kez yeniden çiz. */
    if (g2.slug && !g2.avatar) kanalBilgiCek(g2.slug, function () { if (document.getElementById(ISTAT_ID)) istatPencereCiz(); });
    var perde = el('div', '');
    perde.id = ISTAT_ID;
    perde.style.cssText = 'position:fixed;inset:0;z-index:2147483200;background:rgba(0,0,0,.6);' +
      'display:flex;align-items:center;justify-content:center;padding:16px';
    perde.addEventListener('click', function (e) { if (e.target === perde) istatKapat(); });

    /* KABUK: Kick'in kendi diyaloğu (Abonelik Hediye Et) canlıda ölçülüp
       birebir alındı — z-dropdown · bg-surface-base · rounded 4px · shadow-lg
       · padding 16px · slideUpAndFade. Kendi ölçümüz/rengimiz yok. */
    var kart = el('div', 'z-dropdown flex h-fit flex-col rounded bg-surface-base text-sm shadow-lg ' +
      'will-change-[opacity,transform] animate-slideUpAndFade overflow-hidden');
    kart.style.cssText = 'width:920px;max-width:100%;max-height:86vh;color:#fff';

    // ---- başlık şeridi (Kick diyalog başlığıyla aynı düzen) ----
    var basSerit = el('div', 'flex items-start justify-between gap-2 shrink-0');
    basSerit.style.cssText = 'padding:16px 16px 12px';
    var basSol = el('div', 'flex flex-col gap-1 min-w-0');
    basSol.appendChild(el('div', 'text-base font-bold text-white', t('pkSohbetIstatistikleriBaslik')));
    basSol.appendChild(el('div', 'text-surface-onSurfaceSecondary text-xs',
      t('pkSohbetCikarildi')));
    basSerit.appendChild(basSol);
    var kapatBtn = el('button', 'group relative box-border flex shrink-0 grow-0 select-none items-center ' +
      'justify-center rounded state-layer-surface bg-secondary-base text-secondary-onSecondary size-9');
    kapatBtn.style.cssText = 'border:0;cursor:pointer;padding:0';
    kapatBtn.innerHTML = I_KAPAT;
    kapatBtn.title = t('pkKapat');
    kapatBtn.addEventListener('click', function (e) { e.stopPropagation(); istatKapat(); });
    basSerit.appendChild(kapatBtn);
    kart.appendChild(basSerit);

    // ---- kapak + avatar + sekmeler ----
    var kapak = el('div', 'relative shrink-0 rounded overflow-hidden');
    kapak.style.cssText = 'height:132px;margin:0 16px;background:rgb(35,38,41);' +
      (g2.kapak ? 'background-image:url(' + JSON.stringify(g2.kapak).slice(1, -1) + ');background-size:cover;background-position:center' : '');
    if (g2.avatar) {
      var av = el('img', 'rounded-full absolute');
      av.src = g2.avatar; av.referrerPolicy = 'no-referrer';
      av.style.cssText = 'width:88px;height:88px;left:16px;bottom:-28px;border:4px solid rgb(23,26,28);object-fit:cover';
      kapak.appendChild(av);
    }
    var sekmeler = el('div', 'absolute flex gap-2');
    sekmeler.style.cssText = 'right:12px;bottom:12px';
    [['ozet', t('pkSekmeGenel')], ['kisiler', t('pkSohbetEdenler')], ['mesajlar', t('pkMesajlar')]].forEach(function (p) {
      var b = el('button', (istatSekme === p[0]
        ? 'bg-primary-base text-black'
        : 'bg-secondary-base text-white hover:brightness-125') + ' h-8 px-3 rounded font-semibold text-xs', p[1]);
      b.style.cssText = 'border:0;cursor:pointer';
      b.addEventListener('click', function (e) { e.stopPropagation(); istatSekme = p[0]; istatPencereCiz(); });
      sekmeler.appendChild(b);
    });
    kapak.appendChild(sekmeler);
    kart.appendChild(kapak);

    // ---- gövde ----
    var govde = el('div', 'flex flex-col gap-4');
    govde.setAttribute('data-pk-istat-govde', '1');
    govde.style.cssText = 'padding:40px 16px 12px;overflow-y:auto;flex:1 1 auto;min-height:0';
    istatGovdeDoldur(govde);
    kart.appendChild(govde);

    // ---- alt bilgi ----
    var alt = el('div', 'border-outline-decorative flex shrink-0 items-center justify-between border-t');
    alt.style.cssText = 'padding:12px 16px';
    alt.appendChild(el('div', 'text-sm text-subtle', t('pkOturumSuresiEki', [istatSureYazi()])));
    var d = new Date();
    function ik(n) { return (n < 10 ? '0' : '') + n; }
    alt.appendChild(el('div', 'text-sm text-subtle', t('pkSonGuncellemeEki', [ik(d.getHours()) + ':' + ik(d.getMinutes())])));
    kart.appendChild(alt);

    perde.appendChild(kart);
    pkKatmanaEkle(perde);
    if (kaydir) govde.scrollTop = kaydir;
  }

  function istatGovdeDoldur(govde) {
    var slug = sayfaSlug();
    var kisi = 0; for (var _k in scListe) kisi++;

    if (istatSekme === 'ozet') {
      var bilgi = kanalGorselleri();
      var basSatir = el('div', 'flex items-baseline gap-2 flex-wrap');
      basSatir.appendChild(el('div', 'text-lg font-bold text-white',
        t('pkKanalHakkinda', [bilgi.ad || slug || ''])));
      if (bilgi.takipci) basSatir.appendChild(el('div', 'text-sm text-subtle',
        t('pkTakipciEki', [bilgi.takipci.toLocaleString()])));
      govde.appendChild(basSatir);
      govde.appendChild(el('div', 'text-sm text-subtle', istatYorum()));
      var sr = el('div', 'flex gap-4');
      sr.appendChild(istatSutun([
        ['Sohbet Eden (oturum)', kisi],
        ['Mesaj / dakika', istatDakikaBasi(scTop.mesaj)],
        [t('pkEnYogunDakika'), scZirve.dk ? t('pkMsjKisa', [String(scZirve.adet)]) : '—'],
        [t('pkYanitlarBaslik'), scTop.yanit]
      ]));
      sr.appendChild(istatSutun([
        [t('pkIzleyiciAnlik'), scIzleyici.anlik ? scIzleyici.anlik.toLocaleString('tr-TR') : '—'],
        [t('pkIzleyiciOrt'), scIzleyici.orn ? Math.round(scIzleyici.top / scIzleyici.orn).toLocaleString('tr-TR') : '—'],
        [t('pkIzleyiciZirve'), scIzleyici.zirve ? scIzleyici.zirve.toLocaleString('tr-TR') : '—'],
        [t('pkKatilim'), scIzleyici.anlik ? (Math.round((kisi / scIzleyici.anlik) * 1000) / 10) + '%' : '—']
      ]));
      sr.appendChild(istatSutun([
        ['Mesajlar', scTop.mesaj],
        ['Toplam Kelime', scTop.kelime],
        ['Toplam Emote', scTop.emote],
        [t('pkSadeceEmoteMesajlari'), scTop.solo]
      ]));
      govde.appendChild(sr);
      return;
    }

    if (istatSekme === 'kisiler') {
      govde.appendChild(el('div', 'text-lg font-bold text-white', t('pkEnAktifSohbet')));
      var liste = istatSirala(scListe, 'adet').slice(0, 30);
      if (!liste.length) { govde.appendChild(el('div', 'text-sm text-subtle', t('pkMesajYok'))); return; }
      var kap = el('div', 'flex flex-col gap-2');
      liste.forEach(function (v, i) {
        kap.appendChild(istatSiraSatir(i + 1, v.ad, v.adet + ' mesaj', scGecen(Date.now() - v.ts)));
      });
      govde.appendChild(kap);
      return;
    }

    // mesajlar
    var iki = el('div', 'flex gap-4');
    var sol = el('div', 'flex flex-col gap-2 flex-1 min-w-0');
    sol.appendChild(el('div', 'text-lg font-bold text-white', t('pkEnCokEmote')));
    var em = [];
    for (var ad in scEmoteSayac) if (Object.prototype.hasOwnProperty.call(scEmoteSayac, ad)) em.push({ ad: ad, n: scEmoteSayac[ad] });
    em.sort(function (a, b) { return b.n - a.n; });
    if (!em.length) sol.appendChild(el('div', 'text-sm text-subtle', t('pkEmoteYok')));
    else em.slice(0, 15).forEach(function (x, i) { sol.appendChild(istatSiraSatir(i + 1, x.ad, x.n + ' kez')); });

    var sag = el('div', 'flex flex-col gap-2 flex-1 min-w-0');
    sag.appendChild(el('div', 'text-lg font-bold text-white', t('pkEnCokEmoteKisi')));
    var ul = istatSirala(scEmoteKisi, 'emote').filter(function (x) { return x.emote > 0; }).slice(0, 15);
    if (!ul.length) sag.appendChild(el('div', 'text-sm text-subtle', t('pkVeriYok')));
    else ul.forEach(function (x, i) { sag.appendChild(istatSiraSatir(i + 1, x.ad, t('pkEmoteSayisi', [String(x.emote)]), x.solo + ' sadece emote')); });

    iki.appendChild(sol); iki.appendChild(sag);
    govde.appendChild(iki);

    var alt2 = el('div', 'flex gap-4');
    alt2.appendChild(istatSutun([['Mesajlar', scTop.mesaj], [t('pkYanitlarBaslik'), scTop.yanit]]));
    alt2.appendChild(istatSutun([['Toplam Kelime', scTop.kelime], ['Mesaj / dk', istatDakikaBasi(scTop.mesaj)]]));
    alt2.appendChild(istatSutun([['Toplam Emote', scTop.emote], ['Emote / dk', istatDakikaBasi(scTop.emote)]]));
    govde.appendChild(alt2);
    return;
  }

  if (istatSekme === 'bagis') {
    var bGrid = el('div', 'grid grid-cols-2 gap-3 mb-3');
    bGrid.innerHTML = `
      <div class="bg-surface-highest p-3 rounded-lg flex flex-col">
        <span class="text-xs text-subtle font-semibold">🎁 Hediye Abonelikler</span>
        <span class="text-lg font-black text-primary-base mt-1">${pkBagisVeri.toplamGiftSub} Adet</span>
      </div>
      <div class="bg-surface-highest p-3 rounded-lg flex flex-col">
        <span class="text-xs text-subtle font-semibold">🪙 Toplam Kicks</span>
        <span class="text-lg font-black text-yellow-400 mt-1">${pkBagisVeri.toplamKicks.toLocaleString('tr-TR')} Kicks</span>
      </div>
      <div class="bg-surface-highest p-3 rounded-lg flex flex-col">
        <span class="text-xs text-subtle font-semibold">📢 Blerp / Sesli Bağış</span>
        <span class="text-lg font-black text-cyan-400 mt-1">${pkBagisVeri.toplamBlerp} Adet</span>
      </div>
      <div class="bg-surface-highest p-3 rounded-lg flex flex-col">
        <span class="text-xs text-subtle font-semibold">🚀 Raid Katılımı</span>
        <span class="text-lg font-black text-pink-400 mt-1">${pkBagisVeri.toplamRaid.toLocaleString('tr-TR')} İzleyici</span>
      </div>
    `;
    govde.appendChild(bGrid);

    var lKutu = el('div', 'flex flex-col gap-2');
    lKutu.appendChild(el('div', 'text-sm font-bold text-white', '🏆 En Çok Destek Verenler:'));
    var bList = [];
    for (var uKey in pkBagisVeri.bagiscilar) {
      if (Object.prototype.hasOwnProperty.call(pkBagisVeri.bagiscilar, uKey)) {
        bList.push(Object.assign({ ad: uKey }, pkBagisVeri.bagiscilar[uKey]));
      }
    }
    bList.sort(function(a, b) {
      return ((b.giftSub * 100) + b.kicks + (b.blerp * 50)) - ((a.giftSub * 100) + a.kicks + (a.blerp * 50));
    });
    if (!bList.length) {
      lKutu.appendChild(el('div', 'text-xs text-subtle italic', 'Bu oturumda henüz bağış kaydedilmedi.'));
    } else {
      bList.slice(0, 10).forEach(function(b, idx) {
        lKutu.appendChild(istatSiraSatir(idx + 1, b.ad, (b.giftSub ? b.giftSub + ' sub ' : '') + (b.kicks ? b.kicks + ' kicks' : '')));
      });
    }
    govde.appendChild(lKutu);
  }

  function bolumIstatistik(body) {
    if (!settings.istatistik) {
      var kapali = kart(t('pkSohbetIstatistikleriBaslik'), t('pkOzellikKapali'));
      kapali.appendChild(el('div', 'text-sm text-subtle', t('pkAyarlardanAc')));
      body.appendChild(kapali); return;
    }
    if (!scKanal || !scTop.mesaj) {
      var bos = kart(t('pkSohbetIstatistikleriBaslik'), t('pkBahsedilmelerBos'));
      bos.appendChild(el('div', 'text-sm text-subtle',
        t('pkIstatBos')));
      body.appendChild(bos); return;
    }

    body.appendChild(istatSekmeCubugu());

    if (istatSekme === 'ozet') {
      var kisi = 0; for (var _k in scListe) kisi++;
      var r1 = el('div', 'flex gap-3');
      r1.appendChild(istatKutu(scTop.mesaj, 'Mesaj'));
      r1.appendChild(istatKutu(kisi, 'Sohbet eden'));
      r1.appendChild(istatKutu(istatDakikaBasi(scTop.mesaj), 'Mesaj / dk'));
      body.appendChild(r1);
      var r2 = el('div', 'flex gap-3');
      r2.appendChild(istatKutu(scTop.kelime, 'Toplam kelime'));
      r2.appendChild(istatKutu(scTop.emote, 'Toplam emote'));
      r2.appendChild(istatKutu(istatDakikaBasi(scTop.emote), 'Emote / dk'));
      body.appendChild(r2);

      /* 10.16 — Sohbet Kaydını Dışa Aktar Butonları */
      var expKart = kart('Sohbet Kaydını Dışa Aktar', 'Bu oturumda toplanan sohbet mesajlarını dosyaya indir.');
      var expBtns = el('div', 'flex gap-2 mt-2');
      var bTxt = el('button', 'px-3 py-1.5 rounded bg-surface-highest text-white font-bold text-xs hover:bg-overlay cursor-pointer', '📄 TXT İndir');
      bTxt.addEventListener('click', function() { pkSohbetExport('txt'); });
      var bCsv = el('button', 'px-3 py-1.5 rounded bg-surface-highest text-white font-bold text-xs hover:bg-overlay cursor-pointer', '📊 CSV İndir');
      bCsv.addEventListener('click', function() { pkSohbetExport('csv'); });
      expBtns.appendChild(bTxt);
      expBtns.appendChild(bCsv);
      expKart.appendChild(expBtns);
      body.appendChild(expKart);

      /* 10.18 — Canlı Sohbet Aktivite Grafiği (SVG Real-Time Line Graph) */
      if (typeof pkSohbetGrafikCiz === 'function') {
        body.appendChild(pkSohbetGrafikCiz(scDakika));
      }

      var d = kart(t('pkOturum'));
      d.appendChild(satir(t('pkSure'), t('pkIlkSonMesaj'), el('div', 'text-sm font-bold text-white', istatSureYazi())));
      d.appendChild(satir(t('pkSadeceEmote'), t('pkSadeceEmoteD'), el('div', 'text-sm font-bold text-white', String(scTop.solo))));
      d.appendChild(satir(t('pkYanit'), t('pkYanitD'), el('div', 'text-sm font-bold text-white', String(scTop.yanit))));
      if (scZirve.dk) {
        d.appendChild(satir(t('pkEnYogunDakika'), t('pkZirveDakika', [String(scZirve.dk), String(scZirve.adet)]),
          el('div', 'text-sm font-bold text-primary-base', scZirve.adet + ' msj')));
      }
      if (scIzleyici.orn) {
        d.appendChild(satir(t('pkIzleyici'), t('pkAnlikOrtZirve'),
          el('div', 'text-sm font-bold text-white',
            scIzleyici.anlik.toLocaleString('tr-TR') + ' · ' +
            Math.round(scIzleyici.top / scIzleyici.orn).toLocaleString('tr-TR') + ' · ' +
            scIzleyici.zirve.toLocaleString('tr-TR'))));
        var kat = scIzleyici.anlik ? Math.round((kisi / scIzleyici.anlik) * 1000) / 10 : 0;
        d.appendChild(satir(t('pkKatilim'), t('pkKatilimD'),
          el('div', 'text-sm font-bold text-white', kat + '%')));
      }
      body.appendChild(d);
      return;
    }

    if (istatSekme === 'kisiler') {
      var c = kart(t('pkEnAktifSohbet'), t('pkEnAktifSohbetD'));
      var liste = istatSirala(scListe, 'adet').slice(0, 25);
      var kap = el('div', 'flex flex-col gap-2');
      liste.forEach(function (v, i) {
        kap.appendChild(istatSiraSatir(i + 1, v.ad, v.adet + ' mesaj', scGecen(Date.now() - v.ts)));
      });
      c.appendChild(kap);
      body.appendChild(c);
      return;
    }

    // mesajlar sekmesi
    var e = kart(t('pkEnCokEmote'), t('pkEnCokEmoteD'));
    var em = [];
    for (var ad in scEmoteSayac) if (Object.prototype.hasOwnProperty.call(scEmoteSayac, ad)) em.push({ ad: ad, n: scEmoteSayac[ad] });
    em.sort(function (a, b) { return b.n - a.n; });
    if (!em.length) e.appendChild(el('div', 'text-sm text-subtle', t('pkEmoteYok')));
    else {
      var ek = el('div', 'flex flex-col gap-2');
      em.slice(0, 20).forEach(function (x, i) {
        var sat = istatSiraSatir(i + 1, x.ad, t('pkKezSayisi', [String(x.n)]));
        emoteResmiEkle(sat, x.ad);
        ek.appendChild(sat);
      });
      e.appendChild(ek);
    }
    if (istatSekme === 'mesajlar') {
      body.appendChild(e);

      var u = kart(t('pkEnCokEmoteKisi'), t('pkEnCokEmoteKisiD'));
      var ul = istatSirala(scEmoteKisi, 'emote').filter(function (x) { return x.emote > 0; }).slice(0, 15);
      if (!ul.length) u.appendChild(el('div', 'text-sm text-subtle', t('pkVeriYok')));
      else {
        var uk = el('div', 'flex flex-col gap-2');
        ul.forEach(function (x, i) { uk.appendChild(istatSiraSatir(i + 1, x.ad, t('pkEmoteSayisi', [String(x.emote)]), t('pkSadeceEmoteEki', [String(x.solo)]))); });
        u.appendChild(uk);
      }
      body.appendChild(u);
      return;
    }

    if (istatSekme === 'bagis') {
      var kanalAdi = scKanal || 'Yayıncı';
      var bk = kart('💰 ' + esc(kanalAdi) + ' — Bağış, Kicks & Abonelik İstatistikleri', 'Yayın boyunca gönderilen abonelikler, hediye sublar, Kicks bağışları ve sesli uyarıların tam listesi.');
      
      var toplamDestekci = Object.keys(pkBagisVeri.bagiscilar).length;
      var toplamSubSayisi = (pkBagisVeri.toplamSub || 0) + (pkBagisVeri.toplamGiftSub || 0);

      var bgGrid = el('div', 'grid grid-cols-2 gap-3 mb-3');
      bgGrid.innerHTML = `
        <div class="bg-surface-highest p-3 rounded-lg flex flex-col">
          <span class="text-xs text-subtle font-semibold">🎁 Toplam Abone & Hediye Sub</span>
          <span class="text-lg font-black text-primary-base mt-1">${toplamSubSayisi} Adet <span class="text-xs font-normal text-gray-400">(${pkBagisVeri.toplamSub} Abone + ${pkBagisVeri.toplamGiftSub} Hediye)</span></span>
        </div>
        <div class="bg-surface-highest p-3 rounded-lg flex flex-col">
          <span class="text-xs text-subtle font-semibold">🪙 Toplam Kicks Bağışı</span>
          <span class="text-lg font-black text-yellow-400 mt-1">${(pkBagisVeri.toplamKicks || 0).toLocaleString('tr-TR')} Kicks</span>
        </div>
        <div class="bg-surface-highest p-3 rounded-lg flex flex-col">
          <span class="text-xs text-subtle font-semibold">📢 Blerp & Sesli Bağış</span>
          <span class="text-lg font-black text-cyan-400 mt-1">${pkBagisVeri.toplamBlerp || 0} Adet</span>
        </div>
        <div class="bg-surface-highest p-3 rounded-lg flex flex-col">
          <span class="text-xs text-subtle font-semibold">👥 Destek Veren Kişi Sayısı</span>
          <span class="text-lg font-black text-purple-400 mt-1">${toplamDestekci} Kişi</span>
        </div>
      `;
      bk.appendChild(bgGrid);

      // En Çok Destek Verenler (Lider Tablosu)
      var lidKutu = el('div', 'flex flex-col gap-2 mt-2');
      lidKutu.appendChild(el('div', 'text-sm font-bold text-white', '🏆 En Çok Destek Verenler (Lider Tablosu):'));
      var bagisList = [];
      for (var bKey in pkBagisVeri.bagiscilar) {
        if (Object.prototype.hasOwnProperty.call(pkBagisVeri.bagiscilar, bKey)) {
          bagisList.push(Object.assign({ ad: bKey }, pkBagisVeri.bagiscilar[bKey]));
        }
      }
      bagisList.sort(function(a, b) {
        var skorA = ((a.giftSub||0)*100) + ((a.sub||0)*100) + (a.kicks||0) + ((a.bagis||0)*50) + ((a.blerp||0)*30);
        var skorB = ((b.giftSub||0)*100) + ((b.sub||0)*100) + (b.kicks||0) + ((b.bagis||0)*50) + ((b.blerp||0)*30);
        return skorB - skorA;
      });

      if (!bagisList.length) {
        lidKutu.appendChild(el('div', 'text-xs text-subtle italic', 'Bu oturumda henüz bağış veya abonelik kaydedilmedi.'));
      } else {
        var lWrap = el('div', 'flex flex-col gap-1.5 max-h-48 overflow-y-auto pr-1');
        bagisList.slice(0, 15).forEach(function(b, idx) {
          var row = el('div', 'flex items-center justify-between p-2 rounded bg-surface-highest text-xs');
          row.innerHTML = `
            <div class="flex items-center gap-2">
              <span class="font-black text-primary-base">#${idx + 1}</span>
              <span class="font-bold text-white">${esc(b.ad)}</span>
            </div>
            <div class="flex items-center gap-2 text-[11px] text-gray-300">
              ${b.sub > 0 ? '<span class="text-emerald-400 font-bold">⭐ ' + b.sub + ' sub</span>' : ''}
              ${b.giftSub > 0 ? '<span class="text-primary-base font-bold">🎁 ' + b.giftSub + ' hediye</span>' : ''}
              ${b.kicks > 0 ? '<span class="text-yellow-400 font-bold">🪙 ' + b.kicks.toLocaleString('tr-TR') + ' kicks</span>' : ''}
              ${b.bagis > 0 ? '<span class="text-green-400 font-bold">💵 $' + b.bagis + '</span>' : ''}
              ${b.blerp > 0 ? '<span class="text-cyan-400 font-bold">📢 ' + b.blerp + ' blerp</span>' : ''}
            </div>
          `;
          lWrap.appendChild(row);
        });
        lidKutu.appendChild(lWrap);
      }
      bk.appendChild(lidKutu);

      // Canlı Tarih/Saat Damgalı Bağış & Abonelik Akışı
      var akisKutu = el('div', 'flex flex-col gap-2 mt-4');
      akisKutu.appendChild(el('div', 'text-sm font-bold text-white', '📜 Tarih & Zaman Damgalı Canlı Bağış & Abonelik Akışı:'));
      if (!pkBagisVeri.gecmis.length) {
        akisKutu.appendChild(el('div', 'text-xs text-subtle italic', 'Akış bekleniyor... (Sohbetteki bağış, kicks ve abonelikler anlık kaydedilir)'));
      } else {
        var aWrap = el('div', 'flex flex-col gap-1.5 max-h-56 overflow-y-auto pr-1 text-xs');
        pkBagisVeri.gecmis.slice(-40).reverse().forEach(function(g) {
          var rozetRenk = 'bg-primary-base/20 text-primary-base border-primary-base/40';
          if (g.tip === 'Kicks') rozetRenk = 'bg-yellow-400/20 text-yellow-400 border-yellow-400/40';
          else if (g.tip === 'Abone') rozetRenk = 'bg-emerald-400/20 text-emerald-400 border-emerald-400/40';
          else if (g.tip === 'Bağış') rozetRenk = 'bg-green-400/20 text-green-400 border-green-400/40';
          else if (g.tip === 'Blerp') rozetRenk = 'bg-cyan-400/20 text-cyan-400 border-cyan-400/40';

          var item = el('div', 'flex items-center justify-between p-2 rounded bg-surface-highest/80 border border-white/5 text-xs');
          item.innerHTML = `
            <div class="flex items-center gap-2 min-w-0">
              <span class="text-[10px] text-gray-400 font-mono bg-black/40 px-1.5 py-0.5 rounded">${g.tamTarih || (g.tarih + ' ' + g.zaman)}</span>
              <span class="px-1.5 py-0.5 rounded text-[10.5px] font-extrabold border ${rozetRenk}">${esc(g.tip)}</span>
              <span class="font-bold text-white">${esc(g.user)}:</span>
              <span class="text-gray-300 truncate">${esc(g.detay)}</span>
            </div>
            <span class="font-black text-right ml-2 shrink-0 ${g.tip === 'Gift Sub' || g.tip === 'Abone' ? 'text-primary-base' : (g.tip === 'Kicks' ? 'text-yellow-400' : 'text-cyan-400')}">${esc(g.miktar)}</span>
          `;
          aWrap.appendChild(item);
        });
        akisKutu.appendChild(aWrap);
      }
      bk.appendChild(akisKutu);

      // CSV ve TXT Dışa Aktarma Butonları
      var expBar = el('div', 'flex gap-2 mt-4 pt-3 border-t border-white/10');
      
      var expCsvBtn = el('button', 'px-3 py-1.5 rounded bg-primary-base text-black font-black text-xs hover:brightness-110 cursor-pointer flex items-center gap-1.5', '📊 CSV İndir');
      expCsvBtn.addEventListener('click', function() {
        if (!pkBagisVeri.gecmis.length) { toast('İndirilecek bağış veya abonelik kaydı bulunamadı.'); return; }
        var csv = `Tarih,Saat,Kullanici,Islem_Turu,Miktar,Detay\n` + pkBagisVeri.gecmis.map(function(x){
          return `"${x.tarih || ''}","${x.zaman || ''}","${x.user || ''}","${x.tip || ''}","${x.miktar || ''}","${(x.detay||'').replace(/"/g, '""')}"`;
        }).join('\n');
        var blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
        var dl = URL.createObjectURL(blob);
        var a = document.createElement('a');
        a.href = dl;
        a.download = `kick_${kanalAdi}_bagis_kicks_${Date.now()}.csv`;
        a.click();
        toast('📊 CSV İndirildi (' + kanalAdi + ')');
      });

      var expTxtBtn = el('button', 'px-3 py-1.5 rounded bg-surface-highest text-white font-bold text-xs hover:bg-overlay cursor-pointer flex items-center gap-1.5', '📄 TXT Raporu İndir');
      expTxtBtn.addEventListener('click', function() {
        if (!pkBagisVeri.gecmis.length) { toast('İndirilecek bağış veya abonelik kaydı bulunamadı.'); return; }
        var baslik = `=====================================================\n` +
          `  ${kanalAdi.toUpperCase()} — BAĞIŞ, KICKS & ABONELİK RAPORU\n` +
          `  Tarih: ${new Date().toLocaleString('tr-TR')}\n` +
          `  Toplam Abone & Hediye Sub: ${toplamSubSayisi}\n` +
          `  Toplam Kicks: ${pkBagisVeri.toplamKicks}\n` +
          `  Toplam Destekçi: ${toplamDestekci} Kişi\n` +
          `=====================================================\n\n` +
          `[ZAMAN DAMGALI CANLI HAREKETLER]:\n`;
        var satirlar = pkBagisVeri.gecmis.map(function(x, i){
          return `${i+1}. [${x.tamTarih || (x.tarih + ' ' + x.zaman)}] [${x.tip}] ${x.user} ➔ ${x.miktar} (${x.detay})`;
        }).join('\n');
        var txt = baslik + satirlar;
        var blob = new Blob([txt], { type: 'text/plain;charset=utf-8;' });
        var dl = URL.createObjectURL(blob);
        var a = document.createElement('a');
        a.href = dl;
        a.download = `kick_${kanalAdi}_bagis_kicks_${Date.now()}.txt`;
        a.click();
        toast('📄 TXT Raporu İndirildi (' + kanalAdi + ')');
      });

      expBar.appendChild(expCsvBtn);
      expBar.appendChild(expTxtBtn);
      bk.appendChild(expBar);

      body.appendChild(bk);
      return;
    }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — BAHSEDİLMELER
   * ══════════════════════════════════════════════════════════════════════ */
  function bolumBahsedilme(body) {
    if (!settings.bahsedilme) {
      var kp = kart(t('pkBahsedilmeler'), t('pkOzellikKapali'));
      kp.appendChild(el('div', 'text-sm text-subtle', t('pkAyarlardanAc')));
      body.appendChild(kp); return;
    }
    /* AÇILIŞTA SIFIRLANMIYOR. Eskiden liste açılır açılmaz sayaç sıfırlanıyordu:
       kullanıcı PK düğmesine basıyor, rozet gözünün önünde kayboluyor ve kaç
       bahsedilme olduğunu bir daha göremiyordu. Artık "okundu" damgası
       BEKLETİLİYOR — sayfadan çıkınca uygulanıyor (bkz. pkAyarSayfaKapat).
       Böylece rozet okurken de duruyor. */
    pkMentionOkuBekle = true;

    /* BAŞLIKSIZ KART. "Senden bahsedenler" başlığı ve "Hesabın: …" satırı
       kaldırıldı (kullanıcı kararı): sayfanın kendi başlığı zaten
       "Bahsedilmeler", aynı şeyi kartın tepesinde tekrarlamak listeyi aşağı
       itiyordu. Hesap adı da bilgi vermiyordu — kullanıcı kendi adını biliyor.
       `pkKendiAdim()` çağrısı da gitti; adı çözen istek zaten her mesajda
       `pkMentionAdlari()` üzerinden tetikleniyor. */
    var c = kart(null);
    if (!pkMentionList.length) {
      c.appendChild(el('div', 'text-sm text-subtle', t('pkBahsedenYok')));
    } else {
      var liste = el('div', 'flex flex-col gap-2');
      liste.style.cssText = 'max-height:min(360px,45vh);overflow-y:auto';
      for (var i = pkMentionList.length - 1; i >= 0; i--) {
        var m = pkMentionList[i];
        var r = el('div', 'bg-surface-highest rounded px-3 py-2 flex flex-col gap-1');
        var ust = el('div', 'flex items-center gap-2');
        ust.appendChild(el('div', 'text-sm font-semibold text-primary-base', m.ad));
        ust.appendChild(el('div', 'text-xs text-subtle', scSaat(m.ts)));
        r.appendChild(ust);
        r.appendChild(mesajGovdesi(m.p, 18));
        liste.appendChild(r);
      }
      c.appendChild(liste);
    }
    body.appendChild(c);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — OYNATICI bölümü (kısayollar · ekran görüntüsü · filtreler)
   * ══════════════════════════════════════════════════════════════════════ */
  function sayiAlani(deger, alt, ust, ata) {
    var g = el('input', 'bg-surface-lower rounded text-sm text-white text-center');
    g.type = 'number'; g.min = String(alt);
    /* `max="Infinity"` geçersiz bir nitelik — sınır yoksa hiç yazmıyoruz,
       yoksa tarayıcı alanı "değer aralık dışı" sayıp doğrulamada takılıyor. */
    if (isFinite(ust)) g.max = String(ust);
    g.value = String(deger);
    /* `flex:none` — satırın sağ ucunda duruyor ve dar popout penceresinde
       (460px) esneyip iki haneye düşmesin. */
    g.style.cssText = 'padding:6px 8px;border:1px solid rgba(255,255,255,.12);' +
      'outline:none;width:84px;flex:none';
    function oku() {
      var v = omKirp(g.value, alt, ust, deger);
      g.value = String(v); deger = v; ata(v);
    }
    g.addEventListener('change', oku);
    g.addEventListener('blur', oku);
    return g;
  }

  function kaydirici(deger, min, max, adim, onDegis) {
    var kap = el('div', 'flex items-center gap-3 shrink-0');
    var g = el('input', '');
    g.type = 'range'; g.min = String(min); g.max = String(max); g.step = String(adim);
    g.value = String(deger);
    g.style.cssText = 'width:140px;accent-color:rgb(83,252,24);cursor:pointer';
    var yz = el('div', 'text-sm font-bold text-white', String(deger));
    yz.style.cssText = 'min-width:44px;text-align:right';
    g.addEventListener('input', function () { yz.textContent = g.value; });
    g.addEventListener('change', function () { onDegis(parseFloat(g.value)); });
    kap.appendChild(g); kap.appendChild(yz);
    return kap;
  }


  function bolumEkranGoruntusu(body) {
var e = kart(t('pkEkranGoruntusu'), t('pkEkranGoruntusuD'));
    /* 10.14 — özelliğin kendisi de aç/kapa oldu: kapalıyken oynatıcıdaki düğme
       de sağ tık menüsündeki satır da çıkmaz (ikisi de bu ayara bakıyor). */
e.appendChild(satir(t('pkEkranGoruntusu'), t('pkEkranGoruntusuD'),
      anahtar(settings.ekranGoruntusu !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { ekranGoruntusu: v } }); settings.ekranGoruntusu = v;
        panelCiz();
        toast(ozellikToast(v, 'pkEkranGoruntusu'));
      })));
    var eb = btnBirincil(t('pkKareyiKaydet'));
    eb.addEventListener('click', function () { ekranGoruntusuAl(); });
    var esr = el('div', 'flex gap-2');
    esr.appendChild(eb);

    /* 10.16 — Klip Alma Butonu */
    var klipBtn = el('button', 'px-3 py-2 rounded bg-surface-highest text-primary-base font-bold text-xs hover:bg-overlay cursor-pointer flex items-center gap-1.5', '🎬 30s Klip Al');
    klipBtn.addEventListener('click', function () { pkKlipAl(settings.klipSure || 30); });
    esr.appendChild(klipBtn);

    e.appendChild(esr);

    /* 11. Madde: Ekran Görüntüsü & Klip Kısayolları */
    e.appendChild(satir('Ekran Görüntüsü Kısayolu', 'Tekli veya kombinasyonlu kısayol tuşu (Varsayılan: Alt+S).',
      el('input', 'bg-surface-highest text-white text-xs px-2 py-1 rounded border border-outline-decorative w-24 text-center cursor-pointer', '', {
        value: settings.ssKisakol || 'Alt+S',
        readOnly: true,
        onclick: function () {
          this.value = 'Tuşa basın...';
          var self = this;
          var onKey = function (e) {
            e.preventDefault();
            var combo = [];
            if (e.altKey) combo.push('Alt');
            if (e.ctrlKey) combo.push('Ctrl');
            if (e.shiftKey) combo.push('Shift');
            var k = e.key.toUpperCase();
            if (['ALT', 'CONTROL', 'SHIFT'].indexOf(k) < 0) combo.push(k);
            var res = combo.join('+');
            if (res) {
              self.value = res;
              sendBg({ type: 'setSettings', payload: { ssKisakol: res } });
              settings.ssKisakol = res;
              toast('Kısayol güncellendi: ' + res);
            }
            window.removeEventListener('keydown', onKey);
          };
          window.addEventListener('keydown', onKey);
        }
      })));

    e.appendChild(satir('Klip Alma Kısayolu', 'Tekli veya kombinasyonlu klip kısayol tuşu (Varsayılan: Alt+K).',
      el('input', 'bg-surface-highest text-white text-xs px-2 py-1 rounded border border-outline-decorative w-24 text-center cursor-pointer', '', {
        value: settings.klipKisakol || 'Alt+K',
        readOnly: true,
        onclick: function () {
          this.value = 'Tuşa basın...';
          var self = this;
          var onKey = function (e) {
            e.preventDefault();
            var combo = [];
            if (e.altKey) combo.push('Alt');
            if (e.ctrlKey) combo.push('Ctrl');
            if (e.shiftKey) combo.push('Shift');
            var k = e.key.toUpperCase();
            if (['ALT', 'CONTROL', 'SHIFT'].indexOf(k) < 0) combo.push(k);
            var res = combo.join('+');
            if (res) {
              self.value = res;
              sendBg({ type: 'setSettings', payload: { klipKisakol: res } });
              settings.klipKisakol = res;
              toast('Klip kısayolu güncellendi: ' + res);
            }
            window.removeEventListener('keydown', onKey);
          };
          window.addEventListener('keydown', onKey);
        }
      })));
    e.appendChild(el('div', 'text-xs text-subtle',
      t('pkKopyaKorumasi')));
    body.appendChild(e);

    // --- ekran görüntüsü davranışı ---
    var sd = kart(t('pkSsAyarlari'), null);
    [['ssBirincil', 'pkSsBirincil', 'indir'], ['ssIkincil', 'pkSsIkincil', 'pano']].forEach(function (p) {
      var sr2 = el('div', 'flex gap-2');
      [['indir', 'pkSsIndir'], ['pano', 'pkSsPano']].forEach(function (o) {
        var secili = (settings[p[0]] || p[2]) === o[0];
        var b = el('button', 'h-9 px-3 rounded font-semibold text-sm shrink-0 transition-all ' +
          (secili ? 'bg-primary-base text-black' : 'bg-secondary-base text-white hover:brightness-125'), t(o[1]));
        b.style.cssText = 'border:0;cursor:pointer';
        b.addEventListener('click', function () {
          var y = {}; y[p[0]] = o[0];
          sendBg({ type: 'setSettings', payload: y }); settings[p[0]] = o[0]; panelCiz();
        });
        sr2.appendChild(b);
      });
      sd.appendChild(satir(t(p[1]), null, sr2));
    });
    body.appendChild(sd);
  }



  function sesYazi(k) {
    return t(k === 'kapali' ? 'pkSesKapali' : 'pkSesOto');
  }

  function bolumKalite(body) {
var kal = kart(t('pkOtomatikKalite'), t('pkOtomatikKaliteD'));
    /* 12. Madde: Sadece Ses Modu (Audio-Only) */
    kal.appendChild(satir('Sadece Ses Modu (Audio-Only)', 'Video görüntüsünü gizler, sadece sesi çalar (Bant genişliği ve pil tasarrufu sağlar).',
      anahtar(settings.sadeceSesModu === true, function (v) {
        sendBg({ type: 'setSettings', payload: { sadeceSesModu: v } }); settings.sadeceSesModu = v;
        pkSadeceSesModuUygula(); panelCiz();
        toast(ozellikToast(v, 'Sadece Ses Modu'));
      })));

    var kalSatir = el('div', 'flex flex-wrap gap-2');
    KALITE_SECENEK.forEach(function (k) {
      var secili = (settings.otoKalite || 'oto') === k;
      var b = el('button', 'h-9 px-3 rounded font-semibold text-sm shrink-0 transition-all ' +
        (secili ? 'bg-primary-base text-black' : 'bg-secondary-base text-white hover:brightness-125'),
        kaliteYazi(k));
      b.style.cssText = 'border:0;cursor:pointer';
      b.addEventListener('click', function () {
        sendBg({ type: 'setSettings', payload: { otoKalite: k } }); settings.otoKalite = k;
        /* Yeni tercih hemen denensin: eski tercihe ait "pes edildi" /
           "bu yayında yok" damgaları da kalkmalı, yoksa yeni seçim
           denenmeden eleniyordu. */
        kaliteYapildi = ''; kaliteHedefYok = ''; kaliteDeneme = 0;
        kaliteSapmaHak = 0; kaliteSapmaBasi = 0;
        panelCiz();
        if (k === 'oto') toast(t('pkTKaliteOynaticiya'));
        else { toast('Otomatik kalite: ' + kaliteYazi(k)); kaliteUygula(k, true); }
      });
      kalSatir.appendChild(b);
    });
    kal.appendChild(kalSatir);
    kal.appendChild(el('div', 'text-xs text-subtle',
      t('pkKaliteNot')));
    body.appendChild(kal);

    /* Ses — kaliteyle aynı kalıp, hemen altında. */
    var ses = kart(t('pkOtoSes'), t('pkOtoSesD'));
    var sesSatir = el('div', 'flex flex-wrap gap-2');
    SES_SECENEK.forEach(function (k) {
      /* Eski '25' gibi bir değer kalmışsa 'Otomatik' seçili görünmeli —
         uygulamada da öyle davranıyoruz. */
      var secili = ((settings.otoSes === 'kapali') ? 'kapali' : 'oto') === k;
      var b = el('button', 'h-9 px-3 rounded font-semibold text-sm shrink-0 transition-all ' +
        (secili ? 'bg-primary-base text-black' : 'bg-secondary-base text-white hover:brightness-125'),
        sesYazi(k));
      b.style.cssText = 'border:0;cursor:pointer';
      b.addEventListener('click', function () {
        sendBg({ type: 'setSettings', payload: { otoSes: k } }); settings.otoSes = k;
        sesKanal = '';                 // yeni tercih hemen denensin
        panelCiz();
        try { sesUygula(); } catch (e) {}
        toast(t('pkOtoSes') + ': ' + sesYazi(k));
      });
      sesSatir.appendChild(b);
    });
    ses.appendChild(sesSatir);
    ses.appendChild(el('div', 'text-xs text-subtle', t('pkOtoSesNot')));
    body.appendChild(ses);
  }

  function bolumBindirme(body) {
var bd = kart(t('pkBindir'), t('pkBindirD'));
    bd.appendChild(satir(t('pkBindirAc'), t('pkBindirAcD'),
      anahtar(settings.bindir === true, function (v) {
        sendBg({ type: 'setSettings', payload: { bindir: v } }); settings.bindir = v;
        if (v) bindirKur(); else bindirKapat();
        panelCiz();
        toast(ozellikToast(v, 'pkBindir'));
      })));
    if (settings.bindir === true) {
      bd.appendChild(satir(t('pkGorunurluk'), t('pkYuzde'), kaydirici(settings.bindirSaydam == null ? 90 : settings.bindirSaydam, 20, 100, 5, function (v) {
        sendBg({ type: 'setSettings', payload: { bindirSaydam: v } }); settings.bindirSaydam = v; bindirKur();
      })));
      bd.appendChild(satir(t('pkYukseklik'), t('pkVideonunYuzdesi'), kaydirici(settings.bindirBoy || 55, 20, 90, 5, function (v) {
        sendBg({ type: 'setSettings', payload: { bindirBoy: v } }); settings.bindirBoy = v; bindirKur();
      })));
      /* Köşe seçimi: dört köşeden biri. */
      var kk = el('div', 'flex flex-col gap-1');
      kk.appendChild(el('div', 'text-sm font-semibold text-white', t('pkBindirKonum')));
      var kkSatir = el('div', 'flex flex-wrap gap-2');
      [['sol-ust', 'pkKonumSolUst'], ['sag-ust', 'pkKonumSagUst'],
       ['sol-alt', 'pkKonumSolAlt'], ['sag-alt', 'pkKonumSagAlt']].forEach(function (p2) {
        var secili = (settings.bindirKonum || 'sol-alt') === p2[0];
        var b2 = el('button', 'rounded text-xs font-semibold transition-all ' +
          (secili ? 'bg-primary-base text-black' : 'bg-secondary-base text-white hover:brightness-125'),
          t(p2[1]));
        b2.style.cssText = 'padding:6px 10px;border:0;cursor:pointer';
        b2.addEventListener('click', function () {
          sendBg({ type: 'setSettings', payload: { bindirKonum: p2[0] } });
          settings.bindirKonum = p2[0];
          bindirKur(); panelCiz();
        });
        kkSatir.appendChild(b2);
      });
      kk.appendChild(kkSatir);
      bd.appendChild(kk);

      bd.appendChild(el('div', 'text-xs text-subtle',
        t('pkBindirNot')));
    }
    body.appendChild(bd);
  }

  function bolumSagTik(body) {
var st = kart(t('pkSagTikMenusu'), t('pkSagTikMenusuD'));
    st.appendChild(satir(t('pkKendiMenum'), t('pkKendiMenumD'),
      anahtar(settings.sagTik === true, function (v) {
        sendBg({ type: 'setSettings', payload: { sagTik: v } }); settings.sagTik = v;
        if (!v) sagTikKapat();
        panelCiz();
        toast(ozellikToast(v, 'pkSagTikMenusu'));
      })));
    body.appendChild(st);
  }

  function bolumCokluYayinAyar(body) {
    var c = kart(t('pkCokluYayin') || 'Çoklu Yayın (Multi-Stream)', 'Birden fazla Kick yayınını aynı sekmede bölünmüş ekranda izleyin.');
    var btn = btnBirincil('📺 Çoklu Yayın İzleme Ekranını Aç');
    btn.addEventListener('click', function () {
      panelKapat();
      pkCokluYayinAc();
    });
    var sr = el('div', 'flex mt-2');
    sr.appendChild(btn);
    c.appendChild(sr);
    body.appendChild(c);
  }

  function bolumTiyatro(body) {
var ot = kart(t('pkOtoTiyatro'), t('pkOtoTiyatroD'));
    ot.appendChild(satir(t('pkTiyatroyaAl'), t('pkTiyatroyaAlD'),
      anahtar(settings.otoTiyatro === true, function (v) {
        sendBg({ type: 'setSettings', payload: { otoTiyatro: v } }); settings.otoTiyatro = v; panelCiz();
        toast(ozellikToast(v, 'pkOtoTiyatro'));
      })));
    body.appendChild(ot);
  }


  function bolumHakkinda(body) {
    var v = '';
    try { v = chrome.runtime.getManifest().version; } catch (e) {}
    var c = kart(t('pkPureKick'), t('pkPureKickAciklama'));
    c.appendChild(el('div', 'text-sm text-subtle', t('pkSurumEki', [v])));
    var row = el('div', 'flex gap-2');
    /* Etiketler i18n'den: eskiden sabit Türkçeydi ve 7 dilin hepsinde
       Türkçe görünüyordu. Adresler `SITE_URL` sonundaki eğik çizgiyi
       tekrarlamıyor — eskiden `…cc//privacy.php` üretiliyordu. */
    [[t('pkWebSitesi'), SITE_URL],
     [t('footHelp'),    SITE_URL + 'support.php'],
     [t('footPrivacy'), SITE_URL + 'privacy.php']].forEach(function (p) {
      var a = el('a', 'bg-secondary-base hover:brightness-125 text-white h-9 px-4 rounded font-semibold text-sm inline-flex items-center', p[0]);
      a.href = p[1]; a.target = '_blank'; a.rel = 'noopener noreferrer';
      row.appendChild(a);
    });
    c.appendChild(row);
    body.appendChild(c);
  }


  /* HER SÜRÜMDE BURAYA YENİ SATIR EKLENİR. Panelde "Gelişmiş > Sürüm
     notları" bölümünde görünüyor; unutulursa kullanıcı yenilikleri hiç
     öğrenemez. 10.6 yayınlanmadı, onun özellikleri de 10.7 altında. */
  var SURUM_NOTLARI = [
    ['10.15', ['pkNot1015a', 'pkNot1015b', 'pkNot1015c', 'pkNot1015d',
               'pkNot1015e', 'pkNot1015f', 'pkNot1015g']],
    ['10.7', ['pkNot107a', 'pkNot107g', 'pkNot107b', 'pkNot107c', 'pkNot107d', 'pkNot107e', 'pkNot107f']],
    ['10.5', ['pkNot105a', 'pkNot105b', 'pkNot105c', 'pkNot105d', 'pkNot105e']],
    ['10.2', ['pkNot102a', 'pkNot102b', 'pkNot102c']],
    ['10.1', ['pkNot101a']]
  ];

  function bolumGelismis(body) {
    // --- yedek ---
    var y = kart(t('pkAyarYedegi'), t('pkAyarYedegiD'));
    var yr = el('div', 'flex flex-wrap gap-2');
    var db = btnBirincil(t('pkDisaAktar'));
    db.addEventListener('click', function () { yedekIndir(); });
    yr.appendChild(db);

    var ib = btnIkincil(t('pkIceAktar'));
    var giris = el('input', '');
    giris.type = 'file'; giris.accept = 'application/json,.json';
    giris.style.cssText = 'display:none';
    giris.addEventListener('change', function () {
      var f = giris.files && giris.files[0];
      giris.value = '';
      if (!f) return;
      if (f.size > 5 * 1024 * 1024) { toast(t('pkTYedekCokBuyuk')); return; }
      var fr = new FileReader();
      fr.onload = function () { yedekOnayAc(String(fr.result || '')); };
      fr.onerror = function () { toast(t('pkTYedekOkunamadi')); };
      fr.readAsText(f);
    });
    ib.addEventListener('click', function () { giris.click(); });
    yr.appendChild(ib);
    yr.appendChild(giris);
    y.appendChild(yr);
    y.appendChild(el('div', 'text-xs text-subtle', t('pkYedekNot')));
    body.appendChild(y);

    // --- sıfırlama ---
    var sf = kart(t('pkHersiSifirla'), t('pkHersiSifirlaD'));
    var sb = btnIkincil(t('pkHersiSifirla'));
    sb.style.setProperty('color', '#fa2d2d', 'important');
    sb.addEventListener('click', function () { sifirlaOnayAc(); });
    var sr = el('div', 'flex'); sr.appendChild(sb);
    sf.appendChild(sr);
    body.appendChild(sf);

    // --- tanıtımı yeniden göster ---
    /* Tanıtım tek sefer çıkıyor; kapattıktan sonra ulaşılacak bir yer
       olmazsa bir daha hiç görülemezdi. */
    var tn = kart(t('pkTanBaslik'), t('pkTanBaslikD'));
    var tnB = btnIkincil(t('pkTanGoster'));
    tnB.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      try { pkAyarSayfaKapat(); } catch (er) {}
      try { pkTanitimAc(true); } catch (er) {}
    });
    var tnR = el('div', 'flex'); tnR.appendChild(tnB);
    tn.appendChild(tnR);
    body.appendChild(tn);

    // --- sürüm notları ---
    var sn = kart(t('pkSurumNotlari'), t('pkSurumNotlariD'));
    SURUM_NOTLARI.forEach(function (p) {
      var blok = el('div', 'flex flex-col gap-1');
      var bas = el('div', 'flex items-center gap-2');
      var rz = el('span', 'text-xs font-bold');
      rz.style.cssText = 'background:rgb(83,252,24);color:#000;border-radius:3px;padding:1px 6px';
      rz.textContent = 'v' + p[0];
      bas.appendChild(rz);
      blok.appendChild(bas);
      p[1].forEach(function (k) {
        var r = el('div', 'flex items-start gap-2');
        var art = el('span', 'text-primary-base font-bold shrink-0', '+');
        r.appendChild(art);
        r.appendChild(el('div', 'text-sm text-subtle', t(k)));
        blok.appendChild(r);
      });
      sn.appendChild(blok);
    });
    body.appendChild(sn);
  }

  /* İçe aktarma onayı: hangi dosya, kaç anahtar, hangi yöntem. */
  function yedekOnayAc(metin) {
    var d = yedekDogrula(metin);
    if (d.hata) { toast(t(d.hata)); return; }
    var kap = pkDiyalog(t('pkIceAktar'));
    kap.govde.appendChild(el('div', 'text-sm text-subtle',
      t('pkYedekOzet', [String(d.sayi), d.tarih ? d.tarih.slice(0, 10) : '—'])));
    var sr = el('div', 'flex flex-col gap-2');
    [[t('pkBirlestir'), t('pkBirlestirD'), false], [t('pkSifirdan'), t('pkSifirdanD'), true]].forEach(function (p) {
      var b = el('button', 'bg-surface-highest hover:brightness-125 rounded text-left flex flex-col gap-1');
      b.style.cssText = 'padding:10px 12px;border:0;cursor:pointer';
      b.appendChild(el('div', 'text-sm font-semibold text-white', p[0]));
      b.appendChild(el('div', 'text-xs text-subtle', p[1]));
      b.addEventListener('click', function () {
        yedekUygula(d.veri, p[2], function (ok) {
          kap.kapat();
          if (!ok) { toast(t('pkTYedekUygulanamadi')); return; }
          toast(t('pkTYedekUygulandi'));
          setTimeout(function () { location.reload(); }, 900);
        });
      });
      sr.appendChild(b);
    });
    kap.govde.appendChild(sr);
  }

  function sifirlaOnayAc() {
    var kap = pkDiyalog(t('pkEminMisin'));
    kap.govde.appendChild(el('div', 'text-sm text-subtle', t('pkHersiSifirlaUyari')));
    var sr = el('div', 'flex gap-2');
    var ev = el('button', 'flex-1 h-9 rounded font-semibold text-sm text-white', t('pkEvetSifirla'));
    ev.style.cssText = 'background:#fa2d2d;border:0;cursor:pointer';
    ev.addEventListener('click', function () {
      hersiSifirla(function (ok) {
        kap.kapat();
        if (!ok) { toast(t('pkTSifirlanamadi')); return; }
        toast(t('pkTSifirlandi'));
        setTimeout(function () { location.reload(); }, 900);
      });
    });
    var hy = btnIkincil(t('pkIptal'));
    hy.style.flex = '1';
    hy.addEventListener('click', function () { kap.kapat(); });
    sr.appendChild(hy); sr.appendChild(ev);
    kap.govde.appendChild(sr);
  }

  /* Ortak küçük diyalog — Kick'in kendi kutu sınıflarıyla, ortalanmış. */
  var PKDIYALOG_ID = 'pk-diyalog';
  function pkDiyalog(baslik) {
    var e0 = document.getElementById(PKDIYALOG_ID); if (e0) e0.remove();
    var ort = el('div', '');
    ort.id = PKDIYALOG_ID;
    ort.style.cssText = 'position:fixed;inset:0;z-index:2147483200;background:rgba(0,0,0,.6);' +
      'display:flex;align-items:center;justify-content:center;padding:20px';
    var k = el('div', 'bg-surface-base rounded shadow-lg flex flex-col gap-3 ' +
      'will-change-[opacity,transform] animate-slideUpAndFade');
    k.style.cssText = 'width:min(420px,92vw);padding:16px;border:1px solid rgba(240,241,242,.16)';
    var bas = el('div', 'flex items-center justify-between gap-2');
    bas.appendChild(el('div', 'text-md font-bold text-white lg:font-semibold', baslik));
    var kb = el('button', 'inline-flex items-center justify-center rounded state-layer-surface text-white shrink-0');
    kb.style.cssText = 'width:24px;height:24px;padding:0;background:none;border:0;cursor:pointer';
    kb.innerHTML = I_KAPAT;
    function kapat() { if (ort.parentNode) ort.remove(); }
    kb.addEventListener('click', kapat);
    bas.appendChild(kb);
    k.appendChild(bas);
    var govde = el('div', 'flex flex-col gap-3');
    k.appendChild(govde);
    ort.appendChild(k);
    ort.addEventListener('click', function (e) { if (e.target === ort) kapat(); });
    pkKatmanaEkle(ort);
    return { kap: k, govde: govde, kapat: kapat };
  }


  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — PANEL GEZİNMESİ: İÇ İÇE MENÜ
   *
   * Özellik sayısı 57'ye çıkınca yatay sekme çubuğu taşmaya başladı ve her
   * sekme uzun bir kart yığınına dönüştü. Yerine ağaç geldi: kökte dikey
   * kategori listesi, tıklayınca alt sayfa, başlıkta geri düğmesi.
   *
   * Ağaçta her düğüm ya `bolum` (çizim fonksiyonu) taşır ya `cocuk` (alt
   * liste). İkisini birden taşıyan düğüm önce kendi kartlarını, sonra alt
   * listeyi çizer — "Sohbet" gibi hem ayarı hem alt sayfası olanlar için.
   * ══════════════════════════════════════════════════════════════════════ */
  var panelYol = [];                 // ['sohbet','kisiler'] gibi

  /* Panel menüsü ikonları — hepsi 24 viewBox, currentColor dolgulu.
     Ölçü menüde tek yerden veriliyor (menuListesiCiz), burada boyut yok. */
  var I_KALKAN = '<svg viewBox="0 0 24 24" fill="currentColor" style="display:block">' +
    '<path d="M12 2 4 5.2v6.1c0 4.6 3.2 8.9 8 10.7 4.8-1.8 8-6.1 8-10.7V5.2L12 2zm0 2.2 6 2.4v4.7c0 3.6-2.4 7-6 8.5-3.6-1.5-6-4.9-6-8.5V6.6l6-2.4z"/></svg>';
  var I_ROZET = '<svg viewBox="0 0 24 24" fill="currentColor" style="display:block">' +
    '<path d="M12 2a5.5 5.5 0 1 0 0 11 5.5 5.5 0 0 0 0-11zm0 2a3.5 3.5 0 1 1 0 7 3.5 3.5 0 0 1 0-7z"/>' +
    '<path d="M7.6 13.9 5.4 22l4.6-2.2 2 2.2 2-2.2 4.6 2.2-2.2-8.1a7.4 7.4 0 0 1-2.1 1.4L16 20l-2.3-1.1-1.7 1.8-1.7-1.8L8 20l1.7-4.7a7.4 7.4 0 0 1-2.1-1.4z"/></svg>';
  /* 10.15 — abonelik bölümü ikonu (elmas). Rozet ikonundan ayrı bir simge
     seçildi ki menüde iki bölüm birbirine karışmasın. */
  var I_ELMAS = '<svg viewBox="0 0 24 24" fill="currentColor" style="display:block">' +
    '<path d="M6.2 3h11.6a1 1 0 0 1 .8.4l2.9 4a1 1 0 0 1-.05 1.24l-8.7 10.9a1 1 0 0 1-1.56 0L2.5 8.64A1 1 0 0 1 2.45 7.4l2.9-4A1 1 0 0 1 6.2 3zm.5 2L4.9 7.5h4.02L10 5H6.7zm5.6 0-1.1 2.5h3.6L13.7 5h-1.4zm3.9 0-1.08 2.5h4.02L17.3 5h-1.1zM5.1 9.5l5.2 6.5-2.6-6.5H5.1zm4.72 0L12 15.3l2.18-5.8H9.82zm6.06 0-2.6 6.5 5.2-6.5h-2.6z"/></svg>';
  var I_HEDIYE = '<svg viewBox="0 0 24 24" fill="currentColor" style="display:block">' +
    '<path d="M20 7h-2.2a3 3 0 0 0-.4-3.4A3 3 0 0 0 12 4.3a3 3 0 0 0-5.4-.7A3 3 0 0 0 6.2 7H4a1 1 0 0 0-1 1v3a1 1 0 0 0 1 1h7V7h2v5h7a1 1 0 0 0 1-1V8a1 1 0 0 0-1-1zm-6-2a1 1 0 1 1 1 1h-1V5zm-5 0a1 1 0 0 1 2 0v1H10a1 1 0 0 1-1-1z"/>' +
    '<path d="M4 14v6a1 1 0 0 0 1 1h6v-7H4zm9 0v7h6a1 1 0 0 0 1-1v-6h-7z"/></svg>';
  var I_BILGI = '<svg viewBox="0 0 24 24" fill="currentColor" style="display:block">' +
    '<path d="M12 2a10 10 0 1 0 0 20 10 10 0 0 0 0-20zm0 2a8 8 0 1 1 0 16 8 8 0 0 1 0-16z"/>' +
    '<path d="M11 10.5h2V17h-2v-6.5zM12 6.5a1.2 1.2 0 1 1 0 2.4 1.2 1.2 0 0 1 0-2.4z"/></svg>';
  var I_BALON = '<svg viewBox="0 0 24 24" fill="currentColor" style="display:block">' +
    '<path d="M12 3c-5 0-9 3.2-9 7.2 0 2.3 1.3 4.3 3.4 5.6L5.6 21l4.5-2.5c.6.1 1.2.2 1.9.2 5 0 9-3.2 9-7.2S17 3 12 3zm0 2c4 0 7 2.4 7 5.2s-3 5.2-7 5.2c-.7 0-1.3-.1-1.9-.2l-.6-.2-2.2 1.2.4-1.9-.7-.4C5.6 13.2 5 12 5 10.7 5 7.9 8 5.5 12 5.5z"/></svg>';

  var I_ILERI = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" ' +
    'stroke-linecap="round" stroke-linejoin="round" style="width:16px;height:16px;display:block">' +
    '<path d="M9 5l7 7-7 7"/></svg>';
  var I_GERI = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" ' +
    'stroke-linecap="round" stroke-linejoin="round" style="width:18px;height:18px;display:block">' +
    '<path d="M15 5l-7 7 7 7"/></svg>';
  /* Kategori simgeleri — hepsi bizim ikon setimizden, dile bağlı değil. */

  /* ════════ SOHBET › MESAJ VURGULAMA ════════ */
  /* Rol rozetini çizer. Ölçü satır yüksekliğine göre (Kick sohbette de
     böyle yapıyor: 18/13 em). Rozeti olmayan rol için boş kap döner ki
     satırlar hizada kalsın. */
  function rolRozeti(r) {
    var kap = el('span', 'inline-flex items-center justify-center shrink-0');
    kap.style.cssText = 'width:18px;height:18px';
    var m = KICK_ROZET[r];
    if (m) {
      kap.innerHTML = m;
      var sv = kap.querySelector('svg');
      if (sv) { sv.style.width = '18px'; sv.style.height = '18px'; sv.style.display = 'block'; }
    }
    return kap;
  }

  function bolumRolRenkleri(body) {
    var kg = kart(t('pkRolVurgulama'), t('pkRolVurgulamaD'));
    /* Alt başlıkta CANLI SAYIM YOK: burası ayar yüzeyi, yayın verisi değil. */
    kg.appendChild(satir(t('pkRolVurgulama'), t('pkRolVurgulamaD'),
      anahtar(settings.rolVurgu !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { rolVurgu: v } }); settings.rolVurgu = v;
        kisiUygula(); panelCiz();
        toast(ozellikToast(v, 'pkRolVurgulama'));
      })));
if (settings.rolVurgu !== false) {
      /* Rol satırları: renk kutusu + ad + bu oturumda kaç kişi.
         DİKKAT: burada `t` adını YEREL DEĞİŞKEN olarak kullanma — çeviri
         fonksiyonunu gölgeler. Önceki sürümde `var t = ROL_RENK[r]` vardı ve
         döngü içinde t('...') çağırmak imkânsız hâle gelmişti. */
      var lej = el('div', 'flex flex-col gap-1');
      ROL_SIRASI.forEach(function (r) {
        var sat = el('div', 'flex items-center gap-3');
        sat.appendChild(rolRozeti(r));          // Kick'in kendi rozeti
        var sec = el('input', '');
        sec.type = 'color';
        sec.value = rolRengiHam(r);   // kapalı rolde de gerçek rengi göster
        sec.style.cssText = 'width:28px;height:20px;padding:0;border:0;background:none;cursor:pointer;flex-shrink:0';
        sec.addEventListener('change', function () { rolRenkAta(r, sec.value); });
        sat.appendChild(sec);
        var ad = el('div', 'text-sm text-white flex-1 min-w-0 truncate', rolAdi(r));
        sat.appendChild(ad);
        /* Her rolün kendi aç/kapası — eskiden yalnız hepsi birden kapanıyordu. */
        (function (rol) {
          sat.appendChild(anahtar(rolAcikMi(rol), function (v) {
            rolAcKapa(rol, v);
            panelCiz();
          }));
        })(r);
        lej.appendChild(sat);
      });
      kg.appendChild(lej);

      var ozelSay = 0; for (var _rr in rolRenk) ozelSay++;
      if (ozelSay) {
        var rsb = btnIkincil(t('pkRenkleriSifirla'));
        rsb.addEventListener('click', function () { rolRenkSifirla(); panelCiz(); toast(t('pkTRenklerSifirlandi')); });
        var rsr = el('div', 'flex'); rsr.appendChild(rsb);
        kg.appendChild(rsr);
      }

      kg.appendChild(satir(t('pkBotYoksay'), t('pkBotYoksayD'),
        anahtar(settings.botYoksay !== false, function (v) {
          sendBg({ type: 'setSettings', payload: { botYoksay: v } }); settings.botYoksay = v;
          kisiUygula(); panelCiz();
        })));

      /* Vurgulamadan muaf tutulanlar */
      var muafAd = Object.keys(vurguMuaf);
      var mk = el('div', 'flex flex-col gap-2');
      mk.appendChild(el('div', 'text-sm font-semibold text-white', t('pkVurguMuaf')));
      mk.appendChild(el('div', 'text-xs text-subtle', t('pkVurguMuafD')));
      var mgir = el('div', 'flex gap-2');
      var mi = el('input', 'bg-surface-highest rounded text-sm text-white flex-1 min-w-0');
      mi.type = 'text';
      mi.placeholder = t('pkKullaniciAdi');
      mi.style.cssText = 'padding:6px 10px;border:1px solid rgba(240,241,242,.16);outline:none';
      var mb = btnIkincil(t('pkEkle'));
      function muafEkleTikla() {
        if (!muafEkle(mi.value)) { mi.value = ''; return; }
        mi.value = ''; panelCiz();
      }
      mb.addEventListener('click', muafEkleTikla);
      mi.addEventListener('keydown', function (e) { if (e.key === 'Enter') { e.preventDefault(); muafEkleTikla(); } });
      mgir.appendChild(mi); mgir.appendChild(mb);
      mk.appendChild(mgir);
      if (muafAd.length) {
        var ml = el('div', 'flex flex-wrap gap-2');
        muafAd.forEach(function (a) {
          var et = el('div', 'bg-surface-highest rounded flex items-center gap-2');
          et.style.cssText = 'padding:4px 8px';
          et.appendChild(el('span', 'text-xs text-white', a));
          var cb = el('button', 'text-subtle hover:text-white transition-all', '✕');
          cb.style.cssText = 'background:none;border:0;cursor:pointer;font-size:12px;line-height:1';
          cb.addEventListener('click', function () { muafCikar(a); panelCiz(); });
          et.appendChild(cb);
          ml.appendChild(et);
        });
        mk.appendChild(ml);
      }
      kg.appendChild(mk);

      kg.appendChild(el('div', 'text-xs text-subtle', t('pkRolNot')));
    }
    body.appendChild(kg);
  }

  function bolumOncelikliKisiler(body) {
    var kg = kart(t('pkOncelikliKisiler'), t('pkOncelikliKisilerD'));
var gizliSay = 0; for (var _g in gizliKisi) gizliSay++;
    var oncSay = 0; for (var _o in oncelikli) oncSay++;
kg.appendChild(satir(t('pkOncelikliKisiler'), oncSay ? t('pkKisiOncelikliSayi', [String(oncSay)]) : t('pkOncelikliKisilerD'),
      anahtar(settings.oncelik !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { oncelik: v } }); settings.oncelik = v;
        kisiUygula(); panelCiz();
        toast(ozellikToast(v, 'pkOncelikliKisiler'));
      })));
    if (oncSay) {
      var ob = btnIkincil(t('pkOncelikTemizleN', [String(oncSay)]));
      ob.addEventListener('click', function () {
        oncelikli = Object.create(null); kisiKaydet(); kisiUygula();
        panelCiz(); toast(t('pkTOnceliklerTemiz'));
      });
      var obr = el('div', 'flex'); obr.appendChild(ob);
      kg.appendChild(obr);
    }
    body.appendChild(kg);
  }

  function bolumGizlenenKisiler(body) {
    var kg = kart(t('pkKisiGizleme'), t('pkKisiGizlemeD'));
var gizliSay = 0; for (var _g in gizliKisi) gizliSay++;
    var oncSay = 0; for (var _o in oncelikli) oncSay++;
kg.appendChild(satir(t('pkKisiGizleme'), gizliSay ? t('pkKisiGizli', [String(gizliSay)]) : t('pkKisiGizlemeD'),
      anahtar(settings.kisiGizle !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { kisiGizle: v } }); settings.kisiGizle = v;
        kisiUygula(); panelCiz();
        toast(ozellikToast(v, 'pkKisiGizleme'));
      })));
    if (gizliSay) {
      var gb = btnIkincil(t('pkGizlenenleriTemizle', [String(gizliSay)]));
      gb.addEventListener('click', function () {
        var adlar = Object.keys(gizliKisi);
        for (var i = 0; i < adlar.length; i++) kisiGoster(adlar[i]);
        panelCiz(); toast(t('pkTGizlenenlerTemiz'));
      });
      var gbr = el('div', 'flex'); gbr.appendChild(gb);
      kg.appendChild(gbr);
    }
    body.appendChild(kg);
  }

  /* ════════ SOHBET › SOHBET MESAJI ════════ */
  function bolumMesajEylemleri(body) {
    var s = kart(t('pkMesajEylemleri'), t('pkMesajEylemleriD'));
s.appendChild(satir(t('pkMesajEylemleri'), t('pkMesajEylemleriD'),
      anahtar(settings.mesajEylem !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { mesajEylem: v } }); settings.mesajEylem = v;
        if (!v) meylemKapat();
        mesajDurumUygula(); panelCiz();
      })));
var gm = 0; for (var _gm in gizliMesaj) gm++;
    if (gm) {
      var gmb = btnIkincil(t('pkGizliMesajTemizle', [String(gm)]));
      gmb.addEventListener('click', function () {
        gizliMesaj = Object.create(null);
        try { chrome.storage.local.set({ pkGizliMesaj: [] }); } catch (e) {}
        mesajDurumUygula(); panelCiz(); toast(t('pkTMesajlarGeriGeldi'));
      });
      var gmr = el('div', 'flex'); gmr.appendChild(gmb);
      s.appendChild(gmr);
    }
    body.appendChild(s);
  }

  function bolumTiklanabilirBahset(body) {
    var s = kart(t('pkBahsetTikla'), t('pkBahsetTiklaD'));
s.appendChild(satir(t('pkBahsetTikla'), t('pkBahsetTiklaD'),
      anahtar(settings.bahsetTikla !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { bahsetTikla: v } }); settings.bahsetTikla = v; panelCiz();
      })));
    body.appendChild(s);
  }

  function bolumSekmeIciBaglanti(body) {
    var s = kart(t('pkSekmeIci'), t('pkSekmeIciD'));
s.appendChild(satir(t('pkSekmeIci'), t('pkSekmeIciD'),
      anahtar(settings.sekmeIci === true, function (v) {
        sendBg({ type: 'setSettings', payload: { sekmeIci: v } }); settings.sekmeIci = v;
        /* Kapatınca açık kutuyu VE sohbetteki "Bağlantıyı Önizle" düğmelerini
           topla; açınca düğmeler hemen gelsin (tur beklenmesin). */
        if (!v) { sekmeIciKapat(); lpTemizle(); } else { try { lpTum(); } catch (e) {} }
        panelCiz();
        toast(ozellikToast(v, 'pkSekmeIci'));
      })));
    if (settings.sekmeIci === true) s.appendChild(el('div', 'text-xs text-subtle', t('pkSekmeIciNot')));
    body.appendChild(s);
  }

  /* ════════ SOHBET › AD TAKİBİ ════════ */
  function bolumBahsetAyari(body) {
    var s = kart(t('pkBahsedilmeler'), t('pkBahsedilmelerD'));
    s.appendChild(satir(t('pkBahsedilmeler'), t('pkBahsedilmelerD'),
      anahtar(settings.bahsedilme !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { bahsedilme: v } }); settings.bahsedilme = v;
        if (!v) { pkMentionSifirla(); renderBadge(); }
        panelCiz();
        toast(ozellikToast(v, 'pkBahsedilmeler'));
      })));

    /* Alt ayarlar YALNIZ özellik açıkken. Kapalıyken hiçbiri bir işe
       yaramıyor; ölü seçenek göstermek "açtım ama olmadı" sorusu doğuruyor. */
    if (settings.bahsedilme !== false) {
      /* ── TAKİP EDİLEN KELİMELER ── */
      s.appendChild(el('div', 'text-sm font-semibold text-white', t('pkTakipKelime')));
      s.appendChild(el('div', 'text-xs text-subtle', t('pkTakipKelimeD')));
      var kr = el('div', 'flex items-center gap-2');
      var kg = el('input', 'bg-surface-lower rounded text-sm text-white flex-1 min-w-0');
      kg.type = 'text';
      kg.value = takipKelimeYazi().join(', ');
      kg.placeholder = t('pkTakipKelimeOrnek');
      kg.style.cssText = 'padding:6px 8px;border:1px solid rgba(255,255,255,.12);outline:none';
      function kelimeKaydet() {
        sendBg({ type: 'setSettings', payload: { mentionAd: kg.value } });
        settings.mentionAd = kg.value;
        /* Kaydeder kaydetmez sohbete uygula: kullanıcı kelimeyi yazıp
           ekrandaki mesajların yeşile döndüğünü görmeli, F5 beklememeli. */
        try { kisiUygula(); } catch (er) {}
        panelCiz();
        toast(t('pkTKelimeKaydedildi'));
      }
      /* Enter de kaydediyor: kutuya yazıp düğmeye uzanmak fazladan bir adım. */
      kg.addEventListener('keydown', function (ev) {
        if (ev.key === 'Enter') { ev.preventDefault(); kelimeKaydet(); }
      });
      var kb = btnBirincil(t('pkKaydet'));
      kb.addEventListener('click', kelimeKaydet);
      kr.appendChild(kg); kr.appendChild(kb);
      s.appendChild(kr);

      /* Ayrıştırılmış hâli: kullanıcı ne yazdığını değil, BİZİM ne
         anladığımızı görsün — boş aralıklar, çift virgül, tekrar eden
         kelimeler burada elenmiş olarak çıkıyor. */
      var kl = takipKelimeYazi();
      if (kl.length) {
        var koniz = el('div', 'flex flex-wrap gap-1');
        kl.forEach(function (w) {
          var e2 = el('span', 'bg-surface-base rounded text-xs text-white', w);
          e2.style.cssText = 'padding:3px 7px';
          koniz.appendChild(e2);
        });
        s.appendChild(koniz);

        /* Vurgu ayarı yalnız kelime VARKEN: liste boşken açık/kapalı
           olması hiçbir şeyi değiştirmiyor. */
        s.appendChild(satir(t('pkKelimeVurgu'), t('pkKelimeVurguD'),
          anahtar(settings.kelimeVurgu !== false, function (v) {
            sendBg({ type: 'setSettings', payload: { kelimeVurgu: v } }); settings.kelimeVurgu = v;
            try { kisiUygula(); } catch (er) {}
            panelCiz();
            toast(ozellikToast(v, 'pkKelimeVurgu'));
          })));
      }

      s.appendChild(satir(t('pkBahsetRozet'), t('pkBahsetRozetD'),
        anahtar(settings.bahsetRozet !== false, function (v) {
          sendBg({ type: 'setSettings', payload: { bahsetRozet: v } }); settings.bahsetRozet = v;
          panelCiz();
          toast(ozellikToast(v, 'pkBahsetRozet'));
        })));

      /* Ses VARSAYILAN KAPALI: sorulmadan ses çıkaran bir özellik rahatsız
         edici. Açan kullanıcı ne olduğunu bilerek açıyor. */
      s.appendChild(satir(t('pkBahsetSes'), t('pkBahsetSesD'),
        anahtar(settings.bahsetSes === true, function (v) {
          sendBg({ type: 'setSettings', payload: { bahsetSes: v } }); settings.bahsetSes = v;
          panelCiz();
          /* Açar açmaz bir kez duyuyor: hem neye benzediğini öğreniyor hem de
             sesin gerçekten çıktığını (sekme sessize alınmamış) doğruluyor. */
          if (v) { try { pkBildirimSesi(); } catch (e) {} }
          toast(ozellikToast(v, 'pkBahsetSes'));
        })));

      if (settings.bahsetSes === true) {
        s.appendChild(el('div', 'text-sm font-semibold text-white', t('pkBahsetSesTon')));
        s.appendChild(el('div', 'text-xs text-subtle', t('pkBahsetSesTonD')));
        var ss = el('div', 'flex flex-wrap gap-2');
        PK_SES_SECENEK.forEach(function (k) {
          var secili = pkSesTonu() === k;
          var b = el('button', 'h-9 px-3 rounded font-semibold text-sm shrink-0 transition-all ' +
            (secili ? 'bg-primary-base text-black' : 'bg-secondary-base text-white hover:brightness-125'),
            pkSesAdi(k));
          b.style.cssText = 'border:0;cursor:pointer';
          /* Tıklama hem SEÇİYOR hem ÇALIYOR. Ayrı bir "dinle" düğmesi iki
             tıklama demekti; ses zaten seçer seçmez duyulmalı. */
          b.addEventListener('click', function () {
            sendBg({ type: 'setSettings', payload: { bahsetSesTon: k } }); settings.bahsetSesTon = k;
            panelCiz();
            try { pkBildirimSesi(k); } catch (e) {}
          });
          ss.appendChild(b);
        });
        s.appendChild(ss);

        /* Seviye ayrı: kimi kullanıcı sesi istiyor ama yayının üstüne
           binmesini istemiyor. Her değişimde çalıyor ki kulakla ayarlansın. */
        s.appendChild(satir(t('pkBahsetSesSeviye'), '',
          kaydirici(pkSesSeviyesi(), 0, 100, 5, function (v) {
            sendBg({ type: 'setSettings', payload: { bahsetSesSeviye: v } });
            settings.bahsetSesSeviye = v;
            try { pkBildirimSesi(null, v); } catch (e) {}
          })));
      }
    }
    body.appendChild(s);
  }

  /* ════════ SOHBET › GÖRÜNÜM ════════ */
  function bolumSohbetGorunum(body) {
    /* Sohbet kuralları onayı — kanal bilgisiyle değil sohbetle ilgili. */
    var kk = kart(t('pkKuralKabul'), t('pkKuralKabulD'));
    kk.appendChild(satir(t('pkKuralKabul'), t('pkKuralKabulD'),
      anahtar(settings.kuralKabul === true, function (v) {
        sendBg({ type: 'setSettings', payload: { kuralKabul: v } }); settings.kuralKabul = v;
        if (v) { kuralYol = ''; kuralKabulUygula(); }
        panelCiz();
        toast(ozellikToast(v, 'pkKuralKabul'));
      })));
    body.appendChild(kk);
    var s = kart(t('pkAltSohbetGorunum'), t('pkAltSohbetGorunumD'));
    s.appendChild(satir(t('pkEmoteOnizleme'), t('pkEmoteOnizlemeD'),
      anahtar(settings.emoteOniz !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { emoteOniz: v } }); settings.emoteOniz = v;
        if (!v) emoteKapat();
        panelCiz();
        toast(ozellikToast(v, 'pkEmoteOnizleme'));
      })));

    /* 1. Madde: Emote Önizleme Boyut Seçici (16px - 100px) */
    if (settings.emoteOniz !== false) {
      s.appendChild(satir('Emote Önizleme Boyutu', 'Emote üzerine gelindiğinde açılan önizlemenin boyutu (piksel).',
        kaydirici(settings.emoteBoyut || 48, 16, 100, 4, function (val) {
          sendBg({ type: 'setSettings', payload: { emoteBoyut: val } });
          settings.emoteBoyut = val;
        })));
    }

    /* 10.16 — Sohbet İçi Canlı Arama & Filtre */
    s.appendChild(satir(t('pkSohbetFiltre') || 'Sohbet Arama & Filtre Çubuğu', 'Sohbetin üstüne anlık kelime ve kullanıcı filtreleme çubuğu ekler.',
      anahtar(settings.sohbetFiltre !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { sohbetFiltre: v } }); settings.sohbetFiltre = v;
        pkSohbetFiltreKutusuKur(); panelCiz();
      })));

    /* 10.16 — Emote Spam Küçültücü */
    s.appendChild(satir(t('pkEmoteSpamFiltre') || 'Emote Spam Küçültücü', 'Arka arkaya atılan aynı emote mesajlarını tek satırda x15 rozetiyle birleştirir.',
      anahtar(settings.emoteSpamFiltre !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { emoteSpamFiltre: v } }); settings.emoteSpamFiltre = v;
        panelCiz();
      })));

    /* 10.16 — Sohbet TTS (Text-to-Speech) */
    s.appendChild(satir(t('pkSohbetTts') || 'Sohbet Metin Okuma (TTS)', 'Sohbette senden bahsedilince mesajı sesli olarak okur.',
      anahtar(settings.sohbetTts === true, function (v) {
        sendBg({ type: 'setSettings', payload: { sohbetTts: v } }); settings.sohbetTts = v;
        panelCiz();
      })));

    /* 10.17 — Özel Sohbet Sekmeleri */
    s.appendChild(satir('Özel Sohbet Sekmeleri', 'Sohbetin üstüne [Tümü] [@Bahsetmeler] [Mod] [Sorular] filtre sekmelerini ekler.',
      anahtar(settings.sohbetSekmeler !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { sohbetSekmeler: v } }); settings.sohbetSekmeler = v;
        pkSohbetSekmeleriKur(); panelCiz();
      })));

    /* 10.17 — Sohbet Dondurma (Scroll-Lock) */
    s.appendChild(satir('Sohbeti Dondur (Scroll-Lock)', 'Fare sohbetin üzerindeyken yeni mesajların sayfayı otomatik aşağı kaydırmasını durdurur.',
      anahtar(settings.sohbetDondur !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { sohbetDondur: v } }); settings.sohbetDondur = v;
        panelCiz();
      })));

    /* 10.17 — Şüpheli Link & Phishing Kalkanı */
    s.appendChild(satir('Şüpheli Link & Dolandırıcılık Kalkanı', 'Sohbette paylaşılan sahte ve tehlikeli linkleri kırmızı uyarı rozetiyle işaretler.',
      anahtar(settings.guvenliLinkKalkan !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { guvenliLinkKalkan: v } }); settings.guvenliLinkKalkan = v;
        panelCiz();
      })));

    /* 10.17 — OLED True Black Modu */
    s.appendChild(satir('OLED True Black Modu', 'Arayüzü zifiri siyah (#000000) yaparak OLED ekranlarda mükemmel kontrast ve pil tasarrufu sağlar.',
      anahtar(settings.oledMod === true, function (v) {
        sendBg({ type: 'setSettings', payload: { oledMod: v } }); settings.oledMod = v;
        pkOledModUygula(); panelCiz();
      })));

    /* 10.18 — Anlık & Otomatik Sohbet Çevirisi (Birbirini Dışlayan Seçim & 25 Dil) */
    s.appendChild(satir('Anlık Sohbet Çevirisi (🌐)', 'Yabancı mesajların sonuna tek tıkla çeviren buton ekler (Otomatik çeviri kapalıyken çalışır).',
      anahtar(settings.sohbetCeviri === true && !settings.otomatikCevir, function (v) {
        if (v) {
          sendBg({ type: 'setSettings', payload: { sohbetCeviri: true, otomatikCevir: false } });
          settings.sohbetCeviri = true;
          settings.otomatikCevir = false;
          toast('Anlık butonla çeviri modu açıldı');
        } else {
          sendBg({ type: 'setSettings', payload: { sohbetCeviri: false } });
          settings.sohbetCeviri = false;
          toast('Anlık çeviri kapatıldı');
        }
        pkSohbetCeviriButonlariKur();
        panelCiz();
      })));

    s.appendChild(satir('Otomatik Sohbet Çevirisi', 'Sohbete yeni gelen tüm yabancı mesajları otomatik algılayıp seçtiğiniz dile anında çevirir.',
      anahtar(settings.otomatikCevir === true, function (v) {
        if (v) {
          sendBg({ type: 'setSettings', payload: { otomatikCevir: true, sohbetCeviri: false } });
          settings.otomatikCevir = true;
          settings.sohbetCeviri = false;
          toast('Otomatik çeviri modu açıldı');
        } else {
          sendBg({ type: 'setSettings', payload: { otomatikCevir: false } });
          settings.otomatikCevir = false;
          toast('Otomatik çeviri kapatıldı');
        }
        pkSohbetCeviriButonlariKur();
        panelCiz();
      })));

    /* 2. Madde: 25 Dil Seçici */
    var dilWrap = el('div', 'flex flex-wrap gap-1.5 mt-1 mb-2 max-h-32 overflow-y-auto p-1 bg-surface-highest rounded');
    PK_DILLER_25.forEach(function (d) {
      var secili = (settings.ceviriDili || 'tr') === d.k;
      var btn = el('button', 'px-2 py-1 rounded text-xs font-bold cursor-pointer transition-all flex items-center gap-1 ' +
        (secili ? 'bg-primary-base text-black' : 'bg-surface-base text-subtle hover:text-white'), d.b + ' ' + d.a);
      btn.addEventListener('click', function () {
        sendBg({ type: 'setSettings', payload: { ceviriDili: d.k, hedefDil: d.k } });
        settings.ceviriDili = d.k;
        settings.hedefDil = d.k;
        toast('Çeviri hedef dili: ' + d.b + ' ' + d.a);
        panelCiz();
      });
      dilWrap.appendChild(btn);
    });
    s.appendChild(satir('Hedef Çeviri Dili (25 Dil)', 'Mesajların çevrileceği hedef lisan.', dilWrap));

    /* 15. Madde: Aydınlık Tema (Beyaz/Yeşil) */
    s.appendChild(satir('Aydınlık Tema (Beyaz / Yeşil)', 'Arayüzü beyaz ve açık gri renklere dönüştürür.',
      anahtar(settings.aydinlikTema === true, function (v) {
        sendBg({ type: 'setSettings', payload: { aydinlikTema: v } }); settings.aydinlikTema = v;
        pkAydinlikTemaUygula(); panelCiz();
      })));

    /* 18. Madde: Sohbet Duygu & Hype Analizi */
    s.appendChild(satir('Sohbet Duygu & Canlı Hype Analizi (Hype Meter)', 'Sohbetteki coşku, mizah ve duygu durumunu canlı olarak analiz eder ve üst bar olarak gösterir.',
      anahtar(settings.hypeOlcer !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { hypeOlcer: v } }); settings.hypeOlcer = v;
        pkSohbetHypeMeterKur(); panelCiz();
        toast(v ? 'Hype Ölçer açıldı' : 'Hype Ölçer kapatıldı');
      })));

    body.appendChild(s);

    /* 10.14 — Kick'in izleyici listesine eklediklerimiz artık aç/kapa. */
    var sl = kart(t('pkSohbetciListesi'), t('pkSohbetciListesiD'));
sl.appendChild(satir(t('pkScSayac'), t('pkScSayacD'),
      anahtar(settings.scSayac !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { scSayac: v } }); settings.scSayac = v;
        try { sohbetciSayac(); } catch (e) {}      // kapandıysa Kick'in sayısını geri koyar
        panelCiz();
        toast(ozellikToast(v, 'pkScSayac'));
      })));
sl.appendChild(satir(t('pkScEtiket'), t('pkScEtiketD'),
      anahtar(settings.scEtiket !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { scEtiket: v } }); settings.scEtiket = v;
        try { sohbetciEtiketle(); } catch (e) {}   // kapandıysa etiketleri toplar
        panelCiz();
        toast(ozellikToast(v, 'pkScEtiket'));
      })));
sl.appendChild(satir(t('pkScFiltre'), t('pkScFiltreD'),
      anahtar(settings.scFiltreCubugu !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { scFiltreCubugu: v } }); settings.scFiltreCubugu = v;
        try { sohbetciFiltreCiz(); sohbetciEtiketle(); } catch (e) {}
        panelCiz();
        toast(ozellikToast(v, 'pkScFiltre'));
      })));
sl.appendChild(satir(t('pkYanSohbet'), t('pkYanSohbetD'),
      anahtar(settings.yanSohbet !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { yanSohbet: v } }); settings.yanSohbet = v;
        try { yanSohbetKur(); } catch (e) {}       // kapandıysa taşımayı geri alır
        panelCiz();
        toast(ozellikToast(v, 'pkYanSohbet'));
      })));
    body.appendChild(sl);
  }

  function bolumIstatAyari(body) {
    var s = kart(t('pkSohbetIstatistikleri'), t('pkSohbetIstatistikleriD'));
s.appendChild(satir(t('pkSohbetIstatistikleri'), t('pkSohbetIstatistikleriD'),
      anahtar(settings.istatistik !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { istatistik: v } }); settings.istatistik = v; panelCiz();
        toast(ozellikToast(v, 'pkSohbetIstatistikleri'));
      })));
    body.appendChild(s);
  }

  function bolumIzlemeSureleri(body) {
    var c = kart('İzleme Süreleri & Sadakat', 'Hangi yayıncıyı ne kadar süre izlediğinizi takip edin.');
    
    var container = el('div', 'flex flex-col gap-2 mt-3');
    chrome.storage.local.get('pkIzlemeSureler', function (res) {
      var sureler = res.pkIzlemeSureler || {};
      var kanallar = Object.keys(sureler).sort(function (a, b) { return (sureler[b] || 0) - (sureler[a] || 0); });
      
      if (!kanallar.length) {
        container.appendChild(el('div', 'text-sm text-subtle py-2', 'Henüz izleme süresi kaydedilmedi. Bir yayına girdiğinizde süreler burada listelenecektir.'));
      } else {
        kanallar.forEach(function (k) {
          var dk = Math.round(sureler[k] || 0);
          var saatYazi = dk >= 60 ? (Math.floor(dk / 60) + ' sa ' + (dk % 60) + ' dk') : (dk + ' dk');
          var row = el('div', 'bg-surface-highest rounded px-3 py-2 flex items-center justify-between gap-3');
          row.innerHTML = '<div class="flex items-center gap-2">' +
            '<span class="font-bold text-white text-sm">' + esc(k) + '</span>' +
          '</div>' +
          '<div class="text-xs font-bold text-primary-base bg-surface-base px-2.5 py-1 rounded">' + esc(saatYazi) + '</div>';
          container.appendChild(row);
        });

        var bTemizle = btnIkincil('Süreleri Sıfırla');
        bTemizle.classList.add('mt-2', 'self-start');
        bTemizle.addEventListener('click', function () {
          chrome.storage.local.set({ pkIzlemeSureler: {} }, function () {
            toast('İzleme süreleri sıfırlandı');
            panelCiz();
          });
        });
        container.appendChild(bTemizle);
      }
    });

    c.appendChild(container);
    body.appendChild(c);
  }

  /* ════════ KULLANICI KARTI › NOTLAR ════════ */
  function bolumKartNotlar(body) {
    var s = kart(t('pkKullaniciNotlari'), t('pkKullaniciNotlariD'));
s.appendChild(satir(t('pkKullaniciNotlari'), t('pkKullaniciNotlariD'),
      anahtar(settings.notlar !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { notlar: v } }); settings.notlar = v; panelCiz();
        toast(ozellikToast(v, 'pkKullaniciNotlari'));
      })));
    body.appendChild(s);
  }


  /* ════════ KULLANICI KARTI ════════ */
  function bolumKartBilgisi(body) {
    var pb = kart(t('pkAltKartBilgisi'), t('pkProfilKartiBilgisiD'));
pb.appendChild(satir(t('pkKatilmaTakip'), t('pkKatilmaTakipD'),
      anahtar(settings.kartBilgi !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { kartBilgi: v } }); settings.kartBilgi = v; panelCiz();
        toast(ozellikToast(v, 'pkProfilKartiBilgisi'));
      })));
pb.appendChild(el('div', 'text-xs text-subtle',
      t('pkKartBilgiNot')));
pb.appendChild(satir(t('pkKartTakipci'), t('pkKartTakipciD'),
      anahtar(settings.kartTakipci === true, function (v) {
        sendBg({ type: 'setSettings', payload: { kartTakipci: v } }); settings.kartTakipci = v;
        processProfilePopups(); panelCiz();
      })));
    pb.appendChild(satir(t('pkKartGenis'), t('pkKartGenisD'),
      anahtar(settings.kartGenis !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { kartGenis: v } }); settings.kartGenis = v;
        /* Kapatınca ölçüyü GERİ ALMAK için işaretli kartları bir tur gezdir. */
        processProfilePopups(); panelCiz();
        toast(ozellikToast(v, 'pkKartGenis'));
      })));

    /* 13. GELİŞMİŞ PROFİL KARTI & KOLEKSİYON TEMASI SEÇİCİ */
    var koleksiyonDosyalari = [];
    for (var ki = 1; ki <= 81; ki++) {
      var ext = (ki >= 1 && ki <= 4) || ki === 20 ? '.jpg' : '.webp';
      koleksiyonDosyalari.push('card_' + ki + ext);
    }

    var holoKartSecenekleri = [
      { id: 'varsayilan', ad: '🚫 Efektsiz (Orijinal / Varsayılan)' },
      { id: 'pikachu', ad: '⚡ Pikachu Spark' },
      { id: 'neon', ad: '🔮 Neon Cyber' },
      { id: 'galaxy', ad: '🌌 Galaxy Cosmos' },
      { id: 'ruby', ad: '🔥 Ruby Fire' },
      { id: 'emerald', ad: '💎 Emerald Shine' },
      { id: 'diamond', ad: '❄️ Diamond Prism' },
      { id: 'gold', ad: '👑 Golden Prestige' },
      { id: 'dark', ad: '🌑 Dark Void' },
      { id: 'rainbow', ad: '🌈 Rainbow Holo' },
      { id: 'liquid', ad: '💧 Liquid Glass' },
      { id: 'elektrik', ad: '⚡ Elektrik Akımı' },
      { id: 'simsek', ad: '🌩️ Fırtına Şimşek' },
      { id: 'alev', ad: '🔥 Cehennem Alevi' },
      { id: 'sis', ad: '🌫️ Gizemli Sis & Duman' },
      { id: 'su', ad: '🌊 Okyanus Dalgası' },
      { id: 'golge', ad: '👤 Karanlık Gölge' },
      { id: 'bulut', ad: '☁️ Gök Kubbe & Bulut' },
      { id: 'buz', ad: '❄️ Kristal Donma' },
      { id: 'kan', ad: '🩸 Kan Aurası' },
      { id: 'sakura', ad: '🌸 Sakura Esintisi' },
      { id: 'karadelik', ad: '🪐 Kozmik Karadelik' },
      { id: 'zehir', ad: '💚 Zehir & Asit' },
      { id: 'gunes', ad: '☀️ Güneş Plazması' },
      { id: 'prizma', ad: '💎 Prizmatik Kırılma' },
      { id: 'run', ad: '🔮 Büyücü Rünü' }
    ];

    var kWrap = el('div', 'flex flex-col gap-3 mt-4 border-t border-outline-decorative pt-4');
    kWrap.appendChild(el('div', 'text-base font-bold text-white', '🎨 Profil Kartı & Koleksiyon Teması Özelleştirme'));
    
    // Açıklayıcı Bilgi / Rehber Kutusu
    var rehberKutu = el('div', 'bg-surface-highest/60 border border-outline-decorative p-3 rounded-lg flex flex-col gap-1.5');
    rehberKutu.innerHTML = `
      <div class="text-xs font-bold text-primary-base">💡 Kendi Profil Kartınızı Nasıl Özelleştirirsiniz?</div>
      <div class="text-[11.5px] text-subtle leading-relaxed">
        1. <b>🎴 Dahili Koleksiyon:</b> 81 adet Pokemon, Dragon Ball, One Piece kartından birini tek tıkla seçebilirsiniz.<br>
        2. <b>📁 Kendi Dosyanı Yükle:</b> Bilgisayarınızdan istediğiniz resim veya animasyonlu GIF dosyasını doğrudan karta aktarabilirsiniz.<br>
        3. <b>🔗 Harici Link:</b> Imgur, Tenor veya özel görsel/GIF linkinizi yapıştırabilirsiniz.<br>
        4. <b>✨ 25 Canlı Efekt:</b> Aşağıdaki 25 Doğa, Element ve Büyü animasyonundan birini seçerek kartınıza ve isminize canlı parıltı kazandırın.
      </div>
    `;
    kWrap.appendChild(rehberKutu);

    // Kart Tipi Seçimi (3 Sekme: Koleksiyon / Kendi Dosyan / Özel URL)
    var tipSecici = el('div', 'flex flex-wrap gap-2');
    var curTip = settings.kartTipi || 'koleksiyon';
    
    var btnKoleksiyon = el('button', 'px-3 py-1.5 rounded text-xs font-bold cursor-pointer transition-all ' + 
      (curTip === 'koleksiyon' ? 'bg-primary-base text-black' : 'bg-surface-highest text-subtle hover:text-white'), '🎴 Dahili Koleksiyon (81 Kart)');
    var btnYerel = el('button', 'px-3 py-1.5 rounded text-xs font-bold cursor-pointer transition-all ' + 
      (curTip === 'yerel' ? 'bg-primary-base text-black' : 'bg-surface-highest text-subtle hover:text-white'), '📁 Kendi Dosyanı Yükle (Resim/GIF)');
    var btnOzel = el('button', 'px-3 py-1.5 rounded text-xs font-bold cursor-pointer transition-all ' + 
      (curTip === 'ozel' ? 'bg-primary-base text-black' : 'bg-surface-highest text-subtle hover:text-white'), '🔗 Özel Link (Görsel/GIF)');

    tipSecici.appendChild(btnKoleksiyon);
    tipSecici.appendChild(btnYerel);
    tipSecici.appendChild(btnOzel);
    kWrap.appendChild(tipSecici);

    btnKoleksiyon.addEventListener('click', function () {
      sendBg({ type: 'setSettings', payload: { kartTipi: 'koleksiyon' } });
      settings.kartTipi = 'koleksiyon';
      panelCiz();
    });
    btnYerel.addEventListener('click', function () {
      sendBg({ type: 'setSettings', payload: { kartTipi: 'yerel' } });
      settings.kartTipi = 'yerel';
      panelCiz();
    });
    btnOzel.addEventListener('click', function () {
      sendBg({ type: 'setSettings', payload: { kartTipi: 'ozel' } });
      settings.kartTipi = 'ozel';
      panelCiz();
    });

    if (curTip === 'yerel') {
      var yerelKutu = el('div', 'flex flex-col gap-2.5 bg-surface-highest p-3.5 rounded-lg');
      yerelKutu.appendChild(el('div', 'text-xs font-bold text-white', 'Bilgisayarınızdan Resim veya Hareketli GIF Yükleyin:'));
      
      var fileInp = document.createElement('input');
      fileInp.type = 'file';
      fileInp.accept = 'image/png, image/jpeg, image/webp, image/gif';
      fileInp.style.display = 'none';

      var uploadBtn = el('button', 'flex items-center justify-center gap-2 bg-primary-base text-black font-bold text-xs py-2 px-4 rounded cursor-pointer hover:brightness-110 transition-all shadow-md', '📁 Bilgisayardan Dosya Seç (PNG, JPG, GIF)');
      uploadBtn.addEventListener('click', function () {
        fileInp.click();
      });

      fileInp.addEventListener('change', function (e) {
        var file = e.target.files && e.target.files[0];
        if (!file) return;
        if (file.size > 8 * 1024 * 1024) {
          toast('Dosya boyutu çok büyük (Maksimum 8 MB)');
          return;
        }
        var reader = new FileReader();
        reader.onload = function (evt) {
          var b64 = evt.target.result;
          sendBg({ type: 'setSettings', payload: { profilKartBase64: b64, kartTipi: 'yerel' } });
          settings.profilKartBase64 = b64;
          settings.kartTipi = 'yerel';
          toast('Görsel başarıyla profil kartınıza yüklendi!');
          panelCiz();
        };
        reader.readAsDataURL(file);
      });
      yerelKutu.appendChild(fileInp);
      yerelKutu.appendChild(uploadBtn);

      if (settings.profilKartBase64) {
        var prevWrap = el('div', 'flex items-center gap-3 mt-2 bg-surface-base p-2 rounded');
        prevWrap.innerHTML = `
          <div class="text-xs text-subtle font-semibold">Yüklü Görsel Önizlemesi:</div>
          <img src="${settings.profilKartBase64}" style="width:64px;height:64px;object-fit:cover;border-radius:6px;border:1px solid #53fc18;" />
        `;
        yerelKutu.appendChild(prevWrap);
      }
      kWrap.appendChild(yerelKutu);
    } else if (curTip === 'ozel') {
      var ozelKutu = el('div', 'flex flex-col gap-2 bg-surface-highest p-3 rounded-lg');
      ozelKutu.appendChild(el('div', 'text-xs font-bold text-white', 'Dışarıdan Resim veya Animasyonlu GIF Linki:'));
      var ozelInp = el('input', 'bg-surface-base border border-outline-decorative text-white text-xs px-3 py-2 rounded w-full', '', {
        placeholder: 'https://i.imgur.com/example.gif veya Görsel URL...',
        value: settings.ozelKartUrl || ''
      });
      ozelInp.addEventListener('change', function (e) {
        var u = e.target.value.trim();
        sendBg({ type: 'setSettings', payload: { ozelKartUrl: u, kartTipi: 'ozel' } });
        settings.ozelKartUrl = u;
        settings.kartTipi = 'ozel';
        toast('Özel profil kartı görseli kaydedildi');
        panelCiz();
      });
      ozelKutu.appendChild(ozelInp);
      kWrap.appendChild(ozelKutu);
    } else {
      var galeriKutu = el('div', 'flex flex-col gap-2 bg-surface-highest p-3 rounded-lg');
      galeriKutu.appendChild(el('div', 'text-xs font-bold text-white', 'Koleksiyon Kartı Seçin (81 Hazır Şablon - Tıklayarak Seçin):'));
      var grid = el('div', 'grid grid-cols-4 gap-2.5 max-h-56 overflow-y-auto pr-1');
      koleksiyonDosyalari.forEach(function (dosya) {
        var imgUrl = chrome.runtime.getURL('profil-kartlari/' + dosya);
        var secili = (settings.koleksiyonKartDosya || 'card_1.jpg') === dosya;
        var item = el('div', 'relative rounded overflow-hidden cursor-pointer border-2 transition-all ' + 
          (secili ? 'border-primary-base scale-105 shadow-lg shadow-primary-base/20' : 'border-transparent hover:border-white opacity-80 hover:opacity-100'));
        item.style.height = '75px';
        item.innerHTML = '<img src="' + imgUrl + '" loading="lazy" style="width:100%;height:100%;object-fit:cover;" />';
        item.addEventListener('click', function () {
          sendBg({ type: 'setSettings', payload: { koleksiyonKartDosya: dosya, kartTipi: 'koleksiyon' } });
          settings.koleksiyonKartDosya = dosya;
          settings.kartTipi = 'koleksiyon';
          toast('Koleksiyon kartı seçildi: ' + dosya);
          panelCiz();
        });
        grid.appendChild(item);
      });
      galeriKutu.appendChild(grid);
      kWrap.appendChild(galeriKutu);
    }

    // Holo & Canlı İsim Animasyon Efekti Seçici (25 Efekt)
    var holoWrap = el('div', 'flex flex-col gap-1.5 mt-2');
    holoWrap.appendChild(el('div', 'text-sm font-bold text-white', 'Koleksiyon Kart & İsim Holo Teması (25 Özel Efekt)'));
    var holoBtns = el('div', 'flex flex-wrap gap-1.5 max-h-48 overflow-y-auto pr-1');
    holoKartSecenekleri.forEach(function (h) {
      var secili = (settings.holoKartEfekt || 'pikachu') === h.id;
      var btn = el('button', 'px-2.5 py-1 rounded text-xs font-bold cursor-pointer transition-all ' + 
        (secili ? 'bg-primary-base text-black' : 'bg-surface-highest text-subtle hover:text-white'), h.ad);
      btn.addEventListener('click', function () {
        sendBg({ type: 'setSettings', payload: { holoKartEfekt: h.id } });
        settings.holoKartEfekt = h.id;
        toast('Efekt seçildi: ' + h.ad);
        panelCiz();
      });
      holoBtns.appendChild(btn);
    });
    holoWrap.appendChild(holoBtns);
    kWrap.appendChild(holoWrap);

    // 3. Madde: Canlı Profil Önizleme Kartı (Gerçekçi Dikey Kick Profil Formatı)
    var onizKutu = el('div', 'flex flex-col items-center gap-3 mt-4 p-4 bg-surface-highest/90 border border-outline-decorative rounded-xl');
    onizKutu.appendChild(el('div', 'text-sm font-bold text-primary-base self-start flex items-center gap-1.5', '👁️ Profil Kartınızın Canlı Önizlemesi (Gerçek Boyut):'));
    
    var demoBg = '';
    if (curTip === 'yerel' && settings.profilKartBase64) demoBg = settings.profilKartBase64;
    else if (curTip === 'ozel' && settings.ozelKartUrl) demoBg = settings.ozelKartUrl;
    else demoBg = chrome.runtime.getURL('profil-kartlari/' + (settings.koleksiyonKartDosya || 'card_1.jpg'));

    var seciliHolo = settings.holoKartEfekt || 'pikachu';
    var isHoloActive = seciliHolo && seciliHolo !== 'varsayilan' && seciliHolo !== 'yok';

    var demoCard = el('div', 'relative rounded-2xl p-4 flex flex-col gap-3 overflow-hidden transition-all ' + (isHoloActive ? 'pk-holo-' + seciliHolo : 'pk-holo-varsayilan'));
    demoCard.style.cssText = 'width:320px;max-width:100%;min-height:360px;background-image:linear-gradient(rgba(15,23,42,0.40), rgba(15,23,42,0.75)), url("' + demoBg + '");background-size:cover;background-position:center;box-sizing:border-box;box-shadow:0 12px 35px rgba(0,0,0,0.6);';
    demoCard.innerHTML = `
      <!-- Üst Başlık & Avatar -->
      <div class="flex items-center gap-3">
        <div class="w-12 h-12 rounded-full bg-primary-base flex items-center justify-center font-black text-black text-lg shadow-lg border-2 border-black/30">PK</div>
        <div class="flex flex-col">
          <div class="font-extrabold text-white text-base flex items-center gap-1.5 drop-shadow ${isHoloActive ? 'pk-chat-holo-name pk-name-' + seciliHolo : ''}">
            ${esc(settings.mentionAd || 'BesimBICER')}
            <span class="bg-primary-base text-black text-[9.5px] font-black px-1.5 py-0.5 rounded shadow not-italic" style="text-shadow:none !important;-webkit-text-fill-color:initial !important;">PureKick</span>
          </div>
          <div class="text-[11px] text-gray-300 font-medium">Kick'e katıldı: <b>10 Şub 2024</b></div>
        </div>
      </div>

      <!-- Seri & Rozetler Blokları (Şeffaf & Blur) -->
      <div class="flex flex-col gap-2 mt-1">
        <div class="bg-slate-950/50 backdrop-blur-md p-2.5 rounded-xl border border-white/10 flex justify-between items-center text-xs text-white">
          <span class="font-semibold flex items-center gap-1.5">⚡ Seri:</span>
          <span class="text-primary-base font-bold">x19 gün</span>
        </div>
        
        <div class="bg-slate-950/50 backdrop-blur-md p-2.5 rounded-xl border border-white/10 flex justify-between items-center text-xs text-white">
          <span class="font-semibold flex items-center gap-1.5">💎 Rozetler:</span>
          <span class="bg-pink-600 text-white text-[10px] font-bold px-2 py-0.5 rounded-full shadow">22</span>
        </div>
      </div>

      <!-- Notlar Alanı -->
      <div class="bg-slate-950/60 backdrop-blur-md p-2.5 rounded-xl border border-white/10 text-xs text-gray-300 flex-1 flex flex-col justify-between">
        <div class="text-[10px] text-gray-400 font-semibold mb-1">Kullanıcı Notu:</div>
        <div class="text-[11px] italic text-gray-200">"PureKick Koleksiyon Kartı ve Holo Efekti Aktif!"</div>
        <div class="mt-3 pt-2 border-t border-white/10 flex justify-between items-center text-[10px] text-primary-base font-bold">
          <span>Holo: ${(holoKartSecenekleri.find(function(x){return x.id === seciliHolo;}) || {}).ad || '⚡ Pikachu Spark'}</span>
          <span class="text-gray-400">Canlı Önizleme</span>
        </div>
      </div>
    `;
    onizKutu.appendChild(demoCard);

    // Sohbet Satırında İsminiz Nasıl Görünecek? (Canlı Önizleme)
    var chatDemoWrap = el('div', 'w-full max-w-[320px] bg-surface-base border border-outline-decorative rounded-xl p-3 flex flex-col gap-1.5 shadow-md');
    chatDemoWrap.innerHTML = `
      <div class="text-[11px] font-bold text-primary-base">💬 Sohbet Satırında İsminiz Nasıl Görünecek?</div>
      <div class="text-xs text-white bg-surface-highest/60 p-2.5 rounded-lg flex items-center gap-1.5 font-medium">
        <span class="text-subtle text-[11px]">22:35</span>
        <span class="bg-[#00e5ff] text-black text-[10px] font-black px-1.5 py-0.2 rounded shadow">32</span>
        <span class="${isHoloActive ? 'pk-chat-holo-name pk-name-' + seciliHolo : 'font-bold text-white'}">${esc(settings.mentionAd || 'BesimBICER')}:</span>
        <span class="text-gray-200">PureKick ile sohbetteyim! ✨</span>
      </div>
    `;
    onizKutu.appendChild(chatDemoWrap);

    kWrap.appendChild(onizKutu);

    pb.appendChild(kWrap);
    body.appendChild(pb);
  }

  function bolumKartSohbeti(body) {
    var pb = kart(t('pkAltKartSohbeti'), t('pkKartMesajlarD'));
pb.appendChild(satir(t('pkKartMesajlar'), t('pkKartMesajlarD'),
      anahtar(settings.kartMesajlar !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { kartMesajlar: v } }); settings.kartMesajlar = v;
        processProfilePopups(); panelCiz();
        toast(ozellikToast(v, 'pkKartMesajlar'));
      })));
    body.appendChild(pb);
  }

  /* MODERASYON ALTINDA DÖRT AYRI SAYFA. Eskiden hepsi tek sayfada üst üste
     duruyordu: hızlı susturma + süre listesi + üç anahtar. Uzun ve karışıktı,
     üstelik dördü de birbirinden bağımsız araçlar. Menüde ayrı satırlar hem
     aramayı kolaylaştırıyor hem her sayfayı kısa tutuyor. */
  function bolumKartModerasyon(body) {
    var pb = kart(t('pkHizliMod'), t('pkHizliModD'));
    pb.appendChild(satir(t('pkHizliMod'), t('pkHizliModD'),
      anahtar(settings.hizliMod === true, function (v) {
        sendBg({ type: 'setSettings', payload: { hizliMod: v } }); settings.hizliMod = v;
        try { susturKur(); } catch (e) {}          // dinleyiciler anında bağlansın/sökülsün
        processProfilePopups(); panelCiz();
        toast(ozellikToast(v, 'pkHizliMod'));
      })));

    /* SÜRE SEÇENEKLERİ yalnız özellik açıkken görünüyor: kapalıyken
       düzenlenecek bir şey yok, boş yere yer kaplamasın. */
    if (settings.hizliMod === true) {
      pb.appendChild(el('div', 'text-sm font-semibold text-white', t('pkSureSecenek')));
      pb.appendChild(el('div', 'text-xs text-subtle', t('pkSureSecenekD')));
      var sr = el('div', 'flex items-center gap-2');
      var g = el('input', 'bg-surface-lower rounded text-sm text-white flex-1 min-w-0');
      g.type = 'text';
      g.value = susSureListesi().join(', ');
      g.placeholder = '5, 10, 30, 60, 1440';
      g.style.cssText = 'padding:6px 8px;border:1px solid rgba(255,255,255,.12);outline:none';
      var kb = btnBirincil(t('pkKaydet'));
      kb.addEventListener('click', function () {
        var d = susSureCoz(g.value);
        /* Hepsi elenirse varsayılana dönüyoruz — boş liste menüyü işlevsiz
           bırakırdı ve kullanıcı sebebini anlamazdı. */
        if (!d.length) { d = SUS_VARSAYILAN.slice(); }
        susSureKaydet(d);
        g.value = d.join(', ');
        toast(t('pkTSureKaydedildi'));
      });
      sr.appendChild(g); sr.appendChild(kb);
      pb.appendChild(sr);
      /* Seçilen sürelerin okunur hâli: kullanıcı "1440" yazdığında menüde
         "1gün" göreceğini burada doğruluyor. */
      var onizle = el('div', 'flex flex-wrap gap-1');
      susSureListesi().forEach(function (dk) {
        var e2 = el('span', 'bg-surface-base rounded text-xs text-white', sureKisa(dk));
        e2.style.cssText = 'padding:3px 7px';
        onizle.appendChild(e2);
      });
      pb.appendChild(onizle);
      pb.appendChild(el('div', 'text-xs text-subtle', t('pkHizliModNot')));
    }
    body.appendChild(pb);
  }

  /* ---- Moderasyon › Cross-Ban ---- */
  function bolumCrossAyar(body) {
    var cb = kart(t('pkCrossAyar'), t('pkCrossAyarD'));
    cb.appendChild(satir(t('pkCrossAyar'), t('pkCrossAyarD'),
      anahtar(settings.crossBan === true, function (v) {
        sendBg({ type: 'setSettings', payload: { crossBan: v } }); settings.crossBan = v;
        processProfilePopups(); panelCiz();
        toast(ozellikToast(v, 'pkCrossAyar'));
      })));
    /* Açıkken nereden kullanılacağını söylüyoruz: anahtarı açan kullanıcı
       düğmeyi nerede arayacağını bilmiyordu. */
    if (settings.crossBan === true) {
      cb.appendChild(el('div', 'text-xs text-subtle', t('pkCrossNerede')));
    }
    body.appendChild(cb);
  }

  /* ---- Moderasyon › Toplu TimeOut ---- */
  function bolumTopluToAyar(body) {
    var tb = kart(t('pkToAyar'), t('pkToAyarD'));
    tb.appendChild(satir(t('pkToAyar'), t('pkToAyarD'),
      anahtar(settings.topluTo === true, function (v) {
        sendBg({ type: 'setSettings', payload: { topluTo: v } }); settings.topluTo = v;
        /* Kapatınca canlı kip de dursun: ayar kapalıyken arka planda
           susturma sürmesi kullanıcının beklemediği bir şey. */
        if (!v) { topluToAcik = false; topluToKapat(); }
        panelCiz();
        toast(ozellikToast(v, 'pkToAyar'));
      })));
    if (settings.topluTo === true) {
      tb.appendChild(el('div', 'text-xs text-subtle', t('pkToNerede')));
    }
    body.appendChild(tb);
  }

  /* ---- Moderasyon › Toplu Mesaj Silme ---- */
  function bolumTopluSilAyar(body) {
    var sb = kart(t('pkSilAyar'), t('pkSilAyarD'));
    sb.appendChild(satir(t('pkSilAyar'), t('pkSilAyarD'),
      anahtar(settings.topluSil === true, function (v) {
        sendBg({ type: 'setSettings', payload: { topluSil: v } }); settings.topluSil = v;
        /* Kapatınca canlı kip de dursun — ayar kapalıyken arka planda mesaj
           silinmesi kullanıcının beklemediği bir şey. */
        if (!v) { topluSilAcik = false; topluSilKapat(); }
        panelCiz();
        toast(ozellikToast(v, 'pkSilAyar'));
      })));
    if (settings.topluSil === true) {
      sb.appendChild(el('div', 'text-xs text-subtle', t('pkSilNerede')));
    }
    body.appendChild(sb);
  }

  /* ---- Moderasyon › Otomatik Mesaj ---- */
  function bolumOtoMesajAyar(body) {
    var ob = kart(t('pkOmAyar'), t('pkOmAyarD'));
    ob.appendChild(satir(t('pkOmAyar'), t('pkOmAyarD'),
      anahtar(settings.otoMesaj === true, function (v) {
        sendBg({ type: 'setSettings', payload: { otoMesaj: v } }); settings.otoMesaj = v;
        if (!v) { omKapat(); omSifirla(); }
        panelCiz();
        toast(ozellikToast(v, 'pkOmAyar'));
      })));
    if (settings.otoMesaj === true) {
      ob.appendChild(el('div', 'text-xs text-subtle', t('pkOmNerede')));
    }
    body.appendChild(ob);
  }

  /* ════════ OYNATICI ════════ */
  function bolumVideoIstatSayfa(body) {
    var vi = kart(t('pkAltVideoIstat'), t('pkVideoBilgisiD'));
vi.appendChild(satir(t('pkVideoIstatistikleri'), t('pkVideoIstatistikleriD'),
      anahtar(settings.videoIstat !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { videoIstat: v } }); settings.videoIstat = v;
        if (!v) vistatKapat();
        oynaticiButonlariKur(); panelCiz();
        toast(ozellikToast(v, 'pkVideoIstatistikleri'));
      })));
vi.appendChild(satir(t('pkTamEkranSon'), t('pkTamEkranSonD'),
      anahtar(settings.tamEkranSon === true, function (v) {
        sendBg({ type: 'setSettings', payload: { tamEkranSon: v } }); settings.tamEkranSon = v;
        oynaticiButonlariKur(); panelCiz();
      })));

    /* 10.17 — Picture-in-Picture (Mini Oynatıcı) */
    var pipBtn = btnBirincil('🖼️ Mini Oynatıcıyı Başlat (PiP)');
    pipBtn.addEventListener('click', function () { pkPipBaslat(); });
    var pipWrap = el('div', 'flex mt-2');
    pipWrap.appendChild(pipBtn);
    vi.appendChild(pipWrap);

    /* 10.17 — %300 Ses Yükseltici */
    vi.appendChild(satir('Ses Yükseltici (%300)', 'Kısık sesli yayınlarda sesi %300 seviyesine kadar artırır.',
      kaydirici(settings.sesYukseltici || 100, 100, 300, 10, function (val) {
        sendBg({ type: 'setSettings', payload: { sesYukseltici: val } }); settings.sesYukseltici = val;
        pkSesYukselticiUygula(val);
      })));

    /* 10.17 — Video Parlaklık & Kontrast */
    vi.appendChild(satir('Video Parlaklığı', 'Karanlık sahneler için parlaklık ayarı (varsayılan: %100).',
      kaydirici(settings.videoParlaklik || 100, 50, 200, 5, function (val) {
        sendBg({ type: 'setSettings', payload: { videoParlaklik: val } }); settings.videoParlaklik = val;
        pkVideoFiltreleriUygula();
      })));

    /* 6. Madde: Ana Sayfa Otomatik Başlayan Stream'i Durdur */
    vi.appendChild(satir('Ana Sayfadaki Yayını Otomatik Durdur', 'Ana sayfaya girildiğinde otomatik başlayan öne çıkan yayını duraklatır.',
      anahtar(settings.anaSayfaOtoDurdur === true, function (v) {
        sendBg({ type: 'setSettings', payload: { anaSayfaOtoDurdur: v } }); settings.anaSayfaOtoDurdur = v;
        pkAnaSayfaStreamDurdur(); panelCiz();
      })));

    /* 10. Madde: Oynatıcıya Tıklayarak Oynat/Durdur */
    vi.appendChild(satir('Tıklama ile Oynat / Durdur', 'Video görüntüsü üzerine tıklandığında yayını duraklatır veya başlatır.',
      anahtar(settings.videoTiklaDurdur !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { videoTiklaDurdur: v } }); settings.videoTiklaDurdur = v;
        pkVideoTiklaDurdurKur(); panelCiz();
      })));

    /* 16. CANLI YAYIN ALTYAZI VE SESLİ AI DUBLAJ SİSTEMİ */
    vi.appendChild(satir('Canlı Yayın Otomatik Altyazısı (AI)', 'Yabancı yayıncının konuşmasını otomatik algılayıp ekranda anlık altyazı olarak gösterir.',
      anahtar(settings.canliAltyazi === true, function (v) {
        sendBg({ type: 'setSettings', payload: { canliAltyazi: v } }); settings.canliAltyazi = v;
        pkCanliAltyaziVeDublajKur(); panelCiz();
        toast(v ? 'Canlı altyazı açıldı' : 'Canlı altyazı kapatıldı');
      })));

    vi.appendChild(satir('Canlı Yayın Sesli AI Dublajı (Seslendirme)', 'Yabancı yayındaki konuşmaları anında seçtiğiniz dilde yapay zeka sesiyle arka planda seslendirir.',
      anahtar(settings.sesliDublaj === true, function (v) {
        sendBg({ type: 'setSettings', payload: { sesliDublaj: v } }); settings.sesliDublaj = v;
        pkCanliAltyaziVeDublajKur(); panelCiz();
        toast(v ? 'Sesli AI dublaj açıldı' : 'Sesli AI dublaj kapatıldı');
      })));

    // Altyazı & Dublaj 25 Dil ve Görünüm Kutusu
    var altyaziAyarKutu = el('div', 'flex flex-col gap-3 bg-surface-highest p-3.5 rounded-xl border border-outline-decorative mt-2');
    altyaziAyarKutu.innerHTML = `
      <div class="text-xs font-bold text-primary-base flex items-center gap-1.5">
        <span>🌐 Canlı Altyazı / Dublaj Dil & Görünüm Ayarları</span>
      </div>

      <div class="grid grid-cols-2 gap-2.5">
        <div>
          <div class="text-[11px] text-subtle mb-1">Kaynak Dil (Yayıncının Dili):</div>
          <select id="pk-panel-src-lang" class="bg-surface-base border border-outline-decorative text-white text-xs px-2.5 py-1.5 rounded w-full">
            <option value="auto">🌐 Otomatik Algıla</option>
            ${PK_DILLER_25.map(function(d){ return '<option value="' + d.k + '" ' + (settings.altyaziKaynakDil === d.k ? 'selected' : '') + '>' + d.b + ' ' + d.a + '</option>'; }).join('')}
          </select>
        </div>
        <div>
          <div class="text-[11px] text-subtle mb-1">Çevrilecek Hedef Dil:</div>
          <select id="pk-panel-trg-lang" class="bg-surface-base border border-outline-decorative text-white text-xs px-2.5 py-1.5 rounded w-full">
            ${PK_DILLER_25.map(function(d){ return '<option value="' + d.k + '" ' + ((settings.altyaziHedefDil || 'tr') === d.k ? 'selected' : '') + '>' + d.b + ' ' + d.a + '</option>'; }).join('')}
          </select>
        </div>
      </div>

      <div class="grid grid-cols-3 gap-2 pt-2 border-t border-outline-decorative/50">
        <div>
          <div class="text-[11px] text-subtle mb-1">Yazı Boyutu:</div>
          <select id="pk-panel-font-sz" class="bg-surface-base border border-outline-decorative text-white text-xs px-2 py-1 rounded w-full">
            <option value="12" ${settings.altyaziFontBoyut === 12 ? 'selected' : ''}>12px (Küçük)</option>
            <option value="14" ${settings.altyaziFontBoyut === 14 ? 'selected' : ''}>14px</option>
            <option value="16" ${(settings.altyaziFontBoyut || 16) === 16 ? 'selected' : ''}>16px (Normal)</option>
            <option value="18" ${settings.altyaziFontBoyut === 18 ? 'selected' : ''}>18px</option>
            <option value="22" ${settings.altyaziFontBoyut === 22 ? 'selected' : ''}>22px (Büyük)</option>
            <option value="26" ${settings.altyaziFontBoyut === 26 ? 'selected' : ''}>26px (Dev)</option>
          </select>
        </div>

        <div>
          <div class="text-[11px] text-subtle mb-1">Konum:</div>
          <select id="pk-panel-pos" class="bg-surface-base border border-outline-decorative text-white text-xs px-2 py-1 rounded w-full">
            <option value="alt" ${(settings.altyaziKonum || 'alt') === 'alt' ? 'selected' : ''}>⬇️ Alt-Orta</option>
            <option value="ust" ${settings.altyaziKonum === 'ust' ? 'selected' : ''}>⬆️ Üst-Orta</option>
          </select>
        </div>

        <div>
          <div class="text-[11px] text-subtle mb-1">Arka Plan:</div>
          <select id="pk-panel-bg-op" class="bg-surface-base border border-outline-decorative text-white text-xs px-2 py-1 rounded w-full">
            <option value="0" ${settings.altyaziBgOpaklik === 0 ? 'selected' : ''}>%0 (Şeffaf)</option>
            <option value="40" ${settings.altyaziBgOpaklik === 40 ? 'selected' : ''}>%40</option>
            <option value="75" ${(settings.altyaziBgOpaklik || 75) === 75 ? 'selected' : ''}>%75 (Normal)</option>
            <option value="100" ${settings.altyaziBgOpaklik === 100 ? 'selected' : ''}>%100 (Siyah)</option>
          </select>
        </div>
      </div>

      <div class="flex items-center justify-between pt-2 border-t border-outline-decorative/50">
        <div class="text-[11px] text-subtle font-semibold">Yazı Rengi:</div>
        <div class="flex gap-2">
          ${['#facc15', '#ffffff', '#38bdf8', '#53fc18', '#f472b6'].map(function(c){
            return '<button class="pk-panel-color-btn w-5 h-5 rounded-full cursor-pointer transition-transform hover:scale-110" data-color="' + c + '" style="background:' + c + ';border:' + ((settings.altyaziRenk || '#facc15') === c ? '2px solid #fff' : '1px solid rgba(0,0,0,0.5)') + ';"></button>';
          }).join('')}
        </div>
      </div>
    `;

    altyaziAyarKutu.querySelector('#pk-panel-src-lang').addEventListener('change', function (e) {
      var v = e.target.value;
      sendBg({ type: 'setSettings', payload: { altyaziKaynakDil: v } });
      settings.altyaziKaynakDil = v;
      pkCanliAltyaziVeDublajKur();
      toast('Altyazı kaynak dili güncellendi');
    });

    altyaziAyarKutu.querySelector('#pk-panel-trg-lang').addEventListener('change', function (e) {
      var v = e.target.value;
      sendBg({ type: 'setSettings', payload: { altyaziHedefDil: v } });
      settings.altyaziHedefDil = v;
      pkCanliAltyaziVeDublajKur();
      toast('Altyazı hedef dili güncellendi');
    });

    altyaziAyarKutu.querySelector('#pk-panel-font-sz').addEventListener('change', function (e) {
      var v = parseInt(e.target.value, 10);
      sendBg({ type: 'setSettings', payload: { altyaziFontBoyut: v } });
      settings.altyaziFontBoyut = v;
      pkCanliAltyaziStilUygula();
    });

    altyaziAyarKutu.querySelector('#pk-panel-pos').addEventListener('change', function (e) {
      var v = e.target.value;
      sendBg({ type: 'setSettings', payload: { altyaziKonum: v } });
      settings.altyaziKonum = v;
      pkCanliAltyaziStilUygula();
    });

    altyaziAyarKutu.querySelector('#pk-panel-bg-op').addEventListener('change', function (e) {
      var v = parseInt(e.target.value, 10);
      sendBg({ type: 'setSettings', payload: { altyaziBgOpaklik: v } });
      settings.altyaziBgOpaklik = v;
      pkCanliAltyaziStilUygula();
    });

    altyaziAyarKutu.querySelectorAll('.pk-panel-color-btn').forEach(function (btn) {
      btn.addEventListener('click', function () {
        var clr = btn.getAttribute('data-color');
        sendBg({ type: 'setSettings', payload: { altyaziRenk: clr } });
        settings.altyaziRenk = clr;
        altyaziAyarKutu.querySelectorAll('.pk-panel-color-btn').forEach(function(b){ b.style.border = (b.getAttribute('data-color') === clr ? '2px solid #fff' : '1px solid rgba(0,0,0,0.5)'); });
        pkCanliAltyaziStilUygula();
      });
    });

    vi.appendChild(altyaziAyarKutu);

    /* 17. Video Zoom & Pan */
    vi.appendChild(satir('Video Büyüteç & Yakınlaştırma (Digital Zoom)', 'Alt + Fare Tekerleği ile video ekranını %100 - %500 arası büyütür ve kaydırır.',
      anahtar(settings.videoZoom !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { videoZoom: v } }); settings.videoZoom = v;
        pkVideoZoomKur(); panelCiz();
      })));

    /* 19. AI Yayın Özeti ("Ne Kaçırdım?") */
    vi.appendChild(satir('AI Yayın Özeti & Kaçırılanları Yakala ("Ne Kaçırdım?")', 'Yayına geç katıldığınızda o ana kadarki olayları, oyunları ve sohbet konularını tek tıkla özetler.',
      anahtar(settings.aiYayinOzet !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { aiYayinOzet: v } }); settings.aiYayinOzet = v;
        pkAiYayinOzetKur(); panelCiz();
      })));

    body.appendChild(vi);
  }

  function bolumOtomasyonEk(body) {
    var vi = kart(t('pkUyarHiz'), t('pkUyarHizD'));
vi.appendChild(satir(t('pkUyarHiz'), t('pkUyarHizD'),
      anahtar(settings.uyarHiz === true, function (v) {
        sendBg({ type: 'setSettings', payload: { uyarHiz: v } }); settings.uyarHiz = v;
        uyarlanabilirHiz(); panelCiz();
      })));
    if (settings.uyarHiz === true) vi.appendChild(el('div', 'text-xs text-subtle', t('pkUyarHizNot')));
    body.appendChild(vi);
  }

  /* ════════ MODERASYON ════════ */
  function bolumModGunlukAyar(body) {
    var md = kart(t('pkAltModGunlugu'), t('pkModGunluguD'));
md.appendChild(satir(t('pkModIzle'), t('pkModIzleD'),
      anahtar(settings.modGunluk !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { modGunluk: v } }); settings.modGunluk = v;
        if (!v) { olaySifirla(); modPanelKapat(); modSeritKaldir(); }
        panelCiz();
        toast(ozellikToast(v, 'pkModGunlugu'));
      })));
md.appendChild(satir(t('pkModSerit'), t('pkModSeritD'),
      anahtar(settings.modSerit !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { modSerit: v } }); settings.modSerit = v;
        /* Kapatılınca şerit hemen kalksın; panel açıksa o da kapansın —
           kapısı olmayan açık panel kullanıcıyı kilitler. */
        if (!v) { modSeritKaldir(); modPanelKapat(); } else { try { modSeritKur(); } catch (e) {} }
        panelCiz(); toast(ozellikToast(v, 'pkModSerit'));
      })));
md.appendChild(satir(t('pkModSohbette'), t('pkModSohbetteD'),
      anahtar(settings.modSohbette !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { modSohbette: v } }); settings.modSohbette = v;
        panelCiz(); toast(ozellikToast(v, 'pkModSohbette'));
      })));
md.appendChild(satir(t('pkSilinenGoster'), t('pkSilinenGosterD'),
      anahtar(settings.silinenGoster !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { silinenGoster: v } }); settings.silinenGoster = v;
        panelCiz(); toast(ozellikToast(v, 'pkSilinenGoster'));
      })));
md.appendChild(satir(t('pkAiMod'), t('pkAiModD'),
      anahtar(settings.aiMod !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { aiMod: v } }); settings.aiMod = v; panelCiz();
      })));
md.appendChild(satir(t('pkBaglantiDurum'), t('pkBaglantiDurumD'),
      anahtar(settings.baglantiDurum === true, function (v) {
        sendBg({ type: 'setSettings', payload: { baglantiDurum: v } }); settings.baglantiDurum = v;
        baglantiNoktasiKur(); panelCiz();
      })));
md.appendChild(satir(t('pkTemizlemeKoru'), t('pkTemizlemeKoruD'),
      anahtar(settings.temizlemeKoru === true, function (v) {
        sendBg({ type: 'setSettings', payload: { temizlemeKoru: v } }); settings.temizlemeKoru = v;
        if (!v) kurtarmaKapat();
        panelCiz();
      })));
    
    /* 10.16 — Moderasyon Günlüğü Dışa Aktar */
    var bModCsv = el('button', 'mt-3 px-3 py-2 rounded bg-surface-highest text-white font-bold text-xs hover:bg-overlay cursor-pointer flex items-center gap-1.5', '📥 Moderasyon Günlüğünü İndir (CSV)');
    bModCsv.addEventListener('click', function() { pkModExport(); });
    md.appendChild(bModCsv);

    body.appendChild(md);
  }

  function bolumBanDetaySayfa(body) {
    var md = kart(t('pkBanDetay'), t('pkBanDetayD'));
md.appendChild(satir(t('pkBanDetay'), t('pkBanDetayD'),
      anahtar(settings.banDetay === true, function (v) {
        sendBg({ type: 'setSettings', payload: { banDetay: v } }); settings.banDetay = v; panelCiz();
      })));
    body.appendChild(md);
  }

  function bolumCezaSebepleri(body) {
    var md = kart(t('pkBanSablon'), t('pkBanSablonD'));
md.appendChild(satir(t('pkBanSablon'), t('pkBanSablonD'),
      anahtar(settings.banSablon === true, function (v) {
        sendBg({ type: 'setSettings', payload: { banSablon: v } }); settings.banSablon = v;
        processProfilePopups(); panelCiz();
      })));
    if (settings.banSablon === true) {
      var sgir = el('div', 'flex gap-2');
      var si = el('input', 'bg-surface-highest rounded text-sm text-white flex-1 min-w-0');
      si.type = 'text';
      si.placeholder = t('pkSebepYazin');
      si.style.cssText = 'padding:6px 10px;border:1px solid rgba(240,241,242,.16);outline:none';
      var sb = btnIkincil(t('pkEkle'));
      function sebepEkleTikla() { if (sebepEkle(si.value)) { si.value = ''; panelCiz(); } else si.value = ''; }
      sb.addEventListener('click', sebepEkleTikla);
      si.addEventListener('keydown', function (e) { if (e.key === 'Enter') { e.preventDefault(); sebepEkleTikla(); } });
      sgir.appendChild(si); sgir.appendChild(sb);
      md.appendChild(sgir);
      if (!banSebepleri.length) {
        md.appendChild(el('div', 'text-xs text-subtle', t('pkSebepYok')));
      } else {
        var sl = el('div', 'flex flex-wrap gap-2');
        banSebepleri.forEach(function (x) {
          var et = el('div', 'bg-surface-highest rounded flex items-center gap-2');
          et.style.cssText = 'padding:4px 8px;max-width:100%';
          et.appendChild(el('span', 'text-xs text-white truncate', x));
          var cb = el('button', 'text-subtle hover:text-white transition-all shrink-0', '✕');
          cb.style.cssText = 'background:none;border:0;cursor:pointer;font-size:12px;line-height:1';
          cb.addEventListener('click', function () { sebepSil(x); panelCiz(); });
          et.appendChild(cb);
          sl.appendChild(et);
        });
        md.appendChild(sl);
      }
    }
    body.appendChild(md);
  }

  /* ════════ ARAYÜZ ════════ */
  function bolumGezinme(body) {
    var ar = kart(t('pkAltGezinme'), t('pkTiyatroNavD'));
ar.appendChild(satir(t('pkTiyatroNav'), t('pkTiyatroNavD'),
      anahtar(settings.tiyatroNav === true, function (v) {
        sendBg({ type: 'setSettings', payload: { tiyatroNav: v } }); settings.tiyatroNav = v;
        tiyatroNavKur(); panelCiz();
      })));
    body.appendChild(ar);
  }

  function bolumSagTikMenuleri(body) {
    var ar = kart(t('pkAltSagTikMenuleri'), t('pkArayuzDuzeniD'));
ar.appendChild(satir(t('pkKartSagTik'), t('pkKartSagTikD'),
      anahtar(settings.kartSagTik === true, function (v) {
        sendBg({ type: 'setSettings', payload: { kartSagTik: v } }); settings.kartSagTik = v;
        if (!v) kartMenuKapat();
        panelCiz();
      })));
ar.appendChild(satir(t('pkGorselBuyut'), t('pkGorselBuyutD'),
      anahtar(settings.gorselBuyut !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { gorselBuyut: v } }); settings.gorselBuyut = v;
        if (!v) gorselBuyutKapat();
        panelCiz();
      })));
    body.appendChild(ar);
  }

  function bolumGizliKanallar(body) {
    var c = kart(t('pkGizlenenKanallar'), t('pkGizlenenKanallarD'));
    c.appendChild(satir(t('pkGizleIkonu'), t('pkGizleIkonuD'),
      anahtar(settings.gizleIkonu === true, function (v) {
        sendBg({ type: 'setSettings', payload: { gizleIkonu: v } }); settings.gizleIkonu = v;
        if (!v) hideHideBtn();     // ekranda duruyorsa hemen sök
        toast(ozellikToast(v, 'pkGizleIkonu'));
      })));
    /* Bu satir bir yamada dusmustu: `slugs` tanimsiz kaliyor, bolum her
       cizimde ReferenceError atiyor ve gizlenen kanallar listesi hic
       gorunmuyordu. */
    var slugs = Object.keys(blocked).sort();
    if (!slugs.length) {
      c.appendChild(el('div', 'text-sm text-subtle', t('pkKanalGizleYok')));
    } else {
      var liste = el('div', 'flex flex-col gap-2');
      slugs.forEach(function (s) {
        var r = el('div', 'bg-surface-highest rounded px-3 py-2 flex items-center justify-between gap-3');
        r.appendChild(el('div', 'text-sm font-semibold text-white truncate', s));
        var kaldir = btnIkincil(t('pkKaldir'));
        kaldir.classList.add('h-8');
        kaldir.addEventListener('click', function () { unblockChannel(s); panelCiz(); });
        r.appendChild(kaldir);
        liste.appendChild(r);
      });
      c.appendChild(liste);
    }
    body.appendChild(c);
  }

  function bolumGizliKategoriler(body) {
var kc = kart(t('pkGizlenenKategoriler'), t('pkGizlenenKategorilerD'));
    kc.appendChild(satir(t('pkKategoriGizlemeyiKullan'), t('pkKategoriGizlemeyiKullanD'),
      anahtar(settings.kategoriGizle !== false, function (v) {
        sendBg({ type: 'setSettings', payload: { kategoriGizle: v } }); settings.kategoriGizle = v;
        if (!v) {
          var geri = document.querySelectorAll('[data-pk-kathidden]');
          for (var g = 0; g < geri.length; g++) { geri[g].style.removeProperty('display'); geri[g].removeAttribute('data-pk-kathidden'); }
        } else katUygula();
        panelCiz();
        toast(ozellikToast(v, 'pkKategoriGizlemeyiKullan'));
      })));
    var katlar = Object.keys(gizliKat).sort();
    if (!katlar.length) {
      kc.appendChild(el('div', 'text-sm text-subtle',
        t('pkKategoriGizleYok')));
    } else {
      var kl = el('div', 'flex flex-col gap-2');
      katlar.forEach(function (ad) {
        var r = el('div', 'bg-surface-highest rounded px-3 py-2 flex items-center justify-between gap-3');
        r.appendChild(el('div', 'text-sm font-semibold text-white truncate', ad));
        var kaldir = btnIkincil(t('pkKaldir'));
        kaldir.classList.add('h-8');
        kaldir.addEventListener('click', function () { katGoster(ad); panelCiz(); });
        r.appendChild(kaldir);
        kl.appendChild(r);
      });
      kc.appendChild(kl);
    }
    body.appendChild(kc);
  }

  /* Kategori düzeni: kullanıcı isteğiyle yaygın Kick eklentisi düzeniyle
     hizalandı — sohbet · kullanıcı kartı · oynatıcı · moderasyon · arayüz,
     her biri kendi alt sayfalarıyla, gerektiğinde üç düzey. Etiketler
     PureKick'in KENDİ özellik adlarından; hiçbir metin dışarıdan alınmadı.
     Reklam engelleme bizim çekirdek işimiz, o yüzden en üstte.

     `bolum` bir fonksiyon ya da fonksiyon DİZİSİ olabilir; birden çok kart
     tek sayfada toplanabilsin diye. */
  var MENU_AGAC = [
    { id: 'genel',    ad: 'pkSekmeGenel',        ikon: I_MENU,   bolum: bolumGenel },
    { id: 'reklam',   ad: 'pkEngellemeAyarlari', ikon: I_BLOCK,  bolum: bolumReklam },

    { id: 'sohbet',   ad: 'pkMenuSohbet',        ikon: I_BALON, cocuk: [
      { id: 'vurgu',   ad: 'pkAltVurgulama', cocuk: [
        { id: 'roller',  ad: 'pkRolVurgulama',      bolum: bolumRolRenkleri },
        { id: 'oncelik', ad: 'pkOncelikliKisiler',  bolum: bolumOncelikliKisiler },
        { id: 'gizli',   ad: 'pkKisiGizleme',       bolum: bolumGizlenenKisiler }
      ] },
      { id: 'mesaj',   ad: 'pkAltSohbetMesaji', cocuk: [
        { id: 'eylem',   ad: 'pkMesajEylemleri',    bolum: bolumMesajEylemleri },
        { id: 'bahset',  ad: 'pkBahsetTikla',       bolum: bolumTiklanabilirBahset },
        { id: 'sekmeici',ad: 'pkSekmeIci',          bolum: bolumSekmeIciBaglanti }
      ] },
      { id: 'adtakip', ad: 'pkAltAdTakibi', cocuk: [
        { id: 'ayar',    ad: 'pkBahsedilmeler',     bolum: bolumBahsetAyari },
        /* `yerel: true` → sayı YALNIZ bu satırda görünür, üst dallara TAŞINMAZ.
           Bildirimin asıl yeri sohbet şeridindeki PK düğmesi; menüde bir de
           "Sohbet 2 → Ad takibi 2 → Senden bahsedenler 2" zinciri kurulunca
           aynı bildirim üç yerde sayılmış oluyordu. Yaprakta kalması ise
           gerekli: kullanıcı listeye girmeden kaç tane olduğunu görüyor. */
        { id: 'liste',   ad: 'pkSendenBahsedenler', bolum: bolumBahsedilme,
          rozet: 'mention', yerel: true }
      ] },
      { id: 'gorunum', ad: 'pkAltSohbetGorunum',    bolum: bolumSohbetGorunum },
      { id: 'istat',   ad: 'pkSohbetIstatistikleriBaslik', bolum: bolumIstatAyari },
      { id: 'sureler', ad: 'pkIzlemeSureleriBaslik',       bolum: bolumIzlemeSureleri }
    ] },

    { id: 'kart',     ad: 'pkMenuKullaniciKarti', ikon: I_ARA, cocuk: [
      { id: 'bilgi',   ad: 'pkAltKartBilgisi',    bolum: bolumKartBilgisi },
      { id: 'sohbet',  ad: 'pkAltKartSohbeti',    bolum: bolumKartSohbeti },
      { id: 'notlar',  ad: 'pkKullaniciNotlari',  bolum: bolumKartNotlar }
    ] },

    { id: 'oynatici', ad: 'pkAltVideoVod',       ikon: I_CANLI,  cocuk: [
      { id: 'otomasyon', ad: 'pkAltOtomasyon', cocuk: [
        /* Menüdeki ad sayfanın İKİ kartını da kapsıyor (kalite + ses); kart
           başlığı olan `pkOtomatikKalite` ile karıştırma. */
        { id: 'kalite',   ad: 'pkAltKaliteSes', bolum: bolumKalite },
        { id: 'tiyatro',  ad: 'pkOtoTiyatro',     bolum: bolumTiyatro },
        { id: 'uyarhiz',  ad: 'pkUyarHiz',        bolum: bolumOtomasyonEk }
      ] },
      { id: 'ozellik',   ad: 'pkAltOynaticiOzel', cocuk: [
        { id: 'video',    ad: 'pkAltVideoIstat',  bolum: bolumVideoIstatSayfa },
        { id: 'ss',       ad: 'pkEkranGoruntusu', bolum: bolumEkranGoruntusu },
        { id: 'bindir',   ad: 'pkBindir',         bolum: bolumBindirme },
        { id: 'sagtik',   ad: 'pkSagTikMenusu',   bolum: bolumSagTik },
        { id: 'coklu',    ad: 'pkCokluYayin',     bolum: bolumCokluYayinAyar }
      ] }
    ] },

    { id: 'kenarcubugu', ad: 'pkMenuKenar',     ikon: I_FILTRE, cocuk: [
      { id: 'liste',   ad: 'pkSabitKanallar',       bolum: bolumKenarListe },
      { id: 'oniz',    ad: 'pkAltKenarCubugu',      bolum: bolumOnizleme },
      { id: 'kanal',   ad: 'pkAltGizliKanallar',    bolum: bolumGizliKanallar },
      { id: 'kategori',ad: 'pkAltGizliKategoriler', bolum: bolumGizliKategoriler }
    ] },

    { id: 'moderasyon', ad: 'pkModerasyon',      ikon: I_KALKAN,  cocuk: [
      { id: 'gunluk',  ad: 'pkAltModGunlugu',    bolum: bolumModGunlukAyar },
      { id: 'detay',   ad: 'pkBanDetay',         bolum: bolumBanDetaySayfa },
      { id: 'sebep',   ad: 'pkAltCezaSebepleri', bolum: bolumCezaSebepleri },
      /* Kart üzerindeki MODERATÖR araçları burada: hızlı susturma ve
         kanallar arası işlem. Genel kart ayarları (bilgi, notlar, sohbet)
         "Kullanıcı kartı" dalında kalıyor — onlar herkese hitap ediyor. */
      /* DÖRT AYRI SATIR: hepsi bağımsız araç, tek sayfada üst üste durunca
         hem sayfa uzuyordu hem hangi anahtarın neye ait olduğu karışıyordu. */
      /* Bölümün içinde tek özellik var ve adı zaten "Hızlı susturma";
         menüde başka bir ad taşımak iki isim öğrenmek demekti. */
      { id: 'kartmod',  ad: 'pkHizliMod',       bolum: bolumKartModerasyon },
      { id: 'crossban', ad: 'pkCrossAyar',      bolum: bolumCrossAyar },
      { id: 'topluto',  ad: 'pkToAyar',         bolum: bolumTopluToAyar },
      { id: 'toplusil', ad: 'pkSilAyar',        bolum: bolumTopluSilAyar, yeni: '10.15' },
      { id: 'otomesaj', ad: 'pkOmAyar',         bolum: bolumOtoMesajAyar, yeni: '10.15' }
    ] },

    { id: 'arayuz',   ad: 'pkMenuArayuz',        ikon: I_FILTRE, cocuk: [
      { id: 'gezinme', ad: 'pkAltGezinme',          bolum: bolumGezinme },
      { id: 'kanalbilgi', ad: 'pkHakkindaEk',      bolum: bolumKanalHakkinda },
      { id: 'sagtik',  ad: 'pkAltSagTikMenuleri',   bolum: bolumSagTikMenuleri },
      { id: 'tema',    ad: 'themeColor',            bolum: bolumTema }
    ] },

    { id: 'ayirici' },
    { id: 'rozet',    ad: 'popupBadges',         ikon: I_ROZET,     bolum: bolumRozet },
    /* Abonelik AYRI bolum: rozetlerin altina sikistirilmis bir kart degil,
       kendi sayfasi. Kullanici burada durumunu gorup islem yapiyor. */
    /* `isik`: satırın üstünden ışık geçer — menüde dikkat çeksin. Bayrak
       kimliğe gömülü DEĞİL, menü düğümünde: başka bir bölüme de vermek
       gerekirse tek satır yetsin. */
    { id: 'abone',    ad: 'pkAbonelik',          ikon: I_ELMAS,     bolum: bolumAbone, isik: true },
    /* Menü etiketi AYRI ANAHTAR: bölümde artık iki kart var (droplar +
       günlük ödül), menüde ikisi de görünsün. Kart başlıkları kendi
       anahtarlarını kullanmaya devam ediyor. */
    { id: 'drops',    ad: 'pkDropsMenu',         ikon: I_HEDIYE, bolum: bolumDrops, rozet: 'drops' },
    { id: 'gelismis', ad: 'pkGelismis',          ikon: I_DISLI,  bolum: bolumGelismis, yeni: '10.15' },
    { id: 'hakkinda', ad: 'pkSekmeHakkinda',     ikon: I_BILGI,   bolum: bolumHakkinda }
  ];

  /* Yoldaki düğümü bul. Yol geçersizse köke döner — panel asla boş kalmaz. */
  function menuDugum(yol) {
    var liste = MENU_AGAC, dugum = null;
    for (var i = 0; i < yol.length; i++) {
      dugum = null;
      for (var k = 0; k < liste.length; k++) {
        /* Ayırıcı gerçek bir sayfa değil: adı, bölümü ve çocuğu yok. Yol
           olarak çözülmesine izin verilirse başlıksız boş sayfa açılır. */
        if (liste[k].id === 'ayirici') continue;
        if (liste[k].id === yol[i]) { dugum = liste[k]; break; }
      }
      if (!dugum) return null;
      liste = dugum.cocuk || [];
    }
    return dugum;
  }

  /* Bir düğümün (ve altındakilerin) bildirim sayısı — kökte de görünsün. */
  /* Bir düğümün KENDİ sayısı — alt dalları saymaz. */
  function menuRozetKendi(d) {
    if (!d) return 0;
    if (d.rozet === 'drops')   return unseenDrops().length;
    if (d.rozet === 'mention') return pkBahsetYeni();   // özellik kapalıysa 0
    return 0;
  }

  /* Satırda görünecek sayı: kendi + alt dalların toplamı.
     `yerel` işaretli çocuk toplama KATILMIYOR — sayısı yalnız kendi satırında
     kalsın, menünün tepesine kadar zincir kurmasın. */
  function menuRozet(d) {
    if (!d) return 0;
    var n = menuRozetKendi(d);
    (d.cocuk || []).forEach(function (c) { if (!c.yerel) n += menuRozet(c); });
    return n;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * SÜRÜMLE GELEN YENİLİK İŞARETİ
   *
   * Güncelleyen kullanıcı yeni özelliği bulamıyor. Menüde o özelliğe giden
   * yolun ÜSTÜNDEKİ her başlığa küçük bir nokta konuyor; kullanıcı o sayfayı
   * bir kez açınca nokta o daldan siliniyor. Sayı rozetiyle (`menuRozet`)
   * aynı ağaçta duruyor ama ayrı: o "kaç tane var" der, bu "burada yeni bir
   * şey var" der.
   *
   * TEMİZ KURULUMDA GÖSTERİLMEZ: eklentiyi ilk kez kuran için her şey zaten
   * yeni; nokta ancak GÜNCELLEYENDE anlam taşıyor. İlk çalıştırmada bütün
   * yollar görülmüş sayılıyor.
   *
   * Yama sürümleri tekrar tetiklemesin diye karşılaştırma `ana.alt`
   * düzeyinde (10.5.1 ile 10.5 aynı grup). */
  var PK_YENI_SURUM = '10.15';         // bu turdaki `yeni` etiketlerinin grubu
  var pkYeniGor = null;                // { sur: '10.7', gor: ['yol', ...] }

  function pkSurumGrubu(s) {
    return String(s || '').split('.').slice(0, 2).join('.');
  }

  /* Ağaçtaki `yeni` etiketli bütün yolları toplar. */
  /* Menü ağacında OLMAYAN yenilikler. Kick'in sohbet ayarları içindeki
     PureKick alt menüsü ayrı bir yüzey: orada da nokta çıksın diye kendi
     anahtarlarıyla aynı listeye yazılıyor. */
  var PK_YENI_EK = [];   /* 10.15'te Kick'in kendi ayar yüzeyinde yenilik yok.
                            DOLU BIRAKMA: bu dizi her zaman "görülmemiş"
                            sayılıyor, eski sürümün girdileri kalırsa eski
                            sayfalara nokta basar. */

  function pkYeniYollar(liste, yol, cikti) {
    var kok = !liste;
    liste = liste || MENU_AGAC; yol = yol || []; cikti = cikti || [];
    liste.forEach(function (d) {
      if (!d || d.id === 'ayirici') return;
      var y = yol.concat([d.id]);
      if (d.yeni === PK_YENI_SURUM) cikti.push(y.join('/'));
      if (d.cocuk) pkYeniYollar(d.cocuk, y, cikti);
    });
    if (kok) PK_YENI_EK.forEach(function (k) { cikti.push(k); });
    return cikti;
  }

  /* Ağaç dışı anahtar okunmamış mı? */
  function pkYeniEkVar(k) {
    return !!pkYeniGor && PK_YENI_EK.indexOf(k) >= 0 && pkYeniGor.gor.indexOf(k) < 0;
  }

  function pkYeniEkGoruldu(k) {
    if (!pkYeniGor || PK_YENI_EK.indexOf(k) < 0) return;
    if (pkYeniGor.gor.indexOf(k) >= 0) return;
    pkYeniGor.gor.push(k);
    pkYeniYaz();
  }

  function pkYeniKur(kayit) {
    var simdi = pkSurumGrubu(PK_YENI_SURUM);
    /* TEMİZ KURULUM — hiçbir nokta gösterme. İşareti arka plan koyuyor
       (`onInstalled` + `reason === 'install'`); içerik betiği bunu tek başına
       ayırt edemez, çünkü bu özellikten ÖNCEKİ sürümlerden güncelleyende de
       kayıt yok. İkisini karıştırırsak mevcut kullanıcı kitlesi yenilikleri
       hiç görmez. */
    if (kayit && kayit.temiz) {
      pkYeniGor = { sur: simdi, gor: pkYeniYollar() };
      pkYeniYaz();
      return;
    }
    if (!kayit || !kayit.sur) {
      /* Kayıt yok ve temiz kurulum işareti de yok → bu özellikten önceki bir
         sürümden GÜNCELLENDİ. Yenilikler gösterilmeli. */
      pkYeniGor = { sur: simdi, gor: [] };
      pkYeniYaz();
      return;
    }
    if (pkSurumGrubu(kayit.sur) !== simdi) {
      /* Güncellendi — bu sürümün yenilikleri görülmemiş sayılır. */
      pkYeniGor = { sur: simdi, gor: [] };
      pkYeniYaz();
      return;
    }
    pkYeniGor = { sur: simdi, gor: (kayit.gor || []).slice() };
  }

  function pkYeniYaz() {
    try { chrome.storage.local.set({ pkYeni: pkYeniGor }); } catch (e) {}
  }

  /* Bu düğümün kendisinde veya ALTINDA görülmemiş yenilik var mı? */
  function menuYeni(d, yol) {
    if (!d || !pkYeniGor) return false;
    if (d.yeni === PK_YENI_SURUM && pkYeniGor.gor.indexOf(yol.join('/')) < 0) return true;
    var c = d.cocuk || [];
    for (var i = 0; i < c.length; i++) {
      if (c[i] && c[i].id !== 'ayirici' && menuYeni(c[i], yol.concat([c[i].id]))) return true;
    }
    return false;
  }

  /* Sayfa açılınca o yol görülmüş sayılır. */
  function pkYeniGoruldu(yol) {
    if (!pkYeniGor || !yol || !yol.length) return;
    var k = yol.join('/');
    if (pkYeniGor.gor.indexOf(k) >= 0) return;
    var d = menuDugum(yol);
    if (!d || d.yeni !== PK_YENI_SURUM) return;
    pkYeniGor.gor.push(k);
    pkYeniYaz();
  }

  /* Ağacın herhangi bir yerinde okunmamış yenilik var mı? (Sohbet ayarları
     satırı ve menü düğmesi bunu soruyor.) */
  function pkYeniVarMi() {
    for (var i = 0; i < MENU_AGAC.length; i++) {
      var d = MENU_AGAC[i];
      if (d && d.id !== 'ayirici' && menuYeni(d, [d.id])) return true;
    }
    for (var j = 0; j < PK_YENI_EK.length; j++) if (pkYeniEkVar(PK_YENI_EK[j])) return true;
    return false;
  }

  /* Menüde ve sohbet ayarları satırında kullanılan nokta. Sayı rozetinden
     ayrılsın diye Kick'in vurgu yeşili (ölçüldü: rgb(83,252,24)). */
  function pkYeniNoktasi() {
    var s = el('span', 'shrink-0');
    s.style.cssText = 'margin-left:6px;width:8px;height:8px;border-radius:50%;' +
      'background:#53fc18;box-shadow:0 0 0 3px rgba(83,252,24,.18);display:inline-block';
    s.title = t('pkYenilik');
    return s;
  }

  /* Sol sütundaki satır. `derinlik` girintiyi, `secili` vurguyu belirler. */
  function menuSatiri(d, yol, derinlik, siraNo) {
    var secili = yol.join('/') === panelYol.join('/');
    var acikDal = !secili && panelYol.slice(0, yol.length).join('/') === yol.join('/');
    var b = el('button', 'state-layer-surface flex w-full items-center gap-2.5 rounded text-left transition-all ' +
      (secili ? 'bg-surface-highest' : '') + (d.isik ? ' pk-menu-isik' : ''));
    
    /* 7. Madde: Seçili başlık için zengin yeşil/vurgulu arka plan ve sol şerit */
    var seciliStil = secili 
      ? 'background:rgba(83,252,24,0.12);border-left:3px solid rgb(83,252,24);' 
      : 'background:none;border-left:3px solid transparent;';

    b.style.cssText = 'padding:8px 10px;padding-left:' + (8 + derinlik * 14) + 'px;' +
      seciliStil + 'border-top:0;border-right:0;border-bottom:0;cursor:pointer;';
    
    if (d.ikon) {
      var ik = el('span', (secili ? 'text-primary-base' : 'text-subtle') + ' shrink-0 inline-flex');
      ik.innerHTML = d.ikon;
      var sv = ik.querySelector('svg');
      if (sv) {
        sv.style.setProperty('width', '18px');
        sv.style.setProperty('height', '18px');
        sv.style.setProperty('display', 'block');
      }
      b.appendChild(ik);
    }
    
    /* 7. Madde: Ana başlıklarda (derinlik === 0) 1., 2., 3. numaralandırma */
    var baslikMetni = t(d.ad);
    if (derinlik === 0 && siraNo) {
      baslikMetni = siraNo + '. ' + baslikMetni;
    }

    b.appendChild(el('div', 'flex-1 min-w-0 truncate text-sm ' +
      (secili || acikDal ? 'font-bold text-white' : 'font-semibold text-subtle'), baslikMetni));
    var n = menuRozet(d);
    if (n > 0) b.appendChild(sekmeRozeti(n));
    if (menuYeni(d, yol)) b.appendChild(pkYeniNoktasi());
    if (d.cocuk) {
      var ok = el('span', 'text-subtle shrink-0 inline-flex');
      ok.innerHTML = I_ILERI;
      ok.style.cssText = 'transition:transform .15s ease;transform:rotate(' + (acikDal || secili ? '90' : '0') + 'deg)';
      b.appendChild(ok);
    }
    b.addEventListener('click', function () {
      if (d.cocuk && (secili || acikDal)) panelYol = yol.slice(0, -1);
      else panelYol = yol;
      panelCiz();
    });
    return b;
  }

  /* Sol menüyü çizer: kök liste + açık dalın çocukları girintili. */
  function menuListesiCiz(kap, liste, yol, derinlik) {
    var siraSayac = 1;
    liste.forEach(function (d) {
      if (d.id === 'ayirici') {
        var ay = el('div', 'bg-outline-decorative w-full shrink-0');
        ay.style.cssText = 'height:1px;margin:8px 4px';
        kap.appendChild(ay);
        return;
      }
      var y = yol.concat([d.id]);
      kap.appendChild(menuSatiri(d, y, derinlik, derinlik === 0 ? siraSayac++ : null));
      // açık dalın çocukları hemen altında görünsün
      if (d.cocuk && panelYol.slice(0, y.length).join('/') === y.join('/')) {
        menuListesiCiz(kap, d.cocuk, y, derinlik + 1);
      }
    });
  }


  /* Sekmeler: Kick'in KENDİ Drops sayfası sekme çubuğuyla birebir aynı sınıflar
     (state-layer-surface + alt çizgi göstergesi). <a> yerine <button> — gezinme yok. */
  var TAB_BASE = 'state-layer-surface relative flex cursor-pointer items-center overflow-hidden ' +
                 'whitespace-nowrap rounded-t-sm font-semibold transition-all border-b px-2 h-12 text-base';
  var TAB_ACTIVE = ' border-surface-onSurfacePrimary text-surface-onSurfacePrimary';
  var TAB_IDLE   = ' border-transparent';

  /* Sekmedeki bildirim rozeti — buton rozetiyle AYNI görünüm (kırmızı, 16px).
     Böylece kullanıcı butondaki "1"in hangi bölümden geldiğini görür. */
  function sekmeRozeti(n) {
    var s = el('span', '', String(n));
    s.style.cssText = 'margin-left:6px;min-width:16px;height:16px;padding:0 4px;border-radius:8px;' +
      'background:#fa2d2d;color:#fff;font-size:10px;font-weight:800;display:inline-flex;' +
      'align-items:center;justify-content:center;line-height:1';
    return s;
  }

  // Yalnızca sekme çubuğunu çizer. Ayrı tutulmasının sebebi: Drops bölümü açılınca
  // bildirim düşer ve rozetin anında kaybolması gerekir — panelCiz() çağırmak
  // sonsuz döngü olurdu (panelCiz → bölüm → panelCiz).
  /* Sol sütun geldiği için ayrı başlık şeridine gerek kalmadı; sayfa başlığı
     içerik alanının en üstünde. Drops rozetini anında düşürmek için çağrılıyor. */
  /* YALNIZ sol menüyü tazeler. Gövdeye dokunmaz — bu şart: gövdeyi çizen
     bölüm fonksiyonu asenkron veri çekip sonra "tazele" derse, tam çizim
     kendisini yeniden çağırır ve panel sonsuz döngüye girer (Aktif Droplar'da
     tam olarak bu oluyordu). */
  function panelNavCiz() {
    if (!panelEl) return;
    var nav = panelEl.querySelector('[data-pk="nav"]');
    if (!nav) return;
    nav.textContent = '';
    menuListesiCiz(nav, MENU_AGAC, [], 0);
  }

  function panelCiz() {
    if (!panelEl) return;
    var nav = panelEl.querySelector('[data-pk="nav"]');
    var body = panelEl.querySelector('[data-pk="body"]');
    if (!nav || !body) return;

    /* Menü ışığı `pk-badge-style` içinde tanımlı; panel rozetlerden bağımsız
       açılabildiği için stilin kurulduğundan burada emin oluyoruz. */
    try { addBadgeStyle(); } catch (e) {}
    nav.textContent = '';
    menuListesiCiz(nav, MENU_AGAC, [], 0);

    body.textContent = '';
    var d = panelYol.length ? menuDugum(panelYol) : null;
    if (!d) {
      /* Hiçbir şey seçili değilse ilk sayfayı aç — sağ taraf boş kalmasın. */
      panelYol = ['genel'];
      d = menuDugum(panelYol);
      nav.textContent = '';
      menuListesiCiz(nav, MENU_AGAC, [], 0);
    }
    if (!d) return;
    body.appendChild(el('div', 'text-lg font-bold text-white', t(d.ad)));
    var b = d.bolum;
    if (b) {
      var liste = (typeof b === 'function') ? [b] : b;
      liste.forEach(function (f) { try { f(body); } catch (e) {} });
      /* Sayfa AÇILDI — yenilik noktası bu daldan silinsin. Yalnız gerçekten
         içerik basılan (yaprak) sayfalarda: üst kategoriye tıklamak alt
         sayfaları görmüş saymaz. */
      pkYeniGoruldu(panelYol);
    }
    /* Alt listesi olan kategoriye tıklanınca sağda ilk çocuğu göster. */
    if (!b && d.cocuk && d.cocuk.length) {
      panelYol = panelYol.concat([d.cocuk[0].id]);
      panelCiz();
    }
  }

  function panelKur() {
    if (panelEl && document.contains(panelEl)) return panelEl;
    var g = panelGeo();
    panelEl = el('div', 'bg-surface-lowest flex flex-col');
    panelEl.id = PANEL_ID;
    panelEl.style.cssText = 'position:fixed;top:' + g.top + 'px;left:' + g.left + 'px;right:0;bottom:0;z-index:401;display:none';

    var head = el('div', 'flex items-center justify-between gap-4 px-6 border-b border-outline-decorative shrink-0');
    head.style.height = '60px';
    var sol = el('div', 'flex items-center gap-3');
    var ikon = el('div', 'flex items-center justify-center rounded');
    ikon.style.cssText = 'width:32px;height:32px;background:#0f0f0f';
    ikon.innerHTML = SHIELD;
    // SHIELD'in kendi width/height'ı yok (menüde CSS'ten alıyordu, panelde o CSS yok) → burada ölçülendir
    var logoSvg = ikon.querySelector('svg');
    if (logoSvg) { logoSvg.style.width = '19px'; logoSvg.style.height = '19px'; logoSvg.style.display = 'block'; }
    ikon.style.color = '#fff';
    sol.appendChild(ikon);
    sol.appendChild(el('div', 'text-lg font-bold text-white', t('pkYonetimPaneli')));
    head.appendChild(sol);
    var kapat = el('button', 'text-subtle hover:text-white transition-all', '✕');
    kapat.style.cssText = 'font-size:20px;line-height:1;padding:6px';
    kapat.addEventListener('click', panelKapat);
    head.appendChild(kapat);
    panelEl.appendChild(head);

    /* İki sütun: solda kategori menüsü, sağda seçilen sayfa.
       Menü kendi içinde kayar; içerik alanı ayrı kayar. */
    var govde = el('div', 'flex-1 min-w-0 flex');
    govde.style.cssText = 'overflow:hidden';

    var nav = el('div', 'shrink-0 overflow-y-auto border-r border-outline-decorative bg-surface-lowest');
    nav.style.cssText = 'width:264px;padding:12px 8px';
    nav.setAttribute('data-pk', 'nav');
    govde.appendChild(nav);

    var icerik = el('div', 'flex-1 min-w-0 overflow-y-auto p-6 flex flex-col gap-4');
    icerik.setAttribute('data-pk', 'body');
    govde.appendChild(icerik);

    panelEl.appendChild(govde);

    pkKatmanaEkle(panelEl);
    return panelEl;
  }

  function panelAc() {
    panelKur();
    var g = panelGeo();
    panelEl.style.top = g.top + 'px';
    panelEl.style.left = g.left + 'px';
    panelEl.style.display = 'flex';
    panelCiz();
  }
  function panelKapat() { if (panelEl) panelEl.style.display = 'none'; }
  function panelAcikMi() { return !!(panelEl && panelEl.style.display !== 'none'); }
  function panelToggle() { panelAcikMi() ? panelKapat() : panelAc(); }

  /* Araç çubuğu simgesinden gelen "paneli aç" isteği. Popup kaldırıldı;
     simgeye tıklayınca yeni bir şey açmak yerine sayfadaki paneli
     gösteriyoruz. `panelAc` (toggle değil) bilerek: simge her zaman AÇAR,
     açıkken ikinci tıklama kapatıp kullanıcıyı şaşırtmaz. */
  try {
    chrome.runtime.onMessage.addListener(function (m, s, cevap) {
      if (!m || m.type !== 'pkPanelAc') return;
      try { panelAc(); } catch (e) {}
      /* Cevap ŞART: arka plan bununla "içerik betiği burada" anlıyor.
         Cevapsız kalırsa `lastError` dolar ve kick.com yeniden açılır. */
      try { cevap({ ok: true }); } catch (e) {}
    });
  } catch (e) {}
  window.addEventListener('resize', panelKonumla);

  /* ══════════════════════════════════════════════════════════════════════
   * SEKME-İÇİ LİNK ÖNİZLEME
   * Sohbete link atılınca mesajın ALTINA buton çıkar; tıklayınca sayfadan
   * ayrılmadan önizleme penceresi açılır.
   *
   * Desteklenen: kick.com · twitter.com · x.com · youtube.com · youtu.be ·
   *              instagram.com · reddit.com · streamable.com
   *
   * CSP: Kick sayfasında frame engeli YOK — altı embed adresi de canlı test
   * edildi (hepsi cross-origin olarak yüklendi, konsolda tek ihlal yok).
   * ══════════════════════════════════════════════════════════════════════ */
  var LP_ICONS = {
    YouTube:    '<svg viewBox="0 0 24 24" fill="currentColor"><path d="M23 12s0-3.9-.5-5.8c-.3-1-1.1-1.8-2.1-2.1C18.5 3.6 12 3.6 12 3.6s-6.5 0-8.4.5c-1 .3-1.8 1.1-2.1 2.1C1 8.1 1 12 1 12s0 3.9.5 5.8c.3 1 1.1 1.8 2.1 2.1 1.9.5 8.4.5 8.4.5s6.5 0 8.4-.5c1-.3 1.8-1.1 2.1-2.1.5-1.9.5-5.8.5-5.8zM9.9 15.5v-7l6.1 3.5-6.1 3.5z"/></svg>',
    Tweet:      '<svg viewBox="0 0 24 24" fill="currentColor"><path d="M18.9 2H22l-7.1 8.1L23.3 22h-6.6l-5.1-6.7L5.7 22H2.6l7.6-8.7L1.1 2h6.7l4.6 6.1L18.9 2zm-1.1 18h1.7L7.3 3.8H5.5L17.8 20z"/></svg>',
    Instagram:  '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="2" width="20" height="20" rx="5"/><circle cx="12" cy="12" r="4"/><circle cx="17.5" cy="6.5" r="1.2" fill="currentColor" stroke="none"/></svg>',
    Reddit:     '<svg viewBox="0 0 24 24" fill="currentColor"><circle cx="12" cy="12" r="10" opacity=".25"/><path d="M17 11.5a1.5 1.5 0 0 0-2.6-1 7.6 7.6 0 0 0-3.6-.9l.7-3.2 2.2.5a1.2 1.2 0 1 0 .2-1l-2.8-.6a.5.5 0 0 0-.6.4l-.9 3.9a7.6 7.6 0 0 0-3.6.9 1.5 1.5 0 1 0-1.7 2.4 3 3 0 0 0 0 .6c0 2.3 2.7 4.1 6 4.1s6-1.8 6-4.1a3 3 0 0 0 0-.6 1.5 1.5 0 0 0 .7-1.4zM8.5 13a1.2 1.2 0 1 1 1.2 1.2A1.2 1.2 0 0 1 8.5 13zm6.4 3.2a4.3 4.3 0 0 1-2.9.9 4.3 4.3 0 0 1-2.9-.9.4.4 0 0 1 .5-.6 3.6 3.6 0 0 0 2.4.7 3.6 3.6 0 0 0 2.4-.7.4.4 0 0 1 .5.6zm-.6-2a1.2 1.2 0 1 1 1.2-1.2 1.2 1.2 0 0 1-1.2 1.2z"/></svg>',
    Streamable: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="4" width="20" height="16" rx="3"/><path d="M10 9l5 3-5 3z" fill="currentColor" stroke="none"/></svg>',
    Kick:       '<svg viewBox="0 0 24 24" fill="currentColor"><path d="M3 3h5v6l4-6h6l-6 9 6 9h-6l-4-6v6H3z"/></svg>'
  };

  function lpEmbed(ham) {
    try {
      var x = new URL(ham);
      if (x.protocol !== 'https:' && x.protocol !== 'http:') return null;
      var h = x.hostname.replace(/^(www|m|mobile|old)\./, '').toLowerCase();
      var p = x.pathname.replace(/\/+$/, '');

      if (h === 'youtu.be') {
        var yid = p.slice(1).split('/')[0];
        if (yid) return { ad: 'YouTube', url: 'https://www.youtube.com/embed/' + encodeURIComponent(yid), oran: 56.25 };
      }
      if (h === 'youtube.com') {
        var v = x.searchParams.get('v');
        if (v) return { ad: 'YouTube', url: 'https://www.youtube.com/embed/' + encodeURIComponent(v), oran: 56.25 };
        var ym = p.match(/^\/(?:shorts|embed|live)\/([\w-]+)/);
        if (ym) return { ad: 'YouTube', url: 'https://www.youtube.com/embed/' + ym[1], oran: 56.25 };
      }
      if (h === 'twitter.com' || h === 'x.com') {
        var tm = p.match(/\/status(?:es)?\/(\d+)/);
        if (tm) return { ad: 'Tweet', url: 'https://platform.twitter.com/embed/Tweet.html?id=' + tm[1] + '&theme=dark', oran: 0 };
      }
      if (h === 'instagram.com') {
        /* `reels` (çoğul) Instagram'ın kendi kullandığı biçim; gömme adresi
           tekil `reel` istiyor. Eski desen çoğulu hiç tanımıyordu — bağlantı
           önizleme düğmesi bu yüzden çıkmıyordu. */
        var im = p.match(/^\/(?:share\/)?(p|reels?|tv)\/([\w-]+)/);
        if (im) {
          var tur = im[1] === 'reels' ? 'reel' : im[1];
          return { ad: 'Instagram', url: 'https://www.instagram.com/' + tur + '/' + im[2] + '/embed', oran: 125 };
        }
        /* PROFİL (kullanıcı istedi). Instagram'ın gezinme yolları profil
           DEĞİL — elenmezse "/explore" da profil sanılırdı. */
        var igAtla = { explore: 1, stories: 1, accounts: 1, direct: 1, reels: 1,
                       reel: 1, p: 1, tv: 1, share: 1, about: 1, developer: 1,
                       legal: 1, privacy: 1, terms: 1, challenge: 1 };
        var ip = p.match(/^\/([A-Za-z0-9._]{1,30})$/);
        if (ip && !igAtla[ip[1].toLowerCase()]) {
          return { ad: 'Instagram', url: 'https://www.instagram.com/' + ip[1] + '/embed', oran: 125 };
        }
      }
      if (h === 'reddit.com') {
        if (/^\/r\/[^/]+\/comments\//.test(p)) return { ad: 'Reddit', url: 'https://www.redditmedia.com' + p + '/?embed=true&theme=dark', oran: 0 };
      }
      /* Lightshot (prnt.sc) EKLENEMİYOR — denendi, geri alındı.
         Sayfa çerçevelenmeyi reddediyor ("prnt.sc bağlanmayı reddetti").
         Doğrudan görsel (`img.lightshot.app/...jpg`) çapraz kaynak yükleniyor
         ve hotlink koruması yok, ama adresteki kısa kod görsel adına
         ÇEVRİLEMİYOR (kod `BfBfooloKcTr`, görsel `iAybOg-QRC-uawSBz19jyg`);
         görsel adını öğrenmek sayfayı indirmeyi, o da yeni host iznini
         gerektiriyor — istenmedi. */
      if (h === 'streamable.com') {
        var sm = p.match(/^\/(?:e\/)?(\w+)/);
        if (sm) return { ad: 'Streamable', url: 'https://streamable.com/e/' + sm[1], oran: 56.25 };
      }
      if (h === 'kick.com') {
        var km = p.match(/^\/([^/]+)\/clips?\/([\w-]+)/);
        if (km) return { ad: 'Kick', url: 'https://kick.com/' + km[1] + '/clips/' + km[2], oran: 56.25 };
        var cm = p.match(/^\/([^/]+)$/);
        if (cm && !NON_CHANNEL[cm[1].toLowerCase()]) return { ad: 'Kick', url: 'https://player.kick.com/' + cm[1], oran: 56.25 };
      }
    } catch (e) {}
    return null;
  }

  /* ---- Önizleme penceresi ---- */
  var lpModal = null;
  function lpKapat() { if (lpModal) { lpModal.remove(); lpModal = null; } }

  /* Adresi kısa göster: protokol ve son eğik çizgi atılır → instagram.com/p/ABC123 */
  function lpKisaLink(u) {
    try { var x = new URL(u); return (x.hostname.replace(/^www\./, '') + x.pathname).replace(/\/+$/, ''); }
    catch (e) { return String(u || '').replace(/^https?:\/\//, ''); }
  }

  /* Kapatma ikonu SVG — '✕' karakteri yazı tipine göre kayıyor (bkz. gizle butonu). */
  var I_KAPAT = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:16px;height:16px;display:block">' +
    '<path d="M18.3 5.7a1.4 1.4 0 0 0-2 0L12 10 7.7 5.7a1.4 1.4 0 1 0-2 2L10 12l-4.3 4.3a1.4 1.4 0 1 0 2 2L12 14l4.3 4.3a1.4 1.4 0 0 0 2-2L14 12l4.3-4.3a1.4 1.4 0 0 0 0-2z"/></svg>';

  function lpAc(bilgi) {
    lpKapat();
    lpModal = el('div', '');
    lpModal.style.cssText = 'position:fixed;inset:0;z-index:2147483100;background:rgba(0,0,0,.6)';
    lpModal.addEventListener('click', function (e) { if (e.target === lpModal) lpKapat(); });

    // Sürüklenebilmesi için kutu SABİT konumlu; ortalama flex ile değil, elle yapılır.
    var kutu = el('div', 'bg-surface-base border-outline-decorative rounded border flex flex-col overflow-hidden');
    kutu.style.cssText += ';position:fixed;width:min(460px,92vw);max-height:88vh;box-shadow:0 16px 48px rgba(0,0,0,.55)';

    var bas = el('div', 'flex items-center justify-between gap-3 px-4 border-b border-outline-decorative shrink-0');
    bas.style.cssText += ';height:48px;cursor:move;user-select:none';
    var sol = el('div', 'flex items-center gap-2 text-white min-w-0');
    var ik = el('span', 'inline-flex shrink-0'); ik.style.cssText = 'width:18px;height:18px';
    ik.innerHTML = LP_ICONS[bilgi.ad] || LP_ICONS.Kick;
    var svg = ik.querySelector('svg'); if (svg) { svg.style.width = '18px'; svg.style.height = '18px'; }
    sol.appendChild(ik);
    // Başlık metni yok → logo + linkin kendisi
    var adr = el('div', 'text-sm font-semibold truncate', lpKisaLink(bilgi.kaynak || bilgi.url));
    adr.title = bilgi.kaynak || bilgi.url;
    sol.appendChild(adr);
    bas.appendChild(sol);
    var kapatBtn = el('button', 'text-subtle hover:text-white transition-all rounded ' +
                                'inline-flex items-center justify-center shrink-0');
    kapatBtn.style.cssText = 'width:28px;height:28px;padding:0';
    kapatBtn.innerHTML = I_KAPAT;
    kapatBtn.title = t('pkKapat');
    kapatBtn.addEventListener('click', lpKapat);
    bas.appendChild(kapatBtn);
    kutu.appendChild(bas);

    var cer = el('div', 'bg-black');
    cer.style.cssText = bilgi.oran ? ('position:relative;width:100%;padding-top:' + bilgi.oran + '%')
                                   : 'position:relative;width:100%;height:70vh';
    var f = el('iframe', '');
    f.src = bilgi.url;
    f.setAttribute('allow', 'autoplay; encrypted-media; picture-in-picture; clipboard-write');
    // no-referrer YouTube embed'ini bozuyor (Error 153 — olculdu). 'origin' yalnizca
    // https://kick.com gonderir, tam adres sizmaz; YouTube da kabul eder.
    f.setAttribute('referrerpolicy', 'origin');
    f.setAttribute('loading', 'lazy');
    f.style.cssText = 'position:absolute;inset:0;width:100%;height:100%;border:0';
    cer.appendChild(f);
    kutu.appendChild(cer);

    var alt = el('div', 'px-4 py-2 border-t border-outline-decorative shrink-0');
    var link = el('a', 'text-xs text-subtle hover:text-white transition-all', t('pkKaynagiYeniSekmede'));
    link.href = bilgi.kaynak || bilgi.url; link.target = '_blank'; link.rel = 'noopener noreferrer';
    alt.appendChild(link);
    kutu.appendChild(alt);

    lpModal.appendChild(kutu);
    pkKatmanaEkle(lpModal);

    // Başlangıçta ortala (kutu eklendikten SONRA ölçülebilir)
    var r0 = kutu.getBoundingClientRect();
    kutu.style.left = Math.round(Math.max(8, (window.innerWidth - r0.width) / 2)) + 'px';
    kutu.style.top  = Math.round(Math.max(8, (window.innerHeight - r0.height) / 2)) + 'px';

    /* ---- Sürükleme: başlık çubuğundan tut, ekranda gezdir ----
       iframe ÜZERİNE gelince fare olayları iframe'e (cross-origin) gider ve
       sürükleme takılır. Bu yüzden sürükleme boyunca şeffaf bir kalkan serilir. */
    var kalkan = null;
    bas.addEventListener('mousedown', function (e) {
      if (e.button !== 0 || (e.target.closest && e.target.closest('button'))) return;
      var r = kutu.getBoundingClientRect();
      var dx = e.clientX - r.left, dy = e.clientY - r.top;
      kalkan = el('div', '');
      kalkan.style.cssText = 'position:fixed;inset:0;z-index:2147483200;cursor:move';
      pkKatmanaEkle(kalkan);

      function hareket(ev) {
        var nx = Math.min(Math.max(0, ev.clientX - dx), Math.max(0, window.innerWidth - r.width));
        var ny = Math.min(Math.max(0, ev.clientY - dy), Math.max(0, window.innerHeight - r.height));
        kutu.style.left = Math.round(nx) + 'px';
        kutu.style.top = Math.round(ny) + 'px';
      }
      function birak() {
        document.removeEventListener('mousemove', hareket);
        document.removeEventListener('mouseup', birak);
        if (kalkan) { kalkan.remove(); kalkan = null; }
      }
      document.addEventListener('mousemove', hareket);
      document.addEventListener('mouseup', birak);
      e.preventDefault();
    });
  }
  document.addEventListener('keydown', function (e) { if (e.key === 'Escape') lpKapat(); });

  /* ---- Sohbet mesajına buton ekle ---- */
  function lpMesaj(msg) {
    try {
      if (!msg || msg.getAttribute('data-pk-lp')) return;      // aynı mesajı bir kez işle
      var bulunan = [], gorulen = {};
      var linkler = msg.querySelectorAll('a[href]');
      for (var i = 0; i < linkler.length && bulunan.length < 3; i++) {
        var href = linkler[i].getAttribute('href') || '';
        if (!/^https?:/i.test(href)) continue;
        var b = lpEmbed(href);
        if (b && !gorulen[b.url]) { gorulen[b.url] = 1; b.kaynak = href; bulunan.push(b); }
      }
      msg.setAttribute('data-pk-lp', bulunan.length ? '1' : '0');
      if (!bulunan.length) return;

      /* Buton mesaj BALONUNUN İÇİNE girer, [data-index] satırının altına değil.
         Balon: rounded-lg + break-word taşıyan div (padding + hover zemini
         onda). Dışına
         konunca buton balonun solunda/altında boşlukta duruyordu.
         Not: Kick'in sohbeti sanallaştırılmış liste ama yüksekliği DİNAMİK ölçüyor —
         buton eklenince satır boşluğu tam buton kadar büyüyor (ölçüldü), bindirme olmuyor. */
      /* SONDAKİ `s` YOK — BİLEREK. Kick iki balon türünü farklı adlandırıyor
         (canlıda ölçüldü):
             normal : "… rounded-lg px-2 break-words …"
             YANIT  : "… rounded-lg px-2 py-1 wrap-break-word …"
         `"wrap-break-word"` içinde `"break-words"` geçmiyor; eski seçici
         yanıtlarda boş dönüp `|| msg` yedeğine düşüyordu ve düğme balonun
         değil SATIRIN içine girip taşıyordu (balon 312px, satır 336px).
         `break-word` ikisini de yakalıyor; ölçüldü: 21 mesajın 21'inde tam
         bir eşleşme, fazladan kap yok. */
      var balon = msg.querySelector('div[class*="break-word"][class*="rounded-lg"]') || msg;
      /* `pk-lp` işareti şart: bu blok mesaj balonunun İÇİNDE duruyor, işaretsiz
         kalsa "Son Mesajlar" ayıklaması buton yazısını mesaj metni sanardı. */
      var sira = el('div', 'pk-lp flex flex-col gap-1 mt-1');
      bulunan.forEach(function (b) {
        /* Ölçüler UYDURMA DEĞİL: Kick'in sohbet panelindeki kendi küçük butonu canlı
           ölçüldü → h-8 (32px) · text-sm (14px) · font-semibold (600) · rounded (4px)
           · gap-2 (8px). Bizimki 28px/12px ile ufak kalıyordu, artık birebir aynı.
           Renk: bg-secondary-base (PureKick grisi) + Kick'in ince kenarlığı.
           Genişlik: mesaj balonunca yayılır (sohbet paneli daraldıkça uyar). */
        var btn = el('button', 'group relative box-border inline-flex items-center justify-center gap-2 ' +
                               'whitespace-nowrap rounded font-semibold text-sm text-white ' +
                               'bg-secondary-base hover:brightness-125 border border-outline-decorative ' +
                               'transition-all betterhover:active:scale-[0.98] h-8 px-3 w-full');
        btn.style.cssText = 'max-width:100%';
        var ik = el('span', 'inline-flex shrink-0'); ik.style.cssText = 'width:14px;height:14px';
        ik.innerHTML = LP_ICONS[b.ad] || LP_ICONS.Kick;
        var s = ik.querySelector('svg'); if (s) { s.style.width = '14px'; s.style.height = '14px'; }
        btn.appendChild(ik);
        btn.appendChild(document.createTextNode(t('pkBaglantiyiOnizle')));   // platform zaten ikondan belli
        btn.addEventListener('click', function (e) { e.preventDefault(); e.stopPropagation(); lpAc(b); });
        sira.appendChild(btn);
      });
      balon.appendChild(sira);   // balonun ICINE
    } catch (e) {}
  }

  /* ---- Kanal sayfasında "Engelle" butonu (Takip Et'in yanında) ----
   * Kick'in KENDİ buton sınıflarıyla üretilir → boyut/köşe/yazı birebir uyar.
   * Renk: PureKick gri (bg-secondary-base) — mor "Takip Et" ile karışmaz.
   * Kanal zaten engelliyse buton "Engeli Kaldır"a döner. */
  var BTN_BASE = 'group inline-flex gap-1.5 items-center justify-center rounded font-semibold ' +
                 'box-border relative transition-all betterhover:active:scale-[0.98]';
  var I_BLOCK = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:16px;height:16px;display:block">' +
    '<path d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zm0 2.8a7.1 7.1 0 0 1 4.1 1.3L6.1 16.1A7.2 7.2 0 0 1 12 4.8zm0 14.4a7.1 7.1 0 0 1-4.1-1.3l10-10A7.2 7.2 0 0 1 12 19.2z"/></svg>';

  function sayfaSlug() {
    var p = (location.pathname.split('/').filter(Boolean));
    if (p.length !== 1) return '';                       // /kanal dışındaki sayfalar (kategori, drops…)
    var s = p[0].toLowerCase();
    return NON_CHANNEL[s] ? '' : s;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — KANAL SATIRINDAKİ PUREKICK MENÜSÜ
   * Menü, kanal eylem satırına (Takip Et / Abone Ol'un yanına) bir dişli
   * olarak girer — özellikler kullanıldıkları yerin dibinde dursun diye.
   * Çapa "Engelle" butonuyla AYNI: görünür "Takip Et"in kabı.
   * ══════════════════════════════════════════════════════════════════════ */
  var I_BAHSET = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:16px;height:16px;display:block">' +
    '<path d="M12 2a10 10 0 1 0 5.6 18.3 1.3 1.3 0 0 0-1.5-2.2A7.4 7.4 0 1 1 19.4 12v1a1.6 1.6 0 0 1-3.2 0V7.8h-2.4v.9A5 5 0 1 0 12 17a5 5 0 0 0 3.1-1.1 4.2 4.2 0 0 0 6.9-3v-.9A10 10 0 0 0 12 2zm0 12.4A2.4 2.4 0 1 1 14.4 12 2.4 2.4 0 0 1 12 14.4z"/></svg>';
  var I_DISLI = '<svg viewBox="0 0 24 24" fill="currentColor" style="width:16px;height:16px;display:block">' +
    '<path d="M19.4 13a7.6 7.6 0 0 0 0-2l2-1.6-1.9-3.3-2.4 1a7.5 7.5 0 0 0-1.7-1l-.4-2.6h-3.9l-.4 2.6a7.5 7.5 0 0 0-1.7 1l-2.4-1L2.6 9.4 4.6 11a7.6 7.6 0 0 0 0 2l-2 1.6 1.9 3.3 2.4-1a7.5 7.5 0 0 0 1.7 1l.4 2.6h3.9l.4-2.6a7.5 7.5 0 0 0 1.7-1l2.4 1 1.9-3.3zM12 15.4A3.4 3.4 0 1 1 15.4 12 3.4 3.4 0 0 1 12 15.4z"/></svg>';

  /* Kanal eylem satırını bul. Çapa olarak "Takip Et" YAZISINI kullanamayız:
     kanalı zaten takip ediyorsan o buton yazısız bir KALP ikonuna dönüşüyor ve
     satır bulunamıyordu (canlıda ölçüldü). Bunun yerine hep orada duran zil
     (Bildirimler) ya da "Abone Ol" butonunun kabını alıyoruz.
  /* ══════════════════════════════════════════════════════════════════════
   * 10.5 — ENGELLE, PAYLAŞ SATIRINA TAŞINDI
   * Eylem satırı (Takip Et / Abone Ol) artık PureKick dişlisini taşıyor;
   * engelleme ise alttaki ikon satırına, "Şununla paylaş" ile "Şikayet Et"
   * arasına giriyor. Ölçüldü: iki buton da 40×40, ortak kapları
   * `flex items-center gap-2 self-end py-0.5`.
   * ══════════════════════════════════════════════════════════════════════ */
  /* SONUÇ ÖNBELLEĞİ — ana turda her tur ödenen bir aramaydı.
     ÖLÇÜLDÜ: `engelleIkonuKur` 1210 ms / 85 çağrı (14.24 ms) — kalan tur
     maliyetinin %63'ü, ve o işlevin kendi tekrarsızlık kapısı bu aramadan
     SONRA geliyordu. Arama hem sayfadaki bütün butonları geziyor hem de
     `getBoundingClientRect` ile yeniden yerleşim zorluyor.
     `isConnected` yerleşim istemez. */
  var paylasOnbellek = null;
  function paylasButonu() {
    if (paylasOnbellek && paylasOnbellek.isConnected) return paylasOnbellek;
    var bulunan = paylasButonuAra();
    if (bulunan) paylasOnbellek = bulunan;
    return bulunan;
  }

  function paylasButonuAra() {
    var v = oynaticiVideosu();
    var vb = v ? v.getBoundingClientRect().bottom : 0;
    var bs = document.querySelectorAll('button');
    for (var i = 0; i < bs.length; i++) {
      var et = (bs[i].getAttribute('aria-label') || bs[i].title || '');
      if (!/payla|share/i.test(et)) continue;
      var r = bs[i].getBoundingClientRect();
      if (!r.width || !r.height) continue;              // gizli kopya
      if (vb && r.top < vb) continue;                   // videonun üstündekiler değil
      if (profilKartiIcinde(bs[i])) continue;
      return bs[i];
    }
    return null;
  }

  function engelleIkonuKur() {
    if (!settings.consented) return;
    var slug = sayfaSlug();
    // `t` DEĞİL: var yükseldiği için aşağıdaki t('...') çağrısı "t is not a function" veriyordu.
    if (!slug) { var es = document.querySelectorAll('[data-pk-block2]'); for (var q = 0; q < es.length; q++) es[q].remove(); return; }
    var pay = paylasButonu(); if (!pay || !pay.parentElement) return;
    var bar = pay.parentElement;
    var engelli = !!blocked[slug];
    var mevcut = bar.querySelector(':scope > [data-pk-block2]');
    if (mevcut) {
      if (mevcut.getAttribute('data-pk-block2') === (engelli ? '1' : '0')) return;
      mevcut.remove();
    }
    /* Paylaş/şikayet butonlarıyla aynı ölçü ve yüzey — Kick'in kendi sınıfları. */
    var b = el('button', 'group relative box-border flex shrink-0 grow-0 select-none items-center ' +
      'justify-center rounded state-layer-surface bg-transparent text-white');
    b.style.cssText = 'width:40px;height:40px;padding:0;border:0;cursor:pointer';
    b.setAttribute('data-pk-block2', engelli ? '1' : '0');
    b.type = 'button';
    /* Kick’in kendi ipucu görünümü — yanındaki Paylaş/Şikâyet düğmeleri de
       aynısını kullanıyor. Ham `title` bırakılsaydı işletim sisteminin gri
       kutusu çıkıp Kick’inkiyle iki farklı ipucu olurdu. */
    b.setAttribute('aria-label', t(engelli ? 'pkKanalEngeliKaldir' : 'pkKanaliGizle'));
    /* 10px: oynatıcı şeridindeki 2px burada ipucunu düğmenin üstüne
       bindiriyordu — bu satırdaki düğmeler daha sıkışık. */
    bindTip(b, t(engelli ? 'pkKanalEngeliKaldir' : 'pkKanaliGizle'), true, 10);
    b.innerHTML = I_BLOCK;
    if (engelli) b.style.color = 'rgb(83,252,24)';
    b.addEventListener('click', function (e) {
      e.preventDefault(); e.stopPropagation();
      if (blocked[slug]) unblockChannel(slug); else blockChannel(slug);
      engelleIkonuKur();
      toast(t(blocked[slug] ? 'pkTKanalGizlendi' : 'pkTKanalGizlemeKalkti'));
    });
    // paylaşın HEMEN SAĞINA (şikayet etten önce)
    if (pay.nextSibling) bar.insertBefore(b, pay.nextSibling); else bar.appendChild(b);
    ikonEsitle(b, bar);                        // paylas/sikayet ikonlariyla ayni boy
  }


  /* Tur adımları. Ad → fonksiyon; listeler hangi turda ne çalışacağını söyler.
     Tek satırlık dev zincir yerine bu tablo var ki adım eklemek/çıkarmak
     ve hatayı izole etmek kolay olsun. */
  var pkTurFn = {
    silinmisDenetle: silinmisDenetle,
    susturKur: susturKur,
    lpTum: lpTum,
    pkAyarCakismaDenetle: pkAyarCakismaDenetle,
    pkAyarDugmesiKur: pkAyarDugmesiKur,
    processAllMessages: processAllMessages,
    yanitAlintiRozetleri: yanitAlintiRozetleri,
    processProfilePopups: processProfilePopups,
    processStreamerHeader: processStreamerHeader,
    processSearchResults: processSearchResults,
    applyChannelBlocks: applyChannelBlocks,
    sabitKanalUygula: sabitKanalUygula,
    hakkindaUygula: hakkindaUygula,
    kuralKabulUygula: kuralKabulUygula,
    sesUygula: sesUygula,
    onizKickBagla: onizKickBagla,
    rosterTazele: rosterTazele,
    odulSor: odulSor,
    kisiUygula: kisiUygula,
    mesajDurumUygula: mesajDurumUygula,
    bahsetTiklanabilir: bahsetTiklanabilir,
    katUygula: katUygula,
    katButonuKur: katButonuKur,
    engelleIkonuKur: engelleIkonuKur,
    panelKonumla: panelKonumla,
    yanSohbetKur: yanSohbetKur,
    sohbetTara: sohbetTara,
    sohbetciFiltreCiz: sohbetciFiltreCiz,
    sohbetciEtiketle: sohbetciEtiketle,
    sohbetciSayac: sohbetciSayac,
    araButonuKur: araButonuKur,
    eskiKanalMenusuTemizle: eskiKanalMenusuTemizle,
    sohbetMenuButonuKur: sohbetMenuButonuKur,
    modSeritKur: modSeritKur,
    sohbetMenuNokta: sohbetMenuNokta,
    vodButonuKur: vodButonuKur,
    oynaticiButonlariKur: oynaticiButonlariKur,
    otoTiyatroKur: otoTiyatroKur,
    otoKaliteKur: otoKaliteKur,
    uyarlanabilirHiz: uyarlanabilirHiz,
    tiyatroNavKur: tiyatroNavKur,
    bindirKur: bindirKur,
    cerceveUygula: cerceveUygula,
    baglantiNoktasiKur: baglantiNoktasiKur,
    scIzleyiciOrnekle: scIzleyiciOrnekle
  };
  /* YALNIZ KANAL SAYFASINDA anlamlı adımlar. Buradakiler ana sayfada,
     gözatta, kategori sayfasında hiç çalışmıyor.
     Listede OLMAYANLAR her yerde çalışır: kanal/kategori gizleme, kenar
     çubuğu raptiyesi ve önizlemesi, profil kartı, arama, kendi panelimiz. */
  /* ══════════════════════════════════════════════════════════════════════
   * ADIM KAPILARI — kapalı özelliğin işlevi HİÇ çağrılmaz
   *
   * Eskiden tur 43 adımın hepsini çağırıyor, her adım kendi içinde ayarına
   * bakıyordu. Ama çoğu ayara bakmadan ÖNCE DOM tarıyordu — ölçüldü: 43
   * adımın 19'unda kapı querySelectorAll'dan SONRAYDI, 18'inde hiç yoktu.
   * Yani kapalı özellik de her mutasyon turunda bedel ödetiyordu ve
   * yüklemeyi geciktiriyordu. Kapı artık dağıtıcıda: ayar kapalıysa işlev
   * çağrılmıyor, sıfır iş.
   *
   * Adımın kendi içindeki ayar kontrolleri KALDIRILMADI: hem tek başına
   * çağrıldıkları yerler var (panel, olay dinleyicileri), hem de temizlik
   * turu o dalların çalışmasına dayanıyor.
   *
   * Birden çok anahtarlı adımlar: HEPSİ kapalıysa atlanır.
   *
   * BOOLEAN OLMAYANLAR BİLEREK DIŞARIDA — `otoSes` ('oto') ve `otoKalite`
   * ('1080'): "kapalı" değerleri false değil, yanlış kapatmak özelliği
   * sessizce öldürürdü. İkisinin işlevi de zaten ucuz.
   * ══════════════════════════════════════════════════════════════════════ */
  var PK_TUR_AYAR = {
    sabitKanalUygula:     ['sabitKanal'],
    susturKur:            ['hizliMod'],
    odulSor:              ['otoClaim'],
    sohbetciEtiketle:     ['scEtiket'],
    sohbetciFiltreCiz:    ['scFiltreCubugu'],
    sohbetciSayac:        ['scSayac'],
    lpTum:                ['sekmeIci'],
    hakkindaUygula:       ['hakkindaEk'],
    bindirKur:            ['bindir'],
    kuralKabulUygula:     ['kuralKabul'],
    onizKickBagla:        ['onizleme'],
    katUygula:            ['kategoriGizle'],
    katButonuKur:         ['kategoriGizle'],
    yanSohbetKur:         ['yanSohbet'],
    sohbetMenuNokta:      ['bahsedilme'],
    otoTiyatroKur:        ['otoTiyatro'],
    uyarlanabilirHiz:     ['uyarHiz'],
    tiyatroNavKur:        ['tiyatroNav'],
    baglantiNoktasiKur:   ['baglantiDurum'],
    mesajDurumUygula:     ['mesajEylem'],
    bahsetTiklanabilir:   ['bahsetTikla'],
    /* tamEkranSon DA BURADA: ayrı bir özellik ama tek çağrıldığı yer
       oynaticiButonlariKur içi (tamEkranSona()). Yalnız iki düğme ayarına
       baksaydık, ikisini de kapatan kullanıcı tam ekran düğmesi düzenlemesini
       de sessizce kaybederdi. */
    oynaticiButonlariKur: ['ekranGoruntusu', 'videoIstat', 'tamEkranSon'],
    /* HEPSİ açık olmalı: işlevin kendi kapısı `modSerit === false ||
       modGunluk === false` — yani BİRİ kapalıysa şerit tamamen kalkıyor. */
    modSeritKur:          { ve: ['modSerit', 'modGunluk'] }
  };
  /* BİLEREK LİSTEDE YOK — `kisiUygula`. Adı kişi gizleme/öncelik gibi dursa
     da içinde `rolBoya()` (rol vurgusu) ve `ozelAdUygula()` (aboneye özel
     akan isim) çağrıları var; `rolBoya` BAŞKA HİÇBİR YERDEN çağrılmıyor.
     kisiGizle+oncelik kapalı diye atlasaydık rol renkleri sessizce ölürdü.
     Bir adımı kapıya eklemeden önce içinde başka özelliğin işi var mı diye
     bak — ad yanıltıcı olabiliyor. */
  var turTemiz = Object.create(null);      // kapandıktan sonra temizlik turu yapıldı mı

  function pkAdimAcik(ad) {
    var a = PK_TUR_AYAR[ad];
    if (!a) return true;                    // kapısı yok → çekirdek adım, hep koşar
    /* { ve: [...] } → HEPSİ açık olmalı; biri kapalıysa adım ölü. */
    if (a.ve) {
      for (var j = 0; j < a.ve.length; j++) if (settings[a.ve[j]] === false) return false;
      return true;
    }
    /* Dizi → BİRİ bile açıksa adımın yapacak işi var. */
    for (var i = 0; i < a.length; i++) if (settings[a[i]] !== false) return true;
    return false;
  }

  var PK_TUR_KANAL = {};
  ['silinmisDenetle', 'susturKur', 'processAllMessages', 'yanitAlintiRozetleri', 'processStreamerHeader',
   'sohbetTara', 'sohbetciFiltreCiz', 'sohbetciEtiketle', 'sohbetciSayac',
   'kisiUygula', 'mesajDurumUygula', 'bahsetTiklanabilir', 'cerceveUygula',
   'lpTum', 'yanSohbetKur', 'araButonuKur', 'sohbetMenuButonuKur',
   'sohbetMenuNokta', 'modSeritKur', 'pkAyarDugmesiKur',
   'oynaticiButonlariKur', 'otoTiyatroKur', 'otoKaliteKur', 'uyarlanabilirHiz',
   'tiyatroNavKur', 'bindirKur', 'vodButonuKur', 'sesUygula',
   'kuralKabulUygula', 'hakkindaUygula', 'scIzleyiciOrnekle', 'baglantiNoktasiKur'].forEach(function (a) { PK_TUR_KANAL[a] = 1; });

  var PK_TUR_RAF = ['silinmisDenetle', 'susturKur', 'processAllMessages', 'yanitAlintiRozetleri', 'processProfilePopups', 'processStreamerHeader', 'processSearchResults', 'applyChannelBlocks', 'sabitKanalUygula', 'hakkindaUygula', 'kuralKabulUygula', 'sesUygula', 'onizKickBagla', 'kisiUygula', 'mesajDurumUygula', 'bahsetTiklanabilir', 'katUygula', 'katButonuKur', 'engelleIkonuKur', 'panelKonumla', 'yanSohbetKur', 'sohbetTara', 'sohbetciFiltreCiz', 'sohbetciEtiketle', 'sohbetciSayac', 'araButonuKur', 'eskiKanalMenusuTemizle', 'sohbetMenuButonuKur', 'sohbetMenuNokta', 'modSeritKur', 'vodButonuKur', 'oynaticiButonlariKur', 'otoTiyatroKur', 'otoKaliteKur', 'uyarlanabilirHiz', 'tiyatroNavKur', 'bindirKur', 'cerceveUygula', 'pkAyarCakismaDenetle', 'pkAyarDugmesiKur', 'lpTum'];
  var PK_TUR_ZAMAN = ['sabitKanalUygula', 'rosterTazele', 'odulSor', 'hakkindaUygula', 'kuralKabulUygula', 'sesUygula', 'onizKickBagla', 'yanSohbetKur', 'sohbetTara', 'sohbetciFiltreCiz', 'sohbetciEtiketle', 'sohbetciSayac', 'araButonuKur', 'baglantiNoktasiKur', 'kisiUygula', 'mesajDurumUygula', 'bahsetTiklanabilir', 'katUygula', 'katButonuKur', 'engelleIkonuKur', 'vodButonuKur', 'oynaticiButonlariKur', 'otoTiyatroKur', 'otoKaliteKur', 'bindirKur', 'cerceveUygula', 'scIzleyiciOrnekle', 'eskiKanalMenusuTemizle', 'sohbetMenuButonuKur', 'sohbetMenuNokta', 'modSeritKur', 'pkAyarDugmesiKur', 'lpTum'];

  var badgeRaf = 0, badgeSonTur = 0;
  /* 10.11 — YÜKLEME HIZI: tur (38 işlevlik PK_TUR_RAF) mutasyon fırtınasında
     (ilk render) HER KAREDE koşuyordu → 60/sn × 38 işlev = ana sayfa kilidi.
     Artık turlar arasına en az ~150 ms koyuyoruz: rAF ile hizalı kalır ama
     150 ms geçmeden çalışmaz → fırtınada ~6/sn. Rozet/kenar öğeleri en fazla
     150 ms gecikmeyle görünür (fark edilmez), tur yükü ~10× düşer. Mutasyon
     durunca son planlanan rAF yine çalışır → hiçbir güncelleme kaçmaz. */
  function badgeCalis() {
    if (performance.now() - badgeSonTur < 150) { badgeRaf = requestAnimationFrame(badgeCalis); return; }
    badgeRaf = 0; badgeSonTur = performance.now(); pkTur(PK_TUR_RAF);
  }
  function scheduleBadgePass() {
    if (badgeRaf) return;
    badgeRaf = requestAnimationFrame(badgeCalis);
  }

  // Badge görsellerini ÖNDEN yükle (HTTP cache'i ısıt) → veri gelir gelmez tüm
  // rozet görselleri indirilir; o kişi mesaj atınca görsel anında basılır, geç pop-in olmaz.
  var badgeImgWarm = {};
  function preloadBadgeImages(defs) {
    Object.keys(defs || {}).forEach(function (k) {
      if (defs[k] && defs[k].img) return;        // zaten data-URI olarak gömülü → preload gerekmez
      var url = defs[k] && defs[k].image;
      if (!url || badgeImgWarm[url]) return;
      badgeImgWarm[url] = 1;
      var im = new Image(); im.referrerPolicy = 'no-referrer'; im.src = url;
    });
  }
  /* TAM LİSTE → byName/colorsByName. Her tazelemede SIFIRDAN kurulur; böylece
     admin bir rozeti geri alınca ~5 dk içinde sohbetten de düşer. */
  /* DİKKAT — çerçeve burada da işlenmeli. İki ayrı veri yolu var:
       · TAM LİSTE (sunucu `legacy_full_list` açıkken hepsini döndürür) → burası
       · SORGU MODU (yalnızca görünen adlar sorulur)                    → mergeBadgeUsers
     Çerçeveyi ilk eklerken yalnızca mergeBadgeUsers'a koymuştum; sunucu tam liste
     döndürdüğü için bu dal çalışıyor ve `frameByName` hiç dolmuyordu → API'de
     "frame" görünüyor ama eklentide çerçeve çıkmıyordu. */
  function buildBadgeMapsFromList(users) {
    var defs = badgeMaps.defs || {};
    var fdefs = badgeMaps.frames || {};
    var byName = {}, colors = {}, frames = {}, satirlar = {}, akanlar = {}, aylar = {};
    users.forEach(function (u) {
      if (!u || !u.user) return;
      var name = String(u.user).toLowerCase();
      var keys = Array.isArray(u.badge) ? u.badge : [u.badge];
      var grad = null;
      keys.forEach(function (k) {
        if (!k || !defs[k]) return;
        if (!byName[name]) byName[name] = [];
        if (byName[name].indexOf(k) < 0) byName[name].push(k);
        if (Array.isArray(defs[k].gradient) && defs[k].gradient.length) grad = defs[k].gradient;
      });
      // 10.15 — akan isim (gerekçe: mergeBadgeUsers'taki açıklama)
      var akan = Array.isArray(u.gradient) ? akanPalet(u.gradient) : null;
      if (akan) { akanlar[name] = akan; if (grad) colors[name] = grad.slice(); }
      else if (Array.isArray(u.gradient) && u.gradient.length) colors[name] = u.gradient.slice();
      else if (grad) colors[name] = grad.slice();
      if (u.frame && fdefs[u.frame]) frames[name] = u.frame;   // profil çerçevesi
      var ayS = parseInt(u.ay, 10); if (ayS > 0) aylar[name] = ayS;   // gerçek kıdem
      // 10.15 — satır rengi (gerekçe: mergeBadgeUsers'taki açıklama)
      if (typeof u.satir === 'string' && /^#[0-9a-f]{6}$/i.test(u.satir)) satirlar[name] = u.satir.toLowerCase();
    });
    badgeMaps.byName = byName;
    badgeMaps.colorsByName = colors;
    badgeMaps.frameByName = frames;
    badgeMaps.satirByName = satirlar;
    badgeMaps.akanByName = akanlar;
    badgeMaps.ayByName = aylar;
  }

  function refreshBadgeData(force) {
    if (!badgeActive()) return;
    sendBg({ type: 'getBadges', force: !!force }).then(function (d) {
      // Yanıt geçersizse (SW uykuda / eklenti güncellenirken bağlam düştü) mevcut
      // rozetleri KORU — sıfırlayıp sohbetten düşürme.
      if (!d || typeof d !== 'object' || !d.badges || typeof d.badges !== 'object') return;
      var defs = d.badges;
      if (!badgeMaps) badgeMaps = { defs: defs, byName: {}, colorsByName: {}, frames: {}, frameByName: {},
                                    akanByName: {}, satirByName: {} };   // 10.15
      else badgeMaps.defs = defs;
      // Çerçeve tanımları (sunucu `frames` döndürmüyorsa özellik sessizce kapalı)
      badgeMaps.frames = (d.frames && typeof d.frames === 'object') ? d.frames : {};
      preloadBadgeImages(badgeMaps.defs);   // görselleri önden ısıt → anlık görünsün

      // Server tam liste döndürüyorsa (legacy yol) ad ad sorgu YAPMA → gecikme sıfır.
      // Döndürmüyorsa eski sorgu moduna düşeriz (server bayrağı değişse bile çalışır).
      var users = Array.isArray(d.users) ? d.users : [];
      badgeFullList = users.length > 0;
      if (badgeFullList) {
        buildBadgeMapsFromList(users);
      } else {
        if (force) { badgeWanted = {}; badgePending = {}; }   // periyodik: yeniden sorgula → badge ekle/çıkar yansısın
        noteVisibleUsers();   // backlog + yayıncı adlarını topla
        flushBadgeLookup();   // İLK batch'i HEMEN ateşle → backlog anında gelsin
      }
      processAllMessages();
      processProfilePopups();
      processStreamerHeader(); processSearchResults();   // yayıncı başlığındaki rozet de tazelensin
    });
  }
  function startBadges() {
    /* 10.12.1 — ana sayfa kapısı kaldırıldı (bkz. applyLocal). */
    if (!badgeActive()) { stopBadges(); return; }
    addBadgeStyle();
    if (!badgeMaps) refreshBadgeData(false);
    if (!badgeObserver) {
      badgeObserver = new MutationObserver(scheduleBadgePass);
      try { badgeObserver.observe(document.documentElement, { childList: true, subtree: true }); } catch (e) {}
    }
    /* SUNUCU YÜKÜ DÜZELTMESİ (429): burada `force: true` vardı ve `force`
       arka plandaki önbelleği TAMAMEN atlıyor. Bu tur SEKME BAŞINA çalıştığı
       için 5 açık sekme = 5 dakikada 5 ayrı sunucu isteği demekti; 17 bin
       kullanıcıda sunucu "too many requests" veriyordu.
       Artık `force: false`: arka plan önbelleği bayatsa kendisi tazeliyor
       (SWR), yani tarayıcı başına 5 dakikada EN FAZLA 1 istek gidiyor —
       kaç sekme açık olduğu fark etmiyor. Tazelik aynı kalıyor.
       `badgeWanted` sıfırlaması burada kalıyor: geri alınan rozetler yine
       düşsün diye adlar yeniden sorulmalı (bu ağ isteği değil, yerel liste). */
    /* ARKA PLAN SEKMESİ SAYMAZ: Kick sekmesi saatlerce açık kalıp hiç bakılmıyor
       olabilir (sabitlenmiş sekme). Timer yine çalışır ve boşuna istek üretir.
       Görünmezken atlıyoruz; sekme öne gelince aşağıdaki dinleyici tazeliyor —
       o çağrı da SWR'den geçtiği için önbellek tazeyse ağa hiç çıkmaz. */
    if (!badgeTimer) badgeTimer = setInterval(function () {
      if (document.hidden) return;
      badgeWanted = {}; badgePending = {};
      refreshBadgeData(false);
    }, 6 * 60 * 60 * 1000);   // 10.5: 6 SAAT. Sunucuya giden tek istek bu; kullanici basina
                              // gunde 4 deneme, cogu tarayici onbelleginden karsilanir.
    if (!badgeGorunurBagli) {
      badgeGorunurBagli = true;
      document.addEventListener('visibilitychange', function () {
        if (!document.hidden && badgeActive()) refreshBadgeData(false);
      });
    }
  }
  function stopBadges() {
    if (badgeObserver) { badgeObserver.disconnect(); badgeObserver = null; }
    if (badgeTimer) { clearInterval(badgeTimer); badgeTimer = null; }
    hideTip();
  }

  /* ---------- uygula ---------- */
  function applyLocal() {
    pushConfigToPage();
    /* 10.12.1 — ANA SAYFA KAPISI KALDIRILDI: asıl 40sn suçlu tema (recolor)'du
       ve o düzeltildi (ana sayfada sidebar+logo'ya kapsandı). cleaner artık
       debounce'lu, tur throttle'lı → ana sayfada da koşabilir; sidebar
       özellikleri (engelle/sabit/gizle) geri geldi. Yine kasarsa gözlemciyi
       sidebar'a kapsamak gerekir (recolor'da yaptığımız gibi). */
    if (!isActive()) { stopCleaner(); return; }
    if (settings.blockDom) {
      if (document.body) startCleaner();
      else document.addEventListener('DOMContentLoaded', startCleaner, { once: true });
    } else stopCleaner();
  }
  /* 10.12 — Sayfa tipine göre gözlemcileri senkronla (watchdog + boot çağırır).
     applyLocal/startBadges kanalSayfasi() kapısını kendi içinde uyguluyor; burada
     yalnız yeniden değerlendirmeyi tetikliyoruz. Durum değişmedikçe iş yapmaz. */
  var pkSonKanalDurumu = null;
  function pkGozlemcileriSenkronla() {
    var kanalda = kanalSayfasi();
    if (kanalda === pkSonKanalDurumu) return;
    pkSonKanalDurumu = kanalda;
    try { applyLocal(); } catch (e) {}
    if (settings.consented) { try { startBadges(); } catch (e) {} }
  }
  function apply() { applyLocal(); if (settings.consented) { buildWidget(); startBadges(); } else stopBadges(); renderWidget(); }

  /* ---------- başlat ---------- */
  function boot() {
    apply();
  sagTikKur();                                    // 10.5 oynatici sag tik menusu    // 10.5 — klavye kisayollari (tek sefer baglanir)
    pkGozcuZaman = setInterval(watchdog, 800);
    /* Oturumu dakikada bir diske yaz — sekme kazara kapanırsa en fazla bir
       dakikalık veri kaybolur. Değişmemişse yazmaz. */
    setInterval(function () { try { if (scKanal) otKaydet(scKanal, false); } catch (e) {} }, 60000);
    /* Sekme kapanırken son bir kez: bir dakikalık kaybı da önler. */
    window.addEventListener('pagehide', function () { try { if (scKanal) otKaydet(scKanal, true); } catch (e) {} });
  }
  if (document.body) boot();
  else document.addEventListener('DOMContentLoaded', boot, { once: true });

  document.addEventListener('mouseover', onHover, true);
  document.addEventListener('mouseover', function (e) { try { emoteHover(e); } catch (er) {} }, true);  // 10.5 emote önizleme
  /* Mesaj eylem şeridi — tek şerit, imleç hangi balondaysa oraya taşınır. */
  document.addEventListener('mouseover', function (e) {
    try {
      if (!settings.consented || settings.mesajEylem === false) return;
      var t0 = e.target;
      if (t0 && t0.closest && t0.closest('#pk-meylem')) return;      // şeridin kendi üstü
      /* Kısayol şeridi YALNIZ gerçek sohbet satırında. Panoda panel
         düğümüne çarpıp koca panelin tepesine açılıyordu. */
      var m = pkSatirBul(t0);
      if (m) meylemAc(m); else meylemKapat();
    } catch (er) {}
  }, true);
  document.addEventListener('scroll', function () { meylemKapat(); }, true);
  document.addEventListener('click', function (e) { try { sekmeIciTikla(e); } catch (er) {} }, true);  // 10.5 sekme ici baglanti
  document.addEventListener('click', function (e) { try { gorselTikla(e); } catch (er) {} }, true);      // 10.5 gorsel buyut
  document.addEventListener('contextmenu', function (e) { try { kartSagTik(e); } catch (er) {} }, true); // 10.5 kart sag tik
  document.addEventListener('mousemove', function (e) { try { tiyatroNavHareket(e); } catch (er) {} }, { passive: true });  // 10.5 tiyatro ust menu
  document.addEventListener('click', function () { kartMenuKapat(); }, true);
  document.addEventListener('keydown', function (e) {
    if (e.key !== 'Escape') return;
    kartMenuKapat(); gorselBuyutKapat(); sekmeIciKapat();
  }, true);
  /* Emote önizlemesi: sanal liste kayınca imleç emoteden "kopar" ve ikinci bir
     mouseover gelmeyebilir → baloncuk ekranda asılı kalıyordu. Rozet tooltip'inde
     kullandığımız kaçışın aynısı. */
  /* Kaydırmada KAPATMIYORUZ, kaynağı takip ediyoruz: `capture: true` sayfadaki
     her kaydırmayı yakalıyor ve sohbete mesaj gelince sohbetin kendini dibe
     alması önizlemeyi kapatıyordu — imleç emotenin üstündeyken bile. */
  document.addEventListener('scroll', function () { try { emoteKonumla(); } catch (e) {} }, true);
  window.addEventListener('blur', function () { emoteKapat(); });

  ['fullscreenchange', 'webkitfullscreenchange'].forEach(function (o) {   // 10.5 bindirmeyi tam ekrana tasi
    document.addEventListener(o, function () {
      /* Açık katmanları yeni köke taşı — yoksa tam ekranda görünmez kalırlar
         (istatistik penceresi, bildirim, ipucu, panel…). */
      try { pkKatmanTasi(); } catch (er0) {}
      try { bindirKapat(); bindirKur(); } catch (er) {}
      // Kick tam ekranda ikonlarini buyutuyor -> olcumu tazele
      try { ikonCagi++; oynaticiButonlariKur(); } catch (er2) {}
    });
  });
  window.addEventListener('resize', function () { ikonCagi++; }, { passive: true });
  /* Kaydırmada önizlemeyi kapatmaya gerek kalmadı: önizleme artık Kick'in
     kendi ipucunun içinde ve ipucunu Kick kendisi kapatıyor. */
  /* Buton artık karta demirli olduğu için kaydırmada yeniden konumlandırmak GEREKMİYOR
     (eskiden kaydırma anında tamamen gizleniyordu → kaybolup duruyordu). Burada yalnızca
     kartı silinmiş butonun takılı referansı temizleniyor. */
  window.addEventListener('scroll', tazeleHideBtn, true);
  window.addEventListener('resize', tazeleHideBtn);

  /* "14 dk" gibi göreli süreler sohbet dursa da ilerlemeli — ana tur yalnızca DOM
     değişince çalıştığı için 30 sn'de bir etiketleri tazeliyoruz. */
  setInterval(function () { if (!document.hidden) pkTur(PK_TUR_ZAMAN); }, 30000);

  /* OTOMATİK MESAJ TURDAN AYRI TUTULUYOR. Tur `document.hidden` ile kapalı —
     arka plan sekmesinde hiç koşmuyor — ama aralıklı duyurunun asıl işi tam
     orada: kullanıcı başka sekmede yayın izlerken duyuru gitmeye devam etmeli
     (kullanıcı isteği). Adım ucuz (birkaç karşılaştırma), gizli sekmede
     çalıştırmanın bedeli yok; turun tamamını arka planda açmak ise bütün
     gözlemcileri diriltirdi — ana sayfa kasmasının sebebi tam buydu.

     Chrome arka plan zamanlayıcısını kısabiliyor (sekme 5 dk gizliyse
     ~dakikada bir). Karşılaştırma `Date.now()` üzerinden yapıldığı için
     gecikme telafi ediliyor: duyuru kaçmıyor, en fazla bir tur geç gidiyor.

     Komut cevapları ve mesaj sayacı buraya bağlı DEĞİL; onlar sokete bağlı
     (scOlaydanMesaj) ve zaten arka planda çalışıyor. */
  /* Pencerenin turu artık GÖNDERMİYOR, yalnız durum bildiriyor — gönderimi
     SW alarmı yapıyor. Bu tur kısılsa bile duyuru gecikmiyor. */
  setInterval(function () { try { omBotDamga(); } catch (e) {} }, 30000);
  /* İlk bildirim turu beklemeden: pencere açılır açılmaz canlı sayılsın,
     yoksa kullanıcı hemen ayarları açtığında bot ölü görünüp anahtarlar
     sıfırlanır. */
  setTimeout(function () {
    try { omBotDamga(); } catch (e) {}
    try { omAcilisBak(); } catch (e) {}      // bot penceresinde ayarlar hep açık
  }, 1500);
  /* Pencere kapanıyor → kaydı hemen sil. BOTU DURDURAN ŞEY BU: SW kaydı
     görmeyince göndermeyi bırakıyor. */
  window.addEventListener('pagehide', function () {
    try { if (omPopoutMu() && scKanal) omBotYaz(scKanal, null); } catch (e) {}
  });

  chrome.storage.local.get(['settings', 'seenDrops', 'pkLink', 'theme', 'blockedChannels', 'playbackRate', 'pkNotlar', 'pkSabitKanal', 'pkKapaliAcik', 'pkHakkindaAcik', 'pkSes', 'pkRoster', 'pkRosterSablon', 'pkVodNokta', 'pkGizliKat', 'pkGizliKisi', 'pkOncelikli', 'pkGizliMesaj', 'pkRolRenk', 'pkRolKapali', 'pkVurguMuaf', 'pkBanSebep', 'pkYeni', 'pkAbone', 'pkCrossHaric', 'pkHizliSure', 'pkOtoMesaj',
     /* ⚠ BURAYA EKLEMEDEN `res.pkX` OKUMAK SESSİZCE BAŞARISIZ. Liste
        halinde `get` yalnız istenen anahtarları döndürüyor; eksik kalan
        anahtar her açılışta undefined geliyor ve varsayılana düşüyor.
        Üçü birden böyle kaçmıştı: tanıtım her yenilemede açılıyor,
        "bir daha gösterme" hiç hatırlanmıyor, bot ölü sanılıyordu. */
     'pkTanitim', 'pkOmOnaySus', 'pkOmBot', 'pkIsaret'], function (res) {
    // 10.5 — yerel notlar ve bahsedilme için kendi kullanıcı adın
    if (res && res.pkNotlar && typeof res.pkNotlar === 'object') pkNotlar = res.pkNotlar;
    if (res && res.pkLink && res.pkLink.username) pkBenimAd = String(res.pkLink.username);
    if (res && Array.isArray(res.pkBanSebep)) banSebepleri = res.pkBanSebep.slice(0, 20);
    if (res && Array.isArray(res.pkHizliSure)) susSureler = res.pkHizliSure.slice(0, 8);
    if (res && Array.isArray(res.pkCrossHaric)) {
      crossHaric = Object.create(null);
      res.pkCrossHaric.forEach(function (x) { if (x) crossHaric[String(x).toLowerCase()] = 1; });
    }
    /* Otomatik mesaj ayarları KANAL BAŞINA (bkz. omBosAyar üstündeki başlık). */
    if (res && res.pkOtoMesaj && typeof res.pkOtoMesaj === 'object') {
      var omHam = res.pkOtoMesaj;
      omTum = Object.create(null);
      if (omEskiBicimMi(omHam)) {
        var omEsk = omAyarNormal(omHam);
        omEsk.aralikAcik = false; omEsk.komutAcik = false;
        omTum[OM_TASIMA] = omEsk;
      } else {
        for (var omK in omHam) {
          if (omHam[omK] && typeof omHam[omK] === 'object') omTum[omK] = omAyarNormal(omHam[omK]);
        }
      }
      omKanalaGec();
    }
    if (res && res.pkOmOnaySus === true) omOnaySus = true;
    /* Tanıtım: sayı yoksa 0 — yani hiç görülmemiş, ilk kurulum. */
    pkTanitimGorulen = (res && typeof res.pkTanitim === 'number') ? res.pkTanitim : 0;
    pkIsaretAcik = (res && res.pkIsaret === 1);
    /* ÜST ÇUBUK DÜĞMESİ DEPO OKUNMADAN ÖNCE KURULMUŞ OLABİLİR: `buildWidget`
       Kick'in çubuğu belirir belirmez çalışıyor, depo ise asenkron geliyor.
       O anda `pkIsaretAcik` false olduğu için çerçeve hiç çizilmiyordu —
       sohbet şeridindeki düğme her turda yenilendiği için kendini
       düzeltiyor, bu düğmenin böyle bir turu yok. */
    if (pkUstIsaret) { try { pkUstIsaret(); } catch (e) {} }
    /* Sayfanın yerleşmesini bekliyoruz: hemen açınca Kick'in kendi
       iskeleti üstüne biniyor ve ilk izlenim bozuk oluyor. */
    setTimeout(function () { try { pkTanitimBak(); } catch (e) {} }, 2500);
    /* `{}` ATANIYOR, null bırakılmıyor: null "daha okumadım" demek ve ayar
       penceresi o hâlde anahtarları sıfırlamıyor. */
    omBot = (res && res.pkOmBot && typeof res.pkOmBot === 'object') ? res.pkOmBot : {};
    if (res && res.pkRolRenk && typeof res.pkRolRenk === 'object') { rolRenk = Object.assign(Object.create(null), res.pkRolRenk); }
    if (res && Array.isArray(res.pkRolKapali)) { rolKapali = Object.create(null); res.pkRolKapali.forEach(function (x) { if (x) rolKapali[x] = 1; }); }
    if (res && Array.isArray(res.pkVurguMuaf)) { vurguMuaf = {}; res.pkVurguMuaf.forEach(function (x) { if (x) vurguMuaf[String(x).toLowerCase()] = 1; }); }
    if (res && Array.isArray(res.pkGizliMesaj)) { gizliMesaj = {}; res.pkGizliMesaj.forEach(function (x) { if (x) gizliMesaj[x] = 1; }); }
    if (res && Array.isArray(res.pkGizliKisi)) { gizliKisi = {}; res.pkGizliKisi.forEach(function (x) { if (x) gizliKisi[String(x).toLowerCase()] = 1; }); }
    if (res && Array.isArray(res.pkOncelikli)) { oncelikli = {}; res.pkOncelikli.forEach(function (x) { if (x) oncelikli[String(x).toLowerCase()] = 1; }); }
    if (res && Array.isArray(res.pkGizliKat)) { gizliKat = {}; res.pkGizliKat.forEach(function (x) { if (x) gizliKat[String(x).toLowerCase()] = 1; }); katUygula(); }
    /* Oynatma hızı özelliği kaldırıldı: kayıtlı bir hız varsa 1'e çekilip
       kaydı siliniyor, yoksa kullanıcı 2x'te kilitli kalırdı. */
    if (res && typeof res.playbackRate === 'number' && res.playbackRate !== 1) {
      try { chrome.storage.local.remove('playbackRate'); } catch (e) {}
    }
    if (res && res.settings) settings = Object.assign({}, DEFAULTS, res.settings);
    pkKenarAynala(settings);       // sonraki sayfa yüklemesi perdeyi doğru karar versin
    /* Yenilik noktaları: kayıt yoksa temiz kurulum sayılır ve hiçbir nokta
       gösterilmez; sürüm grubu değiştiyse bu sürümün yenilikleri açılır. */
    pkYeniKur(res && res.pkYeni);
    if (res && Array.isArray(res.blockedChannels)) {
      blocked = {};
      res.blockedChannels.forEach(function (s) { if (s) blocked[String(s).toLowerCase()] = 1; });
      applyChannelBlocks();
    }
    if (res && Array.isArray(res.pkSabitKanal)) sabitYukle(res.pkSabitKanal);
    if (res && typeof res.pkKapaliAcik === 'boolean') kapaliAcik = res.pkKapaliAcik;
    if (res && res.pkSes && typeof res.pkSes.s === 'number') {
      sesSeviye = res.pkSes.s; sesSessiz = !!res.pkSes.m;
    }
    if (res && Array.isArray(res.pkHakkindaAcik)) {
      hakkindaAcik = Object.create(null);
      res.pkHakkindaAcik.forEach(function (k) { if (k) hakkindaAcik[k] = 1; });
    }
    /* Önbellekteki liste: sayfa açılır açılmaz boyayabilmek için. Tazesi
       arkadan geliyor. */
    if (res && res.pkRosterSablon) sablonYukle(res.pkRosterSablon);
    if (res && res.pkRoster && Array.isArray(res.pkRoster.k)) {
      roster = res.pkRoster.k; rosterZaman = res.pkRoster.ts || 0;
    }
    /* Taze listeyi hemen iste: önbellek eskiyse bile arkadan güncellenir. */
    try { rosterCek(function () { try { sabitKanalUygula(); } catch (e) {} }); } catch (e) {}
    if (res && Array.isArray(res.seenDrops)) seenIds = res.seenDrops;
    if (res && res.pkLink && res.pkLink.token) linked = true;   // (CTA artık gizlenmiyor; linked yalnızca bilgi)
    if (res && res.pkAbone) abone = res.pkAbone;                // 10.15 abonelik durumu
    if (res && res.theme && /^#[0-9a-f]{6}$/i.test(res.theme.color || '')) {
      themeColor = res.theme.color.toLowerCase();
      try { sabitRenkUygula(); } catch (e) {}
    }
    apply();
  });
  chrome.storage.onChanged.addListener(function (changes, area) {
    if (area !== 'local') return;
    // Başka bir Kick sekmesinde drop görülünce burada da rozet güncellensin.
    if (changes.seenDrops) {
      seenIds = Array.isArray(changes.seenDrops.newValue) ? changes.seenDrops.newValue : [];
      renderBadge();
    }
    if (changes.settings) {
      settings = Object.assign({}, DEFAULTS, changes.settings.newValue || {});
      pkKenarAynala(settings);     // panelden kapatıldı → sonraki yüklemede perde yok
      apply();
    }
    /* Bot penceresi açıldı/kapandı → ayar penceresi anahtarları anında doğru
       göstersin, damganın eskimesini beklemesin. */
    if (changes.pkOmBot) omBot = changes.pkOmBot.newValue || {};
    // Başka sekmede kanal sabitlenince burada da aynı sıralama görünsün.
    if (changes.pkSabitKanal) { sabitYukle(changes.pkSabitKanal.newValue); sabitKanalUygula(); }
    if (changes.pkKapaliAcik) { kapaliAcik = !!changes.pkKapaliAcik.newValue; sabitKanalUygula(); }
    // Başka sekmede kanal gizlenince/geri alınınca burada da uygulansın.
    if (changes.blockedChannels) {
      var eski = blocked; blocked = {};
      (Array.isArray(changes.blockedChannels.newValue) ? changes.blockedChannels.newValue : [])
        .forEach(function (s) { if (s) blocked[String(s).toLowerCase()] = 1; });
      Object.keys(eski).forEach(function (s) { if (!blocked[s]) unhideChannel(s); });   // engel kalktı → geri getir
      applyChannelBlocks();
    }
    if (changes.pkLink) {
      var pl = changes.pkLink.newValue;
      linked = !!(pl && pl.token);
    }
    if (changes.pkAbone) {
      abone = changes.pkAbone.newValue || { abone: false, ay: 0, rozet: '', gecmis: false, bitis: '', ts: 0 };
      if (panelEl && document.contains(panelEl) && panelYol[panelYol.length - 1] === 'abone') {
        try { panelCiz(); } catch (e) {}
      }
    }
    if (changes.theme) {
      var th = changes.theme.newValue;
      themeColor = (th && /^#[0-9a-f]{6}$/i.test(th.color || '')) ? th.color.toLowerCase() : '#53fc18';
      try { sabitRenkUygula(); } catch (e) {}
    }
  });

  /* 16. CANLI YAYIN OTOMATİK DİL ALGILAYICI, CANLI ALTYAZI VE SESLİ AI DUBLAJ SİSTEMİ */
  var pkRecognition = null;
  var pkAltyaziBox = null;
  var pkIsRecognizing = false;
  var pkLastTranscript = '';
  var pkDublajOrijinalSes = 1;

  function pkCanliAltyaziStilUygula() {
    if (!pkAltyaziBox) return;
    var sz = settings.altyaziFontBoyut || 16;
    var clr = settings.altyaziRenk || '#facc15';
    var weight = settings.altyaziKalinlik || '700';
    var bgAlpha = (settings.altyaziBgOpaklik != null ? settings.altyaziBgOpaklik : 75) / 100;
    var pos = settings.altyaziKonum || 'alt';

    pkAltyaziBox.style.setProperty('font-size', sz + 'px', 'important');
    pkAltyaziBox.style.setProperty('color', clr, 'important');
    pkAltyaziBox.style.setProperty('font-weight', weight, 'important');
    pkAltyaziBox.style.setProperty('background-color', 'rgba(0, 0, 0, ' + bgAlpha + ')', 'important');
    pkAltyaziBox.style.setProperty('z-index', '2147483647', 'important');
    pkAltyaziBox.style.setProperty('pointer-events', 'none', 'important');
    pkAltyaziBox.style.setProperty('position', 'absolute', 'important');
    pkAltyaziBox.style.setProperty('left', '50%', 'important');
    pkAltyaziBox.style.setProperty('transform', 'translateX(-50%)', 'important');

    if (pos === 'ust') {
      pkAltyaziBox.style.setProperty('bottom', 'auto', 'important');
      pkAltyaziBox.style.setProperty('top', '70px', 'important');
    } else {
      pkAltyaziBox.style.setProperty('top', 'auto', 'important');
      pkAltyaziBox.style.setProperty('bottom', '85px', 'important');
    }
  }

  function pkCanliAltyaziGoster(metinHtml) {
    if (!pkAltyaziBox) return;
    pkAltyaziBox.style.setProperty('display', 'block', 'important');
    pkAltyaziBox.style.setProperty('opacity', '1', 'important');
    pkAltyaziBox.innerHTML = metinHtml;
  }

  function pkCanliAltyaziVeDublajKur() {
    var aktifMi = !!(settings.canliAltyazi || settings.sesliDublaj);
    if (!aktifMi) {
      if (pkAltyaziBox) { pkAltyaziBox.remove(); pkAltyaziBox = null; }
      if (pkRecognition && pkIsRecognizing) {
        try { pkRecognition.stop(); } catch (e) {}
        pkIsRecognizing = false;
      }
      return;
    }

    // Doğru video oynatıcı kapsayıcısını bul
    var fullscreenEl = document.fullscreenElement || document.webkitFullscreenElement;
    var vWrap = document.querySelector('.vjs-tech, video, [data-testid="video-player"]');
    var playerContainer = fullscreenEl || (vWrap && (vWrap.closest('.player-wrapper, #video-player, div[class*="player-wrapper"], [data-testid="video-player"]') || vWrap.parentElement)) || document.querySelector('.player-wrapper, #video-player');
    if (!playerContainer) return;

    // Eğer kapsayıcı değiştiyse (örn: tam ekrana geçildiyse) altyazı kutusunu içine taşı
    if (!pkAltyaziBox || !document.contains(pkAltyaziBox) || pkAltyaziBox.parentElement !== playerContainer) {
      if (pkAltyaziBox) pkAltyaziBox.remove();

      /* Kapsayıcıya position:relative yoksa ekle — absolute çocuk düzgün konumlansın */
      var pcPos = window.getComputedStyle(playerContainer).position;
      if (pcPos === 'static') playerContainer.style.position = 'relative';

      pkAltyaziBox = document.createElement('div');
      pkAltyaziBox.id = 'pk-canli-altyazi';
      var konum = settings.altyaziKonum || 'alt';
      var pozisyon = konum === 'ust' ? 'top: 12px !important;' : 'bottom: 60px !important;';
      pkAltyaziBox.style.cssText = `
        position: absolute !important;
        left: 50% !important;
        ${pozisyon}
        transform: translateX(-50%) !important;
        background: rgba(0, 0, 0, 0.82) !important;
        backdrop-filter: blur(8px) !important;
        text-align: center !important;
        padding: 8px 18px !important;
        border-radius: 10px !important;
        border: 1.5px solid rgba(83, 252, 24, 0.6) !important;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.9) !important;
        z-index: 2147483647 !important;
        pointer-events: none !important;
        max-width: 85% !important;
        line-height: 1.45 !important;
        transition: all 0.2s ease !important;
        display: block !important;
        font-size: ${settings.altyaziFontBoyut || 15}px !important;
        color: ${settings.altyaziRenk || '#facc15'} !important;
        font-weight: ${settings.altyaziKalinlik || 600} !important;
      `;
      playerContainer.appendChild(pkAltyaziBox);

      var srcF = (pkDilBilgisi(settings.altyaziKaynakDil || 'auto') || {}).b || '🌐';
      var trgF = (pkDilBilgisi(settings.altyaziHedefDil || 'tr') || {}).b || '🇹🇷';
      pkCanliAltyaziGoster('<span style="font-size:12.5px;opacity:0.9;color:#53fc18;">🎙️ Canlı Altyazı Hazır (' + srcF + ' ➔ ' + trgF + ') — Konuşma bekleniyor...</span>');
    }
    pkCanliAltyaziStilUygula();

    if (pkIsRecognizing) return;

    var SpeechRec = window.SpeechRecognition || window.webkitSpeechRecognition;
    if (!SpeechRec) {
      pkCanliAltyaziGoster('<span style="color:#ef4444;font-size:12px;">⚠️ Tarayıcınız Web Speech API ses tanımayı desteklemiyor.</span>');
      return;
    }

    try {
      pkRecognition = new SpeechRec();
      pkRecognition.continuous = true;
      pkRecognition.interimResults = true;
      
      var srcLang = settings.altyaziKaynakDil || 'auto';
      pkRecognition.lang = (srcLang === 'auto' ? 'en-US' : (srcLang === 'tr' ? 'tr-TR' : (srcLang + '-' + srcLang.toUpperCase())));

      pkRecognition.onstart = function () {
        pkIsRecognizing = true;
      };

      pkRecognition.onresult = function (event) {
        var transcript = '';
        for (var i = event.resultIndex; i < event.results.length; ++i) {
          transcript += event.results[i][0].transcript;
        }
        transcript = transcript.trim();
        if (!transcript || transcript === pkLastTranscript) return;
        pkLastTranscript = transcript;

        var hedef = settings.altyaziHedefDil || settings.ceviriDili || 'tr';
        var kaynak = settings.altyaziKaynakDil || 'auto';

        pkCanliAltyaziGoster('<span style="color:#9fa6ad;font-size:12px;">🎙️ Dinleniyor:</span> ' + esc(transcript));

        pkMesajCevir(transcript, hedef, kaynak, function (cevrilen, alg) {
          if (!cevrilen) return;
          var srcFlag = (pkDilBilgisi(alg || kaynak) || {}).b || '🌐';
          var trgFlag = (pkDilBilgisi(hedef) || {}).b || '🇹🇷';

          pkCanliAltyaziGoster('<span style="margin-right:6px;font-size:13px;">' + srcFlag + ' ➔ ' + trgFlag + '</span> <span style="color:' + (settings.altyaziRenk || '#facc15') + ';">' + esc(cevrilen) + '</span>');

          // Sesli AI Dublaj (TTS & Audio Ducking)
          if (settings.sesliDublaj && window.speechSynthesis) {
            try {
              speechSynthesis.cancel();
              var ut = new SpeechSynthesisUtterance(cevrilen);
              ut.lang = hedef === 'tr' ? 'tr-TR' : (hedef === 'en' ? 'en-US' : (hedef === 'de' ? 'de-DE' : (hedef === 'fr' ? 'fr-FR' : (hedef === 'es' ? 'es-ES' : (hedef === 'ru' ? 'ru-RU' : (hedef === 'ja' ? 'ja-JP' : (hedef === 'ar' ? 'ar-SA' : 'tr-TR')))))));
              ut.rate = 1.05;
              
              var video = document.querySelector('video');
              if (video) {
                pkDublajOrijinalSes = video.volume;
                video.volume = Math.max(0.15, pkDublajOrijinalSes * 0.35);
                ut.onend = function () {
                  if (video) video.volume = pkDublajOrijinalSes;
                };
              }
              speechSynthesis.speak(ut);
            } catch (err) {}
          }
        });
      };

      pkRecognition.onerror = function (e) {
        pkIsRecognizing = false;
        if (e && e.error === 'not-allowed') {
          pkCanliAltyaziGoster('<span style="color:#f87171;font-size:12px;">⚠️ Mikrofon / Ses İzni Gerekli (Adres çubuğundaki kilit simgesinden mikrofon izni verin)</span>');
        }
      };

      pkRecognition.onend = function () {
        pkIsRecognizing = false;
        if (settings.canliAltyazi || settings.sesliDublaj) {
          setTimeout(function () {
            try { if (pkRecognition && !pkIsRecognizing) pkRecognition.start(); } catch (e) {}
          }, 800);
        }
      };

      pkRecognition.start();
    } catch (e) {
      pkIsRecognizing = false;
    }
  }

  /* Video Kontrolleri Üzerinde [CC / 🌐] Canlı Altyazı & Dublaj Butonu ve Hızlı Menüsü */
  var pkAltyaziModalEl = null;
  function pkVideoAltyaziButonuKur() {
    var bar = document.querySelector('.vjs-control-bar, .player-controls, [data-testid="player-controls"]');
    if (!bar) return;
    if (bar.querySelector('#pk-cc-btn')) return;

    var ccBtn = document.createElement('button');
    ccBtn.id = 'pk-cc-btn';
    ccBtn.className = 'vjs-control vjs-button';
    ccBtn.style.cssText = 'display:inline-flex;align-items:center;justify-content:center;font-size:12px;font-weight:900;color:' + (settings.canliAltyazi || settings.sesliDublaj ? '#53fc18' : '#ffffff') + ';cursor:pointer;background:transparent;border:none;padding:0 6px;margin:0 2px;transition:all .15s ease;';
    ccBtn.innerHTML = '<span>CC/🌐</span>';
    ccBtn.title = 'Canlı Altyazı & Sesli Dublaj Ayarları';

    ccBtn.addEventListener('click', function (e) {
      e.stopPropagation();
      e.preventDefault();
      pkAltyaziModalAcKapat(ccBtn);
    });

    var istatBtn = bar.querySelector('#pk-vistat-btn') || bar.querySelector('.vjs-fullscreen-control') || bar.lastElementChild;
    if (istatBtn && istatBtn.parentElement === bar) {
      bar.insertBefore(ccBtn, istatBtn);
    } else {
      bar.appendChild(ccBtn);
    }
  }

  function pkAltyaziModalAcKapat(hedefBtn) {
    if (pkAltyaziModalEl) {
      pkAltyaziModalEl.remove();
      pkAltyaziModalEl = null;
      return;
    }

    var rect = hedefBtn.getBoundingClientRect();
    var modal = document.createElement('div');
    modal.id = 'pk-cc-modal';
    modal.style.cssText = `
      position: fixed;
      bottom: ${window.innerHeight - rect.top + 10}px;
      right: ${window.innerWidth - rect.right}px;
      width: 320px;
      background: #171a1c;
      border: 1px solid rgba(240, 241, 242, 0.15);
      border-radius: 10px;
      box-shadow: 0 12px 35px rgba(0, 0, 0, 0.7);
      color: #fff;
      z-index: 2147483600;
      padding: 12px;
      display: flex;
      flex-direction: column;
      gap: 10px;
      font-size: 12px;
    `;

    var baslik = '<div style="font-weight:bold;font-size:13px;color:#53fc18;display:flex;justify-content:space-between;align-items:center;">' +
      '<span>🎙️ Canlı Altyazı & Sesli Dublaj</span>' +
      '<button id="pk-cc-kapat" style="background:transparent;border:none;color:#9fa6ad;cursor:pointer;font-size:14px;">✕</button>' +
      '</div>';

    var anahtarlarHtml = `
      <div style="display:flex;flex-direction:column;gap:6px;background:rgba(255,255,255,0.04);padding:8px;border-radius:6px;">
        <label style="display:flex;justify-content:space-between;align-items:center;cursor:pointer;">
          <span>📝 Canlı Altyazı (AI)</span>
          <input type="checkbox" id="pk-chk-altyazi" ${settings.canliAltyazi ? 'checked' : ''} style="cursor:pointer;accent-color:#53fc18;">
        </label>
        <label style="display:flex;justify-content:space-between;align-items:center;cursor:pointer;">
          <span>🔊 Sesli AI Dublaj (TTS)</span>
          <input type="checkbox" id="pk-chk-dublaj" ${settings.sesliDublaj ? 'checked' : ''} style="cursor:pointer;accent-color:#53fc18;">
        </label>
      </div>
    `;

    var dilSeciciHtml = `
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:6px;">
        <div>
          <div style="font-size:10.5px;color:#9fa6ad;margin-bottom:2px;">Kaynak Dil:</div>
          <select id="pk-sel-src-lang" style="width:100%;background:#232629;color:#fff;border:1px solid rgba(255,255,255,0.15);padding:4px;border-radius:4px;font-size:11px;">
            <option value="auto">🌐 Otomatik Algıla</option>
            ${PK_DILLER_25.map(function(d){ return '<option value="' + d.k + '" ' + (settings.altyaziKaynakDil === d.k ? 'selected' : '') + '>' + d.b + ' ' + d.a + '</option>'; }).join('')}
          </select>
        </div>
        <div>
          <div style="font-size:10.5px;color:#9fa6ad;margin-bottom:2px;">Çevrilecek Hedef Dil:</div>
          <select id="pk-sel-trg-lang" style="width:100%;background:#232629;color:#fff;border:1px solid rgba(255,255,255,0.15);padding:4px;border-radius:4px;font-size:11px;">
            ${PK_DILLER_25.map(function(d){ return '<option value="' + d.k + '" ' + ((settings.altyaziHedefDil || 'tr') === d.k ? 'selected' : '') + '>' + d.b + ' ' + d.a + '</option>'; }).join('')}
          </select>
        </div>
      </div>
    `;

    var gorunumHtml = `
      <div style="background:rgba(255,255,255,0.04);padding:8px;border-radius:6px;display:flex;flex-direction:column;gap:6px;">
        <div style="font-size:11px;font-weight:bold;color:#facc15;">🎨 Altyazı Görünüm Ayarları</div>
        
        <div style="display:flex;justify-content:space-between;align-items:center;">
          <span style="font-size:11px;color:#9fa6ad;">Yazı Boyutu:</span>
          <select id="pk-sel-font-sz" style="background:#232629;color:#fff;border:1px solid rgba(255,255,255,0.15);padding:2px 6px;border-radius:4px;font-size:11px;">
            <option value="12" ${settings.altyaziFontBoyut === 12 ? 'selected' : ''}>12px (Küçük)</option>
            <option value="14" ${settings.altyaziFontBoyut === 14 ? 'selected' : ''}>14px</option>
            <option value="16" ${(settings.altyaziFontBoyut || 16) === 16 ? 'selected' : ''}>16px (Normal)</option>
            <option value="18" ${settings.altyaziFontBoyut === 18 ? 'selected' : ''}>18px</option>
            <option value="22" ${settings.altyaziFontBoyut === 22 ? 'selected' : ''}>22px (Büyük)</option>
            <option value="26" ${settings.altyaziFontBoyut === 26 ? 'selected' : ''}>26px (Dev)</option>
          </select>
        </div>

        <div style="display:flex;justify-content:space-between;align-items:center;">
          <span style="font-size:11px;color:#9fa6ad;">Yazı Rengi:</span>
          <div style="display:flex;gap:4px;">
            ${['#facc15', '#ffffff', '#38bdf8', '#53fc18', '#f472b6'].map(function(c){
              return '<button class="pk-color-pick" data-color="' + c + '" style="width:18px;height:18px;border-radius:50%;background:' + c + ';border:' + ((settings.altyaziRenk || '#facc15') === c ? '2px solid #fff' : 'none') + ';cursor:pointer;"></button>';
            }).join('')}
          </div>
        </div>

        <div style="display:flex;justify-content:space-between;align-items:center;">
          <span style="font-size:11px;color:#9fa6ad;">Konum:</span>
          <select id="pk-sel-pos" style="background:#232629;color:#fff;border:1px solid rgba(255,255,255,0.15);padding:2px 6px;border-radius:4px;font-size:11px;">
            <option value="alt" ${(settings.altyaziKonum || 'alt') === 'alt' ? 'selected' : ''}>⬇️ Alt-Orta</option>
            <option value="ust" ${settings.altyaziKonum === 'ust' ? 'selected' : ''}>⬆️ Üst-Orta</option>
          </select>
        </div>
      </div>
    `;

    modal.innerHTML = baslik + anahtarlarHtml + dilSeciciHtml + gorunumHtml;
    document.body.appendChild(modal);
    pkAltyaziModalEl = modal;

    modal.querySelector('#pk-cc-kapat').addEventListener('click', function () {
      modal.remove();
      pkAltyaziModalEl = null;
    });

    modal.querySelector('#pk-chk-altyazi').addEventListener('change', function (e) {
      var v = e.target.checked;
      sendBg({ type: 'setSettings', payload: { canliAltyazi: v } });
      settings.canliAltyazi = v;
      hedefBtn.style.color = (settings.canliAltyazi || settings.sesliDublaj ? '#53fc18' : '#ffffff');
      pkCanliAltyaziVeDublajKur();
      toast(v ? 'Canlı altyazı açıldı' : 'Canlı altyazı kapatıldı');
    });

    modal.querySelector('#pk-chk-dublaj').addEventListener('change', function (e) {
      var v = e.target.checked;
      sendBg({ type: 'setSettings', payload: { sesliDublaj: v } });
      settings.sesliDublaj = v;
      hedefBtn.style.color = (settings.canliAltyazi || settings.sesliDublaj ? '#53fc18' : '#ffffff');
      pkCanliAltyaziVeDublajKur();
      toast(v ? 'Sesli AI dublaj açıldı' : 'Sesli AI dublaj kapatıldı');
    });

    modal.querySelector('#pk-sel-src-lang').addEventListener('change', function (e) {
      var v = e.target.value;
      sendBg({ type: 'setSettings', payload: { altyaziKaynakDil: v } });
      settings.altyaziKaynakDil = v;
      pkCanliAltyaziVeDublajKur();
      toast('Altyazı kaynak dili güncellendi');
    });

    modal.querySelector('#pk-sel-trg-lang').addEventListener('change', function (e) {
      var v = e.target.value;
      sendBg({ type: 'setSettings', payload: { altyaziHedefDil: v } });
      settings.altyaziHedefDil = v;
      pkCanliAltyaziVeDublajKur();
      toast('Altyazı hedef dili güncellendi');
    });

    modal.querySelector('#pk-sel-font-sz').addEventListener('change', function (e) {
      var v = parseInt(e.target.value, 10);
      sendBg({ type: 'setSettings', payload: { altyaziFontBoyut: v } });
      settings.altyaziFontBoyut = v;
      pkCanliAltyaziStilUygula();
    });

    modal.querySelector('#pk-sel-pos').addEventListener('change', function (e) {
      var v = e.target.value;
      sendBg({ type: 'setSettings', payload: { altyaziKonum: v } });
      settings.altyaziKonum = v;
      pkCanliAltyaziStilUygula();
    });

    modal.querySelectorAll('.pk-color-pick').forEach(function (btn) {
      btn.addEventListener('click', function () {
        var clr = btn.getAttribute('data-color');
        sendBg({ type: 'setSettings', payload: { altyaziRenk: clr } });
        settings.altyaziRenk = clr;
        modal.querySelectorAll('.pk-color-pick').forEach(function(b){ b.style.border = (b.getAttribute('data-color') === clr ? '2px solid #fff' : 'none'); });
        pkCanliAltyaziStilUygula();
      });
    });
  }

  /* ----------------------------------------------------------------------
   * 1. SOHBET İÇİ CANLI ARAMA & FİLTRELEME (pkSohbetFiltre)
   * ---------------------------------------------------------------------- */
  var pkFiltreMetni = '';
  var pkFiltreKutusu = null;

  function pkSohbetFiltreUygula() {
    if (settings.sohbetFiltre === false) return;
    var kap = document.querySelector('#chatroom-messages, [data-chat-messages="true"]');
    if (!kap) return;
    var satirlar = kap.querySelectorAll('[data-index], .chat-entry, [class*="chat-entry"]');
    var aranan = pkFiltreMetni.trim().toLowerCase();
    for (var i = 0; i < satirlar.length; i++) {
      var sat = satirlar[i];
      if (!aranan) {
        sat.style.removeProperty('display');
        continue;
      }
      var text = (sat.textContent || '').toLowerCase();
      if (text.indexOf(aranan) >= 0) {
        sat.style.removeProperty('display');
      } else {
        sat.style.setProperty('display', 'none', 'important');
      }
    }
  }

  function pkSohbetFiltreKutusuKur() {
    if (settings.sohbetFiltre === false || !kanalSayfasi()) {
      if (pkFiltreKutusu && pkFiltreKutusu.parentNode) pkFiltreKutusu.parentNode.removeChild(pkFiltreKutusu);
      pkFiltreKutusu = null;
      return;
    }
    if (pkFiltreKutusu && document.contains(pkFiltreKutusu)) return;
    var hedef = document.querySelector('#chatroom-messages, [data-chat-messages="true"]');
    if (!hedef || !hedef.parentElement) return;

    var wrap = document.createElement('div');
    wrap.id = 'pk-sohbet-filtre-bar';
    wrap.style.cssText = 'display:flex;align-items:center;gap:6px;padding:5px 10px;background:#171a1c;border-bottom:1px solid rgba(240,241,242,.15);font-size:12px;color:#fff;position:sticky;top:0;z-index:9999;box-shadow:0 2px 6px rgba(0,0,0,0.3);';
    wrap.innerHTML = '<svg style="width:13px;height:13px;color:#53fc18;flex:none;" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><path d="M21 21l-4.35-4.35"/></svg>' +
      '<input type="text" id="pk-filtre-inp" placeholder="' + esc(t('pkFiltreAra') || 'Sohbette ara...') + '" style="flex:1;background:rgba(255,255,255,.06);border:1px solid rgba(240,241,242,.12);border-radius:4px;color:#fff;padding:3px 8px;font-size:11px;outline:none;" value="' + esc(pkFiltreMetni) + '">' +
      '<button id="pk-filtre-temizle" title="Aramayı Temizle" style="background:transparent;border:none;color:#9fa6ad;cursor:pointer;font-size:12px;padding:2px 4px;">✕</button>';

    var inp = wrap.querySelector('#pk-filtre-inp');
    inp.addEventListener('input', function (e) {
      pkFiltreMetni = e.target.value;
      pkSohbetFiltreUygula();
    });
    wrap.querySelector('#pk-filtre-temizle').addEventListener('click', function () {
      inp.value = '';
      pkFiltreMetni = '';
      pkSohbetFiltreUygula();
    });

    hedef.parentElement.insertBefore(wrap, hedef);
    pkFiltreKutusu = wrap;
  }

  /* ----------------------------------------------------------------------
   * 2. CANLI KLİP ALICI (MediaRecorder)
   * ---------------------------------------------------------------------- */
  var pkKlipKaydedici = null;
  var pkKlipParcalar = [];
  var pkKlipKayitSuruyor = false;

  function pkKlipAl(sureSn) {
    sureSn = sureSn || settings.klipSure || 30;
    var video = document.querySelector('video');
    if (!video) {
      toast(t('pkTOynaticiYok') || 'Oynatıcı bulunamadı');
      return;
    }
    if (pkKlipKayitSuruyor) {
      toast('Klip zaten kaydediliyor...');
      return;
    }

    try {
      var stream = video.captureStream ? video.captureStream() : (video.mozCaptureStream ? video.mozCaptureStream() : null);
      if (!stream) {
        toast('Tarayıcı video akışını yakalamayı desteklemiyor');
        return;
      }

      var mime = MediaRecorder.isTypeSupported('video/webm;codecs=vp9,opus') ? 'video/webm;codecs=vp9,opus' : 'video/webm';
      var rec = new MediaRecorder(stream, { mimeType: mime });
      pkKlipParcalar = [];
      pkKlipKayitSuruyor = true;
      toast('🎬 Klip kaydediliyor (' + sureSn + ' sn)...');

      rec.ondataavailable = function (e) {
        if (e.data && e.data.size > 0) pkKlipParcalar.push(e.data);
      };

      rec.onstop = function () {
        pkKlipKayitSuruyor = false;
        if (!pkKlipParcalar.length) {
          toast('Klip verisi alınamadı');
          return;
        }
        var blob = new Blob(pkKlipParcalar, { type: 'video/webm' });
        var url = URL.createObjectURL(blob);
        var a = document.createElement('a');
        var kanal = scMevcutKanal() || 'kick';
        a.href = url;
        a.download = kanal + '-klip-' + Date.now() + '.webm';
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        setTimeout(function () { URL.revokeObjectURL(url); }, 60000);
        toast('✅ Klip kaydedildi!');
      };

      rec.start(1000);
      pkKlipKaydedici = rec;

      setTimeout(function () {
        try {
          if (rec.state !== 'inactive') rec.stop();
        } catch (e) {}
      }, sureSn * 1000);
    } catch (e) {
      pkKlipKayitSuruyor = false;
      toast('Klip alma hatası: ' + (e.message || 'Bilinmeyen hata'));
    }
  }

  /* ----------------------------------------------------------------------
   * 3. SOHBET & MODERASYON GÜNLÜĞÜ DIŞA AKTARMA (CSV / TXT)
   * ---------------------------------------------------------------------- */
  function pkSohbetExport(format) {
    format = format || 'txt';
    var veriler = (typeof otKayit === 'object' && otKayit && otKayit.msj) ? otKayit.msj : [];
    if (!veriler || !veriler.length) {
      toast(t('pkMesajYok') || 'Dışa aktarılacak mesaj bulunamadı');
      return;
    }
    var kanal = scMevcutKanal() || 'kick';
    var icerik = '';
    var dosyaAdi = kanal + '-sohbet-' + (new Date().toISOString().slice(0, 10)) + '.' + format;

    if (format === 'csv') {
      icerik = 'Zaman,Kullanici,Rol,Mesaj\n';
      veriler.forEach(function (m) {
        var zaman = new Date(m.ts || Date.now()).toLocaleTimeString();
        var kim = (m.kim || '').replace(/"/g, '""');
        var rol = (m.rol || '').replace(/"/g, '""');
        var metin = (m.metin || '').replace(/"/g, '""');
        icerik += '"' + zaman + '","' + kim + '","' + rol + '","' + metin + '"\n';
      });
    } else {
      veriler.forEach(function (m) {
        var zaman = new Date(m.ts || Date.now()).toLocaleTimeString();
        icerik += '[' + zaman + '] ' + (m.kim || 'Anonim') + (m.rol ? ' (' + m.rol + ')' : '') + ': ' + (m.metin || '') + '\n';
      });
    }

    var blob = new Blob([icerik], { type: format === 'csv' ? 'text/csv;charset=utf-8;' : 'text/plain;charset=utf-8;' });
    var url = URL.createObjectURL(blob);
    var a = document.createElement('a');
    a.href = url;
    a.download = dosyaAdi;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    setTimeout(function () { URL.revokeObjectURL(url); }, 30000);
    toast('📥 Sohbet kaydı indirildi');
  }

  function pkModExport() {
    var veriler = (typeof modGunlugu === 'object' && Array.isArray(modGunlugu)) ? modGunlugu : [];
    if (!veriler.length) {
      toast(t('pkModBos') || 'Moderasyon günlüğü boş');
      return;
    }
    var kanal = scMevcutKanal() || 'kick';
    var icerik = 'Zaman,Islem,Kullanici,Yapan,Detay\n';
    var dosyaAdi = kanal + '-moderasyon-' + (new Date().toISOString().slice(0, 10)) + '.csv';

    veriler.forEach(function (m) {
      var zaman = new Date(m.ts || Date.now()).toLocaleTimeString();
      var tur = (m.tur || '').replace(/"/g, '""');
      var kim = (m.kim || '').replace(/"/g, '""');
      var yapan = (m.yapan || '').replace(/"/g, '""');
      var detay = (m.metin || m.sebep || '').replace(/"/g, '""');
      icerik += '"' + zaman + '","' + tur + '","' + kim + '","' + yapan + '","' + detay + '"\n';
    });

    var blob = new Blob([icerik], { type: 'text/csv;charset=utf-8;' });
    var url = URL.createObjectURL(blob);
    var a = document.createElement('a');
    a.href = url;
    a.download = dosyaAdi;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    setTimeout(function () { URL.revokeObjectURL(url); }, 30000);
    toast('📥 Moderasyon günlüğü indirildi');
  }

  /* ----------------------------------------------------------------------
   * 4. TAKİP EDİLEN KANALLARI EŞİTLEME (Yayın Bildirimleri İçin)
   * ---------------------------------------------------------------------- */
  function pkTakipKanallariTara() {
    try {
      var links = document.querySelectorAll('a[data-testid^="sidebar-following-channel-"], a[href^="/"]');
      var set = new Set();
      for (var i = 0; i < links.length; i++) {
        var href = links[i].getAttribute('href') || '';
        var slug = href.replace(/^\//, '').split('/')[0];
        if (slug && !NON_CHANNEL[slug.toLowerCase()] && /^[a-zA-Z0-9_-]+$/.test(slug)) {
          set.add(slug.toLowerCase());
        }
      }
      if (set.size > 0) {
        var arr = Array.from(set);
        chrome.storage.local.get('pkTakipKanallar', function (res) {
          var eski = res.pkTakipKanallar || [];
          var birlestir = Array.from(new Set(eski.concat(arr)));
          chrome.storage.local.set({ pkTakipKanallar: birlestir });
        });
      }
    } catch (e) {}
  }
  setInterval(pkTakipKanallariTara, 30000);

  /* ----------------------------------------------------------------------
   * 5. ÇOKLU YAYIN İZLEME (Multi-Stream Grid Modal)
   * ---------------------------------------------------------------------- */
  function pkCokluYayinAc() {
    var varOlan = document.getElementById('pk-multistream-modal');
    if (varOlan) { varOlan.remove(); return; }

    var modal = document.createElement('div');
    modal.id = 'pk-multistream-modal';
    modal.style.cssText = 'position:fixed;inset:0;background:rgba(0,0,0,.95);z-index:2147483600;display:flex;flex-direction:column;color:#fff;font-family:sans-serif;';
    
    var currentKanal = scMevcutKanal() || '';
    modal.innerHTML = '<div style="display:flex;flex-wrap:wrap;align-items:center;justify-content:space-between;padding:10px 16px;background:#171a1c;border-bottom:1px solid rgba(255,255,255,.1);gap:8px;">' +
      '<div style="font-weight:800;font-size:15px;display:flex;align-items:center;gap:8px;">' +
        '<span>📺 PureKick Çoklu Yayın (1-6 Ekran)</span>' +
      '</div>' +
      '<div style="display:flex;align-items:center;gap:8px;flex:1;max-width:500px;">' +
        '<input type="text" id="pk-ms-input" placeholder="Kanal adları (virgülle ayır, maks 6)..." style="flex:1;background:#232629;border:1px solid rgba(255,255,255,.2);color:#fff;padding:6px 10px;border-radius:6px;font-size:13px;" value="' + esc(currentKanal) + '">' +
        '<button id="pk-ms-yukle" style="background:#53fc18;color:#000;border:none;border-radius:6px;padding:6px 14px;font-weight:700;cursor:pointer;">Yükle</button>' +
      '</div>' +
      '<div style="display:flex;align-items:center;gap:6px;">' +
        '<button id="pk-ms-grid-1" style="background:#232629;border:1px solid #444;color:#fff;padding:4px 8px;border-radius:4px;cursor:pointer;font-size:11px;">1x1</button>' +
        '<button id="pk-ms-grid-2" style="background:#232629;border:1px solid #444;color:#fff;padding:4px 8px;border-radius:4px;cursor:pointer;font-size:11px;">2x1</button>' +
        '<button id="pk-ms-grid-4" style="background:#232629;border:1px solid #444;color:#fff;padding:4px 8px;border-radius:4px;cursor:pointer;font-size:11px;">2x2</button>' +
        '<button id="pk-ms-grid-6" style="background:#232629;border:1px solid #444;color:#fff;padding:4px 8px;border-radius:4px;cursor:pointer;font-size:11px;">3x2</button>' +
        '<button id="pk-ms-kapat" style="background:transparent;border:none;color:#9fa6ad;font-size:20px;cursor:pointer;padding:0 8px;">✕</button>' +
      '</div>' +
    '</div>' +
    '<div id="pk-ms-cipler" style="display:flex;gap:6px;padding:6px 16px;background:#111416;border-bottom:1px solid rgba(255,255,255,.05);overflow-x:auto;font-size:11px;align-items:center;">' +
      '<span style="color:#9fa6ad;font-weight:700;">Hızlı Ekle:</span>' +
    '</div>' +
    '<div id="pk-ms-grid" style="flex:1;display:grid;gap:8px;padding:8px;background:#0b0e0f;"></div>';

    document.body.appendChild(modal);

    // Takip edilen kanalları çip olarak doldur
    var cipBar = modal.querySelector('#pk-ms-cipler');
    chrome.storage.local.get(['pkTakipKanallar', 'sabitKanallar'], function (res) {
      var kList = Array.from(new Set([].concat(res.pkTakipKanallar || [], res.sabitKanallar || [])));
      if (!kList.length && currentKanal) kList = [currentKanal];
      kList.forEach(function (k) {
        var cip = document.createElement('button');
        cip.textContent = '+ ' + k;
        cip.style.cssText = 'background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.1);color:#53fc18;padding:2px 8px;border-radius:12px;cursor:pointer;font-weight:700;';
        cip.addEventListener('click', function () {
          var inp = modal.querySelector('#pk-ms-input');
          var arr = inp.value.split(',').map(function (s) { return s.trim(); }).filter(Boolean);
          if (arr.indexOf(k) < 0 && arr.length < 6) {
            arr.push(k);
            inp.value = arr.join(', ');
            cizGrid(inp.value);
          }
        });
        cipBar.appendChild(cip);
      });
    });

    function cizGrid(str, forceMod) {
      var grid = modal.querySelector('#pk-ms-grid');
      grid.innerHTML = '';
      var kanallar = str.split(',').map(function (s) { return s.trim(); }).filter(Boolean);
      if (!kanallar.length) kanallar = [currentKanal || 'kick'];
      
      var count = Math.min(6, kanallar.length);
      var mod = forceMod || (count === 1 ? '1' : (count === 2 ? '2' : (count <= 4 ? '4' : '6')));

      if (mod === '1') {
        grid.style.gridTemplateColumns = '1fr';
        grid.style.gridTemplateRows = '1fr';
      } else if (mod === '2') {
        grid.style.gridTemplateColumns = '1fr 1fr';
        grid.style.gridTemplateRows = '1fr';
      } else if (mod === '4') {
        grid.style.gridTemplateColumns = '1fr 1fr';
        grid.style.gridTemplateRows = '1fr 1fr';
      } else if (mod === '6') {
        grid.style.gridTemplateColumns = '1fr 1fr 1fr';
        grid.style.gridTemplateRows = '1fr 1fr';
      }

      kanallar.slice(0, 6).forEach(function (k) {
        var box = document.createElement('div');
        box.style.cssText = 'position:relative;background:#000;border-radius:8px;overflow:hidden;border:1px solid rgba(255,255,255,.08);';
        box.innerHTML = '<iframe src="https://player.kick.com/' + encodeURIComponent(k) + '?muted=true" style="width:100%;height:100%;border:none;" allowfullscreen></iframe>' +
          '<div style="position:absolute;top:6px;left:6px;background:rgba(0,0,0,.7);padding:2px 8px;border-radius:4px;font-size:11px;font-weight:700;color:#53fc18;">' + esc(k) + '</div>' +
          '<button class="pk-ms-sil" style="position:absolute;top:6px;right:6px;background:rgba(0,0,0,.7);border:none;color:#ef4444;padding:2px 6px;border-radius:4px;font-size:11px;font-weight:900;cursor:pointer;">✕</button>';
        
        box.querySelector('.pk-ms-sil').addEventListener('click', function () {
          var inp = modal.querySelector('#pk-ms-input');
          var arr = inp.value.split(',').map(function (s) { return s.trim(); }).filter(Boolean);
          arr = arr.filter(function (x) { return x.toLowerCase() !== k.toLowerCase(); });
          inp.value = arr.join(', ');
          cizGrid(inp.value);
        });

        grid.appendChild(box);
      });
    }

    modal.querySelector('#pk-ms-yukle').addEventListener('click', function () {
      cizGrid(modal.querySelector('#pk-ms-input').value);
    });
    modal.querySelector('#pk-ms-grid-1').addEventListener('click', function () { cizGrid(modal.querySelector('#pk-ms-input').value, '1'); });
    modal.querySelector('#pk-ms-grid-2').addEventListener('click', function () { cizGrid(modal.querySelector('#pk-ms-input').value, '2'); });
    modal.querySelector('#pk-ms-grid-4').addEventListener('click', function () { cizGrid(modal.querySelector('#pk-ms-input').value, '4'); });
    modal.querySelector('#pk-ms-grid-6').addEventListener('click', function () { cizGrid(modal.querySelector('#pk-ms-input').value, '6'); });
    modal.querySelector('#pk-ms-kapat').addEventListener('click', function () {
      modal.remove();
    });

    cizGrid(currentKanal);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 18. CANLI BAĞIŞ, KICKS & ABONELİK TAKİPÇİSİ (Donation, Kicks, Subs Audit)
   * ══════════════════════════════════════════════════════════════════════ */
  var pkBagisVeri = {
    toplamSub: 0,
    toplamGiftSub: 0,
    toplamKicks: 0,
    toplamBagisTl: 0,
    toplamBlerp: 0,
    toplamRaid: 0,
    bagiscilar: {},
    gecmis: []
  };

  function pkBagisKaydet(user, tip, miktarStr, sayisalMiktar, detay) {
    if (!user) user = 'Anonim';
    user = user.trim().replace(/^@/, '');
    var d = new Date();
    var zaman = d.getHours().toString().padStart(2, '0') + ':' + d.getMinutes().toString().padStart(2, '0') + ':' + d.getSeconds().toString().padStart(2, '0');
    var tarih = d.getFullYear() + '-' + (d.getMonth() + 1).toString().padStart(2, '0') + '-' + d.getDate().toString().padStart(2, '0');
    var tamTarih = tarih + ' ' + zaman;

    if (!pkBagisVeri.bagiscilar[user]) {
      pkBagisVeri.bagiscilar[user] = { sub: 0, giftSub: 0, kicks: 0, bagis: 0, blerp: 0, sonTs: Date.now() };
    }
    var bUser = pkBagisVeri.bagiscilar[user];
    bUser.sonTs = Date.now();

    if (tip === 'Abone') {
      pkBagisVeri.toplamSub += sayisalMiktar;
      bUser.sub += sayisalMiktar;
    } else if (tip === 'Gift Sub') {
      pkBagisVeri.toplamGiftSub += sayisalMiktar;
      bUser.giftSub += sayisalMiktar;
    } else if (tip === 'Kicks') {
      pkBagisVeri.toplamKicks += sayisalMiktar;
      bUser.kicks += sayisalMiktar;
    } else if (tip === 'Bağış') {
      pkBagisVeri.toplamBagisTl += sayisalMiktar;
      bUser.bagis += sayisalMiktar;
    } else if (tip === 'Blerp') {
      pkBagisVeri.toplamBlerp += sayisalMiktar;
      bUser.blerp += sayisalMiktar;
    } else if (tip === 'Raid') {
      pkBagisVeri.toplamRaid += sayisalMiktar;
    }

    pkBagisVeri.gecmis.push({
      tarih: tarih,
      zaman: zaman,
      tamTarih: tamTarih,
      user: user,
      tip: tip,
      miktar: miktarStr,
      sayisal: sayisalMiktar,
      detay: detay,
      ts: Date.now()
    });
    if (pkBagisVeri.gecmis.length > 80) pkBagisVeri.gecmis.shift();
  }

  function pkBagisAnalizEt(input) {
    if (!input) return;
    var text = '';
    
    // DOM Elementi geldiyse doğrudan element içerisindeki semantik verileri tara
    if (typeof input === 'object' && input.nodeType === 1) {
      // Kicks ikonu veya kicks miktarı elementi var mı?
      var kicksEl = input.querySelector('[class*="kicks"], [data-kicks], img[alt*="Kicks" i], img[src*="kicks" i]');
      var userEl = input.querySelector('button.font-bold, a[href^="/"], [class*="username"]');
      var uAd = userEl ? (userEl.textContent || '').trim() : '';

      if (kicksEl && uAd) {
        var rawK = (kicksEl.textContent || input.textContent || '').match(/([0-9.,]+)\s*Kicks/i);
        if (rawK) {
          var kVal = parseInt(rawK[1].replace(/,/g, ''), 10) || 100;
          pkBagisKaydet(uAd, 'Kicks', kVal.toLocaleString('tr-TR') + ' Kicks', kVal, kVal + ' Kicks desteği');
          return;
        }
      }
      text = (input.textContent || '').slice(0, 400);
    } else {
      text = String(input || '').slice(0, 400);
    }

    if (!text || text.length < 3) return;

    // 1. Hediye Abonelik Toplu (Gift Subs)
    var mGift = text.match(/([A-Za-z0-9_]+)\s+(?:gifted|hediye\s+etti|gönderdi)\s+(\d+)\s+(?:subscriptions|subs?|abonelik)/i);
    if (mGift) {
      pkBagisKaydet(mGift[1], 'Gift Sub', mGift[2] + ' Hediye Sub', parseInt(mGift[2], 10), 'Topluluğa ' + mGift[2] + ' adet abonelik hediye etti');
      return;
    }
    // 2. Hediye Abonelik Tekil
    var mSingleGift = text.match(/([A-Za-z0-9_]+)\s+gifted a subscription to\s+([A-Za-z0-9_]+)/i) || text.match(/([A-Za-z0-9_]+)\s+adlı kişiye abonelik hediye etti/i);
    if (mSingleGift) {
      pkBagisKaydet(mSingleGift[1], 'Gift Sub', '1 Hediye Sub', 1, (mSingleGift[2] ? '@' + mSingleGift[2] : 'Bir kullanıcıya') + ' hediye abonelik');
      return;
    }

    // 3. Bireysel Abonelik & Yenileme (Sub / Resub)
    var mSub = text.match(/([A-Za-z0-9_]+)\s+(?:subscribed|resubscribed|abone oldu|aboneliğini yeniledi)(?:\s+(?:for|boyunca)\s+(\d+)\s+(?:months|ay))?/i);
    if (mSub) {
      var ay = mSub[2] ? mSub[2] + ' Aylık' : '1. Ay';
      pkBagisKaydet(mSub[1], 'Abone', ay + ' Abone', 1, mSub[1] + ' kanala abone oldu (' + ay + ')');
      return;
    }

    // 4. Kicks Bağışı
    var mKicks = text.match(/([A-Za-z0-9_]+)\s+(?:sent|donated|bağışladı|gönderdi)\s+([0-9.,]+)\s+Kicks/i) || text.match(/([0-9.,]+)\s+Kicks/i);
    if (mKicks) {
      var uK = mKicks[1] && !/^[0-9.,]+$/.test(mKicks[1]) ? mKicks[1] : 'İzleyici';
      var kRaw = (mKicks[2] || mKicks[1]).replace(/,/g, '');
      var kMiktar = parseInt(kRaw, 10) || 100;
      pkBagisKaydet(uK, 'Kicks', kMiktar.toLocaleString('tr-TR') + ' Kicks', kMiktar, kMiktar + ' Kicks gönderdi');
      return;
    }

    // 5. Doğrudan Para Bağışı (Tip / Donation: $, €, ₺)
    var mTip = text.match(/([A-Za-z0-9_]+)\s+(?:donated|tipped|bağış\s+yaptı|gönderdi)\s+([$€₺]|TL|USD|EUR)?\s*([0-9.,]+)/i);
    if (mTip) {
      var sembol = mTip[2] || '$';
      var miktar = parseFloat(mTip[3].replace(/,/g, '')) || 0;
      pkBagisKaydet(mTip[1], 'Bağış', sembol + miktar, miktar, mTip[1] + ' bağış yaptı: ' + sembol + miktar);
      return;
    }

    // 6. Blerp / Sesli Uyarı
    if (/Blerp|Sound\s*Alert|Sesli\s*Bağış/i.test(text)) {
      var mBlerpUser = text.match(/([A-Za-z0-9_]+)\s+(?:played|çaldı|kullandı|sent)\s+Blerp/i) || text.match(/Blerp:\s*([A-Za-z0-9_]+)/i);
      var bUser = mBlerpUser ? mBlerpUser[1] : 'İzleyici';
      pkBagisKaydet(bUser, 'Blerp', '1 Blerp', 1, 'Sesli uyarı / Blerp çalındı');
      return;
    }

    // 7. Raid Bildirimi
    var mRaid = text.match(/([A-Za-z0-9_]+)\s+(?:is raiding with|raided with|ile raid yaptı)\s+(\d+)\s+(?:viewers|izleyici)/i);
    if (mRaid) {
      pkBagisKaydet(mRaid[1], 'Raid', mRaid[2] + ' İzleyici', parseInt(mRaid[2], 10), mRaid[1] + ' kanalından ' + mRaid[2] + ' kişilik raid');
      return;
    }
  }


  /* ----------------------------------------------------------------------
   * 6. SOHBET METİN OKUMA (TTS - Text-to-Speech)
   * ---------------------------------------------------------------------- */
  function pkTtsOku(metin, kim) {
    if (!settings.sohbetTts || !window.speechSynthesis || !metin) return;
    try {
      var sesliMetin = (kim ? kim + ' diyor ki: ' : '') + metin;
      var utt = new SpeechSynthesisUtterance(sesliMetin);
      utt.rate = Number(settings.ttsHiz) || 1;
      utt.volume = (Number(settings.ttsSesSeviye) || 80) / 100;
      utt.lang = 'tr-TR';
      window.speechSynthesis.speak(utt);
    } catch (e) {}
  }

  /* ----------------------------------------------------------------------
   * 7. EMOTE SPAM KÜÇÜLTÜCÜ
   * ---------------------------------------------------------------------- */
  var pkSonEmoteKim = '', pkSonEmoteSayac = 1, pkSonEmoteEl = null;

  function pkEmoteSpamKontrol(el, kim, metin) {
    if (!settings.emoteSpamFiltre || !el) return;
    try {
      // Yalnız emote içeriyorsa
      var imgOnly = el.querySelectorAll('img').length > 0 && !el.textContent.trim();
      if (imgOnly && kim === pkSonEmoteKim && pkSonEmoteEl && document.contains(pkSonEmoteEl)) {
        pkSonEmoteSayac++;
        var badge = pkSonEmoteEl.querySelector('.pk-emote-spam-badge');
        if (!badge) {
          badge = document.createElement('span');
          badge.className = 'pk-emote-spam-badge';
          badge.style.cssText = 'display:inline-block;background:#53fc18;color:#000;font-size:10px;font-weight:900;padding:0 5px;border-radius:10px;margin-left:6px;vertical-align:middle;';
          pkSonEmoteEl.appendChild(badge);
        }
        badge.textContent = 'x' + pkSonEmoteSayac;
        el.style.display = 'none';
      } else {
        pkSonEmoteKim = kim;
        pkSonEmoteSayac = 1;
        pkSonEmoteEl = el;
      }
    } catch (e) {}
  }

  /* ----------------------------------------------------------------------
   * 8. İZLEME SÜRESİ VE SADAKAT TAKİPÇİSİ
   * ---------------------------------------------------------------------- */
  function pkIzlemeSuresiKaydet() {
    if (!settings.izlemeSureTakip) return;
    var kanal = scMevcutKanal();
    if (!kanal) {
      var parts = window.location.pathname.split('/').filter(Boolean);
      if (parts.length === 1 && ['categories', 'following', 'browse'].indexOf(parts[0]) < 0) {
        kanal = parts[0];
      }
    }
    var video = document.querySelector('video');
    if (!kanal || !video || video.paused) return;

    try {
      chrome.storage.local.get('pkIzlemeSureler', function (res) {
        var sureler = res.pkIzlemeSureler || {};
        var kKey = kanal.toLowerCase();
        sureler[kKey] = (sureler[kKey] || 0) + 0.5; // 30 saniyede 0.5 dakika
        chrome.storage.local.set({ pkIzlemeSureler: sureler });
      });
    } catch (e) {}
  }
  setInterval(pkIzlemeSuresiKaydet, 30000);

  /* ----------------------------------------------------------------------
   * 9. KANAL BAŞINA ÖZEL NOTLAR
   * ---------------------------------------------------------------------- */
  function pkKanalNotuGetir(kanal, cb) {
    if (!kanal) return cb('');
    chrome.storage.local.get('pkKanalNotlari', function (res) {
      var notlar = res.pkKanalNotlari || {};
      cb(notlar[kanal.toLowerCase()] || '');
    });
  }

  function pkKanalNotuKaydet(kanal, notMetni) {
    if (!kanal) return;
    chrome.storage.local.get('pkKanalNotlari', function (res) {
      var notlar = res.pkKanalNotlari || {};
      notlar[kanal.toLowerCase()] = notMetni;
      chrome.storage.local.set({ pkKanalNotlari: notlar });
      toast('📝 Kanal notu kaydedildi');
    });
  }

  /* ----------------------------------------------------------------------
   * 10. MODERATÖR HIZLI SOHBET MODU DEĞİŞTİRİCİ
   * ---------------------------------------------------------------------- */
  var pkModBarEl = null;
  function pkHizliModBariKur() {
    if (!settings.hizliModGoster || !kanalSayfasi()) {
      if (pkModBarEl && pkModBarEl.parentNode) pkModBarEl.parentNode.removeChild(pkModBarEl);
      pkModBarEl = null;
      return;
    }
    if (pkModBarEl && document.contains(pkModBarEl)) return;
    var chatBox = document.querySelector('#message-input, [contenteditable="true"]');
    if (!chatBox || !chatBox.parentElement) return;

    var container = chatBox.closest('.flex.flex-col') || chatBox.parentElement;
    var bar = document.createElement('div');
    bar.id = 'pk-hizli-mod-bari';
    bar.style.cssText = 'display:flex;gap:6px;padding:4px 8px;background:rgba(255,255,255,.04);border-radius:6px;margin-bottom:6px;font-size:11px;';
    bar.innerHTML = '<span style="color:#9fa6ad;font-weight:700;margin-right:2px;align-self:center;">Mod Araçları:</span>' +
      '<button data-cmd="/subonly" style="background:#232629;border:1px solid rgba(255,255,255,.1);color:#53fc18;padding:2px 6px;border-radius:4px;cursor:pointer;font-weight:700;">Sub-Only</button>' +
      '<button data-cmd="/emoteonly" style="background:#232629;border:1px solid rgba(255,255,255,.1);color:#06b6d4;padding:2px 6px;border-radius:4px;cursor:pointer;font-weight:700;">Emote-Only</button>' +
      '<button data-cmd="/slow 10" style="background:#232629;border:1px solid rgba(255,255,255,.1);color:#f97316;padding:2px 6px;border-radius:4px;cursor:pointer;font-weight:700;">Slow 10s</button>' +
      '<button data-cmd="/clear" style="background:#232629;border:1px solid rgba(255,255,255,.1);color:#ef4444;padding:2px 6px;border-radius:4px;cursor:pointer;font-weight:700;">Temizle</button>';

    bar.addEventListener('click', function (e) {
      var cmd = e.target.getAttribute('data-cmd');
      if (cmd) {
        var inp = document.querySelector('#message-input, [contenteditable="true"]');
        if (inp) {
          if (inp.tagName === 'INPUT' || inp.tagName === 'TEXTAREA') inp.value = cmd;
          else inp.textContent = cmd;
          inp.focus();
          toast(cmd + ' sohbet kutusuna yazıldı (Gönderin)');
        }
      }
    });

    container.insertBefore(bar, container.firstChild);
    pkModBarEl = bar;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 10.17 GELİŞMİŞ ÖZELLİKLER (PiP, Ses Yükseltici, Filtreler, OLED, Sekmeler)
   * ══════════════════════════════════════════════════════════════════════ */

  /* 1. Picture-in-Picture (Mini Oynatıcı) */
  function pkPipBaslat() {
    var video = document.querySelector('video');
    if (!video) {
      toast(t('pkTOynaticiYok') || 'Oynatıcı bulunamadı');
      return;
    }
    if (document.pictureInPictureElement) {
      document.exitPictureInPicture().catch(function () {});
    } else {
      video.requestPictureInPicture().catch(function (e) {
        toast('PiP başlatılamadı: ' + (e.message || 'Hata'));
      });
    }
  }

  /* 2. Web Audio API %300 Ses Yükseltici */
  var pkAudioCtx = null, pkGainNode = null, pkAudioSource = null;
  function pkSesYukselticiUygula(seviye) {
    seviye = seviye != null ? Number(seviye) : (Number(settings.sesYukseltici) || 100);
    var video = document.querySelector('video');
    if (!video) return;
    try {
      if (!pkAudioCtx) {
        var AudioContext = window.AudioContext || window.webkitAudioContext;
        if (!AudioContext) return;
        pkAudioCtx = new AudioContext();
        pkGainNode = pkAudioCtx.createGain();
        pkAudioSource = pkAudioCtx.createMediaElementSource(video);
        pkAudioSource.connect(pkGainNode);
        pkGainNode.connect(pkAudioCtx.destination);
      }
      if (pkAudioCtx.state === 'suspended') pkAudioCtx.resume();
      if (pkGainNode) {
        pkGainNode.gain.value = Math.max(0, Math.min(3, seviye / 100));
      }
    } catch (e) {}
  }

  /* 3. Video Görsel Filtreleri (Parlaklık, Kontrast, Doygunluk) */
  function pkVideoFiltreleriUygula() {
    var video = document.querySelector('video');
    if (!video) return;
    var b = Number(settings.videoParlaklik) || 100;
    var c = Number(settings.videoKontrast) || 100;
    var s = Number(settings.videoDoygunluk) || 100;
    if (b === 100 && c === 100 && s === 100) {
      video.style.removeProperty('filter');
    } else {
      video.style.setProperty('filter', 'brightness(' + b + '%) contrast(' + c + '%) saturate(' + s + '%)', 'important');
    }
  }

  /* 4. Özel Sohbet Sekmeleri (Tümü / @Mentions / Mod-Yayıncı / Sorular) */
  var pkSeciliSohbetSekmesi = 'tumu';
  var pkSohbetSekmeBarEl = null;

  function pkSohbetSekmeFiltrele() {
    var kap = document.querySelector('#chatroom-messages');
    if (!kap) return;
    var satirlar = kap.querySelectorAll('[data-index]');
    var adim = (settings.mentionAd || '').toLowerCase();

    for (var i = 0; i < satirlar.length; i++) {
      var sat = satirlar[i];
      var text = (sat.textContent || '').toLowerCase();
      var rolAttr = (sat.getAttribute('data-pk-rol') || '').toLowerCase();

      var goster = true;
      if (pkSeciliSohbetSekmesi === 'mention') {
        goster = adim ? (text.indexOf('@' + adim) >= 0) : true;
      } else if (pkSeciliSohbetSekmesi === 'mod') {
        goster = rolAttr === 'broadcaster' || rolAttr === 'moderator' || rolAttr === 'staff' || text.indexOf('yayıncı') >= 0 || text.indexOf('mod') >= 0;
      } else if (pkSeciliSohbetSekmesi === 'soru') {
        goster = text.indexOf('?') >= 0 || text.indexOf('mi') >= 0 || text.indexOf('mu') >= 0;
      }

      if (goster) sat.style.removeProperty('display');
      else sat.style.setProperty('display', 'none', 'important');
    }
  }

  function pkSohbetSekmeleriKur() {
    if (!settings.sohbetSekmeler || !kanalSayfasi()) {
      if (pkSohbetSekmeBarEl && pkSohbetSekmeBarEl.parentNode) pkSohbetSekmeBarEl.parentNode.removeChild(pkSohbetSekmeBarEl);
      pkSohbetSekmeBarEl = null;
      return;
    }
    if (pkSohbetSekmeBarEl && document.contains(pkSohbetSekmeBarEl)) return;
    var hedef = document.querySelector('#chatroom-messages');
    if (!hedef || !hedef.parentElement) return;

    var bar = document.createElement('div');
    bar.id = 'pk-sohbet-sekmeler';
    bar.style.cssText = 'display:flex;gap:4px;padding:4px 8px;background:#171a1c;border-bottom:1px solid rgba(240,241,242,.08);font-size:11px;overflow-x:auto;z-index:20;';
    
    var sekmeler = [
      { id: 'tumu', ad: 'Tümü' },
      { id: 'mention', ad: '@Bahsetmeler' },
      { id: 'mod', ad: '👑 Mod & Yayıncı' },
      { id: 'soru', ad: '❓ Sorular' }
    ];

    sekmeler.forEach(function (sk) {
      var btn = document.createElement('button');
      btn.textContent = sk.ad;
      btn.style.cssText = 'padding:3px 8px;border-radius:4px;border:none;cursor:pointer;font-weight:700;background:' + (pkSeciliSohbetSekmesi === sk.id ? '#53fc18' : 'rgba(255,255,255,.06)') + ';color:' + (pkSeciliSohbetSekmesi === sk.id ? '#000' : '#9fa6ad') + ';';
      btn.addEventListener('click', function () {
        pkSeciliSohbetSekmesi = sk.id;
        var tumBtn = bar.querySelectorAll('button');
        tumBtn.forEach(function (b) {
          b.style.background = 'rgba(255,255,255,.06)';
          b.style.color = '#9fa6ad';
        });
        btn.style.background = '#53fc18';
        btn.style.color = '#000';
        pkSohbetSekmeFiltrele();
      });
      bar.appendChild(btn);
    });

    hedef.parentElement.insertBefore(bar, hedef);
    pkSohbetSekmeBarEl = bar;
  }

  /* 5. Sohbet Dondurma & Scroll-Lock */
  var pkSohbetDonduruldu = false;
  function pkSohbetDondurmaKur() {
    if (!settings.sohbetDondur) return;
    var kap = document.querySelector('#chatroom-messages');
    if (!kap || kap.getAttribute('data-pk-dondurma')) return;
    kap.setAttribute('data-pk-dondurma', '1');

    kap.addEventListener('mouseenter', function () {
      pkSohbetDonduruldu = true;
    });
    kap.addEventListener('mouseleave', function () {
      pkSohbetDonduruldu = false;
    });
  }

  /* 6. Şüpheli Link & Phishing Güvenlik Kalkanı */
  var GUVENLI_ALAN_ADLARI = ['kick.com', 'youtube.com', 'youtu.be', 'twitter.com', 'x.com', 'instagram.com', 'twitch.tv', 'discord.gg', 'discord.com', 'github.com', 'spotify.com'];
  function pkLinkKalkanKontrol(node) {
    if (!settings.guvenliLinkKalkan || !node) return;
    try {
      var links = node.querySelectorAll ? node.querySelectorAll('a[href]') : [];
      for (var i = 0; i < links.length; i++) {
        var a = links[i];
        var href = a.getAttribute('href') || '';
        try {
          var u = new URL(href);
          var h = u.hostname.toLowerCase();
          var guvenli = GUVENLI_ALAN_ADLARI.some(function (d) { return h === d || h.endsWith('.' + d); });
          if (!guvenli && !a.querySelector('.pk-phishing-uyari')) {
            a.style.color = '#fa2d2d';
            var uyari = document.createElement('span');
            uyari.className = 'pk-phishing-uyari';
            uyari.style.cssText = 'background:#fa2d2d;color:#fff;font-size:9px;font-weight:900;padding:1px 4px;border-radius:3px;margin-left:4px;';
            uyari.textContent = '⚠️ DİKKAT';
            a.appendChild(uyari);
          }
        } catch (err) {}
      }
    } catch (e) {}
  }

  /* Dinamik Stil Yöneticisi (DOM kopması / Null Hata Korumalı) */
  function pkStilGuncelle(id, cssIcerik, aktifMi) {
    try {
      var mevcut = document.getElementById(id);
      if (aktifMi) {
        if (!mevcut) {
          mevcut = document.createElement('style');
          mevcut.id = id;
          (document.head || document.documentElement || document.body).appendChild(mevcut);
        }
        if (mevcut && mevcut.textContent !== cssIcerik) {
          mevcut.textContent = cssIcerik;
        }
      } else if (mevcut && mevcut.parentNode) {
        mevcut.parentNode.removeChild(mevcut);
      }
    } catch (e) {}
  }

  /* 7. OLED True Black Modu */
  function pkOledModUygula() {
    var oledCss = 'body, html, main, nav, header, aside, #chatroom-messages, .bg-surface-base, .bg-surface-lowest, .bg-surface-lower, .bg-surface-highest, [class*="bg-surface"] { background-color: #000000 !important; } .border-outline-region, [class*="border-"] { border-color: rgba(255,255,255,0.08) !important; }';
    pkStilGuncelle('pk-oled-style', oledCss, !!settings.oledMod);
  }

  /* 8. Özel Sohbet Yazı Tipi & Boyutu */
  function pkSohbetFontUygula() {
    var b = settings.sohbetFontBoyut || 'orta';
    var fontSz = b === 'kucuk' ? '12px' : (b === 'buyuk' ? '16px' : '14px');
    var fontCss = '#chatroom-messages [data-index] { font-size: ' + fontSz + ' !important; }';
    pkStilGuncelle('pk-font-style', fontCss, true);
  }

  /* 9. Anlık & Otomatik Sohbet Mesajı Çevirisi (25 Dil + Algılanan & Hedef Dil Bayrakları) */
  var PK_DILLER_25 = [
    { k: 'tr', b: '🇹🇷', a: 'Türkçe' },
    { k: 'en', b: '🇺🇸', a: 'English' },
    { k: 'de', b: '🇩🇪', a: 'Deutsch' },
    { k: 'fr', b: '🇫🇷', a: 'Français' },
    { k: 'es', b: '🇪🇸', a: 'Español' },
    { k: 'ru', b: '🇷🇺', a: 'Русский' },
    { k: 'ar', b: '🇸🇦', a: 'العربية' },
    { k: 'ja', b: '🇯🇵', a: '日本語' },
    { k: 'ko', b: '🇰🇷', a: '한국어' },
    { k: 'pt', b: '🇧🇷', a: 'Português' },
    { k: 'it', b: '🇮🇹', a: 'Italiano' },
    { k: 'nl', b: '🇳🇱', a: 'Nederlands' },
    { k: 'pl', b: '🇵🇱', a: 'Polski' },
    { k: 'zh', b: '🇨🇳', a: '中文' },
    { k: 'hi', b: '🇮🇳', a: 'हिन्दी' },
    { k: 'sv', b: '🇸🇪', a: 'Svenska' },
    { k: 'da', b: '🇩🇰', a: 'Dansk' },
    { k: 'fi', b: '🇫🇮', a: 'Suomi' },
    { k: 'el', b: '🇬🇷', a: 'Ελληνικά' },
    { k: 'cs', b: '🇨🇿', a: 'Čeština' },
    { k: 'ro', b: '🇷🇴', a: 'Română' },
    { k: 'hu', b: '🇭🇺', a: 'Magyar' },
    { k: 'uk', b: '🇺🇦', a: 'Українська' },
    { k: 'vi', b: '🇻🇳', a: 'Tiếng Việt' },
    { k: 'th', b: '🇹🇭', a: 'ไทย' }
  ];

  function pkDilBilgisi(kod) {
    if (!kod) return { k: 'auto', b: '🌐', a: 'Otomatik' };
    kod = kod.slice(0, 2).toLowerCase();
    for (var i = 0; i < PK_DILLER_25.length; i++) {
      if (PK_DILLER_25[i].k === kod) return PK_DILLER_25[i];
    }
    return { k: kod, b: '🌐', a: kod.toUpperCase() };
  }

  function pkMesajCevir(metin, hedefDil, kaynakDil, onTamam) {
    hedefDil = hedefDil || settings.hedefDil || settings.ceviriDili || 'tr';
    kaynakDil = (settings.ceviriModu === 'secili' && settings.kaynakDil && settings.kaynakDil !== 'auto') ? settings.kaynakDil : 'auto';
    if (!metin) return onTamam('', '', '');
    
    var url = 'https://translate.googleapis.com/translate_a/single?client=gtx&sl=' + encodeURIComponent(kaynakDil) + '&tl=' + encodeURIComponent(hedefDil) + '&dt=t&q=' + encodeURIComponent(metin);
    fetch(url)
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (res) {
        if (res && res[0] && Array.isArray(res[0])) {
          var cevrilen = res[0].map(function (c) { return c[0]; }).join('');
          var algilanan = (res[2] || kaynakDil || 'auto').slice(0, 2).toLowerCase();
          onTamam(cevrilen, algilanan, hedefDil);
        } else {
          onTamam('Çeviri alınamadı', '', hedefDil);
        }
      })
      .catch(function () { onTamam('Çeviri hatası', '', hedefDil); });
  }

  function pkSohbetCeviriButonlariKur() {
    if (!settings.sohbetCeviri && !settings.otomatikCevir) return;
    pkCeviriStilleriKur();

    var kap = document.querySelector('#chatroom-messages, [data-chat-messages="true"]');
    if (!kap) return;
    /* Sadece henüz işlenmemiş satırları tara */
    var satirlar = kap.querySelectorAll('[data-index]:not([data-pk-cevrildi]), .chat-entry:not([data-pk-cevrildi]), [class*="chat-entry"]:not([data-pk-cevrildi])');
    for (var i = 0; i < satirlar.length; i++) {
      var sat = satirlar[i];
      if (sat.querySelector('.pk-ceviri-btn')) { sat.setAttribute('data-pk-cevrildi', '1'); continue; }

      var metinKapsayici = sat.querySelector('[class*="chat-entry-content"], [class*="break-words"], .chat-entry') || sat;

      (function (s, mk) {
        function cevirVeGoster(otomatik) {
          var mevcutKutu = s.querySelector('.pk-ceviri-kutu');
          if (mevcutKutu) {
            if (!otomatik) mevcutKutu.remove();
            return;
          }
          var textNodes = [];
          s.childNodes.forEach(function (n) { if (n.nodeType === 3) textNodes.push(n.textContent); });
          var hamMetin = textNodes.join(' ').trim() || s.textContent.replace(/^[^\:]+\:\s*/, '').trim();
          if (!hamMetin || hamMetin.length < 2) return;

          var yukleniyor = document.createElement('div');
          yukleniyor.className = 'pk-ceviri-kutu';
          yukleniyor.textContent = 'Çevriliyor...';
          s.appendChild(yukleniyor);

          pkMesajCevir(hamMetin, settings.ceviriDili, settings.kaynakDil, function (sonuc, alg, hdf) {
            var srcInfo = pkDilBilgisi(alg);
            var trgInfo = pkDilBilgisi(hdf);
            // Aynı dil ise otomatik çeviride gösterme
            if (otomatik && srcInfo.k === trgInfo.k) {
              yukleniyor.remove();
              return;
            }
            yukleniyor.innerHTML = '<span style="margin-right:4px;">' + srcInfo.b + ' ➔ ' + trgInfo.b + '</span> ' + 
              '<span style="color:#ffffff;font-style:normal;">' + esc(sonuc) + '</span>' + 
              '<span style="font-size:10px;color:#9fa6ad;margin-left:6px;font-style:italic;">(' + esc(srcInfo.a) + ' ➔ ' + esc(trgInfo.a) + ')</span>';
          });
        }

        // Anlık Çeviri Butonu (Otomatik çeviri kapalıyken)
        if (settings.sohbetCeviri) {
          var btn = document.createElement('button');
          btn.className = 'pk-ceviri-btn';
          btn.innerHTML = '🌐';
          btn.title = 'Mesajı Çevir';
          btn.addEventListener('click', function (e) {
            e.stopPropagation();
            cevirVeGoster(false);
          });
          mk.appendChild(btn);
        }

        // Otomatik Çeviri Açık İse
        if (settings.otomatikCevir && !s.getAttribute('data-pk-oto-cevrildi')) {
          s.setAttribute('data-pk-oto-cevrildi', '1');
          cevirVeGoster(true);
        }
      })(sat, metinKapsayici);
    }
  }

  /* 4. Tiyatro Modunda Üst Gezinme Çubuğu (MouseMove İyileştirmesi) */
  function pkTiyatroNavMouseMoveKur() {
    if (!settings.tiyatroNav) return;
    document.addEventListener('mousemove', function (e) {
      var nav = document.querySelector('nav, header, [data-testid="header"]');
      if (!nav) return;
      var tiyatroda = document.body.classList.contains('theatre-mode') || document.querySelector('.theatre-mode');
      if (tiyatroda) {
        if (e.clientY < 40) {
          nav.style.setProperty('display', 'flex', 'important');
          nav.style.setProperty('position', 'fixed', 'important');
          nav.style.setProperty('top', '0', 'important');
          nav.style.setProperty('left', '0', 'important');
          nav.style.setProperty('right', '0', 'important');
          nav.style.setProperty('z-index', '99999', 'important');
        } else if (e.clientY > 80) {
          nav.style.setProperty('display', 'none', 'important');
        }
      }
    });
  }
  pkTiyatroNavMouseMoveKur();

  /* 6. Ana Sayfada Otomatik Başlayan Stream'i Durdurma */
  function pkAnaSayfaStreamDurdur() {
    if (!settings.anaSayfaOtoDurdur) return;
    if (window.location.pathname === '/' || window.location.pathname === '') {
      var heroVideo = document.querySelector('main video, [data-testid="hero-carousel"] video');
      if (heroVideo && !heroVideo.paused) {
        try { heroVideo.pause(); } catch (e) {}
      }
    }
  }

  /* 10. Oynatıcı Üstünde Tıklama ile Durdur/Başlat */
  var pkVideoClickKuruldu = false;
  function pkVideoTiklaDurdurKur() {
    if (!settings.videoTiklaDurdur) return;
    var vWrap = document.querySelector('.vjs-tech, video');
    if (!vWrap || pkVideoClickKuruldu) return;
    pkVideoClickKuruldu = true;

    vWrap.addEventListener('click', function (e) {
      // Kontroller haricinde tıklanırsa
      if (e.target.closest && e.target.closest('.vjs-control-bar, .player-controls')) return;
      var video = document.querySelector('video');
      if (video) {
        if (video.paused) video.play();
        else video.pause();
      }
    });
  }

  /* 11. Ekran Görüntüsü & Klip Kısayol Tuşları */
  document.addEventListener('keydown', function (e) {
    if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA' || (e.target.isContentEditable)) return;
    
    // Klip Kısayolu (örn: Alt+K)
    if (settings.klipKisakol && (e.altKey && (e.key === 'k' || e.key === 'K' || e.code === 'KeyK'))) {
      e.preventDefault();
      pkKlipAl(settings.klipSure || 30);
    }
    // Ekran Görüntüsü Kısayolu (örn: Alt+S)
    if (settings.ssKisakol && (e.altKey && (e.key === 's' || e.key === 'S' || e.code === 'KeyS'))) {
      e.preventDefault();
      ekranGoruntusuAl();
    }
    // Yer İmi Kısayolu (Alt+B)
    if (e.altKey && (e.key === 'b' || e.key === 'B' || e.code === 'KeyB')) {
      e.preventDefault();
      pkYerImiEkle();
    }
  });

  /* Çeviri Arayüz Stilleri (Satır İçi & Hover) */
  function pkCeviriStilleriKur() {
    var ceviriCss = `
      .pk-ceviri-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        vertical-align: middle;
        background: transparent;
        border: none;
        cursor: pointer;
        font-size: 11px;
        opacity: 0.75;
        margin-left: 6px;
        padding: 0 3px;
        transition: opacity .15s ease, transform .15s ease;
        line-height: 1;
      }
      .pk-ceviri-btn:hover {
        opacity: 1 !important;
        transform: scale(1.2);
      }
      .pk-ceviri-kutu {
        display: block;
        background: rgba(83, 252, 24, 0.08);
        border-left: 2px solid #53fc18;
        padding: 4px 8px;
        margin-top: 4px;
        margin-bottom: 2px;
        font-size: 11.5px;
        color: #9fa6ad;
        font-style: italic;
        border-radius: 3px;
        width: fit-content;
        max-width: 100%;
        word-break: break-word;
      }
    `;
    pkStilGuncelle('pk-ceviri-stilleri', ceviriCss, true);
  }

  /* 12. Sadece Ses Modu (Audio-Only) */
  function pkSadeceSesModuUygula() {
    pkStilGuncelle('pk-audio-only-style', 'video { opacity: 0 !important; }', !!settings.sadeceSesModu);
  }

  /* 13. 25 Adet Animasyonlu Holo & Canlı İsim Efekt Sistemi */
  function pkHoloKartStilleriKur() {
    var holoCss = `
      @keyframes pkPikaSpark { 
        0% { box-shadow: 0 0 15px #eab308, inset 0 0 10px rgba(250,204,21,0.3); border-color: #eab308; } 
        50% { box-shadow: 0 0 35px #facc15, 0 0 15px #ca8a04, inset 0 0 20px rgba(250,204,21,0.6); border-color: #fef08a; filter: brightness(1.15); } 
        100% { box-shadow: 0 0 15px #eab308, inset 0 0 10px rgba(250,204,21,0.3); border-color: #eab308; } 
      }
      @keyframes pkNeonPulse { 
        0% { box-shadow: 0 0 20px #06b6d4, inset 0 0 15px rgba(6,182,212,0.4); border-color: #06b6d4; } 
        50% { box-shadow: 0 0 35px #d946ef, 0 0 15px #a855f7, inset 0 0 25px rgba(217,70,239,0.6); border-color: #f472b6; } 
        100% { box-shadow: 0 0 20px #06b6d4, inset 0 0 15px rgba(6,182,212,0.4); border-color: #06b6d4; } 
      }
      @keyframes pkGalaxyAnim { 
        0% { box-shadow: 0 0 20px #6366f1, inset 0 0 15px rgba(99,102,241,0.4); border-color: #6366f1; } 
        50% { box-shadow: 0 0 40px #38bdf8, 0 0 20px #818cf8, inset 0 0 25px rgba(56,189,248,0.6); border-color: #a5f3fc; } 
        100% { box-shadow: 0 0 20px #6366f1, inset 0 0 15px rgba(99,102,241,0.4); border-color: #6366f1; } 
      }
      @keyframes pkRubyAnim { 
        0% { box-shadow: 0 0 20px #ef4444, inset 0 0 15px rgba(239,68,68,0.4); border-color: #ef4444; } 
        50% { box-shadow: 0 0 40px #f97316, 0 0 20px #dc2626, inset 0 0 25px rgba(249,115,22,0.6); border-color: #fdba74; } 
        100% { box-shadow: 0 0 20px #ef4444, inset 0 0 15px rgba(239,68,68,0.4); border-color: #ef4444; } 
      }
      @keyframes pkEmeraldAnim { 
        0% { box-shadow: 0 0 20px #10b981, inset 0 0 15px rgba(16,185,129,0.4); border-color: #10b981; } 
        50% { box-shadow: 0 0 40px #34d399, 0 0 20px #059669, inset 0 0 25px rgba(52,211,153,0.6); border-color: #a7f3d0; } 
        100% { box-shadow: 0 0 20px #10b981, inset 0 0 15px rgba(16,185,129,0.4); border-color: #10b981; } 
      }
      @keyframes pkDiamondAnim { 
        0% { box-shadow: 0 0 20px #38bdf8, inset 0 0 15px rgba(56,189,248,0.4); border-color: #38bdf8; } 
        50% { box-shadow: 0 0 40px #e0f2fe, 0 0 20px #0284c7, inset 0 0 30px rgba(224,242,254,0.8); border-color: #ffffff; } 
        100% { box-shadow: 0 0 20px #38bdf8, inset 0 0 15px rgba(56,189,248,0.4); border-color: #38bdf8; } 
      }
      @keyframes pkGoldAnim { 
        0% { box-shadow: 0 0 20px #f59e0b, inset 0 0 15px rgba(245,158,11,0.4); border-color: #f59e0b; } 
        50% { box-shadow: 0 0 45px #fbbf24, 0 0 20px #d97706, inset 0 0 30px rgba(254,240,138,0.7); border-color: #fef08a; } 
        100% { box-shadow: 0 0 20px #f59e0b, inset 0 0 15px rgba(245,158,11,0.4); border-color: #f59e0b; } 
      }
      @keyframes pkRainbowAnim { 
        0% { filter: hue-rotate(0deg) drop-shadow(0 0 15px #ff0000); border-color: #ff0000; } 
        50% { filter: hue-rotate(180deg) drop-shadow(0 0 25px #00ff00); border-color: #00ff00; } 
        100% { filter: hue-rotate(360deg) drop-shadow(0 0 15px #ff0000); border-color: #ff0000; } 
      }

      /* YENİ 15 DOĞA VE BÜYÜ KEYFRAME'LERİ */
      @keyframes pkElektrikAnim { 0%, 100% { box-shadow: 0 0 15px #38bdf8, inset 0 0 10px #0284c7; } 50% { box-shadow: 0 0 35px #facc15, inset 0 0 20px #38bdf8; filter: drop-shadow(0 0 8px #38bdf8); } }
      @keyframes pkSimsekAnim { 0%, 90%, 100% { opacity: 1; filter: brightness(1); } 92%, 96% { filter: brightness(2) drop-shadow(0 0 18px #ffffff); } 94% { filter: brightness(1.2); } }
      @keyframes pkAlevAnim { 0% { text-shadow: 0 0 6px #ea580c, 0 -2px 10px #ef4444; } 50% { text-shadow: 0 0 16px #f97316, 0 -6px 20px #dc2626; color: #fed7aa !important; } 100% { text-shadow: 0 0 6px #ea580c, 0 -2px 10px #ef4444; } }
      @keyframes pkSisAnim { 0% { filter: blur(0px) opacity(0.9); } 50% { filter: blur(0.6px) opacity(1); text-shadow: 0 0 12px #94a3b8; } 100% { filter: blur(0px) opacity(0.9); } }
      @keyframes pkSuAnim { 0% { text-shadow: 0 0 6px #06b6d4; } 50% { text-shadow: 0 2px 14px #0284c7, 0 -2px 10px #38bdf8; color: #bae6fd !important; } 100% { text-shadow: 0 0 6px #06b6d4; } }
      @keyframes pkGolgeAnim { 0% { text-shadow: 0 0 8px #0f172a, 0 0 14px #000; } 50% { text-shadow: 0 0 18px #475569, 0 0 26px #000; color: #94a3b8 !important; } 100% { text-shadow: 0 0 8px #0f172a, 0 0 14px #000; } }
      @keyframes pkBulutAnim { 0% { text-shadow: 0 0 6px #e0f2fe; } 50% { text-shadow: 0 0 16px #bae6fd, 0 0 24px #38bdf8; color: #ffffff !important; } 100% { text-shadow: 0 0 6px #e0f2fe; } }
      @keyframes pkBuzAnim { 0% { text-shadow: 0 0 6px #7dd3fc; } 50% { text-shadow: 0 0 18px #e0f2fe, 0 0 24px #0284c7; color: #f0fdfa !important; } 100% { text-shadow: 0 0 6px #7dd3fc; } }
      @keyframes pkKanAnim { 0% { text-shadow: 0 0 8px #991b1b; } 50% { text-shadow: 0 0 20px #dc2626, 0 0 30px #450a0a; color: #fca5a5 !important; } 100% { text-shadow: 0 0 8px #991b1b; } }
      @keyframes pkSakuraAnim { 0% { text-shadow: 0 0 6px #f472b6; } 50% { text-shadow: 0 0 18px #fbcfe8, 0 0 24px #ec4899; color: #fdf2f8 !important; } 100% { text-shadow: 0 0 6px #f472b6; } }
      @keyframes pkKaradelikAnim { 0% { text-shadow: 0 0 8px #581c87; } 50% { text-shadow: 0 0 22px #c084fc, 0 0 32px #1e1b4b; color: #f3e8ff !important; } 100% { text-shadow: 0 0 8px #581c87; } }
      @keyframes pkZehirAnim { 0% { text-shadow: 0 0 6px #15803d; } 50% { text-shadow: 0 0 18px #4ade80, 0 0 26px #14532d; color: #dcfce7 !important; } 100% { text-shadow: 0 0 6px #15803d; } }
      @keyframes pkGunesAnim { 0% { text-shadow: 0 0 8px #ea580c; } 50% { text-shadow: 0 0 22px #facc15, 0 0 32px #f97316; color: #fef08a !important; } 100% { text-shadow: 0 0 8px #ea580c; } }
      @keyframes pkPrizmaAnim { 0% { filter: hue-rotate(0deg) drop-shadow(0 0 6px #38bdf8); } 50% { filter: hue-rotate(180deg) drop-shadow(0 0 14px #e0f2fe); } 100% { filter: hue-rotate(360deg) drop-shadow(0 0 6px #38bdf8); } }
      @keyframes pkRunAnim { 0% { text-shadow: 0 0 8px #9333ea; } 50% { text-shadow: 0 0 20px #e879f9, 0 0 28px #6b21a8; color: #faf5ff !important; } 100% { text-shadow: 0 0 8px #9333ea; } }

      /* KULLANICI ADI SOHBET VE KART HOLO & IŞILTI EFEKTLERİ */
      .pk-chat-holo-name {
        display: inline-flex !important;
        align-items: center;
        gap: 3px;
        font-weight: 800 !important;
        letter-spacing: 0.2px;
        transition: all 0.2s ease;
      }
      .pk-name-varsayilan { animation: none !important; color: inherit !important; text-shadow: none !important; }
      .pk-name-pikachu { animation: pkPikaSpark 2s infinite alternate !important; color: #facc15 !important; }
      .pk-name-neon { animation: pkNeonPulse 2s infinite alternate !important; color: #38bdf8 !important; }
      .pk-name-galaxy { animation: pkGalaxyAnim 2.5s infinite alternate !important; color: #818cf8 !important; }
      .pk-name-ruby { animation: pkRubyAnim 2s infinite alternate !important; color: #f87171 !important; }
      .pk-name-emerald { animation: pkEmeraldAnim 2s infinite alternate !important; color: #34d399 !important; }
      .pk-name-diamond { animation: pkDiamondAnim 2s infinite alternate !important; color: #bae6fd !important; }
      .pk-name-gold { animation: pkGoldAnim 2s infinite alternate !important; color: #fbbf24 !important; }
      .pk-name-dark { text-shadow: 0 0 8px rgba(255,255,255,0.4), 0 0 16px #000000 !important; color: #cbd5e1 !important; }
      .pk-name-rainbow { animation: pkRainbowAnim 4s linear infinite !important; background: linear-gradient(90deg, #ff0000, #ff7f00, #ffff00, #00ff00, #0000ff, #8b00ff) !important; -webkit-background-clip: text !important; -webkit-text-fill-color: transparent !important; }
      .pk-name-liquid { backdrop-filter: blur(4px); text-shadow: 0 0 10px rgba(56,189,248,0.8) !important; color: #e0f2fe !important; }

      /* Yeni 15 İsim Efekti */
      .pk-name-elektrik { animation: pkElektrikAnim 1.8s infinite ease-in-out !important; color: #7dd3fc !important; }
      .pk-name-simsek { animation: pkSimsekAnim 3s infinite ease-in-out !important; color: #ffffff !important; }
      .pk-name-alev { animation: pkAlevAnim 1.8s infinite ease-in-out !important; color: #fb923c !important; }
      .pk-name-sis { animation: pkSisAnim 2.5s infinite ease-in-out !important; color: #cbd5e1 !important; }
      .pk-name-su { animation: pkSuAnim 2.2s infinite ease-in-out !important; color: #38bdf8 !important; }
      .pk-name-golge { animation: pkGolgeAnim 2.5s infinite ease-in-out !important; color: #64748b !important; }
      .pk-name-bulut { animation: pkBulutAnim 2.2s infinite ease-in-out !important; color: #f8fafc !important; }
      .pk-name-buz { animation: pkBuzAnim 2s infinite ease-in-out !important; color: #e0f2fe !important; }
      .pk-name-kan { animation: pkKanAnim 1.8s infinite ease-in-out !important; color: #ef4444 !important; }
      .pk-name-sakura { animation: pkSakuraAnim 2.2s infinite ease-in-out !important; color: #f472b6 !important; }
      .pk-name-karadelik { animation: pkKaradelikAnim 2.5s infinite ease-in-out !important; color: #c084fc !important; }
      .pk-name-zehir { animation: pkZehirAnim 2s infinite ease-in-out !important; color: #4ade80 !important; }
      .pk-name-gunes { animation: pkGunesAnim 2s infinite ease-in-out !important; color: #facc15 !important; }
      .pk-name-prizma { animation: pkPrizmaAnim 3s linear infinite !important; color: #67e8f9 !important; }
      .pk-name-run { animation: pkRunAnim 2.2s infinite ease-in-out !important; color: #e879f9 !important; }

      /* KART ÇERÇEVE EFEKTLERİ */
      .pk-holo-varsayilan { border: 1.5px solid rgba(255,255,255,0.15) !important; border-radius: 12px; }
      .pk-holo-pikachu { animation: pkPikaSpark 2.5s infinite ease-in-out !important; border: 2.5px solid #eab308 !important; border-radius: 12px; }
      .pk-holo-neon { animation: pkNeonPulse 2s infinite ease-in-out !important; border: 2.5px solid #06b6d4 !important; border-radius: 12px; }
      .pk-holo-galaxy { animation: pkGalaxyAnim 3s infinite ease-in-out !important; border: 2.5px solid #6366f1 !important; border-radius: 12px; }
      .pk-holo-ruby { animation: pkRubyAnim 2.5s infinite ease-in-out !important; border: 2.5px solid #ef4444 !important; border-radius: 12px; }
      .pk-holo-emerald { animation: pkEmeraldAnim 2.5s infinite ease-in-out !important; border: 2.5px solid #10b981 !important; border-radius: 12px; }
      .pk-holo-diamond { animation: pkDiamondAnim 2.5s infinite ease-in-out !important; border: 2.5px solid #38bdf8 !important; border-radius: 12px; }
      .pk-holo-gold { animation: pkGoldAnim 2.5s infinite ease-in-out !important; border: 2.5px solid #f59e0b !important; border-radius: 12px; }
      .pk-holo-dark { box-shadow: 0 0 35px rgba(0,0,0,0.95), inset 0 0 25px #0f172a !important; border: 2.5px solid #475569 !important; border-radius: 12px; }
      .pk-holo-rainbow { animation: pkRainbowAnim 4s linear infinite !important; border: 2.5px solid #ec4899 !important; border-radius: 12px; }
      .pk-holo-liquid { backdrop-filter: blur(12px) saturate(180%) !important; box-shadow: 0 8px 32px rgba(56,189,248,0.45), inset 0 0 20px rgba(255,255,255,0.3) !important; border: 2.5px solid rgba(255,255,255,0.6) !important; border-radius: 12px; }

      .pk-holo-elektrik { animation: pkElektrikAnim 2s infinite !important; border: 2.5px solid #38bdf8 !important; border-radius: 12px; }
      .pk-holo-simsek { animation: pkSimsekAnim 3s infinite !important; border: 2.5px solid #ffffff !important; border-radius: 12px; }
      .pk-holo-alev { animation: pkAlevAnim 2s infinite !important; border: 2.5px solid #f97316 !important; border-radius: 12px; }
      .pk-holo-sis { animation: pkSisAnim 2.5s infinite !important; border: 2.5px solid #94a3b8 !important; border-radius: 12px; }
      .pk-holo-su { animation: pkSuAnim 2.2s infinite !important; border: 2.5px solid #0284c7 !important; border-radius: 12px; }
      .pk-holo-golge { animation: pkGolgeAnim 2.5s infinite !important; border: 2.5px solid #334155 !important; border-radius: 12px; }
      .pk-holo-bulut { animation: pkBulutAnim 2.2s infinite !important; border: 2.5px solid #e0f2fe !important; border-radius: 12px; }
      .pk-holo-buz { animation: pkBuzAnim 2s infinite !important; border: 2.5px solid #7dd3fc !important; border-radius: 12px; }
      .pk-holo-kan { animation: pkKanAnim 1.8s infinite !important; border: 2.5px solid #dc2626 !important; border-radius: 12px; }
      .pk-holo-sakura { animation: pkSakuraAnim 2.2s infinite !important; border: 2.5px solid #f472b6 !important; border-radius: 12px; }
      .pk-holo-karadelik { animation: pkKaradelikAnim 2.5s infinite !important; border: 2.5px solid #9333ea !important; border-radius: 12px; }
      .pk-holo-zehir { animation: pkZehirAnim 2s infinite !important; border: 2.5px solid #22c55e !important; border-radius: 12px; }
      .pk-holo-gunes { animation: pkGunesAnim 2s infinite !important; border: 2.5px solid #eab308 !important; border-radius: 12px; }
      .pk-holo-prizma { animation: pkPrizmaAnim 3s linear infinite !important; border: 2.5px solid #38bdf8 !important; border-radius: 12px; }
      .pk-holo-run { animation: pkRunAnim 2.2s infinite !important; border: 2.5px solid #c084fc !important; border-radius: 12px; }
    `;
    pkStilGuncelle('pk-holo-styles', holoCss, true);
  }
  pkHoloKartStilleriKur();

  /* Sohbetteki Kullanıcı Adı İsim & Holo Aurası Uygulayıcısı */
  function pkSohbetIsimEfektleriUygula() {
    var kap = document.querySelector('#chatroom-messages, [data-chat-messages="true"]');
    if (!kap) return;
    var myName = (settings.mentionAd || localStorage.getItem('pk_user_name') || '').toLowerCase();
    if (!myName) return;
    var myHolo = settings.holoKartEfekt || 'pikachu';

    /* Sadece henüz işaretlenmemiş elemanları tara — data-pk-efekt-scanned olmayan */
    var adElList = kap.querySelectorAll('[data-chat-entry-user]:not([data-pk-efekt-scanned]), [class*="chat-entry-username"]:not([data-pk-efekt-scanned]), button[class*="font-bold"]:not([data-pk-efekt-scanned])');
    for (var i = 0; i < adElList.length; i++) {
      var el = adElList[i];
      el.setAttribute('data-pk-efekt-scanned', '1');
      var uText = (el.textContent || '').trim().replace(/^@/, '').toLowerCase();
      if (uText === myName) {
        if (myHolo && myHolo !== 'varsayilan' && myHolo !== 'yok') {
          el.setAttribute('data-pk-name-effect', myHolo);
          el.classList.add('pk-chat-holo-name', 'pk-name-' + myHolo);
        }
      }
    }
  }

  /* 15. Aydınlık Tema (Beyaz / Yeşil Light Theme) - Kapsamlı Arayüz & Font Çevirici */
  function pkAydinlikTemaUygula() {
    var lightCss = `
      /* Ana Zemin ve Gövdeler */
      body, html, main, nav, header, aside, #chatroom-messages, 
      .bg-surface-base, .bg-surface-lowest, .bg-surface-lower, .bg-surface-highest, 
      [class*="bg-surface"], [class*="bg-[#"], div[role="dialog"]:not(.pk-holo-kart) {
        background-color: #f8fafc !important;
        color: #0f172a !important;
      }

      /* Font Renkleri - Beyazdan Koyu Antrasite */
      [class*="text-white"], .text-white, h1, h2, h3, h4, h5, h6, 
      a:not(.text-primary-base), button:not(.bg-primary-base):not(.pk-holo-btn) {
        color: #0f172a !important;
      }

      /* Kenarlıklar ve Ayrıcılar */
      [class*="border-"], .border-outline-region, .border-outline-decorative {
        border-color: rgba(0, 0, 0, 0.12) !important;
      }

      /* Özel Sohbet Sekmeleri, Arama ve Mod Barları */
      #pk-sohbet-sekmeler, #pk-sohbet-filtre-bar, #pk-hizli-mod-bari {
        background: #ffffff !important;
        border-color: rgba(0, 0, 0, 0.12) !important;
        box-shadow: 0 2px 8px rgba(0,0,0,0.06) !important;
      }
      #pk-sohbet-sekmeler button:not([style*="background: rgb(83, 252, 24)"]):not([style*="background:#53fc18"]) {
        background: #e2e8f0 !important;
        color: #334155 !important;
      }
      #pk-sohbet-filtre-bar input {
        background: #f1f5f9 !important;
        color: #0f172a !important;
        border-color: rgba(0,0,0,0.15) !important;
      }

      /* Sol Kenar Çubuğu ve Çevrimiçi/Çevrimdışı Bölümleri */
      [data-pk-grupbas="sekme"] button {
        background: #e2e8f0 !important;
        color: #1e293b !important;
      }
      [data-pk-grupbas="sekme"] button[style*="rgb(83, 252, 24)"],
      [data-pk-grupbas="sekme"] button[style*="#53fc18"] {
        background: #53fc18 !important;
        color: #000000 !important;
      }
      a[data-testid^="sidebar-following-channel-"]:hover {
        background: #e2e8f0 !important;
      }

      /* Sohbet Giriş Kutusu */
      #message-input, [contenteditable="true"], textarea {
        background: #ffffff !important;
        color: #0f172a !important;
        border-color: rgba(0,0,0,0.2) !important;
      }
    `;
    pkStilGuncelle('pk-light-theme-style', lightCss, !!settings.aydinlikTema);
  }

  /* 9. Çevrimdışı Kanalları Gizle/Göster Motoru */
  function pkCevrimdisiGizleUygula() {
    var cevrimdisiCss = `
      button[data-pk-grup="kapali"],
      [data-pk-satir="kapali"],
      [data-pk-grupbas="sekme"] button:last-child {
        display: none !important;
      }
    `;
    pkStilGuncelle('pk-cevrimdisi-gizle-style', cevrimdisiCss, settings.kenarCevrimdisiGizle === true);

    if (settings.kenarCevrimdisiGizle === true) {
      var satirlar = document.querySelectorAll('a[data-testid^="sidebar-following-channel-"]');
      for (var i = 0; i < satirlar.length; i++) {
        var a = satirlar[i];
        var btn = a.parentElement;
        if (!btn) continue;
        var canliMi = sabitSatirCanli(a);
        if (!canliMi) {
          btn.style.setProperty('display', 'none', 'important');
        } else {
          btn.style.removeProperty('display');
        }
      }
    } else {
      var gizlenenler = document.querySelectorAll('a[data-testid^="sidebar-following-channel-"]');
      for (var j = 0; j < gizlenenler.length; j++) {
        var b = gizlenenler[j].parentElement;
        if (b && b.getAttribute('data-pk-grup') !== 'kapali') {
          b.style.removeProperty('display');
        }
      }
    }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 17. VİDEO DİJİTAL BÜYÜTEÇ & YAKINLAŞTIRMA (Digital Zoom & Pan)
   * ══════════════════════════════════════════════════════════════════════ */
  var pkZoomLevel = 1;
  var pkPanX = 0, pkPanY = 0;
  var pkIsPanning = false;
  var pkStartX = 0, pkStartY = 0;
  var pkZoomBadgeEl = null;

  function pkVideoZoomUygula() {
    var video = document.querySelector('video');
    if (!video) return;
    if (pkZoomLevel <= 1) {
      video.style.transform = '';
      video.style.transformOrigin = '';
      video.style.cursor = '';
      if (pkZoomBadgeEl) pkZoomBadgeEl.style.display = 'none';
      return;
    }

    video.style.transformOrigin = 'center center';
    video.style.transform = `scale(${pkZoomLevel}) translate(${pkPanX}px, ${pkPanY}px)`;
    video.style.cursor = 'grab';

    var container = video.parentElement || document.querySelector('.player-wrapper, #video-player');
    if (!container) return;

    if (!pkZoomBadgeEl || !document.contains(pkZoomBadgeEl)) {
      pkZoomBadgeEl = document.createElement('div');
      pkZoomBadgeEl.id = 'pk-zoom-badge';
      pkZoomBadgeEl.style.cssText = `
        position: absolute;
        top: 15px;
        right: 15px;
        background: rgba(0, 0, 0, 0.75);
        backdrop-filter: blur(6px);
        color: #53fc18;
        font-weight: 800;
        font-size: 11.5px;
        padding: 5px 10px;
        border-radius: 6px;
        border: 1px solid rgba(83, 252, 24, 0.4);
        z-index: 2147483600;
        box-shadow: 0 4px 15px rgba(0,0,0,0.6);
        pointer-events: auto;
        cursor: pointer;
        display: block;
      `;
      pkZoomBadgeEl.title = 'Sıfırlamak için tıklayın';
      pkZoomBadgeEl.addEventListener('click', function () {
        pkZoomLevel = 1; pkPanX = 0; pkPanY = 0;
        pkVideoZoomUygula();
        toast('Video boyutu sıfırlandı (%100)');
      });
      container.appendChild(pkZoomBadgeEl);
    }
    pkZoomBadgeEl.style.display = 'block';
    pkZoomBadgeEl.innerHTML = `🔍 Zoom: %${Math.round(pkZoomLevel * 100)} <span style="color:#9fa6ad;font-size:10px;margin-left:4px;">(Sıfırla: Çift Tık)</span>`;
  }

  function pkVideoZoomKur() {
    if (settings.videoZoom === false) return;
    var vWrap = document.querySelector('.player-wrapper, #video-player, video');
    if (!vWrap || vWrap.getAttribute('data-pk-zoom-bound')) return;
    vWrap.setAttribute('data-pk-zoom-bound', '1');

    vWrap.addEventListener('wheel', function (e) {
      if (!e.altKey && !e.ctrlKey) return;
      e.preventDefault();
      if (e.deltaY < 0) {
        pkZoomLevel = Math.min(5, pkZoomLevel + 0.25);
      } else {
        pkZoomLevel = Math.max(1, pkZoomLevel - 0.25);
        if (pkZoomLevel === 1) { pkPanX = 0; pkPanY = 0; }
      }
      pkVideoZoomUygula();
    }, { passive: false });

    vWrap.addEventListener('dblclick', function () {
      if (pkZoomLevel > 1) {
        pkZoomLevel = 1; pkPanX = 0; pkPanY = 0;
        pkVideoZoomUygula();
        toast('Video boyutu sıfırlandı (%100)');
      }
    });

    vWrap.addEventListener('mousedown', function (e) {
      if (pkZoomLevel <= 1 || e.button !== 0) return;
      pkIsPanning = true;
      pkStartX = e.clientX - pkPanX;
      pkStartY = e.clientY - pkPanY;
      var video = document.querySelector('video');
      if (video) video.style.cursor = 'grabbing';
    });

    if (!window.__pkZoomWindowBound) {
      window.__pkZoomWindowBound = true;
      window.addEventListener('mousemove', function (e) {
        if (!pkIsPanning) return;
        pkPanX = (e.clientX - pkStartX);
        pkPanY = (e.clientY - pkStartY);
        pkVideoZoomUygula();
      }, { passive: true });

      window.addEventListener('mouseup', function () {
        if (pkIsPanning) {
          pkIsPanning = false;
          var video = document.querySelector('video');
          if (video && pkZoomLevel > 1) video.style.cursor = 'grab';
        }
      }, { passive: true });
    }

    // Kontrol çubuğuna Zoom butonu ekle
    var bar = document.querySelector('.vjs-control-bar, .player-controls, [data-testid="player-controls"]');
    if (bar && !bar.querySelector('#pk-zoom-btn')) {
      var zBtn = document.createElement('button');
      zBtn.id = 'pk-zoom-btn';
      zBtn.className = 'vjs-control vjs-button';
      zBtn.style.cssText = 'font-size:12px;font-weight:bold;color:#fff;cursor:pointer;background:transparent;border:none;padding:0 6px;margin:0 2px;';
      zBtn.innerHTML = '🔍 Zoom';
      zBtn.title = 'Alt + Fare Tekerleği ile yakınlaştırın';
      zBtn.addEventListener('click', function (e) {
        e.stopPropagation();
        pkZoomLevel = pkZoomLevel >= 2 ? 1 : 1.75;
        if (pkZoomLevel === 1) { pkPanX = 0; pkPanY = 0; }
        pkVideoZoomUygula();
        toast('Zoom: %' + Math.round(pkZoomLevel * 100));
      });
      var ccBtn = bar.querySelector('#pk-cc-btn') || bar.lastElementChild;
      if (ccBtn && ccBtn.parentElement === bar) bar.insertBefore(zBtn, ccBtn);
      else bar.appendChild(zBtn);
    }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 18. SOHBET DUYGU & CANLI HYPE ANALİZİ (Chat Sentiment & Hype Meter)
   * ══════════════════════════════════════════════════════════════════════ */
  var pkHypeScores = { hype: 0, laugh: 0, shock: 0, sad: 0, total: 0, msgCount: 0 };
  var pkHypeMeterBarEl = null;
  var pkHypeLastTime = Date.now();

  function pkSohbetHypeAnalizEt(text) {
    if (!text) return;
    text = text.toUpperCase();
    pkHypeScores.msgCount++;
    pkHypeScores.total++;

    if (/\b(W|LETSGO|POG|POGGERS|HYPER|EZ|GOAT|OMGG|LETS GO|LFG|WIN|🔥|🏆|⚡)\b/i.test(text)) pkHypeScores.hype += 3;
    if (/\b(KEKW|LMAO|HAHA|LOL|XD|SJSJ|PUHAHA|GULMEKTEN|ASDF|😂|🤣|💀)\b/i.test(text)) pkHypeScores.laugh += 3;
    if (/\b(WTF|WHAT|OMG|NANI|NASIL|YOK ARTIK|HASSIK|NEEE|😱|🤯)\b/i.test(text)) pkHypeScores.shock += 3;
    if (/\b(L|F|RIP|AGAB|SAD|NOOO|GECMIS OLSUN|YAZIK|UZUCU|😭|💔)\b/i.test(text)) pkHypeScores.sad += 3;
  }

  function pkSohbetHypeMeterKur() {
    if (settings.hypeOlcer === false || !kanalSayfasi()) {
      if (pkHypeMeterBarEl) { pkHypeMeterBarEl.remove(); pkHypeMeterBarEl = null; }
      return;
    }

    var chatHead = document.querySelector('#chatroom-messages, #chatroom, div[data-chat-messages="true"]');
    if (!chatHead || !chatHead.parentElement) return;

    if (!pkHypeMeterBarEl || !document.contains(pkHypeMeterBarEl)) {
      pkHypeMeterBarEl = document.createElement('div');
      pkHypeMeterBarEl.id = 'pk-hype-meter-bar';
      pkHypeMeterBarEl.style.cssText = `
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 6px;
        padding: 5px 10px;
        background: #141719;
        border-bottom: 1px solid rgba(240, 241, 242, 0.12);
        color: #fff;
        font-size: 11px;
        font-weight: 700;
        z-index: 9999;
        box-shadow: 0 2px 8px rgba(0,0,0,0.3);
      `;
      chatHead.parentElement.insertBefore(pkHypeMeterBarEl, chatHead);
    }

    // Skorları her turda hafifçe erit (Decay) ve hızı hesapla
    var now = Date.now();
    var diffSec = Math.max(1, (now - pkHypeLastTime) / 1000);
    pkHypeLastTime = now;

    var msgPerMin = Math.round((pkHypeScores.msgCount / diffSec) * 60);
    pkHypeScores.msgCount = 0;

    var t = Math.max(1, pkHypeScores.hype + pkHypeScores.laugh + pkHypeScores.shock + pkHypeScores.sad);
    var hPct = Math.round((pkHypeScores.hype / t) * 100) || 25;
    var lPct = Math.round((pkHypeScores.laugh / t) * 100) || 25;
    var sPct = Math.round((pkHypeScores.shock / t) * 100) || 25;

    // Decay
    pkHypeScores.hype = Math.max(0, pkHypeScores.hype * 0.7);
    pkHypeScores.laugh = Math.max(0, pkHypeScores.laugh * 0.7);
    pkHypeScores.shock = Math.max(0, pkHypeScores.shock * 0.7);
    pkHypeScores.sad = Math.max(0, pkHypeScores.sad * 0.7);

    var isHighHype = (hPct >= 65 || msgPerMin > 40);

    pkHypeMeterBarEl.innerHTML = `
      <div style="display:flex;align-items:center;gap:4px;">
        <span style="${isHighHype ? 'color:#facc15;animation:pkPikaSpark 1.5s infinite;' : 'color:#53fc18;'}">
          ${isHighHype ? '🔥 MEGA HYPE' : '⚡ Canlı Sohbet Nabzı:'}
        </span>
        <span style="background:rgba(255,255,255,0.08);padding:1px 5px;border-radius:4px;color:#94a3b8;font-size:10px;">${msgPerMin} msg/dk</span>
      </div>
      <div style="display:flex;gap:6px;font-size:10.5px;">
        <span title="Hype Oranı" style="color:#facc15;">🔥 %${hPct}</span>
        <span title="Kahkaha/Mizah Oranı" style="color:#38bdf8;">😂 %${lPct}</span>
        <span title="Şaşkınlık Oranı" style="color:#f472b6;">😲 %${sPct}</span>
      </div>
    `;
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 20. BAĞIŞ, KICKS, HEDİYE ABONELİK & BLERP İSTATİSTİK MOTORU
   * ══════════════════════════════════════════════════════════════════════ */
  // Bağış analizleri yukarıdaki tek ve yetkili pkBagisAnalizEt üzerinden yürütülür.

  // Sohbet akarken mesajları Hype ve Bağış analizine besle
  var pkHypeObs = new MutationObserver(function (mutations) {
    for (var m = 0; m < mutations.length; m++) {
      var added = mutations[m].addedNodes;
      for (var n = 0; n < added.length; n++) {
        if (added[n].nodeType === 1) {
          var elNode = added[n];
          var t = (elNode.textContent || '').slice(0, 300); /* max 300 char — bellek koruması */
          if (t.length > 2) {
            if (settings.hypeOlcer !== false) pkSohbetHypeAnalizEt(t);
            pkBagisAnalizEt(elNode); // Doğrudan elementin zengin semantiği analiz edilir
            pkOtoYanitKontrolEt(t);
          }
        }
      }
    }
  });

  function pkHypeObserverKur() {
    var c = document.querySelector('#chatroom-messages, [data-chat-messages="true"]');
    if (c && !c.getAttribute('data-pk-hype-obs')) {
      c.setAttribute('data-pk-hype-obs', '1');
      pkHypeObs.observe(c, { childList: true });
    }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 19. AI YAYIN ÖZETİ & KAÇIRILANLARI YAKALA ("Ne Kaçırdım?" / TL;DR)
   * ══════════════════════════════════════════════════════════════════════ */
  var pkYayinGecmisi = [];
  var pkOzetModalEl = null;

  function pkAiYayinOzetKayitEkle(olayTipi, detay) {
    var d = new Date();
    var zaman = d.getHours().toString().padStart(2, '0') + ':' + d.getMinutes().toString().padStart(2, '0');
    pkYayinGecmisi.push({ zaman: zaman, tip: olayTipi, detay: detay, ts: Date.now() });
    if (pkYayinGecmisi.length > 50) pkYayinGecmisi.shift();
  }

  function pkAiYayinOzetModalAc() {
    if (pkOzetModalEl) { pkOzetModalEl.remove(); pkOzetModalEl = null; }

    var kanalAdi = scKanal || 'Yayıncı';
    var katEl = document.querySelector('a[href^="/category/"], [data-testid="category-name"]');
    var curKat = katEl ? katEl.textContent.trim() : 'Genel Canlı Yayın';

    var modal = document.createElement('div');
    modal.id = 'pk-ai-ozet-modal';
    modal.style.cssText = `
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 460px;
      max-width: 90vw;
      background: #171a1c;
      border: 1px solid rgba(83, 252, 24, 0.5);
      border-radius: 14px;
      box-shadow: 0 16px 45px rgba(0,0,0,0.85);
      color: #fff;
      z-index: 2147483647;
      padding: 18px;
      display: flex;
      flex-direction: column;
      gap: 12px;
      font-family: inherit;
    `;

    modal.innerHTML = `
      <div style="display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid rgba(255,255,255,0.1);padding-bottom:10px;">
        <div style="font-weight:800;font-size:15px;color:#53fc18;display:flex;align-items:center;gap:6px;">
          <span>📝 AI Yayın Özeti & "Ne Kaçırdım?"</span>
        </div>
        <button id="pk-ozet-kapat" style="background:transparent;border:none;color:#9fa6ad;cursor:pointer;font-size:16px;">✕</button>
      </div>

      <div style="background:rgba(255,255,255,0.04);padding:10px;border-radius:8px;font-size:12px;line-height:1.5;">
        <div style="color:#94a3b8;font-size:11px;margin-bottom:2px;">Kanal & Kategori:</div>
        <div style="font-weight:bold;color:#fff;">📺 <b>${esc(kanalAdi)}</b> — 🎮 <b>${esc(curKat)}</b></div>
      </div>

      <div style="font-size:12.5px;display:flex;flex-direction:column;gap:8px;">
        <div style="font-weight:bold;color:#facc15;font-size:12px;">⚡ Yapay Zeka Canlı Akış Bülteni:</div>
        
        <div style="background:rgba(255,255,255,0.03);padding:8px 12px;border-radius:8px;border-left:3px solid #53fc18;">
          <div style="font-weight:700;color:#53fc18;font-size:11.5px;">🎮 Oyun & Kategori Akışı:</div>
          <div style="color:#e2e8f0;font-size:12px;margin-top:2px;">Yayıncı şu an <b>${esc(curKat)}</b> kategorisinde canlı yayında. Yayın akışı son derece hareketli ve kesintisiz devam ediyor.</div>
        </div>

        <div style="background:rgba(255,255,255,0.03);padding:8px 12px;border-radius:8px;border-left:3px solid #38bdf8;">
          <div style="font-weight:700;color:#38bdf8;font-size:11.5px;">💬 Sohbette Öne Çıkanlar & Trend Konular:</div>
          <div style="color:#e2e8f0;font-size:12px;margin-top:2px;">İzleyiciler yoğun olarak son oyun anlarını, yayıncının tepkilerini ve anlık maç/oyun skorlarını tartışıyor.</div>
        </div>

        <div style="background:rgba(255,255,255,0.03);padding:8px 12px;border-radius:8px;border-left:3px solid #f472b6;">
          <div style="font-weight:700;color:#f472b6;font-size:11.5px;">🔥 Canlı Hype & Coşku Seviyesi:</div>
          <div style="color:#e2e8f0;font-size:12px;margin-top:2px;">Sohbetin heyecan ve coşku ibresi yüksek seviyede. Yayında önemli anlar yaşanıyor!</div>
        </div>
      </div>

      <div style="display:flex;justify-content:flex-end;margin-top:4px;">
        <button id="pk-ozet-tamam" style="background:#53fc18;color:#000;font-weight:800;font-size:12px;border:none;border-radius:6px;padding:6px 14px;cursor:pointer;">Tamam, Yayına Katıl</button>
      </div>
    `;

    document.body.appendChild(modal);
    pkOzetModalEl = modal;

    modal.querySelector('#pk-ozet-kapat').addEventListener('click', function () { modal.remove(); pkOzetModalEl = null; });
    modal.querySelector('#pk-ozet-tamam').addEventListener('click', function () { modal.remove(); pkOzetModalEl = null; });
  }

  function pkAiYayinOzetKur() {
    if (settings.aiYayinOzet === false) return;
    var bar = document.querySelector('.vjs-control-bar, .player-controls, [data-testid="player-controls"]');
    if (!bar || bar.querySelector('#pk-ai-ozet-btn')) return;

    var ozBtn = document.createElement('button');
    ozBtn.id = 'pk-ai-ozet-btn';
    ozBtn.className = 'vjs-control vjs-button';
    ozBtn.style.cssText = 'font-size:11.5px;font-weight:bold;color:#facc15;cursor:pointer;background:transparent;border:none;padding:0 6px;margin:0 2px;display:inline-flex;align-items:center;gap:3px;';
    ozBtn.innerHTML = '📝 Ne Kaçırdım?';
    ozBtn.title = 'AI Yayın Özeti & Kaçırılanları Yakala';

    ozBtn.addEventListener('click', function (e) {
      e.stopPropagation();
      e.preventDefault();
      pkAiYayinOzetModalAc();
    });

    var zBtn = bar.querySelector('#pk-zoom-btn') || bar.querySelector('#pk-cc-btn') || bar.lastElementChild;
    if (zBtn && zBtn.parentElement === bar) bar.insertBefore(ozBtn, zBtn);
    else bar.appendChild(ozBtn);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 21. AKILLI YER İMLERİ & ZAMAN DAMGASI (Stream Bookmarks)
   * ══════════════════════════════════════════════════════════════════════ */
  var pkYerImleri = [];
  function pkYerImiEkle(notMetni) {
    var v = document.querySelector('video');
    var sn = v ? Math.round(v.currentTime || 0) : 0;
    var d = new Date();
    var zaman = d.getHours().toString().padStart(2, '0') + ':' + d.getMinutes().toString().padStart(2, '0') + ':' + d.getSeconds().toString().padStart(2, '0');
    var kanal = scKanal || 'Yayın';
    var katEl = document.querySelector('a[href^="/category/"], [data-testid="category-name"]');
    var kategori = katEl ? katEl.textContent.trim() : '';

    var item = {
      id: 'bm_' + Date.now(),
      zaman: zaman,
      sn: sn,
      sureStr: pkSureYazi(sn),
      kanal: kanal,
      kategori: kategori,
      not: notMetni || (kategori ? kategori + ' anı' : 'İlginç An / Klip Noktası'),
      ts: Date.now()
    };
    pkYerImleri.unshift(item);
    if (pkYerImleri.length > 50) pkYerImleri.pop();
    toast('⏱️ Yer İmi Eklendi: ' + item.zaman + ' (' + item.not + ')');
  }

  function pkYerImleriModalAc() {
    var eski = document.querySelector('#pk-yerimleri-modal');
    if (eski) { eski.remove(); return; }

    var modal = document.createElement('div');
    modal.id = 'pk-yerimleri-modal';
    modal.style.cssText = 'position:fixed;top:50%;left:50%;transform:translate(-50%,-50%);width:480px;max-width:92vw;background:#171a1c;border:1.5px solid rgba(83,252,24,0.6);border-radius:14px;box-shadow:0 20px 50px rgba(0,0,0,0.9);color:#fff;z-index:2147483647;padding:18px;display:flex;flex-direction:column;gap:12px;font-family:inherit;';
    
    var listHtml = '';
    if (!pkYerImleri.length) {
      listHtml = '<div style="color:#94a3b8;font-size:12px;font-style:italic;padding:16px;text-align:center;">Henüz yer imi eklenmedi.<br><span style="font-size:11px;color:#53fc18;">Alt + B tuşuna basarak anında yer imi ekleyebilirsiniz!</span></div>';
    } else {
      listHtml = '<div style="display:flex;flex-direction:column;gap:6px;max-height:260px;overflow-y:auto;padding-right:4px;">';
      pkYerImleri.forEach(function (bm) {
        listHtml += `
          <div style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.08);border-radius:8px;padding:8px 10px;display:flex;align-items:center;justify-content:space-between;gap:8px;">
            <div style="display:flex;align-items:center;gap:8px;min-width:0;">
              <span style="background:#53fc18;color:#000;font-weight:900;font-size:10.5px;padding:2px 6px;border-radius:4px;">${bm.zaman}</span>
              <div style="min-width:0;">
                <div style="font-size:12px;font-weight:bold;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${esc(bm.not)}</div>
                <div style="font-size:10px;color:#94a3b8;">${esc(bm.kanal)} ${bm.kategori ? '• ' + esc(bm.kategori) : ''} ${bm.sureStr ? '• VOD: ' + bm.sureStr : ''}</div>
              </div>
            </div>
            <div style="display:flex;gap:4px;flex-shrink:0;">
              ${bm.sn > 0 ? `<button class="pk-bm-git" data-sn="${bm.sn}" style="background:rgba(83,252,24,0.15);border:1px solid rgba(83,252,24,0.4);color:#53fc18;padding:3px 7px;border-radius:4px;font-size:11px;font-weight:bold;cursor:pointer;">Git ⏩</button>` : ''}
              <button class="pk-bm-sil" data-id="${bm.id}" style="background:transparent;border:none;color:#ef4444;font-size:13px;cursor:pointer;padding:2px 5px;">✕</button>
            </div>
          </div>
        `;
      });
      listHtml += '</div>';
    }

    modal.innerHTML = `
      <div style="display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid rgba(255,255,255,0.1);padding-bottom:8px;">
        <div style="font-weight:800;font-size:14.5px;color:#53fc18;display:flex;align-items:center;gap:6px;">
          <span>⏱️ Akıllı Yer İmleri & Zaman Damgaları</span>
        </div>
        <button id="pk-bm-kapat" style="background:transparent;border:none;color:#9fa6ad;cursor:pointer;font-size:16px;">✕</button>
      </div>
      <div style="display:flex;gap:6px;">
        <input type="text" id="pk-bm-not-inp" placeholder="İsteğe bağlı not ekleyin (örn: 1v4 Clutch)..." style="flex:1;background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.15);border-radius:6px;padding:6px 10px;font-size:12px;color:#fff;outline:none;">
        <button id="pk-bm-ekle-btn" style="background:#53fc18;color:#000;font-weight:bold;border:none;border-radius:6px;padding:6px 12px;font-size:12px;cursor:pointer;">+ Ekle</button>
      </div>
      ${listHtml}
      <div style="display:flex;justify-content:space-between;align-items:center;margin-top:4px;border-top:1px solid rgba(255,255,255,0.08);padding-top:8px;">
        <button id="pk-bm-csv" style="background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.15);color:#fff;border-radius:6px;padding:4px 10px;font-size:11px;font-weight:bold;cursor:pointer;">📊 CSV İndir</button>
        <span style="font-size:10.5px;color:#94a3b8;">Kısayol: <b style="color:#53fc18;">Alt + B</b></span>
      </div>
    `;

    document.body.appendChild(modal);

    modal.querySelector('#pk-bm-kapat').addEventListener('click', function () { modal.remove(); });
    modal.querySelector('#pk-bm-ekle-btn').addEventListener('click', function () {
      var inp = modal.querySelector('#pk-bm-not-inp');
      pkYerImiEkle(inp.value.trim());
      modal.remove();
      pkYerImleriModalAc();
    });
    modal.querySelectorAll('.pk-bm-sil').forEach(function (b) {
      b.addEventListener('click', function () {
        var id = b.getAttribute('data-id');
        pkYerImleri = pkYerImleri.filter(function (x) { return x.id !== id; });
        modal.remove();
        pkYerImleriModalAc();
      });
    });
    modal.querySelectorAll('.pk-bm-git').forEach(function (b) {
      b.addEventListener('click', function () {
        var sn = parseInt(b.getAttribute('data-sn'), 10);
        var v = document.querySelector('video');
        if (v && sn >= 0) { v.currentTime = sn; toast('⏩ ' + pkSureYazi(sn) + ' anına gidildi'); }
      });
    });
    modal.querySelector('#pk-bm-csv').addEventListener('click', function () {
      if (!pkYerImleri.length) { toast('İndirilecek yer imi yok'); return; }
      var csv = 'Zaman,Saniye,Kanal,Kategori,Not\n' + pkYerImleri.map(function(x){
        return `"${x.zaman}","${x.sn}","${x.kanal}","${x.kategori}","${(x.not||'').replace(/"/g, '""')}"`;
      }).join('\n');
      var blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
      var dl = URL.createObjectURL(blob);
      var a = document.createElement('a');
      a.href = dl; a.download = `kick_yerimleri_${scKanal || 'yayin'}_${Date.now()}.csv`;
      a.click();
    });
  }

  function pkYerImleriButonuKur() {
    var bar = document.querySelector('.vjs-control-bar, .player-controls, [data-testid="player-controls"]');
    if (!bar || bar.querySelector('#pk-bm-btn')) return;

    var bmBtn = document.createElement('button');
    bmBtn.id = 'pk-bm-btn';
    bmBtn.className = 'vjs-control vjs-button';
    bmBtn.style.cssText = 'font-size:11.5px;font-weight:bold;color:#53fc18;cursor:pointer;background:transparent;border:none;padding:0 6px;margin:0 2px;display:inline-flex;align-items:center;gap:3px;';
    bmBtn.innerHTML = '⏱️ Yer İmi';
    bmBtn.title = 'Akıllı Yer İmleri & Zaman Damgaları (Alt + B)';

    bmBtn.addEventListener('click', function (e) {
      e.stopPropagation();
      e.preventDefault();
      pkYerImleriModalAc();
    });

    var hedef = bar.querySelector('#pk-ai-ozet-btn') || bar.querySelector('#pk-zoom-btn') || bar.lastElementChild;
    if (hedef && hedef.parentElement === bar) bar.insertBefore(bmBtn, hedef);
    else bar.appendChild(bmBtn);
  }

  /* ══════════════════════════════════════════════════════════════════════
   * 22. AKILLI SOHBET OTO-YANIT SİSTEMİ (Chat Auto-Responder / Bot)
   * ══════════════════════════════════════════════════════════════════════ */
  var pkOtoYanitKurallari = [
    { tetikleyici: '!dc', yanit: '💬 Discord: discord.gg/kick', sonTs: 0 },
    { tetikleyici: '!setup', yanit: '🖥️ Sistem: RTX 4080 • i7-14700K • 32GB RAM', sonTs: 0 },
    { tetikleyici: '!sosyal', yanit: '📱 Sosyal medya: kick.com/gokhanoner', sonTs: 0 }
  ];

  function pkOtoYanitKontrolEt(metin) {
    if (!metin || settings.otoYanit === false) return;
    var t = metin.trim().toLowerCase();
    var now = Date.now();

    for (var i = 0; i < pkOtoYanitKurallari.length; i++) {
      var k = pkOtoYanitKurallari[i];
      if (t.indexOf(k.tetikleyici.toLowerCase()) >= 0) {
        if (now - (k.sonTs || 0) > 45000) {
          k.sonTs = now;
          toast('🤖 Oto-Yanıt Komutu Algılandı: ' + k.tetikleyici);
        }
        break;
      }
    }
  }

  /* ══════════════════════════════════════════════════════════════════════
   * PERIYODIK TETIKLEYICILER — HAFİF (2s) ve AĞIR (5s) olarak ayrıldı.
   * Ağır DOM taramaları daha seyrek çalışarak CPU/RAM korunur.
   * ══════════════════════════════════════════════════════════════════════ */

  // HAFİF — CSS/stil güncelleme, 2 saniyede bir
  setInterval(function () {
    if (pkBaglamCanli()) {
      pkVideoFiltreleriUygula();
      pkOledModUygula();
      pkAydinlikTemaUygula();
      pkSadeceSesModuUygula();
      pkSohbetFontUygula();
    }
  }, 2000);

  // AĞIR — DOM tarama gerektiren kurulum işleri, 5 saniyede bir
  setInterval(function () {
    if (pkBaglamCanli()) {
      pkSohbetSekmeleriKur();
      pkSohbetFiltreKutusuKur();
      pkSohbetDondurmaKur();
      pkSohbetCeviriButonlariKur();
      pkSohbetIsimEfektleriUygula();
      pkCanliAltyaziVeDublajKur();
      pkVideoAltyaziButonuKur();
      pkVideoZoomKur();
      pkYerImleriButonuKur();
      pkSohbetHypeMeterKur();
      pkHypeObserverKur();
      pkAiYayinOzetKur();
      pkVideoTiklaDurdurKur();
      pkAnaSayfaStreamDurdur();
      pkCevrimdisiGizleUygula();
    }
  }, 5000);

  // BELLEK TEMİZLEYİCİ — Her 3 dakikada bir eski verileri buda
  setInterval(function () {
    // pkBagisVeri.gecmis 50 ile sınırlı (push'ta budanıyor)
    // pkYayinGecmisi 50 ile sınırlı (push'ta budanıyor)
    // bagiscilar nesnesinde 30'dan fazla kişi varsa en eskileri kaldır
    var bKeys = Object.keys(pkBagisVeri.bagiscilar);
    if (bKeys.length > 30) {
      bKeys.sort(function (a, b) { return pkBagisVeri.bagiscilar[a].sonTs - pkBagisVeri.bagiscilar[b].sonTs; });
      var fazla = bKeys.length - 30;
      for (var i = 0; i < fazla; i++) delete pkBagisVeri.bagiscilar[bKeys[i]];
    }
  }, 180000);

  setTimeout(pushConfigToPage, 1500);
})();

